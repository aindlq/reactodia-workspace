(function() {	try {		if (typeof document != "undefined") {			var elementStyle = document.createElement("style");			elementStyle.appendChild(document.createTextNode(".reactodia-badge{color:#fff;text-align:center;white-space:nowrap;vertical-align:middle;background-color:#777;border-radius:10px;min-width:10px;padding:3px 7px;font-size:12px;font-weight:700;line-height:1;display:inline-block}.reactodia-btn{font-size:14px;font-weight:400;line-height:var(--reactodia-line-height-base);text-transform:none;text-align:center;vertical-align:middle;white-space:nowrap;border:var(--reactodia-button-border-width) solid transparent;border-radius:var(--reactodia-button-border-radius);transition:background-color var(--reactodia-transition-duration);touch-action:manipulation;-webkit-user-select:none;user-select:none;cursor:pointer;background-image:none;margin-bottom:0;padding:6px 12px;display:inline-block}.reactodia-btn[disabled]{cursor:not-allowed;opacity:.65}.reactodia-btn-default{color:var(--reactodia-button-default-color);background-color:var(--reactodia-button-default-background-color);border-color:var(--reactodia-button-default-border-color)}.reactodia-btn-default:hover{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-focus)}.reactodia-btn-default:active,.reactodia-btn-default.active{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-active)}.reactodia-btn-default[disabled],.reactodia-btn-default[disabled]:hover,.reactodia-btn-default[disabled]:focus,.reactodia-btn-default[disabled]:active,.reactodia-btn-default[disabled].active{color:var(--reactodia-button-default-color);background-color:var(--reactodia-input-background-color-disabled)}.reactodia-btn-primary{color:var(--reactodia-color-emphasis-0);background-color:var(--reactodia-color-primary);border-color:var(--reactodia-color-primary-darker)}.reactodia-btn-primary:hover{border-color:var(--reactodia-color-primary-darkest);background-color:var(--reactodia-color-primary-dark)}.reactodia-btn-primary:active,.reactodia-btn-primary.active{border-color:var(--reactodia-color-primary-darker);background-color:var(--reactodia-color-primary-darkest)}.reactodia-btn-primary[disabled],.reactodia-btn-primary[disabled]:hover,.reactodia-btn-primary[disabled]:focus,.reactodia-btn-primary[disabled]:active,.reactodia-btn-primary[disabled].active{background-color:var(--reactodia-color-primary-dark)}.reactodia-btn-secondary{color:var(--reactodia-color-gray-900);background-color:var(--reactodia-color-secondary);border-color:var(--reactodia-color-secondary-darker)}.reactodia-btn-secondary:hover{border-color:var(--reactodia-color-secondary-darkest);background-color:var(--reactodia-color-secondary-dark)}.reactodia-btn-secondary:active,.reactodia-btn-secondary.active{border-color:var(--reactodia-color-secondary-darker);background-color:var(--reactodia-color-secondary-darkest)}.reactodia-btn-secondary[disabled],.reactodia-btn-secondary[disabled]:hover,.reactodia-btn-secondary[disabled]:focus,.reactodia-btn-secondary[disabled]:active,.reactodia-btn-secondary[disabled].active{background-color:var(--reactodia-color-secondary-dark)}.reactodia-btn-success{color:var(--reactodia-color-emphasis-0);background-color:var(--reactodia-color-success);border-color:var(--reactodia-color-success-darker)}.reactodia-btn-success:hover{border-color:var(--reactodia-color-success-darkest);background-color:var(--reactodia-color-success-dark)}.reactodia-btn-success:active,.reactodia-btn-success.active{border-color:var(--reactodia-color-success-darker);background-color:var(--reactodia-color-success-darkest)}.reactodia-btn-success[disabled],.reactodia-btn-success[disabled]:hover,.reactodia-btn-success[disabled]:focus,.reactodia-btn-success[disabled]:active,.reactodia-btn-success[disabled].active{background-color:var(--reactodia-color-success-dark)}.reactodia-btn-info{color:var(--reactodia-color-emphasis-0);background-color:var(--reactodia-color-info);border-color:var(--reactodia-color-info-darker)}.reactodia-btn-info:hover{border-color:var(--reactodia-color-info-darkest);background-color:var(--reactodia-color-info-dark)}.reactodia-btn-info:active,.reactodia-btn-info.active{border-color:var(--reactodia-color-info-darker);background-color:var(--reactodia-color-info-darkest)}.reactodia-btn-info[disabled],.reactodia-btn-info[disabled]:hover,.reactodia-btn-info[disabled]:focus,.reactodia-btn-info[disabled]:active,.reactodia-btn-info[disabled].active{background-color:var(--reactodia-color-info-dark)}.reactodia-btn-warning{color:var(--reactodia-color-emphasis-0);background-color:var(--reactodia-color-warning);border-color:var(--reactodia-color-warning-darker)}.reactodia-btn-warning:hover{border-color:var(--reactodia-color-warning-darkest);background-color:var(--reactodia-color-warning-dark)}.reactodia-btn-warning:active,.reactodia-btn-warning.active{border-color:var(--reactodia-color-warning-darker);background-color:var(--reactodia-color-warning-darkest)}.reactodia-btn-warning[disabled],.reactodia-btn-warning[disabled]:hover,.reactodia-btn-warning[disabled]:focus,.reactodia-btn-warning[disabled]:active,.reactodia-btn-warning[disabled].active{background-color:var(--reactodia-color-warning-dark)}.reactodia-btn-danger{color:var(--reactodia-color-emphasis-0);background-color:var(--reactodia-color-danger);border-color:var(--reactodia-color-danger-darker)}.reactodia-btn-danger:hover{border-color:var(--reactodia-color-danger-darkest);background-color:var(--reactodia-color-danger-dark)}.reactodia-btn-danger:active,.reactodia-btn-danger.active{border-color:var(--reactodia-color-danger-darker);background-color:var(--reactodia-color-danger-darkest)}.reactodia-btn-danger[disabled],.reactodia-btn-danger[disabled]:hover,.reactodia-btn-danger[disabled]:focus,.reactodia-btn-danger[disabled]:active,.reactodia-btn-danger[disabled].active{background-color:var(--reactodia-color-danger-dark)}.reactodia-btn-group{vertical-align:middle;display:inline-block;position:relative}.reactodia-btn-group>.reactodia-btn,.reactodia-btn-group>.reactodia-btn-group{float:left;position:relative}.reactodia-btn-group>.reactodia-btn:hover,.reactodia-btn-group>.reactodia-btn:focus,.reactodia-btn-group>.reactodia-btn.active,.reactodia-btn-group>.reactodia-btn.active:hover{z-index:2}.reactodia-btn-group .reactodia-btn+.reactodia-btn,.reactodia-btn-group .reactodia-btn+.reactodia-btn-group,.reactodia-btn-group .reactodia-btn-group+.reactodia-btn,.reactodia-btn-group .reactodia-btn-group+.reactodia-btn-group{margin-left:calc(-1 * var(--reactodia-button-border-width))}.reactodia-btn-group>.reactodia-btn{border-radius:unset}.reactodia-btn-group>.reactodia-btn:first-of-type{border-top-left-radius:var(--reactodia-button-border-radius);border-bottom-left-radius:var(--reactodia-button-border-radius);margin-left:0}.reactodia-btn-group>.reactodia-btn:last-of-type{border-top-right-radius:var(--reactodia-button-border-radius);border-bottom-right-radius:var(--reactodia-button-border-radius)}.reactodia-btn-group-sm>.reactodia-btn{padding:5px 10px;font-size:12px;line-height:1.5}.reactodia-btn-group-xs>.reactodia-btn{padding:1px 5px;font-size:12px;line-height:1.5}.reactodia-form{flex-direction:column;flex:auto;display:flex}.reactodia-form__body{flex-direction:column;flex:auto;padding:8px;display:flex;position:relative}.reactodia-form__controls{border-top:1px solid var(--reactodia-border-color-base);text-align:right;flex:none;padding:8px}.reactodia-form__controls button+button{margin-left:var(--reactodia-spacing-horizontal)}.reactodia-form__row{margin-bottom:8px;display:block}.reactodia-form__progress{position:absolute;bottom:0;left:0;right:0}.reactodia-form__control-row{padding-bottom:18px;position:relative}.reactodia-form__control-error{color:var(--reactodia-color-danger);position:absolute;bottom:0;left:0}.reactodia-form-control{width:100%;height:34px;font-family:var(--reactodia-font-family-base);line-height:var(--reactodia-line-height-base);color:var(--reactodia-input-color);background-color:var(--reactodia-input-background-color);border:var(--reactodia-input-border-width) solid var(--reactodia-input-border-color);border-radius:var(--reactodia-input-border-radius);margin:0;padding:6px 12px;font-size:14px;transition:border-color .15s ease-in-out;display:block}.reactodia-form-control:focus{border-color:var(--reactodia-input-border-color-focus);outline:0}.reactodia-form-control:disabled{background-color:var(--reactodia-input-background-color-disabled);cursor:not-allowed;opacity:.6}.reactodia-canvas-area{--reactodia-paper-panning-overlay-z-index:calc(var(--reactodia-z-index-base) + 70);background-color:var(--reactodia-canvas-background-color);flex:auto;width:0}.reactodia-canvas-area__widgets{position:absolute;top:0;left:0}.reactodia-canvas-area__watermark{cursor:pointer;opacity:.3;width:8%;min-width:50px;max-width:130px;transition:opacity var(--reactodia-transition-duration);background-size:cover;position:absolute;top:15px;right:25px}.reactodia-canvas-area__watermark:hover{opacity:.5}.reactodia-canvas-area--animated .reactodia-overlaid-element,.reactodia-canvas-area--animated .reactodia-element-decorations{transition:transform var(--reactodia-canvas-animation-duration) ease-in-out}.reactodia-canvas-area--animated .reactodia-link-layer,.reactodia-canvas-area--animated .reactodia-label-layer,.reactodia-canvas-area--animated .reactodia-canvas-area__widgets{opacity:0;transition:none}.reactodia-exported-watermark{opacity:.3;transition:opacity var(--reactodia-transition-duration)}.reactodia-exported-canvas{box-sizing:border-box;white-space:initial;color:var(--reactodia-font-color-base);font-family:var(--reactodia-font-family-base);font-size:var(--reactodia-font-size-base);line-height:var(--reactodia-line-height-base)}.reactodia-exported-canvas *,.reactodia-exported-canvas :before,.reactodia-exported-canvas :after{box-sizing:inherit}.reactodia-exported-layer{position:absolute}.reactodia-overlaid-element{cursor:move;outline:none}.reactodia-overlaid-element img{vertical-align:middle}.reactodia-overlaid-element--blurred{filter:grayscale();opacity:.5}.reactodia-overlaid-element--only-selected{touch-action:none}.reactodia-element-decorations{pointer-events:none}.reactodia-element-decorations>div>*{pointer-events:initial}.reactodia-link--blurred{opacity:.5}.reactodia-link--only-selected{touch-action:none}.reactodia-link-path__wrap{stroke-width:12px;stroke-linejoin:round;stroke-linecap:round;stroke:#0000;fill:none}.reactodia-link:hover .reactodia-link-path__wrap{stroke:var(--reactodia-color-emphasis-400);opacity:.44}.reactodia-link-vertices__vertex{cursor:all-scroll}.reactodia-link-vertices__vertex:not([fill]){fill:var(--reactodia-link-stroke-color)}.reactodia-link:not(:hover):not(.reactodia-link--only-selected) .reactodia-link-vertices__vertex{fill:#0000}.reactodia-link-vertices__handle{opacity:0;transition:opacity var(--reactodia-transition-duration);cursor:pointer}.reactodia-link-vertices__handle>circle{fill:var(--reactodia-color-emphasis-500)}.reactodia-link-vertices__handle>path{stroke:var(--reactodia-color-emphasis-200)}.reactodia-link-vertices__handle:hover>circle{fill:var(--reactodia-color-emphasis-900)}.reactodia-link:hover .reactodia-link-vertices__handle,.reactodia-link--only-selected .reactodia-link-vertices__handle{opacity:.8}.reactodia-link-marker__path:not([fill]){fill:var(--reactodia-link-stroke-color)}.reactodia-link-label{margin-top:calc(1.5em * var(--reactodia-link-label-line,0));border-radius:var(--reactodia-border-radius-s);color:var(--reactodia-font-color-base);background-color:var(--reactodia-canvas-background-color);white-space:nowrap;padding:0 2px}.reactodia-spinner__arrow{animation-name:reactodia-spinner-rotation;animation-duration:1.5s;animation-timing-function:cubic-bezier(.645,.045,.355,1);animation-iteration-count:infinite}.reactodia-spinner__text{color:var(--reactodia-font-color-base)}.reactodia-spinner[data-error=true] .reactodia-spinner__arrow{color:var(--reactodia-color-danger);animation-iteration-count:1}@keyframes reactodia-spinner-rotation{0%{transform:rotate(0)}to{transform:rotate(360deg)}}.reactodia-authoring-state__element-outlines{pointer-events:none;position:absolute;overflow:visible}.reactodia-authoring-state__outline-overlay{fill:var(--reactodia-color-emphasis-0);fill-opacity:.3}.reactodia-authoring-state__outline-stripe-line{stroke:var(--reactodia-color-emphasis-300);stroke-width:10px;stroke-opacity:.2}.reactodia-authoring-state__outline-cross-line{stroke:var(--reactodia-color-danger)}.reactodia-authoring-state__link-temporary,.reactodia-authoring-state__link-added,.reactodia-authoring-state__link-changed,.reactodia-authoring-state__link-deleted,.reactodia-authoring-state__link-uncertain{stroke-width:5px;stroke-opacity:.5;fill:none}.reactodia-authoring-state__link-temporary{stroke:var(--reactodia-color-gray-600);stroke-dasharray:8 8}.reactodia-authoring-state__link-added{stroke:var(--reactodia-color-success)}.reactodia-authoring-state__link-changed{stroke:var(--reactodia-color-primary)}.reactodia-authoring-state__link-deleted{stroke:var(--reactodia-color-danger)}.reactodia-authoring-state__link-uncertain{stroke:var(--reactodia-color-primary)}.reactodia-authoring-state__item-validation{cursor:help;align-self:flex-end;align-items:center;display:flex}.reactodia-authoring-state__item-validation-icon{width:17px;height:15px;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat}.reactodia-authoring-state--severity-info .reactodia-authoring-state__item-validation-icon{background:var(--reactodia-color-info);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.568%201.031A6.8%206.8%200%200%201%2012.76%203.05a7.06%207.06%200%200%201%20.46%209.39%206.85%206.85%200%200%201-8.58%201.74%207%207%200%200%201-3.12-3.5%207.12%207.12%200%200%201-.23-4.71%207%207%200%200%201%202.77-3.79%206.8%206.8%200%200%201%204.508-1.149zM9.04%2013.88a5.89%205.89%200%200%200%203.41-2.07%206.07%206.07%200%200%200-.4-8.06%205.82%205.82%200%200%200-7.43-.74%206.06%206.06%200%200%200%20.5%2010.29%205.81%205.81%200%200%200%203.92.58zM7.375%206h1.25V5h-1.25v1zm1.25%201v4h-1.25V7h1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.568%201.031A6.8%206.8%200%200%201%2012.76%203.05a7.06%207.06%200%200%201%20.46%209.39%206.85%206.85%200%200%201-8.58%201.74%207%207%200%200%201-3.12-3.5%207.12%207.12%200%200%201-.23-4.71%207%207%200%200%201%202.77-3.79%206.8%206.8%200%200%201%204.508-1.149zM9.04%2013.88a5.89%205.89%200%200%200%203.41-2.07%206.07%206.07%200%200%200-.4-8.06%205.82%205.82%200%200%200-7.43-.74%206.06%206.06%200%200%200%20.5%2010.29%205.81%205.81%200%200%200%203.92.58zM7.375%206h1.25V5h-1.25v1zm1.25%201v4h-1.25V7h1.25z'/%3e%3c/svg%3e\")}.reactodia-authoring-state--severity-warning .reactodia-authoring-state__item-validation-icon{background:var(--reactodia-color-warning);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\")}.reactodia-authoring-state--severity-error .reactodia-authoring-state__item-validation-icon{background:var(--reactodia-color-danger);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\")}.reactodia-authoring-state__state-label{font-weight:700}.reactodia-authoring-state__state-indicator{position:relative}.reactodia-authoring-state__state-indicator-body{white-space:nowrap;align-items:center;gap:var(--reactodia-spacing-horizontal);background:var(--reactodia-canvas-underlay-color);border-radius:var(--reactodia-border-radius-s);height:22px;padding:1px;display:flex;position:absolute;bottom:0}.reactodia-authoring-state__actions{gap:var(--reactodia-spacing-horizontal);display:flex}.reactodia-authoring-state__action{cursor:pointer;opacity:.5;transition:opacity var(--reactodia-transition-duration);background:0 0;border:0;padding:0}.reactodia-authoring-state__action:hover{opacity:1}.reactodia-authoring-state__action[disabled]{cursor:not-allowed;opacity:.2}.reactodia-authoring-state__action-edit{align-items:center;display:flex}.reactodia-authoring-state__action-edit:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-authoring-state__action-edit:not(:empty):before{margin-right:3px}.reactodia-authoring-state__action-delete{align-items:center;display:flex}.reactodia-authoring-state__action-delete:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-authoring-state__action-delete:not(:empty):before{margin-right:3px}.reactodia-authoring-state__action-discard{align-items:center;display:flex}.reactodia-authoring-state__action-discard:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-authoring-state__action-discard:not(:empty):before{margin-right:3px}.reactodia-authoring-state__decorator .reactodia-authoring-state__state-indicator{transition:opacity var(--reactodia-transition-duration)}.reactodia-authoring-state__decorator--optional .reactodia-authoring-state__state-indicator{opacity:0}.reactodia-overlaid-element:hover+.reactodia-element-decorations .reactodia-authoring-state__state-indicator,.reactodia-authoring-state__decorator--selected .reactodia-authoring-state__state-indicator,.reactodia-authoring-state__decorator:hover .reactodia-authoring-state__state-indicator{opacity:1}.reactodia-edit-entity-form--loading{justify-content:center;align-items:center}.reactodia-edit-entity-form label .reactodia-form-control+.reactodia-form-control{margin-top:var(--reactodia-spacing-vertical)}.reactodia-edit-relation-form--loading{justify-content:center;align-items:center}.reactodia-element-selector{flex-direction:column;display:flex}.reactodia-element-selector--search-existing{flex:auto}.reactodia-element-selector__search{flex-shrink:0;position:relative}.reactodia-element-selector__search-icon{content:\"\";opacity:.6;background-color:currentColor;width:16px;height:16px;margin-top:-7px;display:block;position:absolute;top:50%;left:10px;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-element-selector__search-input{padding-left:33px}.reactodia-element-selector__existing-elements-list{flex:1 1 0;margin-top:7px;padding-right:10px;overflow-y:scroll}.reactodia-element-selector__results-spinner{flex-direction:column;justify-content:center;align-items:center;height:100%;display:flex}.reactodia-element-selector__separator{text-align:center;margin:7px 0;overflow:hidden}.reactodia-element-selector__separator-text{color:var(--reactodia-border-color-base);font-size:13px;display:inline-block;position:relative}.reactodia-element-selector__separator-text:before,.reactodia-element-selector__separator-text:after{content:\"\";border-top:1px solid;width:500px;margin:0 10px;position:absolute;top:50%}.reactodia-element-selector__separator-text:before{left:100%}.reactodia-element-selector__separator-text:after{right:100%}.reactodia-viewport-overlay{background-color:var(--reactodia-canvas-overlay-color);justify-content:center;align-items:center;display:flex;position:absolute;inset:0}.reactodia-loading-widget{color:var(--reactodia-color-emphasis-1000);z-index:calc(var(--reactodia-z-index-base) + 30);margin:auto}.reactodia-viewport-dialog-overlay{z-index:calc(var(--reactodia-z-index-base) + 60)}.reactodia-fetch-status--loading{animation:2s linear infinite reactodia-loading-pulse}.reactodia-fetch-status--error:before{content:\"⚠ \";color:#ff000080}@keyframes reactodia-loading-pulse{0%{opacity:1;stroke-opacity:1}50%{opacity:.3;stroke-opacity:.3}to{opacity:1;stroke-opacity:1}}.reactodia-inline-diagnostic__icon{vertical-align:middle;width:16px;height:16px;margin-right:2px;display:inline-block}.reactodia-inline-diagnostic[data-reactodia-severity=info]{color:var(--reactodia-color-info)}.reactodia-inline-diagnostic[data-reactodia-severity=info] .reactodia-inline-diagnostic__icon{background:var(--reactodia-color-info);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.568%201.031A6.8%206.8%200%200%201%2012.76%203.05a7.06%207.06%200%200%201%20.46%209.39%206.85%206.85%200%200%201-8.58%201.74%207%207%200%200%201-3.12-3.5%207.12%207.12%200%200%201-.23-4.71%207%207%200%200%201%202.77-3.79%206.8%206.8%200%200%201%204.508-1.149zM9.04%2013.88a5.89%205.89%200%200%200%203.41-2.07%206.07%206.07%200%200%200-.4-8.06%205.82%205.82%200%200%200-7.43-.74%206.06%206.06%200%200%200%20.5%2010.29%205.81%205.81%200%200%200%203.92.58zM7.375%206h1.25V5h-1.25v1zm1.25%201v4h-1.25V7h1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.568%201.031A6.8%206.8%200%200%201%2012.76%203.05a7.06%207.06%200%200%201%20.46%209.39%206.85%206.85%200%200%201-8.58%201.74%207%207%200%200%201-3.12-3.5%207.12%207.12%200%200%201-.23-4.71%207%207%200%200%201%202.77-3.79%206.8%206.8%200%200%201%204.508-1.149zM9.04%2013.88a5.89%205.89%200%200%200%203.41-2.07%206.07%206.07%200%200%200-.4-8.06%205.82%205.82%200%200%200-7.43-.74%206.06%206.06%200%200%200%20.5%2010.29%205.81%205.81%200%200%200%203.92.58zM7.375%206h1.25V5h-1.25v1zm1.25%201v4h-1.25V7h1.25z'/%3e%3c/svg%3e\")}.reactodia-inline-diagnostic[data-reactodia-severity=warning]{color:var(--reactodia-color-warning)}.reactodia-inline-diagnostic[data-reactodia-severity=warning] .reactodia-inline-diagnostic__icon{background:var(--reactodia-color-warning);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\")}.reactodia-inline-diagnostic[data-reactodia-severity=error]{color:var(--reactodia-color-danger)}.reactodia-inline-diagnostic[data-reactodia-severity=error] .reactodia-inline-diagnostic__icon{background:var(--reactodia-color-danger);-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.56%201h.88l6.54%2012.26-.44.74H1.44L1%2013.26%207.56%201zM8%202.28L2.28%2013H13.7L8%202.28zM8.625%2012v-1h-1.25v1h1.25zm-1.25-2V6h1.25v4h-1.25z'/%3e%3c/svg%3e\")}.reactodia-property-input-file{border:2px dashed var(--reactodia-border-color-base);flex-direction:column;gap:4px;padding:6px;display:flex}.reactodia-property-input-file[data-reactodia-drag-state=accept]{background-color:var(--reactodia-color-success-contrast-background);border-color:var(--reactodia-color-success)}.reactodia-property-input-file[data-reactodia-drag-state=reject]{background-color:var(--reactodia-color-danger-contrast-background);border-color:var(--reactodia-color-danger)}.reactodia-property-input-file__input{display:none}.reactodia-property-input-file__spinner{flex:auto;justify-content:center;display:grid}.reactodia-property-input-file__placeholder{text-align:center;flex:auto}.reactodia-property-input-file__hint{color:var(--reactodia-color-emphasis-600);margin-bottom:4px}.reactodia-property-input-file__item{border:var(--reactodia-border-width-base) solid var(--reactodia-border-color-base);border-radius:var(--reactodia-border-radius-s);color:var(--reactodia-color-primary-contrast-foreground);gap:4px;padding:2px;display:flex}.reactodia-property-input-file__item-file{content:\"\";background-color:currentColor;flex:none;justify-content:center;align-items:center;width:16px;height:16px;display:grid;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.71%204.29l-3-3L10%201H4L3%202v12l1%201h9l1-1V5l-.29-.71zM13%2014H4V2h5v4h4v8zm-3-9V2l3%203h-3z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.71%204.29l-3-3L10%201H4L3%202v12l1%201h9l1-1V5l-.29-.71zM13%2014H4V2h5v4h4v8zm-3-9V2l3%203h-3z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-property-input-file__item-image,.reactodia-property-input-file__item-file{background-color:var(--reactodia-color-primary-contrast-background);width:60px;height:60px}.reactodia-property-input-file__item-properties{white-space:nowrap;flex-direction:column;flex:auto;justify-content:center;display:flex;overflow:hidden}.reactodia-property-input-file__item-label{font-weight:700}.reactodia-property-input-file__item-label,.reactodia-property-input-file__item-property{text-overflow:ellipsis;overflow:hidden}.reactodia-property-input-file__item-remove[class]{align-items:center;display:flex}.reactodia-property-input-file__item-remove[class]:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-property-input-file__item-remove[class]:not(:empty):before{margin-right:3px}.reactodia-property-input-file__item-remove[class]{border:none;align-self:stretch;padding:6px 4px}.reactodia-property-input-list__row{gap:var(--reactodia-spacing-horizontal);display:flex}.reactodia-property-input-list__row+.reactodia-property-input-list__row{margin-top:var(--reactodia-spacing-vertical)}.reactodia-property-input-list__value-remove{align-items:center;display:flex}.reactodia-property-input-list__value-remove:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-property-input-list__value-remove:not(:empty):before{margin-right:3px}.reactodia-property-input-list__value-remove{border:none;align-self:flex-start;height:34px;padding:6px 4px}.reactodia-property-input-list__value-add{align-items:center;display:flex}.reactodia-property-input-list__value-add:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M14%207v1H8v6H7V8H1V7h6V1h1v6h6z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M14%207v1H8v6H7V8H1V7h6V1h1v6h6z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-property-input-list__value-add:not(:empty):before{margin-right:3px}.reactodia-property-input-list__value-add{padding:6px}.reactodia-property-input-text__language{width:unset;padding:0 2px}textarea.reactodia-property-input-text{resize:vertical;height:4em}.reactodia-paper{cursor:default;display:flex;position:relative}.reactodia-paper__pane{touch-action:none;will-change:scroll-position;flex:auto;overflow:scroll}.reactodia-paper--pan-on-touch .reactodia-paper__pane{touch-action:pan-x pan-y}.reactodia-paper--hide-scrollbars .reactodia-paper__pane{scrollbar-width:none}.reactodia-paper--hide-scrollbars .reactodia-paper__pane::-webkit-scrollbar{display:none}.reactodia-paper__layers{position:relative}.reactodia-paper--panning{cursor:grabbing}.reactodia-paper--panning:before{content:\" \";width:100%;height:100%;z-index:var(--reactodia-paper-panning-overlay-z-index,1);display:block;position:fixed}.reactodia-workspace-dark{--lightningcss-light: ;--lightningcss-dark:initial;color-scheme:dark;--reactodia-color-gray-0:#fff;--reactodia-color-gray-100:#f5f6f7;--reactodia-color-gray-200:#ebedf0;--reactodia-color-gray-300:#dadde1;--reactodia-color-gray-400:#ccd0d5;--reactodia-color-gray-500:#bec3c9;--reactodia-color-gray-600:#8d949e;--reactodia-color-gray-700:#606770;--reactodia-color-gray-800:#444950;--reactodia-color-gray-900:#1c1e21;--reactodia-color-gray-1000:#000;--reactodia-color-primary:#337ab7;--reactodia-color-primary-dark:#306cce;--reactodia-color-primary-darker:#2d66c3;--reactodia-color-primary-darkest:#2554a0;--reactodia-color-primary-light:#538ce9;--reactodia-color-primary-lighter:#72a1ed;--reactodia-color-primary-lightest:#9abcf2;--reactodia-color-secondary:#ebedf0;--reactodia-color-secondary-dark:#d4d5d8;--reactodia-color-secondary-darker:#c8c9cc;--reactodia-color-secondary-darkest:#a4a6a8;--reactodia-color-secondary-light:#eef0f2;--reactodia-color-secondary-lighter:#f1f2f5;--reactodia-color-secondary-lightest:#f5f6f8;--reactodia-color-success:#5cb85c;--reactodia-color-success-dark:#009400;--reactodia-color-success-darker:#008b00;--reactodia-color-success-darkest:#007300;--reactodia-color-success-light:#26b226;--reactodia-color-success-lighter:#4dbf4d;--reactodia-color-success-lightest:#80d280;--reactodia-color-info:#54c7ec;--reactodia-color-info-dark:#4cb3d4;--reactodia-color-info-darker:#47a9c9;--reactodia-color-info-darkest:#3b8ba5;--reactodia-color-info-light:#6ecfef;--reactodia-color-info-lighter:#87d8f2;--reactodia-color-info-lightest:#aae3f6;--reactodia-color-warning:#ffba00;--reactodia-color-warning-dark:#e6a700;--reactodia-color-warning-darker:#d99e00;--reactodia-color-warning-darkest:#b38200;--reactodia-color-warning-light:#ffc426;--reactodia-color-warning-lighter:#ffcf4d;--reactodia-color-warning-lightest:#ffdd80;--reactodia-color-danger:#c9302c;--reactodia-color-danger-dark:#e13238;--reactodia-color-danger-darker:#d53035;--reactodia-color-danger-darkest:#af272b;--reactodia-color-danger-light:#fb565b;--reactodia-color-danger-lighter:#fb7478;--reactodia-color-danger-lightest:#fd9c9f;--reactodia-font-family-base:\"Helvetica Neue\", \"Helvetica\", \"Arial\", sans-serif;--reactodia-font-family-monospace:\"Menlo\", \"Monaco\", \"Consolas\", \"Liberation Mono\", \"Courier New\", monospace;--reactodia-font-size-base:87.5%;--reactodia-line-height-base:1.42857;--reactodia-font-color-base:var(--reactodia-color-content);--reactodia-font-color-base-inverse:var(--reactodia-color-content-inverse);--reactodia-spacing-base:5px;--reactodia-spacing-vertical:var(--reactodia-spacing-base);--reactodia-spacing-horizontal:var(--reactodia-spacing-base);--reactodia-z-index-base:0;--reactodia-border-radius-base:4px;--reactodia-border-radius-s:2px;--reactodia-border-width-base:1px;--reactodia-transition-duration:.3s;--reactodia-input-border-color:var(--reactodia-border-color-base);--reactodia-input-border-color-focus:var(--reactodia-color-primary-lighter);--reactodia-input-border-radius:var(--reactodia-border-radius-base);--reactodia-input-border-width:var(--reactodia-border-width-base);--reactodia-button-border-radius:var(--reactodia-border-radius-base);--reactodia-button-border-width:var(--reactodia-border-width-base);--reactodia-button-default-border-color:var(--reactodia-border-color-base);--reactodia-draggable-handle-corner-width:10px;--reactodia-draggable-handle-side-width:5px;--reactodia-tree-background-color-active:#65a6ff4d;--reactodia-tree-background-color-focus:#bdd9ff4d;--reactodia-tree-border-color-active:#73d3ff80;--reactodia-tree-border-color-focus:#73d3ff4d;--reactodia-dialog-border-color:var(--reactodia-border-color-base);--reactodia-dialog-border-radius:var(--reactodia-border-radius-base);--reactodia-dialog-border-width:var(--reactodia-border-width-base);--reactodia-dialog-viewport-breakpoint-s:600px;--reactodia-canvas-background-color:var(--reactodia-background-color);--reactodia-viewport-dock-margin:10px;--reactodia-selection-link-color:#d8956d;--reactodia-selection-single-box-margin:5px;--reactodia-selection-single-box-color:#d8956d;--reactodia-selection-single-box-shadow:0 0 5px 0 var(--reactodia-selection-single-box-color) inset;--reactodia-navigator-scrollable-pane-fill:var(--reactodia-navigator-background-fill);--reactodia-navigator-viewport-fill:var(--reactodia-canvas-background-color);--reactodia-navigator-viewport-stroke-width:2;--reactodia-navigator-overflow-stroke-color:var(--reactodia-navigator-viewport-stroke-color);--reactodia-navigator-overflow-stroke-width:2;--reactodia-navigator-overflow-stroke-dash:5 5;--reactodia-toolbar-height:30px;--reactodia-item-min-width:150px;--reactodia-accordion-transition-duration:var(--reactodia-transition-duration);--reactodia-color-emphasis-0:var(--reactodia-color-gray-1000);--reactodia-color-emphasis-100:var(--reactodia-color-gray-900);--reactodia-color-emphasis-200:var(--reactodia-color-gray-800);--reactodia-color-emphasis-300:var(--reactodia-color-gray-700);--reactodia-color-emphasis-400:var(--reactodia-color-gray-600);--reactodia-color-emphasis-500:var(--reactodia-color-gray-500);--reactodia-color-emphasis-600:var(--reactodia-color-gray-400);--reactodia-color-emphasis-700:var(--reactodia-color-gray-300);--reactodia-color-emphasis-800:var(--reactodia-color-gray-200);--reactodia-color-emphasis-900:var(--reactodia-color-gray-100);--reactodia-color-emphasis-1000:var(--reactodia-color-gray-0);--reactodia-color-primary-contrast-background:#102445;--reactodia-color-primary-contrast-foreground:#ebf2fc;--reactodia-color-secondary-contrast-background:#474748;--reactodia-color-secondary-contrast-foreground:#fdfdfe;--reactodia-color-success-contrast-background:#003100;--reactodia-color-success-contrast-foreground:#e6f6e6;--reactodia-color-info-contrast-background:#193c47;--reactodia-color-info-contrast-foreground:#eef9fd;--reactodia-color-warning-contrast-background:#4d3800;--reactodia-color-warning-contrast-foreground:#fff8e6;--reactodia-color-danger-contrast-background:#4b1113;--reactodia-color-danger-contrast-foreground:#ffebec;--reactodia-color-content:#e3e3e3;--reactodia-color-content-inverse:var(--reactodia-color-gray-0);--reactodia-color-content-contrast:var(--reactodia-color-gray-900);--reactodia-background-color:#1b1b1d;--reactodia-background-color-surface:#242526;--reactodia-border-color-base:#555;--reactodia-input-color:#ddd;--reactodia-input-color-placeholder:#878787;--reactodia-input-background-color:#393c3e;--reactodia-input-background-color-disabled:#2f2f2f;--reactodia-button-default-background-color:var(--reactodia-background-color-surface);--reactodia-button-default-background-color-active:#a4a6a8;--reactodia-button-default-background-color-focus:var(--reactodia-button-default-color);--reactodia-button-default-color:#ebedf0;--reactodia-button-default-color-focus:var(--reactodia-background-color-surface);--reactodia-draggable-handle-corner-color:#ffffff61;--reactodia-draggable-handle-corner-color-focus:#ffffff80;--reactodia-draggable-handle-side-color:#fff;--reactodia-canvas-box-shadow:0 4px 15px 7px #aaaaaa0d;--reactodia-canvas-overlay-color:#000000b3;--reactodia-canvas-underlay-color:#1c1c1c99;--reactodia-element-background-color:#242526;--reactodia-link-stroke-color:var(--reactodia-color-emphasis-1000);--reactodia-monochrome-icon-filter:invert(1);--reactodia-selection-icon-filter:invert(1);--reactodia-selection-multiple-box-shadow:0 0 7px 7px #aaaaaa21;--reactodia-navigator-background-fill:var(--reactodia-color-gray-900);--reactodia-navigator-viewport-stroke-color:var(--reactodia-color-gray-800)}.reactodia-workspace-light{--lightningcss-light:initial;--lightningcss-dark: ;color-scheme:light;--reactodia-color-gray-0:#fff;--reactodia-color-gray-100:#f5f6f7;--reactodia-color-gray-200:#ebedf0;--reactodia-color-gray-300:#dadde1;--reactodia-color-gray-400:#ccd0d5;--reactodia-color-gray-500:#bec3c9;--reactodia-color-gray-600:#8d949e;--reactodia-color-gray-700:#606770;--reactodia-color-gray-800:#444950;--reactodia-color-gray-900:#1c1e21;--reactodia-color-gray-1000:#000;--reactodia-color-primary:#337ab7;--reactodia-color-primary-dark:#306cce;--reactodia-color-primary-darker:#2d66c3;--reactodia-color-primary-darkest:#2554a0;--reactodia-color-primary-light:#538ce9;--reactodia-color-primary-lighter:#72a1ed;--reactodia-color-primary-lightest:#9abcf2;--reactodia-color-secondary:#ebedf0;--reactodia-color-secondary-dark:#d4d5d8;--reactodia-color-secondary-darker:#c8c9cc;--reactodia-color-secondary-darkest:#a4a6a8;--reactodia-color-secondary-light:#eef0f2;--reactodia-color-secondary-lighter:#f1f2f5;--reactodia-color-secondary-lightest:#f5f6f8;--reactodia-color-success:#5cb85c;--reactodia-color-success-dark:#009400;--reactodia-color-success-darker:#008b00;--reactodia-color-success-darkest:#007300;--reactodia-color-success-light:#26b226;--reactodia-color-success-lighter:#4dbf4d;--reactodia-color-success-lightest:#80d280;--reactodia-color-info:#54c7ec;--reactodia-color-info-dark:#4cb3d4;--reactodia-color-info-darker:#47a9c9;--reactodia-color-info-darkest:#3b8ba5;--reactodia-color-info-light:#6ecfef;--reactodia-color-info-lighter:#87d8f2;--reactodia-color-info-lightest:#aae3f6;--reactodia-color-warning:#ffba00;--reactodia-color-warning-dark:#e6a700;--reactodia-color-warning-darker:#d99e00;--reactodia-color-warning-darkest:#b38200;--reactodia-color-warning-light:#ffc426;--reactodia-color-warning-lighter:#ffcf4d;--reactodia-color-warning-lightest:#ffdd80;--reactodia-color-danger:#c9302c;--reactodia-color-danger-dark:#e13238;--reactodia-color-danger-darker:#d53035;--reactodia-color-danger-darkest:#af272b;--reactodia-color-danger-light:#fb565b;--reactodia-color-danger-lighter:#fb7478;--reactodia-color-danger-lightest:#fd9c9f;--reactodia-font-family-base:\"Helvetica Neue\", \"Helvetica\", \"Arial\", sans-serif;--reactodia-font-family-monospace:\"Menlo\", \"Monaco\", \"Consolas\", \"Liberation Mono\", \"Courier New\", monospace;--reactodia-font-size-base:87.5%;--reactodia-line-height-base:1.42857;--reactodia-font-color-base:var(--reactodia-color-content);--reactodia-font-color-base-inverse:var(--reactodia-color-content-inverse);--reactodia-spacing-base:5px;--reactodia-spacing-vertical:var(--reactodia-spacing-base);--reactodia-spacing-horizontal:var(--reactodia-spacing-base);--reactodia-z-index-base:0;--reactodia-border-radius-base:4px;--reactodia-border-radius-s:2px;--reactodia-border-width-base:1px;--reactodia-transition-duration:.3s;--reactodia-input-border-color:var(--reactodia-border-color-base);--reactodia-input-border-color-focus:var(--reactodia-color-primary-lighter);--reactodia-input-border-radius:var(--reactodia-border-radius-base);--reactodia-input-border-width:var(--reactodia-border-width-base);--reactodia-button-border-radius:var(--reactodia-border-radius-base);--reactodia-button-border-width:var(--reactodia-border-width-base);--reactodia-button-default-border-color:var(--reactodia-border-color-base);--reactodia-draggable-handle-corner-width:10px;--reactodia-draggable-handle-side-width:5px;--reactodia-tree-background-color-active:#65a6ff4d;--reactodia-tree-background-color-focus:#bdd9ff4d;--reactodia-tree-border-color-active:#73d3ff80;--reactodia-tree-border-color-focus:#73d3ff4d;--reactodia-dialog-border-color:var(--reactodia-border-color-base);--reactodia-dialog-border-radius:var(--reactodia-border-radius-base);--reactodia-dialog-border-width:var(--reactodia-border-width-base);--reactodia-dialog-viewport-breakpoint-s:600px;--reactodia-canvas-background-color:var(--reactodia-background-color);--reactodia-viewport-dock-margin:10px;--reactodia-selection-link-color:#d8956d;--reactodia-selection-single-box-margin:5px;--reactodia-selection-single-box-color:#d8956d;--reactodia-selection-single-box-shadow:0 0 5px 0 var(--reactodia-selection-single-box-color) inset;--reactodia-navigator-scrollable-pane-fill:var(--reactodia-navigator-background-fill);--reactodia-navigator-viewport-fill:var(--reactodia-canvas-background-color);--reactodia-navigator-viewport-stroke-width:2;--reactodia-navigator-overflow-stroke-color:var(--reactodia-navigator-viewport-stroke-color);--reactodia-navigator-overflow-stroke-width:2;--reactodia-navigator-overflow-stroke-dash:5 5;--reactodia-toolbar-height:30px;--reactodia-item-min-width:150px;--reactodia-accordion-transition-duration:var(--reactodia-transition-duration);--reactodia-color-emphasis-0:var(--reactodia-color-gray-0);--reactodia-color-emphasis-100:var(--reactodia-color-gray-100);--reactodia-color-emphasis-200:var(--reactodia-color-gray-200);--reactodia-color-emphasis-300:var(--reactodia-color-gray-300);--reactodia-color-emphasis-400:var(--reactodia-color-gray-400);--reactodia-color-emphasis-500:var(--reactodia-color-gray-500);--reactodia-color-emphasis-600:var(--reactodia-color-gray-600);--reactodia-color-emphasis-700:var(--reactodia-color-gray-700);--reactodia-color-emphasis-800:var(--reactodia-color-gray-800);--reactodia-color-emphasis-900:var(--reactodia-color-gray-900);--reactodia-color-emphasis-1000:var(--reactodia-color-gray-1000);--reactodia-color-primary-contrast-background:#ebf2fc;--reactodia-color-primary-contrast-foreground:#102445;--reactodia-color-secondary-contrast-background:#fdfdfe;--reactodia-color-secondary-contrast-foreground:#474748;--reactodia-color-success-contrast-background:#e6f6e6;--reactodia-color-success-contrast-foreground:#003100;--reactodia-color-info-contrast-background:#eef9fd;--reactodia-color-info-contrast-foreground:#193c47;--reactodia-color-warning-contrast-background:#fff8e6;--reactodia-color-warning-contrast-foreground:#4d3800;--reactodia-color-danger-contrast-background:#ffebec;--reactodia-color-danger-contrast-foreground:#4b1113;--reactodia-color-content:var(--reactodia-color-emphasis-900);--reactodia-color-content-inverse:var(--reactodia-color-emphasis-0);--reactodia-color-content-contrast:var(--reactodia-color-content);--reactodia-background-color:var(--reactodia-color-emphasis-0);--reactodia-background-color-surface:var(--reactodia-color-content-inverse);--reactodia-border-color-base:#ccc;--reactodia-input-color:#555;--reactodia-input-color-placeholder:var(--reactodia-color-emphasis-600);--reactodia-input-background-color:var(--reactodia-background-color-surface);--reactodia-input-background-color-disabled:var(--reactodia-color-emphasis-400);--reactodia-button-default-background-color:var(--reactodia-background-color-surface);--reactodia-button-default-background-color-active:#e0e0e0;--reactodia-button-default-background-color-focus:#d4d4d4;--reactodia-button-default-color:#333;--reactodia-button-default-color-focus:var(--reactodia-button-default-color);--reactodia-draggable-handle-corner-color:#00000061;--reactodia-draggable-handle-corner-color-focus:#00000080;--reactodia-draggable-handle-side-color:#000;--reactodia-canvas-box-shadow:0 4px 15px 7px #3333330d;--reactodia-canvas-overlay-color:#ffffffb3;--reactodia-canvas-underlay-color:#ffffffb3;--reactodia-element-background-color:#fafaf9;--reactodia-link-stroke-color:var(--reactodia-color-emphasis-1000);--reactodia-monochrome-icon-filter:;--reactodia-selection-icon-filter:;--reactodia-selection-multiple-box-shadow:0 0 7px 7px #33333321;--reactodia-navigator-background-fill:var(--reactodia-color-gray-100);--reactodia-navigator-viewport-stroke-color:var(--reactodia-color-gray-200)}.reactodia-draggable-handle--dock-n,.reactodia-draggable-handle--dock-s{background-color:var(--reactodia-draggable-handle-side-color);position:absolute}.reactodia-draggable-handle--dock-n:empty,.reactodia-draggable-handle--dock-s:empty{opacity:0}.reactodia-draggable-handle--dock-n:empty:hover,.reactodia-draggable-handle--dock-s:empty:hover{opacity:.1}.reactodia-draggable-handle--dock-n,.reactodia-draggable-handle--dock-s{cursor:ns-resize;width:100%}.reactodia-draggable-handle--dock-n:empty,.reactodia-draggable-handle--dock-s:empty{height:var(--reactodia-draggable-handle-side-width)}.reactodia-draggable-handle--dock-n{top:0}.reactodia-draggable-handle--dock-s{bottom:0}.reactodia-draggable-handle--dock-w,.reactodia-draggable-handle--dock-e{background-color:var(--reactodia-draggable-handle-side-color);position:absolute}.reactodia-draggable-handle--dock-w:empty,.reactodia-draggable-handle--dock-e:empty{opacity:0}.reactodia-draggable-handle--dock-w:empty:hover,.reactodia-draggable-handle--dock-e:empty:hover{opacity:.1}.reactodia-draggable-handle--dock-w,.reactodia-draggable-handle--dock-e{cursor:ew-resize;height:100%;top:0}.reactodia-draggable-handle--dock-w:empty,.reactodia-draggable-handle--dock-e:empty{width:var(--reactodia-draggable-handle-side-width)}.reactodia-draggable-handle--dock-w{left:0}.reactodia-draggable-handle--dock-e{right:0}.reactodia-draggable-handle--dock-nw,.reactodia-draggable-handle--dock-ne,.reactodia-draggable-handle--dock-sw,.reactodia-draggable-handle--dock-se{width:0;height:0;position:absolute}.reactodia-draggable-handle--dock-nw:empty,.reactodia-draggable-handle--dock-ne:empty,.reactodia-draggable-handle--dock-sw:empty,.reactodia-draggable-handle--dock-se:empty{border-radius:var(--reactodia-border-radius-base);border-style:solid}.reactodia-draggable-handle--dock-nw:empty:before,.reactodia-draggable-handle--dock-ne:empty:before,.reactodia-draggable-handle--dock-sw:empty:before,.reactodia-draggable-handle--dock-se:empty:before{content:\"\";border-radius:var(--reactodia-border-radius-base);border-style:solid;width:0;height:0;position:absolute}.reactodia-draggable-handle--dock-nw,.reactodia-draggable-handle--dock-ne{border-color:var(--reactodia-draggable-handle-corner-color) transparent transparent transparent}.reactodia-draggable-handle--dock-nw:before,.reactodia-draggable-handle--dock-ne:before{top:calc(-1 * var(--reactodia-draggable-handle-corner-width));border-color:var(--reactodia-draggable-handle-corner-color) transparent transparent transparent}.reactodia-draggable-handle--dock-nw:hover,.reactodia-draggable-handle--dock-ne:hover{border-color:var(--reactodia-draggable-handle-corner-color-focus) transparent transparent transparent}.reactodia-draggable-handle--dock-nw{border-width:var(--reactodia-draggable-handle-corner-width) var(--reactodia-draggable-handle-corner-width) 0 0;cursor:nwse-resize;top:0;left:0}.reactodia-draggable-handle--dock-nw:before{border-width:calc(var(--reactodia-draggable-handle-corner-width) / 2) calc(var(--reactodia-draggable-handle-corner-width) / 2) 0 0;left:0}.reactodia-draggable-handle--dock-ne{border-width:var(--reactodia-draggable-handle-corner-width) 0 0 var(--reactodia-draggable-handle-corner-width);cursor:nesw-resize;top:0;right:0}.reactodia-draggable-handle--dock-ne:before{border-width:calc(var(--reactodia-draggable-handle-corner-width) / 2) 0 0 calc(var(--reactodia-draggable-handle-corner-width) / 2);right:0}.reactodia-draggable-handle--dock-sw,.reactodia-draggable-handle--dock-se{border-color:transparent transparent var(--reactodia-draggable-handle-corner-color) transparent}.reactodia-draggable-handle--dock-sw:before,.reactodia-draggable-handle--dock-se:before{bottom:calc(-1 * var(--reactodia-draggable-handle-corner-width));border-color:transparent transparent var(--reactodia-draggable-handle-corner-color) transparent}.reactodia-draggable-handle--dock-sw:hover,.reactodia-draggable-handle--dock-se:hover{border-color:transparent transparent var(--reactodia-draggable-handle-corner-color-focus) transparent}.reactodia-draggable-handle--dock-sw{border-width:0 var(--reactodia-draggable-handle-corner-width) var(--reactodia-draggable-handle-corner-width) 0;cursor:nesw-resize;bottom:0;left:0}.reactodia-draggable-handle--dock-sw:before{border-width:0 calc(var(--reactodia-draggable-handle-corner-width) / 2) calc(var(--reactodia-draggable-handle-corner-width) / 2) 0;left:0}.reactodia-draggable-handle--dock-se{border-width:0 0 var(--reactodia-draggable-handle-corner-width) var(--reactodia-draggable-handle-corner-width);cursor:nwse-resize;bottom:0;right:0}.reactodia-draggable-handle--dock-se:before{border-width:0 0 calc(var(--reactodia-draggable-handle-corner-width) / 2) calc(var(--reactodia-draggable-handle-corner-width) / 2);right:0}.reactodia-drag-link-mover{overflow:visible}.reactodia-drag-link-mover__highlight-overlay{fill:var(--reactodia-color-emphasis-0);fill-opacity:.3}.reactodia-drag-link-mover__highlight-allow,.reactodia-drag-link-mover__highlight-deny{stroke-width:3px;fill:#0000}.reactodia-drag-link-mover__highlight-allow{stroke:var(--reactodia-color-success)}.reactodia-drag-link-mover__highlight-deny{stroke:var(--reactodia-color-danger)}.reactodia-drag-link-mover__drop-underlay{fill:var(--reactodia-color-emphasis-1000);fill-opacity:.1}.reactodia-drag-link-mover__drop-allow{stroke:var(--reactodia-color-success)}.reactodia-drag-link-mover__drop-deny>*{stroke:var(--reactodia-color-danger)}.reactodia-dropdown{float:left;z-index:calc(var(--reactodia-z-index-base) + 40);position:relative}.reactodia-dropdown__content{opacity:1;flex-direction:column;transition:opacity .1s ease-in-out;display:flex;position:absolute}.reactodia-dropdown--up .reactodia-dropdown__content{bottom:100%}.reactodia-dropdown--expanded .reactodia-dropdown__content{box-shadow:var(--reactodia-canvas-box-shadow)}.reactodia-dropdown--collapsed .reactodia-dropdown__content{opacity:0;pointer-events:none;height:0;transition:none}.reactodia-dropdown--down.reactodia-dropdown--expanded .reactodia-dropdown-menu__toggle{border-bottom-left-radius:unset;border-bottom-right-radius:unset;border-bottom:none}.reactodia-dropdown--up.reactodia-dropdown--expanded .reactodia-dropdown-menu__toggle{border-top-left-radius:unset;border-top-right-radius:unset;border-top:none}.reactodia-dropdown-menu__toggle:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M16%205H0V4h16v1zm0%208H0v-1h16v1zm0-4.008H0V8h16v.992z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M16%205H0V4h16v1zm0%208H0v-1h16v1zm0-4.008H0V8h16v.992z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-dropdown-menu__items{background-color:var(--reactodia-background-color-surface);display:flex}.reactodia-dropdown--down .reactodia-dropdown-menu__items{flex-direction:column}.reactodia-dropdown--up .reactodia-dropdown-menu__items{flex-direction:column-reverse}.reactodia-dropdown-menu-item{border-radius:unset;outline-offset:-2px;display:flex}.reactodia-dropdown--down .reactodia-dropdown-menu-item:first-child{border-top-right-radius:var(--reactodia-button-border-radius)}.reactodia-dropdown--down .reactodia-dropdown-menu-item:last-child{border-bottom-left-radius:var(--reactodia-button-border-radius);border-bottom-right-radius:var(--reactodia-button-border-radius)}.reactodia-dropdown--down .reactodia-dropdown-menu-item~.reactodia-dropdown-menu-item{margin-top:calc(-1 * var(--reactodia-button-border-width));border-top-color:var(--reactodia-button-default-border-color)}.reactodia-dropdown--up .reactodia-dropdown-menu-item:first-child{border-bottom-right-radius:var(--reactodia-button-border-radius)}.reactodia-dropdown--up .reactodia-dropdown-menu-item:last-child{border-top-left-radius:var(--reactodia-button-border-radius);border-top-right-radius:var(--reactodia-button-border-radius)}.reactodia-dropdown--up .reactodia-dropdown-menu-item~.reactodia-dropdown-menu-item{margin-bottom:calc(-1 * var(--reactodia-button-border-width));border-bottom-color:var(--reactodia-button-default-border-color)}.reactodia-inline-entity{border:1px solid var(--reactodia-inline-entity-color);background:var(--reactodia-inline-entity-color);word-wrap:break-word;padding-left:5px;line-height:1.3}.reactodia-inline-entity__label{background:var(--reactodia-element-background-color);padding:0 2px}.reactodia-list-element-view{background:var(--reactodia-element-style-color);border-radius:var(--reactodia-border-radius-s);opacity:1;cursor:move;-webkit-user-select:none;user-select:none;margin:0 0 2px;padding:1px 1px 1px 10px;display:block;position:relative}.reactodia-list-element-view--disabled{opacity:.4;cursor:default}.reactodia-list-element-view__label{background:var(--reactodia-element-background-color);border-radius:0 var(--reactodia-border-radius-s) var(--reactodia-border-radius-s) 0;font-family:var(--reactodia-font-family-base);overflow-wrap:break-word;min-height:1.3em;padding-left:7px;padding-right:5px;font-size:15px}.reactodia-list-element-view--selected .reactodia-list-element-view__label{color:var(--reactodia-color-content-contrast)}.reactodia-text-highlight{font-weight:700}.reactodia-no-search-results{text-align:center;color:var(--reactodia-input-color-placeholder);font-style:italic}@keyframes reactodia-progress-bar-stripes{0%{background-position:40px 0}to{background-position:0 0}}.reactodia-progress-bar{background-color:var(--reactodia-color-emphasis-200);flex-shrink:0;width:100%;overflow:hidden;box-shadow:inset 0 1px 2px #0000001a}.reactodia-progress-bar__bar{float:left;background-color:var(--reactodia-color-primary);background-image:linear-gradient(45deg,#ffffff26 25%,#0000 25% 50%,#ffffff26 50% 75%,#0000 75%,#0000);background-repeat:repeat-x;background-size:40px 40px;height:100%;transition:width .6s;animation:2s linear infinite reactodia-progress-bar-stripes}.reactodia-progress-bar--error .reactodia-progress-bar__bar{background-color:var(--reactodia-color-danger)}.reactodia-progress-bar--loading,.reactodia-progress-bar--error{transition:height var(--reactodia-transition-duration) .3s}.reactodia-progress-bar--completed{transition:height var(--reactodia-transition-duration) 0s}.reactodia-resizable-box{border:var(--reactodia-resizable-box-border-width) solid var(--reactodia-resizable-box-color)}.reactodia-resizable-box__handle{background-color:unset;touch-action:none}.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-n,.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-s,.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-w,.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-e{justify-content:center;align-items:center;display:flex}.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-n{margin-top:calc(-.5 * var(--reactodia-resizable-box-border-width))}.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-s{margin-bottom:calc(-.5 * var(--reactodia-resizable-box-border-width))}.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-w{margin-left:calc(-.5 * var(--reactodia-resizable-box-border-width))}.reactodia-resizable-box__handle.reactodia-draggable-handle--dock-e{margin-right:calc(-.5 * var(--reactodia-resizable-box-border-width))}.reactodia-resizable-box__handle rect{stroke-width:2px}.reactodia-resizable-box__handle circle,.reactodia-resizable-box__handle rect{stroke:var(--reactodia-resizable-box-color);fill:var(--reactodia-canvas-background-color)}.reactodia-resizable-box__handle:hover circle,.reactodia-resizable-box__handle:hover rect{fill:var(--reactodia-resizable-box-color)}.reactodia-resizable-box__corner{width:20px;height:20px;position:absolute;top:-10px;left:-10px}.reactodia-resizable-box__vertical{width:30px;height:10px;position:absolute}.reactodia-resizable-box__horizontal{width:10px;height:30px;position:absolute}.reactodia-search-input{display:flex}.reactodia-search-input__input::placeholder{color:var(--reactodia-input-color-placeholder)}.reactodia-search-input--has-submit .reactodia-search-input__input{border-top-right-radius:unset;border-bottom-right-radius:unset}.reactodia-search-input__clear-container{align-items:center;width:0;display:flex;overflow:visible}.reactodia-search-input__clear{cursor:pointer;color:var(--reactodia-button-default-color);border-top-right-radius:var(--reactodia-input-border-radius);border-bottom-right-radius:var(--reactodia-input-border-radius);transition:background-color var(--reactodia-transition-duration);background:0 0;border:0;padding:8px;position:relative;left:-33px}.reactodia-search-input__clear:hover{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-focus)}.reactodia-search-input__clear:after{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-search-input--has-submit .reactodia-search-input__clear{border-top-right-radius:unset;border-bottom-right-radius:unset}.reactodia-search-input__submit{border-top-left-radius:unset;border-bottom-left-radius:unset;margin-left:-1px}.reactodia-search-input__submit:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-search-results__root{margin:0;padding:0}.reactodia-search-results__item{list-style:none;display:contents}.reactodia-viewport-dock--row-n,.reactodia-viewport-dock--row-s,.reactodia-viewport-dock--column-w,.reactodia-viewport-dock--column-e{pointer-events:none}.reactodia-viewport-dock--row-n>*,.reactodia-viewport-dock--row-s>*,.reactodia-viewport-dock--column-w>*,.reactodia-viewport-dock--column-e>*{pointer-events:auto}.reactodia-viewport-dock--row-n,.reactodia-viewport-dock--row-s{justify-content:center;display:flex;position:absolute;left:0;right:0}.reactodia-viewport-dock--row-n{top:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px))}.reactodia-viewport-dock--row-s{bottom:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px))}.reactodia-viewport-dock--column-w,.reactodia-viewport-dock--column-e{flex-direction:column;justify-content:center;display:flex;position:absolute;top:0;bottom:0}.reactodia-viewport-dock--column-w{left:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px))}.reactodia-viewport-dock--column-e{right:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px))}.reactodia-viewport-dock--corner{display:contents}.reactodia-viewport-dock--dock-nw{left:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px));top:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px));position:absolute}.reactodia-viewport-dock--dock-ne{right:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px));top:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px));position:absolute}.reactodia-viewport-dock--dock-se{right:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px));bottom:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px));position:absolute}.reactodia-viewport-dock--dock-sw{left:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-x,0px));bottom:calc(var(--reactodia-viewport-dock-margin) + var(--reactodia-viewport-dock-offset-y,0px));position:absolute}.reactodia-canvas{flex:1 1 0;width:100%;display:flex;overflow:hidden}.reactodia-class-tree{flex-direction:column;flex:auto;display:flex}.reactodia-class-tree__filter{flex-shrink:0;margin:10px 0}.reactodia-class-tree--controlled .reactodia-class-tree__filter{margin-top:0}.reactodia-class-tree__filter-group{margin-left:10px;margin-right:10px}.reactodia-class-tree__only-creatable{margin-top:5px;display:block}.reactodia-class-tree__tree{border-top:1px solid var(--reactodia-border-color-base)}.reactodia-class-tree__no-results{margin-top:10px}.reactodia-class-tree__spinner{align-self:center;margin:auto}.reactodia-class-tree-item{margin:1px 0;list-style:none}.reactodia-class-tree-item__row{white-space:nowrap;-webkit-user-select:none;user-select:none;align-items:center;display:flex}.reactodia-class-tree-item__row>*{flex-shrink:0}.reactodia-class-tree-item__body{border-radius:var(--reactodia-border-radius-s);border:1px solid #0000;align-items:center;padding:1px;font-size:15px;text-decoration:none;display:flex}.reactodia-class-tree-item__body:hover{background:var(--reactodia-tree-background-color-focus);border-color:var(--reactodia-tree-border-color-focus);cursor:pointer}.reactodia-class-tree-item__body--selected{background-color:var(--reactodia-tree-background-color-active);border-color:var(--reactodia-tree-border-color-active)}.reactodia-class-tree-item__icon-container{height:20px}.reactodia-class-tree-item__icon{height:100%;display:block}.reactodia-class-tree-item__icon--monochrome{filter:var(--reactodia-monochrome-icon-filter)}.reactodia-class-tree-item__default-icon-leaf,.reactodia-class-tree-item__default-icon-parent{content:\"\";width:20px;height:20px;color:var(--reactodia-element-style-color);background-color:currentColor;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.51%204l-5-3h-1l-5%203-.49.86v6l.49.85%205%203h1l5-3%20.49-.85v-6L13.51%204zm-6%209.56l-4.5-2.7V5.7l4.5%202.45v5.41zM3.27%204.7l4.74-2.84%204.74%202.84-4.74%202.59L3.27%204.7zm9.74%206.16l-4.5%202.7V8.15l4.5-2.45v5.16z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.51%204l-5-3h-1l-5%203-.49.86v6l.49.85%205%203h1l5-3%20.49-.85v-6L13.51%204zm-6%209.56l-4.5-2.7V5.7l4.5%202.45v5.41zM3.27%204.7l4.74-2.84%204.74%202.84-4.74%202.59L3.27%204.7zm9.74%206.16l-4.5%202.7V8.15l4.5-2.45v5.16z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-class-tree-item__label{color:var(--reactodia-font-color-base);margin-left:5px}.reactodia-class-tree-item__highlighted-term{font-weight:700}.reactodia-class-tree-item__count{margin-left:5px}.reactodia-class-tree-item__root{margin:0;padding:0}.reactodia-class-tree-item__children{margin:0 0 0 20px;padding:0}.reactodia-class-tree-item__toggle,.reactodia-class-tree-item__toggle-expanded,.reactodia-class-tree-item__toggle-collapsed{margin:2px}.reactodia-class-tree-item__toggle-expanded,.reactodia-class-tree-item__toggle-collapsed{opacity:.5}.reactodia-class-tree-item__toggle-expanded:hover,.reactodia-class-tree-item__toggle-collapsed:hover{cursor:pointer;opacity:1;transition:opacity var(--reactodia-transition-duration)}.reactodia-class-tree-item__toggle:before{content:\"\";width:16px;height:16px;display:block}.reactodia-class-tree-item__toggle-expanded:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M2%205.56L2.413%205h11.194l.393.54L8.373%2011h-.827L2%205.56z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M2%205.56L2.413%205h11.194l.393.54L8.373%2011h-.827L2%205.56z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-class-tree-item__toggle-collapsed:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M5.56%2014L5%2013.587V2.393L5.54%202%2011%207.627v.827L5.56%2014z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M5.56%2014L5%2013.587V2.393L5.54%202%2011%207.627v.827L5.56%2014z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-class-tree-item__create-button{cursor:move;margin-left:5px;padding:5px 4px 5px 5px}.reactodia-class-tree-item__create-button:before{content:\"\";background-color:currentColor;width:12px;height:12px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M14%207v1H8v6H7V8H1V7h6V1h1v6h6z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M14%207v1H8v6H7V8H1V7h6V1h1v6h6z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-connections-menu{flex-direction:column;flex:auto;display:flex}.reactodia-connections-menu__breadcrumbs{text-overflow:ellipsis;flex-grow:0;height:30px;min-height:30px;padding:10px 10px 0;overflow:hidden}.reactodia-connections-menu__breadcrumbs-link{color:var(--reactodia-color-primary);cursor:pointer;font-family:unset;font-size:unset;font-weight:unset;background:0 0;border:none;padding:0;text-decoration:none}.reactodia-connections-menu__breadcrumbs-link:hover{text-decoration:underline}.reactodia-connections-menu__search{flex:none;align-items:center;padding:10px}.reactodia-connections-menu__sort-switches{flex:0 0 20px;margin-left:10px}.reactodia-connections-menu__sort-switch{display:none}.reactodia-connections-menu__sort-switch:checked+.reactodia-connections-menu__sort-switch-label{color:var(--reactodia-color-primary-light);opacity:1}.reactodia-connections-menu__sort-switch-label{cursor:pointer;opacity:.4;text-align:center;margin:0;display:block}.reactodia-connections-menu__sort-switch-label:hover{opacity:1}.reactodia-connections-menu__sort-label-alpha:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.223%2010.933c.326.192.699.29%201.077.282a2.159%202.159%200%200%200%201.754-.842%203.291%203.291%200%200%200%20.654-2.113%202.886%202.886%200%200%200-.576-1.877%201.99%201.99%200%200%200-1.634-.733%202.294%202.294%200%200%200-1.523.567V3.475h-.991V11.1h.995v-.344c.076.066.158.125.244.177zM7.85%206.7c.186-.079.388-.113.59-.1a1.08%201.08%200%200%201%20.896.428c.257.363.382.802.357%201.245a2.485%202.485%200%200%201-.4%201.484%201.133%201.133%200%200%201-.96.508%201.224%201.224%200%200%201-.976-.417A1.522%201.522%200%200%201%206.975%208.8v-.6a1.722%201.722%200%200%201%20.393-1.145c.13-.154.296-.276.482-.355zM3.289%205.675a3.03%203.03%200%200%200-.937.162%202.59%202.59%200%200%200-.8.4l-.1.077v1.2l.423-.359a2.1%202.1%200%200%201%201.366-.572.758.758%200%200%201%20.661.282c.15.232.23.503.231.779L2.9%207.825a2.6%202.6%200%200%200-1.378.575%201.65%201.65%200%200%200-.022%202.336%201.737%201.737%200%200%200%201.253.454%201.96%201.96%200%200%200%201.107-.332c.102-.068.197-.145.286-.229v.444h.941V7.715a2.193%202.193%200%200%200-.469-1.5%201.687%201.687%200%200%200-1.329-.54zm.857%203.041c.02.418-.12.829-.391%201.148a1.221%201.221%200%200%201-.955.422.832.832%200%200%201-.608-.2.833.833%200%200%201%200-1.091c.281-.174.6-.277.93-.3l1.02-.148.004.169zm8.313%202.317c.307.13.64.193.973.182.495.012.983-.114%201.41-.365l.123-.075.013-.007V9.615l-.446.32c-.316.224-.696.34-1.084.329A1.3%201.3%200%200%201%2012.4%209.8a1.975%201.975%200%200%201-.4-1.312%202.01%202.01%200%200%201%20.453-1.381A1.432%201.432%200%200%201%2013.6%206.6a1.8%201.8%200%200%201%20.971.279l.43.265V5.97l-.17-.073a2.9%202.9%200%200%200-1.17-.247%202.52%202.52%200%200%200-1.929.817%202.9%202.9%200%200%200-.747%202.049c-.028.707.21%201.4.67%201.939.222.249.497.446.804.578z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.223%2010.933c.326.192.699.29%201.077.282a2.159%202.159%200%200%200%201.754-.842%203.291%203.291%200%200%200%20.654-2.113%202.886%202.886%200%200%200-.576-1.877%201.99%201.99%200%200%200-1.634-.733%202.294%202.294%200%200%200-1.523.567V3.475h-.991V11.1h.995v-.344c.076.066.158.125.244.177zM7.85%206.7c.186-.079.388-.113.59-.1a1.08%201.08%200%200%201%20.896.428c.257.363.382.802.357%201.245a2.485%202.485%200%200%201-.4%201.484%201.133%201.133%200%200%201-.96.508%201.224%201.224%200%200%201-.976-.417A1.522%201.522%200%200%201%206.975%208.8v-.6a1.722%201.722%200%200%201%20.393-1.145c.13-.154.296-.276.482-.355zM3.289%205.675a3.03%203.03%200%200%200-.937.162%202.59%202.59%200%200%200-.8.4l-.1.077v1.2l.423-.359a2.1%202.1%200%200%201%201.366-.572.758.758%200%200%201%20.661.282c.15.232.23.503.231.779L2.9%207.825a2.6%202.6%200%200%200-1.378.575%201.65%201.65%200%200%200-.022%202.336%201.737%201.737%200%200%200%201.253.454%201.96%201.96%200%200%200%201.107-.332c.102-.068.197-.145.286-.229v.444h.941V7.715a2.193%202.193%200%200%200-.469-1.5%201.687%201.687%200%200%200-1.329-.54zm.857%203.041c.02.418-.12.829-.391%201.148a1.221%201.221%200%200%201-.955.422.832.832%200%200%201-.608-.2.833.833%200%200%201%200-1.091c.281-.174.6-.277.93-.3l1.02-.148.004.169zm8.313%202.317c.307.13.64.193.973.182.495.012.983-.114%201.41-.365l.123-.075.013-.007V9.615l-.446.32c-.316.224-.696.34-1.084.329A1.3%201.3%200%200%201%2012.4%209.8a1.975%201.975%200%200%201-.4-1.312%202.01%202.01%200%200%201%20.453-1.381A1.432%201.432%200%200%201%2013.6%206.6a1.8%201.8%200%200%201%20.971.279l.43.265V5.97l-.17-.073a2.9%202.9%200%200%200-1.17-.247%202.52%202.52%200%200%200-1.929.817%202.9%202.9%200%200%200-.747%202.049c-.028.707.21%201.4.67%201.939.222.249.497.446.804.578z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-connections-menu__sort-label-smart:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M11.67%208.658a3.661%203.661%200%200%200-.781%201.114%203.28%203.28%200%200%200-.268%201.329v1.6a1.304%201.304%200%200%201-.794%201.197%201.282%201.282%200%200%201-.509.102H7.712a1.285%201.285%200%200%201-.922-.379%201.303%201.303%200%200%201-.38-.92v-1.6c0-.479-.092-.921-.274-1.329a3.556%203.556%200%200%200-.776-1.114%204.689%204.689%200%200%201-1.006-1.437A4.187%204.187%200%200%201%204%205.5a4.432%204.432%200%200%201%20.616-2.27c.197-.336.432-.64.705-.914a4.6%204.6%200%200%201%20.911-.702c.338-.196.7-.348%201.084-.454a4.45%204.45%200%200%201%201.2-.16%204.476%204.476%200%200%201%202.276.614%204.475%204.475%200%200%201%201.622%201.616%204.438%204.438%200%200%201%20.616%202.27c0%20.617-.117%201.191-.353%201.721a4.69%204.69%200%200%201-1.006%201.437zM9.623%2010.5H7.409v2.201c0%20.081.028.15.09.212a.29.29%200%200%200%20.213.09h1.606a.289.289%200%200%200%20.213-.09.286.286%200%200%200%20.09-.212V10.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M11.67%208.658a3.661%203.661%200%200%200-.781%201.114%203.28%203.28%200%200%200-.268%201.329v1.6a1.304%201.304%200%200%201-.794%201.197%201.282%201.282%200%200%201-.509.102H7.712a1.285%201.285%200%200%201-.922-.379%201.303%201.303%200%200%201-.38-.92v-1.6c0-.479-.092-.921-.274-1.329a3.556%203.556%200%200%200-.776-1.114%204.689%204.689%200%200%201-1.006-1.437A4.187%204.187%200%200%201%204%205.5a4.432%204.432%200%200%201%20.616-2.27c.197-.336.432-.64.705-.914a4.6%204.6%200%200%201%20.911-.702c.338-.196.7-.348%201.084-.454a4.45%204.45%200%200%201%201.2-.16%204.476%204.476%200%200%201%202.276.614%204.475%204.475%200%200%201%201.622%201.616%204.438%204.438%200%200%201%20.616%202.27c0%20.617-.117%201.191-.353%201.721a4.69%204.69%200%200%201-1.006%201.437zM9.623%2010.5H7.409v2.201c0%20.081.028.15.09.212a.29.29%200%200%200%20.213.09h1.606a.289.289%200%200%200%20.213-.09.286.286%200%200%200%20.09-.212V10.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-connections-menu__spinner{align-self:center;margin:auto}.reactodia-connections-menu__links-list{border-top:1px solid var(--reactodia-border-color-base);flex-grow:1;padding:5px;overflow:hidden auto}.reactodia-connections-menu__links-list-empty{align-items:center;display:flex}.reactodia-connections-menu__links-root{margin:0;padding:0;list-style-type:none}.reactodia-connections-menu__links-item{display:contents}.reactodia-connections-menu__links-no-results{width:100%;color:var(--reactodia-color-emphasis-400);text-align:center;font-size:x-large;font-weight:700}.reactodia-connections-menu__links-probably-label{margin:5px 0;font-weight:700}.reactodia-connections-menu__links-list-hr{border:0;border-top:1px solid var(--reactodia-border-color-base);height:0;margin-top:6px;margin-bottom:6px}.reactodia-connections-menu__link{background:var(--reactodia-color-emphasis-200);border-radius:var(--reactodia-border-radius-s);margin-bottom:4px;display:flex;position:relative}.reactodia-connections-menu__link:hover{background-color:var(--reactodia-color-emphasis-400)}.reactodia-connections-menu__link-button{border:unset;font-family:unset;font-size:unset;text-align:start;background:0 0;flex:auto;align-items:center;height:30px;padding:0 5px 0 0;display:flex;overflow:hidden}.reactodia-connections-menu__link-direction{flex-shrink:0;align-items:center;min-height:30px;padding:5px 0 5px 10px;display:flex}.reactodia-connections-menu__link:not(:hover) .reactodia-connections-menu__link-direction{background:var(--reactodia-color-emphasis-300)}.reactodia-connections-menu__link-direction-in,.reactodia-connections-menu__link-direction-out{opacity:.5;background-color:currentColor;flex-shrink:0;width:20px;height:20px;-webkit-mask-position:0 0;mask-position:0 0;-webkit-mask-size:contain;mask-size:contain;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat}.reactodia-connections-menu__link-direction-in{margin:0 14px 0 0;-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%205.9,6%20h%20-1%20V%203%20h%2010%20v%2010%20h%20-10%20v%20-3%20h%201%20v%202%20h%208%20V%204%20h%20-8%20z'%20/%3e%3cpath%20d='M%201.5,7.5%20H%207.771919%20L%206.44298,6%207.10745,5.25%209.4,7.8376461%20V%208.1623539%20L%207.10745,10.75%206.44298,10%207.771919,8.5%20H%201.5%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%205.9,6%20h%20-1%20V%203%20h%2010%20v%2010%20h%20-10%20v%20-3%20h%201%20v%202%20h%208%20V%204%20h%20-8%20z'%20/%3e%3cpath%20d='M%201.5,7.5%20H%207.771919%20L%206.44298,6%207.10745,5.25%209.4,7.8376461%20V%208.1623539%20L%207.10745,10.75%206.44298,10%207.771919,8.5%20H%201.5%20Z'%20/%3e%3c/svg%3e\")}.reactodia-connections-menu__link-direction-out{margin:0 8px 0 6px;-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2010.5,6%20h%201%20V%203%20h%20-10%20v%2010%20h%2010%20v%20-3%20h%20-1%20v%202%20h%20-8%20V%204%20h%208%20z'%20/%3e%3cpath%20d='m%207,7.5%20h%206.271919%20L%2011.94298,6%2012.60745,5.25%2014.9,7.8376461%20V%208.1623539%20L%2012.60745,10.75%2011.94298,10%2013.271919,8.5%20H%207%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2010.5,6%20h%201%20V%203%20h%20-10%20v%2010%20h%2010%20v%20-3%20h%20-1%20v%202%20h%20-8%20V%204%20h%208%20z'%20/%3e%3cpath%20d='m%207,7.5%20h%206.271919%20L%2011.94298,6%2012.60745,5.25%2014.9,7.8376461%20V%208.1623539%20L%2012.60745,10.75%2011.94298,10%2013.271919,8.5%20H%207%20Z'%20/%3e%3c/svg%3e\")}.reactodia-connections-menu__link-title{text-overflow:ellipsis;white-space:nowrap;flex-grow:1;margin:5px 0 5px 5px;overflow:hidden}.reactodia-connections-menu__link-count{flex-shrink:0;margin-left:10px}.reactodia-connections-menu__link-navigate-button{float:right;opacity:.5;background-color:currentColor;flex-shrink:0;width:20px;min-width:16px;height:20px;margin-left:10px;-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\");-webkit-mask-size:cover;mask-size:cover}.reactodia-connections-menu__link-navigate-button:hover{opacity:1}.reactodia-connections-menu__link-filter-spacer{flex-shrink:0;width:20px;height:20px;margin-left:10px}.reactodia-connections-menu__link-filter-button{background:0 0;border:none;width:20px;height:20px;padding:0;position:absolute;top:calc(50% - 10px);right:35px}.reactodia-connections-menu__link-filter-button:before{content:\"\";opacity:.4;background-color:currentColor;width:100%;height:100%;display:block;-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2014.5,3.5%20-1,-1%20m%200,0%20H%202.5%20V%203.0652324%20L%206.9861526,8%207.5,8.5652322%20V%2012.5103%20l%201,-0.733333%20V%208.5489178%20l%205,-5.4999998%20z%20m%200,0.548918%20V%204.6%20m%200,0%20-4,4.4%20v%203.3%20l%20-3,2.2%20V%209%20l%20-5,-5.5%20v%20-2%20h%2013%20v%202'%20/%3e%3cpath%20d='M%203.0187424,8%20H%204%20v%201.5%20h%201.5%20v%201%20L%204,10.467287%20V%2012%20H%203%20V%2010.5%20H%201.5%20v%20-1%20H%203%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2014.5,3.5%20-1,-1%20m%200,0%20H%202.5%20V%203.0652324%20L%206.9861526,8%207.5,8.5652322%20V%2012.5103%20l%201,-0.733333%20V%208.5489178%20l%205,-5.4999998%20z%20m%200,0.548918%20V%204.6%20m%200,0%20-4,4.4%20v%203.3%20l%20-3,2.2%20V%209%20l%20-5,-5.5%20v%20-2%20h%2013%20v%202'%20/%3e%3cpath%20d='M%203.0187424,8%20H%204%20v%201.5%20h%201.5%20v%201%20L%204,10.467287%20V%2012%20H%203%20V%2010.5%20H%201.5%20v%20-1%20H%203%20Z'%20/%3e%3c/svg%3e\");-webkit-mask-size:20px 20px;mask-size:20px 20px}.reactodia-connections-menu__link-filter-button:hover:before{opacity:1}.reactodia-connections-menu__move-to-filter{cursor:pointer;-webkit-user-select:none;user-select:none;padding:5px 10px;display:flex}.reactodia-connections-menu__move-to-filter:hover{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-focus)}.reactodia-connections-menu__objects{flex-direction:column;flex-grow:1;padding:0 10px 10px;display:flex;position:relative}.reactodia-connections-menu__objects-select-all{white-space:nowrap;margin-bottom:5px;display:flex}.reactodia-connections-menu__objects-select-all input{margin-right:5px}.reactodia-connections-menu__objects-select-all input:disabled{opacity:.3}.reactodia-connections-menu__objects-loading,.reactodia-connections-menu__objects-no-results{border:1px solid var(--reactodia-color-emphasis-400);flex-grow:1;justify-content:center;align-items:center;margin-bottom:5px;display:flex}.reactodia-connections-menu__objects-no-results{color:var(--reactodia-color-emphasis-400);font-size:x-large;font-weight:700;position:relative}.reactodia-connections-menu__objects-list{border:var(--reactodia-border-width-base) solid var(--reactodia-border-color-base);border-radius:var(--reactodia-border-radius-base);flex-grow:1;height:0;margin-bottom:5px;padding:5px;overflow:hidden scroll}.reactodia-connections-menu__objects-statusbar{white-space:nowrap;justify-content:flex-end;height:25px;display:flex}.reactodia-connections-menu__objects-extra{color:var(--reactodia-color-emphasis-600);cursor:help}.reactodia-connections-menu__objects-spacer{flex:auto}.reactodia-connections-menu__objects-add-button{height:25px;padding-top:1px;padding-bottom:2px}.reactodia-connections-menu__objects-add-button:not(:last-child){margin-right:var(--reactodia-spacing-horizontal)}.reactodia-dialog{background:var(--reactodia-background-color-surface);border:var(--reactodia-dialog-border-width) solid var(--reactodia-dialog-border-color);border-radius:var(--reactodia-dialog-border-radius);box-shadow:var(--reactodia-canvas-box-shadow);flex-direction:column;display:flex;position:absolute}.reactodia-dialog__header{display:flex}.reactodia-dialog__caption{text-overflow:ellipsis;white-space:nowrap;flex:auto;margin:8px 0 0 8px;font-weight:700;overflow:hidden}.reactodia-dialog__close-button{color:var(--reactodia-button-default-color);border-top-left-radius:unset;border-bottom-right-radius:unset;transition:background-color var(--reactodia-transition-duration);background:0 0;border:none;padding:6px}.reactodia-dialog__close-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-dialog__close-button:hover{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-focus)}.reactodia-halo{left:calc(var(--reactodia-halo-left) - var(--reactodia-selection-single-box-margin));top:calc(var(--reactodia-halo-top) - var(--reactodia-selection-single-box-margin));width:calc(var(--reactodia-halo-width) + 2 * var(--reactodia-selection-single-box-margin));height:calc(var(--reactodia-halo-height) + 2 * var(--reactodia-selection-single-box-margin));pointer-events:none;border:1.5px dashed var(--reactodia-selection-single-box-color);border-radius:var(--reactodia-border-radius-s);box-shadow:var(--reactodia-selection-single-box-shadow);position:absolute}.reactodia-halo--resizable{border:unset;box-shadow:unset}.reactodia-halo__resizer{--reactodia-resizable-box-border-width:3px;--reactodia-resizable-box-color:var(--reactodia-color-primary-lighter);left:var(--reactodia-selection-single-box-margin);top:var(--reactodia-selection-single-box-margin);right:var(--reactodia-selection-single-box-margin);bottom:var(--reactodia-selection-single-box-margin);position:absolute}.reactodia-halo__resizer>*{pointer-events:all}.reactodia-halo-link__label-highlight{pointer-events:none;border-bottom:2px solid var(--reactodia-selection-link-color);position:absolute}.reactodia-halo-link__path-highlight{fill:none;stroke:var(--reactodia-selection-link-color);stroke-width:4px;opacity:.3}.reactodia-instances-search{flex-direction:column;flex:auto;display:flex;overflow:hidden}.reactodia-instances-search__criteria{flex-shrink:0;margin:10px 0}.reactodia-instances-search--controlled .reactodia-instances-search__criteria{margin-top:0;margin-bottom:0}.reactodia-instances-search__criterions{margin-bottom:10px;padding-left:15px}.reactodia-instances-search__criterions:empty{margin-bottom:0}.reactodia-instances-search__criterion{width:100%;display:block;position:relative}.reactodia-instances-search__criterion-class,.reactodia-instances-search__criterion-element,.reactodia-instances-search__criterion-link-type{border:var(--reactodia-color-primary) 1px dashed;color:var(--reactodia-color-primary-contrast-foreground);background-color:var(--reactodia-color-primary-contrast-background);word-wrap:break-word;padding:0 .5em}.reactodia-instances-search__link-direction-in,.reactodia-instances-search__link-direction-out{opacity:.5;vertical-align:middle;background-color:currentColor;width:1em;height:1em;display:inline-block;-webkit-mask-position:0 0;mask-position:0 0;-webkit-mask-size:contain;mask-size:contain;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat}.reactodia-instances-search__link-direction-in{-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%205.9,6%20h%20-1%20V%203%20h%2010%20v%2010%20h%20-10%20v%20-3%20h%201%20v%202%20h%208%20V%204%20h%20-8%20z'%20/%3e%3cpath%20d='M%201.5,7.5%20H%207.771919%20L%206.44298,6%207.10745,5.25%209.4,7.8376461%20V%208.1623539%20L%207.10745,10.75%206.44298,10%207.771919,8.5%20H%201.5%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%205.9,6%20h%20-1%20V%203%20h%2010%20v%2010%20h%20-10%20v%20-3%20h%201%20v%202%20h%208%20V%204%20h%20-8%20z'%20/%3e%3cpath%20d='M%201.5,7.5%20H%207.771919%20L%206.44298,6%207.10745,5.25%209.4,7.8376461%20V%208.1623539%20L%207.10745,10.75%206.44298,10%207.771919,8.5%20H%201.5%20Z'%20/%3e%3c/svg%3e\")}.reactodia-instances-search__link-direction-out{-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2010.5,6%20h%201%20V%203%20h%20-10%20v%2010%20h%2010%20v%20-3%20h%20-1%20v%202%20h%20-8%20V%204%20h%208%20z'%20/%3e%3cpath%20d='m%207,7.5%20h%206.271919%20L%2011.94298,6%2012.60745,5.25%2014.9,7.8376461%20V%208.1623539%20L%2012.60745,10.75%2011.94298,10%2013.271919,8.5%20H%207%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2010.5,6%20h%201%20V%203%20h%20-10%20v%2010%20h%2010%20v%20-3%20h%20-1%20v%202%20h%20-8%20V%204%20h%208%20z'%20/%3e%3cpath%20d='m%207,7.5%20h%206.271919%20L%2011.94298,6%2012.60745,5.25%2014.9,7.8376461%20V%208.1623539%20L%2012.60745,10.75%2011.94298,10%2013.271919,8.5%20H%207%20Z'%20/%3e%3c/svg%3e\")}.reactodia-instances-search__criterion-remove{float:right;margin:0 10px 4px 4px}.reactodia-instances-search__criterion-remove-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-instances-search__text-criteria{margin:0 10px}.reactodia-instances-search__search-button{height:34px}.reactodia-instances-search__search-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-instances-search__rest{border-top:1px solid var(--reactodia-border-color-base);padding:10px 10px 0}.reactodia-instances-search__results{outline:none;margin:0 10px;padding-top:10px;padding-left:0}.reactodia-instances-search__rest-end{margin:5px 0 10px}.reactodia-instances-search__load-more{justify-content:center;align-items:center;width:100%;display:flex}.reactodia-instances-search__load-more:before{content:\"\";background-color:currentColor;width:16px;height:16px;margin-right:5px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.976%2010.072l4.357-4.357.62.618L8.284%2011h-.618L3%206.333l.619-.618%204.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.976%2010.072l4.357-4.357.62.618L8.284%2011h-.618L3%206.333l.619-.618%204.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-instances-search__actions{justify-content:flex-end;gap:var(--reactodia-spacing-horizontal);border-top:1px solid var(--reactodia-border-color-base);height:40px;transition:height .2s;display:flex}.reactodia-instances-search__actions-hidden{visibility:hidden;height:0;padding:0}.reactodia-instances-search__action{margin-top:6px;margin-bottom:6px;margin-left:6px;padding-top:1px;padding-bottom:2px}.reactodia-instances-search__action:last-child{margin-right:6px}.reactodia-link-action{cursor:pointer;opacity:.8;transition:opacity var(--reactodia-transition-duration);border:0;outline:none;padding:0;position:absolute}.reactodia-link-action:hover{opacity:1}.reactodia-link-action[disabled]{cursor:not-allowed;opacity:.5}.reactodia-link-action__spinner{position:absolute}.reactodia-link-action__button{background-color:var(--reactodia-color-gray-400);border-radius:calc(var(--reactodia-link-button-size) * .5);height:var(--reactodia-link-button-size);width:var(--reactodia-link-button-size);background-position:50%;background-repeat:no-repeat;background-size:65%}.reactodia-link-action__endpoint{touch-action:none;color:var(--reactodia-color-primary);background:0 0}.reactodia-link-action__edit{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\")}.reactodia-link-action__delete{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\")}.reactodia-link-action__rename{filter:invert(100%) var(--reactodia-selection-icon-filter);border-radius:var(--reactodia-border-radius-s);cursor:pointer;opacity:.5;transition:opacity var(--reactodia-transition-duration);background:#fff url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\") 50%/80% no-repeat;border:0;outline:none;margin-left:5px;padding:0;position:absolute}.reactodia-link-action__rename:hover{opacity:1}.reactodia-links-toolbox{flex-direction:column;flex:auto;margin-bottom:0;display:flex}.reactodia-links-toolbox__heading{padding:10px}.reactodia-links-toolbox__filter{margin-bottom:5px}.reactodia-links-toolbox__rest{border-top:1px solid var(--reactodia-border-color-base);flex-direction:column;flex:auto;display:flex}.reactodia-links-toolbox__toggle-invisible:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M1.47978%201.4797C1.30227%201.65721%201.28614%201.93498%201.43137%202.13072L1.47978%202.1868L4.1695%204.87652C2.88817%205.77616%201.93052%207.11985%201.53259%208.70952C1.46554%208.97738%201.62834%209.24892%201.89621%209.31598C2.16409%209.38298%202.4356%209.22025%202.50266%208.95232C2.85564%207.54225%203.72742%206.35956%204.88944%205.59626L6.09586%206.80278C5.62419%207.28378%205.33334%207.94278%205.33334%208.66965C5.33334%2010.1424%206.52724%2011.3363%208%2011.3363C8.72694%2011.3363%209.38587%2011.0454%209.86694%2010.5738L13.8131%2014.5201C14.0084%2014.7154%2014.3249%2014.7154%2014.5202%2014.5201C14.6977%2014.3426%2014.7139%2014.0649%2014.5686%2013.8691L14.5202%2013.813L10.4445%209.73692L10.4453%209.73592L9.64527%208.93732L7.732%207.02445L7.73334%207.02392L5.81252%205.10513L5.81334%205.10392L5.05782%204.35024L2.18689%201.4797C1.99163%201.28444%201.67504%201.28444%201.47978%201.4797ZM6.80274%207.51025L9.15947%209.86698C8.85947%2010.1575%208.4506%2010.3363%208%2010.3363C7.07954%2010.3363%206.33334%209.59012%206.33334%208.66965C6.33334%208.21905%206.51216%207.81018%206.80274%207.51025ZM8%203.66658C7.33314%203.66658%206.68607%203.7653%206.07406%203.94992L6.89874%204.77404C7.25594%204.70346%207.62427%204.66658%208%204.66658C10.6154%204.66658%2012.8733%206.45342%2013.4981%208.95538C13.565%209.22325%2013.8364%209.38618%2014.1043%209.31932C14.3723%209.25238%2014.5352%208.98098%2014.4683%208.71305C13.7329%205.7684%2011.077%203.66658%208%203.66658ZM8.1298%206.0061L10.664%208.53992C10.5961%207.16865%209.49814%206.07168%208.1298%206.0061Z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M1.47978%201.4797C1.30227%201.65721%201.28614%201.93498%201.43137%202.13072L1.47978%202.1868L4.1695%204.87652C2.88817%205.77616%201.93052%207.11985%201.53259%208.70952C1.46554%208.97738%201.62834%209.24892%201.89621%209.31598C2.16409%209.38298%202.4356%209.22025%202.50266%208.95232C2.85564%207.54225%203.72742%206.35956%204.88944%205.59626L6.09586%206.80278C5.62419%207.28378%205.33334%207.94278%205.33334%208.66965C5.33334%2010.1424%206.52724%2011.3363%208%2011.3363C8.72694%2011.3363%209.38587%2011.0454%209.86694%2010.5738L13.8131%2014.5201C14.0084%2014.7154%2014.3249%2014.7154%2014.5202%2014.5201C14.6977%2014.3426%2014.7139%2014.0649%2014.5686%2013.8691L14.5202%2013.813L10.4445%209.73692L10.4453%209.73592L9.64527%208.93732L7.732%207.02445L7.73334%207.02392L5.81252%205.10513L5.81334%205.10392L5.05782%204.35024L2.18689%201.4797C1.99163%201.28444%201.67504%201.28444%201.47978%201.4797ZM6.80274%207.51025L9.15947%209.86698C8.85947%2010.1575%208.4506%2010.3363%208%2010.3363C7.07954%2010.3363%206.33334%209.59012%206.33334%208.66965C6.33334%208.21905%206.51216%207.81018%206.80274%207.51025ZM8%203.66658C7.33314%203.66658%206.68607%203.7653%206.07406%203.94992L6.89874%204.77404C7.25594%204.70346%207.62427%204.66658%208%204.66658C10.6154%204.66658%2012.8733%206.45342%2013.4981%208.95538C13.565%209.22325%2013.8364%209.38618%2014.1043%209.31932C14.3723%209.25238%2014.5352%208.98098%2014.4683%208.71305C13.7329%205.7684%2011.077%203.66658%208%203.66658ZM8.1298%206.0061L10.664%208.53992C10.5961%207.16865%209.49814%206.07168%208.1298%206.0061Z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-links-toolbox__toggle-lines-only:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3%209l2.146%202.146-.707.708-3-3v-.708l3-3%20.707.708L3%208h10l-2.146-2.146.707-.708%203%203v.708l-3%203-.707-.707L13%209H3z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3%209l2.146%202.146-.707.708-3-3v-.708l3-3%20.707.708L3%208h10l-2.146-2.146.707-.708%203%203v.708l-3%203-.707-.707L13%209H3z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-links-toolbox__toggle-visible:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M7.99993%206.00316C9.47266%206.00316%2010.6666%207.19708%2010.6666%208.66981C10.6666%2010.1426%209.47266%2011.3365%207.99993%2011.3365C6.52715%2011.3365%205.33324%2010.1426%205.33324%208.66981C5.33324%207.19708%206.52715%206.00316%207.99993%206.00316ZM7.99993%207.00315C7.07946%207.00315%206.33324%207.74935%206.33324%208.66981C6.33324%209.59028%207.07946%2010.3365%207.99993%2010.3365C8.9204%2010.3365%209.6666%209.59028%209.6666%208.66981C9.6666%207.74935%208.9204%207.00315%207.99993%207.00315ZM7.99993%203.66675C11.0756%203.66675%2013.7307%205.76675%2014.4673%208.70968C14.5344%208.97755%2014.3716%209.24908%2014.1037%209.31615C13.8358%209.38315%2013.5643%209.22041%2013.4973%208.95248C12.8713%206.45205%2010.6141%204.66675%207.99993%204.66675C5.38454%204.66675%203.12664%206.45359%202.50182%208.95555C2.43491%209.22341%202.16348%209.38635%201.89557%209.31948C1.62766%209.25255%201.46471%208.98115%201.53162%208.71321C2.26701%205.76856%204.9229%203.66675%207.99993%203.66675Z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M7.99993%206.00316C9.47266%206.00316%2010.6666%207.19708%2010.6666%208.66981C10.6666%2010.1426%209.47266%2011.3365%207.99993%2011.3365C6.52715%2011.3365%205.33324%2010.1426%205.33324%208.66981C5.33324%207.19708%206.52715%206.00316%207.99993%206.00316ZM7.99993%207.00315C7.07946%207.00315%206.33324%207.74935%206.33324%208.66981C6.33324%209.59028%207.07946%2010.3365%207.99993%2010.3365C8.9204%2010.3365%209.6666%209.59028%209.6666%208.66981C9.6666%207.74935%208.9204%207.00315%207.99993%207.00315ZM7.99993%203.66675C11.0756%203.66675%2013.7307%205.76675%2014.4673%208.70968C14.5344%208.97755%2014.3716%209.24908%2014.1037%209.31615C13.8358%209.38315%2013.5643%209.22041%2013.4973%208.95248C12.8713%206.45205%2010.6141%204.66675%207.99993%204.66675C5.38454%204.66675%203.12664%206.45359%202.50182%208.95555C2.43491%209.22341%202.16348%209.38635%201.89557%209.31948C1.62766%209.25255%201.46471%208.98115%201.53162%208.71321C2.26701%205.76856%204.9229%203.66675%207.99993%203.66675Z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-links-toolbox__links-heading{margin:4px 6px;font-size:14px;font-weight:700}.reactodia-links-toolbox__links-heading>*{font-weight:400}.reactodia-links-toolbox__links{flex-direction:column;margin:0;padding:0;list-style:none;display:flex}.reactodia-links-toolbox__link-item{word-break:break-word;padding:0 0 0 5px}.reactodia-links-toolbox__link-item:last-child{margin-bottom:0}.reactodia-links-toolbox__link-item>div{display:inline}.reactodia-links-toolbox__link-title{font-size:16px;display:inline}.reactodia-links-toolbox__link-buttons{float:left;margin-top:2px;margin-bottom:2px;margin-right:6px}.reactodia-links-toolbox__filter-button{float:right;cursor:pointer;opacity:.4;width:20px;height:20px;transition:opacity var(--reactodia-transition-duration);background-color:currentColor;margin:3px;display:none;-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2014.5,3.5%20-1,-1%20m%200,0%20H%202.5%20V%203.0652324%20L%206.9861526,8%207.5,8.5652322%20V%2012.5103%20l%201,-0.733333%20V%208.5489178%20l%205,-5.4999998%20z%20m%200,0.548918%20V%204.6%20m%200,0%20-4,4.4%20v%203.3%20l%20-3,2.2%20V%209%20l%20-5,-5.5%20v%20-2%20h%2013%20v%202'%20/%3e%3cpath%20d='M%203.0187424,8%20H%204%20v%201.5%20h%201.5%20v%201%20L%204,10.467287%20V%2012%20H%203%20V%2010.5%20H%201.5%20v%20-1%20H%203%20Z'%20/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2014.5,3.5%20-1,-1%20m%200,0%20H%202.5%20V%203.0652324%20L%206.9861526,8%207.5,8.5652322%20V%2012.5103%20l%201,-0.733333%20V%208.5489178%20l%205,-5.4999998%20z%20m%200,0.548918%20V%204.6%20m%200,0%20-4,4.4%20v%203.3%20l%20-3,2.2%20V%209%20l%20-5,-5.5%20v%20-2%20h%2013%20v%202'%20/%3e%3cpath%20d='M%203.0187424,8%20H%204%20v%201.5%20h%201.5%20v%201%20L%204,10.467287%20V%2012%20H%203%20V%2010.5%20H%201.5%20v%20-1%20H%203%20Z'%20/%3e%3c/svg%3e\");-webkit-mask-size:20px 20px;mask-size:20px 20px}.reactodia-links-toolbox__link-item:hover .reactodia-links-toolbox__filter-button{opacity:1}.reactodia-links-toolbox__no-results{margin-top:10px}.reactodia-navigator{background:var(--reactodia-navigator-background-fill);border:var(--reactodia-border-width-base) solid var(--reactodia-border-color-base);border-radius:var(--reactodia-border-radius-s);box-shadow:var(--reactodia-canvas-box-shadow);display:block;overflow:hidden}.reactodia-navigator--animated{transition:width var(--reactodia-transition-duration), height var(--reactodia-transition-duration)}.reactodia-navigator--collapsed{width:24px;height:24px}.reactodia-navigator>canvas{transition:opacity var(--reactodia-transition-duration)}.reactodia-navigator--expanded>canvas{opacity:1}.reactodia-navigator--collapsed>canvas{opacity:0}.reactodia-navigator__toggle{outline-offset:-5px;opacity:.5;transition:opacity var(--reactodia-transition-duration);background:0 0;border:none;padding:4px;position:absolute}.reactodia-navigator__toggle:not(:disabled):hover{opacity:1}.reactodia-navigator__toggle:disabled{opacity:.2}.reactodia-navigator--expanded .reactodia-navigator__toggle{top:5px;left:5px}.reactodia-navigator--collapsed .reactodia-navigator__toggle{top:0;left:0}.reactodia-navigator__toggle-icon{width:18px;height:18px;-webkit-mask-size:18px 18px;mask-size:18px 18px;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat}.reactodia-navigator--expanded .reactodia-navigator__toggle-icon{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M10%207v1H5V7h5z'/%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%201h12l.5.5v12l-.5.5h-12l-.5-.5v-12l.5-.5zM2%2013h11V2H2v11z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M10%207v1H5V7h5z'/%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%201h12l.5.5v12l-.5.5h-12l-.5-.5v-12l.5-.5zM2%2013h11V2H2v11z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-navigator--collapsed .reactodia-navigator__toggle-icon{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%201h12l.5.5v12l-.5.5h-12l-.5-.5v-12l.5-.5zM2%2013h11V2H2v11z'/%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%204H7v3H4v1h3v3h1V8h3V7H8V4z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%201h12l.5.5v12l-.5.5h-12l-.5-.5v-12l.5-.5zM2%2013h11V2H2v11z'/%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%204H7v3H4v1h3v3h1V8h3V7H8V4z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-selection{--reactodia-dock-margin-x:2px;--reactodia-dock-margin-y:2px;position:absolute}.reactodia-selection__selectedBox{touch-action:none;border-radius:var(--reactodia-border-radius-s);box-shadow:var(--reactodia-selection-multiple-box-shadow);cursor:move;-webkit-user-select:none;user-select:none;background-color:#ffa6000f;border:1px solid orange;position:absolute}.reactodia-selection__selectedActions{position:absolute}.reactodia-selection__selectedItem{background-color:#ffa60029;border:1px dashed orange;position:absolute}.reactodia-selection__highlightedBox{border-radius:var(--reactodia-border-radius-s);background-color:#00dbff21;border:2px dashed #00b3d1;position:absolute}.reactodia-selection__highlightedItem{background:#00bc7e54;border:1px dashed #00bc7e;position:absolute}.reactodia-selection-action{cursor:pointer;pointer-events:auto;width:20px;height:20px;filter:var(--reactodia-selection-icon-filter);opacity:.5;transition:opacity var(--reactodia-transition-duration);background-color:#0000;background-position:50%;background-repeat:no-repeat;background-size:contain;border:0;outline:none;padding:0;position:absolute}.reactodia-selection-action:hover{opacity:1}.reactodia-selection-action[disabled]{cursor:not-allowed;opacity:.2}.reactodia-selection-action--dock-nw{left:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);top:calc(-22px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-n{margin-left:calc(-11px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);left:50%;top:calc(-11px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-ne{right:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);top:calc(-22px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-e{right:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);margin-top:calc(-11px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px);top:50%}.reactodia-selection-action--dock-se{right:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);bottom:calc(-22px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-s{margin-left:calc(-11px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);left:50%;bottom:calc(-22px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-sw{left:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);bottom:calc(-22px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px)}.reactodia-selection-action--dock-w{left:calc(-22px - var(--reactodia-dock-margin-x,0px) + var(--reactodia-dock-x) * 22px);margin-top:calc(-11px - var(--reactodia-dock-margin-y,0px) + var(--reactodia-dock-y) * 22px);top:50%}.reactodia-selection-action__spinner{width:20px;height:20px;position:absolute}.reactodia-selection-action__remove{background-image:url(\"data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='UTF-8'%20standalone='no'?%3e%3csvg%20xmlns:svg='http://www.w3.org/2000/svg'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20110%20110'%20version='1.1'%20width='500'%20height='500'%3e%3cpath%20d='M%2082.182422,8.0521107%20C%2056.44775,-6.9018137%2023.463185,1.8377258%208.5092706,27.57238%20-6.4446434,53.307034%202.2948357,86.291616%2028.029508,101.24554%2053.76418,116.19946%2086.748746,107.45993%20101.70266,81.725271%20116.65657,55.990616%20107.91709,23.006035%2082.182422,8.0521107%20Z%20M%2071.75602,28.578553%2082.28685,38.846984%2065.453479,56.373296%2083.064738,73.314301%2072.796937,83.844485%2055.169736,66.657039%2039.010998,83.228711%2028.480167,72.96028%2044.639378,56.389069%2028.415211,40.568631%2038.683013,30.038446%2054.938893,45.85871%20Z'%20style='fill:%23000000;stroke-width:1.06812763'%20/%3e%3c/svg%3e\")}.reactodia-selection-action__delete{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\")}.reactodia-selection-action__zoomToFit{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M%201.5,6%20V%201.5%20H%206%20v%201%20H%202.5%20V%206%20Z'%20/%3e%3cpath%20d='m%2010,2.5%20v%20-1%20h%204.5%20V%206%20h%20-1%20V%202.5%20Z'%20/%3e%3cpath%20d='m%201.5,10%20v%204.5%20H%206%20v%20-1%20H%202.5%20V%2010%20Z'%20/%3e%3cpath%20d='m%2010,13.5%20v%201%20h%204.5%20V%2010%20h%20-1%20v%203.5%20z'%20/%3e%3c/svg%3e\")}.reactodia-selection-action__layout{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.5%2011h-1.729L8.438%206H9.5l.5-.5v-4L9.5%201h-4l-.5.5v4l.5.5h1.062l-3.333%205H1.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5h-.068L7.5%206.4l3.068%204.6H10.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5zM6%205V2h3v3H6zm-2%207v2H2v-2h2zm9%202h-2v-2h2v2z'/%3e%3c/svg%3e\")}.reactodia-selection-action__expand{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.976%2010.072l4.357-4.357.62.618L8.284%2011h-.618L3%206.333l.619-.618%204.357%204.357z'/%3e%3c/svg%3e\")}.reactodia-selection-action__collapse{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.024%205.928l-4.357%204.357-.62-.618L7.716%205h.618L13%209.667l-.619.618-4.357-4.357z'/%3e%3c/svg%3e\")}.reactodia-selection-action__link{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M4.4%203h3.085a3.4%203.4%200%200%201%203.4%203.4v.205A3.4%203.4%200%200%201%207.485%2010H7V9h.485A2.4%202.4%200%200%200%209.88%206.61V6.4A2.4%202.4%200%200%200%207.49%204H4.4A2.4%202.4%200%200%200%202%206.4v.205A2.394%202.394%200%200%200%204%208.96v1a3.4%203.4%200%200%201-3-3.35V6.4A3.405%203.405%200%200%201%204.4%203zM12%207.04v-1a3.4%203.4%200%200%201%203%203.36v.205A3.405%203.405%200%200%201%2011.605%2013h-3.09A3.4%203.4%200%200%201%205.12%209.61V9.4A3.4%203.4%200%200%201%208.515%206H9v1h-.485A2.4%202.4%200%200%200%206.12%209.4v.205A2.4%202.4%200%200%200%208.515%2012h3.09A2.4%202.4%200%200%200%2014%209.61V9.4a2.394%202.394%200%200%200-2-2.36z'/%3e%3c/svg%3e\")}.reactodia-selection-action__navigate-open{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M9.00885%206.99103L11.0333%2011.0332L6.99114%209.00874L4.96666%204.96655L9.00885%206.99103ZM9.43541%209.4353L8.48072%207.51916L6.56458%206.56447L7.51926%208.48062L9.43541%209.4353Z'/%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.9795%208.5C13.739%2011.4149%2011.4149%2013.739%208.49999%2013.9795V13H7.49999V13.9795C4.57233%2013.7379%202.24067%2011.3945%202.0175%208.46167H3V7.46167H2.02382C2.28141%204.56475%204.59788%202.25996%207.49999%202.02054V3H8.49999V2.02054C11.4149%202.26101%2013.739%204.5851%2013.9795%207.5H13V8.5H13.9795ZM8%2015C11.866%2015%2015%2011.866%2015%208C15%204.13401%2011.866%201%208%201C4.13401%201%201%204.13401%201%208C1%2011.866%204.13401%2015%208%2015Z'/%3e%3c/svg%3e\")}.reactodia-selection-action__navigate-close{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.6%201c1.6.1%203.1.9%204.2%202%201.3%201.4%202%203.1%202%205.1%200%201.6-.6%203.1-1.6%204.4-1%201.2-2.4%202.1-4%202.4-1.6.3-3.2.1-4.6-.7-1.4-.8-2.5-2-3.1-3.5C.9%209.2.8%207.5%201.3%206c.5-1.6%201.4-2.9%202.8-3.8C5.4%201.3%207%20.9%208.6%201zm.5%2012.9c1.3-.3%202.5-1%203.4-2.1.8-1.1%201.3-2.4%201.2-3.8%200-1.6-.6-3.2-1.7-4.3-1-1-2.2-1.6-3.6-1.7-1.3-.1-2.7.2-3.8%201-1.1.8-1.9%201.9-2.3%203.3-.4%201.3-.4%202.7.2%204%20.6%201.3%201.5%202.3%202.7%203%201.2.7%202.6.9%203.9.6zM7.9%207.5L10.3%205l.7.7-2.4%202.5%202.4%202.5-.7.7-2.4-2.5-2.4%202.5-.7-.7%202.4-2.5-2.4-2.5.7-.7%202.4%202.5z'/%3e%3c/svg%3e\")}.reactodia-selection-action__add-to-filter{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='m%2014.5,3.5%20-1,-1%20m%200,0%20H%202.5%20V%203.0652324%20L%206.9861526,8%207.5,8.5652322%20V%2012.5103%20l%201,-0.733333%20V%208.5489178%20l%205,-5.4999998%20z%20m%200,0.548918%20V%204.6%20m%200,0%20-4,4.4%20v%203.3%20l%20-3,2.2%20V%209%20l%20-5,-5.5%20v%20-2%20h%2013%20v%202'%20/%3e%3cpath%20d='M%203.0187424,8%20H%204%20v%201.5%20h%201.5%20v%201%20L%204,10.467287%20V%2012%20H%203%20V%2010.5%20H%201.5%20v%20-1%20H%203%20Z'%20/%3e%3c/svg%3e\")}.reactodia-selection-action__group{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%201h2v1H2v12h1.5v1h-2l-.5-.5v-13l.5-.5zm6%206h-2L5%206.5v-2l.5-.5h2l.5.5v2l-.5.5zM6%206h1V5H6v1zm7.5%201h-3l-.5-.5v-3l.5-.5h3l.5.5v3l-.5.5zM11%206h2V4h-2v2zm-3.5%206h-2l-.5-.5v-2l.5-.5h2l.5.5v2l-.5.5zM6%2011h1v-1H6v1zm7.5%202h-3l-.5-.5v-3l.5-.5h3l.5.5v3l-.5.5zM11%2012h2v-2h-2v2zm-1-2H8v1h2v-1zm0-5H8v1h2V5z'/%3e%3c/svg%3e\")}.reactodia-selection-action__ungroup{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M2.9%201L5%203.1l-.8.7L3%202.6V7H2V2.5L.8%203.8l-.7-.7L2.2%201h.7zM3%2013.4V9H2v4.4L.8%2012.2l-.7.7L2.2%2015h.7L5%2012.9l-.7-.7L3%2013.4zM8.5%207h-2L6%206.5v-2l.5-.5h2l.5.5v2l-.5.5zM7%206h1V5H7v1zm7.5%201h-3l-.5-.5v-3l.5-.5h3l.5.5v3l-.5.5zM12%206h2V4h-2v2zm-3.5%206h-2l-.5-.5v-2l.5-.5h2l.5.5v2l-.5.5zM7%2011h1v-1H7v1zm7.5%202h-3l-.5-.5v-3l.5-.5h3l.5.5v3l-.5.5zM12%2012h2v-2h-2v2zm-1-2H9v1h2v-1zm0-5H9v1h2V5z'/%3e%3c/svg%3e\")}.reactodia-selection-action__establish-link{touch-action:none;background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7%201H6v3H4.5l-.5.5V8a4%204%200%200%200%203.5%203.969V15h1v-3.031A4%204%200%200%200%2012%208V4.5l-.5-.5H10V1H9v3H7V1zm3.121%209.121A3%203%200%200%201%205%208V5h6v3a3%203%200%200%201-.879%202.121z'/%3e%3c/svg%3e\")}.reactodia-selection-action__annotate{background-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1.5%202h13l.5.5v10l-.5.5h-13l-.5-.5v-10l.5-.5zM2%203v9h12V3H2zm2%202h8v1H4V5zm6%202H4v1h6V7zM4%209h4v1H4V9z'/%3e%3c/svg%3e\")}.reactodia-toolbar{white-space:nowrap;pointer-events:none;display:flex}.reactodia-toolbar>*{pointer-events:auto;white-space:normal}.reactodia-toolbar--drop-up{align-items:flex-end}.reactodia-toolbar__menu{z-index:calc(var(--reactodia-z-index-base) + 50);margin-right:5px}.reactodia-toolbar-action__spacer{flex:auto;min-width:8px}.reactodia-toolbar-action__hotkey{color:var(--reactodia-color-secondary-darkest)}.reactodia-toolbar-action__open{align-items:center;display:flex}.reactodia-toolbar-action__open:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M1.5%2014h11l.48-.37%202.63-7-.48-.63H14V3.5l-.5-.5H7.71l-.86-.85L6.5%202h-5l-.5.5v11l.5.5zM2%203h4.29l.86.85.35.15H13v2H8.5l-.35.15-.86.85H3.5l-.47.34-1%203.08L2%203zm10.13%2010H2.19l1.67-5H7.5l.35-.15.86-.85h5.79l-2.37%206z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M1.5%2014h11l.48-.37%202.63-7-.48-.63H14V3.5l-.5-.5H7.71l-.86-.85L6.5%202h-5l-.5.5v11l.5.5zM2%203h4.29l.86.85.35.15H13v2H8.5l-.35.15-.86.85H3.5l-.47.34-1%203.08L2%203zm10.13%2010H2.19l1.67-5H7.5l.35-.15.86-.85h5.79l-2.37%206z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__open:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__open-input{display:none}.reactodia-toolbar-action__save{align-items:center;display:flex}.reactodia-toolbar-action__save:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.353%201.146l1.5%201.5L15%203v11.5l-.5.5h-13l-.5-.5v-13l.5-.5H13l.353.146zM2%202v12h12V3.208L12.793%202H11v4H4V2H2zm6%200v3h2V2H8z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.353%201.146l1.5%201.5L15%203v11.5l-.5.5h-13l-.5-.5v-13l.5-.5H13l.353.146zM2%202v12h12V3.208L12.793%202H11v4H4V2H2zm6%200v3h2V2H8z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__save:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__clear-all{align-items:center;display:flex}.reactodia-toolbar-action__clear-all:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__clear-all:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__export-image{align-items:center;display:flex}.reactodia-toolbar-action__export-image:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M2%202h6v3.5l.5.5H12v1h1V4.8l-.15-.36-3.28-3.3L9.22%201H1.5l-.5.5v13l.5.5H5v-1H2V2zm7%200l3%203H9V2zm5.5%206h-8l-.5.5v6l.5.5h8l.5-.5v-6l-.5-.5zM14%209v4l-1.63-1.6h-.71l-1.16%201.17-2.13-2.13h-.71L7%2011.1V9h7zm-2.8%204.27l.81-.81L13.55%2014h-1.62l-.73-.73zM7%2014v-1.49l1-1L10.52%2014H7zm5.5-3.5a.5.5%200%201%200%200-1%20.5.5%200%200%200%200%201z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M2%202h6v3.5l.5.5H12v1h1V4.8l-.15-.36-3.28-3.3L9.22%201H1.5l-.5.5v13l.5.5H5v-1H2V2zm7%200l3%203H9V2zm5.5%206h-8l-.5.5v6l.5.5h8l.5-.5v-6l-.5-.5zM14%209v4l-1.63-1.6h-.71l-1.16%201.17-2.13-2.13h-.71L7%2011.1V9h7zm-2.8%204.27l.81-.81L13.55%2014h-1.62l-.73-.73zM7%2014v-1.49l1-1L10.52%2014H7zm5.5-3.5a.5.5%200%201%200%200-1%20.5.5%200%200%200%200%201z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__export-image:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__print{align-items:center;display:flex}.reactodia-toolbar-action__print:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.85%204.44l-3.28-3.3-.35-.14H2.5l-.5.5V7h1V2h6v3.5l.5.5H13v1h1V4.8l-.15-.36zM10%205V2l3%203h-3zM2.5%208l-.5.5v6l.5.5h11l.5-.5v-6l-.5-.5h-11zM13%2013v1H3V9h10v4zm-8-1h-.32v1H4v-3h1.06c.75%200%201.13.36%201.13%201a.94.94%200%200%201-.32.72A1.33%201.33%200%200%201%205%2012zm-.06-1.45h-.26v.93h.26c.36%200%20.54-.16.54-.47%200-.31-.18-.46-.54-.46zM9%2012.58a1.48%201.48%200%200%200%20.44-1.12c0-1-.53-1.46-1.6-1.46H6.78v3h1.06A1.6%201.6%200%200%200%209%2012.58zm-1.55-.13v-1.9h.33a.94.94%200%200%201%20.7.25.91.91%200%200%201%20.25.67%201%201%200%200%201-.25.72.94.94%200%200%201-.69.26h-.34zm4.45-.61h-.97V13h-.68v-3h1.74v.55h-1.06v.74h.97v.55z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M13.85%204.44l-3.28-3.3-.35-.14H2.5l-.5.5V7h1V2h6v3.5l.5.5H13v1h1V4.8l-.15-.36zM10%205V2l3%203h-3zM2.5%208l-.5.5v6l.5.5h11l.5-.5v-6l-.5-.5h-11zM13%2013v1H3V9h10v4zm-8-1h-.32v1H4v-3h1.06c.75%200%201.13.36%201.13%201a.94.94%200%200%201-.32.72A1.33%201.33%200%200%201%205%2012zm-.06-1.45h-.26v.93h.26c.36%200%20.54-.16.54-.47%200-.31-.18-.46-.54-.46zM9%2012.58a1.48%201.48%200%200%200%20.44-1.12c0-1-.53-1.46-1.6-1.46H6.78v3h1.06A1.6%201.6%200%200%200%209%2012.58zm-1.55-.13v-1.9h.33a.94.94%200%200%201%20.7.25.91.91%200%200%201%20.25.67%201%201%200%200%201-.25.72.94.94%200%200%201-.69.26h-.34zm4.45-.61h-.97V13h-.68v-3h1.74v.55h-1.06v.74h.97v.55z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__print:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__undo:empty:before{content:\"\";background-color:currentColor;width:18px;height:18px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__undo:not(:empty){align-items:center;display:flex}.reactodia-toolbar-action__undo:not(:empty):before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M3.5%202v3.5L4%206h3.5V5H4.979l.941-.941a3.552%203.552%200%201%201%205.023%205.023L5.746%2014.28l.72.72%205.198-5.198A4.57%204.57%200%200%200%205.2%203.339l-.7.7V2h-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__undo:not(:empty):not(:empty):before{margin-right:3px}.reactodia-toolbar-action__redo:empty:before{content:\"\";background-color:currentColor;width:18px;height:18px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.5%202v3.5L12%206H8.5V5h2.521l-.941-.941a3.552%203.552%200%201%200-5.023%205.023l5.197%205.198-.72.72-5.198-5.198A4.57%204.57%200%200%201%2010.8%203.339l.7.7V2h1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.5%202v3.5L12%206H8.5V5h2.521l-.941-.941a3.552%203.552%200%201%200-5.023%205.023l5.197%205.198-.72.72-5.198-5.198A4.57%204.57%200%200%201%2010.8%203.339l.7.7V2h1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__redo:not(:empty){align-items:center;display:flex}.reactodia-toolbar-action__redo:not(:empty):before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.5%202v3.5L12%206H8.5V5h2.521l-.941-.941a3.552%203.552%200%201%200-5.023%205.023l5.197%205.198-.72.72-5.198-5.198A4.57%204.57%200%200%201%2010.8%203.339l.7.7V2h1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.5%202v3.5L12%206H8.5V5h2.521l-.941-.941a3.552%203.552%200%201%200-5.023%205.023l5.197%205.198-.72.72-5.198-5.198A4.57%204.57%200%200%201%2010.8%203.339l.7.7V2h1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__redo:not(:empty):not(:empty):before{margin-right:3px}.reactodia-toolbar-action__layout{align-items:center;display:flex}.reactodia-toolbar-action__layout:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.5%2011h-1.729L8.438%206H9.5l.5-.5v-4L9.5%201h-4l-.5.5v4l.5.5h1.062l-3.333%205H1.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5h-.068L7.5%206.4l3.068%204.6H10.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5zM6%205V2h3v3H6zm-2%207v2H2v-2h2zm9%202h-2v-2h2v2z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.5%2011h-1.729L8.438%206H9.5l.5-.5v-4L9.5%201h-4l-.5.5v4l.5.5h1.062l-3.333%205H1.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5h-.068L7.5%206.4l3.068%204.6H10.5l-.5.5v3l.5.5h3l.5-.5v-3l-.5-.5zM6%205V2h3v3H6zm-2%207v2H2v-2h2zm9%202h-2v-2h2v2z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-toolbar-action__layout:not(:empty):before{margin-right:3px}.reactodia-toolbar-action__language-selector{float:left;height:var(--reactodia-toolbar-height);background:var(--reactodia-button-default-background-color);border:var(--reactodia-button-border-width) solid var(--reactodia-button-default-border-color);border-radius:var(--reactodia-button-border-radius);align-items:center;padding:0 5px;display:flex;position:relative}.reactodia-toolbar-action__language-selector:focus-within{border-color:var(--reactodia-input-border-color-focus)}.reactodia-toolbar-action__language-selector select{height:var(--reactodia-toolbar-height);background-color:var(--reactodia-button-default-background-color);color:inherit;font:inherit;line-height:inherit;height:calc(var(--reactodia-toolbar-height) - 4px);border:none;outline:none;margin:0}.reactodia-toolbar-action__language-selector label{margin-right:5px;display:inline-block}.reactodia-toolbar-action__language-selector label:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.5%201a6.5%206.5%200%201%201%200%2013%206.5%206.5%200%200%201%200-13zm4.894%204a5.527%205.527%200%200%200-3.053-2.676c.444.84.765%201.74.953%202.676h2.1zm.582%202.995A5.11%205.11%200%200%200%2014%207.5a5.464%205.464%200%200%200-.213-1.5h-2.342c.032.331.055.664.055%201a10.114%2010.114%200%200%201-.206%202h2.493c.095-.329.158-.665.19-1.005zm-3.535%200l.006-.051A9.04%209.04%200%200%200%2010.5%207a8.994%208.994%200%200%200-.076-1H6.576A8.82%208.82%200%200%200%206.5%207a8.98%208.98%200%200%200%20.233%202h3.534c.077-.332.135-.667.174-1.005zM10.249%205a8.974%208.974%200%200%200-1.255-2.97C8.83%202.016%208.666%202%208.5%202a3.62%203.62%200%200%200-.312.015l-.182.015L8%202.04A8.97%208.97%200%200%200%206.751%205h3.498zM5.706%205a9.959%209.959%200%200%201%20.966-2.681A5.527%205.527%200%200%200%203.606%205h2.1zM3.213%206A5.48%205.48%200%200%200%203%207.5%205.48%205.48%200%200%200%203.213%209h2.493A10.016%2010.016%200%200%201%205.5%207c0-.336.023-.669.055-1H3.213zm2.754%204h-2.36a5.515%205.515%200%200%200%203.819%202.893A10.023%2010.023%200%200%201%205.967%2010zM8.5%2012.644A8.942%208.942%200%200%200%209.978%2010H7.022A8.943%208.943%200%200%200%208.5%2012.644zM11.033%2010a10.024%2010.024%200%200%201-1.459%202.893A5.517%205.517%200%200%200%2013.393%2010h-2.36z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.5%201a6.5%206.5%200%201%201%200%2013%206.5%206.5%200%200%201%200-13zm4.894%204a5.527%205.527%200%200%200-3.053-2.676c.444.84.765%201.74.953%202.676h2.1zm.582%202.995A5.11%205.11%200%200%200%2014%207.5a5.464%205.464%200%200%200-.213-1.5h-2.342c.032.331.055.664.055%201a10.114%2010.114%200%200%201-.206%202h2.493c.095-.329.158-.665.19-1.005zm-3.535%200l.006-.051A9.04%209.04%200%200%200%2010.5%207a8.994%208.994%200%200%200-.076-1H6.576A8.82%208.82%200%200%200%206.5%207a8.98%208.98%200%200%200%20.233%202h3.534c.077-.332.135-.667.174-1.005zM10.249%205a8.974%208.974%200%200%200-1.255-2.97C8.83%202.016%208.666%202%208.5%202a3.62%203.62%200%200%200-.312.015l-.182.015L8%202.04A8.97%208.97%200%200%200%206.751%205h3.498zM5.706%205a9.959%209.959%200%200%201%20.966-2.681A5.527%205.527%200%200%200%203.606%205h2.1zM3.213%206A5.48%205.48%200%200%200%203%207.5%205.48%205.48%200%200%200%203.213%209h2.493A10.016%2010.016%200%200%201%205.5%207c0-.336.023-.669.055-1H3.213zm2.754%204h-2.36a5.515%205.515%200%200%200%203.819%202.893A10.023%2010.023%200%200%201%205.967%2010zM8.5%2012.644A8.942%208.942%200%200%200%209.978%2010H7.022A8.943%208.943%200%200%200%208.5%2012.644zM11.033%2010a10.024%2010.024%200%200%201-1.459%202.893A5.517%205.517%200%200%200%2013.393%2010h-2.36z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-btn+.reactodia-toolbar-action__language-selector{margin-left:5px}.reactodia-unified-search{border:var(--reactodia-input-border-width) solid var(--reactodia-input-border-color);border-radius:var(--reactodia-input-border-radius);background-color:var(--reactodia-background-color-surface);transition:border-color .15s ease-in-out}.reactodia-unified-search:focus-within{border-color:var(--reactodia-input-border-color-focus)}.reactodia-unified-search.reactodia-dropdown--expanded{box-shadow:var(--reactodia-canvas-box-shadow)}.reactodia-unified-search__toggle{border:var(--reactodia-input-border-width) solid transparent;border-radius:var(--reactodia-input-border-radius);align-items:stretch;height:28px;display:flex}.reactodia-unified-search .reactodia-dropdown__content{border-bottom-left-radius:var(--reactodia-input-border-radius);border-bottom-right-radius:var(--reactodia-input-border-radius)}.reactodia-unified-search__search-input{min-width:0;color:var(--reactodia-input-color);background-color:var(--reactodia-background-color-surface);border:none;flex:auto;margin:0;padding:6px 44px 6px 12px;display:block}.reactodia-unified-search__search-input::placeholder{color:var(--reactodia-input-color-placeholder)}.reactodia-unified-search__search-input:focus{outline:0}.reactodia-unified-search__search-icon{cursor:text;background:0 0;border:none;align-self:center;padding:0 8px;position:absolute;right:0}.reactodia-unified-search__search-icon:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='24'%20height='24'%20viewBox='0%200%2024%2024'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M15.25%200a8.25%208.25%200%200%200-6.18%2013.72L1%2022.88l1.12%201%208.05-9.12A8.251%208.251%200%201%200%2015.25.01V0zm0%2015a6.75%206.75%200%201%201%200-13.5%206.75%206.75%200%200%201%200%2013.5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-unified-search__collapse-button,.reactodia-unified-search__clear-button{color:var(--reactodia-button-default-color);border-radius:var(--reactodia-input-border-radius);transition:background-color var(--reactodia-transition-duration);background:0 0;border:0;align-self:center;padding:6px;position:absolute;right:0}.reactodia-unified-search__collapse-button:hover,.reactodia-unified-search__clear-button:hover{color:var(--reactodia-button-default-color-focus);background-color:var(--reactodia-button-default-background-color-focus)}.reactodia-dropdown--down .reactodia-unified-search__collapse-button,.reactodia-dropdown--down .reactodia-unified-search__clear-button{border-top-left-radius:unset}.reactodia-dropdown--down.reactodia-dropdown--expanded .reactodia-unified-search__collapse-button,.reactodia-dropdown--down.reactodia-dropdown--expanded .reactodia-unified-search__clear-button{border-bottom-right-radius:unset}.reactodia-dropdown--up .reactodia-unified-search__collapse-button,.reactodia-dropdown--up .reactodia-unified-search__clear-button{border-bottom-left-radius:unset}.reactodia-dropdown--up.reactodia-dropdown--expanded .reactodia-unified-search__collapse-button,.reactodia-dropdown--up.reactodia-dropdown--expanded .reactodia-unified-search__clear-button{border-top-right-radius:unset}.reactodia-dropdown--down .reactodia-unified-search__collapse-button:after{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.024%205.928l-4.357%204.357-.62-.618L7.716%205h.618L13%209.667l-.619.618-4.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8.024%205.928l-4.357%204.357-.62-.618L7.716%205h.618L13%209.667l-.619.618-4.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-dropdown--up .reactodia-unified-search__collapse-button:after{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.976%2010.072l4.357-4.357.62.618L8.284%2011h-.618L3%206.333l.619-.618%204.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M7.976%2010.072l4.357-4.357.62.618L8.284%2011h-.618L3%206.333l.619-.618%204.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-unified-search__clear-button:after{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M8%208.707l3.646%203.647.708-.707L8.707%208l3.647-3.646-.707-.708L8%207.293%204.354%203.646l-.707.708L7.293%208l-3.646%203.646.707.708L8%208.707z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-unified-search .reactodia-dropdown__content{position:unset;box-shadow:unset}.reactodia-unified-search__panel{flex-direction:column;display:flex}.reactodia-unified-search__section-tabs{flex-wrap:wrap;gap:6px;margin:8px 10px;display:flex}.reactodia-unified-search__section-tab{text-align:center;white-space:nowrap;vertical-align:middle;touch-action:manipulation;cursor:pointer;-webkit-user-select:none;user-select:none;border-width:var(--reactodia-button-border-width);transition:background-color var(--reactodia-transition-duration);border-style:solid;padding:4px;font-size:13px}.reactodia-unified-search__section{flex-direction:column;flex:auto;display:flex}.reactodia-unified-search__section:not(.reactodia-unified-search__section--active){display:none}.reactodia-zoom-control{box-shadow:var(--reactodia-canvas-box-shadow);flex-direction:column;display:flex}.reactodia-zoom-control .reactodia-btn{border-radius:unset;padding:3px}.reactodia-zoom-control .reactodia-btn+.reactodia-btn{margin-top:calc(-1 * var(--reactodia-button-border-width))}.reactodia-zoom-control>.reactodia-btn:first-child{border-top-left-radius:var(--reactodia-button-border-radius);border-top-right-radius:var(--reactodia-button-border-radius)}.reactodia-zoom-control>.reactodia-btn:last-child{border-bottom-left-radius:var(--reactodia-button-border-radius);border-bottom-right-radius:var(--reactodia-button-border-radius)}.reactodia-zoom-control__zoom-in-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.027%206.149a5.52%205.52%200%200%201-1.27%203.908l4.26%204.26-.7.71-4.26-4.27a5.52%205.52%200%201%201%201.97-4.608zm-5.45%204.888a4.51%204.51%200%200%200%203.18-1.32l-.04.02a4.51%204.51%200%200%200%201.36-3.2%204.5%204.5%200%201%200-4.5%204.5zm2.44-4v-1h-2v-2h-1v2h-2v1h2v2h1v-2h2z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.027%206.149a5.52%205.52%200%200%201-1.27%203.908l4.26%204.26-.7.71-4.26-4.27a5.52%205.52%200%201%201%201.97-4.608zm-5.45%204.888a4.51%204.51%200%200%200%203.18-1.32l-.04.02a4.51%204.51%200%200%200%201.36-3.2%204.5%204.5%200%201%200-4.5%204.5zm2.44-4v-1h-2v-2h-1v2h-2v1h2v2h1v-2h2z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-zoom-control__zoom-out-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.027%206.149a5.52%205.52%200%200%201-1.27%203.908l4.26%204.26-.7.71-4.26-4.27a5.52%205.52%200%201%201%201.97-4.608zm-5.45%204.888a4.51%204.51%200%200%200%203.18-1.32l-.04.02a4.51%204.51%200%200%200%201.36-3.2%204.5%204.5%200%201%200-4.5%204.5zm-2.54-4.98h5v1h-5v-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M12.027%206.149a5.52%205.52%200%200%201-1.27%203.908l4.26%204.26-.7.71-4.26-4.27a5.52%205.52%200%201%201%201.97-4.608zm-5.45%204.888a4.51%204.51%200%200%200%203.18-1.32l-.04.02a4.51%204.51%200%200%200%201.36-3.2%204.5%204.5%200%201%200-4.5%204.5zm-2.54-4.98h5v1h-5v-1z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-zoom-control__zoom-fit-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M%201.5,6%20V%201.5%20H%206%20v%201%20H%202.5%20V%206%20Z'%20/%3e%3cpath%20d='m%2010,2.5%20v%20-1%20h%204.5%20V%206%20h%20-1%20V%202.5%20Z'%20/%3e%3cpath%20d='m%201.5,10%20v%204.5%20H%206%20v%20-1%20H%202.5%20V%2010%20Z'%20/%3e%3cpath%20d='m%2010,13.5%20v%201%20h%204.5%20V%2010%20h%20-1%20v%203.5%20z'%20/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M%201.5,6%20V%201.5%20H%206%20v%201%20H%202.5%20V%206%20Z'%20/%3e%3cpath%20d='m%2010,2.5%20v%20-1%20h%204.5%20V%206%20h%20-1%20V%202.5%20Z'%20/%3e%3cpath%20d='m%201.5,10%20v%204.5%20H%206%20v%20-1%20H%202.5%20V%2010%20Z'%20/%3e%3cpath%20d='m%2010,13.5%20v%201%20h%204.5%20V%2010%20h%20-1%20v%203.5%20z'%20/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-zoom-control__selection-mode-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1%203l1-1h12l1%201v6h-1V3H2v8h5v1H2l-1-1V3zm14.707%209.707L9%206v9.414l2.707-2.707h4zM10%2013V8.414l3.293%203.293h-2L10%2013z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M1%203l1-1h12l1%201v6h-1V3H2v8h5v1H2l-1-1V3zm14.707%209.707L9%206v9.414l2.707-2.707h4zM10%2013V8.414l3.293%203.293h-2L10%2013z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-accordion{flex:auto;width:100%;height:100%;display:flex}.reactodia-accordion--vertical{flex-direction:column}.reactodia-accordion--vertical:not(.reactodia-accordion--resizing):not(:has(.reactodia-accordion-item__handle:focus))>.reactodia-accordion-item{transition:height var(--reactodia-accordion-transition-duration) ease-in-out}.reactodia-accordion--horizontal:not(.reactodia-accordion--resizing):not(:has(.reactodia-accordion-item__handle:focus))>.reactodia-accordion-item{transition:width var(--reactodia-accordion-transition-duration) ease-in-out}.reactodia-accordion-item{flex:auto;display:flex;position:relative}.reactodia-accordion-item--resizing>.reactodia-accordion-item__handle,.reactodia-accordion-item__handle:hover{background:#0000001a}.reactodia-accordion-item--vertical{border-top:var(--reactodia-border-width-base) solid var(--reactodia-border-color-base)}.reactodia-accordion-item--vertical:first-child{border-top-width:0}.reactodia-accordion-item--horizontal{border-right:var(--reactodia-border-width-base) solid var(--reactodia-border-color-base)}.reactodia-accordion-item--horizontal:last-child{border-right-width:0}.reactodia-accordion-item__handle{z-index:calc(var(--reactodia-z-index-base) + 10);position:absolute}.reactodia-accordion-item__handle-vertical{cursor:ns-resize;width:100%;height:5px;bottom:-2.5px;left:0}.reactodia-accordion-item__handle-horizontal{cursor:ew-resize;width:5px;height:100%;top:0;right:-2.5px}.reactodia-accordion-item__handle-btn{background-color:var(--reactodia-button-default-background-color);border:2px solid var(--reactodia-color-primary);cursor:pointer;width:20px;height:20px;z-index:calc(var(--reactodia-z-index-base) + 20);transition:var(--reactodia-accordion-transition-duration);border-radius:10px;justify-content:center;align-items:center;margin-top:-10px;padding:0;display:flex;position:absolute;top:50%;box-shadow:0 0 0 1px #091e4214,0 2px 4px 1px #091e4214}.reactodia-accordion-item__handle-btn:before{content:\"\";width:16px;height:16px;transition:opacity filter var(--reactodia-accordion-transition-duration);color:var(--reactodia-color-emphasis-700);background-color:currentColor;-webkit-mask-position:0 0;mask-position:0 0;-webkit-mask-size:cover;mask-size:cover;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat}.reactodia-accordion-item__handle-btn:hover{background-color:var(--reactodia-color-primary)}.reactodia-accordion-item__handle-btn:hover:before{opacity:1;color:var(--reactodia-color-gray-0)}.reactodia-accordion-item__handle-btn-left{margin-left:-10px;left:100%}.reactodia-accordion-item__handle-btn-left:before{-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M5.928%207.976l4.357%204.357-.618.62L5%208.284v-.618L9.667%203l.618.619-4.357%204.357z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M5.928%207.976l4.357%204.357-.618.62L5%208.284v-.618L9.667%203l.618.619-4.357%204.357z'/%3e%3c/svg%3e\")}.reactodia-accordion-item--collapsed .reactodia-accordion-item__handle-btn-left{margin-left:calc(-1 * min(10px, var(--reactodia-accordion-item-size,0px) - 4px))}.reactodia-accordion-item__handle-btn-right{margin-right:-10px;right:100%}.reactodia-accordion-item__handle-btn-right:before{-webkit-mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\");mask-image:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\")}.reactodia-accordion-item--collapsed .reactodia-accordion-item__handle-btn-right{margin-right:calc(-1 * min(10px, var(--reactodia-accordion-item-size,0px) - 4px))}.reactodia-accordion-item--collapsed .reactodia-accordion-item__handle-btn:before{transform:rotate(180deg)}.reactodia-accordion-item__inner{flex-direction:column;flex:auto;display:flex;overflow:hidden}.reactodia-accordion-item__body{background-color:var(--reactodia-background-color);min-width:var(--reactodia-item-min-width);flex-direction:column;flex:1 1 0;display:flex}.reactodia-accordion-item__header{text-align:unset;background-color:var(--reactodia-color-emphasis-200);cursor:default;outline-offset:-2px;-webkit-touch-callout:none;-webkit-user-select:none;user-select:none;border:none;flex-shrink:0;padding:0 0 0 20px;font-size:16px;position:relative}.reactodia-accordion-item__header:before{border-top:6px solid var(--reactodia-color-emphasis-700);content:\"\";border-left:3.5px solid #0000;border-right:3.5px solid #0000;margin-top:-3px;transition:all .1s;position:absolute;top:50%;left:7px}.reactodia-accordion-item--collapsed .reactodia-accordion-item__inner{background-color:var(--reactodia-background-color-surface)}.reactodia-accordion-item--collapsed .reactodia-accordion-item__header:before{transform:rotate(-90deg)}.reactodia-accordion-item--collapsed .reactodia-accordion-item__body{display:none}.reactodia-workspace{box-sizing:border-box;height:100%;white-space:initial;color:var(--reactodia-font-color-base);font-family:var(--reactodia-font-family-base);font-size:var(--reactodia-font-size-base);line-height:var(--reactodia-line-height-base);flex-direction:column;display:flex}.reactodia-workspace *,.reactodia-workspace :before,.reactodia-workspace :after{box-sizing:inherit}.reactodia-scrollable{flex:1 1 0;overflow-y:scroll}.reactodia-scrollable>*{position:relative}.reactodia-classic-template{float:left;border-style:solid;border-width:1px;border-radius:12px;max-width:450px}.reactodia-classic-template__thumbnail{text-align:center}.reactodia-classic-template__thumbnail-image{max-width:200px}.reactodia-classic-template--expanded .reactodia-classic-template__thumbnail-image{max-width:350px}.reactodia-classic-template__type-line{color:#fff;align-items:baseline;height:18px;margin-top:-3px;margin-bottom:2px;padding:0 7px;display:flex;overflow:hidden}.reactodia-classic-template__type-line-icon{margin-right:4px;position:inherit!important}.reactodia-classic-template__type-line-text-container{text-overflow:ellipsis;flex-grow:1;width:0;overflow:hidden}.reactodia-classic-template__type-line-text{white-space:nowrap;font-size:12px;display:inline}.reactodia-classic-template__body{background:var(--reactodia-element-background-color);border-top-style:solid;border-top-width:1px;border-bottom-right-radius:12px;border-bottom-left-radius:12px;flex-direction:column;padding:7px 10px 8px;display:flex;overflow:hidden}.reactodia-classic-template__label{text-overflow:ellipsis;white-space:nowrap;word-wrap:break-word;margin-bottom:0;font-size:19px;font-weight:100;overflow:hidden}.reactodia-classic-template__expander{width:100%;display:flex;overflow:hidden}.reactodia-classic-template__iri-heading{color:var(--reactodia-color-emphasis-500);margin-right:15px;font-size:12px;font-weight:100}.reactodia-classic-template__iri-container{white-space:nowrap;text-overflow:ellipsis;width:100%;font-size:12px;overflow:hidden}.reactodia-classic-template__iri{color:var(--reactodia-color-emphasis-500)}.reactodia-classic-template__divider{border:0;border-top:1px solid var(--reactodia-border-color-base);height:0;margin:5px 0}.reactodia-classic-template__property-table{max-height:200px;margin-bottom:5px;font-size:15px;font-weight:100;overflow:hidden scroll}.reactodia-classic-template__property-row{white-space:nowrap}.reactodia-classic-template__property-label{text-overflow:ellipsis;vertical-align:top;width:50%;display:inline-block;overflow:hidden}.reactodia-classic-template__property-values{text-overflow:ellipsis;width:50%;display:inline-block;overflow:hidden}.reactodia-classic-template__property-value{text-overflow:ellipsis;width:100%;white-space:initial;padding-right:10px;overflow:hidden}.reactodia-group-paginator{height:22px;display:flex}.reactodia-group-paginator__previous,.reactodia-group-paginator__next{padding:0 5px}.reactodia-group-paginator__previous{align-items:center;display:flex}.reactodia-group-paginator__previous:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M5.928%207.976l4.357%204.357-.618.62L5%208.284v-.618L9.667%203l.618.619-4.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M5.928%207.976l4.357%204.357-.618.62L5%208.284v-.618L9.667%203l.618.619-4.357%204.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-group-paginator__previous:not(:empty):before{margin-right:3px}.reactodia-group-paginator__previous{border-top-right-radius:unset;border-bottom-right-radius:unset}.reactodia-group-paginator__next{align-items:center;display:flex}.reactodia-group-paginator__next:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-group-paginator__next:not(:empty):before{margin-right:3px}.reactodia-group-paginator__next{border-top-left-radius:unset;border-bottom-left-radius:unset}.reactodia-group-paginator__status{text-align:center;border-top:var(--reactodia-button-border-width) solid var(--reactodia-button-default-border-color);border-bottom:var(--reactodia-button-border-width) solid var(--reactodia-button-default-border-color);flex:0 0 60px;line-height:22px}.reactodia-group-paginator__page-size{margin-left:4px}.reactodia-note-annotation{border:2px solid var(--reactodia-color-gray-500);border-radius:var(--reactodia-border-radius-s);align-content:center;width:200px;height:120px;display:grid}.reactodia-note-annotation--variant-default{color:var(--reactodia-font-color-base);background-color:var(--reactodia-element-background-color);border-color:var(--reactodia-color-gray-500)}.reactodia-note-annotation--variant-primary{color:var(--reactodia-color-primary-contrast-foreground);background-color:var(--reactodia-color-primary-contrast-background);border-color:var(--reactodia-color-primary)}.reactodia-note-annotation--variant-success{color:var(--reactodia-color-success-contrast-foreground);background-color:var(--reactodia-color-success-contrast-background);border-color:var(--reactodia-color-success)}.reactodia-note-annotation--variant-info{color:var(--reactodia-color-info-contrast-foreground);background-color:var(--reactodia-color-info-contrast-background);border-color:var(--reactodia-color-info)}.reactodia-note-annotation--variant-warning{color:var(--reactodia-color-warning-contrast-foreground);background-color:var(--reactodia-color-warning-contrast-background);border-color:var(--reactodia-color-warning)}.reactodia-note-annotation--variant-danger{color:var(--reactodia-color-danger-contrast-foreground);background-color:var(--reactodia-color-danger-contrast-background);border-color:var(--reactodia-color-danger)}.reactodia-note-annotation--editing{align-content:unset}.reactodia-note-annotation__editor{overflow-wrap:break-word;pointer-events:none;text-align:center;outline:none;margin:0;padding:10px;font-size:18px}.reactodia-note-annotation--editing .reactodia-note-annotation__editor{cursor:text;pointer-events:all}.reactodia-note-annotation__controls{gap:var(--reactodia-spacing-base);flex-direction:column;width:max-content;display:flex;position:absolute;top:10px;right:-10px;transform:translate(100%)}.reactodia-note-annotation__style-bold{align-items:center;display:flex}.reactodia-note-annotation__style-bold:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-bold%3c/title%3e%3cpath%20d='M18.25,25H9V7h8.5a5.25,5.25,0,0,1,4,8.65A5.25,5.25,0,0,1,18.25,25ZM12,22h6.23a2.25,2.25,0,1,0,0-4.5H12Zm0-7.5h5.5a2.25,2.25,0,1,0,0-4.5H12Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-bold%3c/title%3e%3cpath%20d='M18.25,25H9V7h8.5a5.25,5.25,0,0,1,4,8.65A5.25,5.25,0,0,1,18.25,25ZM12,22h6.23a2.25,2.25,0,1,0,0-4.5H12Zm0-7.5h5.5a2.25,2.25,0,1,0,0-4.5H12Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__style-bold:not(:empty):before{margin-right:3px}.reactodia-note-annotation__style-italic{align-items:center;display:flex}.reactodia-note-annotation__style-italic:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-italic%3c/title%3e%3cpolygon%20points='25%209%2025%207%2012%207%2012%209%2017.14%209%2012.77%2023%207%2023%207%2025%2020%2025%2020%2023%2014.86%2023%2019.23%209%2025%209'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-italic%3c/title%3e%3cpolygon%20points='25%209%2025%207%2012%207%2012%209%2017.14%209%2012.77%2023%207%2023%207%2025%2020%2025%2020%2023%2014.86%2023%2019.23%209%2025%209'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__style-italic:not(:empty):before{margin-right:3px}.reactodia-note-annotation__style-underline{align-items:center;display:flex}.reactodia-note-annotation__style-underline:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-underline%3c/title%3e%3crect%20x='4'%20y='26'%20width='24'%20height='2'/%3e%3cpath%20d='M16,23a7,7,0,0,1-7-7V5h2V16a5,5,0,0,0,10,0V5h2V16A7,7,0,0,1,16,23Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext-underline%3c/title%3e%3crect%20x='4'%20y='26'%20width='24'%20height='2'/%3e%3cpath%20d='M16,23a7,7,0,0,1-7-7V5h2V16a5,5,0,0,0,10,0V5h2V16A7,7,0,0,1,16,23Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__style-underline:not(:empty):before{margin-right:3px}.reactodia-note-annotation__style-strikethrough{align-items:center;display:flex}.reactodia-note-annotation__style-strikethrough:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20width='32'%20height='32'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext--strikethrough%3c/title%3e%3cpath%20d='M28,15H17.9563c-.4522-.1237-.9037-.2324-1.3381-.3352-2.8077-.6641-4.396-1.1506-4.396-3.4231a2.8684,2.8684,0,0,1,.7866-2.145,4.7888,4.7888,0,0,1,3.0137-1.09c2.8291-.07,4.1347.8894,5.2011,2.35l1.6153-1.1792a7.4727,7.4727,0,0,0-6.83-3.1706,6.7726,6.7726,0,0,0-4.4,1.6611,4.8274,4.8274,0,0,0-1.3862,3.5735A4.3723,4.3723,0,0,0,11.9573,15H4v2H17.6519c1.9668.57,3.1432,1.3123,3.1733,3.3579a3.119,3.119,0,0,1-.8623,2.3931A5.8241,5.8241,0,0,1,16.2432,24a6.6344,6.6344,0,0,1-5.1451-2.6912L9.5649,22.593A8.5262,8.5262,0,0,0,16.2119,26c.0088-.0012.042,0,.1,0A7.67,7.67,0,0,0,21.36,24.1812a5.0779,5.0779,0,0,0,1.4648-3.8531A4.952,4.952,0,0,0,21.6753,17H28Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20width='32'%20height='32'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3etext--strikethrough%3c/title%3e%3cpath%20d='M28,15H17.9563c-.4522-.1237-.9037-.2324-1.3381-.3352-2.8077-.6641-4.396-1.1506-4.396-3.4231a2.8684,2.8684,0,0,1,.7866-2.145,4.7888,4.7888,0,0,1,3.0137-1.09c2.8291-.07,4.1347.8894,5.2011,2.35l1.6153-1.1792a7.4727,7.4727,0,0,0-6.83-3.1706,6.7726,6.7726,0,0,0-4.4,1.6611,4.8274,4.8274,0,0,0-1.3862,3.5735A4.3723,4.3723,0,0,0,11.9573,15H4v2H17.6519c1.9668.57,3.1432,1.3123,3.1733,3.3579a3.119,3.119,0,0,1-.8623,2.3931A5.8241,5.8241,0,0,1,16.2432,24a6.6344,6.6344,0,0,1-5.1451-2.6912L9.5649,22.593A8.5262,8.5262,0,0,0,16.2119,26c.0088-.0012.042,0,.1,0A7.67,7.67,0,0,0,21.36,24.1812a5.0779,5.0779,0,0,0,1.4648-3.8531A4.952,4.952,0,0,0,21.6753,17H28Z'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__style-strikethrough:not(:empty):before{margin-right:3px}.reactodia-note-annotation__align-left{align-items:center;display:flex}.reactodia-note-annotation__align-left:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--left%3c/title%3e%3crect%20x='12'%20y='6'%20width='14'%20height='2'/%3e%3crect%20x='12'%20y='12'%20width='10'%20height='2'/%3e%3crect%20x='12'%20y='18'%20width='14'%20height='2'/%3e%3crect%20x='12'%20y='24'%20width='10'%20height='2'/%3e%3crect%20x='6'%20y='4'%20width='2'%20height='24'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--left%3c/title%3e%3crect%20x='12'%20y='6'%20width='14'%20height='2'/%3e%3crect%20x='12'%20y='12'%20width='10'%20height='2'/%3e%3crect%20x='12'%20y='18'%20width='14'%20height='2'/%3e%3crect%20x='12'%20y='24'%20width='10'%20height='2'/%3e%3crect%20x='6'%20y='4'%20width='2'%20height='24'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__align-left:not(:empty):before{margin-right:3px}.reactodia-note-annotation__align-center{align-items:center;display:flex}.reactodia-note-annotation__align-center:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--center%3c/title%3e%3crect%20x='6'%20y='6'%20width='20'%20height='2'/%3e%3crect%20x='10'%20y='12'%20width='12'%20height='2'/%3e%3crect%20x='6'%20y='18'%20width='20'%20height='2'/%3e%3crect%20x='10'%20y='24'%20width='12'%20height='2'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--center%3c/title%3e%3crect%20x='6'%20y='6'%20width='20'%20height='2'/%3e%3crect%20x='10'%20y='12'%20width='12'%20height='2'/%3e%3crect%20x='6'%20y='18'%20width='20'%20height='2'/%3e%3crect%20x='10'%20y='24'%20width='12'%20height='2'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__align-center:not(:empty):before{margin-right:3px}.reactodia-note-annotation__align-right{align-items:center;display:flex}.reactodia-note-annotation__align-right:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--right%3c/title%3e%3crect%20x='6'%20y='6'%20width='14'%20height='2'/%3e%3crect%20x='10'%20y='12'%20width='10'%20height='2'/%3e%3crect%20x='6'%20y='18'%20width='14'%20height='2'/%3e%3crect%20x='10'%20y='24'%20width='10'%20height='2'/%3e%3crect%20x='24'%20y='4'%20width='2'%20height='24'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20id='icon'%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%2032%2032'%3e%3cdefs%3e%3cstyle%3e.cls-1{fill:none;}%3c/style%3e%3c/defs%3e%3ctitle%3ealign--right%3c/title%3e%3crect%20x='6'%20y='6'%20width='14'%20height='2'/%3e%3crect%20x='10'%20y='12'%20width='10'%20height='2'/%3e%3crect%20x='6'%20y='18'%20width='14'%20height='2'/%3e%3crect%20x='10'%20y='24'%20width='10'%20height='2'/%3e%3crect%20x='24'%20y='4'%20width='2'%20height='24'/%3e%3crect%20id='_Transparent_Rectangle_'%20data-name='&lt;Transparent%20Rectangle&gt;'%20class='cls-1'%20width='32'%20height='32'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-note-annotation__align-right:not(:empty):before{margin-right:3px}.reactodia-note-annotation__variant{border-radius:var(--reactodia-border-radius-s);border-top-style:solid;border-top-width:16px;width:16px;height:16px}.reactodia-note-link__path:not([stroke]){stroke:var(--reactodia-color-gray-500)}.reactodia-note-link__path:not([stroke-width]){stroke-width:2px}.reactodia-note-link__label{color:var(--reactodia-color-emphasis-600);font-weight:700}.reactodia-round-entity{background-color:var(--reactodia-element-background-color);border:4px solid var(--reactodia-element-style-color);text-align:center;border-radius:50%;grid-template-rows:1fr 1fr 1fr;align-items:center;width:120px;height:120px;display:grid}.reactodia-round-entity__types{white-space:nowrap;text-overflow:ellipsis;color:#999;place-self:end center;width:65%;font-size:11px;font-style:italic;overflow:hidden}.reactodia-round-entity__label{white-space:nowrap;text-overflow:ellipsis;margin:0 10px;font-size:18px;overflow:hidden}.reactodia-standard-element{float:left;min-width:180px;max-width:400px}.reactodia-standard-element--group{border-radius:var(--reactodia-border-radius-s);border:1px solid;border-color:var(--reactodia-standard-group-color);background:var(--reactodia-element-background-color);padding:4px}.reactodia-standard-element__main{border-radius:var(--reactodia-border-radius-s);border:1px solid;border-color:var(--reactodia-element-style-color);background-color:var(--reactodia-element-style-color)}.reactodia-standard-element__body{border-radius:0 var(--reactodia-border-radius-s) var(--reactodia-border-radius-s) 0;border-left:1px solid;border-left-color:var(--reactodia-element-style-color);background:var(--reactodia-element-background-color);margin-left:8px;padding:3px 0}.reactodia-standard-element--new .reactodia-standard-element__main{background-color:var(--reactodia-element-background-color)}.reactodia-standard-element__body-horizontal{justify-content:space-between;align-items:center;display:flex;overflow:hidden}.reactodia-standard-element__body-content{flex-grow:1;min-width:0;margin-right:12px}.reactodia-standard-element__item{border-radius:var(--reactodia-border-radius-s);border:0;display:flex}.reactodia-standard-element__item-filler{padding:3px;font-size:15px}.reactodia-standard-element__item:not(:first-child),.reactodia-standard-element__item-filler:not(:first-child){margin-top:4px}.reactodia-standard-element__item-stripe{background:var(--reactodia-element-style-color);width:6px}.reactodia-standard-element--new .reactodia-standard-element__item-stripe{background:var(--reactodia-element-background-color);border:1px solid var(--reactodia-element-style-color)}.reactodia-standard-element__item-body{text-overflow:ellipsis;background:var(--reactodia-element-background-color);flex:auto;padding:3px;display:flex;overflow:hidden}.reactodia-standard-element__label{white-space:nowrap;text-overflow:ellipsis;overflow:hidden}.reactodia-standard-element--standalone .reactodia-standard-element__label{font-size:19px}.reactodia-standard-element--group .reactodia-standard-element__label{flex:auto;font-size:15px}.reactodia-standard-element--group .reactodia-standard-element--changed .reactodia-standard-element__label{color:#09f;font-style:italic}.reactodia-standard-element--group .reactodia-standard-element--deleted .reactodia-standard-element__label{color:#ff3b3b;text-decoration:line-through}.reactodia-standard-element__thumbnail{width:50px;height:50px;color:var(--reactodia-element-style-color);flex-shrink:0;justify-content:center;align-items:center;margin:5px 10px;font-size:26px;display:flex}.reactodia-standard-element__thumbnail-image{max-width:100%;max-height:100%}.reactodia-standard-element__thumbnail-icon{object-fit:contain;width:26px;height:26px}.reactodia-standard-element__thumbnail-icon--monochrome{filter:var(--reactodia-monochrome-icon-filter)}.reactodia-standard-element__photo{border-bottom:1px solid;border-color:var(--reactodia-element-style-color)}.reactodia-standard-element__photo-image{border-radius:var(--reactodia-border-radius-s) var(--reactodia-border-radius-s) 0 0;width:100%}.reactodia-standard-element__type{white-space:nowrap;text-overflow:ellipsis;color:#999;font-size:11px;font-style:italic;display:flex;overflow:hidden}.reactodia-standard-element__type-value{text-overflow:ellipsis;flex-grow:1;width:0;overflow:hidden}.reactodia-standard-element__iri{width:100%;display:flex;overflow:hidden}.reactodia-standard-element__iri-key{color:var(--reactodia-color-emphasis-500);margin-right:4px;font-size:12px}.reactodia-standard-element__iri-value{white-space:nowrap;text-overflow:ellipsis;width:100%;font-size:12px;overflow:hidden}.reactodia-standard-element__iri-value a{color:var(--reactodia-color-emphasis-500)}.reactodia-standard-element__hr{border:0;border-top:1px solid var(--reactodia-border-color-base);height:0;margin:5px 0}.reactodia-standard-element__properties{max-height:200px;font-size:15px;overflow:hidden scroll}.reactodia-standard-element__properties-row{white-space:nowrap;grid-template-columns:1fr 1fr;display:grid}.reactodia-standard-element__properties-key{text-overflow:ellipsis;vertical-align:top;display:inline-block;overflow:hidden}.reactodia-standard-element__properties-values{text-overflow:ellipsis;display:inline-block;overflow:hidden}.reactodia-standard-element__properties-value{text-overflow:ellipsis;width:100%;white-space:initial;padding-right:10px;overflow:hidden}.reactodia-standard-element__pinned-props{border-top:1px solid;border-color:var(--reactodia-element-style-color);margin:0 5px}.reactodia-standard-element__dropdown{border:1px solid;border-color:var(--reactodia-element-style-color);border-radius:var(--reactodia-border-radius-s);background-color:var(--reactodia-element-background-color);margin-top:5px}.reactodia-standard-element__dropdown-content{width:100%;padding:9px}.reactodia-standard-element__actions{justify-content:space-between;display:flex}.reactodia-standard-element__delete-button,.reactodia-standard-element__edit-button{align-items:center;min-width:60px;padding:5px;display:flex}.reactodia-standard-element__delete-button{align-items:center;display:flex}.reactodia-standard-element__delete-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10%203h3v1h-1v9l-1%201H4l-1-1V4H2V3h3V2a1%201%200%200%201%201-1h3a1%201%200%200%201%201%201v1zM9%202H6v1h3V2zM4%2013h7V4H4v9zm2-8H5v7h1V5zm1%200h1v7H7V5zm2%200h1v7H9V5z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-standard-element__delete-button:not(:empty):before{margin-right:3px}.reactodia-standard-element__edit-button{align-items:center;display:flex}.reactodia-standard-element__edit-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20d='M13.23%201h-1.46L3.52%209.25l-.16.22L1%2013.59%202.41%2015l4.12-2.36.22-.16L15%204.23V2.77L13.23%201zM2.41%2013.59l1.51-3%201.45%201.45-2.96%201.55zm3.83-2.06L4.47%209.76l8-8%201.77%201.77-8%208z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-standard-element__edit-button:not(:empty):before{margin-right:3px}.reactodia-standard-element__ungroup-one-button{align-items:center;display:flex}.reactodia-standard-element__ungroup-one-button:before{content:\"\";background-color:currentColor;width:16px;height:16px;display:block;-webkit-mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat;mask:url(\"data:image/svg+xml,%3csvg%20width='16'%20height='16'%20viewBox='0%200%2016%2016'%20xmlns='http://www.w3.org/2000/svg'%20fill='currentColor'%3e%3cpath%20fill-rule='evenodd'%20clip-rule='evenodd'%20d='M10.072%208.024L5.715%203.667l.618-.62L11%207.716v.618L6.333%2013l-.618-.619%204.357-4.357z'/%3e%3c/svg%3e\") 0 0/contain no-repeat}.reactodia-standard-element__ungroup-one-button:not(:empty):before{margin-right:3px}.reactodia-standard-element__ungroup-one-button{opacity:0;align-self:center;width:18px;height:18px;margin-left:2px;padding:0;transition:opacity .2s;display:block}.reactodia-standard-element:hover .reactodia-standard-element__ungroup-one-button,.reactodia-standard-element:focus .reactodia-standard-element__ungroup-one-button,.reactodia-overlaid-element--only-selected .reactodia-standard-element__ungroup-one-button{opacity:1}.reactodia-standard-element--group .reactodia-group-paginator{opacity:.4;margin-top:4px;transition:opacity .2s}.reactodia-standard-element--group .reactodia-group-paginator:hover{opacity:1}.reactodia-standard-link--group .reactodia-link-path{stroke-width:2px}.reactodia-standard-link__path:not([stroke]){stroke:var(--reactodia-link-stroke-color)}.reactodia-standard-link__label{font-weight:700}.reactodia-standard-link__label--renamed{font-style:italic;font-weight:400}.reactodia-standard-link__property{text-overflow:ellipsis;max-width:150px;font-size:90%;font-style:normal;font-weight:400;overflow:hidden}.reactodia-standard-link__source-count,.reactodia-standard-link__target-count{color:var(--reactodia-color-emphasis-600);opacity:.8;font-size:12px;font-weight:700}\n/*$vite$:1*/"));			document.head.appendChild(elementStyle);		}	} catch (e) {		console.error("vite-plugin-css-injected-by-js", e);	}})();
import { A as KeyedObserver, B as useLatest, C as DefaultDataFactory, D as hashQuad, E as getLocalName, F as useTranslation, G as moveComparator, H as useSyncStore, I as neverSyncStore, J as multimapDelete, K as multimapAdd, L as useAsync, M as TranslatedText, N as TranslationContext, O as hashTerm, P as TranslationProvider, R as useEventStore, S as isEncodedBlank, T as equalTerms, U as useSyncStoreWithComparator, V as useObservedProperty, W as OrderedMap, Y as shallowArrayEqual, _ as equalLinks, a as subscribeLinkTypes, b as hashLink, c as useResolvedAssetUrl, d as owl, f as rdf, g as equalElements, h as xsd, i as subscribeElementTypes, j as useKeyedSyncStore, k as rdfModel_exports, l as HtmlSpinner, m as schema, n as WorkspaceEventKey, o as subscribePropertyTypes, p as rdfs, q as multimapArrayAdd, r as useWorkspace, s as DefaultDataLocaleProvider, t as WorkspaceContext, u as Spinner, v as equalProperties, w as equalQuads, x as hashSubtypeEdge, y as equalSubtypeEdges, z as useFrameDebouncedStore } from "./common/common-DLtZRGHN.js";
import * as React from "react";
import { createElement } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { HashMap, HashSet, chainHash, dropHighestNonSignBit, hashNumber, hashString } from "@reactodia/hashmap";
import * as N3 from "n3";
import cx from "clsx";
import * as ReactDOM from "react-dom";
import { createPortal, flushSync } from "react-dom";
import { refCountedWorker } from "@reactodia/worker-proxy";
import { hcl } from "d3-color";
import { saveAs } from "file-saver";
//#region src/coreUtils/async.ts
/**
* Transforms `Promise` in such a way that resolved or rejected results
* are mapped to `null` if specified abort `signal` is aborted.
*
* @category Utilities
*/
function mapAbortedToNull(promise, signal) {
	const onResolve = (value) => {
		if (signal && signal.aborted) return null;
		return value;
	};
	const onReject = (err) => {
		if (signal && signal.aborted) return null;
		return Promise.reject(err);
	};
	return promise.then(onResolve, onReject);
}
/**
* Waits a specified timeout in milliseconds the resolves the result promise.
*
* Can be cancelled via specified `AbortSignal`, in which case the promise
* will be rejected with abort signal reason (an error with `name === "AbortError"`).
*
* @category Utilities
*/
function delay(timeout, options) {
	const signal = options?.signal;
	return new Promise((resolve, reject) => {
		let onAbort;
		const timeoutId = setTimeout(() => {
			if (signal && onAbort) signal.removeEventListener("abort", onAbort);
			resolve();
		}, timeout);
		if (signal) {
			if (signal.aborted) try {
				signal.throwIfAborted();
			} catch (err) {
				reject(err);
			}
			onAbort = () => {
				clearTimeout(timeoutId);
				signal.removeEventListener("abort", onAbort);
				try {
					signal.throwIfAborted();
				} catch (err) {
					reject(err);
				}
			};
			signal.addEventListener("abort", onAbort);
		}
	});
}
var AsyncLock = class {
	active;
	acquire() {
		let item;
		const promise = new Promise((resolve, reject) => {
			item = {
				resolve,
				reject,
				token: { release: () => this.release(item) }
			};
		});
		if (this.active) this.active.next = item;
		else {
			this.active = item;
			this.activate();
		}
		return promise;
	}
	release(item) {
		if (this.active === item) {
			this.active = this.active.next;
			this.activate();
		}
		return Promise.resolve();
	}
	activate() {
		if (this.active) this.active.resolve(this.active.token);
	}
	dispose() {
		let item = this.active;
		while (item) {
			item.reject(/* @__PURE__ */ new Error("AsyncLock is disposed"));
			item = item.next;
		}
	}
};
//#endregion
//#region src/coreUtils/colorScheme.tsx
var ColorSchemeContext = React.createContext(null);
/**
* React hook to get currently active color scheme for the UI components.
*
* @category Hooks
* @see {@link WorkspaceRootProps.colorScheme}
*/
function useColorScheme() {
	return React.useContext(ColorSchemeContext) ?? "light";
}
var ColorSchemeApi = React.createContext({
	defined: false,
	actInColorScheme: () => {}
});
//#endregion
//#region src/coreUtils/events.ts
/**
* Implements an event bus, exposing both an observable object ({@link Events}) and
* event emitter ({@link EventTrigger}) sides.
*
* **Example**:
* ```ts
* interface CollectionEvents {
*     addItem: AddItemEvent;
*     removeItem: RemoveItemEvent;
* }
* 
* const source = new EventSource<CollectionEvents>();
* const events: Events<CollectionEvents> = source;
* 
* events.on('addItem', e => { ... });
*
* source.trigger('addItem', { item: someItem });
* ```
* 
* @category Core
*/
var EventSource = class {
	listeners = /* @__PURE__ */ new Map();
	anyListeners;
	on(eventKey, listener) {
		let listeners = this.listeners.get(eventKey);
		if (!listeners) {
			listeners = /* @__PURE__ */ new Set();
			this.listeners.set(eventKey, listeners);
		}
		listeners.add(listener);
	}
	onAny(listener) {
		let listeners = this.anyListeners;
		if (!listeners) {
			listeners = /* @__PURE__ */ new Set();
			this.anyListeners = listeners;
		}
		listeners.add(listener);
	}
	off(eventKey, listener) {
		const listeners = this.listeners.get(eventKey);
		if (listeners) listeners.delete(listener);
	}
	offAny(listener) {
		const listeners = this.anyListeners;
		if (listeners) listeners.delete(listener);
	}
	trigger(eventKey, data) {
		const listeners = this.listeners.get(eventKey);
		if (listeners) for (const listener of listeners) listener(data);
		if (this.anyListeners) for (const anyListener of this.anyListeners) anyListener({ [eventKey]: data });
	}
};
/**
* Provides a convenient way to subscribe to one or many observable objects
* and unsubscribe from all of them at once.
* 
* @category Core
* @see {@link Events}
*/
var EventObserver = class {
	onDispose = /* @__PURE__ */ new Set();
	listen(events, eventKey, listener) {
		events.on(eventKey, listener);
		this.onDispose.add(() => events.off(eventKey, listener));
	}
	listenAny(events, listener) {
		events.onAny(listener);
		this.onDispose.add(() => events.offAny(listener));
	}
	listenOnce(events, eventKey, listener) {
		let unsubscribe;
		const onceListener = (data) => {
			events.off(eventKey, onceListener);
			this.onDispose.delete(unsubscribe);
			listener(data);
		};
		unsubscribe = () => {
			events.off(eventKey, onceListener);
			this.onDispose.delete(unsubscribe);
		};
		events.on(eventKey, onceListener);
		this.onDispose.add(unsubscribe);
	}
	stopListening() {
		for (const unsubscribe of this.onDispose) unsubscribe();
		this.onDispose.clear();
	}
};
//#endregion
//#region src/coreUtils/scheduler.ts
/**
* Debounces a function call such that only one is performed if multiple
* requests are made since initial one for the waiting time.
*
* If `timeout` is 'frame', then the timeout is assumed to be up until next
* rendered frame via
* [requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame).
*
* @category Utilities
*/
var Debouncer = class {
	scheduled;
	_timeout;
	callback;
	constructor(timeout = "frame") {
		this._timeout = timeout;
		this.runSynchronously = this.runSynchronously.bind(this);
	}
	get timeout() {
		return this._timeout;
	}
	setTimeout(timeout) {
		this._timeout = timeout;
	}
	call(callback) {
		this.callback = callback;
		this.schedule();
	}
	schedule() {
		if (this.scheduled === void 0) if (this.timeout === "frame") this.scheduled = requestAnimationFrame(this.runSynchronously);
		else this.scheduled = setTimeout(this.runSynchronously, this.timeout);
	}
	run() {
		const callback = this.callback;
		callback?.();
	}
	runSynchronously = () => {
		if (this.cancelScheduledTimeout()) this.run();
	};
	dispose() {
		this.cancelScheduledTimeout();
	}
	cancelScheduledTimeout() {
		if (typeof this.scheduled !== "undefined") {
			if (this.timeout === "frame") cancelAnimationFrame(this.scheduled);
			else clearTimeout(this.scheduled);
			this.scheduled = void 0;
			return true;
		}
		return false;
	}
};
var BufferingQueue = class {
	onFetch;
	debouncer;
	queuedItems = /* @__PURE__ */ new Set();
	constructor(onFetch, waitingTime = 200) {
		this.onFetch = onFetch;
		this.debouncer = new Debouncer(waitingTime);
	}
	has(key) {
		return this.queuedItems.has(key);
	}
	push(key) {
		this.queuedItems.add(key);
		this.debouncer.call(this.run);
	}
	clear() {
		this.queuedItems.clear();
	}
	run = () => {
		const { queuedItems, onFetch } = this;
		const keys = Array.from(queuedItems);
		queuedItems.clear();
		onFetch(keys);
	};
};
//#endregion
//#region src/coreUtils/workers.ts
/**
* Gets a shared instance of the ref-counted worker proxy.
*
* The worker instance will be created on the first call and
* disposed when the last component using the hook is unmounted.
*
* See [`@reactodia/worker-proxy`](https://github.com/reactodia/worker-proxy)
* for more information on working with transparent Web Worker proxies.
*
* @category Hooks
* @see {defineLayoutWorker}
*/
function useWorker(worker) {
	React.useEffect(() => {
		worker.acquire();
		return () => worker.release();
	}, [worker]);
	return worker.getProxy();
}
//#endregion
//#region src/data/metadataProvider.ts
/**
* Metadata provider to use as a stable base to implement {@link MetadataProvider}
* interface.
*
* @category Core
*/
var BaseMetadataProvider = class {
	methods;
	emptyProperties = /* @__PURE__ */ new Map();
	constructor(methods = {}) {
		this.methods = methods;
	}
	getLiteralLanguages() {
		if (this.methods.getLiteralLanguages) return this.methods.getLiteralLanguages();
		return [];
	}
	async createEntity(type, options) {
		if (this.methods.createEntity) return this.methods.createEntity(type, options);
		return { data: {
			id: "",
			types: [],
			properties: {}
		} };
	}
	async createRelation(source, target, linkType, options) {
		if (this.methods.createRelation) return this.methods.createRelation(source, target, linkType, options);
		return { data: {
			linkTypeId: linkType,
			sourceId: source.id,
			targetId: target.id,
			properties: {}
		} };
	}
	async canConnect(source, target, linkType, options) {
		if (this.methods.canConnect) return this.methods.canConnect(source, target, linkType, options);
		return [];
	}
	async canModifyEntity(entity, options) {
		if (this.methods.canModifyEntity) return this.methods.canModifyEntity(entity, options);
		return {};
	}
	async canModifyRelation(link, source, target, options) {
		if (this.methods.canModifyRelation) return this.methods.canModifyRelation(link, source, target, options);
		return {};
	}
	async getEntityShape(types, options) {
		if (this.methods.getEntityShape) return this.methods.getEntityShape(types, options);
		return { properties: this.emptyProperties };
	}
	async getRelationShape(linkType, source, target, options) {
		if (this.methods.getRelationShape) return this.methods.getRelationShape(linkType, source, target, options);
		return { properties: this.emptyProperties };
	}
	async filterConstructibleTypes(types, options) {
		if (this.methods.filterConstructibleTypes) return this.methods.filterConstructibleTypes(types, options);
		return /* @__PURE__ */ new Set();
	}
};
//#endregion
//#region src/data/schema.ts
/**
* [JSON-LD](https://json-ld.org/) context IRI (`@context` value) for the
* serialized diagram state.
*
* @category Constants
*/
var DiagramContextV1 = "https://ontodia.org/context/v1.json";
/**
* Property type to mark placeholder (i.e. not loaded yet) entity data.
*
* @category Constants
* @see {@link EntityElement.placeholderData}
*/
var PlaceholderDataProperty = "urn:reactodia:isPlaceholder";
/**
* Type for a newly created temporary entity in graph authoring mode.
*
* @category Constants
*/
var PlaceholderEntityType = "urn:reactodia:newElement";
/**
* Type for an newly created temporary relation in graph authoring mode.
*
* @category Constants
*/
var PlaceholderRelationType = "urn:reactodia:newLink";
/**
* Contains a state with typed values for any defined
* {@link TemplateProperties template properties}.
*
* Each property value should be JSON-serializable to be able
* to export and import it as part of the serialized diagram layout.
*/
var TemplateState = class TemplateState {
	value;
	static empty = new TemplateState(void 0);
	constructor(value) {
		this.value = value;
	}
	/**
	* Utility function to define typed {@link TemplateProperty template property}.
	*
	* **Example**:
	* ```ts
	* const MyNumberProperty = TemplateState.property('urn:my:prop1').of<number>();
	* ```
	*/
	static property(key) {
		return { of: () => key };
	}
	/**
	* Gets typed {@link TemplateProperties template property} value from
	* a template state.
	*/
	get(property) {
		return this.value?.[property];
	}
	/**
	* Sets optional typed {@link TemplateProperties template property} value
	* on a template state.
	*
	* This function never mutates the original state and always return
	* either original state (if the property value is the same) or newly
	* created updated state.
	*
	* If the `value` is `undefined`, the property will be removed from the `state`,
	* otherwise the new value will be set.
	*/
	set(property, value) {
		const state = this.value;
		if (value !== void 0) {
			if (state?.[property] !== value) return new TemplateState({
				...state,
				[property]: value
			});
		} else if (state?.[property] !== void 0) {
			const { [property]: _, ...withoutProperty } = state;
			return new TemplateState(withoutProperty);
		}
		return this;
	}
	static fromJSON(value) {
		return value === void 0 ? TemplateState.empty : new TemplateState(value);
	}
	toJSON() {
		return this.value ? { ...this.value } : void 0;
	}
};
var TemplateProperties;
(function(_TemplateProperties) {
	_TemplateProperties.Expanded = TemplateState.property("urn:reactodia:expanded").of();
	_TemplateProperties.ElementSize = TemplateState.property("urn:reactodia:elementSize").of();
	_TemplateProperties.PinnedProperties = TemplateState.property("urn:reactodia:pinnedProperties").of();
	_TemplateProperties.CustomLabel = TemplateState.property("urn:reactodia:customLabel").of();
	_TemplateProperties.LayoutOnly = TemplateState.property("urn:reactodia:layoutOnly").of();
	_TemplateProperties.GroupPageIndex = TemplateState.property("urn:reactodia:groupPageIndex").of();
	_TemplateProperties.GroupPageSize = TemplateState.property("urn:reactodia:groupPageSize").of();
	_TemplateProperties.AnnotationContent = TemplateState.property("urn:reactodia:annotationContent").of();
	_TemplateProperties.ColorVariant = TemplateState.property("urn:reactodia:colorVariant").of();
})(TemplateProperties || (TemplateProperties = {}));
var DefaultColorVariants = [
	"default",
	"primary",
	"success",
	"info",
	"warning",
	"danger"
];
//#endregion
//#region src/data/composite/mergeUtils.ts
function mergeKnownElementTypes(composite) {
	const classes = /* @__PURE__ */ new Map();
	const edges = new HashSet(hashSubtypeEdge, equalSubtypeEdges);
	for (const [response] of composite) {
		for (const model of response.elementTypes) {
			const existing = classes.get(model.id);
			classes.set(model.id, existing ? mergeClassModel(existing, model) : model);
		}
		for (const edge of response.subtypeOf) edges.add(edge);
	}
	return {
		elementTypes: Array.from(classes.values()),
		subtypeOf: Array.from(edges.values())
	};
}
function mergeKnownLinkTypes(responses) {
	const result = /* @__PURE__ */ new Map();
	for (const [response] of responses) for (const model of response) {
		const existing = result.get(model.id);
		result.set(model.id, existing ? mergeLinkType(existing, model) : model);
	}
	return Array.from(result.values());
}
function mergePropertyTypes(responses) {
	return mergeMapResponses(responses, mergePropertyModel);
}
function mergePropertyModel(a, b) {
	return {
		...a,
		...b,
		label: mergeLabels(a.label, b.label)
	};
}
function mergeElementTypes(responses) {
	return mergeMapResponses(responses, mergeClassModel);
}
function mergeLinkTypes(responses) {
	return mergeMapResponses(responses, mergeLinkType);
}
function mergeLinkType(a, b) {
	return {
		...a,
		...b,
		label: mergeLabels(a.label, b.label),
		count: a.count || b.count ? (a.count ?? 0) + (b.count ?? 0) : void 0
	};
}
function mergeMapResponses(responses, mergeItems) {
	const result = /* @__PURE__ */ new Map();
	for (const [response] of responses) for (const [key, model] of response) {
		const existing = result.get(key);
		result.set(key, existing ? mergeItems(existing, model) : model);
	}
	return result;
}
function mergeElementInfo(responses, originProperty) {
	const result = /* @__PURE__ */ new Map();
	for (const [response, provider] of responses) for (const [key, baseModel] of response) {
		const model = {
			...baseModel,
			properties: addOriginProperty(baseModel.properties, provider, originProperty)
		};
		const existing = result.get(key);
		result.set(key, existing ? mergeElementModels(existing, model) : model);
	}
	return result;
}
function addOriginProperty(properties, source, originProperty) {
	const originValue = source.origin ? source.origin : source.origin === null || source.name === void 0 ? null : source.provider.factory.literal(source.name);
	if (!originValue) return properties;
	return {
		...properties,
		[originProperty]: [originValue]
	};
}
function mergeElementModels(a, b) {
	const typeSet = new Set(a.types);
	for (const t of b.types) typeSet.add(t);
	return {
		...a,
		...b,
		types: Array.from(typeSet).sort(),
		properties: mergeProperties(a.properties, b.properties)
	};
}
function mergeProperties(a, b) {
	const reusedTermSet = new HashSet(hashTerm, equalTerms);
	const result = {};
	for (const key in a) if (Object.prototype.hasOwnProperty.call(a, key)) if (Object.prototype.hasOwnProperty.call(b, key)) {
		const terms = [];
		addUniqueTerms(a[key], terms, reusedTermSet);
		addUniqueTerms(b[key], terms, reusedTermSet);
		result[key] = terms;
		reusedTermSet.clear();
	} else result[key] = a[key];
	for (const key in b) if (Object.prototype.hasOwnProperty.call(b, key)) {
		if (!Object.prototype.hasOwnProperty.call(result, key)) result[key] = b[key];
	}
	return result;
}
function mergeLinksInfo(responses, originProperty) {
	const resultSet = new HashMap(hashLink, equalLinks);
	for (const [response, source] of responses) for (const rawLink of response) {
		const link = {
			...rawLink,
			properties: addOriginProperty(rawLink.properties, source, originProperty)
		};
		const existing = resultSet.get(link);
		if (existing) resultSet.set(link, {
			...existing,
			properties: mergeProperties(existing.properties, link.properties)
		});
		else resultSet.set(link, link);
	}
	return Array.from(resultSet.values());
}
function mergeConnectedLinkStats(responses) {
	const result = /* @__PURE__ */ new Map();
	for (const [response] of responses) for (const model of response) {
		const existing = result.get(model.id);
		result.set(model.id, existing ? mergeLinkCount(existing, model) : model);
	}
	return Array.from(result.values());
}
function mergeLinkCount(a, b) {
	return {
		...a,
		...b,
		inCount: a.inCount + b.inCount,
		outCount: a.outCount + b.outCount,
		inexact: Boolean(a.inexact || b.inexact)
	};
}
function mergeLookup(responses, originProperty) {
	const linkedElements = /* @__PURE__ */ new Map();
	for (const [response, provider] of responses) for (const { element: baseElement, inLinks, outLinks } of response) {
		const element = {
			...baseElement,
			properties: addOriginProperty(baseElement.properties, provider, originProperty)
		};
		const existing = linkedElements.get(element.id);
		if (existing) {
			existing.element = mergeElementModels(existing.element, element);
			for (const inLink of inLinks) existing.inLinks.add(inLink);
			for (const outLink of outLinks) existing.outLinks.add(outLink);
		} else linkedElements.set(baseElement.id, {
			element,
			inLinks: new Set(inLinks),
			outLinks: new Set(outLinks)
		});
	}
	return Array.from(linkedElements.values());
}
function mergeLabels(a, b) {
	const labels = [];
	const labelSet = new HashSet(hashTerm, equalTerms);
	addUniqueTerms(a, labels, labelSet);
	addUniqueTerms(b, labels, labelSet);
	return labels;
}
function addUniqueTerms(input, output, outputSet) {
	for (const term of input) if (!outputSet.has(term)) {
		outputSet.add(term);
		output.push(term);
	}
}
function mergeClassModel(a, b) {
	return {
		...a,
		...b,
		label: mergeLabels(a.label, b.label),
		count: mergeCounts(a.count, b.count)
	};
}
function mergeCounts(a, b) {
	if (a === void 0 && b === void 0) return;
	return (a ?? 0) + (b ?? 0);
}
//#endregion
//#region src/data/composite/composite.ts
var DEFAULT_ORIGIN_PROPERTY = "urn:reactodia:sourceProvider";
/**
* Provides graph data by combining results from multiple other data providers.
*
* @category Data
*/
var CompositeDataProvider = class {
	providers;
	originProperty;
	constructor(options) {
		const { providers, originProperty } = options;
		this.providers = providers;
		this.originProperty = originProperty ?? DEFAULT_ORIGIN_PROPERTY;
	}
	get factory() {
		if (this.providers.length > 0) return this.providers[0].provider.factory;
		else return DefaultDataFactory;
	}
	async requestWithMerge(method, merge) {
		return merge(await Promise.all(this.providers.map((p) => method(p.provider).then((r) => [r, p]))));
	}
	knownElementTypes(params) {
		return this.requestWithMerge((p) => p.knownElementTypes(params), mergeKnownElementTypes);
	}
	knownLinkTypes(params) {
		return this.requestWithMerge((p) => p.knownLinkTypes(params), mergeKnownLinkTypes);
	}
	elementTypes(params) {
		return this.requestWithMerge((p) => p.elementTypes(params), mergeElementTypes);
	}
	propertyTypes(params) {
		return this.requestWithMerge((p) => p.propertyTypes(params), mergePropertyTypes);
	}
	linkTypes(params) {
		return this.requestWithMerge((p) => p.linkTypes(params), mergeLinkTypes);
	}
	elements(params) {
		return this.requestWithMerge((p) => p.elements(params), (results) => mergeElementInfo(results, this.originProperty));
	}
	links(params) {
		return this.requestWithMerge((p) => p.links(params), (results) => mergeLinksInfo(results, this.originProperty));
	}
	connectedLinkStats(params) {
		return this.requestWithMerge((p) => p.connectedLinkStats(params), mergeConnectedLinkStats);
	}
	lookup(params) {
		return this.requestWithMerge((p) => p.lookup(params), (results) => mergeLookup(results, this.originProperty));
	}
};
//#endregion
//#region src/data/decorated/decoratedDataProvider.ts
/**
* Utility graph data provider to wrap over another provider to modify
* how the requests are made or alter the results.
*
* @category Data
*/
var DecoratedDataProvider = class {
	baseProvider;
	decorator;
	constructor(options) {
		this.baseProvider = options.baseProvider;
		this.decorator = options.decorator;
	}
	get factory() {
		return this.baseProvider.factory;
	}
	decorate(method, params) {
		const bound = this.baseProvider[method].bind(this.baseProvider);
		const { decorator } = this;
		return decorator(method, params[0], bound);
	}
	knownElementTypes(params) {
		return this.decorate("knownElementTypes", [params]);
	}
	knownLinkTypes(params) {
		return this.decorate("knownLinkTypes", [params]);
	}
	elementTypes(params) {
		return this.decorate("elementTypes", [params]);
	}
	propertyTypes(params) {
		return this.decorate("propertyTypes", [params]);
	}
	linkTypes(params) {
		return this.decorate("linkTypes", [params]);
	}
	elements(params) {
		return this.decorate("elements", [params]);
	}
	links(params) {
		return this.decorate("links", [params]);
	}
	connectedLinkStats(params) {
		return this.decorate("connectedLinkStats", [params]);
	}
	lookup(params) {
		return this.decorate("lookup", [params]);
	}
};
/**
* Data provider decorator which delays each request to the base provider.
*
* @category Data
* @see {@link DecoratedDataProvider}
*/
function delayProviderDecorator(meanDelay, distribution) {
	return (method, params, body) => {
		return delay(distribution === "linear" ? Math.random() * meanDelay * 2 : distribution === "exponential" ? -Math.log(Math.random()) * meanDelay : meanDelay, { signal: params.signal }).then(() => body(params));
	};
}
//#endregion
//#region src/data/decorated/emptyDataProvider.ts
/**
* Empty graph data provider, which always return nothing.
*
* @category Data
*/
var EmptyDataProvider = class {
	get factory() {
		return DefaultDataFactory;
	}
	knownElementTypes(params) {
		return Promise.resolve({
			elementTypes: [],
			subtypeOf: []
		});
	}
	knownLinkTypes(params) {
		return Promise.resolve([]);
	}
	elementTypes(params) {
		return Promise.resolve(/* @__PURE__ */ new Map());
	}
	propertyTypes(params) {
		return Promise.resolve(/* @__PURE__ */ new Map());
	}
	linkTypes(params) {
		return Promise.resolve(/* @__PURE__ */ new Map());
	}
	elements(params) {
		return Promise.resolve(/* @__PURE__ */ new Map());
	}
	links(params) {
		return Promise.resolve([]);
	}
	connectedLinkStats(params) {
		return Promise.resolve([]);
	}
	lookup(params) {
		return Promise.resolve([]);
	}
};
//#endregion
//#region src/data/indexedDb/adjacencyBlocks.ts
function subtractAdjacencyBlocks(base, disjointBySources) {
	const leftoverSources = new Set(base.sources);
	const partialBlocks = new HashMap(hashRange, sameRange);
	const orderedBlocks = [];
	const addBlock = (sources, targets) => {
		let block = partialBlocks.get(targets);
		if (block) for (const source of sources) block.sources.add(source);
		else {
			block = {
				targets,
				sources: new Set(sources)
			};
			partialBlocks.set(targets, block);
			orderedBlocks.push(block);
		}
	};
	for (const block of disjointBySources) {
		for (const foundSource of block.sources) leftoverSources.delete(foundSource);
		const leftoverTargets = new Set(base.targets);
		for (const foundTarget of block.targets) leftoverTargets.delete(foundTarget);
		if (leftoverTargets.size > 0) addBlock(block.sources, leftoverTargets);
	}
	if (leftoverSources.size > 0) addBlock(leftoverSources, base.targets);
	return orderedBlocks;
}
function hashRange(range) {
	let hash = 0;
	for (const item of range) hash = (hash | 0) + (hashString(String(item)) | 0);
	return hash | 0;
}
function sameRange(a, b) {
	if (a.size !== b.size) return false;
	for (const item of a) if (!b.has(item)) return false;
	return true;
}
function hashAdjacencyRange(range, hasher) {
	const keys = Array.from(range).sort();
	const encoder = new TextEncoder();
	const resultDigest = hasher.create();
	const keyDigest = hasher.create();
	for (const key of keys) {
		const encodedKey = encoder.encode(key);
		keyDigest.start();
		keyDigest.update(encodedKey);
		resultDigest.update(keyDigest.digest());
	}
	const digestBytes = resultDigest.digest();
	return Array.from(digestBytes, (b) => b.toString(16).padStart(2, "0")).join("");
}
//#endregion
//#region src/data/indexedDb/circularBuffer.ts
var CircularBuffer = class {
	buffer;
	_length = 0;
	start = 0;
	end = 0;
	constructor(initialSize = 16) {
		this.buffer = new Uint8Array(initialSize ?? 16);
	}
	get length() {
		return this._length;
	}
	ensureFreeCapacity(count) {
		const requiredSize = this.length + count;
		const oldSize = this.buffer.length;
		if (requiredSize > oldSize) {
			const nextSize = Math.max(Math.pow(2, Math.ceil(Math.log2(requiredSize))), 16);
			const nextBuffer = new Uint8Array(nextSize);
			nextBuffer.set(this.buffer);
			this.buffer = nextBuffer;
			if (this.length > 0 && this.end <= this.start) {
				const nextStart = this.start + (nextSize - oldSize);
				this.buffer.copyWithin(nextStart, this.start, oldSize - this.start);
				this.start = nextStart;
			}
		}
	}
	advanceStart(count) {
		const freeToRead = freeAfterOffset(this.start, this.end, this.buffer.length);
		const readCount = Math.min(freeToRead, count);
		this.start += readCount;
		this._length -= readCount;
		if (this.start === this.buffer.length) this.start = 0;
		return readCount;
	}
	advanceEnd(count) {
		const freeToWrite = freeAfterOffset(this.end, this.start, this.buffer.length);
		const writeCount = Math.min(freeToWrite, count);
		this.end += writeCount;
		this._length += writeCount;
		if (this.end === this.buffer.length) this.end = 0;
		return writeCount;
	}
	*peekBytes(count = this.length) {
		if (count < 0 || count > this.length) throw new Error("CircularBuffer: invalid byte count to peek");
		let total = count;
		let peekOffset = this.start;
		while (total > 0) {
			const spanCount = freeAfterOffset(peekOffset, this.end, this.buffer.length);
			const readCount = Math.min(spanCount, total);
			yield this.buffer.subarray(peekOffset, peekOffset + readCount);
			total -= readCount;
			peekOffset += readCount;
			if (peekOffset >= this.buffer.length) peekOffset = 0;
		}
	}
	readBytes(count) {
		if (count < 0 || count > this.length) throw new Error("CircularBuffer: invalid byte count to read");
		const result = new Uint8Array(count);
		let resultOffset = 0;
		let totalToRead = count;
		while (totalToRead > 0) {
			const readOffset = this.start;
			const readCount = this.advanceStart(totalToRead);
			result.set(this.buffer.subarray(readOffset, readOffset + readCount), resultOffset);
			resultOffset += readCount;
			totalToRead -= readCount;
		}
		return result;
	}
	writeBytes(bytes, count = bytes.length) {
		if (count < 0 || count > bytes.length) throw new Error("CircularBuffer: invalid offset and/or count to write bytes");
		this.ensureFreeCapacity(count);
		let totalToWrite = count;
		while (totalToWrite > 0) {
			const writeOffset = this.end;
			const writeCount = this.advanceEnd(totalToWrite);
			this.buffer.set(bytes.subarray(0, writeCount), writeOffset);
			totalToWrite -= writeCount;
		}
	}
	readInt32() {
		if (this._length < 4) throw new Error("CircularBuffer: not enough length to read Int32");
		const b3 = this.buffer[this.start] << 24;
		this.advanceStart(1);
		const b2 = this.buffer[this.start] << 16;
		this.advanceStart(1);
		const b1 = this.buffer[this.start] << 8;
		this.advanceStart(1);
		const b0 = this.buffer[this.start];
		this.advanceStart(1);
		return b3 | b2 | b1 | b0;
	}
	writeInt32(value) {
		this.ensureFreeCapacity(4);
		this.buffer[this.end] = value >> 24 & 255;
		this.advanceEnd(1);
		this.buffer[this.end] = value >> 16 & 255;
		this.advanceEnd(1);
		this.buffer[this.end] = value >> 8 & 255;
		this.advanceEnd(1);
		this.buffer[this.end] = value & 255;
		this.advanceEnd(1);
	}
};
function freeAfterOffset(offset, another, length) {
	return offset < another ? another - offset : length - offset;
}
//#endregion
//#region src/data/indexedDb/sha256.ts
var Sha256 = class {
	initialized = false;
	_padding;
	_k;
	/**
	* Initializes the constant tables.
	*/
	initialize() {
		if (this.initialized) return;
		this._padding = new Uint8Array(64);
		this._padding[0] = 128;
		this._k = new Uint32Array([
			1116352408,
			1899447441,
			3049323471,
			3921009573,
			961987163,
			1508970993,
			2453635748,
			2870763221,
			3624381080,
			310598401,
			607225278,
			1426881987,
			1925078388,
			2162078206,
			2614888103,
			3248222580,
			3835390401,
			4022224774,
			264347078,
			604807628,
			770255983,
			1249150122,
			1555081692,
			1996064986,
			2554220882,
			2821834349,
			2952996808,
			3210313671,
			3336571891,
			3584528711,
			113926993,
			338241895,
			666307205,
			773529912,
			1294757372,
			1396182291,
			1695183700,
			1986661051,
			2177026350,
			2456956037,
			2730485921,
			2820302411,
			3259730800,
			3345764771,
			3516065817,
			3600352804,
			4094571909,
			275423344,
			430227734,
			506948616,
			659060556,
			883997877,
			958139571,
			1322822218,
			1537002063,
			1747873779,
			1955562222,
			2024104815,
			2227730452,
			2361852424,
			2428436474,
			2756734187,
			3204031479,
			3329325298
		]);
		this.initialized = true;
	}
	/**
	* Creates a SHA-256 message digest object.
	*
	* @return a message digest object.
	*/
	create() {
		this.initialize();
		return new Sha256Digest(this._padding, this._k);
	}
};
var Sha256Digest = class {
	_padding;
	_k;
	blockLength = 64;
	digestLength = 32;
	messageLength = 0;
	messageLength64 = [0, 0];
	_state;
	_input;
	_w = Array.from({ length: 64 }, () => 0);
	constructor(_padding, _k) {
		this._padding = _padding;
		this._k = _k;
		this.start();
	}
	/**
	* Starts the digest.
	*/
	start() {
		this.messageLength = 0;
		this.messageLength64 = [0, 0];
		this._input = new CircularBuffer();
		this._state = {
			h0: 1779033703,
			h1: 3144134277,
			h2: 1013904242,
			h3: 2773480762,
			h4: 1359893119,
			h5: 2600822924,
			h6: 528734635,
			h7: 1541459225
		};
	}
	/**
	* Updates the digest with the given message input.
	*
	* @param bytes the binary data to update with.
	*/
	update(bytes) {
		this.messageLength += bytes.length;
		this.messageLength64[0] += bytes.length / 4294967296 >>> 0;
		this.messageLength64[1] += bytes.length >>> 0;
		this._input.writeBytes(bytes);
		_update(this._state, this._w, this._input, this._k);
	}
	/**
	* Produces the digest.
	*
	* @return a byte buffer containing the digest value.
	*/
	digest() {
		const padBytes = new CircularBuffer();
		padBytes.ensureFreeCapacity(this._input.length);
		for (const span of this._input.peekBytes()) padBytes.writeBytes(span);
		padBytes.writeBytes(this._padding, 64 - (this.messageLength64[1] + 8 & 63));
		padBytes.writeInt32(this.messageLength64[0] << 3 | this.messageLength64[0] >>> 28);
		padBytes.writeInt32(this.messageLength64[1] << 3);
		const s2 = { ...this._state };
		_update(s2, this._w, padBytes, this._k);
		const result = new CircularBuffer(this.digestLength);
		result.writeInt32(s2.h0);
		result.writeInt32(s2.h1);
		result.writeInt32(s2.h2);
		result.writeInt32(s2.h3);
		result.writeInt32(s2.h4);
		result.writeInt32(s2.h5);
		result.writeInt32(s2.h6);
		result.writeInt32(s2.h7);
		return result.readBytes(result.length);
	}
};
/**
* Updates a SHA-256 state with the given byte buffer.
*
* @param s the SHA-256 state to update.
* @param w the array to use to store words.
* @param bytes the byte buffer to update with.
* @param _k SHA-256 coefficients (readonly)
*/
function _update(s, w, bytes, _k) {
	let t1, t2, s0, s1, ch, maj, i, a, b, c, d, e, f, g, h;
	let len = bytes.length;
	while (len >= 64) {
		for (i = 0; i < 16; ++i) w[i] = bytes.readInt32();
		for (; i < 64; ++i) {
			t1 = w[i - 2];
			t1 = (t1 >>> 17 | t1 << 15) ^ (t1 >>> 19 | t1 << 13) ^ t1 >>> 10;
			t2 = w[i - 15];
			t2 = (t2 >>> 7 | t2 << 25) ^ (t2 >>> 18 | t2 << 14) ^ t2 >>> 3;
			w[i] = t1 + w[i - 7] + t2 + w[i - 16] | 0;
		}
		a = s.h0;
		b = s.h1;
		c = s.h2;
		d = s.h3;
		e = s.h4;
		f = s.h5;
		g = s.h6;
		h = s.h7;
		for (i = 0; i < 64; ++i) {
			s1 = (e >>> 6 | e << 26) ^ (e >>> 11 | e << 21) ^ (e >>> 25 | e << 7);
			ch = g ^ e & (f ^ g);
			s0 = (a >>> 2 | a << 30) ^ (a >>> 13 | a << 19) ^ (a >>> 22 | a << 10);
			maj = a & b | c & (a ^ b);
			t1 = h + s1 + ch + _k[i] + w[i];
			t2 = s0 + maj;
			h = g;
			g = f;
			f = e;
			e = d + t1 | 0;
			d = c;
			c = b;
			b = a;
			a = t1 + t2 | 0;
		}
		s.h0 = s.h0 + a | 0;
		s.h1 = s.h1 + b | 0;
		s.h2 = s.h2 + c | 0;
		s.h3 = s.h3 + d | 0;
		s.h4 = s.h4 + e | 0;
		s.h5 = s.h5 + f | 0;
		s.h6 = s.h6 + g | 0;
		s.h7 = s.h7 + h | 0;
		len -= 64;
	}
}
//#endregion
//#region src/data/indexedDb/indexedDbCachedProvider.ts
var MISSING_RECORD_KEY = "__missing";
var KNOWN_ELEMENT_TYPES_KEY = "knownElementTypes";
var KNOWN_LINK_TYPES_KEY = "knownLinkTypes";
var LOOKUP_KEY_PROPERTIES = [
	"elementType",
	"element",
	"link",
	"direction",
	"text",
	"limit"
];
/**
* Caches graph data returned from another data provider using
* [IndexedDB](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API) storage.
*
* @category Data
*/
var IndexedDbCachedProvider = class IndexedDbCachedProvider {
	static DB_VERSION = 4;
	hasher = new Sha256();
	baseProvider;
	dbName;
	cacheMissing;
	cacheLinks;
	cacheTextLookups;
	linkLock = new AsyncLock();
	closeSignal;
	openedDb;
	deleteRequest;
	constructor(options) {
		this.baseProvider = options.baseProvider;
		this.dbName = options.dbName;
		this.cacheMissing = options.cacheMissing ?? true;
		this.cacheLinks = options.cacheLinks ?? true;
		this.cacheTextLookups = options.cacheTextLookups ?? false;
		this.closeSignal = options.closeSignal;
		this.closeSignal.addEventListener("abort", this.onClose);
	}
	get factory() {
		return this.baseProvider.factory;
	}
	clearCache() {
		this.deleteRequest = this.deleteDatabase();
		return this.deleteRequest;
	}
	async deleteDatabase() {
		await this.closeDatabase();
		await indexedDbRequestAsPromise(indexedDB.deleteDatabase(this.dbName));
	}
	async openDb() {
		if (this.deleteRequest) await this.deleteRequest.catch(() => {});
		if (!this.openedDb) this.openedDb = new Promise((resolve, reject) => {
			const request = indexedDB.open(this.dbName, IndexedDbCachedProvider.DB_VERSION);
			request.onupgradeneeded = (e) => {
				const db = request.result;
				if (e.newVersion !== e.oldVersion) for (const storeName of db.objectStoreNames) db.deleteObjectStore(storeName);
				if (!db.objectStoreNames.contains("knownElementTypes")) db.createObjectStore("knownElementTypes", { keyPath: "id" });
				if (!db.objectStoreNames.contains("knownLinkTypes")) db.createObjectStore("knownLinkTypes", { keyPath: "id" });
				if (!db.objectStoreNames.contains("elementTypes")) db.createObjectStore("elementTypes", { keyPath: "id" });
				if (!db.objectStoreNames.contains("linkTypes")) db.createObjectStore("linkTypes", { keyPath: "id" });
				if (!db.objectStoreNames.contains("propertyTypes")) db.createObjectStore("propertyTypes", { keyPath: "id" });
				if (!db.objectStoreNames.contains("elements")) db.createObjectStore("elements", { keyPath: "id" });
				if (!db.objectStoreNames.contains("links")) db.createObjectStore("links", {
					keyPath: "__id",
					autoIncrement: true
				}).createIndex("bySourceTarget", ["sourceId", "targetId"]);
				if (!db.objectStoreNames.contains("linkBlocks")) db.createObjectStore("linkBlocks", { keyPath: "endpoint" }).createIndex("byRangeKey", "connectedRange");
				if (!db.objectStoreNames.contains("linkRanges")) db.createObjectStore("linkRanges");
				if (!db.objectStoreNames.contains("connectedLinkStats")) db.createObjectStore("connectedLinkStats", { keyPath: ["elementId", "inexactCount"] });
				if (!db.objectStoreNames.contains("lookup")) db.createObjectStore("lookup", { keyPath: LOOKUP_KEY_PROPERTIES });
			};
			request.onblocked = (e) => {
				reject(/* @__PURE__ */ new Error("IndexedDB is blocked from upgrade due to being opened at another browser tab"));
			};
			request.onsuccess = (e) => {
				const db = request.result;
				resolve(db);
			};
			request.onerror = (e) => {
				reject(request.error);
			};
		});
		return this.openedDb;
	}
	onClose = async () => {
		this.closeSignal.removeEventListener("abort", this.onClose);
		await this.closeDatabase();
	};
	async closeDatabase() {
		if (this.openedDb) {
			(await this.openedDb).close();
			this.openedDb = void 0;
		}
	}
	async knownElementTypes(params) {
		const result = await fetchSingleWithDbCache(await this.openDb(), "knownElementTypes", KNOWN_ELEMENT_TYPES_KEY, async (key) => ({
			id: key,
			value: await this.baseProvider.knownElementTypes(params)
		}));
		rehydrateLabels(result.value.elementTypes.values(), this.factory);
		return result.value;
	}
	async knownLinkTypes(params) {
		const result = await fetchSingleWithDbCache(await this.openDb(), "knownLinkTypes", KNOWN_LINK_TYPES_KEY, async (key) => ({
			id: key,
			value: await this.baseProvider.knownLinkTypes(params)
		}));
		rehydrateLabels(result.value.values(), this.factory);
		return result.value;
	}
	async elementTypes(params) {
		const { classIds, signal } = params;
		const result = await fetchManyWithDbCache(await this.openDb(), "elementTypes", classIds, this.cacheMissing, async (ids) => await this.baseProvider.elementTypes({
			classIds: ids,
			signal
		}));
		rehydrateLabels(result.values(), this.factory);
		return result;
	}
	async propertyTypes(params) {
		const { propertyIds, signal } = params;
		const result = await fetchManyWithDbCache(await this.openDb(), "propertyTypes", propertyIds, this.cacheMissing, async (ids) => await this.baseProvider.propertyTypes({
			propertyIds: ids,
			signal
		}));
		rehydrateLabels(result.values(), this.factory);
		return result;
	}
	async linkTypes(params) {
		const { linkTypeIds, signal } = params;
		const result = await fetchManyWithDbCache(await this.openDb(), "linkTypes", linkTypeIds, this.cacheMissing, async (ids) => await this.baseProvider.linkTypes({
			linkTypeIds: ids,
			signal
		}));
		rehydrateLabels(result.values(), this.factory);
		return result;
	}
	async elements(params) {
		const { elementIds, signal } = params;
		const result = await fetchManyWithDbCache(await this.openDb(), "elements", elementIds, this.cacheMissing, async (ids) => await this.baseProvider.elements({
			elementIds: ids,
			signal
		}));
		rehydrateProperties(result.values(), this.factory);
		return result;
	}
	async links(params) {
		if (!this.cacheLinks) return this.baseProvider.links(params);
		if (params.primary.length === 0 || params.secondary.length === 0) return [];
		const db = await this.openDb();
		const orderedPrimary = [...params.primary].sort();
		const orderedSecondary = [...params.secondary].sort();
		const request = {
			sources: new Set(orderedPrimary),
			targets: new Set(orderedSecondary)
		};
		const lock = await this.linkLock.acquire();
		try {
			const ranges = await this.readLinkRanges(db, request);
			const blocks = this.selectMissingLinkBlocks(request, ranges);
			if (blocks.length > 0) {
				await this.fetchAndCacheLinks(db, blocks, params.signal);
				await this.updateLinkRanges(db, ranges, request);
			}
		} finally {
			await lock.release();
		}
		const links = [];
		const onlyTypeIds = params.linkTypeIds ? new Set(params.linkTypeIds) : void 0;
		await this.readLinksFromCache(db, orderedPrimary, orderedSecondary, (link) => {
			if (!onlyTypeIds || onlyTypeIds.has(link.linkTypeId)) links.push(link);
		});
		const nonSelfPrimary = orderedPrimary.filter((element) => !request.targets.has(element));
		await this.readLinksFromCache(db, orderedSecondary, nonSelfPrimary, (link) => {
			if (!onlyTypeIds || onlyTypeIds.has(link.linkTypeId)) links.push(link);
		});
		rehydrateProperties(links, this.factory);
		return links;
	}
	async readLinkRanges(db, request) {
		const tx = db.transaction(["linkBlocks", "linkRanges"], "readonly");
		try {
			const blockStore = tx.objectStore("linkBlocks");
			const rangeStore = tx.objectStore("linkRanges");
			const endpoints = new Set(request.sources);
			for (const target of request.targets) endpoints.add(target);
			const rangeByEndpoint = /* @__PURE__ */ new Map();
			await indexedDbGetMany(blockStore, Array.from(endpoints), (value) => {
				const block = value;
				if (block) rangeByEndpoint.set(block.endpoint, block.connectedRange);
			});
			const uniqueRangeKeys = new Set(rangeByEndpoint.values());
			const rangeByKey = /* @__PURE__ */ new Map();
			await indexedDbGetMany(rangeStore, Array.from(uniqueRangeKeys), (value, key) => {
				const range = value;
				if (range) rangeByKey.set(key, range);
			});
			tx.commit();
			return {
				endpoints,
				rangeByEndpoint,
				rangeByKey
			};
		} catch (err) {
			indexedDbSilentAbort(tx);
			throw new Error("Failed to read link ranges for an update", { cause: err });
		}
	}
	selectMissingLinkBlocks(request, ranges) {
		const rangeToSources = /* @__PURE__ */ new Map();
		for (const source of request.sources) {
			const range = ranges.rangeByEndpoint.get(source);
			if (range) multimapAdd(rangeToSources, range, source);
		}
		const intersectedRanges = Array.from(rangeToSources.keys()).sort();
		const blocks = [];
		for (const rangeKey of intersectedRanges) {
			const sources = rangeToSources.get(rangeKey);
			const targets = ranges.rangeByKey.get(rangeKey);
			if (sources && targets) blocks.push({
				sources,
				targets
			});
		}
		return subtractAdjacencyBlocks(request, blocks);
	}
	async fetchAndCacheLinks(db, blocks, signal) {
		const serializedLinks = [];
		await Promise.all(blocks.map(async (block) => {
			const links = await this.baseProvider.links({
				primary: Array.from(block.sources),
				secondary: Array.from(block.targets),
				signal
			});
			for (const link of links) serializedLinks.push(serializeForDb(link));
		}));
		signal?.throwIfAborted();
		const tx = db.transaction("links", "readwrite");
		try {
			await indexedDbPutMany(tx.objectStore("links"), serializedLinks);
			tx.commit();
		} catch (err) {
			indexedDbSilentAbort(tx);
			throw new Error("Failed to fetch and cache missing links from base provider", { cause: err });
		}
	}
	async updateLinkRanges(db, ranges, update) {
		const fullBothRequest = { items: ranges.endpoints };
		const fullSourcesRequest = { items: update.sources };
		const fullTargetsRequest = { items: update.targets };
		const bothRequests = /* @__PURE__ */ new Map();
		const sourceRequests = /* @__PURE__ */ new Map();
		const targetRequests = /* @__PURE__ */ new Map();
		for (const [endpoint, rangeKey] of ranges.rangeByEndpoint) if (update.sources.has(endpoint) && update.targets.has(endpoint)) {
			if (!bothRequests.has(rangeKey)) {
				const before = ranges.rangeByKey.get(rangeKey);
				const items = new Set(before);
				for (const source of update.sources) items.add(source);
				for (const target of update.targets) items.add(target);
				bothRequests.set(rangeKey, {
					before,
					items
				});
			}
		} else if (update.sources.has(endpoint)) {
			if (!sourceRequests.has(rangeKey)) {
				const before = ranges.rangeByKey.get(rangeKey);
				const items = new Set(before);
				for (const target of update.targets) items.add(target);
				sourceRequests.set(rangeKey, {
					before,
					items
				});
			}
		} else if (update.targets.has(endpoint)) {
			if (!targetRequests.has(rangeKey)) {
				const before = ranges.rangeByKey.get(rangeKey);
				const items = new Set(before);
				for (const source of update.sources) items.add(source);
				targetRequests.set(rangeKey, {
					before,
					items
				});
			}
		}
		const requests = [
			fullBothRequest,
			fullSourcesRequest,
			fullTargetsRequest
		];
		for (const requestMap of [
			bothRequests,
			sourceRequests,
			targetRequests
		]) for (const request of requestMap.values()) if (request.items.size > (request.before?.size ?? 0)) requests.push(request);
		for (const request of requests) request.computedHash = hashAdjacencyRange(request.items, this.hasher);
		const tx = db.transaction(["linkBlocks", "linkRanges"], "readwrite");
		try {
			const blockStore = tx.objectStore("linkBlocks");
			const rangeStore = tx.objectStore("linkRanges");
			const updatedBlocks = [];
			const addedRanges = /* @__PURE__ */ new Map();
			for (const endpoint of ranges.endpoints) {
				const rangeKey = ranges.rangeByEndpoint.get(endpoint);
				let request;
				if (update.sources.has(endpoint) && update.targets.has(endpoint)) request = rangeKey ? bothRequests.get(rangeKey) : fullBothRequest;
				else if (update.sources.has(endpoint)) request = rangeKey ? sourceRequests.get(rangeKey) : fullTargetsRequest;
				else if (update.targets.has(endpoint)) request = rangeKey ? targetRequests.get(rangeKey) : fullSourcesRequest;
				if (request?.computedHash) {
					updatedBlocks.push({
						endpoint,
						connectedRange: request.computedHash
					});
					if (!ranges.rangeByKey.has(request.computedHash)) addedRanges.set(request.computedHash, request);
				}
			}
			await indexedDbPutMany(rangeStore, Array.from(addedRanges.values()), (request) => [request.computedHash, request.items]);
			await indexedDbPutMany(blockStore, updatedBlocks);
			const changedRangeSet = /* @__PURE__ */ new Set();
			for (const block of updatedBlocks) {
				const beforeKey = ranges.rangeByEndpoint.get(block.endpoint);
				if (beforeKey) changedRangeSet.add(beforeKey);
			}
			const blockByRange = blockStore.index("byRangeKey");
			const changedRanges = Array.from(changedRangeSet);
			const foundRanges = /* @__PURE__ */ new Set();
			await indexedDbGetMany(blockByRange, changedRanges, (value, key) => {
				if (value) foundRanges.add(key);
			});
			await indexedDbDeleteMany(rangeStore, changedRanges.filter((key) => !foundRanges.has(key)));
			tx.commit();
		} catch (err) {
			indexedDbSilentAbort(tx);
			throw new Error("Failed to update cached link blocks", { cause: err });
		}
	}
	async readLinksFromCache(db, orderedSources, orderedTargets, onLink) {
		const tx = db.transaction("links", "readonly");
		try {
			await indexedDbScanOrderedArea2D(tx.objectStore("links").index("bySourceTarget"), orderedSources, orderedTargets, (cursor) => {
				const { __id, ...link } = cursor.value;
				onLink(link);
			});
			tx.commit();
		} catch (err) {
			indexedDbSilentAbort(tx);
			throw new Error("Failed to read links from cache", { cause: err });
		}
	}
	async connectedLinkStats(params) {
		const { signal } = params;
		return (await fetchSingleWithDbCache(await this.openDb(), "connectedLinkStats", [params.elementId, params.inexactCount ? 1 : 0], async ([elementId, inexactCount]) => ({
			elementId,
			inexactCount,
			stats: await this.baseProvider.connectedLinkStats({
				elementId,
				inexactCount: Boolean(inexactCount),
				signal
			})
		}))).stats;
	}
	async lookup(params) {
		if (!this.cacheTextLookups && params.text !== void 0) return this.baseProvider.lookup(params);
		const key = [
			params.elementTypeId ?? "",
			params.refElementId ?? "",
			params.refElementLinkId ?? "",
			params.linkDirection ?? "",
			params.text ?? "",
			params.limit === void 0 ? "" : params.limit === null ? "null" : String(params.limit)
		];
		const record = await fetchSingleWithDbCache(await this.openDb(), "lookup", key, async ([elementType, element, link, direction, text, limit]) => ({
			elementType,
			element,
			link,
			direction,
			text,
			limit,
			result: await this.baseProvider.lookup(params)
		}));
		rehydrateProperties(record.result.map((el) => el.element), this.factory);
		return record.result;
	}
};
async function fetchSingleWithDbCache(db, storeName, key, fetchBase) {
	{
		const readTx = db.transaction(storeName, "readonly");
		const cached = await indexedDbRequestAsPromise(readTx.objectStore(storeName).get(key));
		readTx.commit();
		if (cached) return cached;
	}
	const fetched = serializeForDb(await fetchBase(key));
	const writeTx = db.transaction(storeName, "readwrite");
	await indexedDbRequestAsPromise(writeTx.objectStore(storeName).put(fetched));
	writeTx.commit();
	return fetched;
}
async function fetchManyWithDbCache(db, storeName, keys, cacheMissing, fetchBase) {
	const result = /* @__PURE__ */ new Map();
	const missingKeys = [];
	if (keys.length > 0) {
		const readTx = db.transaction(storeName, "readonly");
		await indexedDbGetMany(readTx.objectStore(storeName), keys, (value, classId) => {
			const model = value;
			if (model) if (isMissingRecord(model)) {
				if (!cacheMissing) missingKeys.push(classId);
			} else result.set(classId, model);
			else missingKeys.push(classId);
		});
		readTx.commit();
	}
	if (missingKeys.length > 0) {
		const fetched = await fetchBase(missingKeys);
		const stored = [];
		for (const key of missingKeys) if (fetched.has(key)) {
			const serialized = serializeForDb(fetched.get(key));
			result.set(key, serialized);
			stored.push(serialized);
		} else stored.push({
			id: key,
			[MISSING_RECORD_KEY]: true
		});
		const writeTx = db.transaction(storeName, "readwrite");
		await indexedDbPutMany(writeTx.objectStore(storeName), stored);
		writeTx.commit();
	}
	return result;
}
function indexedDbRequestAsPromise(request) {
	return new Promise((resolve, reject) => {
		request.onsuccess = () => resolve(request.result);
		request.onerror = () => reject(request.error);
	});
}
function indexedDbGetMany(store, keys, onGet) {
	if (keys.length === 0) return Promise.resolve();
	return new Promise((resolve, reject) => {
		let resolvedCount = 0;
		let rejected = false;
		for (const key of keys) {
			const request = store.get(key);
			request.onsuccess = () => {
				if (rejected) return;
				resolvedCount++;
				try {
					onGet(request.result, key);
				} catch (err) {
					rejected = true;
					reject(err);
				}
				if (resolvedCount === keys.length) resolve();
			};
			request.onerror = () => {
				rejected = true;
				reject(request.error);
			};
		}
	});
}
function indexedDbPutMany(store, items, asKeyValue) {
	if (items.length === 0) return Promise.resolve([]);
	return new Promise((resolve, reject) => {
		const addedKeys = [];
		let rejected = false;
		for (const item of items) {
			let request;
			if (asKeyValue) {
				const [key, value] = asKeyValue(item);
				request = store.put(value, key);
			} else request = store.put(item);
			request.onsuccess = () => {
				if (rejected) return;
				addedKeys.push(request.result);
				if (addedKeys.length === items.length) resolve(addedKeys);
			};
			request.onerror = () => {
				rejected = true;
				reject(request.error);
			};
		}
	});
}
function indexedDbDeleteMany(store, keys) {
	if (keys.length === 0) return Promise.resolve();
	return new Promise((resolve, reject) => {
		let deletedCount = 0;
		let rejected = false;
		for (const key of keys) {
			const request = store.delete(key);
			request.onsuccess = () => {
				if (rejected) return;
				deletedCount++;
				if (deletedCount === keys.length) resolve();
			};
			request.onerror = () => {
				rejected = true;
				reject(request.error);
			};
		}
	});
}
function indexedDbScanOrderedArea2D(store, first, second, scanner) {
	if (first.length === 0 || second.length === 0) return Promise.resolve();
	const range = IDBKeyRange.bound([first[0], second[0]], [first[first.length - 1], second[second.length - 1]]);
	let i = 0;
	let j = 0;
	return indexedDbScan(store, range, (cursor) => {
		const [firstKey, secondKey] = cursor.key;
		nextLeft: while (true) {
			while (i < first.length && indexedDB.cmp(first[i], firstKey) < 0) {
				i++;
				j = 0;
			}
			if (i >= first.length) return;
			if (indexedDB.cmp(first[i], firstKey) > 0) {
				cursor.continue([first[i], second[0]]);
				return;
			}
			while (j < second.length && indexedDB.cmp(second[j], secondKey) < 0) j++;
			if (j >= second.length) {
				j = 0;
				i++;
				continue nextLeft;
			}
			if (indexedDB.cmp(second[j], secondKey) > 0) {
				cursor.continue([first[i], second[j]]);
				return;
			}
			scanner(cursor);
			cursor.continue();
			return;
		}
	});
}
function indexedDbScan(store, query, scanner) {
	return new Promise((resolve, reject) => {
		const request = store.openCursor(query);
		request.onsuccess = (e) => {
			const cursor = request.result;
			if (!cursor) {
				resolve();
				return;
			}
			scanner(cursor);
		};
		request.onerror = () => {
			reject(request.error);
		};
	});
}
function indexedDbSilentAbort(tx) {
	try {
		tx.abort();
	} catch (err) {}
}
function serializeForDb(value) {
	return JSON.parse(JSON.stringify(value));
}
function isMissingRecord(value) {
	return typeof value === "object" && value && Object.prototype.hasOwnProperty.call(value, MISSING_RECORD_KEY) && Boolean(value[MISSING_RECORD_KEY]);
}
function rehydrateLabels(items, factory) {
	for (const item of items) item.label = item.label.map((t) => rehydrateTerm(t, factory));
}
function rehydrateProperties(items, factory) {
	for (const item of items) for (const propertyId in item.properties) if (Object.prototype.hasOwnProperty.call(item.properties, propertyId)) {
		const terms = item.properties[propertyId];
		if (terms) item.properties[propertyId] = terms.map((t) => rehydrateTerm(t, factory));
	}
}
function rehydrateTerm(term, factory) {
	switch (term.termType) {
		case "NamedNode": return factory.namedNode(term.value);
		case "Literal": return factory.literal(term.value, term.language ? term.language : factory.namedNode(term.datatype.value));
		default: return term;
	}
}
//#endregion
//#region src/data/rdf/memoryDataset.ts
/**
* Bit-flags for dataset indexing modes.
*/
var IndexQuadBy = /* @__PURE__ */ function(IndexQuadBy) {
	/** Index by whole quad (default, required). */
	IndexQuadBy[IndexQuadBy["OnlyQuad"] = 0] = "OnlyQuad";
	/** Index by quad subject. */
	IndexQuadBy[IndexQuadBy["S"] = 1] = "S";
	/** Index by quad predicate. */
	IndexQuadBy[IndexQuadBy["P"] = 2] = "P";
	/** Index by quad object. */
	IndexQuadBy[IndexQuadBy["O"] = 4] = "O";
	/** Index by quad subject and predicate. */
	IndexQuadBy[IndexQuadBy["SP"] = 8] = "SP";
	/** Index by quad object and predicate. */
	IndexQuadBy[IndexQuadBy["OP"] = 16] = "OP";
	return IndexQuadBy;
}({});
/**
* Creates an empty mutable in-memory RDF quad dataset,
* with optional simple indexes on quad sub-parts (subject, predicate, object).
*
* @category Utilities
*/
function indexedDataset(indexBy) {
	return new IndexedDataset(indexBy);
}
var IndexedDataset = class {
	_size = 0;
	byQuad;
	bySubject;
	byPredicate;
	byObject;
	bySubjectPredicate;
	byObjectPredicate;
	constructor(indexBy) {
		this.byQuad = new HashMap(hashQuad, equalQuads);
		if (indexBy & 1) this.bySubject = new HashMap(hashTerm, equalTerms);
		if (indexBy & 2) this.byPredicate = new HashMap(hashTerm, equalTerms);
		if (indexBy & 4) this.byObject = new HashMap(hashTerm, equalTerms);
		if (indexBy & 8) this.bySubjectPredicate = new HashMap(SourcePredicateKey.hashCode, SourcePredicateKey.equals);
		if (indexBy & 16) this.byObjectPredicate = new HashMap(SourcePredicateKey.hashCode, SourcePredicateKey.equals);
	}
	get size() {
		return this._size;
	}
	add(quad) {
		if (!this.byQuad.has(quad)) {
			this.byQuad.set(quad, quad);
			if (this.bySubject) pushToIndex(this.bySubject, quad.subject, quad);
			if (this.byPredicate) pushToIndex(this.byPredicate, quad.predicate, quad);
			if (this.byObject) pushToIndex(this.byObject, quad.object, quad);
			if (this.bySubjectPredicate) pushToIndex(this.bySubjectPredicate, {
				source: quad.subject,
				predicate: quad.predicate
			}, quad);
			if (this.byObjectPredicate) pushToIndex(this.byObjectPredicate, {
				source: quad.object,
				predicate: quad.predicate
			}, quad);
			this._size++;
		}
		return this;
	}
	addAll(quads) {
		for (const quad of quads) this.add(quad);
		return this;
	}
	delete(quad) {
		const existing = this.byQuad.get(quad);
		if (existing) {
			this.byQuad.delete(existing);
			if (this.bySubject) deleteFromIndex(this.bySubject, existing.subject, existing);
			if (this.byPredicate) deleteFromIndex(this.byPredicate, existing.predicate, existing);
			if (this.byObject) deleteFromIndex(this.byObject, existing.object, existing);
			if (this.bySubjectPredicate) deleteFromIndex(this.bySubjectPredicate, {
				source: quad.subject,
				predicate: quad.predicate
			}, existing);
			if (this.byObjectPredicate) deleteFromIndex(this.byObjectPredicate, {
				source: quad.object,
				predicate: quad.predicate
			}, existing);
			this._size--;
		}
		return this;
	}
	clear() {
		this._size = 0;
		this.byQuad.clear();
		if (this.bySubject) this.bySubject.clear();
		if (this.byPredicate) this.byPredicate.clear();
		if (this.byObject) this.byObject.clear();
		if (this.bySubjectPredicate) this.bySubjectPredicate.clear();
		if (this.byObjectPredicate) this.byObjectPredicate.clear();
	}
	has(quad) {
		return this.byQuad.has(quad);
	}
	hasMatches(subject, predicate, object, graph) {
		for (const q of this.iterateMatches(subject, predicate, object, graph)) return true;
		return false;
	}
	iterateMatches(subject, predicate, object, graph) {
		let result;
		if (subject && predicate && object && graph) {
			const found = this.byQuad.get(DefaultDataFactory.quad(subject, predicate, object, graph));
			result = found ? [found] : [];
		} else if (this.bySubjectPredicate && subject && predicate) result = filterBySPO(iterateSmallSet(this.bySubjectPredicate.get({
			source: subject,
			predicate
		})), null, null, object);
		else if (this.byObjectPredicate && predicate && object) result = filterBySPO(iterateSmallSet(this.byObjectPredicate.get({
			source: object,
			predicate
		})), subject, null, null);
		else if (this.bySubject && subject) result = filterBySPO(iterateSmallSet(this.bySubject.get(subject)), null, predicate, object);
		else if (this.byPredicate && predicate) result = filterBySPO(iterateSmallSet(this.byPredicate.get(predicate)), subject, null, object);
		else if (this.byObject && object) result = filterBySPO(iterateSmallSet(this.byObject.get(object)), subject, predicate, null);
		else result = filterBySPO(this.byQuad.values(), subject, predicate, object);
		return graph ? filterByGraph(result, graph) : result;
	}
	[Symbol.iterator]() {
		return this.byQuad.values();
	}
	forEach(callback) {
		this.byQuad.forEach(callback);
	}
};
var SourcePredicateKey;
(function(_SourcePredicateKey) {
	function hashCode(key) {
		return dropHighestNonSignBit(Math.imul(hashTerm(key.source), 31) + hashTerm(key.predicate) | 0);
	}
	_SourcePredicateKey.hashCode = hashCode;
	function equals(a, b) {
		return equalTerms(a.source, b.source) && equalTerms(a.predicate, b.predicate);
	}
	_SourcePredicateKey.equals = equals;
})(SourcePredicateKey || (SourcePredicateKey = {}));
function iterateSmallSet(set) {
	return set instanceof Set ? set : set ? [set] : [];
}
function pushToIndex(index, key, quad) {
	let bucket = index.get(key);
	if (!bucket) {
		bucket = quad;
		index.set(key, bucket);
	} else if (bucket instanceof Set) bucket.add(quad);
	else {
		const single = bucket;
		bucket = /* @__PURE__ */ new Set();
		bucket.add(single);
		bucket.add(quad);
		index.set(key, bucket);
	}
}
function deleteFromIndex(index, key, quad) {
	const items = index.get(key);
	if (items) if (items instanceof Set) items.delete(quad);
	else index.delete(key);
}
function* filterBySPO(quads, subject, predicate, object) {
	for (const quad of quads) {
		if (subject && !equalTerms(subject, quad.subject)) continue;
		if (predicate && !equalTerms(predicate, quad.predicate)) continue;
		if (object && !equalTerms(object, quad.object)) continue;
		yield quad;
	}
}
function* filterByGraph(quads, graph) {
	for (const quad of quads) if (equalTerms(quad.graph, graph)) yield quad;
}
//#endregion
//#region src/data/utils.ts
/** Generates random 32-digit hexadecimal string. */
function generate128BitID() {
	function random32BitDigits() {
		return Math.floor((1 + Math.random()) * 4294967296).toString(16).substring(1);
	}
	return random32BitDigits() + random32BitDigits() + random32BitDigits() + random32BitDigits();
}
function makeCaseInsensitiveFilter(token) {
	const filter = new RegExp(escapeRegexp(token), "i");
	return (item) => filter.test(item);
}
function escapeRegexp(token) {
	return token.replace(/[\\^$.*+?()[\]{}|]/g, "\\$&");
}
//#endregion
//#region src/data/rdf/rdfDataProvider.ts
var BLANK_PREFIX = "urn:reactodia:blank:rdf:";
/**
* Provides graph data from in-memory [RDF/JS-compatible](https://rdf.js.org/data-model-spec/)
* graph dataset.
*
* @category Data
*/
var RdfDataProvider = class {
	factory;
	dataset;
	acceptBlankNodes;
	typePredicate;
	labelPredicate;
	imagePredicate;
	datatypePredicates;
	elementTypeBaseTypes;
	elementSubtypePredicate;
	linkTypeBaseTypes;
	EMPTY_LINKS = /* @__PURE__ */ new Set();
	constructor(options = {}) {
		this.factory = options.factory ?? DefaultDataFactory;
		this.dataset = indexedDataset(IndexQuadBy.S | IndexQuadBy.SP | IndexQuadBy.O | IndexQuadBy.OP);
		this.acceptBlankNodes = options.acceptBlankNodes ?? true;
		this.typePredicate = this.factory.namedNode(options.typePredicate ?? rdf.type);
		this.labelPredicate = options.labelPredicate === null ? null : this.factory.namedNode(options.labelPredicate ?? rdfs.label);
		this.imagePredicate = options.imagePredicate === null ? null : this.factory.namedNode(options.imagePredicate ?? schema.thumbnailUrl);
		this.datatypePredicates = new Set(options.datatypePredicates ?? []);
		this.elementTypeBaseTypes = (options.elementTypeBaseTypes ?? [owl.Class, rdfs.Class]).map((iri) => this.factory.namedNode(iri));
		this.elementSubtypePredicate = options.elementSubtypePredicate === null ? null : this.factory.namedNode(options.elementSubtypePredicate ?? rdfs.subClassOf);
		this.linkTypeBaseTypes = (options.linkTypeBaseTypes ?? [owl.ObjectProperty, rdf.Property]).map((iri) => this.factory.namedNode(iri));
	}
	/**
	* Adds [RDF/JS-compatible](https://rdf.js.org/data-model-spec/) graph in a form of quads (triplets)
	* to be able to query it via this provider's methods.
	*/
	addGraph(quads) {
		if (this.acceptBlankNodes) this.dataset.addAll(quads);
		else for (const q of quads) if (!(q.subject.termType === "BlankNode" || q.object.termType === "BlankNode" || q.graph.termType === "BlankNode")) this.dataset.add(q);
	}
	/**
	* Removes all previously added graphs to this provider so it becomes empty.
	*/
	clear() {
		this.dataset.clear();
	}
	/**
	* Encodes [RDF/JS-compatible](https://rdf.js.org/data-model-spec/) resource-like term
	* (named node or blank) into a plain string:
	*  - IRI (named node) value is returned as-is;
	*  - blank node is value is prepended with a prefix `urn:reactodia:blank:rdf:`.
	*
	* @see {@link RdfDataProvider.decodeTerm}
	*/
	encodeTerm(term) {
		return encodeTerm(term);
	}
	/**
	* Decodes a previously encoded by {@link RdfDataProvider.encodeTerm} resource-like term
	* (named node or blank) back from a plain string.
	*
	* @see {@link RdfDataProvider.encodeTerm}
	*/
	decodeTerm(iri) {
		return decodeTerm(iri, this.factory);
	}
	knownElementTypes(params) {
		const typeCounts = this.computeTypeCounts();
		for (const baseType of this.elementTypeBaseTypes) for (const t of this.dataset.iterateMatches(null, this.typePredicate, baseType)) if (isResourceTerm(t.subject)) {
			const elementTypeId = this.encodeTerm(t.subject);
			if (!typeCounts.has(elementTypeId)) typeCounts.set(elementTypeId, 0);
		}
		const foundEdges = new HashSet(hashSubtypeEdge, equalSubtypeEdges);
		if (this.elementSubtypePredicate) {
			for (const t of this.dataset.iterateMatches(null, this.elementSubtypePredicate, null)) if (isResourceTerm(t.subject) && isResourceTerm(t.object)) {
				const derivedTypeId = this.encodeTerm(t.subject);
				if (!typeCounts.has(derivedTypeId)) typeCounts.set(derivedTypeId, 0);
				const baseTypeId = this.encodeTerm(t.object);
				if (!typeCounts.has(baseTypeId)) typeCounts.set(baseTypeId, 0);
				foundEdges.add([this.encodeTerm(t.subject), this.encodeTerm(t.object)]);
			}
		}
		const elementTypes = [];
		const excluded = /* @__PURE__ */ new Set();
		for (const [typeId, count] of typeCounts) {
			const typeIri = this.decodeTerm(typeId);
			const label = this.labelPredicate ? findLiterals(this.dataset, typeIri, this.labelPredicate) : [];
			if (typeIri.termType === "BlankNode" && label.length === 0) excluded.add(typeId);
			else elementTypes.push({
				id: typeId,
				label,
				count
			});
		}
		const subtypeOf = [];
		for (const edge of foundEdges.values()) {
			const [from, to] = edge;
			if (!excluded.has(from) && !excluded.has(to)) subtypeOf.push(edge);
		}
		const classTree = {
			elementTypes,
			subtypeOf
		};
		return Promise.resolve(classTree);
	}
	knownLinkTypes(params) {
		const linkCounts = this.computeLinkCounts();
		for (const baseType of this.linkTypeBaseTypes) for (const t of this.dataset.iterateMatches(null, this.typePredicate, baseType)) if (isResourceTerm(t.subject)) {
			const linkTypeId = this.encodeTerm(t.subject);
			if (!linkCounts.has(linkTypeId)) linkCounts.set(linkTypeId, 0);
		}
		const models = /* @__PURE__ */ new Map();
		for (const [linkTypeId, count] of linkCounts) {
			const linkTypeIri = this.decodeTerm(linkTypeId);
			const label = this.labelPredicate ? findLiterals(this.dataset, linkTypeIri, this.labelPredicate) : [];
			if (linkTypeIri.termType === "BlankNode" && label.length === 0) continue;
			models.set(linkTypeId, {
				id: linkTypeId,
				label,
				count
			});
		}
		return Promise.resolve(Array.from(models.values()));
	}
	elementTypes(params) {
		const { classIds } = params;
		const models = /* @__PURE__ */ new Map();
		for (const classId of classIds) {
			const classIri = this.decodeTerm(classId);
			let instanceCount = 0;
			for (const t of this.dataset.iterateMatches(null, this.typePredicate, classIri)) instanceCount++;
			const model = {
				id: classId,
				label: this.labelPredicate ? findLiterals(this.dataset, classIri, this.labelPredicate) : [],
				count: instanceCount
			};
			models.set(classId, model);
		}
		return Promise.resolve(models);
	}
	propertyTypes(params) {
		const { propertyIds } = params;
		const models = /* @__PURE__ */ new Map();
		for (const propertyId of propertyIds) {
			const propertyIri = this.decodeTerm(propertyId);
			const model = {
				id: propertyId,
				label: this.labelPredicate ? findLiterals(this.dataset, propertyIri, this.labelPredicate) : []
			};
			models.set(propertyId, model);
		}
		return Promise.resolve(models);
	}
	linkTypes(params) {
		const { linkTypeIds } = params;
		const linkCounts = this.computeLinkCounts(linkTypeIds);
		const models = /* @__PURE__ */ new Map();
		for (const linkTypeId of linkTypeIds) {
			const linkTypeIri = this.decodeTerm(linkTypeId);
			const model = {
				id: linkTypeId,
				label: this.labelPredicate ? findLiterals(this.dataset, linkTypeIri, this.labelPredicate) : [],
				count: linkCounts.get(linkTypeId) ?? 0
			};
			models.set(linkTypeId, model);
		}
		return Promise.resolve(models);
	}
	elements(params) {
		const { elementIds } = params;
		const result = /* @__PURE__ */ new Map();
		for (const elementId of elementIds) {
			const elementIri = this.decodeTerm(elementId);
			if (this.dataset.hasMatches(elementIri, null, null)) {
				const model = {
					id: elementId,
					types: findTypes(this.dataset, elementIri, this.typePredicate),
					properties: findProperties(this.dataset, elementIri, this.datatypePredicates)
				};
				result.set(elementId, model);
			}
		}
		return Promise.resolve(result);
	}
	links(params) {
		const { primary, secondary, linkTypeIds } = params;
		const primarySet = new HashSet(hashTerm, equalTerms);
		for (const elementIri of primary) primarySet.add(this.decodeTerm(elementIri));
		const secondarySet = new HashSet(hashTerm, equalTerms);
		for (const elementIri of secondary) secondarySet.add(this.decodeTerm(elementIri));
		const linkTypeSet = linkTypeIds ? new Set(linkTypeIds) : void 0;
		const links = [];
		for (const t of this.dataset) if (isResourceTerm(t.subject) && t.predicate.termType === "NamedNode" && isResourceTerm(t.object) && (primarySet.has(t.subject) && secondarySet.has(t.object) || secondarySet.has(t.subject) && primarySet.has(t.object)) && (!linkTypeSet || linkTypeSet.has(t.predicate.value))) {
			const properties = findProperties(this.dataset, t, this.datatypePredicates);
			links.push({
				sourceId: this.encodeTerm(t.subject),
				targetId: this.encodeTerm(t.object),
				linkTypeId: this.encodeTerm(t.predicate),
				properties
			});
		}
		return Promise.resolve(links);
	}
	connectedLinkStats(params) {
		const { elementId } = params;
		const elementIri = this.decodeTerm(elementId);
		const outCounts = /* @__PURE__ */ new Map();
		for (const t of this.dataset.iterateMatches(elementIri, null, null)) if (t.predicate.termType === "NamedNode" && isResourceTerm(t.object)) {
			const linkTypeIri = this.encodeTerm(t.predicate);
			outCounts.set(linkTypeIri, (outCounts.get(linkTypeIri) ?? 0) + 1);
		}
		const inCounts = /* @__PURE__ */ new Map();
		for (const t of this.dataset.iterateMatches(null, null, elementIri)) if (t.predicate.termType === "NamedNode" && isResourceTerm(t.subject)) {
			const linkTypeIri = this.encodeTerm(t.predicate);
			inCounts.set(linkTypeIri, (inCounts.get(linkTypeIri) ?? 0) + 1);
		}
		const counts = [];
		for (const [linkTypeId, outCount] of outCounts) counts.push({
			id: linkTypeId,
			inCount: inCounts.get(linkTypeId) ?? 0,
			outCount
		});
		for (const [linkTypeId, inCount] of inCounts) {
			if (outCounts.has(linkTypeId)) continue;
			counts.push({
				id: linkTypeId,
				inCount,
				outCount: 0
			});
		}
		return Promise.resolve(counts);
	}
	lookup(params) {
		const items = new HashMap(hashTerm, equalTerms);
		let requiredTextFilter = params.text ? makeCaseInsensitiveFilter(params.text) : void 0;
		if (params.refElementId) {
			const refElementIri = this.decodeTerm(params.refElementId);
			const refLinkIri = params.refElementLinkId ? this.decodeTerm(params.refElementLinkId) : null;
			if (!params.linkDirection || params.linkDirection === "out") {
				for (const t of this.dataset.iterateMatches(refElementIri, refLinkIri, null)) if (t.predicate.termType === "NamedNode" && isResourceTerm(t.object)) {
					const term = t.object;
					let item = items.get(term);
					if (!item) {
						item = { term };
						items.set(term, item);
					}
					const predicate = this.encodeTerm(t.predicate);
					if (!item.outLinks) item.outLinks = /* @__PURE__ */ new Set();
					item.outLinks.add(predicate);
				}
			}
			if (!params.linkDirection || params.linkDirection === "in") {
				for (const t of this.dataset.iterateMatches(null, refLinkIri, refElementIri)) if (t.predicate.termType === "NamedNode" && isResourceTerm(t.subject)) {
					const term = t.subject;
					let item = items.get(term);
					if (!item) {
						item = { term };
						items.set(term, item);
					}
					const predicate = this.encodeTerm(t.predicate);
					if (!item.inLinks) item.inLinks = /* @__PURE__ */ new Set();
					item.inLinks.add(predicate);
				}
			}
			if (params.elementTypeId) {
				const typeTerm = this.decodeTerm(params.elementTypeId);
				for (const item of Array.from(items.values())) if (!this.dataset.hasMatches(item.term, this.typePredicate, typeTerm)) items.delete(item.term);
			}
		} else if (params.elementTypeId) {
			const typeTerm = this.decodeTerm(params.elementTypeId);
			for (const t of this.dataset.iterateMatches(null, this.typePredicate, typeTerm)) if (isResourceTerm(t.subject) && !items.has(t.subject)) items.set(t.subject, { term: t.subject });
		} else if (requiredTextFilter && this.labelPredicate) {
			for (const t of this.dataset.iterateMatches(null, this.labelPredicate, null)) if (isResourceTerm(t.subject) && t.object.termType === "Literal" && requiredTextFilter(t.object.value) && !items.has(t.subject)) items.set(t.subject, { term: t.subject });
			requiredTextFilter = void 0;
		}
		const linkedElements = [];
		const limit = typeof params.limit === "number" ? params.limit : Number.POSITIVE_INFINITY;
		for (const item of items.values()) {
			if (linkedElements.length >= limit) break;
			let properties = {};
			if (this.labelPredicate) {
				const labels = findLiterals(this.dataset, item.term, this.labelPredicate);
				if (requiredTextFilter) {
					let foundMatch = false;
					for (const label of labels) if (requiredTextFilter(label.value)) {
						foundMatch = true;
						break;
					}
					if (!foundMatch) continue;
				}
				properties = {
					...properties,
					[this.labelPredicate.value]: labels
				};
			}
			if (this.imagePredicate) {
				const imageTerm = findFirstIriOrLiteral(this.dataset, item.term, this.imagePredicate);
				if (imageTerm) properties = {
					...properties,
					[this.imagePredicate.value]: [imageTerm]
				};
			}
			const model = {
				id: this.encodeTerm(item.term),
				types: findTypes(this.dataset, item.term, this.typePredicate),
				properties
			};
			linkedElements.push({
				element: model,
				inLinks: item.inLinks ?? this.EMPTY_LINKS,
				outLinks: item.outLinks ?? this.EMPTY_LINKS
			});
		}
		return Promise.resolve(linkedElements);
	}
	computeTypeCounts() {
		const instanceCounts = /* @__PURE__ */ new Map();
		for (const t of this.dataset.iterateMatches(null, this.typePredicate, null)) if (isResourceTerm(t.object)) {
			const elementTypeId = this.encodeTerm(t.object);
			instanceCounts.set(elementTypeId, (instanceCounts.get(elementTypeId) ?? 0) + 1);
		}
		return instanceCounts;
	}
	computeLinkCounts(onlyLinkTypes) {
		const linkTypeSet = onlyLinkTypes ? new Set(onlyLinkTypes) : void 0;
		const linkStats = /* @__PURE__ */ new Map();
		for (const t of this.dataset) if (t.predicate.termType === "NamedNode") {
			const linkTypeId = this.encodeTerm(t.predicate);
			if (!linkTypeSet || linkTypeSet.has(linkTypeId)) linkStats.set(linkTypeId, (linkStats.get(linkTypeId) ?? 0) + 1);
		}
		return linkStats;
	}
};
function isResourceTerm(term) {
	switch (term.termType) {
		case "NamedNode":
		case "BlankNode": return true;
		default: return false;
	}
}
function findFirstIriOrLiteral(dataset, subject, predicate) {
	for (const t of dataset.iterateMatches(subject, predicate, null)) if (equalTerms(t.predicate, predicate) && (t.object.termType === "NamedNode" || t.object.termType === "Literal")) return t.object;
}
function findTypes(dataset, subject, predicate) {
	const typeSet = /* @__PURE__ */ new Set();
	for (const t of dataset.iterateMatches(subject, predicate, null)) if (isResourceTerm(t.object)) {
		const typeId = encodeTerm(t.object);
		typeSet.add(typeId);
	}
	return Array.from(typeSet).sort();
}
function findLiterals(dataset, subject, predicate) {
	const literals = [];
	for (const t of dataset.iterateMatches(subject, predicate, null)) if (equalTerms(t.predicate, predicate) && t.object.termType === "Literal") literals.push(t.object);
	return literals;
}
function findProperties(dataset, subject, datatypePredicates) {
	const properties = {};
	for (const { predicate, object: v } of dataset.iterateMatches(subject, null, null)) {
		if (predicate.termType !== "NamedNode") continue;
		if (v.termType === "Literal" || v.termType === "NamedNode" && datatypePredicates.has(predicate.value)) {
			const propertyId = encodeTerm(predicate);
			let values;
			if (Object.prototype.hasOwnProperty.call(properties, propertyId)) values = properties[propertyId];
			else {
				values = [];
				properties[propertyId] = values;
			}
			values.push(v);
		}
	}
	return properties;
}
function encodeTerm(term) {
	switch (term.termType) {
		case "NamedNode": return term.value;
		case "BlankNode": return BLANK_PREFIX + term.value;
		default: throw new Error(`Unexpected term type to encode: ${term.termType}`);
	}
}
function decodeTerm(iri, factory) {
	if (iri.startsWith(BLANK_PREFIX)) return factory.blankNode(iri.substring(24));
	else return factory.namedNode(iri);
}
//#endregion
//#region src/data/sparql/requestChunking.ts
function* chunkArray(items, measure, maxChunkSize) {
	if (!Number.isFinite(maxChunkSize)) {
		yield items;
		return;
	}
	const chunk = [];
	let chunkSize = 0;
	for (const item of items) {
		const addedSize = measure(item);
		if (chunkSize > 0 && chunkSize + addedSize > maxChunkSize) {
			yield [...chunk];
			chunk.length = 0;
			chunkSize = 0;
		}
		chunk.push(item);
		chunkSize += addedSize;
	}
	if (chunkSize > 0) yield chunk;
}
function* chunkUndirectedCrossProduct(main, paired, measure, maxChunkSize) {
	if (main.length < paired.length) [main, paired] = [paired, main];
	const pairedSet = new Set(paired);
	const selfSet = /* @__PURE__ */ new Set();
	for (const item of main) if (pairedSet.has(item)) selfSet.add(item);
	yield* chunkDirectedCrossProduct(main, paired, measure, maxChunkSize);
	yield* chunkDirectedCrossProduct(paired, main, measure, maxChunkSize, pairedSet.size === selfSet.size ? selfSet : void 0);
}
function* chunkDirectedCrossProduct(from, to, measure, maxChunkSize, excludedTargets) {
	const halfSize = Math.ceil(maxChunkSize / 2);
	const sources = /* @__PURE__ */ new Set();
	const targets = [];
	let sourceSize = 0;
	let targetSize = 0;
	function popPairedChunk() {
		const chunk = {
			sources: [...sources],
			targets: [...targets]
		};
		targets.length = 0;
		targetSize = 0;
		return chunk;
	}
	for (let i = 0; i < from.length; i++) {
		const fromItem = from[i];
		sources.add(fromItem);
		sourceSize += measure(fromItem);
		if (sourceSize >= halfSize || i === from.length - 1) {
			for (const toItem of to) {
				if (excludedTargets && excludedTargets.has(toItem)) continue;
				const addedSize = measure(toItem);
				if (targetSize > 0 && sourceSize + targetSize + addedSize > maxChunkSize) yield popPairedChunk();
				targets.push(toItem);
				targetSize += addedSize;
			}
			if (targetSize > 0) yield popPairedChunk();
			sources.clear();
			sourceSize = 0;
		}
	}
}
//#endregion
//#region src/data/sparql/sparqlModels.ts
function mapSparqlResponseIntoRdfJs(response, factory) {
	function mapSparqlBinding(binding) {
		const mapped = {};
		for (const key in binding) if (Object.prototype.hasOwnProperty.call(binding, key)) {
			const term = binding[key];
			switch (term.type) {
				case "uri":
					mapped[key] = factory.namedNode(term.value);
					break;
				case "bnode":
					mapped[key] = factory.blankNode(term.value);
					break;
				case "literal":
				case "typed-literal":
					mapped[key] = factory.literal(term.value, term["xml:lang"] ? term["xml:lang"] : term.datatype ? factory.namedNode(term.datatype) : void 0);
					break;
				default: throw new Error(`Unexpected SPARQL term type: "${term.type}"`);
			}
		}
		return mapped;
	}
	return {
		...response,
		results: {
			...response.results,
			bindings: response.results.bindings.map(mapSparqlBinding)
		}
	};
}
function isRdfIri(term) {
	return Boolean(term && term.termType === "NamedNode");
}
function isRdfBlank(term) {
	return Boolean(term && term.termType === "BlankNode");
}
function isRdfLiteral(term) {
	return Boolean(term && term.termType === "Literal");
}
//#endregion
//#region src/data/sparql/responseHandler.ts
var TYPE_PREDICATE = "urn:reactodia:sparql:type";
var LABEL_PREDICATE = "urn:reactodia:sparql:label";
var EMPTY_MAP = /* @__PURE__ */ new Map();
var EMPTY_SET = /* @__PURE__ */ new Set();
function getClassTree(response) {
	const nodes = /* @__PURE__ */ new Map();
	const edges = new HashSet(hashSubtypeEdge, equalSubtypeEdges);
	for (const binding of response.results.bindings) {
		if (!isRdfIri(binding.class)) continue;
		const classIri = binding.class.value;
		let node = nodes.get(classIri);
		if (!node) {
			node = createEmptyModel(classIri);
			nodes.set(classIri, node);
		}
		appendLabel(node.label, binding.label);
		if (binding.parent) {
			const parentIri = binding.parent.value;
			edges.add([classIri, parentIri]);
		}
		if (binding.instcount) node.count = parseCount(binding.instcount);
	}
	for (const binding of response.results.bindings) if (binding.parent) {
		const parentIri = binding.parent.value;
		if (!nodes.has(parentIri)) nodes.set(parentIri, createEmptyModel(parentIri));
	}
	function createEmptyModel(iri) {
		return {
			id: iri,
			label: [],
			count: void 0
		};
	}
	return {
		elementTypes: Array.from(nodes.values()),
		subtypeOf: Array.from(edges.values())
	};
}
function collectClassInfo(response, result) {
	for (const binding of response.results.bindings) {
		if (!binding.class) continue;
		const id = binding.class.value;
		const model = result.get(id);
		if (model) {
			appendLabel(model.label, binding.label);
			if (binding.instcount) {
				const instanceCount = parseCount(binding.instcount);
				if (instanceCount !== void 0) model.count = model.count === void 0 ? instanceCount : Math.max(model.count, instanceCount);
			}
		} else result.set(id, {
			id,
			label: binding.label ? [binding.label] : [],
			count: binding.instcount ? parseCount(binding.instcount) : void 0
		});
	}
}
function collectPropertyInfo(response, result) {
	for (const binding of response.results.bindings) {
		const propertyTypeId = binding.property.value;
		const existing = result.get(propertyTypeId);
		if (existing) appendLabel(existing.label, binding.label);
		else result.set(propertyTypeId, {
			id: binding.property.value,
			label: binding.label ? [binding.label] : []
		});
	}
}
function collectLinkTypes(response, result) {
	for (const binding of response.results.bindings) {
		const linkTypeId = binding.link.value;
		const existing = result.get(linkTypeId);
		if (existing) appendLabel(existing.label, binding.label);
		else result.set(linkTypeId, getLinkTypeInfo(binding));
	}
}
function getLinkTypes(response) {
	const result = /* @__PURE__ */ new Map();
	collectLinkTypes(response, result);
	return result;
}
function triplesToElementBinding(triples) {
	const elements = /* @__PURE__ */ new Map();
	const convertedResponse = {
		head: { vars: [
			"inst",
			"class",
			"label",
			"blankType",
			"propType",
			"propValue"
		] },
		results: { bindings: [] }
	};
	for (const t of triples) {
		if (!isRdfIri(t.subject)) continue;
		const subject = t.subject.value;
		if (!elements.has(subject)) elements.set(subject, createAndPushBinding(t));
		if (t.predicate.value === LABEL_PREDICATE && isRdfLiteral(t.object)) {
			if (elements.get(subject).label) elements.set(subject, createAndPushBinding(t));
			elements.get(subject).label = t.object;
		} else if (t.predicate.value === TYPE_PREDICATE && isRdfIri(t.object) && isRdfIri(t.predicate)) {
			if (elements.get(subject).class) elements.set(subject, createAndPushBinding(t));
			elements.get(subject).class = t.object;
		} else if ((isRdfIri(t.object) || isRdfLiteral(t.object)) && isRdfIri(t.predicate)) {
			if (elements.get(subject).propType) elements.set(subject, createAndPushBinding(t));
			elements.get(subject).propType = t.predicate;
			elements.get(subject).propValue = t.object;
		}
	}
	function createAndPushBinding(quad) {
		const binding = { inst: quad.subject };
		convertedResponse.results.bindings.push(binding);
		return binding;
	}
	return convertedResponse;
}
function getElementsInfo(response, types = EMPTY_MAP, propertyByPredicate = EMPTY_MAP, labelPredicate, openWorldProperties) {
	const instances = /* @__PURE__ */ new Map();
	for (const binding of response.results.bindings) {
		if (!isRdfIri(binding.inst)) continue;
		const iri = binding.inst.value;
		let model = instances.get(iri);
		if (!model) {
			model = emptyElementInfo(iri);
			instances.set(iri, model);
		}
		enrichElement(model, binding, labelPredicate);
	}
	if (!openWorldProperties || propertyByPredicate.size > 0) for (const model of instances.values()) model.properties = mapPropertiesByConfig(model, types.get(model.id), propertyByPredicate, openWorldProperties);
	for (const instance of instances.values()) instance.types.sort();
	return instances;
}
function mapPropertiesByConfig(model, modelTypes, propertyByPredicate, openWorldProperties) {
	const mapped = {};
	for (const propertyIri in model.properties) {
		if (!Object.hasOwnProperty.call(model.properties, propertyIri)) continue;
		const properties = propertyByPredicate.get(propertyIri);
		if (properties && properties.length > 0) {
			for (const property of properties) if (typeMatchesDomain(property, modelTypes)) mapped[property.id] = model.properties[propertyIri];
		} else if (openWorldProperties) mapped[propertyIri] = model.properties[propertyIri];
	}
	return mapped;
}
function enrichElementsWithImages(response, elements, imagePropertyIri) {
	for (const binding of response.results.bindings) {
		if (!isRdfIri(binding.inst)) continue;
		const elementInfo = elements.get(binding.inst.value);
		if (elementInfo) appendProperty(elementInfo.properties, imagePropertyIri, binding.image);
	}
}
function collectElementTypes(response, result) {
	for (const binding of response.results.bindings) if (isRdfIri(binding.inst) && isRdfIri(binding.class)) {
		const element = binding.inst.value;
		const type = binding.class.value;
		multimapAdd(result, element, type);
	}
}
function getLinksInfo(bindings, types = EMPTY_MAP, linkByPredicateType = EMPTY_MAP, openWorldLinks = true) {
	const links = new HashMap(hashLink, equalLinks);
	for (const binding of bindings) {
		const model = {
			sourceId: binding.source.value,
			linkTypeId: binding.type.value,
			targetId: binding.target.value,
			properties: {}
		};
		const existing = links.get(model);
		if (existing) {
			if (binding.propType && binding.propValue) appendProperty(existing.properties, binding.propType.value, binding.propValue);
		} else {
			if (binding.propType && binding.propValue) appendProperty(model.properties, binding.propType.value, binding.propValue);
			const linkConfigs = linkByPredicateType.get(model.linkTypeId);
			if (linkConfigs && linkConfigs.length > 0) {
				for (const linkConfig of linkConfigs) if (typeMatchesDomain(linkConfig, types.get(model.sourceId))) {
					const mappedModel = isDirectLink(linkConfig) ? {
						...model,
						linkTypeId: linkConfig.id
					} : model;
					links.set(mappedModel, mappedModel);
				}
			} else if (openWorldLinks) links.set(model, model);
		}
	}
	return Array.from(links.values());
}
function getConnectedLinkTypes(response, linkByPredicateType = EMPTY_MAP, openWorldLinks = true) {
	const linkTypes = /* @__PURE__ */ new Map();
	const pushLinkType = (linkType, direction) => {
		let connectedLink = linkTypes.get(linkType);
		if (!connectedLink) {
			connectedLink = { linkType };
			linkTypes.set(linkType, connectedLink);
		}
		if (isRdfLiteral(direction)) {
			if (direction.value === "in") connectedLink.hasInLink = true;
			else if (direction.value === "out") connectedLink.hasOutLink = true;
		}
	};
	for (const binding of response.results.bindings) {
		if (!isRdfIri(binding.link)) continue;
		const linkConfigs = linkByPredicateType.get(binding.link.value);
		if (linkConfigs && linkConfigs.length > 0) for (const linkConfig of linkConfigs) pushLinkType(isDirectLink(linkConfig) ? linkConfig.id : binding.link.value, binding.direction);
		else if (openWorldLinks) pushLinkType(binding.link.value, binding.direction);
	}
	return Array.from(linkTypes.values());
}
function getLinkStatistics(response) {
	for (const binding of response.results.bindings) if (isRdfIri(binding.link)) return getLinkCount(binding);
}
function getFilteredData(response, sourceTypes, linkByPredicateType, labelPredicate, openWorldLinks) {
	const predicateToConfig = linkByPredicateType ?? EMPTY_MAP;
	const instances = /* @__PURE__ */ new Map();
	const resultTypes = /* @__PURE__ */ new Map();
	const outPredicates = /* @__PURE__ */ new Map();
	const inPredicates = /* @__PURE__ */ new Map();
	for (const binding of response.results.bindings) {
		if (!isRdfIri(binding.inst) && !isRdfBlank(binding.inst)) continue;
		const iri = binding.inst.value;
		let model = instances.get(iri);
		if (!model) {
			model = emptyElementInfo(iri);
			instances.set(iri, model);
		}
		enrichElement(model, binding, labelPredicate);
		if (isRdfIri(binding.classAll)) multimapAdd(resultTypes, iri, binding.classAll.value);
		if (!openWorldLinks && binding.link && binding.direction) {
			const predicates = binding.direction.value === "in" ? inPredicates : binding.direction.value === "out" ? outPredicates : void 0;
			if (predicates) multimapAdd(predicates, model.id, binding.link.value);
		}
	}
	const linkedElements = [];
	for (const model of instances.values()) {
		const targetTypes = resultTypes.get(model.id);
		if (openWorldLinks || matchesDomainForLink(sourceTypes, outPredicates.get(model.id), predicateToConfig) && matchesDomainForLink(targetTypes, inPredicates.get(model.id), predicateToConfig)) {
			model.types.sort();
			const outLinks = new Set(translateLinkPredicates(sourceTypes, outPredicates.get(model.id) ?? EMPTY_SET, predicateToConfig, openWorldLinks));
			const inLinks = new Set(translateLinkPredicates(targetTypes, inPredicates.get(model.id) ?? EMPTY_SET, predicateToConfig, openWorldLinks));
			linkedElements.push({
				element: model,
				outLinks,
				inLinks
			});
		}
	}
	return linkedElements;
}
function matchesDomainForLink(types, predicates, linkByPredicateType) {
	if (!predicates) return true;
	const translatedPredicates = translateLinkPredicates(types, predicates, linkByPredicateType, false);
	for (const linkTypeId of translatedPredicates) return true;
	return false;
}
function* translateLinkPredicates(types, predicates, linkByPredicateType, openWorldLinks) {
	for (const predicate of predicates) {
		const matched = linkByPredicateType.get(predicate);
		if (matched) {
			for (const link of matched) if (typeMatchesDomain(link, types)) yield link.id;
		} else if (openWorldLinks) yield predicate;
	}
}
function isDirectLink(link) {
	return !/[?$][a-zA-Z]+\b/.test(link.path);
}
function isDirectProperty(property) {
	return !/[?$][a-zA-Z]+\b/.test(property.path);
}
function typeMatchesDomain(config, types) {
	if (!config.domain || config.domain.length === 0) return true;
	else if (!types) return false;
	else {
		for (const type of config.domain) if (types.has(type)) return true;
		return false;
	}
}
function enrichElement(element, binding, labelPredicate) {
	if (!element) return;
	if (binding.label) appendProperty(element.properties, labelPredicate, binding.label);
	if (binding.class && element.types.indexOf(binding.class.value) < 0) element.types.push(binding.class.value);
	if (binding.propType && binding.propValue && binding.propType.value !== LABEL_PREDICATE) appendProperty(element.properties, binding.propType.value, binding.propValue);
}
function appendLabel(container, newLabel) {
	if (!newLabel) return;
	for (const existing of container) if (equalTerms(existing, newLabel)) return;
	container.push(newLabel);
}
function appendProperty(properties, propType, propValue) {
	let values = Object.prototype.hasOwnProperty.call(properties, propType) ? properties[propType] : void 0;
	if (values) {
		for (const existing of values) if (equalTerms(existing, propValue)) return;
	} else {
		values = [];
		properties[propType] = values;
	}
	values.push(propValue);
}
function parseCount(countLiteral) {
	const numericCount = +countLiteral.value;
	return Number.isFinite(numericCount) ? numericCount : 0;
}
function getLinkCount(sLinkType) {
	return {
		id: sLinkType.link.value,
		inCount: parseCount(sLinkType.inCount),
		outCount: parseCount(sLinkType.outCount)
	};
}
function emptyElementInfo(id) {
	return {
		id,
		types: [],
		properties: {}
	};
}
function getLinkTypeInfo(binding) {
	return {
		id: binding.link.value,
		label: binding.label ? [binding.label] : [],
		count: binding.instcount ? parseCount(binding.instcount) : void 0
	};
}
//#endregion
//#region src/data/sparql/sparqlDataProviderSettings.ts
/**
* @category Constants
*/
var RdfSettings = {
	linkConfigurations: [],
	openWorldLinks: false,
	propertyConfigurations: [],
	openWorldProperties: false,
	linksInfoQuery: `SELECT ?source ?type ?target
            WHERE {
                \${linkConfigurations}
                VALUES (?source) {\${sourceIris}}
                VALUES (?target) {\${targetIris}}
            }`,
	defaultPrefix: "",
	schemaLabelProperty: "rdfs:label",
	dataLabelProperty: "rdfs:label",
	fullTextSearch: {
		prefix: "",
		queryPattern: ""
	},
	classTreeQuery: "",
	classInfoQuery: `SELECT ?class ?label ?instcount WHERE {
    VALUES(?class) {\${ids}}
    OPTIONAL {
        ?class \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
    BIND("" as ?instcount)
}`,
	linkTypesQuery: `SELECT DISTINCT ?link ?instcount ?label WHERE {
    \${linkTypesPattern}
    OPTIONAL {
        ?link \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
}`,
	linkTypesPattern: "",
	linkTypesInfoQuery: `SELECT ?link ?label WHERE {
    VALUES(?link) {\${ids}}
    OPTIONAL {
        ?link \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
}`,
	propertyInfoQuery: `SELECT ?property ?label WHERE {
    VALUES(?property) {\${ids}}
    OPTIONAL {
        ?property \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
}`,
	elementInfoQuery: "",
	imageQueryPattern: "",
	linkTypesOfQuery: "",
	linkTypesStatisticsQuery: "",
	lookupQuery: `SELECT \${outerProjection} WHERE {
    \${filterInnerPrelude}
    {
        SELECT DISTINCT \${innerProjection} WHERE {
            \${filterByType}
            \${filterByRefElementLink}
            \${filterByText}
            \${filterAdditionalRestriction}
        } \${orderBy} \${limit}
    }
    \${queryTypes}
    \${queryElementInfo}
} \${orderBy}`,
	filterRefElementLinkPattern: "",
	filterTypePattern: "",
	filterAdditionalRestriction: "",
	filterElementInfoPattern: ""
};
var WikidataSettingsOverride = {
	defaultPrefix: `PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
 PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
 PREFIX wdt: <http://www.wikidata.org/prop/direct/>
 PREFIX wd: <http://www.wikidata.org/entity/>
 PREFIX owl:  <http://www.w3.org/2002/07/owl#>
`,
	schemaLabelProperty: "rdfs:label",
	dataLabelProperty: "rdfs:label",
	classTreeQuery: `
        SELECT distinct ?class ?label ?parent WHERE {
            ?class rdfs:label ?label.
            \${labelLanguageFilter}
            { ?class wdt:P279 wd:Q35120. }
            UNION
            { ?parent wdt:P279 wd:Q35120.
            ?class wdt:P279 ?parent. }
            UNION
            { ?parent wdt:P279/wdt:P279 wd:Q35120.
            ?class wdt:P279 ?parent. }
        }
    `,
	linkTypesQuery: "",
	linkTypesPattern: `
        ?link wdt:P279* wd:Q18616576.
        BIND(0 as ?instcount)
    `,
	linkTypesInfoQuery: `SELECT ?link ?label WHERE {
    VALUES (?link) {\${ids}}
    OPTIONAL {
        ?claim <http://wikiba.se/ontology#directClaim> ?link .
        ?claim \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
}`,
	propertyInfoQuery: `SELECT ?property ?label WHERE {
    VALUES (?property) {\${ids}}
    OPTIONAL {
        ?claim <http://wikiba.se/ontology#directClaim> ?property .
        ?claim \${schemaLabelProperty} ?label
        \${labelLanguageFilter}
    }
}`,
	elementInfoQuery: `
        CONSTRUCT {
            ?inst <urn:reactodia:sparql:type> ?class .
            ?inst <urn:reactodia:sparql:label> ?label .
            ?inst ?propType ?propValue.
        } WHERE {
            VALUES (?inst) {\${ids}}
            OPTIONAL {
                ?inst wdt:P31 ?class
            }
            OPTIONAL {
                ?inst rdfs:label ?label
                \${labelLanguageFilter}
            }
            OPTIONAL {
                \${propertyConfigurations}
                FILTER (isLiteral(?propValue))
                \${valueLanguageFilter}
            }
        }
    `,
	imageQueryPattern: ` { ?inst ?linkType ?fullImage } union { ?inst wdt:P163/wdt:P18 ?fullImage }
                BIND(CONCAT("https://commons.wikimedia.org/w/thumb.php?f=",
                    STRAFTER(STR(?fullImage), "Special:FilePath/"), "&w=200") AS ?image)`,
	linkTypesOfQuery: `
        SELECT DISTINCT ?link ?direction
        WHERE {
            \${linkConfigurations}
            ?claim <http://wikiba.se/ontology#directClaim> ?link .
        }
    `,
	linkTypesStatisticsQuery: `
        SELECT (\${linkId} as ?link) (COUNT(?outObject) AS ?outCount) (COUNT(?inObject) AS ?inCount)
        WHERE {
            {
                {
                    SELECT DISTINCT ?outObject WHERE {
                        \${linkConfigurationOut}
                        FILTER(ISIRI(?outObject))
                        ?outObject ?someprop ?someobj.
                    }
                    LIMIT 101
                }
            } UNION {
                {
                    SELECT DISTINCT ?inObject WHERE {
                        \${linkConfigurationIn}
                        FILTER(ISIRI(?inObject))
                        ?inObject ?someprop ?someobj.
                    }
                    LIMIT 101
                }
            }
        }
    `,
	filterRefElementLinkPattern: "?claim <http://wikiba.se/ontology#directClaim> ?link .",
	filterTypePattern: "?inst wdt:P31 ?instType. ?instType wdt:P279* ?class",
	filterAdditionalRestriction: `FILTER ISIRI(?inst)
                        BIND(STR(?inst) as ?strInst)
                        FILTER exists {?inst ?someprop ?someobj}
`,
	filterElementInfoPattern: `
        OPTIONAL {?inst wdt:P31 ?foundClass}
        BIND (coalesce(?foundClass, owl:Thing) as ?class)
        OPTIONAL {
            ?inst rdfs:label ?label
            \${labelLanguageFilter}
        }
    `,
	fullTextSearch: {
		prefix: "PREFIX bds: <http://www.bigdata.com/rdf/search#>\n",
		queryPattern: `
            SERVICE wikibase:mwapi {
                bd:serviceParam wikibase:endpoint "www.wikidata.org";
                    wikibase:api "EntitySearch";
                    mwapi:search "\${text}";
                    mwapi:language "en".
                ?inst wikibase:apiOutputItem mwapi:item.
                ?num wikibase:apiOrdinal true.
            }
            BIND(IF(
                STRLEN(STR(?inst)) > 33,
                0-<http://www.w3.org/2001/XMLSchema#integer>(SUBSTR(STR(?inst), 33)),
                -10000
            ) as ?score)
        `
	}
};
/**
* @category Constants
*/
var WikidataSettings = {
	...RdfSettings,
	...WikidataSettingsOverride
};
var OwlRdfsSettingsOverride = {
	defaultPrefix: `PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
 PREFIX rdf:  <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
 PREFIX owl:  <http://www.w3.org/2002/07/owl#>
`,
	schemaLabelProperty: "rdfs:label",
	dataLabelProperty: "rdfs:label",
	fullTextSearch: {
		prefix: "",
		queryPattern: `?inst \${dataLabelProperty} ?search1
        FILTER regex(COALESCE(str(?search1)), "\${text}", "i")
        BIND(0 as ?score)
`,
		extractLabel: true
	},
	classTreeQuery: `
        SELECT ?class ?label ?parent
        WHERE {
            {
                ?class a rdfs:Class
            } UNION {
                ?class a owl:Class
            }
            FILTER ISIRI(?class)
            OPTIONAL {
                ?class rdfs:label ?label
                \${labelLanguageFilter}
            }
            OPTIONAL {?class rdfs:subClassOf ?parent. FILTER ISIRI(?parent)}
        }
    `,
	linkTypesPattern: `
        { ?link a rdf:Property }
        UNION
        { ?link a owl:ObjectProperty }
        BIND('' as ?instcount)
    `,
	elementInfoQuery: `
        CONSTRUCT {
            ?inst <urn:reactodia:sparql:type> ?class .
            ?inst <urn:reactodia:sparql:label> ?label .
            ?inst ?propType ?propValue.
        } WHERE {
            VALUES (?inst) {\${ids}}
            OPTIONAL { ?inst a ?class }
            OPTIONAL {
                ?inst \${dataLabelProperty} ?label
                \${labelLanguageFilter}
            }
            OPTIONAL {
                \${propertyConfigurations}
                FILTER (isLiteral(?propValue))
                \${valueLanguageFilter}
            }
        }
    `,
	imageQueryPattern: "{ ?inst ?linkType ?image } UNION { [] ?linkType ?inst. BIND(?inst as ?image) }",
	linkTypesOfQuery: `
        SELECT DISTINCT ?link ?direction
        WHERE {
            \${linkConfigurations}
        }
    `,
	linkTypesStatisticsQuery: `
        SELECT ?link ?outCount ?inCount
        WHERE {
            {
                SELECT (\${linkId} as ?link) (count(?outObject) as ?outCount) WHERE {
                    \${linkConfigurationOut}
                    \${navigateElementFilterOut}
                } LIMIT 101
            } {
                SELECT (\${linkId} as ?link) (count(?inObject) as ?inCount) WHERE {
                    \${linkConfigurationIn}
                    \${navigateElementFilterIn}
                } LIMIT 101
            }
        }
    `,
	filterRefElementLinkPattern: "",
	filterTypePattern: "?inst a ?instType. ?instType rdfs:subClassOf* ?class",
	filterElementInfoPattern: `
        OPTIONAL {?inst rdf:type ?foundClass}
        BIND (coalesce(?foundClass, owl:Thing) as ?class)
        OPTIONAL {
            ?inst \${dataLabelProperty} ?label
            \${labelLanguageFilter}
        }
    `,
	filterAdditionalRestriction: ""
};
/**
* @category Constants
*/
var OwlRdfsSettings = {
	...RdfSettings,
	...OwlRdfsSettingsOverride
};
var OWLStatsOverride = { classTreeQuery: `
        SELECT ?class ?instcount ?label ?parent
        WHERE {
            {SELECT ?class (count(?inst) as ?instcount)
                WHERE {
                    ?inst rdf:type ?class.
                    FILTER ISIRI(?class)
                } GROUP BY ?class } UNION
            {
                ?class rdf:type rdfs:Class
            } UNION {
                ?class rdf:type owl:Class
            }
            OPTIONAL {
                ?class rdfs:label ?label
                \${labelLanguageFilter}
            }
            OPTIONAL {?class rdfs:subClassOf ?parent. FILTER ISIRI(?parent)}
        }
    ` };
/**
* @category Constants
*/
var OwlStatsSettings = {
	...OwlRdfsSettings,
	...OWLStatsOverride
};
var DBPediaOverride = {
	fullTextSearch: {
		prefix: "PREFIX dbo: <http://dbpedia.org/ontology/>\n",
		queryPattern: `
              ?inst rdfs:label ?searchLabel.
              ?searchLabel bif:contains "\${text}".
              ?inst dbo:wikiPageID ?origScore .
              BIND(0-?origScore as ?score)
        `
	},
	classTreeQuery: `
        SELECT distinct ?class ?label ?parent WHERE {
            ?class rdfs:label ?label.
            OPTIONAL {?class rdfs:subClassOf ?parent}
            ?root rdfs:subClassOf owl:Thing.
            ?class rdfs:subClassOf? | rdfs:subClassOf/rdfs:subClassOf ?root
        }
    `,
	elementInfoQuery: `
        CONSTRUCT {
            ?inst <urn:reactodia:sparql:type> ?class .
            ?inst <urn:reactodia:sparql:label> ?label .
            ?inst ?propType ?propValue.
        } WHERE {
            VALUES (?inst) {\${ids}}
            ?inst a ?class .
            ?inst rdfs:label ?label .
            FILTER (!contains(str(?class), 'http://dbpedia.org/class/yago'))
            OPTIONAL {
                \${propertyConfigurations}
                FILTER (isLiteral(?propValue))
            }
        }
    `,
	filterTypePattern: "?inst a ?instType. ?instType rdfs:subClassOf* ?class",
	filterElementInfoPattern: `
        OPTIONAL {?inst rdf:type ?foundClass. FILTER (!contains(str(?foundClass), 'http://dbpedia.org/class/yago'))}
        BIND (coalesce(?foundClass, owl:Thing) as ?class)
        OPTIONAL {?inst \${dataLabelProperty} ?label}`,
	imageQueryPattern: ` { ?inst ?linkType ?fullImage } UNION { [] ?linkType ?inst. BIND(?inst as ?fullImage) }
            BIND(CONCAT("https://commons.wikimedia.org/w/thumb.php?f=",
            STRAFTER(STR(?fullImage), "Special:FilePath/"), "&w=200") AS ?image)
    `
};
/**
* @category Constants
*/
var DBPediaSettings = {
	...OwlRdfsSettings,
	...DBPediaOverride
};
//#endregion
//#region src/data/sparql/sparqlDataProvider.ts
/**
* Provides graph data by requesting it from a SPARQL endpoint.
*
* @category Data
*/
var SparqlDataProvider = class {
	factory;
	options;
	settings;
	queryFunction;
	acceptBlankNodes = false;
	chunkMeasure;
	linkByPredicate = /* @__PURE__ */ new Map();
	linkById = /* @__PURE__ */ new Map();
	openWorldLinks;
	propertyByPredicate = /* @__PURE__ */ new Map();
	openWorldProperties;
	labelPredicate;
	imagePredicate;
	constructor(options, settings = OwlStatsSettings) {
		const { factory = DefaultDataFactory, queryFunction = queryInternal } = options;
		this.factory = factory;
		this.options = options;
		this.settings = settings;
		this.queryFunction = queryFunction;
		const { chunk, queryMethod = "GET", prepareLabelPredicate, prepareImagePredicate } = this.options;
		if (chunk) {
			if (!Number.isSafeInteger(chunk.maxSize)) throw new Error("SparqlDataProviderOptions.chunk.maxSize should be a non-negative number");
		}
		if (chunk) this.chunkMeasure = {
			maxSize: chunk.maxSize,
			measure: makeChunkMeasure(chunk.unit)
		};
		else if (chunk === void 0 && queryMethod === "GET") this.chunkMeasure = {
			maxSize: 2500,
			measure: makeChunkMeasure("totalLength")
		};
		else this.chunkMeasure = {
			maxSize: Infinity,
			measure: makeChunkMeasure("itemCount")
		};
		for (const link of settings.linkConfigurations) {
			this.linkById.set(link.id, link);
			const predicate = isDirectLink(link) ? link.path : link.id;
			multimapArrayAdd(this.linkByPredicate, predicate, link);
		}
		this.openWorldLinks = settings.linkConfigurations.length === 0 || Boolean(settings.openWorldLinks);
		for (const property of settings.propertyConfigurations) {
			const predicate = isDirectProperty(property) ? property.path : property.id;
			multimapArrayAdd(this.propertyByPredicate, predicate, property);
		}
		this.openWorldProperties = settings.propertyConfigurations.length === 0 || Boolean(settings.openWorldProperties);
		this.labelPredicate = prepareLabelPredicate ?? rdfs.label;
		this.imagePredicate = prepareImagePredicate ?? schema.thumbnailUrl;
	}
	async queryChunked(items, callback) {
		const { maxSize, measure } = this.chunkMeasure;
		const tasks = [];
		for (const chunk of chunkArray(items, measure, maxSize)) tasks.push(callback(chunk));
		await Promise.all(tasks);
	}
	async knownElementTypes(params) {
		const { signal } = params;
		const { defaultPrefix, schemaLabelProperty, filterOnlyLanguages, classTreeQuery } = this.settings;
		if (!classTreeQuery) return {
			elementTypes: [],
			subtypeOf: []
		};
		const query = defaultPrefix + resolveTemplate(classTreeQuery, {
			schemaLabelProperty,
			labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
		});
		const classTree = getClassTree(await this.executeSparqlSelect(query, { signal }));
		if (this.options.prepareLabels) await attachLabels(classTree.elementTypes, this.options.prepareLabels, signal);
		return classTree;
	}
	async propertyTypes(params) {
		const { propertyIds, signal } = params;
		const { defaultPrefix, schemaLabelProperty, filterOnlyLanguages, propertyInfoQuery } = this.settings;
		const properties = /* @__PURE__ */ new Map();
		if (propertyInfoQuery) await this.queryChunked(propertyIds, async (batch) => {
			const query = defaultPrefix + resolveTemplate(propertyInfoQuery, {
				ids: batch.map(escapeIri).map((id) => ` ( ${id} )`).join(" "),
				schemaLabelProperty,
				labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
			});
			collectPropertyInfo(await this.executeSparqlSelect(query, { signal }), properties);
		});
		else for (const id of propertyIds) properties.set(id, {
			id,
			label: []
		});
		if (this.options.prepareLabels) await attachLabels(Array.from(properties.values()), this.options.prepareLabels, signal);
		return properties;
	}
	async elementTypes(params) {
		const { classIds, signal } = params;
		const { defaultPrefix, schemaLabelProperty, filterOnlyLanguages, classInfoQuery } = this.settings;
		const classes = /* @__PURE__ */ new Map();
		if (classInfoQuery) await this.queryChunked(classIds, async (batch) => {
			const query = defaultPrefix + resolveTemplate(classInfoQuery, {
				ids: batch.map(escapeIri).map((id) => ` ( ${id} )`).join(" "),
				schemaLabelProperty,
				labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
			});
			collectClassInfo(await this.executeSparqlSelect(query, { signal }), classes);
		});
		else for (const classId of classIds) classes.set(classId, {
			id: classId,
			label: []
		});
		if (this.options.prepareLabels) await attachLabels(Array.from(classes.values()), this.options.prepareLabels, signal);
		return classes;
	}
	async linkTypes(params) {
		const { linkTypeIds, signal } = params;
		const { defaultPrefix, schemaLabelProperty, filterOnlyLanguages, linkTypesInfoQuery } = this.settings;
		const linkTypes = /* @__PURE__ */ new Map();
		if (linkTypesInfoQuery) await this.queryChunked(linkTypeIds, async (batch) => {
			const query = defaultPrefix + resolveTemplate(linkTypesInfoQuery, {
				ids: batch.map(escapeIri).map((id) => ` ( ${id} )`).join(" "),
				schemaLabelProperty,
				labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
			});
			collectLinkTypes(await this.executeSparqlSelect(query, { signal }), linkTypes);
		});
		else for (const typeId of linkTypeIds) linkTypes.set(typeId, {
			id: typeId,
			label: []
		});
		if (this.options.prepareLabels) await attachLabels(Array.from(linkTypes.values()), this.options.prepareLabels, signal);
		return linkTypes;
	}
	async knownLinkTypes(params) {
		const { signal } = params;
		const { defaultPrefix, schemaLabelProperty, filterOnlyLanguages, linkTypesQuery, linkTypesPattern } = this.settings;
		if (!linkTypesQuery) return [];
		const query = defaultPrefix + resolveTemplate(linkTypesQuery, {
			linkTypesPattern,
			schemaLabelProperty,
			labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
		});
		const linkTypes = getLinkTypes(await this.executeSparqlSelect(query, { signal }));
		if (this.options.prepareLabels) await attachLabels(Array.from(linkTypes.values()), this.options.prepareLabels, signal);
		return Array.from(linkTypes.values());
	}
	async elements(params) {
		const { elementIds, signal } = params;
		const triples = [];
		if (elementIds.length > 0) await this.queryChunked(elementIds, async (batch) => {
			const ids = batch.map(escapeIri).map((id) => ` (${id})`).join(" ");
			const { defaultPrefix, dataLabelProperty, filterOnlyLanguages, elementInfoQuery } = this.settings;
			const query = defaultPrefix + resolveTemplate(elementInfoQuery, {
				ids,
				dataLabelProperty,
				labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages),
				valueLanguageFilter: formatLanguageFilter("?propValue", filterOnlyLanguages),
				propertyConfigurations: this.formatPropertyInfo()
			});
			const response = await this.executeSparqlConstruct(query);
			for (const triple of response) triples.push(triple);
		});
		const types = this.queryManyElementTypes(this.settings.propertyConfigurations.length > 0 ? elementIds : [], signal);
		const elementModels = getElementsInfo(triplesToElementBinding(triples), await types, this.propertyByPredicate, this.labelPredicate, this.openWorldProperties);
		if (this.options.prepareLabels) await attachProperties(Array.from(elementModels.values()), this.options.prepareLabels, this.labelPredicate, signal);
		if (this.options.prepareImages) await prepareElementImages(elementModels, this.options.prepareImages, this.imagePredicate, this.factory, signal);
		else if (this.options.imagePropertyUris && this.options.imagePropertyUris.length) await this.attachImages(elementModels, this.options.imagePropertyUris, signal);
		return elementModels;
	}
	async attachImages(elements, imagePropertyIris, signal) {
		const imageProperties = imagePropertyIris.map(escapeIri).map((id) => ` ( ${id} )`).join(" ");
		await this.queryChunked(Array.from(elements.keys()), async (batch) => {
			const ids = batch.map((id) => ` ( ${escapeIri(id)} )`).join(" ");
			const query = this.settings.defaultPrefix + `
                SELECT ?inst ?linkType ?image
                WHERE {
                    VALUES (?inst) {${ids}}
                    VALUES (?linkType) {${imageProperties}}
                    ${this.settings.imageQueryPattern}
                }
            `;
			try {
				enrichElementsWithImages(await this.executeSparqlSelect(query, { signal }), elements, this.imagePredicate);
			} catch (err) {
				console.warn("Failed to load entity image URLs", err);
			}
		});
	}
	async links(params) {
		const { primary, secondary, linkTypeIds, signal } = params;
		const { filterOnlyLanguages, linksInfoQuery } = this.settings;
		const propLanguageFilter = formatLanguageFilter("?propValue", filterOnlyLanguages);
		const linkConfigurations = this.formatLinkLinks();
		let bindings;
		if (primary.length > 0 && secondary.length > 0) if (linksInfoQuery.includes("${ids}")) bindings = this.queryUndirectedLinks(primary, secondary, {
			propLanguageFilter,
			linkConfigurations
		}, signal);
		else bindings = this.queryDirectedLinks(primary, secondary, {
			propLanguageFilter,
			linkConfigurations
		}, signal);
		else bindings = Promise.resolve([]);
		let elementsForTypes;
		if (this.linkByPredicate.size > 0) {
			elementsForTypes = new Set(primary);
			for (const iri of secondary) elementsForTypes.add(iri);
		}
		const types = this.queryManyElementTypes(elementsForTypes ? Array.from(elementsForTypes) : [], signal);
		let linksInfo = getLinksInfo(await bindings, await types, this.linkByPredicate, this.openWorldLinks);
		if (linkTypeIds) {
			const allowedLinkTypes = new Set(linkTypeIds);
			linksInfo = linksInfo.filter((link) => allowedLinkTypes.has(link.linkTypeId));
		}
		return linksInfo;
	}
	async queryUndirectedLinks(primary, secondary, queryVariables, signal) {
		const { propLanguageFilter, linkConfigurations } = queryVariables;
		const { defaultPrefix, linksInfoQuery } = this.settings;
		const allElementIris = new Set(primary);
		for (const iri of secondary) allElementIris.add(iri);
		const query = defaultPrefix + resolveTemplate(linksInfoQuery, {
			ids: formatValueClauseContent(allElementIris),
			propLanguageFilter,
			linkConfigurations
		});
		return (await this.executeSparqlSelect(query, { signal })).results.bindings;
	}
	async queryDirectedLinks(primary, secondary, queryVariables, signal) {
		const { propLanguageFilter, linkConfigurations } = queryVariables;
		const { defaultPrefix, linksInfoQuery } = this.settings;
		const { maxSize, measure } = this.chunkMeasure;
		const bindings = [];
		const tasks = Array.from(chunkUndirectedCrossProduct(primary, secondary, measure, maxSize), async (chunk) => {
			const query = defaultPrefix + resolveTemplate(linksInfoQuery, {
				sourceIris: formatValueClauseContent(chunk.sources),
				targetIris: formatValueClauseContent(chunk.targets),
				propLanguageFilter,
				linkConfigurations
			});
			const result = await this.executeSparqlSelect(query, { signal });
			for (const binding of result.results.bindings) bindings.push(binding);
		});
		await Promise.all(tasks);
		return bindings;
	}
	async connectedLinkStats(params) {
		const { elementId, inexactCount, signal } = params;
		const { defaultPrefix, linkTypesOfQuery, linkTypesStatisticsQuery, filterTypePattern } = this.settings;
		const bindDirection = /\?direction\b/.test(linkTypesOfQuery);
		const elementIri = escapeIri(elementId);
		const forAll = this.formatLinkUnion(elementId, void 0, void 0, "?outObject", "?inObject", bindDirection);
		if (forAll.usePredicatePart) {
			forAll.unionParts.push(`{ ${elementIri} ?link ?outObject ${bindDirection ? "BIND(\"out\" AS ?direction)" : ""} }`);
			forAll.unionParts.push(`{ ?inObject ?link ${elementIri} ${bindDirection ? "BIND(\"in\" AS ?direction)" : ""} }`);
		}
		const query = defaultPrefix + resolveTemplate(linkTypesOfQuery, {
			elementIri,
			linkConfigurations: forAll.unionParts.join("\nUNION\n")
		});
		const linkTypeBindings = await this.executeSparqlSelect(query, { signal });
		const hasConnectedDirection = linkTypeBindings.head.vars.includes("direction");
		const connectedLinkTypes = getConnectedLinkTypes(linkTypeBindings, this.linkByPredicate, this.openWorldLinks);
		const navigateElementFilterOut = this.acceptBlankNodes ? "FILTER (IsIri(?outObject) || IsBlank(?outObject))" : "FILTER IsIri(?outObject)";
		const navigateElementFilterIn = this.acceptBlankNodes ? "FILTER (IsIri(?inObject) || IsBlank(?inObject))" : "FILTER IsIri(?inObject)";
		const foundLinkStats = [];
		await Promise.all(connectedLinkTypes.map(async ({ linkType, hasInLink, hasOutLink }) => {
			const linkConfig = this.linkById.get(linkType);
			let linkConfigurationOut;
			let linkConfigurationIn;
			if (!linkConfig || isDirectLink(linkConfig)) {
				const predicate = escapeIri(linkConfig && isDirectLink(linkConfig) ? linkConfig.path : linkType);
				linkConfigurationOut = `${elementIri} ${predicate} ?outObject`;
				linkConfigurationIn = `?inObject ${predicate} ${elementIri}`;
			} else {
				linkConfigurationOut = this.formatLinkPath(linkConfig.path, elementIri, "?outObject");
				linkConfigurationIn = this.formatLinkPath(linkConfig.path, "?inObject", elementIri);
			}
			if (linkConfig && linkConfig.domain && linkConfig.domain.length > 0) {
				const commaSeparatedDomains = linkConfig.domain.map(escapeIri).join(", ");
				const restrictionOut = filterTypePattern.replace(/[?$]inst\b/g, elementIri);
				const restrictionIn = filterTypePattern.replace(/[?$]inst\b/g, "?inObject");
				linkConfigurationOut += ` { ${restrictionOut} FILTER(?class IN (${commaSeparatedDomains})) }`;
				linkConfigurationIn += ` { ${restrictionIn} FILTER(?class IN (${commaSeparatedDomains})) }`;
			}
			if (!linkTypesStatisticsQuery || inexactCount && hasConnectedDirection) foundLinkStats.push({
				id: linkType,
				inCount: hasInLink ? 1 : 0,
				outCount: hasOutLink ? 1 : 0,
				inexact: true
			});
			else {
				const statsQuery = defaultPrefix + resolveTemplate(linkTypesStatisticsQuery, {
					linkId: escapeIri(linkType),
					elementIri,
					linkConfigurationOut,
					linkConfigurationIn,
					navigateElementFilterOut,
					navigateElementFilterIn
				});
				const linkStats = getLinkStatistics(await this.executeSparqlSelect(statsQuery, { signal }));
				if (linkStats) foundLinkStats.push(linkStats);
			}
		}));
		return foundLinkStats;
	}
	async lookup(baseParams) {
		const { signal } = baseParams;
		const params = {
			...baseParams,
			limit: baseParams.limit === void 0 ? 100 : baseParams.limit
		};
		const types = this.querySingleElementTypes(params.refElementId && this.settings.linkConfigurations.length > 0 ? params.refElementId : void 0, signal);
		const filterQuery = this.createFilterQuery(params);
		const linkedElements = getFilteredData(await this.executeSparqlSelect(filterQuery, { signal }), await types, this.linkByPredicate, this.labelPredicate, this.openWorldLinks);
		if (this.options.prepareLabels) await attachProperties(linkedElements.map((linked) => linked.element), this.options.prepareLabels, this.labelPredicate, signal);
		return linkedElements;
	}
	createFilterQuery(params) {
		if (!params.refElementId && params.refElementLinkId) throw new Error("Cannot execute refElementLink filter without refElement");
		let outerProjection = "?inst ?class ?label";
		let innerProjection = "?inst";
		let filterByRefElementLink = "";
		let queryTypes = "";
		if (params.refElementId) {
			outerProjection += " ?link ?direction";
			innerProjection += " ?link ?direction";
			filterByRefElementLink = this.createRefQueryPart({
				elementId: params.refElementId,
				linkId: params.refElementLinkId,
				direction: params.linkDirection
			});
			if (this.settings.linkConfigurations.length > 0) {
				outerProjection += " ?classAll";
				queryTypes = this.settings.filterTypePattern.replace(/[?$]class\b/g, "?classAll");
			}
		}
		let filterByType = "";
		if (params.elementTypeId) {
			const elementTypeIri = escapeIri(params.elementTypeId);
			filterByType = this.settings.filterTypePattern.replace(/[?$]class\b/g, elementTypeIri);
		}
		const { defaultPrefix, dataLabelProperty, lookupQuery, filterOnlyLanguages, filterElementInfoPattern, fullTextSearch } = this.settings;
		const queryElementInfo = resolveTemplate(filterElementInfoPattern, {
			dataLabelProperty,
			labelLanguageFilter: formatLanguageFilter("?label", filterOnlyLanguages)
		});
		let filterByText = "";
		let orderBy = "";
		if (params.text) {
			innerProjection += " ?score";
			if (this.settings.fullTextSearch.extractLabel) filterByText += sparqlExtractLabel("?inst", "?extractedLabel");
			filterByText = resolveTemplate(fullTextSearch.queryPattern, {
				text: params.text,
				dataLabelProperty
			});
			if (filterByText) orderBy = "ORDER BY DESC(?score)";
		}
		let limitPart = "";
		if (typeof params.limit === "number") limitPart = `LIMIT ${params.limit}`;
		return defaultPrefix + "\n" + fullTextSearch.prefix + "\n" + resolveTemplate(lookupQuery, {
			innerProjection,
			outerProjection,
			filterInnerPrelude: this.settings.filterInnerPrelude,
			filterByType,
			filterByRefElementLink,
			filterByText,
			filterAdditionalRestriction: this.settings.filterAdditionalRestriction,
			orderBy,
			limit: limitPart,
			queryTypes,
			queryElementInfo
		});
	}
	/**
	* Executes arbitrary SPARQL SELECT query and returns the result tuples.
	*/
	executeSparqlSelect(query, options) {
		const method = this.options.queryMethod ?? "GET";
		const { signal } = options ?? {};
		return executeSparqlQuery(this.options.endpointUrl, query, method, this.queryFunction, this.factory, signal);
	}
	/**
	* Executes arbitrary SPARQL CONSTRUCT query and returns the result RDF graph.
	*/
	executeSparqlConstruct(query, options) {
		const method = this.options.queryMethod ?? "GET";
		const { signal } = options ?? {};
		return executeSparqlConstruct(this.options.endpointUrl, query, method, this.queryFunction, signal);
	}
	createRefQueryPart(params) {
		const { elementId, linkId, direction } = params;
		const { unionParts, usePredicatePart } = this.formatLinkUnion(elementId, linkId, direction, "?inst", "?inst", true);
		if (usePredicatePart) {
			const refElementIRI = escapeIri(params.elementId);
			let refLinkType;
			if (linkId) {
				const link = this.linkById.get(linkId);
				refLinkType = link && isDirectLink(link) ? escapeIri(link.path) : escapeIri(linkId);
			}
			const linkPattern = refLinkType || "?link";
			const bindType = refLinkType ? `BIND(${refLinkType} as ?link)` : "";
			const blankFilter = this.acceptBlankNodes ? "FILTER(isIri(?inst) || isBlank(?inst))" : "FILTER(isIri(?inst))";
			if (!direction || direction === "out") unionParts.push(`{ ${refElementIRI} ${linkPattern} ?inst BIND("out" as ?direction) ${bindType} ${blankFilter} }`);
			if (!direction || direction === "in") unionParts.push(`{ ?inst ${linkPattern} ${refElementIRI} BIND("in" as ?direction) ${bindType} ${blankFilter} }`);
		}
		let resultPattern = unionParts.length === 0 ? "FILTER(false)" : unionParts.join("\nUNION\n");
		if (!linkId && this.settings.filterRefElementLinkPattern.length > 0) resultPattern += `\n${this.settings.filterRefElementLinkPattern}`;
		return resultPattern;
	}
	formatLinkUnion(refElementIri, linkIri, direction, outElementVar, inElementVar, bindDirection) {
		const { linkConfigurations } = this.settings;
		const fixedIri = escapeIri(refElementIri);
		const unionParts = [];
		let hasDirectLink = false;
		for (const link of linkConfigurations) {
			if (linkIri && link.id !== linkIri) continue;
			if (isDirectLink(link)) hasDirectLink = true;
			else {
				const linkType = escapeIri(link.id);
				if (!direction || direction === "out") {
					const path = this.formatLinkPath(link.path, fixedIri, outElementVar);
					const boundedDirection = bindDirection ? "BIND(\"out\" as ?direction) " : "";
					unionParts.push(`{ ${path} BIND(${linkType} as ?link) ${boundedDirection}}`);
				}
				if (!direction || direction === "in") {
					const path = this.formatLinkPath(link.path, inElementVar, fixedIri);
					const boundedDirection = bindDirection ? "BIND(\"in\" as ?direction) " : "";
					unionParts.push(`{ ${path} BIND(${linkType} as ?link) ${boundedDirection}}`);
				}
			}
		}
		return {
			unionParts,
			usePredicatePart: this.openWorldLinks || hasDirectLink
		};
	}
	formatLinkLinks() {
		const unionParts = [];
		let hasDirectLink = false;
		for (const link of this.settings.linkConfigurations) if (isDirectLink(link)) hasDirectLink = true;
		else {
			const linkType = escapeIri(link.id);
			unionParts.push(`${this.formatLinkPath(link.path, "?source", "?target")} BIND(${linkType} as ?type)`);
		}
		if (this.openWorldLinks || hasDirectLink) unionParts.push("?source ?type ?target");
		return unionParts.length === 0 ? "" : unionParts.length === 1 ? unionParts[0] : "{ " + unionParts.join(" }\nUNION\n{ ") + " }";
	}
	formatLinkPath(path, source, target) {
		return path.replace(/[?$]source\b/g, source).replace(/[?$]target\b/g, target);
	}
	formatPropertyInfo() {
		const unionParts = [];
		let hasDirectProperty = false;
		for (const property of this.settings.propertyConfigurations) if (isDirectProperty(property)) hasDirectProperty = true;
		else {
			const propType = escapeIri(property.id);
			const formatted = this.formatPropertyPath(property.path, "?inst", "?propValue");
			unionParts.push(`{ ${formatted} BIND(${propType} as ?propType) }`);
		}
		if (this.openWorldProperties || hasDirectProperty) unionParts.push("{ ?inst ?propType ?propValue }");
		return unionParts.join("\nUNION\n");
	}
	formatPropertyPath(path, subject, value) {
		return path.replace(/[?$]inst\b/g, subject).replace(/[?$]value\b/g, value);
	}
	async querySingleElementTypes(element, signal) {
		if (!element) return;
		return (await this.queryManyElementTypes([element], signal)).get(element);
	}
	async queryManyElementTypes(elements, signal) {
		if (elements.length === 0) return /* @__PURE__ */ new Map();
		const { filterTypePattern } = this.settings;
		const elementTypes = /* @__PURE__ */ new Map();
		await this.queryChunked(elements, async (batch) => {
			const query = resolveTemplate("SELECT ?inst ?class { VALUES(?inst) { ${ids} } ${filterTypePattern} }", {
				ids: batch.map((iri) => `(${escapeIri(iri)})`).join(" "),
				filterTypePattern
			});
			collectElementTypes(await this.executeSparqlSelect(query, { signal }), elementTypes);
		});
		return elementTypes;
	}
};
function makeChunkMeasure(unit) {
	switch (unit) {
		case "totalLength": return (item) => escapeIri(item).length + 3;
		default: return () => 1;
	}
}
async function attachLabels(items, fetchLabels, signal) {
	const labels = await fetchLabels(new Set(items.map((item) => item.id)), signal);
	for (const item of items) {
		const itemLabels = labels.get(item.id);
		if (itemLabels) item.label = itemLabels;
	}
}
async function attachProperties(items, fetchProperties, propertyIri, signal) {
	const properties = await fetchProperties(new Set(items.map((item) => item.id)), signal);
	for (const item of items) {
		const itemValues = properties.get(item.id);
		if (itemValues) item.properties[propertyIri] = itemValues;
	}
}
function prepareElementImages(elements, fetchImages, imagePropertyIri, factory, signal) {
	return fetchImages(elements.values(), signal).then((images) => {
		for (const [iri, image] of images) {
			const entity = elements.get(iri);
			if (entity) appendProperty(entity.properties, imagePropertyIri, factory.namedNode(image));
		}
	});
}
function resolveTemplate(template, values) {
	let result = template;
	for (const replaceKey in values) {
		if (!Object.prototype.hasOwnProperty.call(values, replaceKey)) continue;
		const replaceValue = values[replaceKey] || "";
		result = result.replace(new RegExp("\\${" + replaceKey + "}", "g"), replaceValue);
	}
	return result;
}
async function executeSparqlQuery(endpoint, query, method, queryFunction, factory, signal) {
	let internalQuery;
	if (method === "GET") internalQuery = queryFunction({
		url: appendQueryParams(endpoint, { query }),
		headers: { "Accept": "application/sparql-results+json" },
		method: "GET",
		signal
	});
	else internalQuery = queryFunction({
		url: endpoint,
		body: query,
		headers: {
			"Accept": "application/sparql-results+json",
			"Content-Type": "application/sparql-query; charset=UTF-8"
		},
		method: "POST",
		signal
	});
	const response = await internalQuery;
	if (response.ok) return mapSparqlResponseIntoRdfJs(await response.json(), factory);
	else {
		const error = new Error(response.statusText);
		error.response = response;
		throw error;
	}
}
async function executeSparqlConstruct(endpoint, query, method, queryFunction, signal) {
	let internalQuery;
	if (method === "GET") internalQuery = queryFunction({
		url: appendQueryParams(endpoint, { query }),
		headers: { "Accept": "text/turtle" },
		method: "GET",
		signal
	});
	else internalQuery = queryFunction({
		url: endpoint,
		body: query,
		headers: {
			"Accept": "text/turtle",
			"Content-Type": "application/sparql-query; charset=UTF-8"
		},
		method: "POST",
		signal
	});
	const response = await internalQuery;
	if (response.ok) {
		const turtleText = await response.text();
		return new N3.Parser().parse(turtleText);
	} else {
		const error = new Error(response.statusText);
		error.response = response;
		throw error;
	}
}
function appendQueryParams(endpoint, queryParams = {}) {
	return endpoint + ((endpoint.indexOf("?") < 0 ? "?" : "&") + Object.keys(queryParams).map((key) => `${key}=${encodeURIComponent(queryParams[key])}`).join("&"));
}
function queryInternal(params) {
	return fetch(params.url, {
		method: params.method,
		body: params.body,
		credentials: "same-origin",
		mode: "cors",
		cache: "default",
		headers: params.headers,
		signal: params.signal
	});
}
function formatLanguageFilter(variable, languages) {
	if (!languages) return "";
	const parts = [`FILTER(!isLiteral(${variable}) || lang(${variable}) = ""`];
	for (const language of languages) parts.push(` || lang(${variable}) = "${language}"`);
	parts.push(")");
	return parts.join("");
}
function sparqlExtractLabel(subject, label) {
	return `
        BIND ( str( ${subject} ) as ?uriStr)
        BIND ( strafter(?uriStr, "#") as ?label3)
        BIND ( strafter(strafter(?uriStr, "//"), "/") as ?label6)
        BIND ( strafter(?label6, "/") as ?label5)
        BIND ( strafter(?label5, "/") as ?label4)
        BIND (if (?label3 != "", ?label3,
            if (?label4 != "", ?label4,
            if (?label5 != "", ?label5, ?label6))) as ${label})
    `;
}
function formatValueClauseContent(iris) {
	return Array.from(iris, (iri) => `(${escapeIri(iri)})`).join(" ");
}
function escapeIri(iri) {
	if (typeof iri !== "string") throw new Error(`Cannot escape IRI of type "${typeof iri}"`);
	return `<${iri}>`;
}
//#endregion
//#region src/diagram/canvasApi.ts
/** @hidden */
var CanvasContext = React.createContext(null);
/**
* React hook to get current canvas context.
*
* Throws an error if called from component which is outside the canvas.
*
* @category Hooks
*/
function useCanvas() {
	const context = React.useContext(CanvasContext);
	if (!context) throw new Error("Reactodia: missing canvas context");
	return context;
}
//#endregion
//#region src/coreUtils/hotkey.ts
var IsMac = /Mac|iPhone|iPad/.test(window?.navigator?.platform || "");
/**
* Parses a keyboard hotkey sequence to an AST.
*/
function parseHotkey(hotkey) {
	const parts = hotkey.split("+");
	let modifiers = 0;
	for (let i = 0; i < parts.length - 1; i++) {
		const part = parts[i];
		switch (part) {
			case "Ctrl":
				modifiers |= 1;
				break;
			case "Meta":
				modifiers |= 2;
				break;
			case "Alt":
				modifiers |= 4;
				break;
			case "Shift":
				modifiers |= 8;
				break;
			case "Mod":
				modifiers |= IsMac ? 2 : 1;
				break;
			case "None": break;
			default: throw new Error(`Unknown hotkey modifier "${part}"`);
		}
	}
	const key = parts[parts.length - 1];
	if (!key) throw new Error("Missing main key for a hotkey");
	return {
		modifiers,
		key: key.length === 1 ? key.toLowerCase() : key
	};
}
function formatHotkey(ast) {
	const { modifiers, key } = ast;
	let result = "";
	if (modifiers & 1) result += "Ctrl+";
	if (modifiers & 2) result += IsMac ? "⌘+" : "Meta+";
	if (modifiers & 4) result += "Alt+";
	if (modifiers & 8) result += "Shift+";
	result += key.length === 1 ? key.toUpperCase() : key;
	return result;
}
function sameHotkeyAst(a, b) {
	return a.modifiers === b.modifiers && a.key === b.key;
}
function hashHotkeyAst(ast) {
	return dropHighestNonSignBit(chainHash(hashNumber(ast.modifiers), hashString(ast.key)));
}
function eventToHotkeyAst(e) {
	const key = e.key.length === 1 ? e.key.toLowerCase() : e.key;
	const keyIsLetter = /^[a-z]$/.test(key);
	const codeMatch = /^Key([A-Z])$/.exec(e.code);
	const codeKey = codeMatch ? codeMatch[1].toLowerCase() : void 0;
	return {
		modifiers: (e.ctrlKey ? 1 : 0) | (e.metaKey ? 2 : 0) | (e.altKey ? 4 : 0) | (e.shiftKey ? 8 : 0),
		key: codeKey && !keyIsLetter ? codeKey : key
	};
}
//#endregion
//#region src/diagram/canvasHotkey.tsx
/**
* Registers an active hotkey while the caller component is mounted on the canvas.
*
* If either `hotkey` or `action` is `undefined` or `null`, the hotkey will be inactive.
*
* @category Hooks
*/
function useCanvasHotkey(hotkey, action) {
	const { canvas } = useCanvas();
	const actionKey = React.useMemo(() => {
		if (hotkey) {
			const ast = parseHotkey(hotkey);
			return {
				_ast: ast,
				text: formatHotkey(ast)
			};
		}
	}, [hotkey]);
	const lastAction = React.useRef();
	lastAction.current = action;
	React.useEffect(() => {
		if (actionKey) return canvas.renderingState.listenHotkey(actionKey._ast, () => {
			lastAction.current?.();
		});
	}, [actionKey]);
	return actionKey;
}
//#endregion
//#region src/paper/baseGeometry.ts
var Vector;
(function(_Vector) {
	function add(a, b) {
		return {
			x: a.x + b.x,
			y: a.y + b.y
		};
	}
	_Vector.add = add;
	function subtract(a, b) {
		return {
			x: a.x - b.x,
			y: a.y - b.y
		};
	}
	_Vector.subtract = subtract;
	function scale(v, factor) {
		return {
			x: v.x * factor,
			y: v.y * factor
		};
	}
	_Vector.scale = scale;
	function equals(a, b) {
		return a.x === b.x && a.y === b.y;
	}
	_Vector.equals = equals;
	function length({ x, y }) {
		return Math.sqrt(x * x + y * y);
	}
	_Vector.length = length;
	function normalize({ x, y }) {
		if (x === 0 && y === 0) return {
			x,
			y
		};
		const inverseLength = 1 / Math.sqrt(x * x + y * y);
		return {
			x: x * inverseLength,
			y: y * inverseLength
		};
	}
	_Vector.normalize = normalize;
	function dot({ x: x1, y: y1 }, { x: x2, y: y2 }) {
		return x1 * x2 + y1 * y2;
	}
	_Vector.dot = dot;
	function cross2D({ x: x1, y: y1 }, { x: x2, y: y2 }) {
		return x1 * y2 - y1 * x2;
	}
	_Vector.cross2D = cross2D;
})(Vector || (Vector = {}));
var Rect;
(function(_Rect) {
	function equals(a, b) {
		return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
	}
	_Rect.equals = equals;
	function center({ x, y, width, height }) {
		return {
			x: x + width / 2,
			y: y + height / 2
		};
	}
	_Rect.center = center;
	function intersects(a, b) {
		return a.x <= b.x + b.width && a.y <= b.y + b.height && b.x <= a.x + a.width && b.y <= a.y + a.height;
	}
	_Rect.intersects = intersects;
})(Rect || (Rect = {}));
function fitRectKeepingAspectRatio(source, targetWidth, targetHeight) {
	if (!(typeof targetWidth === "number" || typeof targetHeight === "number")) return {
		width: source.width,
		height: source.height
	};
	const sourceAspectRatio = source.width / source.height;
	targetWidth = typeof targetWidth === "number" ? targetWidth : targetHeight * sourceAspectRatio;
	targetHeight = typeof targetHeight === "number" ? targetHeight : targetWidth / sourceAspectRatio;
	if (targetHeight * sourceAspectRatio <= targetWidth) return {
		width: targetHeight * sourceAspectRatio,
		height: targetHeight
	};
	else return {
		width: targetWidth,
		height: targetWidth / sourceAspectRatio
	};
}
//#endregion
//#region src/diagram/geometry.ts
/**
* Computes bounding rectangle from an element's position and size.
*
* @category Geometry
*/
function boundsOf(element, sizeProvider) {
	const { x, y } = element.position;
	const size = sizeProvider.getElementSize(element);
	return {
		x,
		y,
		width: size ? size.width : 0,
		height: size ? size.height : 0
	};
}
function intersectRayFromShape(geometry, target) {
	switch (geometry.type) {
		case "ellipse": return intersectRayFromEllipse(geometry.bounds, target);
		default: return intersectRayFromRect(geometry.bounds, target);
	}
}
function intersectRayFromRect(rect, target) {
	if (rect.width === 0 || rect.height === 0 || target.x > rect.x && target.x < rect.x + rect.width && target.y > rect.y && target.y < rect.y + rect.height) return;
	const halfWidth = rect.width / 2;
	const halfHeight = rect.height / 2;
	const center = {
		x: rect.x + halfWidth,
		y: rect.y + halfHeight
	};
	const direction = Vector.normalize({
		x: target.x - center.x,
		y: target.y - center.y
	});
	const rightDirection = {
		x: Math.abs(direction.x),
		y: direction.y
	};
	if (Vector.cross2D({
		x: halfWidth,
		y: -halfHeight
	}, rightDirection) > 0 && Vector.cross2D({
		x: halfWidth,
		y: halfHeight
	}, rightDirection) < 0) return {
		x: center.x + halfWidth * Math.sign(direction.x),
		y: center.y + halfWidth * direction.y / Math.abs(direction.x)
	};
	else return {
		x: center.x + halfHeight * direction.x / Math.abs(direction.y),
		y: center.y + halfHeight * Math.sign(direction.y)
	};
}
function intersectRayFromEllipse(bounds, target) {
	const center = Rect.center(bounds);
	const pointer = Vector.subtract(target, center);
	const normal = Vector.normalize(pointer);
	const intersection = {
		x: normal.x * bounds.width * .5,
		y: normal.y * bounds.height * .5
	};
	return Vector.length(pointer) >= Vector.length(intersection) ? Vector.add(center, intersection) : void 0;
}
/**
* Returns `true` is two line geometries (vertex sequences) are the same,
* otherwise `false`.
*
* @category Geometry
*/
function isPolylineEqual(left, right) {
	if (left === right) return true;
	if (left.length !== right.length) return false;
	for (let i = 0; i < left.length; i++) {
		const a = left[i];
		const b = right[i];
		if (!(a.x === b.x && a.y === b.y)) return false;
	}
	return true;
}
/**
* Computes line geometry between two shapes clipped at each
* ones border with intermediate points in-between.
*
* It is assumed that the line starts at source shape center,
* ends at target shape center and goes through each vertex in the array.
*
* @category Geometry
*/
function computePolyline(source, target, vertices) {
	const sourceShape = "type" in source ? source : {
		type: "rect",
		bounds: source
	};
	const targetShape = "type" in target ? target : {
		type: "rect",
		bounds: target
	};
	let start;
	for (let i = 0; i < vertices.length; i++) {
		start = intersectRayFromShape(sourceShape, vertices[i]);
		if (start) break;
	}
	if (!start) start = intersectRayFromShape(sourceShape, Rect.center(targetShape.bounds)) ?? Rect.center(sourceShape.bounds);
	let end;
	for (let i = vertices.length - 1; i >= 0; i--) {
		end = intersectRayFromShape(targetShape, vertices[i]);
		if (end) break;
	}
	if (!end) end = intersectRayFromShape(targetShape, Rect.center(sourceShape.bounds)) ?? Rect.center(targetShape.bounds);
	return [
		start,
		...vertices,
		end
	];
}
/**
* Computes length of linear line geometry.
*
* @category Geometry
* @see {@link getPointAlongPolyline}
*/
function computePolylineLength(polyline) {
	let previous;
	return polyline.reduce((acc, point) => {
		const segmentLength = previous ? Vector.length({
			x: point.x - previous.x,
			y: point.y - previous.y
		}) : 0;
		previous = point;
		return acc + segmentLength;
	}, 0);
}
/**
* Computes position at the specified `offset` along a linear line geometry
* relative to the start of the line.
*
* If `offset` value is less than 0 or greater than line geometry length,
* the the first or last point of the line will be returned correspondingly.
*
* @category Geometry
* @see {@link computePolylineLength}
*/
function getPointAlongPolyline(polyline, offset) {
	if (polyline.length === 0) throw new Error("Cannot compute a point for an empty polyline");
	if (offset < 0) return polyline[0];
	let currentOffset = 0;
	for (let i = 1; i < polyline.length; i++) {
		const previous = polyline[i - 1];
		const point = polyline[i];
		const segment = {
			x: point.x - previous.x,
			y: point.y - previous.y
		};
		const segmentLength = Vector.length(segment);
		const newOffset = currentOffset + segmentLength;
		if (offset < newOffset) {
			const leftover = (offset - currentOffset) / segmentLength;
			return {
				x: previous.x + leftover * segment.x,
				y: previous.y + leftover * segment.y
			};
		} else currentOffset = newOffset;
	}
	return polyline[polyline.length - 1];
}
/**
* Searches for a closest segment of a linear line geometry.
*
* @returns index of start point for the closes line segment, or 0 if line is empty.
* @category Geometry
* @see {@link getPointAlongPolyline}
*/
function findNearestSegmentIndex(polyline, location) {
	let minDistance = Infinity;
	let foundIndex = 0;
	for (let i = 0; i < polyline.length - 1; i++) {
		const pivot = polyline[i];
		const next = polyline[i + 1];
		const target = {
			x: location.x - pivot.x,
			y: location.y - pivot.y
		};
		const segment = {
			x: next.x - pivot.x,
			y: next.y - pivot.y
		};
		const segmentLength = Vector.length(segment);
		const projectionToSegment = Vector.dot(target, segment) / segmentLength;
		if (projectionToSegment < 0 || projectionToSegment > segmentLength) continue;
		const distanceToSegment = Math.abs(Vector.cross2D(target, segment)) / segmentLength;
		if (distanceToSegment < minDistance) {
			minDistance = distanceToSegment;
			foundIndex = i;
		}
	}
	return foundIndex;
}
var Spline = class Spline {
	geometry;
	constructor(geometry) {
		this.geometry = geometry;
		if (geometry.points.length < 2) throw new Error("Spline must consists of at least two points");
	}
	static defaultType = "smooth";
	static create(geometry) {
		return new Spline(geometry);
	}
	toPath() {
		const { type, points, source, target } = this.geometry;
		if (type === "smooth" && points.length >= 3) {
			const smoothness = .25;
			const parts = [`M${points[0].x} ${points[0].y}`];
			let previousTangent = Vector.normalize(Vector.subtract(source, points[0]));
			for (let i = 1; i < points.length; i++) {
				const previous = points[i - 1];
				const p = points[i];
				const next = points[i + 1];
				const tangent = next ? Vector.normalize(Vector.subtract(previous, next)) : Vector.normalize(Vector.subtract(p, target));
				const length = Vector.length(Vector.subtract(previous, p));
				const c0 = Vector.subtract(previous, Vector.scale(previousTangent, length * smoothness));
				const c1 = Vector.add(p, Vector.scale(tangent, length * smoothness));
				previousTangent = tangent;
				parts.push(` C${c0.x} ${c0.y} ${c1.x} ${c1.y} ${p.x} ${p.y}`);
			}
			return parts.join("");
		}
		return pathFromPolyline(points);
	}
};
/**
* Converts linear line geometry into an SVG path.
*
* @category Geometry
*/
function pathFromPolyline(polyline) {
	return "M" + polyline.map(({ x, y }) => `${x} ${y}`).join(" L");
}
/**
* Returns the first element from specified `elements` which bounding box
* includes the specified `point`.
*
* If the specified `point` is at the edge of a bounding box, it is considered
* to be part of it.
*
* @param elements an array of diagram elements to search
* @param point point on a diagram in paper coordinates
* @param sizeProvider element size provider to compute bounding boxes
* @category Geometry
*/
function findElementAtPoint(elements, point, sizeProvider) {
	for (let i = elements.length - 1; i >= 0; i--) {
		const element = elements[i];
		const { x, y, width, height } = boundsOf(element, sizeProvider);
		if (width === 0 && height === 0) continue;
		if (point.x >= x && point.x <= x + width && point.y >= y && point.y <= y + height) return element;
	}
}
/**
* Computes complete bounding box for the specified `elements` and `links`.
*
* @category Geometry
*/
function getContentFittingBox(elements, links, sizeProvider) {
	let minX = Infinity, minY = Infinity;
	let maxX = -Infinity, maxY = -Infinity;
	for (const element of elements) {
		const { x, y } = element.position;
		const size = sizeProvider.getElementSize(element);
		minX = Math.min(minX, x);
		minY = Math.min(minY, y);
		maxX = Math.max(maxX, x + (size ? size.width : 0));
		maxY = Math.max(maxY, y + (size ? size.height : 0));
	}
	for (const link of links) {
		const vertices = link.vertices || [];
		for (const { x, y } of vertices) {
			minX = Math.min(minX, x);
			minY = Math.min(minY, y);
			maxX = Math.max(maxX, x);
			maxY = Math.max(maxY, y);
		}
	}
	return {
		x: Number.isFinite(minX) ? minX : 0,
		y: Number.isFinite(minY) ? minY : 0,
		width: Number.isFinite(minX) && Number.isFinite(maxX) ? maxX - minX : 0,
		height: Number.isFinite(minY) && Number.isFinite(maxY) ? maxY - minY : 0
	};
}
/**
* Computes average center position of element bounding boxes.
*
* When `elements` is empty, returns `{x: 0, y: 0}`.
*
* @category Geometry
*/
function calculateAveragePosition(elements, sizeProvider) {
	if (elements.length === 0) return {
		x: 0,
		y: 0
	};
	let xSum = 0;
	let ySum = 0;
	for (const element of elements) {
		const { x, y, width, height } = boundsOf(element, sizeProvider);
		xSum += x + width / 2;
		ySum += y + height / 2;
	}
	return {
		x: xSum / elements.length,
		y: ySum / elements.length
	};
}
//#endregion
//#region src/diagram/history.ts
var Command;
(function(_Command) {
	function create(title, action) {
		return new SimpleCommand(title, action);
	}
	_Command.create = create;
	function effect(title, body) {
		return new EffectCommand(title, body);
	}
	_Command.effect = effect;
	function compound(title, commands) {
		return new CompoundCommand(title, commands);
	}
	_Command.compound = compound;
})(Command || (Command = {}));
var SimpleCommand = class {
	title;
	invoke;
	constructor(title, invoke) {
		this.title = title;
		this.invoke = invoke;
	}
};
var EffectCommand = class {
	title;
	body;
	skip;
	constructor(title, body) {
		this.title = title;
		this.body = body;
		this.skip = new SkippedEffect(title, this);
	}
	invoke() {
		const { body } = this;
		body();
		return this.skip;
	}
};
var SkippedEffect = class {
	title;
	effect;
	constructor(title, effect) {
		this.title = title;
		this.effect = effect;
	}
	invoke() {
		return this.effect;
	}
};
var CompoundCommand = class CompoundCommand {
	title;
	commands;
	constructor(title, commands) {
		this.title = title;
		this.commands = commands;
	}
	invoke() {
		const inverses = [];
		for (const command of this.commands) inverses.push(command.invoke());
		inverses.reverse();
		return new CompoundCommand(this.title, inverses);
	}
};
/**
* Implements command history which stores all commands in memory.
*
* @category Core
*/
var InMemoryHistory = class {
	source = new EventSource();
	events = this.source;
	_undoStack = [];
	_redoStack = [];
	batches = [];
	get undoStack() {
		return this._undoStack;
	}
	get redoStack() {
		return this._redoStack;
	}
	hasChanges() {
		return this._undoStack.length > 0;
	}
	reset() {
		this._undoStack.length = 0;
		this._redoStack.length = 0;
		this.source.trigger("historyChanged", { hasChanges: this.hasChanges() });
	}
	undo() {
		const command = this._undoStack.pop();
		if (command) {
			const inverse = command.invoke();
			this._redoStack.push(inverse);
			this.source.trigger("historyChanged", { hasChanges: this.hasChanges() });
		}
	}
	redo() {
		const command = this._redoStack.pop();
		if (command) {
			const inverse = command.invoke();
			this._undoStack.push(inverse);
			this.source.trigger("historyChanged", { hasChanges: this.hasChanges() });
		}
	}
	execute(command) {
		const inverse = command.invoke();
		this.registerToUndo(inverse);
	}
	registerToUndo(command) {
		const batch = this.topBatch();
		if (batch) batch._inverses.push(command);
		else {
			this._undoStack.push(command);
			this._redoStack.length = 0;
			this.source.trigger("historyChanged", { hasChanges: this.hasChanges() });
		}
	}
	topBatch() {
		return this.batches.length === 0 ? void 0 : this.batches[this.batches.length - 1];
	}
	startBatch(title) {
		const batch = {
			_title: title,
			_inverses: [],
			history: this,
			store: () => {
				if (!this.batches.includes(batch)) {
					console.warn("Reactodia: failed to find batch to store (already stored or discarded?)", batch);
					return;
				}
				while (this.batches.length > 0) {
					const other = this.batches.pop();
					if (other !== batch) console.warn("Reactodia: storing other unclosed batch on top", other);
					if (other._inverses.length > 0) {
						const commands = [...other._inverses].reverse();
						this.registerToUndo(Command.compound(batch._title, commands));
					}
					if (other === batch) break;
				}
			},
			discard: (options) => {
				if (!this.batches.includes(batch)) {
					console.warn("Reactodia: failed to find batch to discard (already stored or discarded?)", batch);
					return;
				}
				while (this.batches.length > 0) {
					const other = this.batches.pop();
					if (other !== batch) console.warn("Reactodia: discarding other unclosed batch on top", other);
					if (options?.revert && other._inverses.length > 0) {
						const commands = [...other._inverses].reverse();
						Command.compound(batch._title, commands).invoke();
					}
					if (other === batch) break;
				}
			}
		};
		this.batches.push(batch);
		return batch;
	}
};
//#endregion
//#region src/diagram/commands.ts
/**
* Command to restore element positions and sizes, link geometry (vertices) on a canvas.
*
* **Example**:
* ```ts
* const capturedGeometry = RestoreGeometry.capture(model);
* // ... (move elements, change link vertices) ...
* restoreGeometry = capturedGeometry.filterOutUnchanged();
* if (restoreGeometry.hasChanges()) {
*     model.history.registerToUndo(restoreGeometry);
* }
* ```
*
* @category Commands
*/
var RestoreGeometry = class RestoreGeometry {
	elementState;
	linkState;
	static _title = TranslatedText.text("commands.restore_geometry.title");
	constructor(elementState, linkState) {
		this.elementState = elementState;
		this.linkState = linkState;
	}
	/**
	* Creates {@link RestoreGeometry} command with captured geometry for all diagram content.
	*/
	static capture(graph) {
		return RestoreGeometry.capturePartial(graph.elements, graph.links);
	}
	/**
	* Creates {@link RestoreGeometry} command with captured geometry for the specified
	* subset of a diagram content.
	*/
	static capturePartial(elements, links) {
		return new RestoreGeometry(elements.map((element) => ({
			element,
			position: element.position,
			size: getElementSize(element)
		})), links.map((link) => ({
			link,
			vertices: link.vertices
		})));
	}
	get title() {
		return RestoreGeometry._title;
	}
	/**
	* Returns `true` if command contains any captured geometry state to restore,
	* otherwise `false`.
	*/
	hasChanges() {
		return this.elementState.length > 0 || this.linkState.length > 0;
	}
	/**
	* Creates a derived {@link RestoreGeometry} command by removing any geometry state
	* which is equal to the current diagram content geometry state.
	*
	* This is useful to avoid adding a command without actual changes to the command history
	* and to reduce the amount of memory to store captured geometry withing the command.
	*/
	filterOutUnchanged() {
		return new RestoreGeometry(this.elementState.filter(({ element, position, size }) => !(Vector.equals(element.position, position) && getElementSize(element) === size)), this.linkState.filter(({ link, vertices }) => !isPolylineEqual(link.vertices, vertices)));
	}
	invoke() {
		const previous = RestoreGeometry.capturePartial(this.elementState.map((state) => state.element), this.linkState.map((state) => state.link));
		for (const { element, position, size } of [...this.elementState].reverse()) {
			element.setPosition(position);
			element.setElementState(element.elementState.set(TemplateProperties.ElementSize, size));
		}
		for (const { link, vertices } of this.linkState) link.setVertices(vertices);
		return previous;
	}
};
function getElementSize(element) {
	return element.elementState.get(TemplateProperties.ElementSize);
}
/**
* Command to restore single link geometry (vertices) on a canvas.
*
* **Example**:
* ```ts
* const restoreLink = restoreCapturedLinkGeometry(link);
* new LinkVertex(link, 0).remove();
* model.history.registerToUndo(restoreLink);
* ```
*
* @category Commands
*/
function restoreCapturedLinkGeometry(link) {
	const vertices = link.vertices;
	return Command.create(TranslatedText.text("commands.restore_link_vertices.title"), () => {
		const capturedInverse = restoreCapturedLinkGeometry(link);
		link.setVertices(vertices);
		return capturedInverse;
	});
}
/**
* Command to set {@link Element.elementState element template state}.
*
* @category Commands
*/
function setElementState(element, state) {
	return Command.create(TranslatedText.text("commands.set_element_state.title"), () => {
		const previous = element.elementState;
		element.setElementState(state);
		return setElementState(element, previous);
	});
}
/**
* Command to toggle element expanded or collapsed.
* 
* Expanded state is stored in the {@link Element.elementState element state}
* with {@link TemplateProperties.Expanded} property.
*
* @category Commands
*/
function setElementExpanded(element, expanded) {
	return Command.create(expanded ? TranslatedText.text("commands.expand_element.title") : TranslatedText.text("commands.collapse_element.title"), () => {
		element.setExpanded(expanded);
		return setElementExpanded(element, !expanded);
	});
}
/**
* Command to set link template state.
*
* @category Commands
*/
function setLinkState(link, state) {
	return Command.create(TranslatedText.text("commands.set_link_state.title"), () => {
		const previous = link.linkState;
		link.setLinkState(state);
		return setLinkState(link, previous);
	});
}
/**
* Command to change link type visibility.
*
* @category Commands
*/
function changeLinkTypeVisibility(model, linkTypeId, visibility) {
	return Command.create(TranslatedText.text("commands.change_link_type_visibility.title"), () => {
		const previous = model.getLinkVisibility(linkTypeId);
		model.setLinkVisibility(linkTypeId, visibility);
		return changeLinkTypeVisibility(model, linkTypeId, previous);
	});
}
/**
* Command to restore canvas viewport position and scale.
*
* **Example**:
* ```ts
* const restoreScale = restoreViewport(canvas);
* canvas.zoomToFit();
* model.registerToUndo(restoreScale);
* ```
*
* @category Commands
*/
function restoreViewport(canvas) {
	function capture() {
		const { metrics } = canvas;
		const { clientWidth, clientHeight } = canvas.metrics.pane;
		const center = metrics.clientToPaperCoords(clientWidth / 2, clientHeight / 2);
		const { scale } = metrics.transform;
		return {
			center,
			scale
		};
	}
	function apply({ center, scale }) {
		canvas.centerTo(center, { scale });
	}
	const initialViewport = capture();
	const command = Command.create(TranslatedText.text("commands.restore_viewport.title"), () => {
		const revertedViewport = capture();
		apply(initialViewport);
		return Command.create(TranslatedText.text("commands.restore_viewport.title"), () => {
			apply(revertedViewport);
			return command;
		});
	});
	return command;
}
/**
* Command to move specified `elements` at the distance around `target` element,
* trying to minimize overlapping elements.
*
* @category Commands
*/
function placeElementsAroundTarget(params) {
	const { elements, target, graph, sizeProvider, distance = 300 } = params;
	const commandBody = () => {
		const capturedGeometry = RestoreGeometry.capture(graph);
		const targetElementBounds = boundsOf(target, sizeProvider);
		const targetPosition = {
			x: targetElementBounds.x + targetElementBounds.width / 2,
			y: targetElementBounds.y + targetElementBounds.height / 2
		};
		let outgoingAngle = 0;
		const targetLinks = graph.getElementLinks(target);
		if (targetLinks.length > 0) {
			const averageSourcePosition = calculateAveragePosition(targetLinks.map((link) => {
				const linkSource = graph.sourceOf(link);
				return linkSource !== target ? linkSource : graph.targetOf(link);
			}), sizeProvider);
			const vectorDiff = {
				x: targetPosition.x - averageSourcePosition.x,
				y: targetPosition.y - averageSourcePosition.y
			};
			if (vectorDiff.x !== 0 || vectorDiff.y !== 0) outgoingAngle = Math.atan2(vectorDiff.y, vectorDiff.x);
		}
		const step = Math.min(Math.PI / elements.length, Math.PI / 6);
		const elementStack = [...elements];
		const placeElementFromStack = (curAngle, element) => {
			if (element) {
				const { width, height } = boundsOf(element, sizeProvider);
				element.setPosition({
					x: targetPosition.x + distance * Math.cos(curAngle) - width / 2,
					y: targetPosition.y + distance * Math.sin(curAngle) - height / 2
				});
			}
		};
		if (elementStack.length % 2 === 0) for (let angle = step / 2; elementStack.length > 0; angle += step) {
			placeElementFromStack(outgoingAngle - angle, elementStack.pop());
			placeElementFromStack(outgoingAngle + angle, elementStack.pop());
		}
		else {
			placeElementFromStack(outgoingAngle, elementStack.pop());
			for (let angle = step; elementStack.length > 0; angle += step) {
				placeElementFromStack(outgoingAngle - angle, elementStack.pop());
				placeElementFromStack(outgoingAngle + angle, elementStack.pop());
			}
		}
		return capturedGeometry.filterOutUnchanged();
	};
	return Command.create(TranslatedText.text("commands.place_elements_around.title"), commandBody);
}
//#endregion
//#region src/diagram/elements.ts
/**
* Diagram element (graph node).
*
* @category Core
*/
var Element = class Element {
	/**
	* Event source to trigger events from derived element types.
	*/
	source = new EventSource();
	/**
	* Events for the graph element.
	*/
	events = this.source;
	/**
	* Unique and immutable element ID on the diagram.
	*/
	id;
	_position;
	_elementState;
	constructor(props) {
		const { id = Element.generateId(), position = {
			x: 0,
			y: 0
		}, expanded = false, elementState = TemplateState.empty } = props;
		this.id = id;
		this._position = position;
		this._elementState = expanded ? elementState.set(TemplateProperties.Expanded, expanded) : elementState;
	}
	/**
	* Generates a new unique ID for an element.
	*/
	static generateId() {
		return `urn:reactodia:e:${generate128BitID()}`;
	}
	/**
	* Gets the element position on the canvas in paper coordinates. 
	*/
	get position() {
		return this._position;
	}
	/**
	* Sets a new value for {@link position} property.
	*
	* Triggers {@link ElementEvents.changePosition} event if new value does
	* not equal to the previous one.
	*
	* @see {@link RestoreGeometry}
	*/
	setPosition(value) {
		const previous = this._position;
		if (previous.x === value.x && previous.y === value.y) return;
		this._position = value;
		this.source.trigger("changePosition", {
			source: this,
			previous
		});
	}
	/**
	* Whether the element should be displayed as expanded
	* (as defined by the element template).
	*
	* Expanded state is stored in the {@link Element.elementState element state}
	* with {@link TemplateProperties.Expanded} property.
	*/
	get isExpanded() {
		return Boolean(this._elementState.get(TemplateProperties.Expanded));
	}
	/**
	* Sets a new value for {@link isExpanded} property.
	*
	* Expanded state is stored in the {@link Element.elementState element state}
	* with {@link TemplateProperties.Expanded} property.
	*
	* Triggers {@link ElementEvents.changeElementState} event if new value does
	* not equal to the previous one.
	*/
	setExpanded(value) {
		this.setElementState(this._elementState.set(TemplateProperties.Expanded, value ? true : void 0));
	}
	/**
	* Gets a serializable template-specific state for the element.
	*/
	get elementState() {
		return this._elementState;
	}
	/**
	* Sets a new value for {@link elementState} property.
	*
	* Triggers {@link ElementEvents.changeElementState} event if new value does
	* not equal to the previous one.
	*/
	setElementState(value) {
		const previous = this._elementState;
		if (previous === value) return;
		this._elementState = value;
		this.source.trigger("changeElementState", {
			source: this,
			previous
		});
	}
	/**
	* Focuses on the element template on a canvas (if possible).
	*/
	focus() {
		this.source.trigger("requestedFocus", { source: this });
	}
	/**
	* Forces a re-render of the element displayed by a template on a canvas.
	*
	* @param level specifies which cached state should be invalidated on re-render
	*/
	redraw(level) {
		this.source.trigger("requestedRedraw", {
			source: this,
			level
		});
	}
};
/**
* Diagram element represented by an invisible single point.
*
* @category Core
*/
var VoidElement = class extends Element {
	constructor(props) {
		super(props);
	}
};
/**
* Diagram link (graph edge)
*
* @category Core
*/
var Link = class Link {
	/**
	* Event source to trigger events from derived link types.
	*/
	source = new EventSource();
	/**
	* Events for the graph link.
	*/
	events = this.source;
	/**
	* Unique and immutable link ID on the diagram.
	*/
	id;
	_sourceId;
	_targetId;
	_vertices;
	_linkState;
	constructor(props) {
		const { id = Link.generateId(), sourceId, targetId, vertices = [], linkState = TemplateState.empty } = props;
		this.id = id;
		this._sourceId = sourceId;
		this._targetId = targetId;
		this._vertices = vertices;
		this._linkState = linkState;
	}
	/**
	* Generates a new unique ID for an link.
	*/
	static generateId() {
		return `urn:reactodia:l:${generate128BitID()}`;
	}
	/**
	* Gets an immutable link source {@link Element.id element ID}.
	*/
	get sourceId() {
		return this._sourceId;
	}
	/**
	* Gets an immutable link target {@link Element.id element ID}.
	*/
	get targetId() {
		return this._targetId;
	}
	/**
	* Gets the link type IRI.
	*/
	get typeId() {
		return this.getTypeId();
	}
	/**
	* Gets the link geometry (intermediate points in paper coordinates in order
	* from the link source to the target).
	*/
	get vertices() {
		return this._vertices;
	}
	/**
	* Sets a new value for {@link vertices} property.
	*
	* Triggers {@link LinkEvents.changeVertices} event if new geometry
	* does not equal to the previous one.
	*
	* @see {@link RestoreGeometry}
	* @see {@link restoreCapturedLinkGeometry}
	*/
	setVertices(value) {
		const previous = this._vertices;
		if (isPolylineEqual(this._vertices, value)) return;
		this._vertices = value;
		this.source.trigger("changeVertices", {
			source: this,
			previous
		});
	}
	/**
	* Gets a serializable template-specific state for the link.
	*/
	get linkState() {
		return this._linkState;
	}
	/**
	* Sets a new value for {@link linkState} property.
	*
	* Triggers {@link LinkEvents.changeLinkState} event if new value does
	* not equal to the previous one.
	*/
	setLinkState(value) {
		const previous = this._linkState;
		if (previous === value) return;
		this._linkState = value;
		this.source.trigger("changeLinkState", {
			source: this,
			previous
		});
	}
	/**
	* Forces a re-render of the link displayed by a template on a canvas.
	*/
	redraw() {
		this.source.trigger("requestedRedraw", { source: this });
	}
};
/**
* Represents a convenient way to refer to a particular vertex of a diagram link.
*
* @category Core
*/
var LinkVertex = class {
	link;
	vertexIndex;
	constructor(link, vertexIndex) {
		this.link = link;
		this.vertexIndex = vertexIndex;
	}
	createAt(location) {
		const vertices = [...this.link.vertices];
		vertices.splice(this.vertexIndex, 0, location);
		this.link.setVertices(vertices);
	}
	moveTo(location) {
		const vertices = [...this.link.vertices];
		vertices.splice(this.vertexIndex, 1, location);
		this.link.setVertices(vertices);
	}
	remove() {
		const vertices = [...this.link.vertices];
		const [location] = vertices.splice(this.vertexIndex, 1);
		this.link.setVertices(vertices);
	}
};
//#endregion
//#region src/paper/paperLayers.tsx
/**
* HTML layer to render its children on the diagram in paper coordinate system.
*
* **Unstable**: this component will likely change in the future.
*
* @category Components
* @hidden
*/
function HtmlPaperLayer(props) {
	const { paperTransform, layerRef, style, children, ...otherProps } = props;
	const { originX, originY, scale } = paperTransform;
	let transformStyle = {
		position: "absolute",
		left: 0,
		top: 0,
		transform: `scale(${scale},${scale})translate(${originX}px,${originY}px)`
	};
	if (style) transformStyle = {
		...transformStyle,
		...style
	};
	return /* @__PURE__ */ jsx("div", {
		ref: layerRef,
		style: transformStyle,
		...otherProps,
		children
	});
}
/**
* SVG layer to render its children on the diagram in paper coordinate system.
*
* **Unstable**: this component will likely change in the future.
*
* @category Components
* @hidden
*/
function SvgPaperLayer(props) {
	const { layerRef, paperTransform, style, children, ...otherProps } = props;
	const { width, height, originX, originY, scale } = paperTransform;
	const scaledWidth = width * scale;
	const scaledHeight = height * scale;
	let svgStyle = {
		position: "absolute",
		top: 0,
		left: 0
	};
	if (style) svgStyle = {
		...svgStyle,
		...style
	};
	return /* @__PURE__ */ jsx("svg", {
		ref: layerRef,
		width: scaledWidth,
		height: scaledHeight,
		style: svgStyle,
		role: "none",
		...otherProps,
		children: /* @__PURE__ */ jsx("g", {
			transform: `scale(${scale},${scale})translate(${originX},${originY})`,
			children
		})
	});
}
function emptyPane(pageSize) {
	return {
		width: pageSize.width,
		height: pageSize.height,
		originX: 0,
		originY: 0,
		scale: 1,
		paddingX: 0,
		paddingY: 0
	};
}
function adjustPane(contentBounds, paneClientSize, pageSize, scale) {
	const bbox = contentBounds;
	const bboxLeft = bbox.x;
	const bboxTop = bbox.y;
	const bboxRight = bbox.x + bbox.width;
	const bboxBottom = bbox.y + bbox.height;
	const { width: gridWidth, height: gridHeight } = pageSize;
	const bboxGrid = {
		left: Math.floor(bboxLeft / gridWidth),
		top: Math.floor(bboxTop / gridHeight),
		right: Math.ceil(bboxRight / gridWidth),
		bottom: Math.ceil(bboxBottom / gridHeight)
	};
	return {
		width: Math.max(bboxGrid.right - bboxGrid.left, 1) * gridWidth,
		height: Math.max(bboxGrid.bottom - bboxGrid.top, 1) * gridHeight,
		originX: -bboxGrid.left * gridWidth,
		originY: -bboxGrid.top * gridHeight,
		paddingX: Math.ceil(paneClientSize.width),
		paddingY: Math.ceil(paneClientSize.height),
		scale
	};
}
function equalTransforms(a, b) {
	return a.width === b.width && a.height === b.height && a.originX === b.originX && a.originY === b.originY && a.paddingX === b.paddingX && a.paddingY === b.paddingY && a.scale === b.scale;
}
/**
* @returns scrollable pane size in non-scaled pane coords.
*
* @category Geometry
*/
function totalPaneSize(pt) {
	return {
		x: pt.width * pt.scale + pt.paddingX * 2,
		y: pt.height * pt.scale + pt.paddingY * 2
	};
}
/**
* @returns scrollable pane top-left corner position in non-scaled pane coords.
*
* @category Geometry
*/
function paneTopLeft(pt) {
	return {
		x: -pt.paddingX,
		y: -pt.paddingY
	};
}
/**
* Translates paper to scrollable pane coordinates.
*
* @category Geometry
*/
function paneFromPaperCoords(paper, pt) {
	return {
		x: (paper.x + pt.originX) * pt.scale,
		y: (paper.y + pt.originY) * pt.scale
	};
}
/**
* Translates scrollable pane to paper coordinates.
*
* @category Geometry
*/
function paperFromPaneCoords(pane, pt) {
	return {
		x: pane.x / pt.scale - pt.originX,
		y: pane.y / pt.scale - pt.originY
	};
}
//#endregion
//#region src/diagram/linkRouter.ts
/**
* Default link router which moves links with same source and target apart.
*
* @category Core
*/
var DefaultLinkRouter = class {
	gap;
	constructor(options = {}) {
		const { gap = 20 } = options;
		this.gap = gap;
	}
	route(model, sizeProvider) {
		const routings = /* @__PURE__ */ new Map();
		for (const link of model.links) {
			if (routings.has(link.id)) continue;
			const { sourceId, targetId } = link;
			if (!sourceId || !targetId) continue;
			else if (sourceId === targetId) this.routeFeedbackSiblingLinks(sourceId, model, sizeProvider, routings);
			else this.routeNormalSiblingLinks(sourceId, targetId, model, sizeProvider, routings);
		}
		return routings;
	}
	routeFeedbackSiblingLinks(elementId, model, sizeProvider, routings) {
		const element = model.getElement(elementId);
		const shape = sizeProvider.getElementShape(element);
		const { x, y } = shape.bounds;
		const center = Rect.center(shape.bounds);
		let index = 0;
		for (const sibling of model.getElementLinks(element)) {
			const { sourceId, targetId } = sibling;
			if (routings.has(sibling.id) || model.getLinkVisibility(sibling.typeId) === "hidden" || sourceId !== targetId) continue;
			if (sibling.vertices.length === 0) {
				const offset = this.gap * (index + 1);
				const vertices = [
					{
						x: x - offset,
						y: y + offset
					},
					{
						x: x - offset,
						y: y - offset
					},
					{
						x: x + offset,
						y: y - offset
					}
				];
				routings.set(sibling.id, {
					linkId: sibling.id,
					vertices
				});
				index++;
			} else if (sibling.vertices.length === 1) {
				const [pivot] = sibling.vertices;
				const [intersection] = computePolyline(shape, {
					x: pivot.x,
					y: pivot.y,
					width: 0,
					height: 0
				}, []);
				const offset = Math.min(Math.max(this.gap, Vector.length(Vector.subtract(pivot, intersection)) * .75), shape.bounds.width, shape.bounds.height);
				const ray = Vector.normalize(Vector.subtract(pivot, center));
				const shifted = Vector.add(pivot, Vector.scale(ray, -offset));
				const rotated = {
					x: -ray.y,
					y: ray.x
				};
				const vertices = [
					Vector.add(shifted, Vector.scale(rotated, offset)),
					pivot,
					Vector.add(shifted, Vector.scale(rotated, -offset))
				];
				routings.set(sibling.id, {
					linkId: sibling.id,
					vertices
				});
			}
		}
	}
	routeNormalSiblingLinks(sourceId, targetId, model, sizeProvider, routings) {
		const source = model.getElement(sourceId);
		const target = model.getElement(targetId);
		const sourceCenter = Rect.center(boundsOf(source, sizeProvider));
		const targetCenter = Rect.center(boundsOf(target, sizeProvider));
		const midPoint = {
			x: (sourceCenter.x + targetCenter.x) / 2,
			y: (sourceCenter.y + targetCenter.y) / 2
		};
		const direction = Vector.normalize({
			x: targetCenter.x - sourceCenter.x,
			y: targetCenter.y - sourceCenter.y
		});
		const siblings = model.getElementLinks(source).filter((link) => (link.sourceId === targetId || link.targetId === targetId) && !routings.has(link.id) && !hasUserPlacedVertices(link) && model.getLinkVisibility(link.typeId) !== "hidden");
		if (siblings.length <= 1) return;
		const indexModifier = siblings.length % 2 ? 0 : 1;
		siblings.forEach((sibling, siblingIndex) => {
			const index = siblingIndex + indexModifier;
			const offset = this.gap * Math.ceil(index / 2) - (indexModifier ? this.gap / 2 : 0);
			const offsetDirection = index % 2 ? {
				x: -direction.y,
				y: direction.x
			} : {
				x: direction.y,
				y: -direction.x
			};
			const vertex = {
				x: midPoint.x + offsetDirection.x * offset,
				y: midPoint.y + offsetDirection.y * offset
			};
			routings.set(sibling.id, {
				linkId: sibling.id,
				vertices: [vertex],
				labelTextAnchor: this.getLabelAlignment(direction, siblingIndex, siblings.length)
			});
		});
	}
	getLabelAlignment(connectionDirection, siblingIndex, siblingCount) {
		const angle = Math.atan2(connectionDirection.y, connectionDirection.x);
		const absoluteAngle = Math.abs(angle);
		const isHorizontal = absoluteAngle < Math.PI * 1 / 8 || absoluteAngle > Math.PI * 7 / 8;
		const isTop = angle < 0;
		const isBottom = angle > 0;
		const firstOuter = siblingCount - 2;
		const secondOuter = siblingCount - 1;
		if (!isHorizontal) {
			if (isTop && siblingIndex === secondOuter || isBottom && siblingIndex === firstOuter) return "end";
			else if (isTop && siblingIndex === firstOuter || isBottom && siblingIndex === secondOuter) return "start";
		}
		return "middle";
	}
};
function hasUserPlacedVertices(link) {
	return link.vertices.length > 0;
}
//#endregion
//#region src/diagram/renderingState.ts
/**
* Defines a rendering order which consists of multiple layers.
*
* The layers are organized in such way that changes from an earlier layer
* only affect rendering on the later layers. This way the full rendering
* could be done by rendering on each layer in order.
*
* Each layer index should be considered unspecified, only relative
* layer order is guaranteed to not change.
*/
var RenderingLayer = /* @__PURE__ */ function(RenderingLayer) {
	/**
	* Layer to render element templates.
	*/
	RenderingLayer[RenderingLayer["Element"] = 1] = "Element";
	/**
	* Layer to measure rendered elements to get their sizes.
	*/
	RenderingLayer[RenderingLayer["ElementSize"] = 2] = "ElementSize";
	/**
	* Layer to route links (compute link geometry).
	*/
	RenderingLayer[RenderingLayer["LinkRoutes"] = 3] = "LinkRoutes";
	/**
	* Layer to adjust scrollable area for the underlying canvas.
	*/
	RenderingLayer[RenderingLayer["PaperArea"] = 4] = "PaperArea";
	/**
	* Layer to render link templates.
	*/
	RenderingLayer[RenderingLayer["Link"] = 5] = "Link";
	/**
	* Layer to measure rendered link labels to get their sizes.
	*/
	RenderingLayer[RenderingLayer["LinkLabel"] = 6] = "LinkLabel";
	/**
	* Layer to update additional content placed on the diagram cells.
	*/
	RenderingLayer[RenderingLayer["Overlay"] = 7] = "Overlay";
	return RenderingLayer;
}({});
var FIRST_LAYER = 1;
var LAST_LAYER = 7;
var DEFAULT_ELEMENT_TEMPLATE_RESOLVER = (element) => void 0;
var DEFAULT_LINK_TEMPLATE_RESOLVER = (linkTypeId) => void 0;
var MutableRenderingState = class MutableRenderingState {
	listener = new EventObserver();
	source = new EventSource();
	events = this.source;
	scheduledByLayer = /* @__PURE__ */ new Map();
	layerUpdater = new Debouncer();
	model;
	resolveElementTemplate;
	resolveLinkTemplate;
	mappedTemplates = /* @__PURE__ */ new WeakMap();
	linkRouter;
	decorationContainers = /* @__PURE__ */ new WeakMap();
	elementSizes = /* @__PURE__ */ new WeakMap();
	linkLabelContainer = document.createElement("div");
	linkLabelBounds = /* @__PURE__ */ new WeakMap();
	cachedLinkTemplates = /* @__PURE__ */ new WeakMap();
	linkMarkerIndex = /* @__PURE__ */ new WeakMap();
	static nextLinkMarkerIndex = 1;
	routings = /* @__PURE__ */ new Map();
	hotkeyHandlers = new HashMap(hashHotkeyAst, sameHotkeyAst);
	_interactionBlocks = 0;
	shared;
	/** @hidden */
	constructor(options) {
		this.model = options.model;
		this.shared = options.shared;
		this.resolveElementTemplate = options.elementTemplateResolver ?? DEFAULT_ELEMENT_TEMPLATE_RESOLVER;
		this.resolveLinkTemplate = options.linkTemplateResolver ?? DEFAULT_LINK_TEMPLATE_RESOLVER;
		this.linkRouter = options.linkRouter ?? new DefaultLinkRouter();
		this.listener.listen(this.model.events, "changeCells", () => this.scheduleUpdateRoutings());
		this.listener.listen(this.model.events, "linkEvent", ({ data }) => {
			if (data.changeVertices) this.scheduleUpdateRoutings();
		});
		this.listener.listen(this.model.events, "elementEvent", ({ data }) => {
			if (data.changePosition) this.scheduleUpdateRoutings();
		});
		this.listener.listen(this.model.events, "changeLinkVisibility", (e) => {
			this.scheduleUpdateRoutings();
		});
		this.listener.listen(this.model.events, "discardGraph", () => {
			this.cachedLinkTemplates = /* @__PURE__ */ new WeakMap();
			const routings = this.routings;
			this.routings = /* @__PURE__ */ new Map();
			this.cancelOnLayerUpdate(3, this.updateRoutings);
			this.source.trigger("changeLinkTemplates", { source: this });
			this.source.trigger("changeRoutings", {
				source: this,
				previous: routings
			});
		});
		this.listener.listen(this.events, "changeElementSize", () => {
			this.scheduleUpdateRoutings();
		});
		this.updateRoutings();
	}
	/** @hidden */
	dispose() {
		this.listener.stopListening();
		this.layerUpdater.dispose();
		this.cancelOnLayerUpdate(3, this.updateRoutings);
	}
	get interactionBlocked() {
		return this._interactionBlocks > 0;
	}
	changeInteractionBlocks(change) {
		const previous = this._interactionBlocks !== 0;
		this._interactionBlocks += change;
		if (this._interactionBlocks !== 0 !== previous) this.source.trigger("changeInteractionBlocked", {
			previous,
			source: this
		});
	}
	syncUpdate() {
		this.syncUpdateLayersUpTo(LAST_LAYER);
	}
	scheduleOnLayerUpdate(layer, callback) {
		multimapAdd(this.scheduledByLayer, layer, callback);
		this.layerUpdater.call(this.runLayerUpdate);
	}
	cancelOnLayerUpdate(layer, callback) {
		const callbackSet = this.scheduledByLayer.get(layer);
		if (callbackSet) callbackSet.delete(callback);
	}
	runLayerUpdate = () => this.syncUpdateLayersUpTo(LAST_LAYER);
	updateLayersUpTo(lastLayer) {
		return Promise.resolve().then(() => this.syncUpdateLayersUpTo(lastLayer));
	}
	syncUpdateLayersUpTo(lastLayer) {
		this.layerUpdater.dispose();
		let shouldReschedule = false;
		for (let layer = lastLayer + 1; layer <= LAST_LAYER; layer++) {
			const callbackSet = this.scheduledByLayer.get(layer);
			if (callbackSet && callbackSet.size > 0) shouldReschedule = true;
		}
		const toRun = /* @__PURE__ */ new Set();
		for (let layer = FIRST_LAYER; layer <= lastLayer; layer++) {
			const callbackSet = this.scheduledByLayer.get(layer);
			if (callbackSet && callbackSet.size > 0) {
				for (const callback of callbackSet) toRun.add(callback);
				callbackSet.clear();
				for (const callback of toRun) callback();
				toRun.clear();
			}
			this.source.trigger("syncUpdate", { layer });
		}
		if (shouldReschedule) this.layerUpdater.call(this.runLayerUpdate);
	}
	ensureDecorationContainer(target) {
		let container = this.decorationContainers.get(target);
		if (!container) {
			container = document.createElement("div");
			this.decorationContainers.set(target, container);
		}
		return container;
	}
	getElementSize(element) {
		return this.elementSizes.get(element);
	}
	setElementSize(element, size) {
		const previous = this.elementSizes.get(element);
		if (!(previous && previous.width === size.width && previous.height === size.height)) {
			const decorationContainer = this.ensureDecorationContainer(element);
			decorationContainer.style = `width: ${size.width}px; height: ${size.height}px`;
			this.elementSizes.set(element, size);
			this.source.trigger("changeElementSize", {
				source: element,
				previous
			});
		}
	}
	attachLinkLabelContainer(parent) {
		if (parent) {
			if (this.linkLabelContainer.parentElement) throw new Error("Cannot attach link label container to multiple parents");
			parent.appendChild(this.linkLabelContainer);
		} else if (this.linkLabelContainer.parentElement) this.linkLabelContainer.parentElement.removeChild(this.linkLabelContainer);
	}
	getLinkLabelContainer() {
		return this.linkLabelContainer;
	}
	getLinkLabelBounds(link) {
		return this.linkLabelBounds.get(link);
	}
	setLinkLabelBounds(link, bounds) {
		const previous = this.linkLabelBounds.get(link);
		if (!(!previous && !bounds || previous && bounds && Rect.equals(previous, bounds))) {
			if (bounds) this.linkLabelBounds.set(link, bounds);
			else this.linkLabelBounds.delete(link);
			this.source.trigger("changeLinkLabelBounds", {
				source: link,
				previous
			});
		}
	}
	getElementTemplate(element) {
		let resolved = this.resolveElementTemplate(element);
		if (typeof resolved === "function") {
			let mapped = this.mappedTemplates.get(resolved);
			if (!mapped) {
				const component = resolved;
				mapped = { renderElement: (props) => React.createElement(component, props) };
				this.mappedTemplates.set(resolved, mapped);
			}
			resolved = mapped;
		}
		return resolved ?? this.shared.defaultElementResolver(element);
	}
	getElementShape(element) {
		const template = this.getElementTemplate(element);
		const bounds = boundsOf(element, this);
		return {
			type: template.shape ?? "rect",
			bounds
		};
	}
	ensureLinkMarkerIndex(linkMarker) {
		let index = this.linkMarkerIndex.get(linkMarker);
		if (index === void 0) {
			index = MutableRenderingState.nextLinkMarkerIndex++;
			if (MutableRenderingState.nextLinkMarkerIndex >= Number.MAX_SAFE_INTEGER) MutableRenderingState.nextLinkMarkerIndex = 1;
			this.linkMarkerIndex.set(linkMarker, index);
		}
		return index;
	}
	getLinkTemplate(link) {
		const existingTemplate = this.cachedLinkTemplates.get(link);
		if (existingTemplate) return existingTemplate;
		const template = this.resolveLinkTemplate(link) ?? this.shared.defaultLinkResolver(link);
		this.cachedLinkTemplates.set(link, template);
		this.source.trigger("changeLinkTemplates", { source: this });
		return template;
	}
	getRoutings() {
		return this.routings;
	}
	getRouting(linkId) {
		return this.routings.get(linkId);
	}
	scheduleUpdateRoutings() {
		this.scheduleOnLayerUpdate(3, this.updateRoutings);
	}
	updateRoutings = () => {
		const previousRoutes = this.routings;
		const computedRoutes = this.linkRouter.route(this.model, this);
		previousRoutes.forEach((previous, linkId) => {
			const computed = computedRoutes.get(linkId);
			if (computed && sameRoutedLink(previous, computed)) computedRoutes.set(linkId, previous);
		});
		this.routings = computedRoutes;
		this.source.trigger("changeRoutings", {
			source: this,
			previous: previousRoutes
		});
	};
	listenHotkey(ast, handler) {
		multimapAdd(this.hotkeyHandlers, ast, handler);
		if (this.hotkeyHandlers.get(ast).size === 2) console.warn(`Reactodia: registered multiple handlers for the same hotkey "${formatHotkey(ast)}" but only the first one will run if triggered.`);
		return () => {
			multimapDelete(this.hotkeyHandlers, ast, handler);
		};
	}
	triggerHotkey(e) {
		if (e.repeat) return;
		const pressAst = eventToHotkeyAst(e);
		const handlers = this.hotkeyHandlers.get(pressAst);
		if (handlers) for (const handler of handlers) {
			e.preventDefault();
			handler();
			break;
		}
	}
};
function sameRoutedLink(a, b) {
	return a.linkId === b.linkId && a.labelTextAnchor === b.labelTextAnchor && isPolylineEqual(a.vertices, b.vertices);
}
/**
* Transforms event store in a way that the result store debounces the changes
* until the next time the specified canvas {@link RenderingLayer layer} updates.
*
* @category Hooks
*/
function useLayerDebouncedStore(subscribe, renderingState, layer = 7) {
	return React.useCallback((onChange) => {
		const onUpdate = () => onChange();
		const dispose = subscribe(() => {
			renderingState.scheduleOnLayerUpdate(layer, onUpdate);
		});
		return () => {
			renderingState.cancelOnLayerUpdate(layer, onUpdate);
			dispose();
		};
	}, [
		subscribe,
		renderingState,
		layer
	]);
}
//#endregion
//#region src/diagram/elementLayer.tsx
var ElementLayer = class extends React.Component {
	listener = new EventObserver();
	redrawBatch = {
		requests: /* @__PURE__ */ new Map(),
		forAll: 0
	};
	memoizedElements = /* @__PURE__ */ new WeakMap();
	sizeRequests = /* @__PURE__ */ new Map();
	constructor(props) {
		super(props);
		const { model, renderingState } = this.props;
		this.state = {
			version: 0,
			elementStates: applyRedrawRequests(model, renderingState.shared, this.redrawBatch, /* @__PURE__ */ new Map())
		};
	}
	render() {
		const { model, renderingState, paperTransform, layerRef } = this.props;
		const { version, elementStates } = this.state;
		const { memoizedElements } = this;
		const elementsToRender = [];
		for (const element of model.elements) {
			const state = elementStates.get(element);
			if (state) elementsToRender.push(state);
		}
		return /* @__PURE__ */ jsx(HtmlPaperLayer, {
			layerRef,
			className: "reactodia-element-layer",
			paperTransform,
			role: "figure",
			children: elementsToRender.map((state) => {
				let overlaidElement = memoizedElements.get(state);
				if (!overlaidElement) {
					overlaidElement = /* @__PURE__ */ jsx(OverlaidElement, {
						state,
						model,
						renderingState,
						onResize: this.requestSizeUpdate
					}, state.element.id);
					memoizedElements.set(state, overlaidElement);
				}
				return overlaidElement;
			})
		}, version);
	}
	componentDidMount() {
		const { model, renderingState } = this.props;
		this.listener.listen(model.events, "changeCells", (e) => {
			if (e.updateAll) this.requestRedrawAll(1);
			else if (e.changedElement) this.requestRedraw(e.changedElement, 1);
		});
		this.listener.listen(model.events, "changeCellOrder", () => {
			this.requestRedrawAll(0);
		});
		this.listener.listen(model.events, "changeSelection", ({ previous }) => {
			const previousCell = previous.length === 1 ? previous[0] : void 0;
			const nextSingle = model.selection.length === 1 ? model.selection[0] : void 0;
			const previousElement = previousCell instanceof Element ? previousCell : void 0;
			const nextElement = nextSingle instanceof Element ? nextSingle : void 0;
			if (nextElement !== previousElement) {
				if (previousElement) this.requestRedraw(previousElement, 6);
				if (nextElement) this.requestRedraw(nextElement, 6);
			}
		});
		this.listener.listen(model.events, "elementEvent", ({ data }) => {
			const invalidatesTemplate = data.changeElementState;
			if (invalidatesTemplate) this.requestRedraw(invalidatesTemplate.source, 6);
			const invalidatesRender = data.changePosition;
			if (invalidatesRender) this.requestRedraw(invalidatesRender.source, 2);
			if (data.requestedRedraw) {
				let flags = 2;
				switch (data.requestedRedraw.level) {
					case "template":
						flags = 6;
						break;
				}
				this.requestRedraw(data.requestedRedraw.source, flags);
			}
		});
		this.listener.listen(model.events, "changeLanguage", () => {
			this.requestRedrawAll(6);
		});
		this.listener.listen(model.events, "discardGraph", () => {
			this.requestRedrawAll(31);
		});
		this.listener.listen(renderingState.shared.events, "changeHighlight", () => {
			this.requestRedrawAll(10);
		});
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.props.renderingState.cancelOnLayerUpdate(RenderingLayer.Element, this.redrawElements);
		this.props.renderingState.cancelOnLayerUpdate(RenderingLayer.ElementSize, this.recomputeQueuedSizes);
	}
	requestRedraw = (element, request) => {
		if ((this.redrawBatch.forAll | request) === this.redrawBatch.forAll) return;
		const existing = this.redrawBatch.requests.get(element.id) || 0;
		this.redrawBatch.requests.set(element.id, existing | request);
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.Element, this.redrawElements);
	};
	requestRedrawAll(request) {
		this.redrawBatch.forAll |= request;
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.Element, this.redrawElements);
	}
	redrawElements = () => {
		const committedBatch = this.redrawBatch;
		this.redrawBatch = {
			forAll: 0,
			requests: /* @__PURE__ */ new Map()
		};
		flushSync(() => {
			this.setState((state, props) => ({
				version: committedBatch.forAll === 31 ? state.version + 1 : state.version,
				elementStates: applyRedrawRequests(props.model, props.renderingState.shared, committedBatch, state.elementStates)
			}));
		});
	};
	requestSizeUpdate = (element, node) => {
		this.sizeRequests.set(element.id, {
			element,
			node
		});
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.ElementSize, this.recomputeQueuedSizes);
	};
	recomputeQueuedSizes = () => {
		const { renderingState } = this.props;
		const batch = this.sizeRequests;
		this.sizeRequests = /* @__PURE__ */ new Map();
		for (const request of batch.values()) {
			const { clientWidth, clientHeight } = request.node;
			request.computedSize = {
				width: clientWidth,
				height: clientHeight
			};
		}
		for (const request of batch.values()) if (request.computedSize) renderingState.setElementSize(request.element, request.computedSize);
	};
};
function applyRedrawRequests(model, view, batch, previous) {
	if (batch.forAll === 0 && batch.requests.size === 0) return previous;
	const selectedCell = model.selection.length === 1 ? model.selection[0] : void 0;
	const selectedElement = selectedCell instanceof Element ? selectedCell : void 0;
	const computed = /* @__PURE__ */ new Map();
	for (const element of model.elements) {
		const elementId = element.id;
		let state = previous.get(element);
		if (state) {
			const request = (batch.requests.get(elementId) || 0) | batch.forAll;
			if (request & 2) state = {
				element,
				templateProps: (request & 6) === 6 ? computeTemplateProps(state.element, selectedElement) : state.templateProps,
				blurred: (request & 10) === 10 ? computeIsBlurred(state.element, view) : state.blurred
			};
			computed.set(element, state);
			batch.requests.delete(elementId);
		} else computed.set(element, {
			element,
			templateProps: computeTemplateProps(element, selectedElement),
			blurred: computeIsBlurred(element, view)
		});
	}
	batch.forAll = 0;
	return computed;
}
function computeTemplateProps(element, selectedElement) {
	return {
		elementId: element.id,
		element,
		isExpanded: element.isExpanded,
		elementState: element.elementState,
		onlySelected: element === selectedElement
	};
}
var OVERLAID_ELEMENT_CLASS = "reactodia-overlaid-element";
var OverlaidElement = class extends React.Component {
	elementRef = React.createRef();
	decorationsRef = React.createRef();
	listener = new EventObserver();
	render() {
		const { state: { element, blurred, templateProps } } = this.props;
		if (element instanceof VoidElement) return /* @__PURE__ */ jsx("div", {});
		const { x, y } = element.position;
		const style = {
			position: "absolute",
			transform: `translate(${x}px,${y}px)`
		};
		return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
			className: cx(OVERLAID_ELEMENT_CLASS, blurred ? `${OVERLAID_ELEMENT_CLASS}--blurred` : void 0, templateProps.onlySelected ? `${OVERLAID_ELEMENT_CLASS}--only-selected` : void 0),
			style,
			"data-element-id": element.id,
			tabIndex: templateProps.onlySelected ? 0 : -1,
			ref: this.elementRef,
			onLoad: this.onLoadOrErrorEvent,
			onError: this.onLoadOrErrorEvent,
			children: /* @__PURE__ */ jsx(TemplatedElement, { ...this.props })
		}), /* @__PURE__ */ jsx("div", {
			className: "reactodia-element-decorations",
			style,
			ref: this.decorationsRef
		})] });
	}
	onLoadOrErrorEvent = () => {
		const { state, onResize } = this.props;
		if (this.elementRef.current) onResize(state.element, this.elementRef.current);
	};
	componentDidMount() {
		const { state, onResize, renderingState } = this.props;
		if (this.decorationsRef.current) this.decorationsRef.current.appendChild(renderingState.ensureDecorationContainer(state.element));
		this.listener.listen(state.element.events, "requestedFocus", () => {
			this.elementRef.current?.focus();
		});
		if (this.elementRef.current) onResize(state.element, this.elementRef.current);
	}
	componentWillUnmount() {
		const { state, renderingState } = this.props;
		const container = renderingState.ensureDecorationContainer(state.element);
		container.parentElement?.removeChild(container);
		this.listener.stopListening();
	}
	shouldComponentUpdate(nextProps) {
		return this.props.state !== nextProps.state;
	}
	componentDidUpdate() {
		const { state, onResize } = this.props;
		if (this.elementRef.current) onResize(state.element, this.elementRef.current);
	}
};
var TemplatedElement = class extends React.Component {
	cachedTemplate;
	cachedTemplateProps;
	render() {
		const { state, renderingState } = this.props;
		const { element, templateProps } = state;
		const template = renderingState.getElementTemplate(element);
		this.cachedTemplate = template;
		this.cachedTemplateProps = templateProps;
		return template.renderElement(templateProps);
	}
	shouldComponentUpdate(nextProps) {
		const template = nextProps.renderingState.getElementTemplate(nextProps.state.element);
		return !(this.cachedTemplate === template && this.cachedTemplateProps === nextProps.state.templateProps);
	}
};
function computeIsBlurred(element, view) {
	return Boolean(view.highlighter && !view.highlighter(element));
}
/**
* Component to display a decoration over a canvas element.
*
* All entity decorations are rendered as children of a DOM element
* with `reactodia-element-decorators` CSS class which immediately follows
* target canvas element itself in the DOM.
*
* Parent DOM elements for the decoration has translation, `width` and `height`
* set to the same values as the target element to be able to layout decorations
* via CSS.
*
* @category Components
*/
function ElementDecoration(props) {
	const { target, children } = props;
	const { canvas } = useCanvas();
	const renderingState = canvas.renderingState;
	return createPortal(children, renderingState.ensureDecorationContainer(target));
}
//#endregion
//#region src/diagram/layout.ts
/**
* Computes a layout on the specified diagram elements using specified
* graph layout algorithm function ({@link LayoutFunction}).
*
* **Example**:
* ```ts
* const layout = await calculateLayout({
*     layoutFunction: defaultLayout,
*     model,
*     sizeProvider: canvas.renderingState,
* });
* 
* await canvas.animateGraph(() => {
*     applyLayout(layout, model);
* });
* ```
*
* @category Geometry
* @see {@link applyLayout}
*/
async function calculateLayout(params) {
	const { layoutFunction, model, sizeProvider, typeProvider, fixedElements, selectedElements } = params;
	if (selectedElements && selectedElements.size <= 1) return {
		positions: /* @__PURE__ */ new Map(),
		sizes: /* @__PURE__ */ new Map(),
		nestedLayouts: []
	};
	let elements = model.elements;
	if (selectedElements) elements = elements.filter((el) => selectedElements.has(el));
	const nodes = Object.create(null);
	const bounds = Object.create(null);
	for (const element of elements) {
		nodes[element.id] = {
			types: typeProvider?.getElementTypes?.(element) ?? [],
			fixed: fixedElements?.has(element)
		};
		bounds[element.id] = boundsOf(element, sizeProvider);
	}
	const links = [];
	for (const link of model.links) if (Object.hasOwn(nodes, link.sourceId) && Object.hasOwn(nodes, link.targetId) && model.getLinkVisibility(link.typeId) !== "hidden") links.push({
		type: typeProvider?.getLinkType?.(link) ?? link.typeId,
		source: link.sourceId,
		target: link.targetId
	});
	const state = await layoutFunction({
		nodes,
		links
	}, { bounds });
	const positions = /* @__PURE__ */ new Map();
	const sizes = /* @__PURE__ */ new Map();
	for (const [id, { x, y, width, height }] of Object.entries(state.bounds)) {
		positions.set(id, {
			x,
			y
		});
		sizes.set(id, {
			width,
			height
		});
	}
	return {
		positions,
		sizes,
		nestedLayouts: []
	};
}
/**
* Applies the computed graph layout to the diagram.
*
* @category Geometry
* @see {@link calculateLayout}
*/
function applyLayout(layout, model) {
	const { positions, sizes, nestedLayouts } = layout;
	const sizeProvider = new StaticSizeProvider(sizes);
	const elements = model.elements.filter(({ id }) => positions.has(id));
	for (const nestedLayout of nestedLayouts) applyLayout(nestedLayout, model);
	const averagePosition = calculateAveragePosition(elements, sizeProvider);
	for (const element of elements) {
		const position = positions.get(element.id);
		if (position) element.setPosition(position);
	}
	const newAveragePosition = calculateAveragePosition(elements, sizeProvider);
	const averageDiff = {
		x: averagePosition.x - newAveragePosition.x,
		y: averagePosition.y - newAveragePosition.y
	};
	for (const [elementId, position] of positions) model.getElement(elementId).setPosition({
		x: position.x + averageDiff.x,
		y: position.y + averageDiff.y
	});
}
var StaticSizeProvider = class {
	sizes;
	constructor(sizes) {
		this.sizes = sizes;
	}
	getElementSize(element) {
		return this.sizes.get(element.id);
	}
	getElementShape(element) {
		return {
			type: "rect",
			bounds: boundsOf(element, this)
		};
	}
};
/**
* Moves each point in `positions` by the same vector to ensure every point
* has positive `x` and `y` coordinates, then additionally moves each point by `offset`.
*
* @category Geometry
*/
function translateToPositiveQuadrant(positions, offset) {
	let minX = Infinity, minY = Infinity;
	positions.forEach((position) => {
		minX = Math.min(minX, position.x);
		minY = Math.min(minY, position.y);
	});
	const { x, y } = offset;
	positions.forEach((position, key) => {
		positions.set(key, {
			x: position.x - minX + x,
			y: position.y - minY + y
		});
	});
}
/**
* Make a function that maps successive integer indices into a positions
* on a uniformly sized grid with the specified cell size.
*
* @category Geometry
*/
function uniformGrid(params) {
	return (cellIndex) => {
		const row = Math.floor(cellIndex / params.rows);
		return {
			x: (cellIndex - row * params.rows) * params.cellSize.x,
			y: row * params.cellSize.y,
			width: params.cellSize.x,
			height: params.cellSize.y
		};
	};
}
//#endregion
//#region src/diagram/layoutDefault.ts
/**
* Creates a definition for a a Web Worker with the default layout algorithms.
*
* @category Utilities
*/
function defineLayoutWorker(workerFactory) {
	return refCountedWorker(workerFactory, []);
}
//#endregion
//#region src/diagram/linkLayer.tsx
var CLASS_NAME$44 = "reactodia-link-layer";
var LinkLayer = class extends React.Component {
	listener = new EventObserver();
	providedContext;
	updateState = 1;
	/** List of link IDs to update at the next flush event */
	scheduledToUpdate = /* @__PURE__ */ new Set();
	labelMeasureRequests = /* @__PURE__ */ new Set();
	memoizedLinks = /* @__PURE__ */ new WeakMap();
	constructor(props) {
		super(props);
		this.providedContext = {
			scheduleLabelMeasure: this.scheduleLabelMeasure,
			clearLabelMeasure: this.clearLabelMeasure
		};
		this.state = {
			version: 0,
			shouldUpdateLink: this.popShouldUpdatePredicate()
		};
	}
	componentDidMount() {
		const { model, renderingState } = this.props;
		const scheduleUpdateElementLinks = (element) => {
			for (const link of model.getElementLinks(element)) this.scheduleUpdateLink(link.id);
		};
		this.listener.listen(model.events, "changeLanguage", this.scheduleUpdateAll);
		this.listener.listen(model.events, "changeCells", (e) => {
			if (e.updateAll) this.scheduleUpdateAll();
			else {
				if (e.changedElement) scheduleUpdateElementLinks(e.changedElement);
				if (e.changedLinks) for (const link of e.changedLinks) this.scheduleUpdateLink(link.id);
			}
		});
		this.listener.listen(model.events, "changeSelection", ({ previous }) => {
			const previousCell = previous.length === 1 ? previous[0] : void 0;
			const nextSingle = model.selection.length === 1 ? model.selection[0] : void 0;
			const previousLink = previousCell instanceof Link ? previousCell : void 0;
			const nextLink = nextSingle instanceof Link ? nextSingle : void 0;
			if (nextLink !== previousLink) {
				if (previousLink) this.scheduleUpdateLink(previousLink.id);
				if (nextLink) this.scheduleUpdateLink(nextLink.id);
			}
		});
		this.listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changePosition) scheduleUpdateElementLinks(data.changePosition.source);
		});
		this.listener.listen(model.events, "linkEvent", ({ data }) => {
			const linkEvent = data.changeVertices || data.changeLinkState || data.requestedRedraw;
			if (linkEvent) this.scheduleUpdateLink(linkEvent.source.id);
		});
		this.listener.listen(model.events, "changeLinkVisibility", (e) => {
			for (const link of model.links.filter((link) => link.typeId === e.source)) this.scheduleUpdateLink(link.id);
		});
		this.listener.listen(model.events, "discardGraph", () => {
			this.setState((state) => ({ version: state.version + 1 }));
			this.scheduleUpdateAll();
		});
		this.listener.listen(renderingState.shared.events, "changeHighlight", this.scheduleUpdateAll);
		this.listener.listen(renderingState.events, "changeElementSize", (e) => {
			scheduleUpdateElementLinks(e.source);
		});
		const updateChangedRoutes = (changed, previous) => {
			changed.forEach((routing, linkId) => {
				if (previous.get(linkId) !== routing) this.scheduleUpdateLink(linkId);
			});
		};
		this.listener.listen(renderingState.events, "changeRoutings", ({ previous }) => {
			const newRoutes = renderingState.getRoutings();
			updateChangedRoutes(newRoutes, previous);
			updateChangedRoutes(previous, newRoutes);
		});
	}
	shouldComponentUpdate(nextProps, nextState) {
		return !(this.props.shouldRenderLink === nextProps.shouldRenderLink && this.state.version === nextState.version && this.state.shouldUpdateLink === nextState.shouldUpdateLink);
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.props.renderingState.cancelOnLayerUpdate(RenderingLayer.Link, this.performUpdate);
		this.props.renderingState.cancelOnLayerUpdate(RenderingLayer.LinkLabel, this.measureLabels);
	}
	scheduleUpdateAll = () => {
		if (this.updateState !== 2) {
			this.updateState = 2;
			this.scheduledToUpdate = /* @__PURE__ */ new Set();
		}
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.Link, this.performUpdate);
	};
	scheduleUpdateLink(linkId) {
		if (this.updateState === 1) this.scheduledToUpdate.add(linkId);
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.Link, this.performUpdate);
	}
	popShouldUpdatePredicate() {
		const { updateState, scheduledToUpdate } = this;
		this.scheduledToUpdate = /* @__PURE__ */ new Set();
		this.updateState = 1;
		return updateState === 2 ? () => true : (link) => scheduledToUpdate.has(link.id);
	}
	scheduleLabelMeasure = (label) => {
		this.labelMeasureRequests.add(label);
		this.props.renderingState.scheduleOnLayerUpdate(RenderingLayer.LinkLabel, this.measureLabels);
	};
	clearLabelMeasure = (label) => {
		const { renderingState } = this.props;
		this.labelMeasureRequests.delete(label);
		renderingState.setLinkLabelBounds(label.owner, void 0);
	};
	measureLabels = () => {
		const { renderingState } = this.props;
		const requests = Array.from(this.labelMeasureRequests);
		this.labelMeasureRequests.clear();
		const bounds = [];
		for (const label of requests) bounds.push(label.measureBounds());
		for (let i = 0; i < requests.length; i++) {
			const label = requests[i];
			const measuredBounds = bounds[i];
			if (measuredBounds) renderingState.setLinkLabelBounds(label.owner, measuredBounds);
		}
	};
	performUpdate = () => {
		flushSync(() => {
			this.setState({ shouldUpdateLink: this.popShouldUpdatePredicate() });
		});
	};
	render() {
		const { model, shouldRenderLink, renderingState } = this.props;
		const { version, shouldUpdateLink } = this.state;
		const { memoizedLinks } = this;
		for (const link of model.links) if (shouldUpdateLink(link)) memoizedLinks.delete(link);
		const renderedLinks = [];
		for (const link of model.links) if (!shouldRenderLink || shouldRenderLink(link)) {
			let view = memoizedLinks.get(link);
			if (!view) {
				view = /* @__PURE__ */ jsx(LinkView, {
					link,
					model,
					renderingState
				}, link.id);
				memoizedLinks.set(link, view);
			}
			renderedLinks.push(view);
		}
		return /* @__PURE__ */ jsx(LinkLayerContext.Provider, {
			value: this.providedContext,
			children: /* @__PURE__ */ jsx("g", {
				className: CLASS_NAME$44,
				children: renderedLinks
			}, version)
		});
	}
};
var LinkLayerContext = React.createContext(null);
var LINK_CLASS$1 = "reactodia-link";
function LinkView(props) {
	const { link, model, renderingState } = props;
	const template = renderingState.getLinkTemplate(link);
	const defaultTemplate = renderingState.shared.defaultLinkResolver(link);
	const source = model.getElement(link.sourceId);
	const target = model.getElement(link.targetId);
	const visibility = model.getLinkVisibility(link.typeId);
	if (!(source && target && visibility !== "hidden")) return null;
	const route = renderingState.getRouting(link.id);
	const verticesDefinedByUser = link.vertices;
	const vertices = route ? route.vertices : verticesDefinedByUser;
	const sourceShape = renderingState.getElementShape(source);
	const targetShape = renderingState.getElementShape(target);
	const polyline = computePolyline(sourceShape, targetShape, vertices);
	const spline = Spline.create({
		type: template.spline ?? Spline.defaultType,
		points: polyline,
		source: Rect.center(sourceShape.bounds),
		target: Rect.center(targetShape.bounds)
	});
	const polylineLength = computePolylineLength(polyline);
	const getPathPosition = (offset) => {
		return getPointAlongPolyline(polyline, polylineLength * offset);
	};
	const markerSource = template.markerSource ?? defaultTemplate.markerSource;
	const markerTarget = template.markerTarget ?? defaultTemplate.markerTarget;
	const markerSourceIndex = markerSource ? renderingState.ensureLinkMarkerIndex(markerSource) : void 0;
	const markerTargetIndex = markerTarget ? renderingState.ensureLinkMarkerIndex(markerTarget) : void 0;
	const { highlighter } = renderingState.shared;
	const isBlurred = highlighter && !highlighter(link);
	const onlySelected = model.selection.length === 1 && model.selection[0] === link;
	const renderedLink = template.renderLink({
		link,
		markerSource: markerSourceIndex ? `url(#${linkMarkerKey(markerSourceIndex, true)})` : "",
		markerTarget: markerTargetIndex ? `url(#${linkMarkerKey(markerTargetIndex, false)})` : "",
		path: spline.toPath(),
		getPathPosition,
		route,
		onlySelected
	});
	return /* @__PURE__ */ jsx("g", {
		"data-link-id": link.id,
		"data-source-id": link.sourceId,
		"data-target-id": link.targetId,
		className: cx(LINK_CLASS$1, isBlurred ? `${LINK_CLASS$1}--blurred` : void 0, onlySelected ? `${LINK_CLASS$1}--only-selected` : void 0),
		children: renderedLink
	});
}
var LINK_PATH_CLASS = "reactodia-link-path";
/**
* Component to render link geometry as an SVG path.
*
* @category Components
*/
function LinkPath(props) {
	const { path, pathProps, markerSource, markerTarget } = props;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("path", {
		...pathProps,
		className: cx(LINK_PATH_CLASS, pathProps?.className),
		d: path,
		markerStart: markerSource,
		markerEnd: markerTarget
	}), /* @__PURE__ */ jsx("path", {
		className: `${LINK_PATH_CLASS}__wrap`,
		d: path
	})] });
}
function LinkLabelLayer(props) {
	const { renderingState, paperTransform, layerRef } = props;
	const parentRef = React.useRef(null);
	const onMount = React.useCallback((element) => {
		parentRef.current = element;
		if (layerRef) layerRef.current = element;
	}, [layerRef]);
	React.useEffect(() => {
		const parent = parentRef.current;
		if (parent) {
			renderingState.attachLinkLabelContainer(parent);
			return () => renderingState.attachLinkLabelContainer(null);
		}
	}, []);
	return /* @__PURE__ */ jsx(HtmlPaperLayer, {
		paperTransform,
		className: "reactodia-label-layer",
		layerRef: onMount,
		role: "figure"
	});
}
var LINK_LABEL_CLASS = "reactodia-link-label";
var DEFAULT_TEXT_ANCHOR = "middle";
function LinkLabel(props) {
	const { link, position: { x, y }, line, className, style, textAnchor, title, children } = props;
	const { canvas } = useCanvas();
	const layerContext = React.useContext(LinkLayerContext);
	const elementRef = React.useRef(null);
	const measurableLabelRef = React.useRef(void 0);
	React.useLayoutEffect(() => {
		if (layerContext && props.primary) {
			if (!measurableLabelRef.current) {
				const measurable = {
					owner: link,
					measureBounds: () => {
						const bounds = elementRef.current?.getBoundingClientRect();
						if (!bounds) return;
						const inverseScale = 1 / canvas.getScale();
						return computeLabelBounds(measurable.latestProps, {
							width: bounds.width * inverseScale,
							height: bounds.height * inverseScale
						});
					},
					latestProps: props
				};
				measurableLabelRef.current = measurable;
			}
			measurableLabelRef.current.latestProps = props;
			layerContext.scheduleLabelMeasure(measurableLabelRef.current);
		}
	});
	React.useLayoutEffect(() => {
		return () => {
			const measurable = measurableLabelRef.current;
			if (layerContext && measurable) layerContext.clearLabelMeasure(measurable);
		};
	}, []);
	const anchorTransform = textAnchor === "start" ? "translate(0,-50%)" : textAnchor === "end" ? "translate(-100%,-50%)" : "translate(-50%,-50%)";
	const providedStyle = {
		...style,
		position: "absolute",
		transform: `translate(${x}px,${y}px)${anchorTransform}`,
		"--reactodia-link-label-line": line
	};
	const renderingState = canvas.renderingState;
	return createPortal(/* @__PURE__ */ jsx("div", {
		ref: elementRef,
		"data-link-id": link.id,
		className: cx(LINK_LABEL_CLASS, className),
		style: providedStyle,
		title,
		children
	}), renderingState.getLinkLabelContainer());
}
function computeLabelBounds(props, { width, height }) {
	const { position: { x, y }, textAnchor = DEFAULT_TEXT_ANCHOR } = props;
	let xOffset = 0;
	if (textAnchor === "middle") xOffset = -width / 2;
	else if (textAnchor === "end") xOffset = -width;
	return {
		x: x + xOffset,
		y: y - height / 2,
		width,
		height
	};
}
var LINK_VERTICES_CLASS = "reactodia-link-vertices";
/**
* Component to render interactive vertices of the link geometry.
*
* Each displayed vertex can be moved or deleted which
* adds a command to the command history.
*
* @category Components
*/
function LinkVertices(props) {
	const { linkId, vertices, className, vertexRadius = 10, fill } = props;
	const { model } = useCanvas();
	if (vertices.length === 0) return null;
	const vertexClass = cx(`${LINK_VERTICES_CLASS}__vertex`, className);
	const onRemoveLinkVertex = (vertexIndex) => {
		const link = model.getLink(linkId);
		if (!link) return;
		const vertex = new LinkVertex(link, vertexIndex);
		model.history.registerToUndo(restoreCapturedLinkGeometry(vertex.link));
		vertex.remove();
	};
	const elements = [];
	for (let i = 0; i < vertices.length; i++) {
		const { x, y } = vertices[i];
		const key = elements.length;
		elements.push(/* @__PURE__ */ jsx("circle", {
			"data-vertex": i,
			className: vertexClass,
			cx: x,
			cy: y,
			r: vertexRadius,
			fill
		}, key));
		elements.push(/* @__PURE__ */ jsx(VertexTools, {
			vertexIndex: i,
			vertexRadius,
			x,
			y,
			onRemove: onRemoveLinkVertex
		}, key + 1));
	}
	return /* @__PURE__ */ jsx("g", {
		className: LINK_VERTICES_CLASS,
		children: elements
	});
}
function VertexTools(props) {
	const { vertexIndex, vertexRadius, x, y, onRemove } = props;
	const t = useTranslation();
	const transform = `translate(${x + 2 * vertexRadius},${y - 2 * vertexRadius})scale(${vertexRadius})`;
	const onRemoveVertex = (e) => {
		if (e.button !== 0) return;
		e.preventDefault();
		e.stopPropagation();
		onRemove(vertexIndex);
	};
	return /* @__PURE__ */ jsxs("g", {
		className: `${LINK_VERTICES_CLASS}__handle`,
		"data-reactodia-no-export": "true",
		transform,
		onPointerDown: onRemoveVertex,
		children: [
			/* @__PURE__ */ jsx("title", { children: t.text("link_action.vertex.remove_title") }),
			/* @__PURE__ */ jsx("circle", { r: 1 }),
			/* @__PURE__ */ jsx("path", {
				d: "M-0.5,-0.5 L0.5,0.5 M0.5,-0.5 L-0.5,0.5",
				strokeWidth: 2 / vertexRadius
			})
		]
	});
}
function LinkMarkersInner(props) {
	const { model, renderingState } = props;
	useSyncStore(useLayerDebouncedStore(useEventStore(renderingState.events, "changeLinkTemplates"), renderingState, RenderingLayer.Link), () => model.cellsVersion);
	const sourceMarkers = /* @__PURE__ */ new Set();
	const targetMarkers = /* @__PURE__ */ new Set();
	for (const link of model.links) {
		const template = renderingState.getLinkTemplate(link);
		const defaultTemplate = renderingState.shared.defaultLinkResolver(link);
		const markerSource = template.markerSource ?? defaultTemplate.markerSource;
		if (markerSource) sourceMarkers.add(markerSource);
		const markerTarget = template.markerTarget ?? defaultTemplate.markerTarget;
		if (markerTarget) targetMarkers.add(markerTarget);
	}
	return /* @__PURE__ */ jsxs("defs", { children: [Array.from(sourceMarkers, (marker) => {
		const index = renderingState.ensureLinkMarkerIndex(marker);
		return /* @__PURE__ */ jsx(LinkMarker, {
			markerIndex: index,
			isStartMarker: true,
			style: marker
		}, index);
	}), Array.from(targetMarkers, (marker) => {
		const index = renderingState.ensureLinkMarkerIndex(marker);
		return /* @__PURE__ */ jsx(LinkMarker, {
			markerIndex: index,
			isStartMarker: false,
			style: marker
		}, index);
	})] });
}
var LinkMarkers = React.memo(LinkMarkersInner, () => true);
var SVG_NAMESPACE$1 = "http://www.w3.org/2000/svg";
var LinkMarker = class extends React.Component {
	render() {
		return /* @__PURE__ */ jsx("marker", { ref: this.onMarkerMount });
	}
	shouldComponentUpdate() {
		return false;
	}
	onMarkerMount = (marker) => {
		if (!marker) return;
		const { markerIndex, isStartMarker, style } = this.props;
		const { d, width, height } = style;
		if (!(d !== void 0 && width !== void 0 && height !== void 0)) return;
		const className = "reactodia-link-marker";
		marker.setAttribute("class", className);
		marker.setAttribute("id", linkMarkerKey(markerIndex, isStartMarker));
		marker.setAttribute("markerWidth", String(width));
		marker.setAttribute("markerHeight", String(height));
		marker.setAttribute("orient", "auto");
		const xOffset = isStartMarker ? 0 : width - 1;
		marker.setAttribute("refX", String(xOffset));
		marker.setAttribute("refY", String(height / 2));
		marker.setAttribute("markerUnits", "userSpaceOnUse");
		const path = document.createElementNS(SVG_NAMESPACE$1, "path");
		path.setAttribute("class", `${className}__path`);
		path.setAttribute("d", d);
		if (style.fill !== void 0) path.setAttribute("fill", style.fill);
		if (style.stroke !== void 0) path.setAttribute("stroke", style.stroke);
		if (style.strokeWidth !== void 0) path.setAttribute("stroke-width", String(style.strokeWidth));
		marker.appendChild(path);
	};
};
function linkMarkerKey(markerIndex, startMarker) {
	return `reactodia-marker-${startMarker ? "start" : "end"}-${markerIndex}`;
}
//#endregion
//#region src/diagram/locale.tsx
/**
* Default (fallback) translation bundle `en` language.
*
* @category Constants
*/
var DefaultTranslationBundle = {
	$schema: "../i18n.schema.json",
	authoring_state: {
		"entity_action_delete.label": "delete",
		"entity_action_delete.title": "Delete entity",
		"entity_action_delete.title_disabled": "Cannot delete the entity",
		"entity_action_discard.label": "cancel",
		"entity_action_discard.title": "Discard all changes to the entity",
		"entity_action_edit.label": "edit",
		"entity_action_edit.title": "Edit entity",
		"entity_action_edit.title_disabled": "Cannot edit the entity",
		"entity_add.label": "New",
		"entity_change.label": "Change",
		"entity_delete.label": "Delete",
		"relation_action_discard.label": "cancel",
		"relation_action_discard.title": "Discard all changes to the relation",
		"relation_add.label": "New",
		"relation_change.label": "Change",
		"relation_delete.label": "Delete"
	},
	classic_workspace: {
		"canvas.label": "Canvas",
		"class_tree.heading": "Classes",
		"connections.heading": "Connections",
		"instances.heading": "Instances"
	},
	commands: {
		"change_entity.title": "Change entity data",
		"change_link_type_visibility.title": "Change link type visibility",
		"change_relation.title": "Change relation data",
		"collapse_element.title": "Collapse element",
		"expand_element.title": "Expand element",
		"place_elements_around.title": "Place elements",
		"restore_geometry.title": "Move elements and links",
		"restore_link_vertices.title": "Change link vertices",
		"restore_viewport.title": "Set viewport",
		"set_element_state.title": "Change element state",
		"set_entity_data.title": "Set entity element data",
		"set_entity_group_items.title": "Set entity group items",
		"set_link_state.title": "Change link state",
		"set_relation_data.title": "Set relation link data",
		"set_relation_group_items.title": "Set relation group items",
		"transform_elements.title": "Resize elements"
	},
	connections_menu: {
		"all_link.label": "All",
		"breadcrumbs_root.label": "Link types",
		"dialog.caption": "Connections",
		"entities.add_all": "Add all",
		"entities.add_group": "Add as group",
		"entities.add_selected": "Add selected",
		"entities.counter_label": "{{count}}\xA0of\xA0{{total}}",
		"entities.extra_label": "\xA0({{value}})",
		"entities.extra_title": "Extra entities",
		"entities.missing_label": "\xA0({{value}})",
		"entities.missing_title": "Missing entities",
		"entities.no_results": "No results",
		"entities.truncated_results": "Only the first {{limit}} entities are shown",
		"entities.truncated_results_expand": "Only the first {{limit}} entities are shown,\nclick here for the full results",
		"input.placeholder": null,
		"link.both_navigate_title": "Navigate to all connected elements",
		"link.both_title": "all connected",
		"link.source_navigate_title": "Navigate to source \"{{relation}}\" elements",
		"link.source_title": "source of \"{{relation}}\" {{relationIri}}",
		"link.target_navigate_title": "Navigate to source \"{{target}}\" elements",
		"link.target_title": "target of \"{{relation}}\" {{relationIri}}",
		"link.move_to_filter.title": "Set as filter in the Instances panel",
		"links.no_results": "No results",
		"links.suggest_similar": "Probably, you are looking for...",
		"place_elements.command": "Add connected elements",
		"select_all.label": "Select all",
		"select_all.title": "Select or unselect all entities",
		"sort_alphabetically.title": "Sort alphabetically",
		"sort_smart.title": "Smart sort"
	},
	data_diagram_model: {
		"add_element.command": "Add element",
		"add_link.command": "Add link",
		"create_links.command": "Create links",
		"group_entities.command": "Group entities",
		"import_layout.command": "Import layout",
		"regroup_relations.command": "Regroup relations",
		"request_entities.command": "Request entity data",
		"request_relations.command": "Request relations between entities",
		"remove_element.command": "Remove element",
		"remove_link.command": "Remove link",
		"ungroup_entities.command": "Ungroup entities"
	},
	default_data_locale: {
		"iri": "<{{value}}>",
		"iri_blank": "(blank node)"
	},
	default_workspace: {
		"search_section_entities.label": "Entities",
		"search_section_entities.title": "Entity lookup",
		"search_section_entity_types.label": "Types",
		"search_section_entity_types.title": "Entity type hierarchy",
		"search_section_link_types.label": "Links",
		"search_section_link_types.title": "Relation types on the diagram"
	},
	drop_on_canvas: { "drop.command": "Drag and drop onto diagram" },
	editor_controller: {
		"apply_authored_changes.command": "Apply authored changes to the diagram",
		"discard_change.command": "Discard change",
		"entity_add.command": "Create new entity",
		"entity_change.command": "Edit entity",
		"entity_delete.command": "Delete entity",
		"relation_add.command": "Create new relation",
		"relation_change.command": "Edit relation",
		"relation_delete.command": "Delete relation",
		"relation_move_source.command": "Move relation source",
		"relation_move_target.command": "Move relation target",
		"remove_items.command": "Remove diagram items",
		"set_authoring_state.command": "Create, change or delete entities and relations",
		"validate_entity_failed.message": "Failed to validate entity"
	},
	forms: {
		"input_file.drag_hint": "Drag files over or",
		"input_file.remove_value.title": "Remove the file",
		"input_file.select_files.label": "Select files",
		"input_list.add_value.title": "Add new property value",
		"input_list.remove_value.title": "Remove the property value"
	},
	group_paginator: {
		"current_page.label": "{{page}} of {{pageCount}}",
		"go_next.title": "Go to the next page",
		"go_previous.title": "Go to the previous page",
		"page_size.title": "Number of items to display per page"
	},
	inline_entity: {
		"title": "{{entity}} {{entityIri}}",
		"title_extra": "Types: {{entityTypes}}"
	},
	link_action: {
		"delete_annotation.title": "Remove annotation link",
		"delete_relation.title": "Delete relation",
		"delete_relation.title_disabled": "Cannot delete the relation",
		"edit_relation.title": "Edit relation",
		"edit_relation.title_disabled": "Cannot edit the relation",
		"rename_link.title": "Rename link",
		"move_relation.move_source_title": "Move link source",
		"move_relation.move_target_title": "Move link target",
		"vertex.remove_title": "Remove vertex"
	},
	navigator: {
		"toggle_collapse.title": "Collapse navigator",
		"toggle_expand.title": "Expand navigator"
	},
	note_annotation: {
		"change_text.command": "Change annotation text",
		"style_align_center.title": "Align center",
		"style_align_left.title": "Align left",
		"style_align_right.title": "Align right",
		"style_bold.title": "Bold",
		"style_color_variant.title": "Color variant: {{variant}}",
		"style_italic.title": "Italic",
		"style_strikethrough.title": "Strikethrough",
		"style_underline.title": "Underline"
	},
	overlay_controller: {
		"dialog_close.title": "Close",
		"multiple_tasks_in_progress": "Multiple tasks are in progress",
		"unknown_error": "Unknown error occurred"
	},
	search_defaults: {
		"empty_input_term": "Input a term to search.",
		"input.placeholder": "Search for...",
		"input_clear.title": "Clear the search",
		"input_submit.title": "Search",
		"input_term_too_short": "Input at least {{termLength}} characters to\xA0search.",
		"no_results": "No results found."
	},
	search_element_types: {
		"drag_create.title": "Click or drag to create new entity of this type",
		"input.placeholder": null,
		"no_results": "No element types found.",
		"refresh_progress.title": "Loading element type tree",
		"show_only_creatable": "Show only constructible"
	},
	search_entities: {
		"add_group.label": "Add as group",
		"add_group.title": "Place selected entities on the canvas as a group",
		"add_selected.label": "Add selected",
		"add_selected.title": "Place selected entities on the canvas",
		"criteria_has_type": "Has type {{entityType}}",
		"criteria_connected": "Connected to {{entity}}",
		"criteria_connected_via": "Connected to {{entity}} through {{relationType}}",
		"criteria_connected_to_source": "Connected to {{entity}} through {{relationType}} as {{sourceIcon}}\xA0source",
		"criteria_connected_to_target": "Connected to {{entity}} through {{relationType}} as {{targetIcon}}\xA0target",
		"input.placeholder": null,
		"place_elements.command": "Add selected elements",
		"query_progress.title": "Querying for entities",
		"show_more_results.label": "Show more",
		"show_more_results.title": "Load more results"
	},
	search_link_types: {
		"add_to_filter.title": "Search for connected elements via the relation",
		"heading_connected": "Connected to selected entities",
		"heading_connected_single": "Connected to {{entity}}",
		"heading_other": "Other links",
		"input.placeholder": null,
		"no_results": "No links found.",
		"switch.command": "Change link types visibility",
		"switch_all.label": "Switch all",
		"switch_hidden.title": "Hide links and labels",
		"switch_visible.title": "Show links with labels",
		"switch_without_label.title": "Show only lines for links (without labels)"
	},
	selection_action: {
		"add_to_filter.title": "Search for connected elements",
		"anchor.title": "Jump to resource",
		"annotate.defaultContent": "Annotation",
		"annotate.title": "Add annotation to this element",
		"connections.title": "Navigate to connected elements",
		"expand.collapse_command": "Collapse elements",
		"expand.expand_command": "Expand elements",
		"expand.title": "Expand selected elements to reveal additional properties",
		"expand.title_single": "Expand an element to reveal additional properties",
		"establish_relation.title": "Establish link",
		"establish_relation.title_disabled": "Establishing link is not allowed for the element",
		"group.title": "Group entities",
		"group.title_ungroup": "Ungroup entities",
		"layout.title": "Layout selected elements using force-directed algorithm",
		"remove.title": "Remove selected elements from the diagram",
		"remove.title_new": "Discard new element",
		"remove.title_single": "Remove an element from the diagram",
		"zoom_to_fit.title": "Zoom to fit selected elements into view"
	},
	standard_element: {
		"blank_node": "(blank node)",
		"default_type": "Thing",
		"delete.label": "Delete",
		"delete.title": "Delete entity",
		"delete.title_disabled": "Cannot delete the entity",
		"edit.label": "Edit",
		"edit.title": "Edit entity",
		"edit.title_disabled": "Cannot edit the entity",
		"group_item.title": "{{entity}}\nIRI:{{entityIri}}\nTypes: {{entityTypes}}",
		"iri.label": "IRI:",
		"iri.label_modified": "IRI\xA0(edited):",
		"no_properties": "no properties",
		"property.title": "{{property}} {{propertyIri}}",
		"ungroup.title": "Ungroup an entity"
	},
	standard_link: {
		"group_source.title": "{{value}} source elements",
		"group_source.value": "{{value}}",
		"group_target.title": "{{value}} target elements",
		"group_target.value": "{{value}}",
		"label.title": "{{relation}} {{relationIri}}",
		"property.title": "{{property}} {{propertyIri}}"
	},
	toolbar: { "menu_toggle.title": "Open menu" },
	toolbar_action: {
		"clear_all.command": "Clear all",
		"clear_all.title": "Remove all elements and links from the diagram",
		"clear_all.label": "Clear All",
		"export_print.label": "Print",
		"export_print.title": "Print the diagram",
		"export_raster.label": "Export as PNG",
		"export_raster.title": "Export the diagram as a PNG image",
		"export_svg.label": "Export as SVG",
		"export_svg.title": "Export the diagram as an SVG image",
		"redo.label": "Redo",
		"redo.title": "Redo last undone command",
		"redo.title_named": "Redo: {{command}}",
		"save_authoring.title": "Apply authored graph changes",
		"save_layout.title": "Save diagram layout",
		"language_selector.title": "Select language for the data (labels, properties, etc)",
		"layout.label": "Layout",
		"layout.title": "Layout diagram using force-directed algorithm",
		"undo.label": "Undo",
		"undo.title": "Undo last command",
		"undo.title_named": "Undo: {{command}}"
	},
	unified_search: {
		"input.placeholder": null,
		"input_clear.title": "Clear",
		"input_collapse.title": "Close"
	},
	visual_authoring: {
		"dialog.apply.label": "Apply",
		"dialog.apply.title": "Apply changes",
		"dialog.cancel.label": "Cancel",
		"dialog.cancel.title": "Cancel the dialog",
		"edit_entity.dialog.apply.label": null,
		"edit_entity.dialog.apply.title": null,
		"edit_entity.dialog.caption": "Edit entity",
		"edit_entity.iri.label": "IRI",
		"edit_entity.iri.placeholder": "Entity IRI",
		"edit_relation.dialog.apply.label": null,
		"edit_relation.dialog.apply.title": null,
		"edit_relation.dialog.caption": "Edit relation",
		"edit_relation.dialog.caption_new": "Establish new relation",
		"edit_relation.validation_progress.title": "Validating selected link type",
		"find_or_create.connect_command": "Link to an entity",
		"find_or_create.create_command": "Create new entity",
		"find_or_create.dialog.apply.label": null,
		"find_or_create.dialog.apply.title": null,
		"find_or_create.dialog.caption": "Establish New Connection",
		"find_or_create.loading.label": "Loading entity...",
		"find_or_create.validation_progress.title": "Validating selected element and link types",
		"property.label": "{{property}}",
		"property.text_value.placeholder": "Property value",
		"property.title": "{{property}} {{propertyIri}}",
		"rename_link.dialog.apply.label": null,
		"rename_link.dialog.apply.title": null,
		"rename_link.dialog.caption": "Rename Link",
		"rename_link.label.label": "Label",
		"select_entity.entity_type.label": "{{type}}",
		"select_entity.input.placeholder": null,
		"select_entity.type.label": "Entity Type",
		"select_entity.type.placeholder": "Select entity type",
		"select_entity.results.aria_label": "Select an existing element to put on a diagram",
		"select_entity.separator.label": "or create new entity",
		"select_entity.validation.error_required": "Required.",
		"select_relation.relation_type.label": "{{relation}} [{{source}} → {{target}}]",
		"select_relation.type.label": "Relation Type",
		"select_relation.type.placeholder": "Select relation type",
		"select_relation.validation.error_duplicate": "The relation already exists.",
		"select_relation.validation.error_required": "Required."
	},
	workspace: {
		"group_entities.command": "Group entities",
		"perform_layout.task": "Computing graph layout",
		"perform_layout.command": "Graph layout",
		"ungroup_entities.command": "Ungroup entities"
	},
	workspace_layout: {
		"toggle_collapse.title": "Collapse panel",
		"toggle_expand.title": "Expand panel"
	},
	zoom_control: {
		"pointer_mode.title": "Toggle selection mode:\nDrag pointer to select, Shift + Pointer to pan the canvas",
		"zoom_in.title": "Zoom In",
		"zoom_out.title": "Zoom Out",
		"zoom_to_fit.title": "Fit to Screen"
	}
};
/**
* Default built-in implementation for i18n strings interpolation and other
* methods from {@link Translation} interface.
*
* @category Core
*/
var DefaultTranslation = class {
	bundles;
	_selectLabel;
	constructor(options) {
		const { bundles = [], selectLabel = defaultSelectLabel } = options ?? {};
		this.bundles = bundles;
		this._selectLabel = selectLabel;
	}
	getString(key) {
		const dotIndex = key.indexOf(".");
		if (!(dotIndex > 0 && dotIndex < key.length)) throw new Error(`Reactodia: Invalid translation key: ${key}`);
		const group = key.substring(0, dotIndex);
		const leaf = key.substring(dotIndex + 1);
		for (const bundle of this.bundles) {
			const text = lookupString(bundle, group, leaf);
			if (!(text === null || text === void 0)) return text;
		}
	}
	text(key, placeholders) {
		return this.textOptional(key, placeholders) ?? key;
	}
	textOptional(key, placeholders) {
		const template = this.getString(key);
		if (template === void 0) return;
		return formatPlaceholders(template, placeholders);
	}
	template(key, parts) {
		return templatePlaceholders(this.getString(key) ?? key, parts);
	}
	selectLabel(labels, language) {
		const { _selectLabel } = this;
		return _selectLabel(labels, language);
	}
	selectValues(values, language) {
		return values.filter((v) => v.termType === "NamedNode" || v.language === "" || v.language === language);
	}
	formatLabel(labels, fallbackIri, language) {
		return resolveLabel(labels ? this.selectLabel(labels, language) : void 0, fallbackIri);
	}
};
function lookupString(bundle, group, leaf) {
	if (!Object.prototype.hasOwnProperty.call(bundle, group)) return;
	const bundleGroup = bundle[group];
	if (!(typeof bundleGroup === "object" && bundleGroup && Object.prototype.hasOwnProperty.call(bundleGroup, leaf))) return;
	return bundleGroup[leaf];
}
function formatPlaceholders(template, values) {
	if (!template.includes("{{")) return template;
	return replacePlaceholders(template, (placeholder) => {
		if (!(values && Object.prototype.hasOwnProperty.call(values, placeholder))) return "";
		return values[placeholder] ?? "";
	}).join("");
}
function templatePlaceholders(template, values) {
	const parts = replacePlaceholders(template, (placeholder) => {
		if (!Object.prototype.hasOwnProperty.call(values, placeholder)) return null;
		return values[placeholder] ?? null;
	});
	return React.createElement(React.Fragment, null, ...parts);
}
function replacePlaceholders(template, replacer) {
	const parts = [];
	const templateRegex = /\{\{([a-zA-Z0-9_]+)\}\}/g;
	let lastIndex = 0;
	let result;
	while (result = templateRegex.exec(template)) {
		const [prefix, placeholder] = result;
		const startIndex = templateRegex.lastIndex - prefix.length;
		if (startIndex != lastIndex) parts.push(template.substring(lastIndex, startIndex));
		const part = replacer(placeholder);
		parts.push(part);
		lastIndex = templateRegex.lastIndex;
	}
	if (lastIndex < template.length) parts.push(template.substring(lastIndex, template.length));
	return parts;
}
function defaultSelectLabel(texts, language) {
	if (texts.length === 0) return;
	let defaultValue;
	let englishValue;
	for (const text of texts) if (text.language === language) return text;
	else if (text.language === "") defaultValue = text;
	else if (text.language === "en") englishValue = text;
	return defaultValue !== void 0 ? defaultValue : englishValue !== void 0 ? englishValue : texts[0];
}
function resolveLabel(label, fallbackIri) {
	if (label) return label.value;
	return getLocalName(fallbackIri) || fallbackIri;
}
//#endregion
//#region src/diagram/placeLayer.tsx
var CanvasPlaceLayerContext = React.createContext(null);
function createPlaceLayerContext() {
	const containers = {
		underlay: document.createElement("div"),
		overLinkGeometry: document.createElement("div"),
		overLinks: document.createElement("div"),
		overElements: document.createElement("div")
	};
	for (const container of Object.values(containers)) container.style.display = "contents";
	return { containers };
}
function CanvasPlaceLayer(props) {
	const { context, layer, ...otherProps } = props;
	const outerRef = React.useRef(null);
	const container = context.containers[layer];
	React.useLayoutEffect(() => {
		const outer = outerRef.current;
		if (outer) {
			outer.appendChild(container);
			return () => {
				outer.removeChild(container);
			};
		}
	}, [outerRef, container]);
	return /* @__PURE__ */ jsx("div", {
		...otherProps,
		ref: outerRef,
		"data-reactodia-place-layer": layer
	});
}
function isCanvasPlaceLayer(element) {
	return element.hasAttribute("data-reactodia-place-layer");
}
/**
* Places child components on a specified canvas layer as canvas widgets.
*
* @category Components
*/
function CanvasPlaceAt(props) {
	const { layer, children } = props;
	const placeContext = React.useContext(CanvasPlaceLayerContext);
	if (!placeContext) throw new Error("Reactodia: <CanvasPlaceAt> should be rendered only inside <Canvas>");
	else if (placeContext === "nested") throw new Error("Reactodia: <CanvasPlaceAt> cannot be nested into another <CanvasPlaceAt>");
	const withContext = React.useMemo(() => /* @__PURE__ */ jsx(CanvasPlaceLayerContext.Provider, {
		value: "nested",
		children
	}), [children]);
	const container = placeContext.containers[layer];
	return createPortal(withContext, container);
}
//#endregion
//#region src/diagram/sharedCanvasState.ts
/**
* Stores common state and settings for multiple canvases.
*
* @category Core
*/
var SharedCanvasState = class {
	source = new EventSource();
	/**
	* Event for the shared canvas state.
	*/
	events = this.source;
	dropOnPaperHandler;
	_highlighter;
	/**
	* Default element template resolver to use as a fallback
	* (returns a default template for any element).
	*/
	defaultElementResolver;
	/**
	* Default link template resolver to use as a fallback
	* (returns a default template for any link).
	*/
	defaultLinkResolver;
	/**
	* Default layout algorithm function to use if it's not specified explicitly.
	*/
	defaultLayout;
	/**
	* A strategy to rename diagram links (change labels).
	*/
	renameLinkProvider;
	/** @hidden */
	constructor(options) {
		const { defaultElementResolver, defaultLinkResolver, defaultLayout, renameLinkProvider } = options;
		this.defaultElementResolver = defaultElementResolver;
		this.defaultLinkResolver = defaultLinkResolver;
		this.defaultLayout = defaultLayout;
		this.renameLinkProvider = renameLinkProvider;
	}
	/** @hidden */
	dispose() {
		this.source.trigger("dispose", { source: this });
	}
	/**
	* Returns all canvases that use this shared state.
	*/
	findAllCanvases() {
		const event = { canvases: [] };
		this.source.trigger("findCanvas", event);
		return event.canvases;
	}
	/**
	* Returns any canvas that uses this shared state or `undefined` if none found.
	*/
	findAnyCanvas() {
		const canvases = this.findAllCanvases();
		return canvases.length > 0 ? canvases[0] : void 0;
	}
	/**
	* Sets the handler for the next drop event from drag-and-drop operation on a canvas.
	*
	* **Experimental**: this feature will likely change in the future.
	*/
	setHandlerForNextDropOnPaper(handler) {
		this.dropOnPaperHandler = handler;
	}
	/**
	* Returns `true` if there is a previously set drop handler on a canvas,
	* otherwise `false`.
	*
	* **Experimental**: this feature will likely change in the future.
	*/
	hasHandlerForNextDropOnPaper() {
		return Boolean(this.dropOnPaperHandler);
	}
	/**
	* Tries to run previously set drop handler on a canvas,
	* then removes the handler if it was set.
	*
	* **Experimental**: this feature will likely change in the future.
	*
	* @returns `true` if a handler was set, otherwise `false`
	*/
	tryHandleDropOnPaper(e) {
		const { dropOnPaperHandler } = this;
		if (dropOnPaperHandler) {
			this.dropOnPaperHandler = void 0;
			e.sourceEvent.preventDefault();
			dropOnPaperHandler(e);
			return true;
		}
		return false;
	}
	/**
	* Returns active highlight for the diagram cells.
	*
	* **Experimental**: this feature will likely change in the future.
	*/
	get highlighter() {
		return this._highlighter;
	}
	/**
	* Sets or removes an active highlight for the diagram cells.
	*
	* **Experimental**: this feature will likely change in the future.
	*/
	setHighlighter(value) {
		const previous = this._highlighter;
		if (previous === value) return;
		this._highlighter = value;
		this.source.trigger("changeHighlight", {
			source: this,
			previous
		});
	}
};
/**
* A strategy to rename diagram links which stores changed link label
* in the link template state.
*
* @see {@link TemplateProperties.CustomLabel}
*/
var RenameLinkToLinkStateProvider = class {
	canRename(link) {
		return true;
	}
	getLabel(link) {
		const { linkState } = link;
		const customLabel = linkState.get(TemplateProperties.CustomLabel);
		return typeof customLabel === "string" ? customLabel : void 0;
	}
	setLabel(link, label) {
		link.setLinkState(link.linkState.set(TemplateProperties.CustomLabel, label.length === 0 ? void 0 : label));
	}
};
//#endregion
//#region src/editor/annotationCells.ts
/**
* Diagram-only annotation element, exported and imported with the diagram.
*/
var AnnotationElement = class AnnotationElement extends Element {
	static fromJSONType = "Annotation";
	static fromJSON(state, options) {
		const { "@id": id, position, elementState } = state;
		return new AnnotationElement({
			id,
			position,
			elementState: options.mapTemplateState(TemplateState.fromJSON(elementState))
		});
	}
	toJSON() {
		return {
			"@type": "Annotation",
			"@id": this.id,
			position: this.position,
			elementState: this.elementState.toJSON()
		};
	}
};
/**
* Diagram-only annotation link, exported and imported with the diagram.
*/
var AnnotationLink = class AnnotationLink extends Link {
	static typeId = "urn:reactodia:annotates";
	getTypeId() {
		return AnnotationLink.typeId;
	}
	static fromJSONType = "AnnotationLink";
	static fromJSON(state, options) {
		const { "@id": id, vertices, linkState } = state;
		return new AnnotationLink({
			id,
			sourceId: options.source.id,
			targetId: options.target.id,
			vertices,
			linkState: options.mapTemplateState(TemplateState.fromJSON(linkState)).set(TemplateProperties.LayoutOnly, void 0)
		});
	}
	toJSON() {
		return {
			"@type": "AnnotationLink",
			"@id": this.id,
			source: { "@id": this.sourceId },
			target: { "@id": this.targetId },
			vertices: this.vertices,
			linkState: this.linkState.toJSON()
		};
	}
};
//#endregion
//#region src/editor/authoringState.ts
var AuthoringState;
(function(_AuthoringState) {
	_AuthoringState.empty = {
		elements: /* @__PURE__ */ new Map(),
		links: new HashMap(hashLink, equalLinks)
	};
	function isEmpty(state) {
		return state.elements.size === 0 && state.links.size === 0;
	}
	_AuthoringState.isEmpty = isEmpty;
	function clone(index) {
		return {
			elements: new Map(index.elements),
			links: index.links.clone()
		};
	}
	_AuthoringState.clone = clone;
	function isEntityEvent(event) {
		switch (event.type) {
			case "entityAdd":
			case "entityChange":
			case "entityDelete": return true;
			default: return false;
		}
	}
	_AuthoringState.isEntityEvent = isEntityEvent;
	function isLinkEvent(event) {
		return !isEntityEvent(event);
	}
	_AuthoringState.isLinkEvent = isLinkEvent;
	function has(state, event) {
		return isEntityEvent(event) ? state.elements.get(event.data.id) === event : state.links.get(event.data) === event;
	}
	_AuthoringState.has = has;
	function discard(state, discarded) {
		if (!has(state, discarded)) return state;
		const newState = clone(state);
		if (isEntityEvent(discarded)) {
			newState.elements.delete(discarded.data.id);
			if (discarded.type === "entityAdd") {
				for (const { data: link } of state.links.values()) if (isLinkConnectedToElement(link, discarded.data.id)) newState.links.delete(link);
			}
		} else newState.links.delete(discarded.data);
		return newState;
	}
	_AuthoringState.discard = discard;
	function addEntity(state, data) {
		const newState = clone(state);
		newState.elements.set(data.id, {
			type: "entityAdd",
			data
		});
		return newState;
	}
	_AuthoringState.addEntity = addEntity;
	function addRelation(state, data) {
		const newState = clone(state);
		newState.links.set(data, {
			type: "relationAdd",
			data
		});
		return newState;
	}
	_AuthoringState.addRelation = addRelation;
	function changeEntity(state, before, after) {
		const newState = clone(state);
		newState.elements.delete(before.id);
		const previous = state.elements.get(before.id);
		if (!previous || previous.type === "entityChange" || previous.type === "entityDelete") {
			const iriChanged = after.id !== before.id;
			newState.elements.set(before.id, {
				type: "entityChange",
				before: previous && previous.type === "entityChange" ? previous.before : before,
				data: iriChanged ? {
					...after,
					id: before.id
				} : after,
				newIri: iriChanged ? after.id : void 0
			});
		} else {
			newState.elements.set(after.id, {
				type: "entityAdd",
				data: after
			});
			if (before.id !== after.id) {
				for (const e of state.links.values()) if (e.type !== "relationChange" && isLinkConnectedToElement(e.data, before.id)) {
					const updatedLink = updateLinkToReferByNewIri(e.data, before.id, after.id);
					newState.links.delete(e.data);
					newState.links.set(updatedLink, {
						type: "relationAdd",
						data: updatedLink
					});
				}
			}
		}
		return newState;
	}
	_AuthoringState.changeEntity = changeEntity;
	function changeRelation(state, before, after) {
		if (!equalLinks(before, after)) throw new Error("Cannot move link to another element or change its type");
		const newState = clone(state);
		const previous = state.links.get(before);
		if (previous?.type === "relationAdd") newState.links.set(before, {
			type: "relationAdd",
			data: after
		});
		else newState.links.set(before, {
			type: "relationChange",
			before: previous && previous.type === "relationChange" ? previous.before : before,
			data: after
		});
		return newState;
	}
	_AuthoringState.changeRelation = changeRelation;
	function deleteEntity(state, data) {
		const newState = clone(state);
		newState.elements.delete(data.id);
		for (const { data: link } of state.links.values()) if (isLinkConnectedToElement(link, data.id)) newState.links.delete(link);
		if (!isAddedEntity(state, data.id)) newState.elements.set(data.id, {
			type: "entityDelete",
			data
		});
		return newState;
	}
	_AuthoringState.deleteEntity = deleteEntity;
	function deleteRelation(state, data) {
		const newState = clone(state);
		newState.links.delete(data);
		if (!isAddedRelation(state, data)) newState.links.set(data, {
			type: "relationDelete",
			data
		});
		return newState;
	}
	_AuthoringState.deleteRelation = deleteRelation;
	function discardAddedRelations(state, connectedEntities) {
		const newState = clone(state);
		for (const e of state.links.values()) if (e.type === "relationAdd") {
			const target = e.data;
			if (connectedEntities.has(target.sourceId) || connectedEntities.has(target.targetId)) newState.links.delete(target);
		}
		return newState;
	}
	_AuthoringState.discardAddedRelations = discardAddedRelations;
	function isAddedEntity(state, target) {
		const event = state.elements.get(target);
		return Boolean(event && event.type === "entityAdd");
	}
	_AuthoringState.isAddedEntity = isAddedEntity;
	function isDeletedEntity(state, target) {
		const event = state.elements.get(target);
		return Boolean(event && event.type === "entityDelete");
	}
	_AuthoringState.isDeletedEntity = isDeletedEntity;
	function hasEntityChangedIri(state, target) {
		const event = state.elements.get(target);
		return Boolean(event && event.type === "entityChange" && event.newIri);
	}
	_AuthoringState.hasEntityChangedIri = hasEntityChangedIri;
	function isAddedRelation(state, key) {
		const event = state.links.get(key);
		return Boolean(event && event.type === "relationAdd");
	}
	_AuthoringState.isAddedRelation = isAddedRelation;
	function isDeletedRelation(state, key) {
		const event = state.links.get(key);
		return event && event.type === "relationDelete" || isDeletedEntity(state, key.sourceId) || isDeletedEntity(state, key.targetId);
	}
	_AuthoringState.isDeletedRelation = isDeletedRelation;
})(AuthoringState || (AuthoringState = {}));
var TemporaryState;
(function(_TemporaryState) {
	_TemporaryState.empty = {
		elements: /* @__PURE__ */ new Set(),
		links: new HashSet(hashLink, equalLinks)
	};
	function addEntity(state, element) {
		if (state.elements.has(element.id)) return state;
		const elements = new Set(state.elements);
		elements.add(element.id);
		return {
			...state,
			elements
		};
	}
	_TemporaryState.addEntity = addEntity;
	function removeEntity(state, element) {
		if (!state.elements.has(element.id)) return state;
		const elements = new Set(state.elements);
		elements.delete(element.id);
		return {
			...state,
			elements
		};
	}
	_TemporaryState.removeEntity = removeEntity;
	function addRelation(state, link) {
		if (state.links.has(link)) return state;
		const links = state.links.clone();
		links.add(link);
		return {
			...state,
			links
		};
	}
	_TemporaryState.addRelation = addRelation;
	function removeRelation(state, link) {
		if (!state.links.has(link)) return state;
		const links = state.links.clone();
		links.delete(link);
		return {
			...state,
			links
		};
	}
	_TemporaryState.removeRelation = removeRelation;
})(TemporaryState || (TemporaryState = {}));
function isLinkConnectedToElement(link, elementIri) {
	return link.sourceId === elementIri || link.targetId === elementIri;
}
function updateLinkToReferByNewIri(link, oldIri, newIri) {
	return {
		...link,
		sourceId: link.sourceId === oldIri ? newIri : link.sourceId,
		targetId: link.targetId === oldIri ? newIri : link.targetId
	};
}
//#endregion
//#region src/editor/builtinDialogType.ts
/**
* Well-known overlay dialog types for the built-in workspace components.
*
* @category Constants
*/
var BuiltinDialogType = {
	connectionsMenu: "reactodia:connectionsMenu",
	editEntity: "reactodia:editEntity",
	editRelation: "reactodia:editRelation",
	findOrCreateEntity: "reactodia:findOrCreateEntity",
	renameLink: "reactodia:renameLink"
};
//#endregion
//#region src/diagram/graph.ts
var Graph = class {
	source = new EventSource();
	events = this.source;
	cellsVersion = Math.floor(Math.random() * Number.MAX_SAFE_INTEGER / 2);
	elements = new OrderedMap();
	links = new OrderedMap();
	elementLinks = /* @__PURE__ */ new WeakMap();
	EMPTY_LINKS = [];
	linkTypeVisibility = /* @__PURE__ */ new Map();
	getCellsVersion() {
		return this.cellsVersion;
	}
	getElements() {
		return this.elements.items;
	}
	getLinks() {
		return this.links.items;
	}
	getLink(linkId) {
		return this.links.get(linkId);
	}
	getElementLinks(element) {
		return this.elementLinks.get(element) ?? [];
	}
	*iterateLinks(sourceId, targetId, linkTypeId) {
		const source = this.getElement(sourceId);
		const target = this.getElement(targetId);
		if (!(source && target)) return;
		const sourceLinks = this.elementLinks.get(source) ?? this.EMPTY_LINKS;
		const targetLinks = this.elementLinks.get(target) ?? this.EMPTY_LINKS;
		const linksToIterate = sourceLinks.length <= targetLinks.length ? sourceLinks : targetLinks;
		for (const link of linksToIterate) if (link.sourceId === sourceId && link.targetId === targetId && (!linkTypeId || link.typeId === linkTypeId)) yield link;
	}
	sourceOf(link) {
		return this.getElement(link.sourceId);
	}
	targetOf(link) {
		return this.getElement(link.targetId);
	}
	reorderElements(compare) {
		this.elements.reorder(compare);
		this.incrementCellsVersion();
	}
	getElement(elementId) {
		return this.elements.get(elementId);
	}
	addElement(element) {
		if (this.getElement(element.id)) throw new Error(`Element '${element.id}' already exists.`);
		element.events.onAny(this.onElementEvent);
		this.elements.push(element.id, element);
		this.incrementCellsVersion();
		this.source.trigger("changeCells", {
			updateAll: false,
			changedElement: element
		});
	}
	onElementEvent = (data) => {
		this.source.trigger("elementEvent", { data });
	};
	removeElement(elementId) {
		const element = this.elements.get(elementId);
		if (element) {
			const options = { silent: true };
			const changedLinks = [...this.getElementLinks(element)];
			for (const link of changedLinks) this.removeLink(link.id, options);
			this.elements.delete(elementId);
			element.events.offAny(this.onElementEvent);
			this.incrementCellsVersion();
			this.source.trigger("changeCells", {
				updateAll: false,
				changedElement: element,
				changedLinks
			});
		}
	}
	addLink(link) {
		if (this.getLink(link.id)) throw new Error(`Link already exists: ${link.id}`);
		const source = this.sourceOf(link);
		if (!source) throw new Error(`Link source not found: ${link.sourceId}`);
		const target = this.targetOf(link);
		if (!target) throw new Error(`Link target not found: ${link.targetId}`);
		let sourceLinks = this.elementLinks.get(source);
		if (!sourceLinks) {
			sourceLinks = [];
			this.elementLinks.set(source, sourceLinks);
		}
		sourceLinks.push(link);
		if (link.sourceId !== link.targetId) {
			let targetLinks = this.elementLinks.get(target);
			if (!targetLinks) {
				targetLinks = [];
				this.elementLinks.set(target, targetLinks);
			}
			targetLinks.push(link);
		}
		link.events.onAny(this.onLinkEvent);
		this.links.push(link.id, link);
		this.incrementCellsVersion();
		this.source.trigger("changeCells", {
			updateAll: false,
			changedLinks: [link]
		});
	}
	onLinkEvent = (data) => {
		this.source.trigger("linkEvent", { data });
	};
	removeLink(linkId, options) {
		const link = this.links.delete(linkId);
		if (link) {
			link.events.offAny(this.onLinkEvent);
			this.removeLinkReferences(link);
			this.incrementCellsVersion();
			if (!(options && options.silent)) this.source.trigger("changeCells", {
				updateAll: false,
				changedLinks: [link]
			});
		}
	}
	removeLinkReferences(link) {
		const source = this.getElement(link.sourceId);
		if (source) {
			const sourceLinks = this.elementLinks.get(source);
			if (sourceLinks) {
				removeLinkFrom(sourceLinks, link);
				if (sourceLinks.length === 0) this.elementLinks.delete(source);
			}
		}
		const target = this.getElement(link.targetId);
		if (target) {
			const targetLinks = this.elementLinks.get(target);
			if (targetLinks) {
				removeLinkFrom(targetLinks, link);
				if (targetLinks.length === 0) this.elementLinks.delete(target);
			}
		}
	}
	incrementCellsVersion() {
		this.cellsVersion = this.cellsVersion + 1;
		if (this.cellsVersion >= Number.MAX_SAFE_INTEGER) this.cellsVersion = 1;
	}
	get linkVisibility() {
		return this.linkTypeVisibility;
	}
	getLinkVisibility(linkTypeId) {
		return this.linkTypeVisibility.get(linkTypeId) ?? "visible";
	}
	setLinkVisibility(linkTypeId, value) {
		const previous = this.getLinkVisibility(linkTypeId);
		if (value === previous) return;
		this.linkTypeVisibility.set(linkTypeId, value);
		this.source.trigger("changeLinkVisibility", {
			source: linkTypeId,
			previous
		});
	}
};
function removeLinkFrom(links, link) {
	const index = links.indexOf(link);
	if (index >= 0) links.splice(index, 1);
}
//#endregion
//#region src/diagram/model.ts
var InternalGetGraph = Symbol("DiagramModel.getGraph");
/**
* Stores the diagram content: graph (elements, links);
* maintains selection and the current language to display the data.
*
* Additionally, the diagram model provides the means to undo/redo commands
* via {@link DiagramModel.history history}.
*
* @category Core
*/
var DiagramModel = class {
	/**
	* Event source to trigger events.
	*/
	source = new EventSource();
	/**
	* Events for the diagram model.
	*/
	events = this.source;
	_language = "en";
	_selection = [];
	graph = new Graph();
	graphListener = new EventObserver();
	/**
	* Provides the mechanism to undo/redo commands on the diagram.
	*/
	history;
	/** @hidden */
	constructor(options) {
		const { history } = options;
		this.history = history;
	}
	/** @hidden */
	[InternalGetGraph]() {
		return this.graph;
	}
	/**
	* Current language for the diagram content.
	*
	* Language code is specified as lowercase [BCP47](https://www.rfc-editor.org/rfc/rfc5646)
	* string (examples: `en`, `en-gb`, etc).
	*
	* Initial language is `en`.
	*/
	get language() {
		return this._language;
	}
	/**
	* Sets current language for the diagram content.
	*
	* Language code is specified as lowercase [BCP47](https://www.rfc-editor.org/rfc/rfc5646)
	* string (examples: `en`, `en-gb`, etc).
	*/
	setLanguage(value) {
		if (!value) throw new Error("Reactodia: cannot set empty language");
		const previous = this._language;
		if (previous === value) return;
		this._language = value;
		this.source.trigger("changeLanguage", {
			source: this,
			previous
		});
	}
	/**
	* Current diagram selection (elements and/or links).
	*/
	get selection() {
		return this._selection;
	}
	/**
	* Sets current diagram selection (elements and/or links).
	*
	* When called, selected cells will be brought to the front
	* before all other diagram cells.
	*/
	setSelection(value) {
		const previous = this._selection;
		if (previous === value) return;
		const nextSelection = Array.from(value);
		const selectedElements = nextSelection.filter((cell) => cell instanceof Element);
		this.bringElements(selectedElements, "front");
		this._selection = nextSelection;
		this.source.trigger("changeSelection", {
			source: this,
			previous
		});
	}
	get factory() {
		return this.getTermFactory();
	}
	get cellsVersion() {
		return this.graph.getCellsVersion();
	}
	get elements() {
		return this.graph.getElements();
	}
	get links() {
		return this.graph.getLinks();
	}
	/**
	* Provides RDF term factory for the diagram model.
	*/
	getTermFactory() {
		return DefaultDataFactory;
	}
	getElement(elementId) {
		return this.graph.getElement(elementId);
	}
	getElementLinks(element) {
		return this.graph.getElementLinks(element);
	}
	getLink(linkId) {
		return this.graph.getLink(linkId);
	}
	findLink(linkTypeId, sourceId, targetId) {
		for (const link of this.graph.iterateLinks(sourceId, targetId, linkTypeId)) return link;
	}
	sourceOf(link) {
		return this.getElement(link.sourceId);
	}
	targetOf(link) {
		return this.getElement(link.targetId);
	}
	getLinkVisibility(linkTypeId) {
		return this.graph.getLinkVisibility(linkTypeId);
	}
	/**
	* Sets current visibility mode for the specified link type.
	*/
	setLinkVisibility(linkTypeId, value) {
		this.graph.setLinkVisibility(linkTypeId, value);
	}
	resetGraph() {
		if (this.graphListener) {
			this.graphListener.stopListening();
			this.graphListener = new EventObserver();
		}
		this.graph = new Graph();
		this._selection = [];
		this.source.trigger("discardGraph", { source: this });
	}
	subscribeGraph() {
		this.graphListener.listen(this.graph.events, "changeCells", (e) => {
			this.triggerChangeCells(e);
		});
		this.graphListener.listen(this.graph.events, "elementEvent", (e) => {
			this.triggerElementEvent(e);
		});
		this.graphListener.listen(this.graph.events, "linkEvent", (e) => {
			this.triggerLinkEvent(e);
		});
		this.graphListener.listen(this.graph.events, "changeLinkVisibility", (e) => {
			this.source.trigger("changeLinkVisibility", e);
		});
		this.triggerChangeCells({ updateAll: true });
	}
	triggerChangeCells(e) {
		if (this.selection.length > 0) {
			const newSelection = this.selection.filter((item) => item instanceof Element ? this.getElement(item.id) : item instanceof Link ? this.getLink(item.id) : false);
			if (newSelection.length < this.selection.length) this.setSelection(newSelection);
		}
		this.source.trigger("changeCells", e);
	}
	triggerElementEvent(e) {
		this.source.trigger("elementEvent", e);
	}
	triggerLinkEvent(e) {
		this.source.trigger("linkEvent", e);
	}
	/**
	* Changes display order of elements on the diagram.
	*
	* @param compare Sort comparator to establish a particular ordering
	*/
	reorderElements(compare) {
		this.graph.reorderElements(compare);
		this.source.trigger("changeCellOrder", { source: this });
	}
	/**
	* Puts specified elements before or after all other in the display order.
	*/
	bringElements(targets, to) {
		if (targets.length === 0) return;
		this.reorderElements(moveComparator(this.elements, targets, to === "front" ? "end" : "start"));
	}
	/**
	* Adds the element to the diagram.
	*
	* Throws an error if element with the same {@link Element.id} already exists
	* in the graph.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	addElement(element) {
		this.history.execute(new AddElementCommand(this, element, []));
	}
	/**
	* Removes the element with specified ID from the diagram if exists.
	*
	* When element is removed, all connected links will be removed as well.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	removeElement(elementId) {
		const element = this.getElement(elementId);
		if (element) this.history.execute(new RemoveElementCommand(this, element));
	}
	/**
	* Adds the link to the diagram.
	*
	* Throws an error if link with the same {@link Link.id} already exists
	* in the graph or any of source or target is not in the graph.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	addLink(link) {
		this.history.execute(new AddLinkCommand(this, link));
	}
	/**
	* Removes the link with specified ID from the diagram if exists.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	removeLink(linkId) {
		const link = this.graph.getLink(linkId);
		if (link) this.history.execute(new RemoveLinkCommand(this, link));
	}
};
var AddElementCommand = class {
	model;
	element;
	connectedLinks;
	constructor(model, element, connectedLinks) {
		this.model = model;
		this.element = element;
		this.connectedLinks = connectedLinks;
	}
	get title() {
		return TranslatedText.text("data_diagram_model.add_element.command");
	}
	invoke() {
		const { model, element, connectedLinks } = this;
		const graph = model[InternalGetGraph]();
		graph.addElement(element);
		for (const link of connectedLinks) if (!graph.getLink(link.id)) graph.addLink(link);
		return new RemoveElementCommand(model, element);
	}
};
var RemoveElementCommand = class {
	model;
	element;
	constructor(model, element) {
		this.model = model;
		this.element = element;
	}
	get title() {
		return TranslatedText.text("data_diagram_model.remove_element.command");
	}
	invoke() {
		const { model, element } = this;
		const graph = model[InternalGetGraph]();
		const connectedLinks = [...graph.getElementLinks(element)];
		graph.removeElement(element.id);
		return new AddElementCommand(model, element, connectedLinks);
	}
};
var AddLinkCommand = class {
	model;
	link;
	constructor(model, link) {
		this.model = model;
		this.link = link;
	}
	get title() {
		return TranslatedText.text("data_diagram_model.add_link.command");
	}
	invoke() {
		const { model, link } = this;
		model[InternalGetGraph]().addLink(link);
		return new RemoveLinkCommand(model, link);
	}
};
var RemoveLinkCommand = class {
	model;
	link;
	constructor(model, link) {
		this.model = model;
		this.link = link;
	}
	get title() {
		return TranslatedText.text("data_diagram_model.remove_link.command");
	}
	invoke() {
		const { model, link } = this;
		model[InternalGetGraph]().removeLink(link.id);
		return new AddLinkCommand(model, link);
	}
};
//#endregion
//#region src/editor/dataElements.ts
/**
* Data graph entity represented by a diagram element and referenced by an IRI.
*
* @category Core
*/
var EntityElement = class EntityElement extends Element {
	_data;
	constructor(props) {
		super(props);
		this._data = props.data;
	}
	/**
	* Creates an empty (placeholder) data for the specified entity IRI.
	*
	* This data can be used to display an entity in the UI
	* until the actual data is loaded from a data provider.
	* 
	* @see {@link PlaceholderDataProperty}
	*/
	static placeholderData(iri) {
		return {
			id: iri,
			types: [],
			properties: { [PlaceholderDataProperty]: [] }
		};
	}
	/**
	* Returns `true` if the `data` is an empty placeholder (not yet loaded) data,
	* otherwise `false`.
	*
	* The entity data is considered to be a placeholder data if `data.properties`
	* contains `PlaceholderDataProperty` key with a empty or non-empty values.
	*
	* @see {@link PlaceholderDataProperty}
	*/
	static isPlaceholderData(data) {
		return Object.prototype.hasOwnProperty.call(data.properties, "urn:reactodia:isPlaceholder") && data.properties["urn:reactodia:isPlaceholder"] !== void 0;
	}
	get entitySource() {
		return this.source;
	}
	get iri() {
		return this._data.id;
	}
	get data() {
		return this._data;
	}
	setData(value) {
		const previous = this._data;
		if (previous === value) return;
		this._data = value;
		this.entitySource.trigger("changeData", {
			source: this,
			previous
		});
		this.entitySource.trigger("requestedRedraw", {
			source: this,
			level: "template"
		});
	}
	static fromJSONType = "Element";
	static fromJSON(state, options) {
		const { "@id": id, iri, position, isExpanded, elementState } = state;
		if (iri) return new EntityElement({
			id,
			data: options.getInitialData(iri) ?? EntityElement.placeholderData(iri),
			position,
			expanded: isExpanded,
			elementState: options.mapTemplateState(TemplateState.fromJSON(elementState))
		});
	}
	toJSON() {
		return {
			"@type": "Element",
			"@id": this.id,
			iri: this.iri,
			position: this.position,
			elementState: this.elementState.toJSON()
		};
	}
};
/**
* Command to set {@link EntityElement.data entity element data}.
*
* @category Commands
*/
function setEntityElementData(entity, data) {
	return Command.create(TranslatedText.text("commands.set_entity_data.title"), () => {
		const previous = entity.data;
		entity.setData(data);
		return setEntityElementData(entity, previous);
	});
}
/**
* A group of multiple data graph entities represented by a diagram element.
*
* @category Core
*/
var EntityGroup = class EntityGroup extends Element {
	_items;
	_itemIris = /* @__PURE__ */ new Set();
	constructor(props) {
		super(props);
		this._items = props.items ?? [];
		this.updateItemIris();
	}
	get entitySource() {
		return this.source;
	}
	get items() {
		return this._items;
	}
	setItems(value) {
		const previous = this._items;
		if (previous === value) return;
		this._items = value;
		this.updateItemIris();
		this.entitySource.trigger("changeItems", {
			source: this,
			previous
		});
		this.entitySource.trigger("requestedRedraw", {
			source: this,
			level: "template"
		});
	}
	get itemIris() {
		return this._itemIris;
	}
	updateItemIris() {
		this._itemIris.clear();
		for (const item of this._items) this._itemIris.add(item.data.id);
	}
	static fromJSONType = "ElementGroup";
	static fromJSON(state, options) {
		const { "@id": id, items, position, elementState } = state;
		const groupItems = [];
		for (const item of items) {
			const initialData = options.getInitialData(item.iri);
			groupItems.push({
				data: initialData ?? EntityElement.placeholderData(item.iri),
				elementState: options.mapTemplateState(TemplateState.fromJSON(item.elementState))
			});
		}
		return new EntityGroup({
			id,
			items: groupItems,
			position,
			elementState: TemplateState.fromJSON(elementState)
		});
	}
	toJSON() {
		return {
			"@type": "ElementGroup",
			"@id": this.id,
			items: this.items.map((item) => ({
				"@type": "ElementItem",
				iri: item.data.id,
				elementState: item.elementState?.toJSON()
			})),
			position: this.position,
			elementState: this.elementState.toJSON()
		};
	}
};
/**
* Command to set {@link EntityGroup.items entity group items}.
*
* @category Commands
*/
function setEntityGroupItems(group, items) {
	return Command.create(TranslatedText.text("commands.set_entity_group_items.title"), () => {
		const before = group.items;
		group.setItems(items);
		return setEntityGroupItems(group, before);
	});
}
/**
* Iterates over data for all entities of the target element.
*
* @category Core
*/
function* iterateEntitiesOf(element) {
	if (element instanceof EntityElement) yield element.data;
	else if (element instanceof EntityGroup) for (const item of element.items) yield item.data;
}
/**
* Data graph relation represented by a diagram link, uniquely identified by
* (source entity IRI, target entity IRI, link type IRI) tuple.
*
* @category Core
*/
var RelationLink = class RelationLink extends Link {
	_data;
	constructor(props) {
		super(props);
		this._data = props.data;
	}
	get relationSource() {
		return this.source;
	}
	getTypeId() {
		return this._data.linkTypeId;
	}
	get data() {
		return this._data;
	}
	setData(value) {
		const previous = this._data;
		if (previous === value) return;
		this._data = value;
		this.relationSource.trigger("changeData", {
			source: this,
			previous
		});
		this.relationSource.trigger("requestedRedraw", { source: this });
	}
	withDirection(data) {
		if (!(data.sourceId === this.data.sourceId || data.sourceId === this.data.targetId)) throw new Error("New link source IRI is unrelated to original link");
		if (!(data.targetId === this.data.sourceId || data.targetId === this.data.targetId)) throw new Error("New link target IRI is unrelated to original link");
		return new RelationLink({
			sourceId: data.sourceId === this.data.sourceId ? this.sourceId : this.targetId,
			targetId: data.targetId === this.data.targetId ? this.targetId : this.sourceId,
			data
		});
	}
	static fromJSONType = "Link";
	static fromJSON(state, options) {
		const { "@id": id, property, vertices, linkState } = state;
		const { source, target } = options;
		const sourceIri = state.sourceIri ?? (source instanceof EntityElement ? source.data.id : void 0);
		const targetIri = state.targetIri ?? (target instanceof EntityElement ? target.data.id : void 0);
		if (sourceIri && targetIri) {
			const key = {
				linkTypeId: property,
				sourceId: sourceIri,
				targetId: targetIri,
				properties: {}
			};
			const initialData = options.getInitialData(key);
			return new RelationLink({
				id,
				sourceId: source.id,
				targetId: target.id,
				data: initialData ?? key,
				vertices,
				linkState: options.mapTemplateState(TemplateState.fromJSON(linkState))
			});
		}
	}
	toJSON() {
		return {
			"@type": "Link",
			"@id": this.id,
			property: this.typeId,
			source: { "@id": this.sourceId },
			target: { "@id": this.targetId },
			sourceIri: this.data.sourceId,
			targetIri: this.data.targetId,
			vertices: [...this.vertices],
			linkState: this.linkState.toJSON()
		};
	}
};
/**
* Command to set relation {@link RelationLink.data relation link data}.
*
* @category Commands
*/
function setRelationLinkData(relation, data) {
	return Command.create(TranslatedText.text("commands.set_relation_data.title"), () => {
		const previous = relation.data;
		relation.setData(data);
		return setRelationLinkData(relation, previous);
	});
}
/**
* A group of multiple data graph relations represented by a diagram link.
*
* @category Core
*/
var RelationGroup = class RelationGroup extends Link {
	_typeId;
	_items;
	_itemKeys = new HashSet(hashLink, equalLinks);
	_sources = /* @__PURE__ */ new Set();
	_targets = /* @__PURE__ */ new Set();
	constructor(props) {
		super(props);
		this._typeId = props.typeId;
		this._items = props.items ?? [];
		this.updateItemKeys();
	}
	get relationSource() {
		return this.source;
	}
	getTypeId() {
		return this._typeId;
	}
	get items() {
		return this._items;
	}
	setItems(value) {
		for (const item of value) if (item.data.linkTypeId !== this._typeId) throw new Error("RelationGroup should have only items with same type IRI");
		const previous = this._items;
		if (previous === value) return;
		this._items = value;
		this.updateItemKeys();
		this.relationSource.trigger("changeItems", {
			source: this,
			previous
		});
		this.relationSource.trigger("requestedRedraw", { source: this });
	}
	get itemKeys() {
		return this._itemKeys;
	}
	get itemSources() {
		return this._sources;
	}
	get itemTargets() {
		return this._targets;
	}
	updateItemKeys() {
		this._itemKeys.clear();
		this._sources.clear();
		this._targets.clear();
		for (const item of this._items) {
			this._itemKeys.add(item.data);
			this._sources.add(item.data.sourceId);
			this._targets.add(item.data.targetId);
		}
	}
	static fromJSONType = "LinkGroup";
	static fromJSON(state, options) {
		const { "@id": id, property, vertices, linkState } = state;
		const { source, target } = options;
		const groupItems = [];
		for (const item of state.items) {
			const key = {
				linkTypeId: state.property,
				sourceId: item.sourceIri,
				targetId: item.targetIri,
				properties: {}
			};
			const initialData = options.getInitialData(key);
			groupItems.push({
				data: initialData ?? key,
				linkState: options.mapTemplateState(TemplateState.fromJSON(item.linkState))
			});
		}
		return new RelationGroup({
			id,
			typeId: property,
			sourceId: source.id,
			targetId: target.id,
			items: groupItems,
			vertices,
			linkState: options.mapTemplateState(TemplateState.fromJSON(linkState)).set(TemplateProperties.LayoutOnly, void 0)
		});
	}
	toJSON() {
		return {
			"@type": "LinkGroup",
			"@id": this.id,
			property: this.typeId,
			source: { "@id": this.sourceId },
			target: { "@id": this.targetId },
			items: this.items.map((item) => ({
				"@type": "LinkItem",
				sourceIri: item.data.sourceId,
				targetIri: item.data.targetId,
				linkState: item.linkState?.toJSON()
			})),
			vertices: [...this.vertices],
			linkState: this.linkState.toJSON()
		};
	}
};
/**
* Command to set {@link RelationGroup.items relation group items}.
*
* @category Commands
*/
function setRelationGroupItems(group, items) {
	return Command.create(TranslatedText.text("commands.set_relation_group_items.title"), () => {
		const before = group.items;
		group.setItems(items);
		return setRelationGroupItems(group, before);
	});
}
/**
* Iterates over data for all relations of the target link.
*
* @category Core
*/
function* iterateRelationsOf(link) {
	if (link instanceof RelationLink) yield link.data;
	else if (link instanceof RelationGroup) for (const item of link.items) yield item.data;
}
/**
* Stores data of an entity type in the graph.
*
* @category Core
*/
var ElementType = class {
	source = new EventSource();
	events = this.source;
	id;
	_data;
	constructor(props) {
		const { id, data } = props;
		this.id = id;
		this.setData(data);
	}
	get data() {
		return this._data;
	}
	setData(value) {
		if (value && value.id !== this.id) throw new Error("ElementTypeModel.id does not match ElementType.id");
		const previous = this._data;
		if (previous === value) return;
		this._data = value;
		this.source.trigger("changeData", {
			source: this,
			previous
		});
	}
};
/**
* Stores data of a property type in the graph.
*
* @category Core
*/
var PropertyType = class {
	source = new EventSource();
	events = this.source;
	id;
	_data;
	constructor(props) {
		const { id, data } = props;
		this.id = id;
		this.setData(data);
	}
	get data() {
		return this._data;
	}
	setData(value) {
		if (value && value.id !== this.id) throw new Error("PropertyTypeModel.id does not match PropertyType.id");
		const previous = this._data;
		if (previous === value) return;
		this._data = value;
		this.source.trigger("changeData", {
			source: this,
			previous
		});
	}
};
/**
* Stores data of a link type in the graph.
*
* @category Core
*/
var LinkType = class {
	source = new EventSource();
	events = this.source;
	id;
	_data;
	constructor(props) {
		const { id, data } = props;
		this.id = id;
		this.setData(data);
	}
	get data() {
		return this._data;
	}
	setData(value) {
		if (value && value.id !== this.id) throw new Error("LinkTypeModel.id does not match LinkType.id");
		const previous = this._data;
		if (previous === value) return;
		this._data = value;
		this.source.trigger("changeData", {
			source: this,
			previous
		});
	}
};
/**
* Command to replace {@link EntityElement.data data} for all entities with target IRI on the diagram.
*
* If IRI in the new `data` is different from the `target`, the relations
* connected to the entities will have their data changed as well to refer
* to the same entities by the new IRI.
*
* @category Commands
*/
function changeEntityData(model, target, data) {
	const command = Command.create(TranslatedText.text("commands.change_entity.title"), () => {
		const captured = new CapturedDataGraphState();
		const previousIri = target;
		const newIri = data.id;
		const updateLinksToReferByNewIri = (element) => {
			if (previousIri === newIri) return;
			for (const link of model.getElementLinks(element)) if (link instanceof RelationLink) {
				captured.captureRelation(link);
				link.setData(mapRelationEndpoint(link.data, previousIri, newIri));
			} else if (link instanceof RelationGroup) {
				if (link.itemSources.has(previousIri) || link.itemTargets.has(previousIri)) {
					captured.captureRelationGroup(link);
					const items = link.items.map((item) => ({
						...item,
						data: mapRelationEndpoint(item.data, previousIri, newIri)
					}));
					link.setItems(items);
				}
			}
		};
		for (const element of model.elements) if (element instanceof EntityElement) {
			if (element.iri === target) {
				captured.captureEntity(element);
				element.setData(data);
				updateLinksToReferByNewIri(element);
			}
		} else if (element instanceof EntityGroup) {
			if (element.itemIris.has(target)) {
				captured.captureEntityGroup(element);
				const nextItems = element.items.map((item) => item.data.id === target ? {
					...item,
					data
				} : item);
				element.setItems(nextItems);
				updateLinksToReferByNewIri(element);
			}
		}
		return Command.create(TranslatedText.text("commands.change_entity.title"), () => {
			captured.restore();
			return command;
		});
	});
	return command;
}
function mapRelationEndpoint(relation, oldIri, newIri) {
	let data = relation;
	if (data.sourceId === oldIri) data = {
		...data,
		sourceId: newIri
	};
	if (data.targetId === oldIri) data = {
		...data,
		targetId: newIri
	};
	return data;
}
/**
* Command to replace {@link RelationLink.data data} for all relations with same target identity.
*
* The relation identity should be the same for both `oldData` and `newData`
* otherwise an error wil be thrown.
*
* @category Commands
*/
function changeRelationData(model, oldData, newData) {
	if (!equalLinks(oldData, newData)) throw new Error("Cannot change typeId, sourceId or targetId when changing link data");
	const command = Command.create(TranslatedText.text("commands.change_relation.title"), () => {
		const captured = new CapturedDataGraphState();
		for (const link of model.links) if (link instanceof RelationLink && equalLinks(link.data, oldData)) {
			captured.captureRelation(link);
			link.setData(newData);
		} else if (link instanceof RelationGroup && link.itemKeys.has(oldData)) {
			captured.captureRelationGroup(link);
			const items = link.items.map((item) => ({
				...item,
				data: newData
			}));
			link.setItems(items);
		}
		return Command.create(TranslatedText.text("commands.change_entity.title"), () => {
			captured.restore();
			return command;
		});
	});
	return command;
}
var CapturedDataGraphState = class {
	data = /* @__PURE__ */ new Map();
	captureEntity(element) {
		this.data.set(element, element.data);
	}
	captureEntityGroup(element) {
		this.data.set(element, element.items);
	}
	captureRelation(link) {
		this.data.set(link, link.data);
	}
	captureRelationGroup(link) {
		this.data.set(link, link.items);
	}
	restore() {
		for (const [target, data] of this.data) if (target instanceof EntityElement) target.setData(data);
		else if (target instanceof EntityGroup) target.setItems(data);
		else if (target instanceof RelationLink) target.setData(data);
		else if (target instanceof RelationGroup) target.setItems(data);
	}
};
//#endregion
//#region src/editor/dataFetcher.ts
var DataFetcher = class {
	graph;
	dataGraph;
	dataProvider;
	source = new EventSource();
	events = this.source;
	cancellation = new AbortController();
	_operations = [];
	_failReasons = /* @__PURE__ */ new Map();
	elementTypeQueue = new BufferingQueue((classIds) => {
		const operation = {
			type: "elementType",
			targets: new Set(classIds)
		};
		const task = this.dataProvider.elementTypes({
			classIds,
			signal: this.signal
		}).then(this.onElementTypesLoaded);
		this.addOperation(operation, task);
	});
	linkTypeQueue = new BufferingQueue((linkTypeIds) => {
		const operation = {
			type: "linkType",
			targets: new Set(linkTypeIds)
		};
		const task = this.dataProvider.linkTypes({
			linkTypeIds,
			signal: this.signal
		}).then(this.onLinkTypesLoaded);
		this.addOperation(operation, task);
	});
	propertyTypeQueue = new BufferingQueue((propertyIds) => {
		const operation = {
			type: "propertyType",
			targets: new Set(propertyIds)
		};
		const task = this.dataProvider.propertyTypes({
			propertyIds,
			signal: this.signal
		}).then(this.onPropertyTypesLoaded);
		this.addOperation(operation, task);
	});
	constructor(graph, dataGraph, dataProvider) {
		this.graph = graph;
		this.dataGraph = dataGraph;
		this.dataProvider = dataProvider;
	}
	get signal() {
		return this.cancellation.signal;
	}
	dispose() {
		this.cancellation.abort();
	}
	get operations() {
		return this._operations;
	}
	getFailReason(type, target) {
		return this._failReasons.get(type)?.get(target);
	}
	addOperation(operation, task) {
		const previous = this._operations;
		const next = [...previous, operation];
		task.then(() => this.onOperationComplete(operation), (error) => this.onOperationComplete(operation, error));
		this._operations = next;
		this.source.trigger("changeOperations", { previous });
	}
	onOperationComplete(operation, error) {
		const previous = this._operations;
		const index = previous.indexOf(operation);
		if (index >= 0) {
			const next = [...previous];
			next.splice(index, 1);
			this._operations = next;
		}
		switch (operation.type) {
			case "element":
			case "elementType":
			case "linkType":
			case "propertyType": {
				const reasons = this.ensureFailReasons(operation.type);
				for (const target of operation.targets) reasons.set(target, error);
			}
		}
		if (this._operations !== previous || error) this.source.trigger("changeOperations", {
			previous,
			fail: error ? {
				operation,
				error
			} : void 0
		});
	}
	ensureFailReasons(type) {
		let reasons = this._failReasons.get(type);
		if (!reasons) {
			reasons = /* @__PURE__ */ new Map();
			this._failReasons.set(type, reasons);
		}
		return reasons;
	}
	fetchElementData(targets) {
		if (targets.size === 0) return Promise.resolve();
		const operation = {
			type: "element",
			targets
		};
		const task = this.dataProvider.elements({
			elementIds: Array.from(targets),
			signal: this.signal
		}).then((result) => this.onElementInfoLoaded(targets, result));
		this.addOperation(operation, task);
		return task;
	}
	onElementInfoLoaded(targets, elements) {
		for (const element of this.graph.getElements()) if (element instanceof EntityElement) {
			const loadedModel = elements.get(element.iri);
			if (loadedModel) element.setData(loadedModel);
			else if (targets.has(element.iri)) element.setData(unsetPlaceholder(element.data));
		} else if (element instanceof EntityGroup) {
			let hasLoadedModel = false;
			for (const item of element.items) if (targets.has(item.data.id)) hasLoadedModel = true;
			if (hasLoadedModel) {
				const loadedItems = element.items.map((item) => {
					const nextData = elements.get(item.data.id) ?? unsetPlaceholder(item.data);
					return nextData === item.data ? item : {
						...item,
						data: nextData
					};
				});
				element.setItems(loadedItems);
			}
		}
	}
	fetchLinks(primaryIris, secondaryIris, linkTypeIris) {
		const operation = { type: "link" };
		const task = this.dataProvider.links({
			primary: primaryIris,
			secondary: secondaryIris,
			linkTypeIds: linkTypeIris
		});
		this.addOperation(operation, task);
		return task;
	}
	fetchElementType(model) {
		this.elementTypeQueue.push(model.id);
	}
	onElementTypesLoaded = (elementTypes) => {
		for (const data of elementTypes.values()) {
			const model = this.dataGraph.getElementType(data.id);
			if (model) model.setData(data);
		}
	};
	fetchLinkType(linkType) {
		this.linkTypeQueue.push(linkType.id);
	}
	onLinkTypesLoaded = (linkTypes) => {
		for (const data of linkTypes.values()) {
			const model = this.dataGraph.getLinkType(data.id);
			if (model) model.setData(data);
		}
	};
	fetchPropertyType(propertyType) {
		this.propertyTypeQueue.push(propertyType.id);
	}
	onPropertyTypesLoaded = (propertyTypes) => {
		for (const data of propertyTypes.values()) {
			const model = this.dataGraph.getPropertyType(data.id);
			if (model) model.setData(data);
		}
	};
};
function unsetPlaceholder(data) {
	if (EntityElement.isPlaceholderData(data)) {
		const { [PlaceholderDataProperty]: _, ...restProperties } = data.properties;
		return {
			...data,
			properties: restProperties
		};
	}
	return data;
}
//#endregion
//#region src/editor/serializedDiagram.ts
/**
* Makes an empty serialized diagram state.
*/
function emptyDiagram() {
	return {
		"@context": DiagramContextV1,
		"@type": "Diagram",
		layoutData: {
			"@type": "Layout",
			elements: [],
			links: []
		},
		linkTypeOptions: []
	};
}
/**
* Exports diagram model state for [JSON-LD](https://json-ld.org/) compatible serialization.
*
* @see {@link deserializeDiagram}
*/
function serializeDiagram(diagram) {
	const { elements, links, linkTypeVisibility } = diagram;
	let linkTypeOptions;
	if (linkTypeVisibility) {
		linkTypeOptions = [];
		for (const [linkTypeIri, visibility] of linkTypeVisibility) if (visibility !== "visible" && linkTypeIri !== "urn:reactodia:newLink") linkTypeOptions.push({
			"@type": "LinkTypeOptions",
			property: linkTypeIri,
			visible: visibility !== "hidden",
			showLabel: visibility !== "withoutLabel"
		});
	}
	return {
		...emptyDiagram(),
		layoutData: serializeLayout(elements, links),
		linkTypeOptions
	};
}
function serializeLayout(modelElements, modelLinks) {
	const elements = [];
	for (const element of modelElements) if (hasToJSON(element)) {
		const state = element.toJSON();
		if (isValidSerializedState(state)) elements.push(state);
	}
	const links = [];
	for (const link of modelLinks) if (hasToJSON(link)) {
		const state = link.toJSON();
		if (isValidSerializedState(state)) links.push(state);
	}
	return {
		"@type": "Layout",
		elements,
		links
	};
}
/**
* Imports diagram model state from the serialized form.
*
* @see {@link serializeDiagram}
*/
function deserializeDiagram(diagram, options) {
	const { layoutData, linkTypeOptions } = diagram;
	const linkTypeVisibility = /* @__PURE__ */ new Map();
	if (linkTypeOptions) for (const setting of linkTypeOptions) {
		const { visible = true, showLabel = true } = setting;
		const linkTypeId = setting.property;
		const visibility = visible && showLabel ? "visible" : visible && !showLabel ? "withoutLabel" : "hidden";
		linkTypeVisibility.set(linkTypeId, visibility);
	}
	const { elements, links } = deserializeLayout(layoutData, options);
	return {
		elements,
		links,
		linkTypeVisibility
	};
}
function deserializeLayout(layout, options) {
	const { preloadedElements, preloadedLinks, markLinksAsLayoutOnly = false } = options;
	const typeToElement = /* @__PURE__ */ new Map();
	for (const elementCellType of options.elementCellTypes) typeToElement.set(elementCellType.fromJSONType, elementCellType);
	const elementOptions = {
		getInitialData: (iri) => preloadedElements?.get(iri),
		mapTemplateState: (state) => state
	};
	const typeToLink = /* @__PURE__ */ new Map();
	for (const linkCellType of options.linkCellTypes) typeToLink.set(linkCellType.fromJSONType, linkCellType);
	const getInitialLinkData = (key) => preloadedLinks?.get(key);
	const mapLinkTemplateState = (state) => markLayoutOnly(state, markLinksAsLayoutOnly);
	const elements = /* @__PURE__ */ new Map();
	const links = [];
	for (const layoutElement of layout.elements) {
		const elementClass = typeToElement.get(layoutElement["@type"]);
		if (elementClass) {
			const element = elementClass.fromJSON(layoutElement, elementOptions);
			if (element) elements.set(element.id, element);
		}
	}
	for (const layoutLink of layout.links) {
		const { source, target } = layoutLink;
		const sourceElement = elements.get(source["@id"]);
		const targetElement = elements.get(target["@id"]);
		if (!(sourceElement && targetElement)) continue;
		const linkClass = typeToLink.get(layoutLink["@type"]);
		if (linkClass) {
			const link = linkClass.fromJSON(layoutLink, {
				source: sourceElement,
				target: targetElement,
				getInitialData: getInitialLinkData,
				mapTemplateState: mapLinkTemplateState
			});
			if (link) links.push(link);
		}
	}
	return {
		elements: Array.from(elements.values()),
		links
	};
}
function markLayoutOnly(linkState, value) {
	return linkState.set(TemplateProperties.LayoutOnly, value ? true : void 0);
}
function hasToJSON(instance) {
	return Boolean(typeof instance.toJSON === "function");
}
function isValidSerializedState(state) {
	return typeof state === "object" && state && typeof state["@type"] === "string";
}
//#endregion
//#region src/editor/dataGraph.ts
var DataGraph = class {
	source = new EventSource();
	events = this.source;
	classesById = /* @__PURE__ */ new Map();
	propertiesById = /* @__PURE__ */ new Map();
	linkTypes = /* @__PURE__ */ new Map();
	getLinkType(linkTypeId) {
		return this.linkTypes.get(linkTypeId);
	}
	addLinkType(linkType) {
		if (this.getLinkType(linkType.id)) throw new Error(`Link type already exists: ${linkType.id}`);
		linkType.events.onAny(this.onLinkTypeEvent);
		this.linkTypes.set(linkType.id, linkType);
	}
	onLinkTypeEvent = (data) => {
		this.source.trigger("linkTypeEvent", { data });
	};
	getPropertyType(propertyId) {
		return this.propertiesById.get(propertyId);
	}
	addPropertyType(propertyType) {
		if (this.getPropertyType(propertyType.id)) throw new Error(`Property type already exists: ${propertyType.id}`);
		propertyType.events.onAny(this.onPropertyTypeEvent);
		this.propertiesById.set(propertyType.id, propertyType);
	}
	onPropertyTypeEvent = (data) => {
		this.source.trigger("propertyTypeEvent", { data });
	};
	getElementType(elementTypeId) {
		return this.classesById.get(elementTypeId);
	}
	getElementTypes() {
		const classes = [];
		this.classesById.forEach((richClass) => classes.push(richClass));
		return classes;
	}
	addElementType(elementType) {
		if (this.getElementType(elementType.id)) throw new Error(`Element type already exists: ${elementType.id}`);
		elementType.events.onAny(this.onElementTypeEvent);
		this.classesById.set(elementType.id, elementType);
	}
	onElementTypeEvent = (data) => {
		this.source.trigger("elementTypeEvent", { data });
	};
};
//#endregion
//#region src/editor/dataDiagramModel.ts
/**
* Asynchronously fetches and stores the entity diagram content:
* graph elements and links, as well as element, link and property types;
* maintains selection and the current language to display the data.
*
* Additionally, the diagram model provides the means to undo/redo commands
* via {@link DataDiagramModel.history history}.
*
* @category Core
*/
var DataDiagramModel = class extends DiagramModel {
	translation;
	dataGraph = new DataGraph();
	loadingScope;
	_dataProvider;
	_locale;
	fetcher;
	discardingTask = Promise.resolve();
	/** @hidden */
	constructor(options) {
		super(options);
		this.translation = options.translation;
		this._dataProvider = new EmptyDataProvider();
		this.fetcher = new DataFetcher(this.graph, this.dataGraph, this._dataProvider);
		this._locale = new DefaultDataLocaleProvider({
			model: this,
			translation: this.translation
		});
		this.subscribeGraph();
	}
	get extendedSource() {
		return this.source;
	}
	/**
	* Returns the data provider that is associated with the current diagram
	* via creating new or importing an existing layout.
	*
	* This provider is used to fetch entity graph data on-demand.
	*
	* By default, it is set to {@link EmptyDataProvider} instance without any graph data.
	*
	* @see {@link createNewDiagram}
	* @see {@link importLayout}
	*/
	get dataProvider() {
		return this._dataProvider;
	}
	getTermFactory() {
		return this._dataProvider.factory;
	}
	/**
	* Provides the methods to format the graph data according to the current language.
	*/
	get locale() {
		return this._locale;
	}
	/**
	* Returns an immutable snapshot of current fetch operations.
	*/
	get operations() {
		return this.fetcher.operations;
	}
	/**
	* Returns a reason (thrown error) why latest fetch operation for specific target
	* failed, if any; otherwise returns `undefined`.
	*/
	getOperationFailReason(type, target) {
		return this.fetcher.getFailReason(type, target);
	}
	resetGraph() {
		this.dataGraph = new DataGraph();
		super.resetGraph();
		this.loadingScope?.abort();
		this.fetcher.dispose();
	}
	subscribeGraph() {
		this.graphListener.listen(this.dataGraph.events, "elementTypeEvent", (e) => {
			this.extendedSource.trigger("elementTypeEvent", e);
		});
		this.graphListener.listen(this.dataGraph.events, "linkTypeEvent", (e) => {
			this.extendedSource.trigger("linkTypeEvent", e);
		});
		this.graphListener.listen(this.dataGraph.events, "propertyTypeEvent", (e) => {
			this.extendedSource.trigger("propertyTypeEvent", e);
		});
		super.subscribeGraph();
	}
	setDataProvider(dataProvider) {
		this._dataProvider = dataProvider;
		this.fetcher = new DataFetcher(this.graph, this.dataGraph, dataProvider);
		this.graphListener.listen(this.fetcher.events, "changeOperations", (e) => {
			this.extendedSource.trigger("changeOperations", e);
		});
	}
	/**
	* Clears up the diagram and associates a new data provider for it.
	*
	* This method discards all current diagram state (elements, links and other data)
	* and resets the command history.
	*
	* @see {@link importLayout}
	*/
	async createNewDiagram(params) {
		const { dataProvider, signal } = params;
		return this.importLayout({
			dataProvider,
			signal
		});
	}
	/**
	* Restores diagram content from previously exported state and associates
	* a new data provider for the diagram.
	*
	* This method discards all current diagram state (elements, links and other data)
	* and resets the command history.
	*
	* @see {@link createNewDiagram}
	* @see {@link exportLayout}
	*/
	async importLayout(params) {
		const { dataProvider, locale, diagram = emptyDiagram(), elementCellTypes = [
			EntityElement,
			EntityGroup,
			AnnotationElement
		], linkCellTypes = [
			RelationLink,
			RelationGroup,
			AnnotationLink
		], preloadedElements, preloadedLinks, validateLinks = false, hideUnusedLinkTypes = false, signal: parentSignal } = params;
		await this.discardingTask;
		this.resetGraph();
		this.setDataProvider(dataProvider);
		this._locale = locale ?? new DefaultDataLocaleProvider({
			model: this,
			translation: this.translation
		});
		const scope = new AbortController();
		this.loadingScope = scope;
		const onParentSignalAbort = () => scope.abort();
		this.extendedSource.trigger("loadingStart", { source: this });
		const signal = this.loadingScope.signal;
		try {
			parentSignal?.addEventListener("abort", onParentSignalAbort);
			signal.throwIfAborted();
			this.setLinkSettings(diagram.linkTypeOptions ?? []);
			this.createGraphElements({
				diagram,
				elementCellTypes,
				linkCellTypes,
				preloadedElements,
				preloadedLinks,
				markLinksAsLayoutOnly: validateLinks
			});
			if (hideUnusedLinkTypes) {
				const linkTypes = await this.dataProvider.knownLinkTypes({ signal });
				signal.throwIfAborted();
				const knownLinkTypes = this.initLinkTypes(linkTypes);
				this.hideUnusedLinkTypes(knownLinkTypes);
			}
			this.subscribeGraph();
			const elementIrisToRequestData = [];
			for (const element of this.graph.getElements()) for (const entity of iterateEntitiesOf(element)) if (!(preloadedElements && preloadedElements.has(entity.id))) elementIrisToRequestData.push(entity.id);
			const requestingModels = this.requestElementData(elementIrisToRequestData);
			const requestingLinks = params.validateLinks ? this.requestLinks() : Promise.resolve();
			await Promise.all([requestingModels, requestingLinks]);
			this.history.reset();
			this.extendedSource.trigger("loadingSuccess", { source: this });
		} catch (error) {
			this.extendedSource.trigger("loadingError", {
				source: this,
				error
			});
			throw new Error("Reactodia: failed to import a layout", { cause: error });
		} finally {
			parentSignal?.removeEventListener("abort", onParentSignalAbort);
			this.loadingScope?.abort();
			this.loadingScope = void 0;
		}
	}
	/**
	* Discards all diagram content and resets associated data provider to en empty one.
	*/
	discardLayout() {
		const previous = this.discardingTask;
		this.discardingTask = Promise.resolve().then(async () => {
			await previous;
			this.resetGraph();
			this.setDataProvider(new EmptyDataProvider());
			this.extendedSource.trigger("loadingStart", { source: this });
			this.subscribeGraph();
			this.history.reset();
			this.extendedSource.trigger("loadingSuccess", { source: this });
		}).catch((err) => {
			console.warn("Error while discarding a layout", err);
		});
	}
	/**
	* Exports current diagram state to a serializable object.
	*
	* The exported state includes element and link geometry, template state,
	* references to described entities and relations (via IRIs).
	* Additionally, link type visibility settings are exported as well.
	*
	* @see {@link importLayout}
	*/
	exportLayout() {
		const knownLinkTypes = new Set(this.graph.getLinks().filter((link) => link instanceof RelationLink || link instanceof RelationGroup).map((link) => link.typeId));
		const linkTypeVisibility = /* @__PURE__ */ new Map();
		for (const linkTypeIri of knownLinkTypes) linkTypeVisibility.set(linkTypeIri, this.getLinkVisibility(linkTypeIri));
		return serializeDiagram({
			elements: this.graph.getElements(),
			links: this.graph.getLinks(),
			linkTypeVisibility
		});
	}
	initLinkTypes(linkTypes) {
		const types = [];
		for (const data of linkTypes) {
			const linkType = new LinkType({
				id: data.id,
				data
			});
			this.dataGraph.addLinkType(linkType);
			types.push(linkType);
		}
		return types;
	}
	setLinkSettings(settings) {
		for (const setting of settings) {
			const { visible = true, showLabel = true } = setting;
			const linkTypeId = setting.property;
			const visibility = visible && showLabel ? "visible" : visible && !showLabel ? "withoutLabel" : "hidden";
			this.setLinkVisibility(linkTypeId, visibility);
		}
	}
	createGraphElements(params) {
		const { diagram, elementCellTypes, linkCellTypes, preloadedElements, preloadedLinks, markLinksAsLayoutOnly } = params;
		const { elements, links, linkTypeVisibility } = deserializeDiagram(diagram, {
			elementCellTypes,
			linkCellTypes,
			preloadedElements,
			preloadedLinks,
			markLinksAsLayoutOnly
		});
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.import_layout.command"));
		for (const [linkTypeIri, visibility] of linkTypeVisibility) this.setLinkVisibility(linkTypeIri, visibility);
		for (const link of links) if (link instanceof RelationLink || link instanceof RelationGroup) this.createLinkType(link.typeId);
		for (const element of elements) this.addElement(element);
		for (const link of links) this.addLink(link);
		batch.store();
	}
	hideUnusedLinkTypes(knownLinkTypes) {
		const usedTypes = /* @__PURE__ */ new Set();
		for (const link of this.graph.getLinks()) if (link instanceof RelationLink || link instanceof RelationGroup) usedTypes.add(link.typeId);
		for (const linkType of knownLinkTypes) if (!usedTypes.has(linkType.id)) this.setLinkVisibility(linkType.id, "hidden");
	}
	/**
	* Requests to load all {@link EntityElement entities} with
	* {@link EntityElement.isPlaceholderData placeholder data} and all
	* {@link RelationLink relations} connected to them.
	*
	* @see {@link DataDiagramModel.requestElementData}
	* @see {@link DataDiagramModel.requestLinks}
	*/
	async requestData() {
		const targets = /* @__PURE__ */ new Set();
		for (const element of this.elements) for (const entity of iterateEntitiesOf(element)) if (EntityElement.isPlaceholderData(entity)) targets.add(entity.id);
		const elements = Array.from(targets);
		await Promise.all([this.fetcher.fetchElementData(targets), this.requestLinks({ addedElements: elements })]);
	}
	/**
	* Requests to fetch the data for the specified elements from a data provider.
	*/
	requestElementData(elementIris) {
		return this.fetcher.fetchElementData(new Set(elementIris));
	}
	/**
	* Requests to fetch links between all elements on the diagram from a data provider.
	*/
	requestLinks(options = {}) {
		const { addedElements, linkTypes } = options;
		const primaryIris = [];
		for (const element of this.graph.getElements()) for (const entity of iterateEntitiesOf(element)) primaryIris.push(entity.id);
		const secondaryIris = addedElements ?? primaryIris;
		if (primaryIris.length === 0 || secondaryIris.length === 0) return Promise.resolve();
		return this.fetcher.fetchLinks(primaryIris, secondaryIris, linkTypes).then((links) => this.onLinkInfoLoaded(links));
	}
	/**
	* @deprecated Use {@link DataDiagramModel.requestLinks} instead.
	*/
	requestLinksOfType(linkTypeIds) {
		return this.requestLinks({ linkTypes: linkTypeIds });
	}
	/**
	* Creates or gets an existing entity element on the diagram.
	*
	* If element is specified as an IRI only, then the
	* {@link EntityElement.placeholderData placeholder data} will be used.
	*
	* If multiple entity elements with the same IRI is on the diagram,
	* the first one in the order will be returned.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	createElement(elementIriOrModel) {
		const elementIri = typeof elementIriOrModel === "string" ? elementIriOrModel : elementIriOrModel.id;
		const elements = this.elements.filter((el) => el instanceof EntityElement && el.iri === elementIri);
		if (elements.length > 0) return elements[0];
		let data = typeof elementIriOrModel === "string" ? EntityElement.placeholderData(elementIri) : elementIriOrModel;
		data = {
			...data,
			id: data.id
		};
		const element = new EntityElement({ data });
		this.addElement(element);
		return element;
	}
	onLinkInfoLoaded(links) {
		let allowToCreate;
		const cancel = () => {
			allowToCreate = false;
		};
		const batch = this.history.startBatch();
		for (const linkModel of links) {
			allowToCreate = true;
			this.extendedSource.trigger("createLoadedLink", {
				source: this,
				model: linkModel,
				cancel
			});
			if (allowToCreate) this.createLinks(linkModel);
		}
		batch.discard();
	}
	/**
	* Creates or gets an existing links for the specified link model.
	*
	* Multiple links may exists for the same link model because in some cases
	* there could be multiple source or target elements with the same IRI.
	*
	* Each existing link for the same link model will be updated with the specified data,
	* link state property `urn:reactodia:layoutOnly` ({@link TemplateProperties.LayoutOnly})
	* will be discarded if set.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	createLinks(data) {
		const sources = this.graph.getElements().filter((el) => el instanceof EntityElement && el.iri === data.sourceId || el instanceof EntityGroup && el.itemIris.has(data.sourceId));
		const targets = this.graph.getElements().filter((el) => el instanceof EntityElement && el.iri === data.targetId || el instanceof EntityGroup && el.itemIris.has(data.targetId));
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.create_links.command"));
		const links = [];
		for (const source of sources) for (const target of targets) {
			const link = this.createRelation(source, target, data);
			links.push(link);
		}
		batch.store();
		return links;
	}
	createRelation(source, target, data) {
		const existingLinks = Array.from(this.graph.iterateLinks(source.id, target.id, data.linkTypeId));
		for (const link of existingLinks) if (link instanceof RelationLink && equalLinks(link.data, data)) {
			this.history.execute(setLinkState(link, markLayoutOnly(link.linkState, false)));
			this.history.execute(setRelationLinkData(link, data));
			return link;
		} else if (link instanceof RelationGroup && link.itemKeys.has(data)) {
			const items = link.items.map((item) => equalLinks(item.data, data) ? {
				...item,
				data,
				linkState: markLayoutOnly(item.linkState ?? TemplateState.empty, false)
			} : item);
			this.history.execute(setRelationGroupItems(link, items));
			return link;
		}
		for (const link of existingLinks) if (link.typeId === link.typeId) {
			if (link instanceof RelationLink) {
				const items = [{
					data: link.data,
					linkState: link.linkState
				}, { data }];
				const group = new RelationGroup({
					sourceId: source.id,
					targetId: target.id,
					typeId: data.linkTypeId,
					items
				});
				this.removeLink(link.id);
				this.addLink(group);
				return group;
			}
			if (link instanceof RelationGroup) {
				const items = [...link.items, { data }];
				this.history.execute(setRelationGroupItems(link, items));
				return link;
			}
		}
		const link = new RelationLink({
			sourceId: source.id,
			targetId: target.id,
			data
		});
		this.addLink(link);
		return link;
	}
	getElementType(elementTypeIri) {
		return this.dataGraph.getElementType(elementTypeIri);
	}
	/**
	* Creates or gets an existing element type in the graph.
	*
	* If element type does not exists in the graph yet, it will be created
	* and the data for it will be requested for it from the data provider.
	*/
	createElementType(elementTypeIri) {
		const existing = this.dataGraph.getElementType(elementTypeIri);
		if (existing) return existing;
		const elementType = new ElementType({ id: elementTypeIri });
		this.dataGraph.addElementType(elementType);
		this.fetcher.fetchElementType(elementType);
		return elementType;
	}
	getLinkType(linkTypeIri) {
		return this.dataGraph.getLinkType(linkTypeIri);
	}
	/**
	* Creates or gets an existing link type in the graph.
	*
	* If link type does not exists in the graph yet, it will be created
	* and the data for it will be requested for it from the data provider.
	*/
	createLinkType(linkTypeIri) {
		const existing = this.dataGraph.getLinkType(linkTypeIri);
		if (existing) return existing;
		const linkType = new LinkType({ id: linkTypeIri });
		this.dataGraph.addLinkType(linkType);
		this.fetcher.fetchLinkType(linkType);
		return linkType;
	}
	getPropertyType(propertyTypeIri) {
		return this.dataGraph.getPropertyType(propertyTypeIri);
	}
	/**
	* Creates or gets an existing property type in the graph.
	*
	* If property type does not exists in the graph yet, it will be created
	* and the data for it will be requested for it from the data provider.
	*/
	createPropertyType(propertyIri) {
		const existing = this.dataGraph.getPropertyType(propertyIri);
		if (existing) return existing;
		const property = new PropertyType({ id: propertyIri });
		this.dataGraph.addPropertyType(property);
		this.fetcher.fetchPropertyType(property);
		return property;
	}
	/**
	* Groups multiple entity elements into an entity group element.
	*
	* Specified entity elements are removed from the diagram and
	* a single entity group element with these entities is created
	* at the center of the bounding box between them.
	*
	* Relation links from/to specified elements are re-grouped to
	* form relation group links the same way.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*
	* @see {@link ungroupAll}
	* @see {@link ungroupSome}
	*/
	group(entities) {
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.group_entities.command"));
		const entityIds = /* @__PURE__ */ new Set();
		for (const entity of entities) entityIds.add(entity.data.id);
		const items = [];
		const links = /* @__PURE__ */ new Set();
		for (const entity of entities) {
			items.push({
				data: entity.data,
				elementState: entity.elementState
			});
			for (const link of this.getElementLinks(entity)) links.add(link);
		}
		for (const link of links) this.removeLink(link.id);
		for (const entity of entities) this.removeElement(entity.id);
		const box = getContentFittingBox(entities, [], { getElementSize: () => void 0 });
		const group = new EntityGroup({
			items,
			position: Rect.center(box)
		});
		this.addElement(group);
		this.recreateLinks(links);
		batch.store();
		return group;
	}
	/**
	* Ungroups one or many entity group elements into all contained entity elements.
	*
	* Specified entity group elements are removed from the diagram and
	* all contained entity elements are created at the same position as
	* the owner group.
	*
	* Relation links from/to ungrouped entities are re-grouped to
	* form relation group links the same way.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*
	* @see {@link group}
	* @see {@link ungroupSome}
	*/
	ungroupAll(groups) {
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.ungroup_entities.command"));
		const ungrouped = [];
		const links = /* @__PURE__ */ new Set();
		for (const group of groups) {
			for (const link of this.getElementLinks(group)) links.add(link);
			this.removeElement(group.id);
			for (const item of group.items) {
				const entity = new EntityElement({
					data: item.data,
					position: group.position,
					elementState: item.elementState
				});
				this.addElement(entity);
				ungrouped.push(entity);
			}
		}
		this.recreateLinks(links);
		batch.store();
		return ungrouped;
	}
	/**
	* Ungroups some entities from an entity group element.
	*
	* Specified entity group is modified to remove target entities
	* and re-create them at the same position as the group.
	* If only one or less entities are left in the group,
	* the group will be completely ungrouped instead.
	*
	* Relation links from/to ungrouped entities are re-grouped to
	* form relation group links the same way.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*
	* @see {@link group}
	* @see {@link ungroupAll}
	*/
	ungroupSome(group, entities) {
		const leftGrouped = group.items.filter((item) => !entities.has(item.data.id));
		if (leftGrouped.length <= 1) return this.ungroupAll([group]);
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.ungroup_entities.command"));
		const links = /* @__PURE__ */ new Set();
		for (const link of this.getElementLinks(group)) if (link instanceof RelationLink) {
			if (entities.has(link.data.sourceId) || entities.has(link.data.targetId)) links.add(link);
		} else if (link instanceof RelationGroup) {
			if (link.items.some((item) => entities.has(item.data.sourceId) || entities.has(item.data.targetId))) links.add(link);
		}
		for (const link of links) this.removeLink(link.id);
		const ungroupedElements = [];
		for (const item of group.items) if (entities.has(item.data.id)) {
			const entity = new EntityElement({
				data: item.data,
				position: group.position,
				elementState: item.elementState
			});
			this.addElement(entity);
			ungroupedElements.push(entity);
		}
		batch.history.execute(setEntityGroupItems(group, leftGrouped));
		this.recreateLinks(links);
		batch.store();
		return ungroupedElements;
	}
	/**
	* Re-creates a set of relations or relation groups to automatically
	* group relations with the same link type connected to entity groups.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	regroupLinks(links) {
		const batch = this.history.startBatch(TranslatedText.text("data_diagram_model.regroup_relations.command"));
		for (const link of links) this.removeLink(link.id);
		this.recreateLinks(new Set(links));
		batch.store();
	}
	recreateLinks(links) {
		for (const link of links) if (link instanceof RelationLink) {
			for (const created of this.createLinks(link.data)) if (created instanceof RelationLink) this.history.execute(setLinkState(created, link.linkState));
		} else if (link instanceof RelationGroup) for (const { data } of link.items) this.createLinks(data);
	}
};
/**
* Command effect to request data for the specified entity elements on the diagram
* from a data provider.
* 
* @category Commands
* @see {@link DataDiagramModel.requestElementData}
*/
function requestElementData(model, elementIris) {
	return Command.effect(TranslatedText.text("data_diagram_model.request_entities.command"), () => void model.requestElementData(elementIris));
}
/**
* Command effect to request links between elements on the diagram
* from a data provider.
*
* @category Commands
* @see {@link DataDiagramModel.requestLinks}
*/
function restoreLinksBetweenElements(model, options = {}) {
	return Command.effect(TranslatedText.text("data_diagram_model.request_relations.command"), () => void model.requestLinks(options));
}
function getAllPresentEntities(graph) {
	const presentOnDiagram = /* @__PURE__ */ new Set();
	for (const element of graph.elements) for (const entity of iterateEntitiesOf(element)) presentOnDiagram.add(entity.id);
	return presentOnDiagram;
}
//#endregion
//#region src/editor/validation.ts
var ValidationState;
(function(_ValidationState) {
	_ValidationState.empty = createMutable();
	_ValidationState.emptyElement = {
		loading: false,
		items: []
	};
	_ValidationState.emptyLink = {
		loading: false,
		items: []
	};
	function createMutable() {
		return {
			elements: /* @__PURE__ */ new Map(),
			links: new HashMap(hashLink, equalLinks)
		};
	}
	_ValidationState.createMutable = createMutable;
	function setElementItems(state, target, items) {
		const elements = new Map(state.elements);
		if (items.length > 0) elements.set(target, {
			loading: false,
			items
		});
		else elements.delete(target);
		return {
			...state,
			elements
		};
	}
	_ValidationState.setElementItems = setElementItems;
	function setLinkItems(state, target, items) {
		const links = state.links.clone();
		if (items.length > 0) links.set(target, {
			loading: false,
			items
		});
		else links.delete(target);
		return {
			...state,
			links
		};
	}
	_ValidationState.setLinkItems = setLinkItems;
})(ValidationState || (ValidationState = {}));
function changedElementsToValidate(previousAuthoring, currentAuthoring, graph) {
	const links = new HashMap(hashLink, equalLinks);
	previousAuthoring.links.forEach((e, model) => links.set(model, true));
	currentAuthoring.links.forEach((e, model) => links.set(model, true));
	const toValidate = /* @__PURE__ */ new Set();
	links.forEach((value, linkModel) => {
		if (currentAuthoring.links.get(linkModel) !== previousAuthoring.links.get(linkModel)) toValidate.add(linkModel.sourceId);
	});
	for (const element of graph.elements) for (const entity of iterateEntitiesOf(element)) {
		const current = currentAuthoring.elements.get(entity.id);
		const previous = previousAuthoring.elements.get(entity.id);
		if (current !== previous) {
			toValidate.add(entity.id);
			if ((current || previous).type === "entityDelete") {
				for (const link of graph.getElementLinks(element)) for (const relation of iterateRelationsOf(link)) if (relation.targetId === entity.id && relation.sourceId !== entity.id) toValidate.add(relation.sourceId);
			}
		}
	}
	return toValidate;
}
function validateElements(targets, validationProvider, graph, editor, translation, language, signal) {
	const previousState = editor.validationState;
	const newState = ValidationState.createMutable();
	for (const element of graph.elements) for (const entity of iterateEntitiesOf(element)) {
		if (newState.elements.has(entity.id)) continue;
		const outboundLinks = [];
		for (const link of graph.getElementLinks(element)) for (const relation of iterateRelationsOf(link)) if (relation.sourceId === entity.id) outboundLinks.push(relation);
		if (targets.has(entity.id)) {
			const event = {
				target: entity,
				outboundLinks,
				state: editor.authoringState,
				graph,
				translation,
				language,
				signal
			};
			const result = mapAbortedToNull(validationProvider.validate(event), signal);
			const loadingElement = {
				loading: true,
				items: []
			};
			const loadingLink = {
				loading: true,
				items: []
			};
			newState.elements.set(entity.id, loadingElement);
			outboundLinks.forEach((link) => newState.links.set(link, loadingLink));
			processValidationResult(result, loadingElement, loadingLink, event, editor, translation);
		} else {
			newState.elements.set(entity.id, previousState.elements.get(entity.id));
			for (const link of outboundLinks) newState.links.set(link, previousState.links.get(link));
		}
	}
	editor.setValidationState(newState);
}
async function processValidationResult(resultTask, previousElement, previousLink, e, editor, translation) {
	let result;
	try {
		result = await resultTask;
		if (result === null) return;
	} catch (err) {
		result = { items: [{
			type: "element",
			target: e.target.id,
			severity: "error",
			message: translation.text("editor_controller.validate_entity_failed.message"),
			errorCause: err
		}] };
	}
	const elementItems = [];
	const linkItems = new HashMap(hashLink, equalLinks);
	e.outboundLinks.forEach((link) => linkItems.set(link, []));
	for (const item of result.items) if (item.type === "element" && item.target === e.target.id) elementItems.push(item);
	else if (item.type === "link" && linkItems.has(item.target)) linkItems.get(item.target).push(item);
	let state = editor.validationState;
	if (state.elements.get(e.target.id) === previousElement) state = ValidationState.setElementItems(state, e.target.id, elementItems);
	linkItems.forEach((items, link) => {
		if (state.links.get(link) === previousLink) state = ValidationState.setLinkItems(state, link, items);
	});
	editor.setValidationState(state);
}
var SEVERITY_INDEX = new Map([
	["info", 0],
	["warning", 1],
	["error", 2]
]);
function getMaxSeverity(items) {
	let maxSeverity = "info";
	let maxSeverityIndex = SEVERITY_INDEX.get(maxSeverity) ?? -1;
	for (const { severity } of items) {
		const index = SEVERITY_INDEX.get(severity) ?? -1;
		if (index > maxSeverityIndex) {
			maxSeverityIndex = index;
			maxSeverity = severity;
		}
	}
	return maxSeverity;
}
//#endregion
//#region src/editor/editorController.tsx
/**
* Stores, modifies and validates changes from the visual graph authoring
* (added, deleted or changed graph entities and/or relations).
*
* @category Core
*/
var EditorController = class {
	source = new EventSource();
	/**
	* Events for the editor controller.
	*/
	events = this.source;
	model;
	translation;
	getDisposeSignal;
	_inAuthoringMode = false;
	_metadataProvider;
	_validationProvider;
	_authoringState = AuthoringState.empty;
	_validationState = ValidationState.empty;
	_temporaryState = TemporaryState.empty;
	/** @hidden */
	constructor(props) {
		const { model, translation, metadataProvider, validationProvider, getDisposeSignal } = props;
		this.model = model;
		this.translation = translation;
		this.getDisposeSignal = getDisposeSignal;
		this._metadataProvider = metadataProvider;
		this._validationProvider = validationProvider;
		const listener = new EventObserver();
		listener.listen(this.events, "changeValidationState", (e) => {
			for (const element of this.model.elements) {
				if (!(element instanceof EntityElement)) continue;
				const previous = e.previous.elements.get(element.iri);
				if (this.validationState.elements.get(element.iri) !== previous) element.redraw();
			}
		});
		listener.listen(this.events, "changeAuthoringState", (e) => {
			this.validateChangedFrom(e.previous);
		});
		listener.listen(model.events, "changeCells", (e) => {
			this.validateChangedCells(e);
		});
	}
	/**
	* Returns `true` if the editor is in the graph authoring mode;
	* otherwise `false`.
	*/
	get inAuthoringMode() {
		return this._inAuthoringMode && Boolean(this._metadataProvider);
	}
	/**
	* Toggles the graph authoring mode for the editor.
	*
	* Does nothing if {@link metadataProvider} is not set.
	*/
	setAuthoringMode(value) {
		const previous = this._inAuthoringMode;
		const next = value && Boolean(this._metadataProvider);
		if (next === previous) return;
		this._inAuthoringMode = next;
		this.source.trigger("changeMode", {
			source: this,
			previous
		});
	}
	/**
	* Provides strategy for the graph authoring mode.
	*/
	get metadataProvider() {
		return this._metadataProvider;
	}
	/**
	* Provides strategy to validate data changes from the graph authoring.
	*/
	get validationProvider() {
		return this._validationProvider;
	}
	/**
	* Graph authoring state snapshot.
	*/
	get authoringState() {
		return this._authoringState;
	}
	/**
	* Sets graph authoring state.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	setAuthoringState(value) {
		if (this._authoringState === value) return;
		this.model.history.execute(this.updateAuthoringState(value));
	}
	updateAuthoringState(state) {
		const previous = this._authoringState;
		return Command.create(TranslatedText.text("editor_controller.set_authoring_state.command"), () => {
			this._authoringState = state;
			this.source.trigger("changeAuthoringState", {
				source: this,
				previous
			});
			return this.updateAuthoringState(previous);
		});
	}
	/**
	* Validation state snapshot for the data changes from the graph authoring.
	*/
	get validationState() {
		return this._validationState;
	}
	/**
	* Sets validation state for the data changes from the graph authoring.
	*/
	setValidationState(value) {
		const previous = this._validationState;
		if (value === previous) return;
		this._validationState = value;
		this.source.trigger("changeValidationState", {
			source: this,
			previous
		});
	}
	/**
	* Temporary (transient) state for the graph authoring.
	*/
	get temporaryState() {
		return this._temporaryState;
	}
	/**
	* Sets temporary (transient) state for the graph authoring.
	*/
	setTemporaryState(value) {
		const previous = this._temporaryState;
		if (value === previous) return;
		this._temporaryState = value;
		this.source.trigger("changeTemporaryState", {
			source: this,
			previous
		});
	}
	validateChangedFrom(previous) {
		if (!this.validationProvider) return;
		const changedElements = changedElementsToValidate(previous, this.authoringState, this.model);
		this.revalidateEntities(changedElements);
	}
	validateChangedCells(e) {
		if (!(this.validationProvider && this.inAuthoringMode)) return;
		if (e.updateAll) {
			this.validateChangedFrom(AuthoringState.empty);
			return;
		}
		const state = this.authoringState;
		let toRevalidate;
		if (e.changedElement) {
			for (const entity of iterateEntitiesOf(e.changedElement)) if (state.elements.has(entity.id)) {
				if (!toRevalidate) toRevalidate = /* @__PURE__ */ new Set();
				toRevalidate.add(entity.id);
			}
		} else if (e.changedLinks) {
			for (const link of e.changedLinks) for (const relation of iterateRelationsOf(link)) if (state.links.has(relation)) {
				if (!toRevalidate) toRevalidate = /* @__PURE__ */ new Set();
				toRevalidate.add(relation.sourceId);
			}
		}
		if (toRevalidate) this.revalidateEntities(toRevalidate);
	}
	/**
	* Forces re-validation for the specified entities.
	*/
	revalidateEntities(entities) {
		if (!this.validationProvider) return;
		validateElements(entities, this.validationProvider, this.model, this, this.translation, this.model.language, this.getDisposeSignal());
	}
	/**
	* Removes all selected diagram elements from the diagram
	* and discards any associated graph authoring state.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	removeSelectedElements() {
		const itemsToRemove = this.model.selection;
		if (itemsToRemove.length > 0) {
			this.model.setSelection([]);
			this.removeItems(itemsToRemove);
		}
	}
	/**
	* Removes the specified diagram cells from the diagram
	* and discards any associated graph authoring state.
	*
	* If the link is a {@link RelationLink relation}, it will be removed
	* only if it's marked as "added" in the graph
	* {@link EditorController.authoringState authoring state}.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	removeItems(items) {
		const { model } = this;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.remove_items.command"));
		const entitiesToDiscard = /* @__PURE__ */ new Set();
		for (const item of items) if (item instanceof Element) {
			for (const entity of iterateEntitiesOf(item)) entitiesToDiscard.add(entity.id);
			this.model.removeElement(item.id);
		} else if (item instanceof Link) {
			for (const relation of iterateRelationsOf(item)) if (AuthoringState.isAddedRelation(this.authoringState, relation)) this.deleteRelation(relation);
			if (!(item instanceof RelationLink || item instanceof RelationGroup)) this.model.removeLink(item.id);
		}
		const discardedEntities = /* @__PURE__ */ new Set();
		for (const entityIri of entitiesToDiscard) {
			const event = this.authoringState.elements.get(entityIri);
			if (event) this.discardChange(event);
			discardedEntities.add(entityIri);
		}
		if (discardedEntities.size > 0) {
			const newState = AuthoringState.discardAddedRelations(this.authoringState, discardedEntities);
			this.setAuthoringState(newState);
		}
		batch.store();
	}
	/**
	* Creates a new entity with graph authoring.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	createEntity(data, options = {}) {
		const { model } = this;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.entity_add.command"));
		const element = model.createElement(data);
		if (options.temporary) {
			this.setTemporaryState(TemporaryState.addEntity(this.temporaryState, element.data));
			batch.discard();
		} else {
			this.setAuthoringState(AuthoringState.addEntity(this._authoringState, element.data));
			batch.store();
		}
		return element;
	}
	/**
	* Changes an existing entity with graph authoring.
	*
	* If `target` is specified as an IRI and no entities with that IRI found on the diagram,
	* no changes will be applied to the graph authoring state.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	changeEntity(target, newData) {
		const { model } = this;
		const oldData = typeof target === "string" ? findAnyEntityData(findEntities(model, target), target) : target;
		if (!oldData) return;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.entity_change.command"));
		const newState = AuthoringState.changeEntity(this._authoringState, oldData, newData);
		const event = newState.elements.get(oldData.id) || newState.elements.get(newData.id);
		model.history.execute(changeEntityData(model, oldData.id, event.data));
		this.setAuthoringState(newState);
		batch.store();
	}
	/**
	* Deletes an existing entity with graph authoring.
	*
	* If `target` is specified as an IRI and no entities with that IRI found on the diagram,
	* no changes will be applied to the graph authoring state.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	deleteEntity(target) {
		const { model } = this;
		const oldData = typeof target === "string" ? findAnyEntityData(findEntities(model, target), target) : target;
		if (!oldData) return;
		const state = this.authoringState;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.entity_delete.command"));
		const event = state.elements.get(oldData.id);
		if (event) this.discardChange(event);
		for (const event of state.links.values()) if (event.data.sourceId === oldData.id || event.data.targetId === oldData.id) this.discardChange(event);
		this.setAuthoringState(AuthoringState.deleteEntity(state, oldData));
		batch.store();
	}
	/**
	* Creates a new relation with graph authoring.
	*
	* An error will be thrown if the relation with same identity
	* already exists on the diagram.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	createRelation(base, options = {}) {
		const { model } = this;
		if (model.findLink(base.typeId, base.sourceId, base.targetId)) throw Error("The relation with same (source IRI, target IRI, type) already exists");
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.relation_add.command"));
		model.addLink(base);
		if (!options.temporary) this.model.createLinks(base.data);
		if (hasRelationOnDiagram(model, base.data)) if (options.temporary) {
			this.setTemporaryState(TemporaryState.addRelation(this.temporaryState, base.data));
			batch.discard();
		} else {
			this.setAuthoringState(AuthoringState.addRelation(this._authoringState, base.data));
			batch.store();
		}
		else batch.discard();
		return base;
	}
	/**
	* Changes an existing relation with graph authoring.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	changeRelation(oldData, newData) {
		const { model } = this;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.relation_change.command"));
		if (equalLinks(oldData, newData)) {
			model.history.execute(changeRelationData(model, oldData, newData));
			this.setAuthoringState(AuthoringState.changeRelation(this._authoringState, oldData, newData));
		} else {
			let newState = this._authoringState;
			newState = AuthoringState.deleteRelation(newState, oldData);
			newState = AuthoringState.addRelation(newState, newData);
			if (AuthoringState.isAddedRelation(this._authoringState, oldData)) this.removeRelationsFromLinks(model.links, (relation) => equalLinks(relation, oldData));
			model.createLinks(newData);
			this.setAuthoringState(newState);
		}
		batch.store();
	}
	/**
	* Changes an existing relation with graph authoring
	* by moving its source to another entity element.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	moveRelationSource(params) {
		const { model } = this;
		const { link, newSource } = params;
		if (link.data.sourceId === newSource.data.id) return link;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.relation_move_source.command"));
		this.changeRelation(link.data, {
			...link.data,
			sourceId: newSource.iri
		});
		const newLink = model.findLink(link.typeId, newSource.id, link.targetId);
		newLink.setVertices(link.vertices);
		batch.store();
		return newLink;
	}
	/**
	* Changes an existing relation with graph authoring
	* by moving its target to another entity element.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	moveRelationTarget(params) {
		const { model } = this;
		const { link, newTarget } = params;
		if (link.data.targetId === newTarget.data.id) return link;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.relation_move_target.command"));
		this.changeRelation(link.data, {
			...link.data,
			targetId: newTarget.iri
		});
		const newLink = model.findLink(link.typeId, link.sourceId, newTarget.id);
		newLink.setVertices(link.vertices);
		batch.store();
		return newLink;
	}
	/**
	* Deletes an existing relation with graph authoring.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	deleteRelation(data) {
		const { model } = this;
		const state = this.authoringState;
		if (AuthoringState.isDeletedRelation(state, data)) return;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.relation_delete.command"));
		const newState = AuthoringState.deleteRelation(state, data);
		if (AuthoringState.isAddedRelation(state, data)) this.removeRelationsFromLinks(model.links, (relation) => equalLinks(relation, data));
		this.setAuthoringState(newState);
		batch.store();
	}
	/**
	* Removes all diagram cells from the temporary state for the graph authoring.
	*
	* @see {@link temporaryState}
	*/
	removeAllTemporaryCells() {
		const { temporaryState } = this;
		const cellsToRemove = [];
		if (temporaryState.elements.size > 0) {
			for (const element of this.model.elements) if (element instanceof EntityElement && temporaryState.elements.has(element.iri)) cellsToRemove.push(element);
		}
		if (temporaryState.links.size > 0) {
			for (const link of this.model.links) if (link instanceof RelationLink && temporaryState.links.has(link.data)) cellsToRemove.push(link);
		}
		if (cellsToRemove.length > 0) this.removeTemporaryCells(cellsToRemove);
	}
	/**
	* Removes the specified diagram cells from the temporary state
	* for the graph authoring.
	*
	* @see {@link temporaryState}
	*/
	removeTemporaryCells(cells) {
		const { temporaryState } = this;
		let nextTemporaryState = temporaryState;
		const batch = this.model.history.startBatch();
		for (const cell of cells) if (cell instanceof EntityElement) {
			if (temporaryState.elements.has(cell.iri)) {
				nextTemporaryState = TemporaryState.removeEntity(nextTemporaryState, cell.data);
				this.model.removeElement(cell.id);
			}
		} else if (cell instanceof RelationLink) {
			if (temporaryState.links.has(cell.data)) {
				nextTemporaryState = TemporaryState.removeRelation(nextTemporaryState, cell.data);
				this.model.removeLink(cell.id);
			}
		}
		batch.discard();
		this.setTemporaryState(nextTemporaryState);
	}
	/**
	* Discards the specified graph authoring event from the state
	* while reverting associated changes to the diagram:
	*   - new entities and links are removed;
	*   - changed entities and links have their data reverted back.
	*/
	discardChange(event) {
		const { model } = this;
		const newState = AuthoringState.discard(this._authoringState, event);
		if (newState === this._authoringState) return;
		const batch = model.history.startBatch(TranslatedText.text("editor_controller.discard_change.command"));
		switch (event.type) {
			case "entityAdd":
				for (const element of findEntities(model, event.data.id)) if (element instanceof EntityElement) model.removeElement(element.id);
				else if (element instanceof EntityGroup) this.discardItemFromGroup(element, event.data.id);
				break;
			case "entityChange":
				model.history.execute(changeEntityData(model, event.data.id, event.before));
				break;
			case "relationAdd":
				this.removeRelationsFromLinks(model.links, (relation) => equalLinks(relation, event.data));
				break;
			case "relationChange":
				model.history.execute(changeRelationData(this.model, event.data, event.before));
				break;
		}
		this.setAuthoringState(newState);
		batch.store();
	}
	discardItemFromGroup(group, target) {
		const nextItems = group.items.filter((item) => item.data.id !== target);
		this.model.history.execute(setEntityGroupItems(group, nextItems));
		this.removeRelationsFromLinks(this.model.getElementLinks(group), ({ sourceId, targetId }) => sourceId === target || targetId === target);
		if (nextItems.length <= 1) this.model.ungroupAll([group]);
	}
	removeRelationsFromLinks(relations, match) {
		const toRemove = [];
		const toRegroup = [];
		for (const link of relations) if (link instanceof RelationLink) {
			if (match(link.data)) toRemove.push(link);
		} else if (link instanceof RelationGroup) {
			if (link.items.some((item) => match(item.data))) {
				const items = link.items.filter((item) => !match(item.data));
				this.model.history.execute(setRelationGroupItems(link, items));
				toRegroup.push(link);
			}
		}
		for (const link of toRemove) this.model.removeLink(link.id);
		this.model.regroupLinks(toRegroup);
	}
	/**
	* Applies changes from {@link authoringState} (including link deletion and entity IRI changes)
	* to the diagram and resets the authoring state to {@link AuthoringState.empty}.
	*
	* The operation puts a command to the {@link DiagramModel.history command history}.
	*/
	applyAuthoringChanges() {
		const state = this.authoringState;
		const batch = this.model.history.startBatch(TranslatedText.text("editor_controller.apply_authored_changes.command"));
		for (const event of state.links.values()) if (event.type === "relationChange") batch.history.execute(changeRelationData(this.model, event.before, event.data));
		const removedLinks = [];
		for (const link of this.model.links) if (link instanceof RelationLink) {
			if (AuthoringState.isDeletedRelation(state, link.data)) removedLinks.push(link);
		} else if (link instanceof RelationGroup) {
			if (link.items.some((item) => AuthoringState.isDeletedRelation(state, item.data))) this.model.history.execute(setRelationGroupItems(link, link.items.filter((item) => !AuthoringState.isDeletedRelation(state, item.data))));
		}
		for (const link of removedLinks) this.model.removeLink(link.id);
		for (const event of state.elements.values()) if (event.type === "entityChange") this.model.history.execute(changeEntityData(this.model, event.before.id, event.newIri === void 0 ? event.data : {
			...event.data,
			id: event.newIri
		}));
		const removedElements = [];
		for (const element of this.model.elements) if (element instanceof EntityElement) {
			if (AuthoringState.isDeletedEntity(state, element.iri)) removedElements.push(element);
		} else if (element instanceof EntityGroup) {
			if (element.items.some((item) => AuthoringState.isDeletedEntity(state, item.data.id))) batch.history.execute(setEntityGroupItems(element, element.items.filter((item) => !AuthoringState.isDeletedEntity(state, item.data.id))));
		}
		for (const element of removedElements) this.model.removeElement(element.id);
		this.setAuthoringState(AuthoringState.empty);
		batch.store();
	}
};
function findEntities(graph, iri) {
	return graph.elements.filter((el) => el instanceof EntityElement && el.iri === iri || el instanceof EntityGroup && el.itemIris.has(iri));
}
function findAnyEntityData(entities, target) {
	for (const element of entities) for (const data of iterateEntitiesOf(element)) if (data.id === target) return data;
}
function hasRelationOnDiagram(graph, target) {
	for (const link of graph.links) for (const relation of iterateRelationsOf(link)) if (equalLinks(relation, target)) return true;
	return false;
}
//#endregion
//#region src/editor/elementGrouping.ts
/**
* Groups **with animation** multiple {@link EntityElement entity elements} into
* an {@link EntityGroup entity group}.
*
* The grouping operation is performed with {@link DataDiagramModel.group} method.
*
* The operation puts a command to the {@link DiagramModel.history command history}.
*
* @see {@link ungroupAllEntities}
* @see {@link ungroupSomeEntities}
*/
async function groupEntities(workspace, params) {
	const { model } = workspace;
	const { elements, canvas } = params;
	const batch = model.history.startBatch(TranslatedText.text("workspace.group_entities.command"));
	const capturedGeometry = RestoreGeometry.capturePartial(elements, []);
	const fittingBox = getContentFittingBox(elements, [], canvas.renderingState);
	const groupCenter = Rect.center(fittingBox);
	await canvas.animateGraph(() => {
		for (const element of elements) {
			const bounds = boundsOf(element, canvas.renderingState);
			const atCenter = {
				x: groupCenter.x - bounds.width / 2,
				y: groupCenter.y - bounds.height / 2
			};
			const normal = Vector.normalize(Vector.subtract(element.position, groupCenter));
			element.setPosition(Vector.add(atCenter, Vector.scale(normal, Math.min(bounds.width, bounds.height) / 2)));
		}
	});
	batch.history.registerToUndo(capturedGeometry);
	const sortedEntities = [...elements].sort((a, b) => {
		const aLabel = model.locale.formatEntityLabel(a.data, model.language);
		const bLabel = model.locale.formatEntityLabel(b.data, model.language);
		return aLabel.localeCompare(bLabel);
	});
	const group = model.group(sortedEntities);
	canvas.renderingState.syncUpdate();
	const bounds = boundsOf(group, canvas.renderingState);
	group.setPosition({
		x: groupCenter.x - bounds.width / 2,
		y: groupCenter.y - bounds.height / 2
	});
	batch.store();
	return group;
}
/**
* Ungroups **with animation** one or many {@link EntityGroup entity groups} into
* all contained {@link EntityElement entity elements}.
*
* The ungrouping operation is performed with {@link DataDiagramModel.ungroupAll} method.
*
* The operation puts a command to the {@link DiagramModel.history command history}.
*
* @see {@link groupEntities}
* @see {@link ungroupSomeEntities}
*/
async function ungroupAllEntities(workspace, params) {
	const { model, performLayout } = workspace;
	const { groups, canvas } = params;
	const batch = model.history.startBatch(TranslatedText.text("workspace.ungroup_entities.command"));
	const ungrouped = model.ungroupAll(groups);
	await performLayout({
		canvas,
		selectedElements: new Set(ungrouped),
		animate: true,
		zoomToFit: false
	});
	batch.store();
	return ungrouped;
}
/**
* Ungroups **with animation** some {@link EntityElement entity elements} from
* an {@link EntityGroup entity group}.
*
* The ungrouping operation is performed with {@link DataDiagramModel.ungroupSome} method.
*
* The operation puts a command to the {@link DiagramModel.history command history}.
*
* @see {@link groupEntities}
* @see {@link ungroupAllEntities}
*/
async function ungroupSomeEntities(workspace, params) {
	const { model } = workspace;
	const { group, entities, canvas } = params;
	const batch = model.history.startBatch(TranslatedText.text("workspace.ungroup_entities.command"));
	const ungrouped = model.ungroupSome(group, entities);
	canvas.renderingState.syncUpdate();
	await canvas.animateGraph(() => {
		batch.history.execute(placeElementsAroundTarget({
			target: group,
			elements: ungrouped.filter((element) => entities.has(element.data.id)),
			graph: model,
			sizeProvider: canvas.renderingState
		}));
	});
	batch.store();
	return ungrouped;
}
//#endregion
//#region src/coreUtils/dom.ts
function findNextWithin(from, parent, condition) {
	let current = from;
	let allowDescent = true;
	do {
		while (true) if (allowDescent && current.firstElementChild) {
			current = current.firstElementChild;
			break;
		} else if (current.nextElementSibling) {
			current = current.nextElementSibling;
			allowDescent = true;
			break;
		} else if (current.parentElement === parent) if (parent.firstElementChild) {
			current = parent.firstElementChild;
			allowDescent = true;
			break;
		} else return;
		else if (current.parentElement) {
			current = current.parentElement;
			allowDescent = false;
		} else return;
		if (condition(current)) return current;
	} while (current !== from);
}
function findPreviousWithin(from, parent, condition) {
	let current = from;
	let descent = false;
	do {
		while (true) if (descent) if (current.lastElementChild) current = current.lastElementChild;
		else {
			descent = false;
			break;
		}
		else if (current.previousElementSibling) {
			current = current.previousElementSibling;
			descent = true;
		} else if (current.parentElement === parent) if (parent.lastElementChild) {
			current = parent.lastElementChild;
			descent = true;
		} else return;
		else if (current.parentElement) {
			current = current.parentElement;
			break;
		} else return;
		if (condition(current)) return current;
	} while (current !== from);
}
function findParentWithin(from, parent, condition) {
	let current = from;
	while (current && current !== parent) {
		if (condition(current)) return current;
		current = current.parentElement;
	}
}
var FOCUSABLE_SELECTORS = [
	"[data-reactodia-autofocus]",
	"[autofocus]",
	"input:not(disabled), textarea:not(disabled), button:not(disabled), select:not(disabled), a[href], details > summary"
];
function findAutoFocusable(parent) {
	const activeElement = document.activeElement;
	if (parent.contains(activeElement)) return activeElement instanceof HTMLElement ? activeElement : void 0;
	for (const selector of FOCUSABLE_SELECTORS) {
		const target = parent.querySelector(selector);
		if (target instanceof HTMLElement) return target;
	}
}
//#endregion
//#region src/widgets/utility/draggableHandle.tsx
var CLASS_NAME$43 = "reactodia-draggable-handle";
var DOCK_CLASS = {
	"nw": `${CLASS_NAME$43}--dock-nw`,
	"n": `${CLASS_NAME$43}--dock-n`,
	"ne": `${CLASS_NAME$43}--dock-ne`,
	"e": `${CLASS_NAME$43}--dock-e`,
	"se": `${CLASS_NAME$43}--dock-se`,
	"s": `${CLASS_NAME$43}--dock-s`,
	"sw": `${CLASS_NAME$43}--dock-sw`,
	"w": `${CLASS_NAME$43}--dock-w`
};
/**
* Utility component for a draggable handle.
*
* @category Components
*/
function DraggableHandle(props) {
	const { dock, axis = "all", onBeginDragHandle, onDragHandle, onEndDragHandle, children, ...otherProps } = props;
	const holdState = React.useRef(void 0);
	const latestOnBegin = useLatest(onBeginDragHandle);
	const latestOnDrag = useLatest(onDragHandle);
	const latestOnEnd = useLatest(onEndDragHandle);
	const onPointerDown = (e) => {
		if (holdState.current) return;
		if (e.button) return;
		const { pageX, pageY, currentTarget, pointerId } = e;
		e.preventDefault();
		const state = {
			origin: {
				pageX,
				pageY
			},
			target: currentTarget,
			pointerId,
			onPointerMove: (e) => {
				e.preventDefault();
				const { origin } = state;
				latestOnDrag.current(e, axis === "y" ? 0 : e.pageX - origin.pageX, axis === "x" ? 0 : e.pageY - origin.pageY);
			},
			onPointerUp: (e) => {
				holdState.current = void 0;
				const { target } = state;
				target.releasePointerCapture(pointerId);
				target.removeEventListener("pointermove", state.onPointerMove);
				target.removeEventListener("pointerup", state.onPointerUp);
				target.removeEventListener("pointercancel", state.onPointerUp);
				latestOnEnd.current?.(e);
			}
		};
		currentTarget.addEventListener("pointermove", state.onPointerMove);
		currentTarget.addEventListener("pointerup", state.onPointerUp);
		currentTarget.addEventListener("pointercancel", state.onPointerUp);
		currentTarget.setPointerCapture(pointerId);
		latestOnBegin.current(e);
	};
	return /* @__PURE__ */ jsx("div", {
		...otherProps,
		className: cx(CLASS_NAME$43, dock ? DOCK_CLASS[dock] : void 0, otherProps.className),
		onPointerDown,
		children
	});
}
//#endregion
//#region src/widgets/dialog.tsx
var CLASS_NAME$42 = "reactodia-dialog";
var DEFAULT_SIZE = {
	width: 300,
	height: 320
};
var MIN_SIZE = {
	width: 250,
	height: 250
};
var MAX_SIZE = {
	width: 800,
	height: 800
};
var DEFAULT_DOCK = "e";
var DEFAULT_DOCK_MARGIN = 40;
var FOCUS_MARGIN = 20;
var Dialog = class extends React.Component {
	static contextType = CanvasContext;
	unsubscribeFromTarget = void 0;
	listener = new EventObserver();
	root = React.createRef();
	updateAll = () => this.forceUpdate();
	startSize;
	constructor(props) {
		super(props);
		this.state = {};
	}
	componentDidMount() {
		const { canvas, model } = this.context;
		const { autoFocus = true } = this.props;
		this.listener.listen(model.events, "changeLanguage", this.updateAll);
		this.listener.listen(canvas.events, "changeTransform", this.updateAll);
		this.listenToTarget(this.props.target);
		if (this.props.target) this.focusOn();
		if (autoFocus && this.root.current) findAutoFocusable(this.root.current)?.focus();
	}
	componentDidUpdate(prevProps) {
		if (this.props.target !== prevProps.target) this.listenToTarget(this.props.target);
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.listenToTarget(void 0);
	}
	listenToTarget(target) {
		if (this.unsubscribeFromTarget) {
			this.unsubscribeFromTarget();
			this.unsubscribeFromTarget = void 0;
		}
		if (target) {
			const unsubscribeFromStore = target.subscribe(this.updateAll, this.context);
			this.unsubscribeFromTarget = () => {
				unsubscribeFromStore();
			};
		}
	}
	calculatePosition(currentSize) {
		const { target, defaultSize = DEFAULT_SIZE, dock = DEFAULT_DOCK, dockMargin = DEFAULT_DOCK_MARGIN } = this.props;
		const { canvas } = this.context;
		if (target) {
			const bounds = target.getBounds(this.context);
			return computeDockedPosition(canvas.metrics.paperToScrollablePaneCoords(bounds.x, bounds.y), canvas.metrics.paperToScrollablePaneCoords(bounds.x + bounds.width, bounds.y + bounds.height), dock, dockMargin, defaultSize, currentSize);
		}
	}
	getViewPortScrollablePoints() {
		const { canvas } = this.context;
		const { clientWidth, clientHeight } = canvas.metrics.pane;
		return {
			min: canvas.metrics.clientToScrollablePaneCoords(0, 0),
			max: canvas.metrics.clientToScrollablePaneCoords(clientWidth, clientHeight)
		};
	}
	getDialogScrollableBounds() {
		const size = this.getCurrentSize();
		const { x, y } = this.calculatePosition(size) ?? {
			x: 0,
			y: 0
		};
		const min = {
			x: x - FOCUS_MARGIN,
			y: y - FOCUS_MARGIN
		};
		return {
			min,
			max: {
				x: min.x + size.width + FOCUS_MARGIN * 2,
				y: min.y + size.height + FOCUS_MARGIN * 2
			}
		};
	}
	focusOn() {
		const { canvas } = this.context;
		const { min: viewPortMin, max: viewPortMax } = this.getViewPortScrollablePoints();
		const { min, max } = this.getDialogScrollableBounds();
		let xOffset = 0;
		if (min.x < viewPortMin.x) xOffset = min.x - viewPortMin.x;
		else if (max.x > viewPortMax.x) xOffset = max.x - viewPortMax.x;
		let yOffset = 0;
		if (min.y < viewPortMin.y) yOffset = min.y - viewPortMin.y;
		else if (max.y > viewPortMax.y) yOffset = max.y - viewPortMax.y;
		const curScrollableCenter = {
			x: viewPortMin.x + (viewPortMax.x - viewPortMin.x) / 2,
			y: viewPortMin.y + (viewPortMax.y - viewPortMin.y) / 2
		};
		const newScrollableCenter = {
			x: curScrollableCenter.x + xOffset,
			y: curScrollableCenter.y + yOffset
		};
		const paperCenter = canvas.metrics.scrollablePaneToPaperCoords(newScrollableCenter.x, newScrollableCenter.y);
		canvas.centerTo(paperCenter);
	}
	onStartDragging = (e) => {
		const { defaultSize = DEFAULT_SIZE, maxSize = MAX_SIZE } = this.props;
		this.startSize = {
			x: Math.min(this.state.width || defaultSize.width, maxSize.width),
			y: Math.min(this.state.height || defaultSize.height, maxSize.height)
		};
	};
	calculateHeight(height) {
		const { defaultSize = DEFAULT_SIZE, minSize = MIN_SIZE, maxSize = MAX_SIZE } = this.props;
		const minHeight = Math.min(defaultSize.height, minSize.height);
		const maxHeight = Math.max(defaultSize.height, maxSize.height);
		return Math.max(minHeight, Math.min(maxHeight, height));
	}
	calculateWidth(width) {
		const { defaultSize = DEFAULT_SIZE, minSize = MIN_SIZE, maxSize = MAX_SIZE } = this.props;
		const minWidth = Math.min(defaultSize.width, minSize.width);
		const maxWidth = Math.max(defaultSize.width, maxSize.width);
		return Math.max(minWidth, Math.min(maxWidth, width));
	}
	onDragHandle = (e, dx, dy) => {
		const { dock = DEFAULT_DOCK, mode } = this.props;
		const centered = mode === "centered";
		let factorX = centered ? 2 : 1;
		let factorY = centered ? 2 : 1;
		switch (dock) {
			case "n":
				factorY *= -1;
				break;
			case "w":
				factorX *= -1;
				break;
		}
		const width = dx ? this.calculateWidth(this.startSize.x + dx * factorX) : void 0;
		const height = dy ? this.calculateHeight(this.startSize.y + dy * factorY) : void 0;
		this.setState((state) => ({
			width: width ?? state.width,
			height: height ?? state.height
		}));
	};
	render() {
		const { target, mode, dock = DEFAULT_DOCK, caption, resizableBy: baseResizableBy = "all", closable = true, closeTitle } = this.props;
		const resizableBy = mode === "fillViewport" ? "none" : baseResizableBy;
		const size = this.getCurrentSize();
		const position = this.calculatePosition(size);
		const style = {
			left: position?.x,
			top: position?.y,
			width: size.width,
			height: size.height
		};
		return /* @__PURE__ */ jsxs("div", {
			ref: this.root,
			className: cx(CLASS_NAME$42, target ? "reactodia-dialog--attached" : void 0, mode === "fillViewport" ? "reactodia-dialog--fill-viewport" : void 0),
			role: "dialog",
			"aria-labelledby": caption ? "reactodia-dialog-caption" : void 0,
			style,
			"data-reactodia-prevent-zoom": true,
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$42}__header`,
					children: [/* @__PURE__ */ jsx("div", {
						id: "reactodia-dialog-caption",
						className: `${CLASS_NAME$42}__caption`,
						title: caption,
						children: caption
					}), closable ? /* @__PURE__ */ jsx("button", {
						title: closeTitle,
						className: cx("reactodia-btn", `${CLASS_NAME$42}__close-button`),
						onClick: this.onClose
					}) : null]
				}),
				this.props.children,
				resizableBy === "y" || resizableBy === "all" ? /* @__PURE__ */ jsx(DraggableHandle, {
					dock: dock === "n" ? "n" : "s",
					axis: "y",
					onBeginDragHandle: this.onStartDragging,
					onDragHandle: this.onDragHandle,
					onEndDragHandle: this.onEndDragHandle
				}) : null,
				resizableBy === "x" || resizableBy === "all" ? /* @__PURE__ */ jsx(DraggableHandle, {
					dock: dock === "w" ? "w" : "e",
					axis: "x",
					onBeginDragHandle: this.onStartDragging,
					onDragHandle: this.onDragHandle,
					onEndDragHandle: this.onEndDragHandle
				}) : null,
				resizableBy === "none" ? null : /* @__PURE__ */ jsx(DraggableHandle, {
					dock: dock === "n" ? "ne" : dock === "w" ? "sw" : "se",
					axis: resizableBy,
					onBeginDragHandle: this.onStartDragging,
					onDragHandle: this.onDragHandle,
					onEndDragHandle: this.onEndDragHandle
				})
			]
		});
	}
	getCurrentSize() {
		const { mode, defaultSize = DEFAULT_SIZE, maxSize = MAX_SIZE } = this.props;
		if (mode === "fillViewport") return maxSize;
		return {
			width: Math.min(this.state.width ?? defaultSize.width, maxSize.width),
			height: Math.min(this.state.height ?? defaultSize.height, maxSize.height)
		};
	}
	onClose = () => {
		const { canvas } = this.context;
		const { onHide } = this.props;
		canvas.focus();
		onHide();
	};
	onEndDragHandle = () => {
		const { onResize } = this.props;
		if (!onResize) return;
		const { width, height } = this.getCurrentSize();
		if (this.startSize && !(width === this.startSize.x && height === this.startSize.y)) onResize({
			width,
			height
		});
	};
};
var DialogTarget = {
	forElement(element) {
		return {
			subscribe: (onChange, { canvas }) => {
				const listener = new EventObserver();
				listener.listen(element.events, "changePosition", onChange);
				listener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
					if (e.source === element) onChange();
				});
				return () => listener.stopListening();
			},
			getBounds: ({ canvas }) => boundsOf(element, canvas.renderingState)
		};
	},
	forLink(link) {
		return {
			subscribe: (onChange, { model, canvas }) => {
				const source = model.getElement(link.sourceId);
				const target = model.getElement(link.targetId);
				if (!source || !target) throw new Error("Source and target are not specified");
				const listener = new EventObserver();
				listener.listen(source.events, "changePosition", onChange);
				listener.listen(target.events, "changePosition", onChange);
				listener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
					if (e.source === source || e.source === target) onChange();
				});
				listener.listen(link.events, "changeVertices", onChange);
				listener.listen(canvas.renderingState.events, "changeLinkLabelBounds", (e) => {
					if (e.source === link) onChange();
				});
				return () => listener.stopListening();
			},
			getBounds: ({ model, canvas }) => {
				const labelBounds = canvas.renderingState.getLinkLabelBounds(link);
				if (labelBounds) return labelBounds;
				const source = model.getElement(link.sourceId);
				const target = model.getElement(link.targetId);
				if (!source || !target) throw new Error("Source and target are not specified");
				const route = canvas.renderingState.getRouting(link.id);
				const verticesDefinedByUser = link.vertices || [];
				const vertices = route ? route.vertices : verticesDefinedByUser;
				const polyline = computePolyline(canvas.renderingState.getElementShape(source), canvas.renderingState.getElementShape(target), vertices);
				const { x, y } = getPointAlongPolyline(polyline, computePolylineLength(polyline) / 2);
				return {
					x,
					y,
					width: 0,
					height: 0
				};
			}
		};
	}
};
function computeDockedPosition(targetMin, targetMax, dock, dockMargin, defaultSize, currentSize) {
	let x;
	switch (dock) {
		case "w":
			x = targetMin.x - currentSize.width - dockMargin;
			break;
		case "e":
			x = targetMax.x + dockMargin;
			break;
		default:
			x = (targetMin.x + targetMax.x - defaultSize.width) / 2;
			break;
	}
	let y;
	switch (dock) {
		case "n":
			y = targetMin.y - currentSize.height - dockMargin;
			break;
		case "s":
			y = targetMax.y + dockMargin;
			break;
		default:
			y = (targetMin.y + targetMax.y - defaultSize.height) / 2;
			break;
	}
	return {
		x,
		y
	};
}
//#endregion
//#region src/widgets/utility/viewportDock.tsx
var VIEWPORT_WIDGET_CLASS = "reactodia-viewport-dock";
var DOCK_CONTAINER_CLASS = {
	"n": `${VIEWPORT_WIDGET_CLASS}--row-n`,
	"s": `${VIEWPORT_WIDGET_CLASS}--row-s`,
	"w": `${VIEWPORT_WIDGET_CLASS}--column-w`,
	"e": `${VIEWPORT_WIDGET_CLASS}--column-e`
};
var DOCK_DIRECTION_CLASS = {
	"nw": `${VIEWPORT_WIDGET_CLASS}--dock-nw`,
	"n": `${VIEWPORT_WIDGET_CLASS}--dock-n`,
	"ne": `${VIEWPORT_WIDGET_CLASS}--dock-ne`,
	"e": `${VIEWPORT_WIDGET_CLASS}--dock-e`,
	"se": `${VIEWPORT_WIDGET_CLASS}--dock-se`,
	"s": `${VIEWPORT_WIDGET_CLASS}--dock-s`,
	"sw": `${VIEWPORT_WIDGET_CLASS}--dock-sw`,
	"w": `${VIEWPORT_WIDGET_CLASS}--dock-w`
};
var DOCK_ALIGN_X = {
	"nw": "start",
	"n": "center",
	"ne": "end",
	"e": "end",
	"se": "end",
	"s": "center",
	"sw": "start",
	"w": "start"
};
var DOCK_ALIGN_Y = {
	"nw": "start",
	"n": "start",
	"ne": "start",
	"e": "center",
	"se": "end",
	"s": "end",
	"sw": "end",
	"w": "center"
};
/**
* Utility component to dock a canvas widget content to a viewport location.
*
* @category Components
*/
function ViewportDock(props) {
	const { dock, dockOffsetX = 0, dockOffsetY = 0, children } = props;
	const { canvas: { renderingState } } = useCanvas();
	const interactionBlocked = useObservedProperty(renderingState.events, "changeInteractionBlocked", () => renderingState.interactionBlocked);
	const style = {
		"--reactodia-viewport-dock-offset-x": `${dockOffsetX}px`,
		"--reactodia-viewport-dock-offset-y": `${dockOffsetY}px`,
		"--reactodia-viewport-dock-align-x": DOCK_ALIGN_X[dock],
		"--reactodia-viewport-dock-align-y": DOCK_ALIGN_Y[dock]
	};
	return /* @__PURE__ */ jsx("div", {
		inert: interactionBlocked ? "" : void 0,
		className: cx(VIEWPORT_WIDGET_CLASS, DOCK_CONTAINER_CLASS[dock] ?? `${VIEWPORT_WIDGET_CLASS}--corner`),
		style,
		children: React.cloneElement(children, { className: cx(children.props.className, DOCK_DIRECTION_CLASS[dock]) })
	});
}
function getParentDockAlignment(target) {
	const style = getComputedStyle(target);
	return [style.getPropertyValue("--reactodia-viewport-dock-align-x"), style.getPropertyValue("--reactodia-viewport-dock-align-y")];
}
function getParentDockMargin(target) {
	const rawValue = getComputedStyle(target).getPropertyValue("--reactodia-viewport-dock-margin");
	if (rawValue) {
		const value = parseFloat(rawValue);
		if (Number.isFinite(value)) return value;
	}
}
//#endregion
//#region src/editor/overlayController.tsx
/**
* Controls UI overlays for the canvases, including dialogs and tasks.
*
* @category Core
*/
var OverlayController = class {
	source = new EventSource();
	events = this.source;
	internalSource = new EventSource();
	model;
	view;
	translation;
	dialogSettingsProvider;
	_openedDialog;
	_tasks = /* @__PURE__ */ new Set();
	_taskError;
	/** @hidden */
	constructor(props) {
		const { model, view, translation, dialogSettingsProvider } = props;
		this.model = model;
		this.view = view;
		this.translation = translation;
		this.dialogSettingsProvider = dialogSettingsProvider;
		withInternalApi(this)[OverlayInternalApi] = {
			events: this.internalSource,
			overlays: /* @__PURE__ */ new Set(),
			dialog: null,
			spinner: null,
			onCanvasPointerUp: this.onAnyCanvasPointerUp,
			onCanvasKeydown: this.onAnyCanvasKeydown
		};
		const listener = new EventObserver();
		listener.listen(this.model.events, "changeSelection", () => {
			const target = this.model.selection.length === 1 ? this.model.selection[0] : void 0;
			if (this.openedDialog && this.openedDialog.target !== target) this.hideDialog();
		});
		listener.listen(this.model.events, "discardGraph", () => {
			if (this.openedDialog && this.openedDialog.target) this.hideDialog();
		});
	}
	/**
	* Currently open dialog.
	*
	* Returns `undefined` if no dialog is opened.
	*/
	get openedDialog() {
		return this._openedDialog;
	}
	onAnyCanvasPointerUp = (event) => {
		const { source: canvas, sourceEvent, target, triggerAsClick } = event;
		if (sourceEvent.ctrlKey || sourceEvent.shiftKey || sourceEvent.metaKey) return;
		if (this.openedDialog && this.openedDialog.holdSelection) return;
		if (target instanceof Element) {
			this.model.setSelection([target]);
			target.focus();
		} else if (target instanceof Link) {
			this.model.setSelection([target]);
			canvas.focus();
		} else if (target instanceof LinkVertex) {
			this.model.setSelection([target.link]);
			canvas.focus();
		} else if (!target && triggerAsClick) {
			this.model.setSelection([]);
			this.hideDialog();
			canvas.focus();
		}
	};
	onAnyCanvasKeydown = (event) => {
		const { source, sourceEvent } = event;
		source.renderingState.triggerHotkey(sourceEvent);
	};
	/**
	* Starts a new foreground task which blocks canvas interaction and
	* displays a loading indicator until the task has ended.
	*
	* If multiple tasks are started at any given time, an indicator will be shown
	* while at least one of them is still active.
	*/
	startTask(params = {}) {
		const { title, delay: delayMs = 0 } = params;
		if (this._tasks.size === 0 && this._taskError) this._taskError = void 0;
		const delayed = delayMs > 0 && this._tasks.size === 0;
		const createdTask = {
			title,
			setError: (error) => {
				this._taskError = { error };
			},
			end: () => {
				this._tasks.delete(createdTask);
				this.updateTaskSpinner();
			},
			[OverlayTaskActive]: !delayed
		};
		this._tasks.add(createdTask);
		this.updateTaskSpinner();
		if (delayed) delay(delayMs).then(() => {
			if (this._tasks.has(createdTask)) {
				for (const task of this._tasks) task[OverlayTaskActive] = true;
				this.updateTaskSpinner();
			}
		});
		return createdTask;
	}
	/**
	* Creates a task via {@link startTask} for the operation defined by a `Promise`.
	*
	* @see {@link startTask}
	*/
	showSpinnerWhile(operation) {
		const { translation: t } = this;
		const task = this.startTask();
		(async () => {
			try {
				await operation;
			} catch (err) {
				console.error(err);
				task.setError(new Error(t.text("overlay_controller.unknown_error"), { cause: err }));
			} finally {
				task.end();
			}
		})();
	}
	updateTaskSpinner() {
		const { translation: t } = this;
		if (this._taskError) {
			const statusText = getErrorMessage(this._taskError.error);
			this.setSpinner({
				statusText,
				errorOccurred: true
			});
		} else {
			let hasActiveTask = false;
			let title;
			for (const task of this._tasks) {
				if (task[OverlayTaskActive]) hasActiveTask = true;
				if (task.title) if (title === void 0) title = task.title;
				else {
					title = t.text("overlay_controller.multiple_tasks_in_progress");
					break;
				}
			}
			if (hasActiveTask) this.setSpinner({ statusText: title });
			else this.setSpinner(void 0);
		}
	}
	setSpinner(props) {
		const internalApi = withInternalApi(this)[OverlayInternalApi];
		const spinner = props ? /* @__PURE__ */ jsx(LoadingWidget, { spinnerProps: props }) : null;
		const previous = internalApi.spinner;
		internalApi.spinner = spinner;
		this.internalSource.trigger("changeSpinner", {
			previous,
			source: this
		});
	}
	/**
	* Shows on-canvas dialog anchored to the target element or link.
	*
	* @see {@link hideDialog}
	*/
	showDialog(params) {
		const { target, style, dialogType, content, holdSelection = false, onClose } = params;
		if (this._openedDialog && this._openedDialog.target !== target) this.hideDialog();
		const openedDialog = {
			target,
			knownType: dialogType,
			holdSelection,
			onClose
		};
		const canvas = this.view.findAnyCanvas();
		const breakpoint = readOverlayProperty(withInternalApi(this)[OverlayInternalApi].overlays, (reader) => reader.getDialogViewportBreakpoint());
		const isSmallViewport = Boolean(canvas && breakpoint !== void 0 && canvas.metrics.pane.clientWidth <= breakpoint);
		let styleWithDefaults = style;
		const sizeOverrides = this.dialogSettingsProvider.getDialogSize(openedDialog);
		if (sizeOverrides) styleWithDefaults = {
			...style,
			defaultSize: sizeOverrides.defaultSize ?? style.defaultSize,
			minSize: sizeOverrides.minSize ?? style.minSize,
			maxSize: sizeOverrides.maxSize ?? style.maxSize
		};
		const onResize = (size) => {
			this.dialogSettingsProvider.persistDialogSize(openedDialog, size);
		};
		const onHide = () => this.hideDialog();
		const closeTitle = this.translation.text("overlay_controller.dialog_close.title");
		if (target && !isSmallViewport) this.setDialog(openedDialog, /* @__PURE__ */ jsx(CanvasPlaceAt, {
			layer: "overElements",
			children: /* @__PURE__ */ jsx(Dialog, {
				...styleWithDefaults,
				target: target instanceof Element ? DialogTarget.forElement(target) : target instanceof Link ? DialogTarget.forLink(target) : target,
				onResize,
				onHide,
				closeTitle,
				children: content
			})
		}));
		else this.setDialog(openedDialog, /* @__PURE__ */ jsx(ViewportDialog, {
			...styleWithDefaults,
			mode: isSmallViewport ? "fillViewport" : "centered",
			onResize,
			onHide,
			closeTitle,
			children: content
		}));
	}
	/**
	* Closes currently open dialog if any is active.
	*
	* @see {@link showDialog}
	*/
	hideDialog() {
		if (this._openedDialog) {
			this._openedDialog.onClose?.();
			this.setDialog(void 0, null);
		}
	}
	setDialog(openedDialog, dialog) {
		const internalApi = withInternalApi(this)[OverlayInternalApi];
		const previousDialog = this._openedDialog;
		const previous = internalApi.dialog;
		this._openedDialog = openedDialog;
		internalApi.dialog = dialog;
		this.internalSource.trigger("changeDialog", {
			previous,
			source: this
		});
		this.source.trigger("changeOpenedDialog", {
			source: this,
			previous: previousDialog
		});
	}
};
var OverlayTaskActive = Symbol("OverlayTask.active");
var OverlayInternalApi = Symbol("OverlayController.internalApi");
function withInternalApi(controller) {
	return controller;
}
function readOverlayProperty(readers, getProperty) {
	for (const reader of readers) {
		const value = getProperty(reader);
		if (value !== void 0) return value;
	}
}
function OverlaySupport(props) {
	const { overlay } = props;
	const { canvas } = useCanvas();
	const internalApi = withInternalApi(overlay)[OverlayInternalApi];
	const { overlays, onCanvasPointerUp, onCanvasKeydown } = internalApi;
	const ref = React.useRef(null);
	React.useLayoutEffect(() => {
		const overlay = ref.current;
		if (overlay) {
			const reader = { getDialogViewportBreakpoint: () => {
				const style = getComputedStyle(overlay);
				return style.maxWidth ? parseFloat(style.maxWidth) : void 0;
			} };
			overlays.add(reader);
			return () => {
				overlays.delete(reader);
			};
		}
	}, [overlays]);
	React.useEffect(() => {
		const listener = new EventObserver();
		listener.listen(canvas.events, "pointerUp", onCanvasPointerUp);
		listener.listen(canvas.events, "keydown", onCanvasKeydown);
		return () => listener.stopListening();
	}, [onCanvasPointerUp, onCanvasKeydown]);
	const dialog = useObservedProperty(internalApi.events, "changeDialog", () => internalApi.dialog);
	const spinner = useObservedProperty(internalApi.events, "changeSpinner", () => internalApi.spinner);
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx("div", {
			ref,
			style: {
				display: "none",
				maxWidth: "var(--reactodia-dialog-viewport-breakpoint-s)"
			}
		}),
		dialog,
		spinner
	] });
}
function LoadingWidget(props) {
	const { spinnerProps } = props;
	const size = useViewportSize();
	const position = {
		x: spinnerProps.statusText ? size.width / 3 : size.width / 2,
		y: size.height / 2
	};
	return /* @__PURE__ */ jsx(ViewportOverlay, {
		className: "reactodia-loading-widget",
		children: /* @__PURE__ */ jsx("svg", {
			width: size.width,
			height: size.height,
			children: /* @__PURE__ */ jsx(Spinner, {
				position,
				...spinnerProps
			})
		})
	});
}
function ViewportDialog(props) {
	const viewportSize = useViewportSize();
	const overlayRef = React.useRef(null);
	const [margin, setMargin] = React.useState(0);
	React.useLayoutEffect(() => {
		if (overlayRef.current) setMargin(getParentDockMargin(overlayRef.current) ?? 0);
	}, []);
	const maxSize = React.useMemo(() => ({
		width: viewportSize.width - margin * 2,
		height: viewportSize.height - margin * 2
	}), [viewportSize, margin]);
	return /* @__PURE__ */ jsx(ViewportOverlay, {
		innerRef: overlayRef,
		className: "reactodia-viewport-dialog-overlay",
		children: /* @__PURE__ */ jsx(Dialog, {
			...props,
			dock: "e",
			maxSize
		})
	});
}
function ViewportOverlay(props) {
	const { innerRef, className, children } = props;
	const { canvas } = useCanvas();
	const renderingState = canvas.renderingState;
	React.useLayoutEffect(() => {
		renderingState.changeInteractionBlocks(1);
		return () => renderingState.changeInteractionBlocks(-1);
	}, [renderingState]);
	return /* @__PURE__ */ jsx("div", {
		ref: innerRef,
		className: cx("reactodia-viewport-overlay", className),
		children
	});
}
function useViewportSize() {
	const { canvas } = useCanvas();
	return useSyncStoreWithComparator(useLayerDebouncedStore(useEventStore(canvas.events, "resize"), canvas.renderingState), () => {
		const { clientWidth, clientHeight } = canvas.metrics.pane;
		return {
			width: clientWidth,
			height: clientHeight
		};
	}, (a, b) => a.width === b.width && a.height === b.height);
}
function getErrorMessage(error) {
	if (error && typeof error === "object" && "message" in error) {
		const message = error.message;
		if (typeof message === "string") return message;
	}
}
/**
* Default dialog settings provider which persists changed dialog size in-memory
* for each {@link OverlayDialog.knownType}.
*
* Dialogs without {@link OverlayDialog.knownType} are ignored.
*
* @category Core
*/
var DefaultDialogSettingsProvider = class {
	lastDialogSize = /* @__PURE__ */ new Map();
	getDialogSize(dialog) {
		if (dialog.knownType) {
			const lastSize = this.lastDialogSize.get(dialog.knownType);
			if (lastSize) return { defaultSize: lastSize };
		}
	}
	persistDialogSize(dialog, size) {
		if (dialog.knownType) this.lastDialogSize.set(dialog.knownType, size);
	}
};
//#endregion
//#region src/editor/useProvidedEntities.tsx
var ENTITIES_CACHE = /* @__PURE__ */ new WeakMap();
var EMPTY_ENTITIES = /* @__PURE__ */ new Map();
/**
* Asynchronously loads entities data for a target set of IRIs.
*
* Reloads the result when either `provider` or `iris` changes.
* When reloading on `iri` changes, previously loaded data is reused.
*
* @category Hooks
*/
function useProvidedEntities(provider, iris) {
	const [result, setResult] = React.useState({
		data: EMPTY_ENTITIES,
		status: "completed"
	});
	const stableIrisRef = React.useRef(iris);
	const stableIris = shallowArrayEqual(stableIrisRef.current, iris) ? stableIrisRef.current : iris;
	React.useEffect(() => {
		stableIrisRef.current = stableIris;
		if (!provider || stableIris.length === 0) {
			setResult({
				data: EMPTY_ENTITIES,
				status: "completed"
			});
			return;
		}
		let cache = ENTITIES_CACHE.get(provider);
		if (!cache) {
			cache = new CachedEntityLoader();
			ENTITIES_CACHE.set(provider, cache);
		}
		const entities = /* @__PURE__ */ new Map();
		const toRequest = [];
		for (const target of stableIris) {
			const data = cache.createEntityData(target);
			entities.set(target, data);
			if (EntityElement.isPlaceholderData(data)) toRequest.push(target);
		}
		const isLoading = toRequest.length > 0;
		setResult({
			data: entities,
			status: isLoading ? "loading" : "completed"
		});
		if (isLoading) {
			const controller = new AbortController();
			cache.requestData(toRequest, {
				dataProvider: provider,
				signal: controller.signal
			}).then(() => {
				if (controller.signal.aborted) return;
				const loadedEntities = /* @__PURE__ */ new Map();
				for (const target of stableIris) loadedEntities.set(target, cache.createEntityData(target));
				setResult({
					data: loadedEntities,
					status: "completed"
				});
			}, (error) => {
				if (controller.signal.aborted) return;
				setResult((previous) => ({
					...previous,
					isLoading: false,
					error
				}));
			});
			return () => controller.abort();
		}
	}, [provider, stableIris]);
	return result;
}
var CachedEntityLoader = class {
	cache = /* @__PURE__ */ new Map();
	createEntityData(target) {
		let data = this.cache.get(target);
		if (!data) {
			data = EntityElement.placeholderData(target);
			this.cache.set(target, data);
		}
		return data;
	}
	async requestData(targets, options) {
		const { dataProvider, signal } = options;
		const result = await dataProvider.elements({
			elementIds: targets,
			signal
		});
		for (const [target, data] of result) this.cache.set(target, data);
	}
};
//#endregion
//#region src/editor/withFetchStatus.tsx
var CLASS_NAME$41 = "reactodia-fetch-status";
/**
* Decorator component that styles the child element based on the fetching status
* of the graph operation target.
*
* Depending on the fetch status, the child element will be rendered
* with an additional CSS class:
*   - none or finished - no additional classes;
*   - loading - `reactodia-fetch-status--loading`;
*   - failed - `reactodia-fetch-status--error`.
*
* @category Components
* @see {@link FetchOperation}
*/
function WithFetchStatus(props) {
	const { type, target, children } = props;
	const { model } = useWorkspace();
	const [status, setStatus] = React.useState(model.getOperationFailReason(type, target) ? 2 : 0);
	React.useEffect(() => {
		const checkOperations = (fail) => {
			setStatus(() => {
				if (fail && isOperationMatch(fail.operation, type, target)) return 2;
				for (const operation of model.operations) if (isOperationMatch(operation, type, target)) return 1;
				if (model.getOperationFailReason(type, target)) return 2;
				return 0;
			});
		};
		checkOperations();
		const listener = (e) => {
			checkOperations(e.fail);
		};
		model.events.on("changeOperations", listener);
		return () => model.events.off("changeOperations", listener);
	}, [type, target]);
	if (status === 0) return children;
	else return React.cloneElement(children, { className: cx(children.props.className, status === 1 ? `${CLASS_NAME$41}--loading` : status === 2 ? `${CLASS_NAME$41}--error` : void 0) });
}
function isOperationMatch(operation, type, target) {
	return operation.type === type && operation.targets.has(target);
}
//#endregion
//#region src/paper/animateInterval.ts
/**
* Runs specified callback on each rendered frame for the `duration` interval
* using [requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame).
*
* @category Utilities
*/
function animateInterval(duration, onProgress, signal) {
	return new Promise((resolve) => {
		let animationFrameId;
		let start;
		let cleanupAbort;
		const animate = (time) => {
			if (signal && signal.aborted) return;
			start = start || time;
			let timePassed = time - start;
			if (timePassed > duration) timePassed = duration;
			onProgress(timePassed / duration);
			if (timePassed < duration) animationFrameId = requestAnimationFrame(animate);
			else {
				cleanupAbort?.();
				resolve();
			}
		};
		if (signal) {
			const onAbort = () => {
				cancelAnimationFrame(animationFrameId);
				cleanupAbort?.();
				resolve();
			};
			cleanupAbort = () => {
				signal.removeEventListener("abort", onAbort);
			};
			signal.addEventListener("abort", onAbort);
		}
		animationFrameId = requestAnimationFrame(animate);
	});
}
function easeInOutBezier(t) {
	if (t < 0) return 0;
	if (t > 1) return 1;
	return t * t * (3 - 2 * t);
}
//#endregion
//#region src/templates/basicLink.tsx
/**
* Link marker style with an arrowhead.
*/
var LinkMarkerArrowhead = {
	d: "M0,0 L0,8 L9,4 z",
	width: 9,
	height: 8,
	fill: "context-stroke"
};
/**
* Link marker style with a circle.
*/
var LinkMarkerCircle = {
	d: "M 6 1 a 5 5 0 1 0 0.0001 0 Z",
	width: 12,
	height: 12,
	fill: "context-stroke",
	stroke: "context-stroke",
	strokeWidth: 2
};
/**
* Link marker style with a diamond.
*/
var LinkMarkerDiamond = {
	d: "M 9 1 L 17 7 L 9 13 L 1 7 Z",
	width: 18,
	height: 14,
	fill: "context-stroke",
	stroke: "context-stroke",
	strokeWidth: 2
};
/**
* Basic link template which displays a {@link link path} with editable geometry (vertices)
* without any labels.
*
* @category Components
*/
function BasicLink(props) {
	const { link, className, path, pathProps, markerSource, markerTarget, children } = props;
	const stroke = pathProps?.stroke;
	return /* @__PURE__ */ jsxs("g", {
		className,
		children: [
			/* @__PURE__ */ jsx(LinkPath, {
				path,
				pathProps: {
					fill: "none",
					...pathProps
				},
				markerSource,
				markerTarget
			}),
			link.vertices.length === 0 ? null : /* @__PURE__ */ jsx(LinkVertices, {
				linkId: link.id,
				vertices: link.vertices,
				fill: stroke
			}),
			children
		]
	});
}
//#endregion
//#region src/templates/classicTemplate.tsx
/**
* Element template component with classic "look and feel" which
* was used for elements before v0.8.0.
*
* Uses {@link ClassicEntity} component to render a single entity.
*
* @category Constants
*/
var ClassicTemplate = {
	renderElement: (props) => /* @__PURE__ */ jsx(ClassicEntity, { ...props }),
	supports: { [TemplateProperties.Expanded]: true }
};
var CLASS_NAME$40 = "reactodia-classic-template";
/**
* Element template component to display component in the classic style.
*
* This classic "look and feel" was used for elements before v0.8.0
*
* The template supports displaying only {@link EntityElement} elements,
* otherwise nothing will be rendered.
*
* The template supports the following template state:
*   - {@link TemplateProperties.Expanded}
*
* @category Components
* @see {@link ClassicTemplate}
*/
function ClassicEntity(props) {
	const { element, isExpanded } = props;
	const data = element instanceof EntityElement ? element.data : void 0;
	const workspace = useWorkspace();
	const t = useTranslation();
	const { model, getElementTypeStyle } = workspace;
	useKeyedSyncStore(subscribeElementTypes, data ? data.types : [], model);
	const { data: imageUrl } = useResolvedAssetUrl(model.locale, data ? model.locale.selectEntityImageUrl(data) : void 0);
	if (!data) return null;
	const types = data.types ?? [];
	const { color, icon } = getElementTypeStyle(types);
	const typesLabel = types.length > 0 ? model.locale.formatEntityTypeList(data, model.language) : t.text("standard_element.default_type");
	const label = model.locale.formatEntityLabel(data, model.language);
	const image = imageUrl === void 0 ? void 0 : /* @__PURE__ */ jsx("div", {
		className: `${CLASS_NAME$40}__thumbnail`,
		children: /* @__PURE__ */ jsx("img", {
			className: `${CLASS_NAME$40}__thumbnail-image`,
			src: imageUrl
		})
	});
	const expander = isExpanded ? /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$40}__expander`,
			children: [/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$40}__iri-heading`,
				children: t.text("standard_element.iri.label")
			}), /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$40}__iri-container`,
				children: /* @__PURE__ */ jsx("a", {
					...model.locale.prepareAnchor(data.id),
					className: `${CLASS_NAME$40}__iri`,
					title: data.id,
					children: data.id
				})
			})]
		}),
		/* @__PURE__ */ jsx("hr", { className: `${CLASS_NAME$40}__divider` }),
		/* @__PURE__ */ jsx(PropertyList$1, { data })
	] }) : null;
	const onDoubleClick = (e) => {
		e.preventDefault();
		e.stopPropagation();
		model.history.execute(setElementExpanded(element, !element.isExpanded));
	};
	return /* @__PURE__ */ jsxs("div", {
		className: cx(CLASS_NAME$40, isExpanded ? `${CLASS_NAME$40}--expanded` : `${CLASS_NAME$40}--collapsed`),
		style: {
			backgroundColor: color,
			borderColor: color
		},
		"data-expanded": isExpanded,
		onDoubleClick,
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$40}__type-line`,
				title: label,
				children: [/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$40}__type-line-icon`,
					"aria-hidden": "true",
					children: /* @__PURE__ */ jsx("img", { src: icon })
				}), /* @__PURE__ */ jsx("div", {
					title: typesLabel,
					className: `${CLASS_NAME$40}__type-line-text-container`,
					children: /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$40}__type-line-text`,
						children: typesLabel
					})
				})]
			}),
			image,
			/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$40}__body`,
				style: { borderColor: color },
				children: [/* @__PURE__ */ jsx(WithFetchStatus, {
					type: "element",
					target: data.id,
					children: /* @__PURE__ */ jsx("span", {
						className: `${CLASS_NAME$40}__label`,
						title: label,
						children: label
					})
				}), expander]
			})
		]
	});
}
function PropertyList$1(props) {
	const { data } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const propertyIris = [];
	for (const iri in data.properties) if (Object.prototype.hasOwnProperty.call(data.properties, iri) && data.properties[iri].length > 0) propertyIris.push(iri);
	useKeyedSyncStore(subscribePropertyTypes, propertyIris, model);
	const properties = propertyIris.map((iri) => {
		const property = model.getPropertyType(iri);
		const selectedValues = t.selectValues(data.properties[iri], model.language);
		return {
			iri,
			label: t.formatLabel(property?.data?.label, iri, model.language),
			values: selectedValues.length === 0 ? data.properties[iri] : selectedValues
		};
	});
	properties.sort((a, b) => a.label.localeCompare(b.label));
	if (properties.length > 0) return /* @__PURE__ */ jsx("div", {
		className: `${CLASS_NAME$40}__property-table`,
		children: properties.map(({ iri, label, values }) => {
			const renderedValues = values.map((term, index) => /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$40}__property-value`,
				title: term.value,
				children: term.value
			}, index));
			return /* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$40}__property-row`,
				children: [/* @__PURE__ */ jsx(WithFetchStatus, {
					type: "propertyType",
					target: iri,
					children: /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$40}__property-label`,
						title: t.text("standard_element.property.title", {
							property: label,
							propertyIri: model.locale.formatIri(iri)
						}),
						children: label
					})
				}), /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$40}__property-values`,
					children: renderedValues
				})]
			}, iri);
		})
	});
	else return /* @__PURE__ */ jsx("div", { children: t.text("standard_element.no_properties") });
}
//#endregion
//#region src/templates/groupPaginator.tsx
var CLASS_NAME$39 = "reactodia-group-paginator";
/**
* Utility component to display paginator for an element group.
*
* @category Components
*/
function GroupPaginator(props) {
	const { pageIndex, pageCount, onChangePage, pageSize, pageSizes, onChangePageSize } = props;
	const t = useTranslation();
	const lastPage = pageCount - 1;
	return /* @__PURE__ */ jsxs("div", {
		className: CLASS_NAME$39,
		children: [
			/* @__PURE__ */ jsx("button", {
				type: "button",
				className: cx("reactodia-btn", "reactodia-btn-default", `${CLASS_NAME$39}__previous`),
				name: t.text("group_paginator.go_previous.title"),
				disabled: pageIndex <= 0,
				onClick: () => onChangePage(pageIndex - 1)
			}),
			/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$39}__status`,
				children: t.text("group_paginator.current_page.label", {
					page: pageIndex + 1,
					pageCount
				})
			}),
			/* @__PURE__ */ jsx("button", {
				type: "button",
				className: cx("reactodia-btn", "reactodia-btn-default", `${CLASS_NAME$39}__next`),
				title: t.text("group_paginator.go_next.title"),
				disabled: pageIndex >= lastPage,
				onClick: () => onChangePage(pageIndex + 1)
			}),
			/* @__PURE__ */ jsx("select", {
				className: `${CLASS_NAME$39}__page-size`,
				title: t.text("group_paginator.page_size.title"),
				value: pageSize,
				onPointerDown: (e) => e.stopPropagation(),
				onChange: (e) => onChangePageSize(Number(e.currentTarget.value)),
				children: pageSizes.map((size) => /* @__PURE__ */ jsx("option", {
					value: size,
					children: size
				}, size))
			})
		]
	});
}
//#endregion
//#region src/widgets/utility/contentEditablePlaintext.tsx
function ContentEditablePlaintext(props) {
	const { className, style, text, setText, isEditing, setEditing } = props;
	const editorRef = React.useRef(null);
	React.useLayoutEffect(() => {
		const editor = editorRef.current;
		if (isEditing && editor) {
			editor.focus();
			window.getSelection()?.selectAllChildren(editor);
		}
	}, [isEditing]);
	React.useLayoutEffect(() => {
		const editor = editorRef.current;
		if (editor) editor.innerText = text || "\n";
	}, [text]);
	return /* @__PURE__ */ jsx("p", {
		ref: editorRef,
		className,
		style,
		contentEditable: isEditing ? "plaintext-only" : void 0,
		onPointerDown: (e) => {
			if (isEditing) e.stopPropagation();
		},
		onKeyDown: (e) => {
			if (isEditing) e.stopPropagation();
		},
		onKeyUp: (e) => {
			if (isEditing) {
				e.stopPropagation();
				if (e.key === "Escape") {
					document.getSelection()?.removeAllRanges();
					editorRef.current?.blur();
				}
			}
		},
		onBlur: (e) => {
			const changedText = (editorRef.current?.innerText ?? "").trim();
			setEditing(false);
			setText(changedText);
		}
	});
}
//#endregion
//#region src/workspace/commandBusTopic.ts
/**
* Represents a definition for an event bus to communicate
* between related components with the same topic.
*
* @category Core
*/
var CommandBusTopic = class CommandBusTopic {
	__commandsMarker;
	constructor() {}
	/**
	* Defines a new command event topic.
	*/
	static define() {
		return new CommandBusTopic();
	}
};
/**
* Event bus to connect {@link AnnotationSupport} to other components.
*
* @category Constants
*/
var AnnotationTopic = CommandBusTopic.define();
/**
* Event bus to connect {@link ConnectionMenu} to other components.
*
* @category Constants
*/
var ConnectionsMenuTopic = CommandBusTopic.define();
/**
* Event bus to connect {@link InstancesSearch} to other components.
*
* @category Constants
*/
var InstancesSearchTopic = CommandBusTopic.define();
/**
* Event bus to connect {@link UnifiedSearch} to other components.
*
* @category Constants
*/
var UnifiedSearchTopic = CommandBusTopic.define();
/**
* Event bus to connect {@link VisualAuthoring} to other components.
*
* @category Constants
*/
var VisualAuthoringTopic = CommandBusTopic.define();
//#endregion
//#region src/widgets/visualAuthoring/authoredEntity.tsx
var NO_AUTHORING_CONTEXT = {
	canEdit: false,
	canDelete: false,
	onEdit: () => {},
	onDelete: () => {}
};
/**
* React hook to load entity authoring status for the graph authoring.
*
* @category Hooks
*/
function useAuthoredEntity(data, shouldLoad) {
	const { editor, getCommandBus } = useWorkspace();
	const entity = editor.inAuthoringMode ? data : void 0;
	const authoringEvent = useObservedProperty(editor.events, "changeAuthoringState", () => entity ? editor.authoringState.elements.get(entity.id) : void 0);
	const fetchedActions = useAsync({
		input: [
			editor.metadataProvider,
			entity,
			authoringEvent,
			shouldLoad
		],
		load: async ([provider, entity, authoringEvent, shouldLoad], { signal }) => {
			if (provider && entity && shouldLoad && !(authoringEvent && authoringEvent.type === "entityDelete")) {
				const { canEdit, canDelete } = await provider.canModifyEntity(entity, { signal });
				return (canEdit ? 1 : 0) | (canDelete ? 2 : 0);
			}
			return 0;
		}
	});
	const allowedActions = fetchedActions.status === "completed" ? fetchedActions.data : void 0;
	if (!entity) return NO_AUTHORING_CONTEXT;
	return {
		editedIri: authoringEvent && authoringEvent.type === "entityChange" ? authoringEvent.newIri : void 0,
		canEdit: allowedActions === void 0 ? void 0 : Boolean(allowedActions & 1),
		canDelete: allowedActions === void 0 ? void 0 : Boolean(allowedActions & 2),
		onEdit: (target) => {
			if (target instanceof EntityElement) getCommandBus(VisualAuthoringTopic).trigger("editEntity", { target });
		},
		onDelete: () => {
			editor.deleteEntity(entity.id);
		}
	};
}
//#endregion
//#region src/templates/noteAnnotation.tsx
/**
* Element template to display an {@link AnnotationElement} on a canvas.
*
* Uses {@link NoteAnnotation} component to render an annotation element.
*
* @category Constants
*/
var NoteTemplate = {
	renderElement: (props) => {
		if (props.element instanceof AnnotationElement) return /* @__PURE__ */ jsx(NoteAnnotation, { ...props });
		else if (props.element instanceof EntityElement) return /* @__PURE__ */ jsx(NoteEntity, { ...props });
		return null;
	},
	supports: {
		[TemplateProperties.ColorVariant]: true,
		[TemplateProperties.ElementSize]: true
	}
};
/**
* Displays an {@link AnnotationElement} in a form of a editable plaintext note.
*
* The template supports displaying any element but always store its content
* in the {@link Element.elementState element state}.
*
* The template supports the following template state:
*   - {@link TemplateProperties.AnnotationContent}
*   - {@link TemplateProperties.ColorVariant}
*   - {@link TemplateProperties.ElementSize}
*
* @category Components
* @see {@link NoteTemplate}
*/
function NoteAnnotation(props) {
	const { element, elementState } = props;
	const { model } = useCanvas();
	const content = elementState.get(TemplateProperties.AnnotationContent);
	return /* @__PURE__ */ jsx(NoteAnnotationView, {
		...props,
		content,
		setContent: (changedContent) => {
			model.history.execute(Command.compound(TranslatedText.text("note_annotation.change_text.command"), [setElementState(element, element.elementState.set(TemplateProperties.AnnotationContent, changedContent))]));
		}
	});
}
/**
* Displays an {@link EntityElement} in a form of a editable plaintext note.
*
* The template supports displaying any element but will be empty
* for any element cell type other than {@link EntityElement}.
*
* The property IRI for note content can be configured with
* {@link NoteEntityProps.textProperty}; by default it uses
* `oa:bodyValue` from [Web Annotation Ontology](https://www.w3.org/ns/oa).
*
* The template supports the following template state:
*   - {@link TemplateProperties.ElementSize}
*
* @category Components
* @see {@link NoteTemplate}
*/
function NoteEntity(props) {
	const { element, textProperty = "http://www.w3.org/ns/oa#bodyValue" } = props;
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const data = element instanceof EntityElement ? element.data : void 0;
	const entityContext = useAuthoredEntity(data, true);
	const content = React.useMemo(() => {
		const values = data?.properties[textProperty];
		if (values && values.length > 0) return {
			type: "plaintext",
			text: t.selectValues(values, model.language).filter((v) => v.termType === "Literal").map((v) => v.value).join("\n")
		};
		else return;
	}, [data]);
	return /* @__PURE__ */ jsx(NoteAnnotationView, {
		...props,
		readOnly: entityContext.canEdit !== true,
		content,
		setContent: (changedContent) => {
			if (data) editor.changeEntity(data.id, {
				...data,
				properties: {
					...data.properties,
					[textProperty]: [model.factory.literal(changedContent.text)]
				}
			});
		}
	});
}
var CLASS_NAME$38 = "reactodia-note-annotation";
function NoteAnnotationView(props) {
	const { element, elementState, onlySelected, readOnly, content, setContent } = props;
	const { canvas, model } = useCanvas();
	const [isEditing, setEditing] = React.useState(false);
	const size = elementState.get(TemplateProperties.ElementSize);
	const colorVariant = elementState.get(TemplateProperties.ColorVariant);
	const plaintext = (content?.type === "plaintext" ? content : void 0) ?? {
		type: "plaintext",
		text: ""
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
		className: cx(CLASS_NAME$38, `${CLASS_NAME$38}--variant-${colorVariant ?? "default"}`, isEditing ? `${CLASS_NAME$38}--editing` : void 0),
		style: {
			width: size?.width,
			height: size?.height
		},
		onDoubleClick: async (e) => {
			e.preventDefault();
			e.stopPropagation();
			if (readOnly) return;
			if (canvas.getScale() < 1) await canvas.centerTo(Rect.center(boundsOf(element, canvas.renderingState)), {
				scale: 1,
				animate: true
			});
			setEditing(true);
		},
		children: /* @__PURE__ */ jsx(ContentEditablePlaintext, {
			className: `${CLASS_NAME$38}__editor`,
			style: plaintext.style,
			text: plaintext.text,
			setText: (changedText) => {
				requestAnimationFrame(() => setContent({
					...plaintext,
					text: changedText
				}));
			},
			isEditing,
			setEditing
		})
	}), onlySelected && /* @__PURE__ */ jsx(ElementDecoration, {
		target: element,
		children: /* @__PURE__ */ jsx(NoteStyleControls, {
			style: plaintext.style,
			setStyle: (changedStyle) => {
				setContent({
					...plaintext,
					style: changedStyle
				});
			},
			variant: colorVariant,
			setVariant: (changedVariant) => {
				model.history.execute(setElementState(element, element.elementState.set(TemplateProperties.ColorVariant, changedVariant)));
			}
		})
	})] });
}
function NoteStyleControls(props) {
	const { style, setStyle, variant: selectedVariant = "default", setVariant } = props;
	const t = useTranslation();
	const { fontStyle, fontWeight, textDecorationLine, textAlign = "center" } = style ?? {};
	const preventPropagation = (e) => e.stopPropagation();
	const buttonProps = {
		type: "button",
		onPointerDown: preventPropagation
	};
	return /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$38}__controls`,
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "reactodia-btn-group reactodia-btn-group-xs",
				children: [
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__style-bold reactodia-btn reactodia-btn-default`, fontWeight === "bold" ? "active" : void 0),
						title: t.text("note_annotation.style_bold.title"),
						onClick: () => setStyle({
							...style,
							fontWeight: fontWeight === "bold" ? void 0 : "bold"
						})
					}),
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__style-italic reactodia-btn reactodia-btn-default`, fontStyle === "italic" ? "active" : void 0),
						title: t.text("note_annotation.style_italic.title"),
						onClick: () => setStyle({
							...style,
							fontStyle: fontStyle === "italic" ? void 0 : "italic"
						})
					}),
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__style-underline reactodia-btn reactodia-btn-default`, textDecorationLine === "underline" ? "active" : void 0),
						title: t.text("note_annotation.style_underline.title"),
						onClick: () => setStyle({
							...style,
							textDecorationLine: textDecorationLine === "underline" ? void 0 : "underline"
						})
					}),
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__style-strikethrough reactodia-btn reactodia-btn-default`, textDecorationLine === "line-through" ? "active" : void 0),
						title: t.text("note_annotation.style_strikethrough.title"),
						onClick: () => setStyle({
							...style,
							textDecorationLine: textDecorationLine === "line-through" ? void 0 : "line-through"
						})
					})
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "reactodia-btn-group reactodia-btn-group-xs",
				children: [
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__align-left reactodia-btn reactodia-btn-default`, textAlign === "left" ? "active" : void 0),
						title: t.text("note_annotation.style_align_left.title"),
						onClick: () => setStyle({
							...style,
							textAlign: "left"
						})
					}),
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__align-center reactodia-btn reactodia-btn-default`, textAlign === "center" ? "active" : void 0),
						title: t.text("note_annotation.style_align_center.title"),
						onClick: () => setStyle({
							...style,
							textAlign: void 0
						})
					}),
					/* @__PURE__ */ jsx("button", {
						...buttonProps,
						className: cx(`${CLASS_NAME$38}__align-right reactodia-btn reactodia-btn-default`, textAlign === "right" ? "active" : void 0),
						title: t.text("note_annotation.style_align_right.title"),
						onClick: () => setStyle({
							...style,
							textAlign: "right"
						})
					})
				]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "reactodia-btn-group reactodia-btn-group-xs",
				children: DefaultColorVariants.map((variant) => /* @__PURE__ */ createElement("button", {
					...buttonProps,
					key: variant,
					className: cx(`${CLASS_NAME$38}__variant-${variant} reactodia-btn reactodia-btn-default`, selectedVariant === variant ? "active" : void 0),
					title: t.text("note_annotation.style_color_variant.title", { variant }),
					onClick: () => setVariant(variant === "default" ? void 0 : variant)
				}, /* @__PURE__ */ jsx("div", { className: `${CLASS_NAME$38}__variant ${CLASS_NAME$38}--variant-${variant}` })))
			})
		]
	});
}
/**
* Link template to display an {@link AnnotationLink} on a canvas.
*
* Uses {@link NoteLink} component to render an annotation link.
*
* @category Constants
*/
var NoteLinkTemplate = {
	markerTarget: {
		...LinkMarkerCircle,
		fill: "var(--reactodia-canvas-background-color)"
	},
	renderLink: (props) => {
		if (props.link instanceof AnnotationLink) return /* @__PURE__ */ jsx(NoteLink, { ...props });
		return null;
	}
};
var LINK_CLASS = "reactodia-note-link";
/**
* Displays a link for an {@link AnnotationLink} on the canvas.
*
* {@link RenameLinkProvider} can be used to display a custom label which will
* override a default one from {@link NoteLinkProps.primaryLabelProps}.
*
* @category Components
* @see {@link NoteTemplate}
*/
function NoteLink(props) {
	const { link, getPathPosition, route, primaryLabelProps } = props;
	const { canvas } = useCanvas();
	const { renameLinkProvider } = canvas.renderingState.shared;
	const labelContent = renameLinkProvider?.getLabel(link) ?? primaryLabelProps?.children ?? (renameLinkProvider?.canRename(link) ? "" : void 0);
	return /* @__PURE__ */ jsx(BasicLink, {
		...props,
		pathProps: { className: `${LINK_CLASS}__path` },
		children: labelContent === void 0 ? null : /* @__PURE__ */ jsx(LinkLabel, {
			...primaryLabelProps,
			primary: true,
			textAnchor: route?.labelTextAnchor ?? primaryLabelProps?.textAnchor,
			className: cx(`${LINK_CLASS}__label`, primaryLabelProps?.className),
			link,
			position: primaryLabelProps?.position ?? getPathPosition(.5),
			children: /* @__PURE__ */ jsx("span", { children: labelContent })
		})
	});
}
//#endregion
//#region src/templates/roundTemplate.tsx
/**
* Basic element template with an round (elliptical) shape to display
* an {@link EntityElement} on a canvas.
*
* Uses {@link RoundEntity} component to render a single entity.
*
* @category Constants
*/
var RoundTemplate = {
	shape: "ellipse",
	renderElement: (props) => /* @__PURE__ */ jsx(RoundEntity, { ...props })
};
var CLASS_NAME$37 = "reactodia-round-entity";
/**
* Basic element template component with a round (elliptical) shape.
*
* The template supports displaying only {@link EntityElement} elements,
* otherwise nothing will be rendered.
*
* @category Components
* @see {@link RoundTemplate}
*/
function RoundEntity(props) {
	const { element, className, style, showTypes } = props;
	const workspace = useWorkspace();
	const t = useTranslation();
	const { model, getElementTypeStyle } = workspace;
	const data = element instanceof EntityElement ? element.data : void 0;
	useKeyedSyncStore(subscribeElementTypes, data && showTypes ? data.types : [], model);
	if (!data) return null;
	const label = model.locale.formatEntityLabel(data, model.language);
	const { color: baseColor } = getElementTypeStyle(data.types);
	const rootStyle = { "--reactodia-element-style-color": baseColor };
	return /* @__PURE__ */ jsxs("div", {
		className: cx(CLASS_NAME$37, className),
		style: style ? {
			...rootStyle,
			...style
		} : rootStyle,
		children: [/* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$37}__types`,
			title: showTypes ? model.locale.formatEntityTypeList(data, model.language) : void 0,
			children: showTypes ? data.types.map((typeIri, index) => {
				const type = model.getElementType(typeIri);
				const label = t.formatLabel(type?.data?.label, typeIri, model.language);
				return /* @__PURE__ */ jsxs(React.Fragment, { children: [index === 0 ? null : ", ", /* @__PURE__ */ jsx(WithFetchStatus, {
					type: "elementType",
					target: typeIri,
					children: /* @__PURE__ */ jsx("span", { children: label })
				})] }, typeIri);
			}) : null
		}), /* @__PURE__ */ jsx(WithFetchStatus, {
			type: "element",
			target: data.id,
			children: /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$37}__label`,
				title: label,
				children: label
			})
		})]
	});
}
//#endregion
//#region src/templates/standardElement.tsx
/**
* Default element template to display an {@link EntityElement} or
* {@link EntityGroup} on a canvas.
*
* Uses {@link StandardEntity} component to render a single entity and
* {@link StandardEntityGroup} component to render an entity group.
*
* @category Constants
*/
var StandardTemplate = {
	renderElement: (props) => {
		const { element } = props;
		if (element instanceof EntityElement) return /* @__PURE__ */ jsx(StandardEntity, { ...props });
		else if (element instanceof EntityGroup) return /* @__PURE__ */ jsx(StandardEntityGroup, { ...props });
		else return null;
	},
	supports: { [TemplateProperties.Expanded]: true }
};
var CLASS_NAME$36 = "reactodia-standard-element";
/**
* Default single entity template component.
*
* The template supports displaying only {@link EntityElement},
* otherwise nothing will be rendered.
*
* The template supports the following template state:
*   - {@link TemplateProperties.Expanded}
*   - {@link TemplateProperties.PinnedProperties}
*
* @category Components
* @see {@link StandardTemplate}
*/
function StandardEntity(props) {
	const { showActions, element, isExpanded, elementState } = props;
	const { model, editor, getElementTypeStyle } = useWorkspace();
	const t = useTranslation();
	const data = element instanceof EntityElement ? element.data : void 0;
	useKeyedSyncStore(subscribeElementTypes, data ? data.types : [], model);
	const entityContext = useAuthoredEntity(data, isExpanded);
	const { data: imageUrl } = useResolvedAssetUrl(model.locale, data ? model.locale.selectEntityImageUrl(data) : void 0);
	if (!data) return null;
	const label = model.locale.formatEntityLabel(data, model.language);
	const typesLabel = formatEntityTypes(data, model, t);
	const typeStyle = getElementTypeStyle(data.types);
	const rootStyle = { "--reactodia-element-style-color": typeStyle.color };
	const pinnedProperties = findPinnedProperties() ?? {};
	function renderTypes(data) {
		if (data.types.length === 0) return t.text("standard_element.default_type");
		return data.types.map((typeIri, index) => {
			const type = model.getElementType(typeIri);
			const label = t.formatLabel(type?.data?.label, typeIri, model.language);
			return /* @__PURE__ */ jsxs(React.Fragment, { children: [index === 0 ? null : ", ", /* @__PURE__ */ jsx(WithFetchStatus, {
				type: "elementType",
				target: typeIri,
				children: /* @__PURE__ */ jsx("span", { children: label })
			})] }, typeIri);
		});
	}
	function findPinnedProperties() {
		return isExpanded ? void 0 : elementState.get(TemplateProperties.PinnedProperties);
	}
	function renderIri(data) {
		const finalIri = entityContext.editedIri === void 0 ? data.id : entityContext.editedIri;
		return /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$36}__iri`,
			children: [/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$36}__iri-key`,
				children: entityContext.editedIri ? t.text("standard_element.iri.label_modified") : t.text("standard_element.iri.label")
			}), /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$36}__iri-value`,
				children: isEncodedBlank(finalIri) ? /* @__PURE__ */ jsx("span", { children: t.text("standard_element.blank_node") }) : /* @__PURE__ */ jsx("a", {
					...model.locale.prepareAnchor(finalIri),
					title: finalIri,
					children: finalIri
				})
			})]
		}), /* @__PURE__ */ jsx("hr", { className: `${CLASS_NAME$36}__hr` })] });
	}
	function renderThumbnail() {
		if (imageUrl !== void 0) return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$36}__thumbnail`,
			"aria-hidden": "true",
			children: /* @__PURE__ */ jsx("img", {
				src: imageUrl,
				className: `${CLASS_NAME$36}__thumbnail-image`
			})
		});
		else if (typeStyle.icon) return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$36}__thumbnail`,
			"aria-hidden": "true",
			children: /* @__PURE__ */ jsx("img", {
				src: typeStyle.icon,
				className: cx(`${CLASS_NAME$36}__thumbnail-icon`, typeStyle.iconMonochrome ? `${CLASS_NAME$36}__thumbnail-icon--monochrome` : void 0)
			})
		});
		return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$36}__thumbnail`,
			"aria-hidden": "true",
			children: typesLabel.length > 0 ? typesLabel.charAt(0).toUpperCase() : "✳"
		});
	}
	const onDoubleClick = (e) => {
		e.preventDefault();
		e.stopPropagation();
		model.history.execute(setElementExpanded(element, !element.isExpanded));
	};
	return /* @__PURE__ */ jsxs("div", {
		style: rootStyle,
		className: cx(CLASS_NAME$36, `${CLASS_NAME$36}--standalone`, getEntityAuthoredStatusClass(data, editor.authoringState)),
		onDoubleClick,
		children: [/* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$36}__main`,
			children: /* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$36}__body`,
				children: [/* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$36}__body-horizontal`,
					children: [renderThumbnail(), /* @__PURE__ */ jsxs("div", {
						className: `${CLASS_NAME$36}__body-content`,
						children: [/* @__PURE__ */ jsx("div", {
							title: typesLabel,
							className: `${CLASS_NAME$36}__type`,
							children: /* @__PURE__ */ jsx("div", {
								className: `${CLASS_NAME$36}__type-value`,
								children: renderTypes(data)
							})
						}), /* @__PURE__ */ jsx(WithFetchStatus, {
							type: "element",
							target: data.id,
							children: /* @__PURE__ */ jsx("div", {
								className: `${CLASS_NAME$36}__label`,
								title: label,
								children: label
							})
						})]
					})]
				}), hasPinnedProperties(data, pinnedProperties) ? /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$36}__pinned-props`,
					children: /* @__PURE__ */ jsx(PropertyList, {
						data,
						shouldInclude: (iri) => isPinnedProperty(iri, pinnedProperties)
					})
				}) : null]
			})
		}), isExpanded ? /* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$36}__dropdown`,
			children: [imageUrl === void 0 ? null : /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$36}__photo`,
				children: /* @__PURE__ */ jsx("img", {
					src: imageUrl,
					className: `${CLASS_NAME$36}__photo-image`
				})
			}), /* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$36}__dropdown-content`,
				children: [
					renderIri(data),
					/* @__PURE__ */ jsx(PropertyList, { data }),
					showActions && editor.inAuthoringMode ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("hr", {
						className: `${CLASS_NAME$36}__hr`,
						"data-reactodia-no-export": "true"
					}), /* @__PURE__ */ jsx(Actions, {
						target: element,
						entityContext,
						translation: t
					})] }) : null
				]
			})]
		}) : null]
	});
}
var DEFAULT_PAGE_SIZE$1 = 10;
var DEFAULT_PAGE_SIZES = [
	5,
	10,
	15,
	20,
	30
];
/**
* Default entity group template component.
*
* The template supports displaying only {@link EntityGroup},
* otherwise nothing will be rendered.
*
* The template supports the following template state:
*   - {@link TemplateProperties.GroupPageIndex}
*   - {@link TemplateProperties.GroupPageSize}
*
* Entities can be ungroup from the element with a corresponding button.
*
* @category Components
* @see {@link StandardTemplate}
*/
function StandardEntityGroup(props) {
	const { element, elementState, groupPageSize = DEFAULT_PAGE_SIZE$1, groupPageSizes = DEFAULT_PAGE_SIZES } = props;
	const { canvas } = useCanvas();
	const workspace = useWorkspace();
	const { getElementStyle } = workspace;
	if (!(element instanceof EntityGroup)) return null;
	const items = element.items;
	const { color: groupColor } = getElementStyle(element);
	const groupStyle = { "--reactodia-standard-group-color": groupColor };
	let pageSize = elementState.get(TemplateProperties.GroupPageSize) ?? groupPageSize;
	pageSize = Number.isFinite(pageSize) ? pageSize : groupPageSize;
	const pageCount = Math.max(Math.ceil(items.length / pageSize), 1);
	let pageIndex = elementState.get(TemplateProperties.GroupPageIndex) ?? 0;
	pageIndex = Number.isFinite(pageIndex) ? pageIndex : 0;
	pageIndex = Math.min(Math.max(pageIndex, 0), pageCount - 1);
	const pageOffset = pageIndex * pageSize;
	const pageItems = items.slice(pageOffset, Math.min(pageOffset + pageSize, items.length));
	const fillerCount = pageCount === 1 ? 0 : pageOffset + pageSize - items.length;
	return /* @__PURE__ */ jsxs("div", {
		className: cx(CLASS_NAME$36, `${CLASS_NAME$36}--group`),
		style: groupStyle,
		role: "list",
		children: [
			pageItems.map((item) => /* @__PURE__ */ createElement(StandardEntityGroupItem, {
				...props,
				key: item.data.id,
				data: item.data,
				isExpanded: false,
				target: element,
				canvas,
				workspace
			})),
			Array.from({ length: fillerCount }, (_, index) => /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$36}__item-filler`,
				"aria-hidden": true,
				children: "\xA0"
			}, index)),
			/* @__PURE__ */ jsx(GroupPaginator, {
				pageIndex,
				pageCount,
				onChangePage: (page) => element.setElementState(element.elementState.set(TemplateProperties.GroupPageIndex, page)),
				pageSize,
				pageSizes: groupPageSizes,
				onChangePageSize: (size) => element.setElementState(element.elementState.set(TemplateProperties.GroupPageSize, size))
			})
		]
	});
}
function StandardEntityGroupItem(props) {
	const { data, target, canvas, workspace } = props;
	const t = useTranslation();
	const { model, editor, getElementTypeStyle } = workspace;
	useKeyedSyncStore(subscribeElementTypes, data ? data.types : [], model);
	const label = model.locale.formatEntityLabel(data, model.language);
	const iri = model.locale.formatIri(data.id);
	const typesLabel = formatEntityTypes(data, model, t);
	const title = t.text("standard_element.group_item.title", {
		entity: label,
		entityIri: iri,
		entityTypes: typesLabel
	});
	const authoringStatusClass = getEntityAuthoredStatusClass(data, editor.authoringState);
	const { color: baseColor } = getElementTypeStyle(data.types);
	const itemStyle = { "--reactodia-element-style-color": baseColor };
	return /* @__PURE__ */ jsxs("div", {
		className: cx(`${CLASS_NAME$36}__item`, authoringStatusClass),
		style: itemStyle,
		role: "listitem",
		children: [/* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$36}__item-stripe`,
			"aria-hidden": "true"
		}), /* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$36}__item-body`,
			children: [/* @__PURE__ */ jsx(WithFetchStatus, {
				type: "element",
				target: data.id,
				children: /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$36}__label`,
					title,
					children: label
				})
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				className: cx(`${CLASS_NAME$36}__ungroup-one-button`, "reactodia-btn reactodia-btn-default"),
				"data-reactodia-no-export": "true",
				title: t.text("standard_element.ungroup.title"),
				onClick: () => void ungroupSomeEntities(workspace, {
					group: target,
					entities: new Set([data.id]),
					canvas
				})
			})]
		})]
	});
}
function isPinnedProperty(iri, pinned) {
	return Boolean(Object.prototype.hasOwnProperty.call(pinned, iri) && pinned[iri]);
}
function hasPinnedProperties(data, pinned) {
	for (const iri in data.properties) if (Object.prototype.hasOwnProperty.call(data.properties, iri)) {
		if (isPinnedProperty(iri, pinned)) return true;
	}
	return false;
}
function formatEntityTypes(data, model, t) {
	return data.types.length === 0 ? t.text("standard_element.default_type") : model.locale.formatEntityTypeList(data, model.language);
}
function getEntityAuthoredStatusClass(data, state) {
	const event = state.elements.get(data.id);
	if (!event) return;
	switch (event.type) {
		case "entityAdd": return `${CLASS_NAME$36}--new`;
		case "entityChange": return `${CLASS_NAME$36}--changed`;
		case "entityDelete": return `${CLASS_NAME$36}--deleted`;
		default: return;
	}
}
function PropertyList(props) {
	const { data, shouldInclude } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const propertyIris = [];
	for (const iri in data.properties) if (Object.prototype.hasOwnProperty.call(data.properties, iri) && data.properties[iri].length > 0 && (!shouldInclude || shouldInclude(iri))) propertyIris.push(iri);
	useKeyedSyncStore(subscribePropertyTypes, propertyIris, model);
	if (propertyIris.length === 0) return /* @__PURE__ */ jsx("div", { children: t.text("standard_element.no_properties") });
	const properties = propertyIris.map((iri) => {
		const property = model.getPropertyType(iri);
		const selectedValues = t.selectValues(data.properties[iri], model.language);
		return {
			iri,
			label: t.formatLabel(property?.data?.label, iri, model.language),
			values: selectedValues.length === 0 ? data.properties[iri] : selectedValues
		};
	});
	properties.sort((a, b) => a.label.localeCompare(b.label));
	return /* @__PURE__ */ jsx("div", {
		role: "list",
		className: `${CLASS_NAME$36}__properties`,
		children: properties.map(({ iri, label, values }) => {
			return /* @__PURE__ */ jsxs("div", {
				role: "listitem",
				className: `${CLASS_NAME$36}__properties-row`,
				children: [/* @__PURE__ */ jsx(WithFetchStatus, {
					type: "propertyType",
					target: iri,
					children: /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$36}__properties-key`,
						title: t.text("standard_element.property.title", {
							property: label,
							propertyIri: model.locale.formatIri(iri)
						}),
						children: label
					})
				}), /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$36}__properties-values`,
					children: values.map((term, index) => /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$36}__properties-value`,
						title: term.value,
						lang: term.termType === "Literal" && term.language ? term.language : void 0,
						children: term.value
					}, index))
				})]
			}, iri);
		})
	});
}
/**
* @deprecated
*/
function Actions(props) {
	const { target, entityContext: { canEdit, canDelete, onEdit, onDelete }, translation: t } = props;
	const SPINNER_WIDTH = 15;
	const SPINNER_HEIGHT = 12;
	return /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$36}__actions`,
		"data-reactodia-no-export": "true",
		children: [/* @__PURE__ */ jsx("button", {
			type: "button",
			className: cx(`${CLASS_NAME$36}__delete-button`, "reactodia-btn reactodia-btn-default"),
			title: canDelete ? t.text("standard_element.delete.title") : t.text("standard_element.delete.title_disabled"),
			disabled: !canDelete,
			onClick: onDelete,
			children: canDelete === void 0 ? /* @__PURE__ */ jsx(HtmlSpinner, {
				width: SPINNER_WIDTH,
				height: SPINNER_HEIGHT
			}) : t.text("standard_element.delete.label")
		}), /* @__PURE__ */ jsx("button", {
			type: "button",
			className: cx(`${CLASS_NAME$36}__edit-button`, "reactodia-btn reactodia-btn-default"),
			title: canEdit ? t.text("standard_element.edit.title") : t.text("standard_element.edit.title_disabled"),
			disabled: !canEdit,
			onClick: () => onEdit(target),
			children: canEdit === void 0 ? /* @__PURE__ */ jsx(HtmlSpinner, {
				width: SPINNER_WIDTH,
				height: SPINNER_HEIGHT
			}) : t.text("standard_element.edit.label")
		})]
	});
}
//#endregion
//#region src/templates/standardLink.tsx
/**
* Default link template to display a {@link RelationLink} or
* {@link RelationGroup} on the canvas.
*
* Uses {@link StandardRelation} to display the link itself.
*
* @category Constants
* @see {@link StandardRelation}
*/
var StandardLinkTemplate = {
	markerTarget: LinkMarkerArrowhead,
	renderLink: (props) => /* @__PURE__ */ jsx(StandardRelation, { ...props })
};
/**
* An alias for {@link StandardLinkTemplate}.
*
* @category Constants
* @deprecated Use {@link StandardLinkTemplate} directly instead.
*/
var DefaultLinkTemplate = StandardLinkTemplate;
var CLASS_NAME$35 = "reactodia-standard-link";
var PROPERTY_CLASS = `${CLASS_NAME$35}__property`;
var LABEL_CLASS = `${CLASS_NAME$35}__label`;
/**
* Default link template component.
*
* The template supports displaying any diagram links, including relation
* links and relation groups.
*
* The template supports the following template state:
*   - {@link TemplateProperties.LayoutOnly}
*
* {@link RenameLinkProvider} can be used to display a different label
* than the default one based on the relation data or the link type.
*
* @category Components
*/
function StandardRelation(props) {
	const { link, className, path, pathProps, markerSource, markerTarget, getPathPosition, route, primaryLabelProps, prependLabels = null, children } = props;
	const { model, view: { renameLinkProvider } } = useWorkspace();
	const t = useTranslation();
	useKeyedSyncStore(subscribeLinkTypes, [link.typeId], model);
	const renamedLabel = renameLinkProvider?.getLabel(link);
	let labelContent = null;
	if (model.getLinkVisibility(link.typeId) === "visible") {
		const linkType = model.getLinkType(link.typeId);
		const label = renamedLabel ?? t.formatLabel(linkType?.data?.label, link.typeId, model.language);
		labelContent = /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsx(LinkLabel, {
				...primaryLabelProps,
				primary: true,
				textAnchor: route?.labelTextAnchor ?? primaryLabelProps?.textAnchor,
				className: cx(LABEL_CLASS, renamedLabel ? `${LABEL_CLASS}--renamed` : void 0, primaryLabelProps?.className),
				title: primaryLabelProps?.title ?? t.text("standard_link.label.title", {
					relation: label,
					relationIri: model.locale.formatIri(link.typeId)
				}),
				link,
				position: getPathPosition(.5),
				children: renamedLabel ? /* @__PURE__ */ jsx("span", { children: label }) : /* @__PURE__ */ jsx(WithFetchStatus, {
					type: "linkType",
					target: link.typeId,
					children: /* @__PURE__ */ jsx("span", { children: label })
				})
			}),
			prependLabels,
			link instanceof RelationLink ? /* @__PURE__ */ jsx(LinkProperties, { ...props }) : null,
			link instanceof RelationGroup ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(LinkLabel, {
				className: `${CLASS_NAME$35}__source-count`,
				link,
				position: getPathPosition(.1),
				title: t.text("standard_link.group_source.title", { value: link.itemSources.size }),
				children: t.text("standard_link.group_source.value", { value: link.itemSources.size })
			}), /* @__PURE__ */ jsx(LinkLabel, {
				className: `${CLASS_NAME$35}__target-count`,
				link,
				position: getPathPosition(.9),
				title: t.text("standard_link.group_target.title", { value: link.itemTargets.size }),
				children: t.text("standard_link.group_target.value", { value: link.itemTargets.size })
			})] }) : null
		] });
	}
	const { linkState } = link;
	const stroke = pathProps?.stroke;
	return /* @__PURE__ */ jsxs("g", {
		className: cx(CLASS_NAME$35, link instanceof RelationGroup ? `${CLASS_NAME$35}--group` : void 0, className),
		children: [
			/* @__PURE__ */ jsx(LinkPath, {
				path,
				pathProps: {
					fill: "none",
					...pathProps,
					className: cx(`${CLASS_NAME$35}__path`, pathProps?.className),
					stroke,
					strokeDasharray: linkState.get(TemplateProperties.LayoutOnly) ? "5,5" : pathProps?.strokeDasharray
				},
				markerSource,
				markerTarget
			}),
			labelContent,
			link.vertices.length === 0 ? null : /* @__PURE__ */ jsx(LinkVertices, {
				linkId: link.id,
				vertices: link.vertices,
				fill: stroke
			}),
			children
		]
	});
}
/**
* An alias for {@link StandardRelation}.
*
* @category Components
* @deprecated Use {@link StandardRelation} directly instead.
*/
function DefaultLink(props) {
	return /* @__PURE__ */ jsx(StandardRelation, { ...props });
}
function LinkProperties(props) {
	const { link, getPathPosition, route, propertyLabelProps, propertyLabelStartLine = 1 } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const { data } = link;
	const propertyIris = [];
	for (const iri in data.properties) if (Object.prototype.hasOwnProperty.call(data.properties, iri) && data.properties[iri].length > 0) propertyIris.push(iri);
	useKeyedSyncStore(subscribePropertyTypes, propertyIris, model);
	const properties = [];
	for (const iri of propertyIris) {
		const property = model.getPropertyType(iri);
		const selectedValues = t.selectValues(data.properties[iri], model.language);
		const style = typeof propertyLabelProps === "function" ? propertyLabelProps(iri) : propertyLabelProps;
		if (style !== null) properties.push({
			iri,
			label: t.formatLabel(property?.data?.label, iri, model.language),
			values: selectedValues.length === 0 ? data.properties[iri] : selectedValues,
			style
		});
	}
	properties.sort((a, b) => a.label.localeCompare(b.label));
	return /* @__PURE__ */ jsx(Fragment, { children: properties.map((property, index) => /* @__PURE__ */ jsxs(LinkLabel, {
		...propertyLabelProps,
		link,
		position: getPathPosition(.5),
		line: propertyLabelStartLine + index,
		textAnchor: route?.labelTextAnchor ?? property.style?.textAnchor,
		className: cx(PROPERTY_CLASS, property.style?.className),
		title: property.style?.title ?? t.text("standard_link.property.title", {
			property: property.label,
			propertyIri: model.locale.formatIri(property.iri)
		}),
		children: [/* @__PURE__ */ jsx(WithFetchStatus, {
			type: "propertyType",
			target: property.iri,
			children: /* @__PURE__ */ jsxs("span", { children: [property.label, ":\xA0"] })
		}), property.values.map((v) => v.value).join(", ")]
	}, property.iri)) });
}
//#endregion
//#region src/widgets/utility/dragLinkMover.tsx
function DragLinkMover(props) {
	const { canvas, model } = useCanvas();
	return /* @__PURE__ */ jsx(CanvasPlaceAt, {
		layer: "overElements",
		children: /* @__PURE__ */ jsx(DragLinkMoverInner, {
			...props,
			model,
			canvas
		})
	});
}
var CLASS_NAME$34 = "reactodia-drag-link-mover";
var NO_CONNECTION = {
	allowed: false,
	connect: () => Promise.resolve(),
	moveSource: () => Promise.resolve(),
	moveTarget: () => Promise.resolve()
};
var DragLinkMoverInner = class extends React.Component {
	listener = new EventObserver();
	cancellation = new AbortController();
	canDropOnElementCancellation = new AbortController();
	temporaryLink;
	temporaryElement;
	oldLink;
	constructor(props) {
		super(props);
		this.state = {};
	}
	componentDidMount() {
		const { canvas, operation } = this.props;
		this.cancellation = new AbortController();
		switch (operation.mode) {
			case "connect":
				this.beginCreatingLink(operation);
				break;
			case "moveSource":
			case "moveTarget":
				this.beginMovingLink(operation);
				break;
			default: throw new Error(`Unknown edit mode: "${operation.mode}"`);
		}
		this.forceUpdate();
		this.queryCanConnectToAny();
		this.listener.listen(canvas.events, "changeTransform", () => this.forceUpdate());
		document.addEventListener("pointermove", this.onPointerMove);
		document.addEventListener("pointerup", this.onPointerUp);
		document.addEventListener("pointercancel", this.onPointerUp);
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.cancellation.abort();
		this.canDropOnElementCancellation.abort();
		document.removeEventListener("pointermove", this.onPointerMove);
		document.removeEventListener("pointerup", this.onPointerUp);
		document.removeEventListener("pointercancel", this.onPointerUp);
		this.cleanup();
	}
	beginCreatingLink(operation) {
		const { createLink, model } = this.props;
		const { source, point } = operation;
		const batch = model.history.startBatch();
		const temporaryElement = this.createTemporaryElement(point);
		const temporaryLink = createLink(source, temporaryElement, void 0);
		batch.discard();
		this.temporaryElement = temporaryElement;
		this.temporaryLink = temporaryLink;
	}
	beginMovingLink(operation) {
		const { createLink, model } = this.props;
		const { link, point } = operation;
		const batch = model.history.startBatch();
		this.oldLink = link;
		model.removeLink(link.id);
		const temporaryElement = this.createTemporaryElement(point);
		const temporaryLink = operation.mode === "moveSource" ? createLink(temporaryElement, model.targetOf(link), link) : createLink(model.sourceOf(link), temporaryElement, link);
		batch.discard();
		this.temporaryElement = temporaryElement;
		this.temporaryLink = temporaryLink;
	}
	createTemporaryElement(point) {
		const { model } = this.props;
		const temporaryElement = new VoidElement({});
		temporaryElement.setPosition(point);
		model.addElement(temporaryElement);
		return temporaryElement;
	}
	onPointerMove = (e) => {
		const { model, canvas } = this.props;
		const { targetElement, waitingForMetadata } = this.state;
		e.preventDefault();
		e.stopPropagation();
		if (waitingForMetadata) return;
		const point = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
		this.temporaryElement.setPosition(point);
		const newTargetElement = findElementAtPoint(model.elements, point, canvas.renderingState);
		if (newTargetElement !== targetElement) this.queryCanDropOnElement(newTargetElement);
		this.setState({ targetElement: newTargetElement });
	};
	queryCanConnectToAny() {
		const { canConnect, model } = this.props;
		this.setState({ anyConnection: void 0 });
		const link = this.temporaryLink;
		mapAbortedToNull(canConnect(model.getElement(link.sourceId), link, void 0, this.cancellation.signal), this.cancellation.signal).then((connection) => {
			if (connection === null) return;
			this.setState({ anyConnection: connection });
		}, (error) => {
			console.error("Error calling canConnect() without target", error);
			this.setState({ anyConnection: NO_CONNECTION });
		});
	}
	queryCanDropOnElement(targetElement) {
		const { operation, canConnect, model } = this.props;
		this.canDropOnElementCancellation.abort();
		this.canDropOnElementCancellation = new AbortController();
		if (!targetElement) {
			this.setState({ targetConnection: NO_CONNECTION });
			return;
		}
		this.setState({ targetConnection: void 0 });
		const link = this.temporaryLink;
		let source;
		let target;
		switch (operation.mode) {
			case "connect":
			case "moveTarget":
				source = link ? model.getElement(link.sourceId) : void 0;
				target = targetElement;
				break;
			case "moveSource":
				source = targetElement;
				target = link ? model.getElement(link.targetId) : void 0;
				break;
		}
		if (!(source && target && link)) {
			this.setState({ targetConnection: NO_CONNECTION });
			return;
		}
		const signal = this.canDropOnElementCancellation.signal;
		mapAbortedToNull(canConnect(source, link, target, signal), signal).then((connection) => {
			if (connection === null) return;
			this.setState({ targetConnection: connection });
		}, (error) => {
			console.error("Error calling canConnect() with target", error);
			this.setState({ targetConnection: NO_CONNECTION });
		});
	}
	onPointerUp = (e) => {
		const { canvas } = this.props;
		if (this.state.waitingForMetadata) return;
		this.setState({ waitingForMetadata: true });
		const selectedPosition = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
		this.executeEditOperation(selectedPosition);
	};
	async executeEditOperation(selectedPosition) {
		const { operation, canvas, model } = this.props;
		try {
			const { targetElement, anyConnection, targetConnection } = this.state;
			const batch = model.history.startBatch();
			const restoredLink = this.restoreOldLink();
			batch.discard();
			const connection = targetElement ? targetConnection : anyConnection;
			if (connection?.allowed) switch (operation.mode) {
				case "connect":
					await connection.connect(operation.source, targetElement, selectedPosition, canvas, this.cancellation.signal);
					break;
				case "moveSource":
					await connection.moveSource(restoredLink, targetElement, canvas, this.cancellation.signal);
					break;
				case "moveTarget":
					await connection.moveTarget(restoredLink, targetElement, canvas, this.cancellation.signal);
					break;
			}
		} finally {
			this.cleanupAndFinish();
		}
	}
	cleanupAndFinish() {
		const { onFinish } = this.props;
		this.cleanup();
		onFinish?.();
	}
	cleanup() {
		const { cleanupLink, model } = this.props;
		const batch = model.history.startBatch();
		model.removeElement(this.temporaryElement.id);
		model.removeLink(this.temporaryLink.id);
		cleanupLink?.(this.temporaryLink);
		this.restoreOldLink();
		batch.discard();
		this.setState({ waitingForMetadata: false });
	}
	restoreOldLink() {
		const { model } = this.props;
		const restoredLink = this.oldLink;
		this.oldLink = void 0;
		if (restoredLink) model.addLink(restoredLink);
		return restoredLink;
	}
	render() {
		const { model, canvas } = this.props;
		const { waitingForMetadata } = this.state;
		if (!this.temporaryLink) return null;
		const transform = canvas.metrics.transform;
		const renderingState = canvas.renderingState;
		return /* @__PURE__ */ jsxs(SvgPaperLayer, {
			paperTransform: transform,
			className: CLASS_NAME$34,
			children: [
				/* @__PURE__ */ jsx(LinkMarkers, {
					model,
					renderingState
				}),
				this.renderHighlight(),
				this.renderCanDropIndicator(),
				waitingForMetadata ? null : /* @__PURE__ */ jsx(LinkLayer, {
					model,
					renderingState,
					shouldRenderLink: (link) => link === this.temporaryLink
				})
			]
		});
	}
	renderHighlight() {
		const { canvas } = this.props;
		const { targetElement, targetConnection, waitingForMetadata } = this.state;
		if (!targetElement) return null;
		const { x, y, width, height } = boundsOf(targetElement, canvas.renderingState);
		if (targetConnection === void 0 || waitingForMetadata) return /* @__PURE__ */ jsxs("g", {
			transform: `translate(${x},${y})`,
			children: [/* @__PURE__ */ jsx("rect", {
				className: `${CLASS_NAME$34}__highlight-overlay`,
				width,
				height
			}), /* @__PURE__ */ jsx(Spinner, {
				size: 30,
				position: {
					x: width / 2,
					y: height / 2
				}
			})]
		});
		return /* @__PURE__ */ jsx("rect", {
			className: targetConnection?.allowed ? `${CLASS_NAME$34}__highlight-allow` : `${CLASS_NAME$34}__highlight-deny`,
			x,
			y,
			width,
			height
		});
	}
	renderCanDropIndicator() {
		const { targetElement, anyConnection, waitingForMetadata } = this.state;
		if (targetElement) return null;
		const { x, y } = this.temporaryElement.position;
		let indicator;
		if (anyConnection === void 0) indicator = /* @__PURE__ */ jsx(Spinner, {
			size: 1.2,
			position: {
				x: .5,
				y: -.5
			}
		});
		else if (anyConnection?.allowed) indicator = /* @__PURE__ */ jsx("path", {
			className: `${CLASS_NAME$34}__drop-allow`,
			d: "M0.5,0 L0.5,-1 M0,-0.5 L1,-0.5",
			strokeWidth: .2
		});
		else indicator = /* @__PURE__ */ jsxs("g", {
			className: `${CLASS_NAME$34}__drop-deny`,
			children: [/* @__PURE__ */ jsx("circle", {
				cx: "0.5",
				cy: "-0.5",
				r: "0.5",
				fill: "none",
				strokeWidth: .2
			}), /* @__PURE__ */ jsx("path", {
				d: "M0.5,0 L0.5,-1",
				strokeWidth: .2,
				transform: "rotate(-45 0.5 -0.5)"
			})]
		});
		return /* @__PURE__ */ jsxs("g", {
			transform: `translate(${x} ${y})scale(40)`,
			children: [/* @__PURE__ */ jsx("rect", {
				className: `${CLASS_NAME$34}__drop-underlay`,
				x: -.5,
				y: -.5,
				width: 1,
				height: 1,
				rx: .25,
				ry: .25
			}), waitingForMetadata ? /* @__PURE__ */ jsx(Spinner, { size: .8 }) : /* @__PURE__ */ jsx("g", {
				transform: `translate(0.5, -0.5)scale(0.25)`,
				children: indicator
			})]
		});
	}
};
//#endregion
//#region src/widgets/annotation/annotationLinkMover.tsx
function AnnotationLinkMover(props) {
	const { operation, onFinish } = props;
	const { model } = useCanvas();
	return /* @__PURE__ */ jsx(DragLinkMover, {
		operation,
		createLink: (source, target, original) => {
			const link = new AnnotationLink({
				sourceId: source.id,
				targetId: target.id,
				vertices: original?.vertices,
				linkState: original?.linkState
			});
			model.addLink(link);
			return link;
		},
		canConnect: async (source, link, target) => {
			return new AnnotationLinkConnection(source instanceof AnnotationElement && link instanceof AnnotationLink && target !== void 0 && !model.getElementLinks(source).some((link) => link instanceof AnnotationLink && link.sourceId === source.id && link.targetId === target.id), model);
		},
		onFinish
	});
}
var AnnotationLinkConnection = class {
	allowed;
	model;
	constructor(allowed, model) {
		this.allowed = allowed;
		this.model = model;
	}
	async connect(source, target) {
		if (!target) return;
		this.model.addLink(new AnnotationLink({
			sourceId: source.id,
			targetId: target.id
		}));
	}
	async moveSource(link, newSource) {
		const batch = this.model.history.startBatch();
		this.model.removeLink(link.id);
		const movedLink = new AnnotationLink({
			id: link.id,
			sourceId: newSource.id,
			targetId: link.targetId,
			vertices: link.vertices,
			linkState: link.linkState
		});
		this.model.addLink(movedLink);
		batch.store();
		this.model.setSelection([movedLink]);
	}
	async moveTarget(link, newTarget) {
		const batch = this.model.history.startBatch();
		this.model.removeLink(link.id);
		const movedLink = new AnnotationLink({
			id: link.id,
			sourceId: link.sourceId,
			targetId: newTarget.id,
			vertices: link.vertices,
			linkState: link.linkState
		});
		this.model.addLink(movedLink);
		batch.store();
		this.model.setSelection([movedLink]);
	}
};
//#endregion
//#region src/widgets/annotation/annotationSupport.tsx
/**
* Canvas widget component to provide UI support for {@link AnnotationElement annotations}.
*
* @category Components
*/
function AnnotationSupport(props) {
	const { getCommandBus } = useWorkspace();
	const { canvas, model } = useCanvas();
	const [mover, setMover] = React.useState(null);
	React.useEffect(() => {
		const commands = getCommandBus(AnnotationTopic);
		const listener = new EventObserver();
		listener.listen(commands, "findCapabilities", (e) => {
			e.capabilities.push({});
		});
		listener.listen(commands, "startDragOperation", ({ operation }) => {
			setMover(/* @__PURE__ */ jsx(AnnotationLinkMover, {
				operation,
				onFinish: () => setMover(null)
			}));
		});
		listener.listen(commands, "createAnnotation", ({ targets, content, position }) => {
			const batch = model.history.startBatch();
			const annotation = new AnnotationElement({ elementState: content ? TemplateState.empty.set(TemplateProperties.AnnotationContent, content) : void 0 });
			model.addElement(annotation);
			if (position) annotation.setPosition(position);
			else {
				const outermost = getOutermostElement(targets, canvas.renderingState);
				if (outermost) {
					annotation.setPosition(outermost.position);
					canvas.renderingState.syncUpdate();
					batch.history.execute(placeElementsAroundTarget({
						elements: [annotation],
						target: outermost,
						graph: model,
						sizeProvider: canvas.renderingState
					}));
				}
			}
			for (const element of targets) model.addLink(new AnnotationLink({
				sourceId: annotation.id,
				targetId: element.id
			}));
			batch.store();
			canvas.renderingState.syncUpdate();
		});
		return () => listener.stopListening();
	}, []);
	return /* @__PURE__ */ jsx(Fragment, { children: mover });
}
function getOutermostElement(elements, sizeProvider) {
	let maxDistance = 0;
	let outermost;
	for (const element of elements) {
		const bounds = boundsOf(element, sizeProvider);
		const distance = Vector.length(Rect.center(bounds));
		if (distance >= maxDistance) {
			maxDistance = distance;
			outermost = element;
		}
	}
	return outermost;
}
//#endregion
//#region src/widgets/editorForms/renameLinkForm.tsx
var FORM_CLASS$6 = "reactodia-form";
function RenameLinkForm(props) {
	const { link, onFinish } = props;
	const { view: { renameLinkProvider } } = useWorkspace();
	const t = useTranslation();
	const defaultLabel = useDefaultLinkLabel(link);
	const [customLabel, setCustomLabel] = React.useState(renameLinkProvider?.getLabel(link) ?? defaultLabel);
	const onApply = () => {
		if (renameLinkProvider) renameLinkProvider.setLabel(link, customLabel);
		onFinish();
	};
	return /* @__PURE__ */ jsxs("div", {
		className: FORM_CLASS$6,
		children: [/* @__PURE__ */ jsx("div", {
			className: `${FORM_CLASS$6}__body`,
			children: /* @__PURE__ */ jsxs("div", {
				className: `${FORM_CLASS$6}__row`,
				children: [/* @__PURE__ */ jsx("label", { children: t.text("visual_authoring.rename_link.label.label") }), /* @__PURE__ */ jsx("input", {
					className: "reactodia-form-control",
					placeholder: defaultLabel,
					autoFocus: true,
					value: customLabel,
					onChange: (e) => setCustomLabel(e.target.value)
				})]
			})
		}), /* @__PURE__ */ jsxs("div", {
			className: `${FORM_CLASS$6}__controls`,
			children: [/* @__PURE__ */ jsx("button", {
				className: `reactodia-btn reactodia-btn-primary ${FORM_CLASS$6}__apply-button`,
				title: t.textOptional("visual_authoring.rename_link.dialog.apply.title") ?? t.text("visual_authoring.dialog.apply.title"),
				onClick: onApply,
				children: t.textOptional("visual_authoring.rename_link.dialog.apply.label") ?? t.text("visual_authoring.dialog.apply.label")
			}), /* @__PURE__ */ jsx("button", {
				className: "reactodia-btn reactodia-btn-secondary",
				title: t.text("visual_authoring.dialog.cancel.title"),
				onClick: onFinish,
				children: t.text("visual_authoring.dialog.cancel.label")
			})]
		})]
	});
}
function useDefaultLinkLabel(link) {
	const { model } = useWorkspace();
	const t = useTranslation();
	const linkType = link instanceof RelationLink || link instanceof RelationGroup ? model.getLinkType(link.typeId) : void 0;
	const linkTypeLabel = useSyncStore(useEventStore(linkType?.events, "changeData"), () => linkType?.data?.label);
	return React.useMemo(() => {
		if (link instanceof RelationLink || link instanceof RelationGroup) return t.formatLabel(linkTypeLabel, link.typeId, model.language);
		else return "";
	}, [
		link,
		linkTypeLabel,
		model.language
	]);
}
//#endregion
//#region src/widgets/annotation/renameLinkSupport.tsx
function RenameLinkSupport() {
	const { overlay, getCommandBus } = useWorkspace();
	const t = useTranslation();
	React.useLayoutEffect(() => {
		const onRenameLink = ({ target: link }) => {
			overlay.showDialog({
				target: link,
				dialogType: BuiltinDialogType.renameLink,
				style: {
					defaultSize: {
						width: 300,
						height: 165
					},
					resizableBy: "x",
					caption: t.text("visual_authoring.rename_link.dialog.caption")
				},
				content: /* @__PURE__ */ jsx(RenameLinkForm, {
					link,
					onFinish: () => overlay.hideDialog()
				})
			});
		};
		const listener = new EventObserver();
		const commands = getCommandBus(AnnotationTopic);
		listener.listen(commands, "renameLink", onRenameLink);
		const authoringCommands = getCommandBus(VisualAuthoringTopic);
		listener.listen(authoringCommands, "renameLink", onRenameLink);
		return () => listener.stopListening();
	}, []);
	return null;
}
//#endregion
//#region src/widgets/utility/focusGroup.tsx
/**
* Utility component to implement [accessible](https://www.w3.org/TR/wai-aria/) focus container.
*
* The root component acts as [focus group](https://www.w3.org/TR/wai-aria/#managingfocus)
* with pre-defined methods to provide keyboard navigation between items (e.g. with arrow keys)
* and `Tab` key to move focus to/from container or within currently focused item
* (if a {@link useFocusGroupItem group item} has multiple focusable children).
*
* @category Components
* @see {@link useFocusGroup}
* @see {@link useFocusGroupItem}
*/
function FocusGroup(props) {
	const { children } = props;
	const [controller] = React.useState(() => new StatefulFocusGroupController());
	const content = children({
		ref: controller._setRoot,
		controller
	});
	const [focusableVersion, setFocusableVersion] = React.useState(0);
	React.useLayoutEffect(() => controller.ensureFocusable(), [focusableVersion, content]);
	React.useEffect(() => {
		let queued = false;
		const listener = () => {
			if (!queued) {
				queued = true;
				setTimeout(() => {
					setFocusableVersion((v) => (v + 1) % Number.MAX_SAFE_INTEGER);
					queued = false;
				}, 0);
			}
		};
		controller.events.on("lostFocusable", listener);
		return () => controller.events.off("lostFocusable", listener);
	}, []);
	return /* @__PURE__ */ jsx(FocusGroupContext.Provider, {
		value: controller,
		children: content
	});
}
var FocusGroupContext = React.createContext(null);
/**
* @category Hooks
* @see {@link FocusGroup}
*/
function useFocusGroup() {
	const controller = React.useContext(FocusGroupContext);
	if (!controller) throw new Error("Reactodia: missing <FocusGroup> context");
	return controller;
}
/**
* Hook to render a focusable item inside {@link FocusGroup}.
*
* Focusable item allows multiple "tab-indexable" sub-children inside reachable via `Tab` key
* when the item is focused. When moving focus with {@link FocusGroupController.focusNext}
* and other methods, the first "tab-indexable" sub-child would be focused.
*
* Focusable item element ***must** use provided {@link UseFocusGroupItemResult.ref ref}
* to properly maintain the focusable state.
*
* @category Hooks
* @see {@link FocusGroup}
*/
function useFocusGroupItem(params) {
	const { debugLabel, active = true } = params ?? {};
	const controller = React.useContext(FocusGroupContext);
	if (!controller) throw new Error("Reactodia: missing <FocusGroup> context");
	const [token] = React.useState(new FocusToken(debugLabel));
	const itemRef = React.useCallback((element) => {
		controller._bindFocusable(token, element);
	}, [controller, token]);
	const isFocused = useObservedProperty(controller.events, "changeFocus", () => controller._getFocus() === token);
	return {
		ref: itemRef,
		tabIndex: active ? isFocused ? 0 : -1 : void 0
	};
}
var FocusToken = class {
	debugLabel;
	constructor(debugLabel) {
		this.debugLabel = debugLabel;
	}
};
var StatefulFocusGroupController = class {
	_source = new EventSource();
	events = this._source;
	_root = null;
	_tokens = /* @__PURE__ */ new WeakMap();
	_focus;
	getRoot() {
		return this._root;
	}
	_setRoot = (root) => {
		this._root = root;
		this._focus = void 0;
	};
	_getFocus() {
		return this._focus;
	}
	_bindFocusable(token, element) {
		if (element) this._tokens.set(element, token);
		else if (token === this._focus) this._source.trigger("lostFocusable", { source: this });
	}
	_isItem = (element) => {
		return this._tokens.has(element);
	};
	_isFocusableItem = (element) => {
		return this._isItem(element) && Boolean(element.hasAttribute("tabindex") || element.querySelector("[tabindex]"));
	};
	focusAt(leaf) {
		if (!this._root) return;
		const item = findParentWithin(leaf, this._root, this._isItem);
		if (item) this._changeFocused(item);
	}
	focusPrevious(params) {
		const { from } = params;
		if (!this._root) return;
		const item = findParentWithin(from, this._root, this._isItem);
		if (item) {
			const previous = findPreviousWithin(item, this._root, this._isFocusableItem);
			this._tryFocusOnItemElement(previous);
		}
	}
	focusNext(params) {
		const { from } = params;
		if (!this._root) return;
		const item = findParentWithin(from, this._root, this._isItem);
		if (item) {
			const next = findNextWithin(item, this._root, this._isFocusableItem);
			this._tryFocusOnItemElement(next);
		}
	}
	focusParent(params) {
		const { from } = params;
		if (!this._root) return;
		const item = findParentWithin(from, this._root, this._isItem);
		if (!item) return;
		const parent = item.parentElement ? findParentWithin(item.parentElement, this._root, this._isItem) : void 0;
		this._tryFocusOnItemElement(parent);
	}
	ensureFocusable(params) {
		const { reset = false } = params ?? {};
		if (!this._root) return;
		const focusedToken = reset ? void 0 : this._focus;
		if (focusedToken) for (const focusable of this._root.querySelectorAll("[tabindex=\"0\"]")) {
			const item = findParentWithin(focusable, this._root, this._isItem);
			if ((item ? this._tokens.get(item) : void 0) === focusedToken) return;
		}
		let foundFocusable = false;
		for (const focusable of this._root.querySelectorAll("[tabindex]")) {
			const item = findParentWithin(focusable, this._root, this._isItem);
			if (item && this._tokens.has(item)) {
				foundFocusable = true;
				this._changeFocused(item);
				break;
			}
		}
		if (reset && !foundFocusable) this._focus = void 0;
	}
	_tryFocusOnItemElement(target) {
		if (target) {
			const focusable = target.hasAttribute("tabindex") ? target : target.querySelector("[tabindex]");
			if (focusable instanceof HTMLElement) focusable.focus();
			this._changeFocused(target);
		}
	}
	_changeFocused(target) {
		const token = this._tokens.get(target);
		if (token && this._focus !== token) {
			this._focus = token;
			this._source.trigger("changeFocus", { source: this });
		}
	}
	defaultClick = (e) => {
		if (e.target instanceof HTMLElement) this.focusAt(e.target);
	};
	defaultKeyDown = (e) => {
		if (e.key === "ArrowUp") {
			e.preventDefault();
			if (e.target instanceof HTMLElement) this.focusPrevious({ from: e.target });
		} else if (e.key === "ArrowDown") {
			e.preventDefault();
			if (e.target instanceof HTMLElement) this.focusNext({ from: e.target });
		}
	};
};
//#endregion
//#region src/widgets/utility/dropdown.tsx
var CLASS_NAME$33 = "reactodia-dropdown";
/**
* Utility component to display a controllable dropdown.
*
* @category Components
*/
function Dropdown(props) {
	const { className, direction = "down", expanded, toggle, onClickOutside, children, ...otherProps } = props;
	const menuRef = React.useRef(null);
	React.useLayoutEffect(() => {
		if (onClickOutside && expanded) {
			const closeMenu = (e) => {
				if (e.target instanceof Node && !menuRef.current?.contains(e.target)) onClickOutside();
			};
			document.body.addEventListener("pointerdown", closeMenu);
			return () => document.body.removeEventListener("pointerdown", closeMenu);
		}
	}, [onClickOutside, expanded]);
	return /* @__PURE__ */ jsxs("div", {
		...otherProps,
		ref: menuRef,
		className: cx(className, CLASS_NAME$33, direction === "down" ? `${CLASS_NAME$33}--down` : `${CLASS_NAME$33}--up`, expanded ? `${CLASS_NAME$33}--expanded` : `${CLASS_NAME$33}--collapsed`),
		children: [
			direction === "down" ? toggle : null,
			/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$33}__content`,
				"aria-hidden": !expanded,
				children
			}),
			direction === "up" ? toggle : null
		]
	});
}
var MENU_CLASS_NAME = "reactodia-dropdown-menu";
/**
* Utility component to display a dropdown menu with a list of items.
*
* @category Components
*/
function DropdownMenu(props) {
	const { className, direction, title, children, ...otherProps } = props;
	const [expanded, setExpanded] = React.useState(false);
	const providedContext = React.useMemo(() => ({
		expanded,
		setExpanded
	}), [expanded, setExpanded]);
	const onClickOutside = React.useCallback(() => setExpanded(false), [setExpanded]);
	const onKeyDown = (e, controller) => {
		if (e.key === "Escape") setExpanded(false);
		else controller.defaultKeyDown(e);
	};
	return /* @__PURE__ */ jsx(DropdownMenuContext.Provider, {
		value: providedContext,
		children: /* @__PURE__ */ jsx(FocusGroup, { children: ({ ref, controller }) => /* @__PURE__ */ jsx(Dropdown, {
			...otherProps,
			className: cx(className, MENU_CLASS_NAME),
			"aria-haspopup": "menu",
			direction,
			expanded,
			toggle: /* @__PURE__ */ jsx(DropdownMenuToggleButton, { title }),
			onClickOutside,
			children: /* @__PURE__ */ jsx("div", {
				ref,
				role: "menu",
				className: `${MENU_CLASS_NAME}__items`,
				onClick: controller.defaultClick,
				onKeyDown: (e) => onKeyDown(e, controller),
				children
			})
		}) })
	});
}
var DropdownMenuContext = React.createContext(null);
function useDropdownMenu() {
	const context = React.useContext(DropdownMenuContext);
	if (!context) throw new Error("Missing Reactodia dropdown menu context");
	return context;
}
function DropdownMenuToggleButton(props) {
	const { title } = props;
	const { expanded, setExpanded } = useDropdownMenu();
	const controller = useFocusGroup();
	React.useLayoutEffect(() => {
		if (!expanded) controller.ensureFocusable({ reset: true });
	}, [expanded]);
	return /* @__PURE__ */ jsx("button", {
		type: "button",
		className: cx(`${MENU_CLASS_NAME}__toggle`, "reactodia-btn", "reactodia-btn-default", expanded ? "active" : void 0),
		title,
		onClick: () => setExpanded((value) => !value)
	});
}
var ITEM_CLASS_NAME = "reactodia-dropdown-menu-item";
/**
* Utility component to display a single dropdown menu item.
*
* @category Components
*/
function DropdownMenuItem(props) {
	const { className, title, disabled, onSelect, children, ...otherProps } = props;
	const { expanded, setExpanded } = useDropdownMenu();
	const { ref, tabIndex } = useFocusGroupItem({ active: expanded && !disabled });
	const wrappedOnClick = React.useCallback(() => {
		setExpanded(() => false);
		onSelect?.();
	}, [onSelect, setExpanded]);
	return /* @__PURE__ */ jsx("button", {
		...otherProps,
		ref,
		type: "button",
		role: "menuitem",
		tabIndex: tabIndex ?? -1,
		className: cx(className, ITEM_CLASS_NAME, disabled ? `${ITEM_CLASS_NAME}--disabled` : void 0, "reactodia-btn reactodia-btn-default"),
		disabled,
		title,
		onClick: disabled ? void 0 : wrappedOnClick,
		children
	});
}
/**
* React hook to check if a component is rendered inside a dropdown menu.
*
* @category Hooks
* @see {@link DropdownMenu}
*/
function useInsideDropdown() {
	const menuContext = React.useContext(DropdownMenuContext);
	return Boolean(menuContext);
}
//#endregion
//#region src/widgets/utility/listElementView.tsx
var CLASS_NAME$32 = "reactodia-list-element-view";
/**
* Utility component to display an entity as a draggable list item.
*
* @category Components
*/
function ListElementView(props) {
	const { element, className, highlightText, disabled, selected, onClick, onDragStart, ref, ...otherProps } = props;
	const { model, getElementTypeStyle } = useWorkspace();
	const t = useTranslation();
	useKeyedSyncStore(subscribeElementTypes, element.types, model);
	const { color: elementColor } = getElementTypeStyle(element.types);
	const { h, c, l } = hcl(elementColor);
	const frontColor = selected && !disabled ? hcl(h, c, l * 1.2).toString() : void 0;
	const combinedClass = cx(CLASS_NAME$32, selected ? `${CLASS_NAME$32}--selected` : void 0, disabled ? `${CLASS_NAME$32}--disabled` : void 0, className);
	const providedStyle = { "--reactodia-element-style-color": elementColor };
	const localizedText = model.locale.formatEntityLabel(element, model.language);
	const onItemClick = (event) => {
		if (!disabled && onClick) {
			event.persist();
			onClick(event, element);
		}
	};
	return /* @__PURE__ */ jsx("div", {
		...otherProps,
		ref,
		className: combinedClass,
		role: otherProps.role ?? "option",
		draggable: !disabled && Boolean(onDragStart),
		title: otherProps.title ?? formatEntityTitle(element, model, t),
		style: {
			...otherProps.style,
			...providedStyle
		},
		onClick: onItemClick,
		onDragStart,
		children: /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$32}__label`,
			style: { background: frontColor },
			children: highlightSubstring(localizedText, highlightText)
		})
	});
}
/**
* Configure [DragEvent](https://developer.mozilla.org/en-US/docs/Web/API/DragEvent) data to
* contain a list of entity IRIs.
*
* The data uses `application/x-reactodia-elements` format with a `text` format fallback.
*
* A list of entity IRIs is represented as serialized JSON array of IRI strings.
*
* When the configured drag data is dropped on the diagram canvas, corresponding
* entities will be added to it.
*
* @category Utilities
*/
function startDragElements(e, iris) {
	try {
		e.dataTransfer.setData("application/x-reactodia-elements", JSON.stringify(iris));
	} catch (ex) {
		e.dataTransfer.setData("text", JSON.stringify(iris));
	}
	return false;
}
var DEFAULT_HIGHLIGHT_PROPS = { className: "reactodia-text-highlight" };
/**
* Renders a text span with all occurrences of the specified sub-string
* highlighted.
* 
* @param text source text to display
* @param substring sub-string to highlight each instance of inside the `text`
* @param highlightProps props to pass to each `<span>` for a sub-string occurrence
*
* @category Utilities
*/
function highlightSubstring(text, substring, highlightProps = DEFAULT_HIGHLIGHT_PROPS) {
	if (!substring) return /* @__PURE__ */ jsx("span", { children: text });
	const start = text.toLowerCase().indexOf(substring.toLowerCase());
	if (start < 0) return /* @__PURE__ */ jsx("span", { children: text });
	const end = start + substring.length;
	const before = text.substring(0, start);
	const highlighted = text.substring(start, end);
	const after = text.substring(end);
	return /* @__PURE__ */ jsxs("span", { children: [
		before,
		/* @__PURE__ */ jsx("span", {
			...highlightProps,
			children: highlighted
		}),
		after
	] });
}
function formatEntityTitle(entity, model, t) {
	const entityIri = model.locale.formatIri(entity.id);
	const entityLabel = model.locale.formatEntityLabel(entity, model.language);
	const entityTypes = model.locale.formatEntityTypeList(entity, model.language);
	const title = t.text("inline_entity.title", {
		entity: entityLabel,
		entityIri,
		entityTypes
	});
	const titleExtra = t.text("inline_entity.title_extra", {
		entity: entityLabel,
		entityIri,
		entityTypes
	});
	return `${title}${titleExtra ? `\n${titleExtra}` : ""}`;
}
//#endregion
//#region src/widgets/utility/progressBar.tsx
var CLASS_NAME$31 = "reactodia-progress-bar";
/**
* Utility component to display a horizontal progress bar.
*
* @category Components
*/
function ProgressBar(props) {
	const { state, title, percent = 100, height = 20 } = props;
	return /* @__PURE__ */ jsx("div", {
		className: `${CLASS_NAME$31} ${CLASS_NAME$31}--${state}`,
		style: { height: state === "loading" || state === "error" ? height : 0 },
		children: /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$31}__bar`,
			style: { width: `${percent}%` },
			title,
			role: "progressbar",
			"aria-valuemin": 0,
			"aria-valuemax": 100,
			"aria-valuenow": percent
		})
	});
}
//#endregion
//#region src/widgets/utility/searchInput.tsx
var CLASS_NAME$30 = "reactodia-search-input";
function SearchInput(props) {
	const { className, inputProps = {}, store, children } = props;
	const t = useTranslation();
	const term = useObservedProperty(store.events, "changeValue", () => store.value);
	const mode = useObservedProperty(store.events, "changeMode", () => store.mode);
	return /* @__PURE__ */ jsxs("div", {
		role: "search",
		className: cx(CLASS_NAME$30, mode === "explicit" ? `${CLASS_NAME$30}--has-submit` : void 0, className),
		children: [
			/* @__PURE__ */ jsx("input", {
				...inputProps,
				role: "searchbox",
				type: inputProps.type ?? "text",
				className: cx(`${CLASS_NAME$30}__input`, "reactodia-form-control", inputProps.className),
				placeholder: inputProps.placeholder ?? t.text("search_defaults.input.placeholder"),
				value: term,
				onChange: (e) => store.change({
					value: e.currentTarget.value,
					action: "input"
				}),
				onKeyUp: (e) => {
					if (e.key === "Enter") store.change({
						value: store.value,
						action: "submit"
					});
				}
			}),
			store.value ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$30}__clear-container`,
				children: /* @__PURE__ */ jsx("button", {
					type: "button",
					className: `${CLASS_NAME$30}__clear`,
					title: t.text("search_defaults.input_clear.title"),
					onClick: () => store.change({
						value: "",
						action: "clear"
					}),
					children: /* @__PURE__ */ jsx("span", { "aria-hidden": "true" })
				})
			}) : null,
			mode === "explicit" ? /* @__PURE__ */ jsx("button", {
				type: "button",
				title: t.text("search_defaults.input_submit.title"),
				className: cx(`${CLASS_NAME$30}__submit`, "reactodia-btn", "reactodia-btn-default"),
				onClick: () => store.change({
					value: store.value,
					action: "submit"
				})
			}) : null,
			children
		]
	});
}
/**
* React hook to create a search input state store with debounce support.
*
* **Experimental**: this feature are likely to change in the future.
*
* @returns stable {@link SearchInputStore} instance which will not change on re-renders
*
* @category Hooks
*/
function useSearchInputStore(params) {
	const { initialValue, submitTimeout = "immediate", allowSubmit } = params;
	const [state] = React.useState(() => new StatefulSearchInputStore(initialValue));
	React.useEffect(() => {
		state.setDebounceTimeout(submitTimeout);
		state.setAllowSubmit(allowSubmit);
	});
	return state;
}
var StatefulSearchInputStore = class {
	events = new EventSource();
	_value;
	_mode = "debounce";
	debouncer = new Debouncer(0);
	allowSubmit;
	constructor(initialValue) {
		this._value = initialValue;
	}
	get mode() {
		return this._mode;
	}
	get value() {
		return this._value;
	}
	setDebounceTimeout(timeout) {
		switch (timeout) {
			case "immediate":
			case "explicit":
				this.debouncer.setTimeout(0);
				this._mode = timeout;
				break;
			default:
				this.debouncer.setTimeout(timeout);
				this._mode = "debounce";
				break;
		}
	}
	setAllowSubmit(allowSubmit) {
		this.allowSubmit = allowSubmit;
	}
	change(params) {
		const { value, action } = params;
		this.debouncer.dispose();
		if (!(value === this._value && action === "input")) {
			const previous = this._value;
			this._value = value;
			this.events.trigger("changeValue", {
				source: this,
				previous,
				action
			});
		}
		switch (action) {
			case "input":
				if (this._mode !== "explicit") this.trySubmit(value, { debounce: this._mode === "debounce" });
				break;
			case "submit":
				this.trySubmit(value, { debounce: false });
				break;
			case "clear":
				this.events.trigger("clearSearch", {
					source: this,
					value
				});
				break;
		}
	}
	trySubmit(value, options) {
		if (!this.allowSubmit || this.allowSubmit(value)) if (options.debounce) this.debouncer.call(() => this.events.trigger("executeSearch", {
			source: this,
			value
		}));
		else this.events.trigger("executeSearch", {
			source: this,
			value
		});
		else this.events.trigger("clearSearch", {
			source: this,
			value
		});
	}
};
//#endregion
//#region src/widgets/utility/searchResults.tsx
var CLASS_NAME$29 = "reactodia-search-results";
/**
* Utility component to display a list of selectable entities.
*
* @category Components
*/
function SearchResults(props) {
	const { items, selection, onSelectionChanged, isItemDisabled, highlightText, useDragAndDrop = true, multiSelection = true, footer } = props;
	const computeIsItemDisabled = useIsItemDisabledWithDefault(isItemDisabled);
	const extendedItems = React.useMemo(() => items.map((data) => ({
		data,
		active: !computeIsItemDisabled(data)
	})), [items, computeIsItemDisabled]);
	const latestItems = useLatest(items);
	const lastSelected = React.useRef();
	const searchResultsContext = React.useMemo(() => ({
		highlightText,
		useDragAndDrop,
		selection,
		onSetSelected: (item, select, e) => {
			if (select) {
				if (multiSelection && e.shiftKey && lastSelected.current) {
					const lastIri = lastSelected.current.id;
					const lastIndex = latestItems.current.findIndex((entity) => entity.id === lastIri);
					const currentIndex = latestItems.current.findIndex((entity) => entity.id === item.data.id);
					if (lastIndex >= 0 && currentIndex >= 0) {
						const nextSelection = new Set(selection);
						const endIndex = Math.max(lastIndex, currentIndex);
						for (let i = Math.min(lastIndex, currentIndex); i <= endIndex; i++) nextSelection.add(latestItems.current[i].id);
						onSelectionChanged(nextSelection);
					}
				} else if (!selection.has(item.data.id)) {
					const nextSelection = new Set(multiSelection ? selection : void 0);
					nextSelection.add(item.data.id);
					onSelectionChanged(nextSelection);
				}
				lastSelected.current = item.data;
			} else if (selection.has(item.data.id)) {
				const nextSelection = new Set(selection);
				nextSelection.delete(item.data.id);
				onSelectionChanged(nextSelection);
			}
		}
	}), [
		highlightText,
		useDragAndDrop,
		selection,
		onSelectionChanged,
		multiSelection
	]);
	React.useEffect(() => {
		const leftovers = new Set(selection);
		for (const item of extendedItems) if (item.active) leftovers.delete(item.data.id);
		if (leftovers.size > 0) onSelectionChanged(new Set(Array.from(selection).filter((iri) => !leftovers.has(iri))));
	}, [computeIsItemDisabled]);
	return /* @__PURE__ */ jsx(SearchResultsContext.Provider, {
		value: searchResultsContext,
		children: /* @__PURE__ */ jsxs("div", {
			className: CLASS_NAME$29,
			children: [/* @__PURE__ */ jsx(FocusGroup, { children: ({ ref, controller }) => /* @__PURE__ */ jsx("ul", {
				ref,
				className: `${CLASS_NAME$29}__root`,
				role: "list",
				"aria-multiselectable": true,
				onClick: controller.defaultClick,
				onKeyDown: controller.defaultKeyDown,
				children: extendedItems.map((item) => /* @__PURE__ */ jsx(ResultItem, {
					item,
					selected: selection.has(item.data.id)
				}, getElementItemKey(item)))
			}) }), footer]
		})
	});
}
function useIsItemDisabledWithDefault(isItemDisabled) {
	const { model } = useWorkspace();
	const changeCellsStore = useFrameDebouncedStore(useEventStore(model.events, "changeCells"));
	const cellsVersion = useSyncStore(isItemDisabled ? neverSyncStore() : changeCellsStore, () => model.cellsVersion);
	return React.useMemo(() => {
		if (isItemDisabled) return isItemDisabled;
		const presentEntities = getAllPresentEntities(model);
		return (item) => presentEntities.has(item.id);
	}, [isItemDisabled, cellsVersion]);
}
function getElementItemKey(item) {
	return item.data.id;
}
var SearchResultsContext = React.createContext(null);
function useSearchResultsContext() {
	const context = React.useContext(SearchResultsContext);
	if (!context) throw new Error("Reactodia: missing search results context");
	return context;
}
function ResultItem(props) {
	const { item, selected } = props;
	const { highlightText, useDragAndDrop, selection, onSetSelected } = useSearchResultsContext();
	const { ref, tabIndex } = useFocusGroupItem({
		debugLabel: item.data.id,
		active: item.active
	});
	return /* @__PURE__ */ jsx("li", {
		ref,
		className: `${CLASS_NAME$29}__item`,
		role: "listitem",
		children: /* @__PURE__ */ jsx(ListElementView, {
			tabIndex,
			element: item.data,
			highlightText,
			disabled: !item.active,
			selected: Boolean(selected),
			onClick: item.active ? (e) => onSetSelected(item, !selected, e) : void 0,
			onKeyDown: (e) => {
				if (item.active && e.key === " ") {
					e.preventDefault();
					onSetSelected(item, !selected, e);
				}
			},
			onDragStart: useDragAndDrop ? (e) => {
				const iris = [];
				selection.forEach((iri) => iris.push(iri));
				if (!selection.has(item.data.id)) iris.push(item.data.id);
				return startDragElements(e, iris);
			} : void 0
		})
	});
}
//#endregion
//#region src/widgets/utility/noSearchResults.tsx
var CLASS_NAME$28 = "reactodia-no-search-results";
function NoSearchResults(props) {
	const { className, hasQuery, minSearchTermLength: termLength = 1, message } = props;
	const t = useTranslation();
	const effectiveMessage = message ?? (hasQuery ? t.text("search_defaults.no_results") : termLength <= 1 ? t.text("search_defaults.empty_input_term", { termLength }) : t.text("search_defaults.input_term_too_short", { termLength }));
	return /* @__PURE__ */ jsx("div", {
		className: cx(CLASS_NAME$28, className),
		children: effectiveMessage
	});
}
//#endregion
//#region src/widgets/utility/accessibleTree.tsx
/**
* Utility component as base for [accessible](https://www.w3.org/TR/wai-aria/)
* tree-like container with selection.
*
* @category Components
*/
function AccessibleTree(props) {
	const { model, items, renderItem, expanded, defaultExpanded, onSetExpanded, selected, defaultSelected, rootProps, forestProps, itemProps } = props;
	const treeContext = React.useMemo(() => ({
		model,
		renderItem,
		forestProps,
		itemProps
	}), [
		model,
		renderItem,
		forestProps,
		itemProps
	]);
	const onKeyDown = (e, controller) => {
		if (e.key === "ArrowUp" || e.key === "ArrowDown") controller.defaultKeyDown(e);
		else if (e.key === "ArrowLeft" || e.key === "ArrowRight") {
			if (e.target instanceof HTMLElement && onSetExpanded) {
				e.preventDefault();
				const current = findParentWithin(e.target, e.currentTarget, (item) => item.hasAttribute("data-tree-index"));
				if (!current) return;
				const currentIndex = Number(current.getAttribute("data-tree-index"));
				if (Number.isFinite(currentIndex)) if (e.key === "ArrowLeft" && (!current.hasAttribute("aria-expanded") || current.getAttribute("aria-expanded") === "false")) controller.focusParent({ from: current });
				else {
					const indexPath = reconstructTreeIndexPath(current, e.currentTarget);
					const found = indexPath ? lookupTreeItem(treeContext.model, items, indexPath) : void 0;
					if (found) {
						const [target, path] = found;
						onSetExpanded(target, path, e.key === "ArrowRight");
					}
				}
			}
		} else rootProps?.onKeyDown?.(e);
	};
	return /* @__PURE__ */ jsx(FocusGroup, { children: ({ ref, controller }) => /* @__PURE__ */ jsx(Forest, {
		treeContext,
		items,
		expanded,
		defaultExpanded: defaultExpanded ?? false,
		selected,
		defaultSelected,
		rootProps: {
			...rootProps,
			ref,
			onClick: controller.defaultClick,
			onKeyDown: (e) => onKeyDown(e, controller)
		}
	}) });
}
function Forest(props) {
	const { treeContext, items, parentPath, expanded, defaultExpanded, selected, defaultSelected, rootProps } = props;
	const { model, forestProps } = treeContext;
	return /* @__PURE__ */ jsx("ul", {
		...rootProps ?? forestProps,
		children: items.map((item, index) => {
			const path = {
				parent: parentPath,
				key: model.getKey(item)
			};
			return /* @__PURE__ */ jsx(Item, {
				treeContext,
				item,
				index,
				path,
				expanded: expanded?.get(path.key),
				defaultExpanded,
				selected: selected?.get(path.key),
				defaultSelected
			}, path.key);
		})
	});
}
function Item(props) {
	const { treeContext, item, index, path, expanded, defaultExpanded, selected, defaultSelected } = props;
	const { model, renderItem, itemProps } = treeContext;
	const { ref, tabIndex } = useFocusGroupItem({ active: model.isActive(item) });
	const leafExpanded = expanded?.value ?? defaultExpanded;
	const leafSelected = selected?.value ?? defaultSelected;
	const children = model.getChildren(item);
	return /* @__PURE__ */ jsxs("li", {
		...itemProps,
		ref,
		"data-tree-index": index,
		"aria-expanded": !children || children.length === 0 ? void 0 : leafExpanded,
		"aria-selected": leafSelected !== void 0,
		children: [renderItem({
			item,
			path,
			focusProps: { tabIndex },
			expanded: leafExpanded,
			selected: leafSelected
		}), leafExpanded && children && children.length > 0 ? /* @__PURE__ */ jsx(Forest, {
			treeContext,
			items: children,
			parentPath: path,
			expanded: expanded?.level,
			defaultExpanded,
			selected: selected?.level,
			defaultSelected: model.getDefaultSelected(item, leafSelected)
		}) : null]
	});
}
var TreeState = class TreeState {
	states;
	constructor(states = /* @__PURE__ */ new Map()) {
		this.states = states;
	}
	[Symbol.iterator]() {
		return this.states[Symbol.iterator]();
	}
	get(key) {
		return this.states.get(key);
	}
	setAt(path, updater) {
		const itemState = this.states.get(path.key);
		const nextState = TreeState.setAtItem(itemState, path.child, updater);
		if (nextState === itemState) return this;
		const nextStates = new Map(this.states);
		if (nextState) nextStates.set(path.key, nextState);
		else nextStates.delete(path.key);
		return new TreeState(nextStates);
	}
	static setAtItem(itemState, path, updater) {
		if (path) if (itemState && itemState.level) {
			const nextLevel = itemState.level.setAt(path, updater);
			return nextLevel === itemState.level ? itemState : {
				...itemState,
				level: nextLevel
			};
		} else {
			const nextItem = TreeState.setAtItem(void 0, path.child, updater);
			if (!nextItem || nextItem === itemState) return nextItem;
			return {
				value: itemState?.value,
				level: new TreeState(new Map([[path.key, nextItem]]))
			};
		}
		const nextValue = updater(itemState?.value);
		if (nextValue === itemState?.value) return itemState;
		return nextValue === void 0 && itemState?.level === void 0 ? void 0 : {
			...itemState,
			value: nextValue
		};
	}
};
function treePathToDown(upPath) {
	let current = upPath.parent;
	let downward = {
		key: upPath.key,
		child: void 0
	};
	while (current) {
		downward = {
			key: current.key,
			child: downward
		};
		current = current.parent;
	}
	return downward;
}
function reconstructTreeIndexPath(item, parent) {
	const indexPath = [];
	let current = item;
	while (current && current !== parent) {
		if (current.hasAttribute("data-tree-index")) {
			const index = Number(current.getAttribute("data-tree-index"));
			if (Number.isFinite(index)) indexPath.push(index);
			else return;
		}
		current = current.parentElement;
	}
	indexPath.reverse();
	return indexPath;
}
function lookupTreeItem(model, items, indexPath) {
	let level = items;
	let item;
	let topPath;
	let tailPath;
	for (const index of indexPath) {
		if (!level || index < 0 || index >= level.length) return;
		item = level[index];
		if (tailPath) {
			tailPath.child = {
				child: void 0,
				key: model.getKey(item)
			};
			tailPath = tailPath.child;
		} else {
			topPath = {
				child: void 0,
				key: model.getKey(item)
			};
			tailPath = topPath;
		}
		level = model.getChildren(item);
	}
	return item && topPath ? [item, topPath] : void 0;
}
//#endregion
//#region src/widgets/classTree/classTreeResults.tsx
var ClassTreeContext = React.createContext(null);
var CLASS_NAME$27 = "reactodia-class-tree-item";
function ClassTreeResults(props) {
	const { nodes, selection, onSelect, searchText, creatableClasses, onClickCreate, onDragCreate, draggableItems } = props;
	const renderItem = React.useCallback(({ item, path, focusProps, expanded, selected }) => /* @__PURE__ */ jsx(Leaf, {
		node: item,
		path,
		focusProps,
		expanded,
		selected
	}), []);
	const rootProps = React.useMemo(() => ({
		className: `${CLASS_NAME$27}__root`,
		role: "tree"
	}), []);
	const forestProps = React.useMemo(() => ({
		className: `${CLASS_NAME$27}__children`,
		role: "none"
	}), []);
	const itemProps = React.useMemo(() => ({
		className: CLASS_NAME$27,
		role: "treeitem"
	}), []);
	const defaultExpanded = Boolean(searchText);
	const [expanded, setExpanded] = React.useState();
	const onExpand = React.useCallback((path) => {
		setExpanded((previous) => (previous ?? new TreeState()).setAt(treePathToDown(path), (itemExpanded) => !(itemExpanded ?? defaultExpanded)));
	}, [defaultExpanded]);
	React.useEffect(() => setExpanded(void 0), [searchText]);
	const classTreeContext = React.useMemo(() => ({
		searchText,
		creatableClasses,
		onClickCreate,
		onDragCreate,
		draggableItems,
		onExpand,
		onSelect: (node, path) => onSelect({
			node,
			selection: new TreeState().setAt(treePathToDown(path), () => node)
		})
	}), [
		searchText,
		creatableClasses,
		onClickCreate,
		onDragCreate,
		draggableItems,
		onExpand,
		onSelect
	]);
	return /* @__PURE__ */ jsx(ClassTreeContext.Provider, {
		value: classTreeContext,
		children: /* @__PURE__ */ jsx(AccessibleTree, {
			model: ClassTreeModel,
			items: nodes,
			renderItem,
			expanded,
			defaultExpanded,
			onSetExpanded: (item, path, expand) => setExpanded((previous) => (previous ?? new TreeState()).setAt(path, () => expand)),
			selected: selection?.selection,
			rootProps,
			forestProps,
			itemProps
		})
	});
}
var TreeNode = { setDerived: (node, derived) => ({
	...node,
	derived
}) };
var ClassTreeModel = {
	getKey: (item) => item.iri,
	getChildren: (item) => item.derived,
	getDefaultSelected: (item, selected) => void 0,
	isActive: (item) => true
};
function Leaf(props) {
	const { node, path, focusProps, expanded, selected } = props;
	const { searchText, creatableClasses, onClickCreate, onDragCreate, draggableItems, onExpand, onSelect } = useClassTreeContext();
	const { getElementTypeStyle } = useWorkspace();
	const t = useTranslation();
	const toggleClass = node.derived.length === 0 ? `${CLASS_NAME$27}__toggle` : expanded ? `${CLASS_NAME$27}__toggle-expanded` : `${CLASS_NAME$27}__toggle-collapsed`;
	const typeStyle = getElementTypeStyle([node.iri]);
	const providedStyle = { "--reactodia-element-style-color": typeStyle.color };
	const bodyClass = cx(`${CLASS_NAME$27}__body`, selected ? `${CLASS_NAME$27}__body--selected` : void 0);
	const label = highlightSubstring(node.label, searchText, { className: `${CLASS_NAME$27}__highlighted-term` });
	const onDragStart = (e) => {
		e.dataTransfer.setData("text", "");
		onDragCreate(node);
	};
	return /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$27}__row`,
		style: providedStyle,
		children: [
			/* @__PURE__ */ jsx("div", {
				className: toggleClass,
				role: "none",
				onClick: () => onExpand(path)
			}),
			/* @__PURE__ */ jsxs("a", {
				...focusProps,
				className: bodyClass,
				href: node.iri,
				onClick: (e) => {
					e.preventDefault();
					onSelect(node, path);
				},
				onKeyDown: (e) => {
					if (e.key === " ") {
						e.preventDefault();
						onSelect(node, path);
					}
				},
				draggable: draggableItems,
				children: [
					/* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$27}__icon-container`,
						children: typeStyle.icon ? /* @__PURE__ */ jsx("img", {
							role: "presentation",
							className: cx(`${CLASS_NAME$27}__icon`, typeStyle.iconMonochrome ? `${CLASS_NAME$27}__icon--monochrome` : void 0),
							src: typeStyle.icon
						}) : /* @__PURE__ */ jsx("div", { className: node.derived.length === 0 ? `${CLASS_NAME$27}__default-icon-leaf` : `${CLASS_NAME$27}__default-icon-parent` })
					}),
					/* @__PURE__ */ jsx("span", {
						className: `${CLASS_NAME$27}__label`,
						children: label
					}),
					node.data?.count ? /* @__PURE__ */ jsx("span", {
						className: `${CLASS_NAME$27}__count reactodia-badge`,
						children: node.data.count
					}) : null
				]
			}),
			creatableClasses.get(node.iri) ? /* @__PURE__ */ jsx("button", {
				...focusProps,
				title: t.text("search_element_types.drag_create.title"),
				className: cx(`${CLASS_NAME$27}__create-button`, "reactodia-btn reactodia-btn-default"),
				draggable: true,
				onClick: () => onClickCreate(node),
				onDragStart
			}) : null
		]
	});
}
function useClassTreeContext() {
	const context = React.useContext(ClassTreeContext);
	if (!context) throw new Error("Reactodia: missing class tree context");
	return context;
}
//#endregion
//#region src/widgets/classTree/classTree.tsx
/**
* Component to display an element type (class) tree for the workspace.
*
* Element type graph is loaded from {@link DataProvider} associated with
* the diagram model.
*
* In graph authoring mode, the class tree can be used to create entity
* elements that are instances of the displayed types.
*
* @category Components
*/
function ClassTree(props) {
	const { searchStore, searchTimeout = 200, minSearchTermLength = 2 } = props;
	const uncontrolledSearch = useSearchInputStore({
		initialValue: "",
		submitTimeout: searchTimeout,
		allowSubmit: (term) => normalizeSearchText(term).length >= minSearchTermLength
	});
	const effectiveSearchStore = searchStore ?? uncontrolledSearch;
	const normalizedTerm = useObservedProperty(effectiveSearchStore.events, "changeValue", () => normalizeSearchText(effectiveSearchStore.value));
	const workspace = useWorkspace();
	const t = useTranslation();
	return /* @__PURE__ */ jsx(ClassTreeInner, {
		...props,
		isControlled: Boolean(searchStore),
		searchStore: effectiveSearchStore,
		normalizedTerm,
		minSearchTermLength,
		workspace,
		translation: t
	});
}
var CLASS_NAME$26 = "reactodia-class-tree";
var EMPTY_CREATABLE_TYPES = /* @__PURE__ */ new Map();
var ClassTreeInner = class extends React.Component {
	listener = new EventObserver();
	searchListener = new EventObserver();
	loadClassesOperation = new AbortController();
	refreshOperation = new AbortController();
	createElementCancellation = new AbortController();
	constructor(props) {
		super(props);
		this.state = {
			fetchedGraph: void 0,
			refreshingState: "none",
			roots: [],
			filteredRoots: [],
			appliedSearchText: "",
			constructibleClasses: /* @__PURE__ */ new Map(),
			showOnlyConstructible: false
		};
	}
	render() {
		const { className, isControlled, searchStore, normalizedTerm, minSearchTermLength, draggableItems = true, workspace: { editor }, translation: t } = this.props;
		const { fetchedGraph, refreshingState, appliedSearchText, roots, filteredRoots, selection, constructibleClasses, showOnlyConstructible } = this.state;
		const searchText = appliedSearchText ? normalizedTerm : void 0;
		return /* @__PURE__ */ jsxs("div", {
			className: cx(CLASS_NAME$26, isControlled ? `${CLASS_NAME$26}--controlled` : void 0, className),
			children: [
				/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$26}__filter`,
					children: /* @__PURE__ */ jsxs("div", {
						className: `${CLASS_NAME$26}__filter-group`,
						children: [isControlled ? null : /* @__PURE__ */ jsx(SearchInput, {
							store: searchStore,
							inputProps: {
								name: "reactodia-class-tree-filter",
								placeholder: t.textOptional("search_element_types.input.placeholder")
							}
						}), editor.inAuthoringMode ? /* @__PURE__ */ jsxs("label", {
							className: `${CLASS_NAME$26}__only-creatable`,
							children: [
								/* @__PURE__ */ jsx("input", {
									type: "checkbox",
									name: "reactodia-class-tree-only-constructible",
									checked: showOnlyConstructible,
									onChange: this.onShowOnlyCreatableChange
								}),
								" ",
								t.text("search_element_types.show_only_creatable")
							]
						}) : null]
					})
				}),
				/* @__PURE__ */ jsx(ProgressBar, {
					state: refreshingState,
					title: t.text("search_element_types.refresh_progress.title")
				}),
				fetchedGraph?.classTree ? /* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$26}__tree reactodia-scrollable`,
					tabIndex: -1,
					children: [/* @__PURE__ */ jsx(ClassTreeResults, {
						nodes: filteredRoots,
						searchText,
						selection,
						onSelect: this.onSelectNode,
						creatableClasses: editor.inAuthoringMode ? constructibleClasses : EMPTY_CREATABLE_TYPES,
						onClickCreate: this.onCreateInstance,
						onDragCreate: this.onDragCreate,
						draggableItems
					}), filteredRoots.length === 0 ? /* @__PURE__ */ jsx(NoSearchResults, {
						className: `${CLASS_NAME$26}__no-results`,
						hasQuery: filteredRoots !== roots,
						minSearchTermLength,
						message: roots.length === 0 ? t.text("search_element_types.no_results") : void 0
					}) : null]
				}) : /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$26}__spinner`,
					children: /* @__PURE__ */ jsx(HtmlSpinner, {
						width: 30,
						height: 30,
						errorOccurred: Boolean(fetchedGraph?.fetchError)
					})
				})
			]
		});
	}
	componentDidMount() {
		const { workspace: { model, editor } } = this.props;
		this.listener.listen(model.events, "changeLanguage", () => this.refreshClassTree());
		this.listener.listen(model.events, "loadingStart", () => {
			this.initClassTree();
		});
		this.listener.listen(editor.events, "changeMode", () => this.refreshClassTree());
		this.listenSearch();
		this.initClassTree();
	}
	componentDidUpdate(prevProps) {
		if (this.props.searchStore.events !== prevProps.searchStore.events) this.listenSearch();
	}
	listenSearch() {
		const { searchStore } = this.props;
		this.searchListener.stopListening();
		this.searchListener.listen(searchStore.events, "executeSearch", ({ value }) => {
			this.performSearch(value);
		});
		this.searchListener.listen(searchStore.events, "clearSearch", () => {
			this.performSearch("");
		});
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.searchListener.stopListening();
		this.loadClassesOperation.abort();
		this.refreshOperation.abort();
		this.createElementCancellation.abort();
	}
	initClassTree() {
		const { workspace: { model } } = this.props;
		const { fetchedGraph } = this.state;
		const { dataProvider } = model;
		if (fetchedGraph && fetchedGraph.dataProvider === dataProvider) this.refreshClassTree();
		else if (dataProvider) this.setState({ fetchedGraph: void 0 }, () => {
			this.fetchTypeGraph(dataProvider);
		});
		else this.refreshClassTree();
	}
	async fetchTypeGraph(dataProvider) {
		const cancellation = new AbortController();
		this.loadClassesOperation.abort();
		this.loadClassesOperation = cancellation;
		let classGraph = null;
		try {
			classGraph = await mapAbortedToNull(dataProvider.knownElementTypes({ signal: cancellation.signal }), cancellation.signal);
		} catch (error) {
			console.error(error);
			this.setState({ fetchedGraph: {
				dataProvider,
				fetchError: error
			} });
			return;
		}
		if (classGraph === null) return;
		const classTree = constructTree(classGraph);
		this.setState({ fetchedGraph: {
			dataProvider,
			classTree
		} }, () => {
			this.refreshClassTree();
		});
	}
	performSearch = (requestedTerm) => {
		const normalizedTerm = normalizeSearchText(requestedTerm);
		this.setState((state) => applyFilters({
			...state,
			appliedSearchText: normalizedTerm
		}));
	};
	onShowOnlyCreatableChange = (e) => {
		this.setState((state) => applyFilters({
			...state,
			showOnlyConstructible: !state.showOnlyConstructible
		}));
	};
	onSelectNode = (selection) => {
		const { workspace: { getCommandBus } } = this.props;
		this.setState({ selection }, () => {
			getCommandBus(InstancesSearchTopic).trigger("setCriteria", { criteria: { elementType: selection.node.iri } });
		});
	};
	onCreateInstance = (node) => {
		this.createInstanceAt(node.iri);
	};
	onDragCreate = (node) => {
		const { workspace: { view } } = this.props;
		view.setHandlerForNextDropOnPaper((e) => {
			this.createInstanceAt(node.iri, e);
		});
	};
	refreshClassTree = () => {
		const cancellation = new AbortController();
		this.refreshOperation.abort();
		this.refreshOperation = cancellation;
		let queryTypeIris;
		this.setState((state, props) => {
			const { workspace: { model, editor }, translation: t } = props;
			const classTree = state.fetchedGraph?.classTree;
			if (!classTree) return { refreshingState: "none" };
			let refreshingState = "none";
			if (editor.inAuthoringMode) {
				const newIris = getNewClassIris(state.constructibleClasses, classTree);
				if (newIris.size > 0) {
					refreshingState = "loading";
					queryTypeIris = newIris;
				}
			}
			const roots = createRoots(classTree, model, t);
			return applyFilters({
				...state,
				roots: sortTree(roots),
				refreshingState
			});
		}, () => {
			if (queryTypeIris) this.queryCreatableTypes(queryTypeIris, cancellation.signal);
		});
	};
	async queryCreatableTypes(typeIris, signal) {
		const { workspace: { editor } } = this.props;
		const { metadataProvider } = editor;
		if (!metadataProvider) return;
		try {
			const result = await mapAbortedToNull(metadataProvider.filterConstructibleTypes(typeIris, { signal }), signal);
			if (result === null) return;
			this.setState((state) => {
				const constructibleClasses = new Map(state.constructibleClasses);
				typeIris.forEach((type) => {
					constructibleClasses.set(type, result.has(type));
				});
				return applyFilters({
					...state,
					constructibleClasses,
					refreshingState: "completed"
				});
			});
		} catch (err) {
			console.error(err);
			this.setState((state) => applyFilters({
				...state,
				refreshingState: "error"
			}));
		}
	}
	async createInstanceAt(elementType, dropEvent) {
		const { placeCreatedEntity, workspace: { model, view, editor, getCommandBus }, translation: t } = this.props;
		const batch = model.history.startBatch();
		this.createElementCancellation.abort();
		this.createElementCancellation = new AbortController();
		const signal = this.createElementCancellation.signal;
		const createdEntity = await mapAbortedToNull(editor.metadataProvider.createEntity(elementType, {
			translation: t,
			language: model.language,
			signal
		}), signal);
		if (createdEntity === null) return;
		const element = editor.createEntity(createdEntity.data);
		if (createdEntity.elementState) element.setElementState(createdEntity.elementState);
		let createAt;
		if (dropEvent) createAt = [dropEvent.source, dropEvent.position];
		else {
			const canvas = view.findAnyCanvas();
			if (canvas) createAt = [canvas, getViewportCenterInPaperCoords(canvas)];
		}
		if (createAt) {
			const [canvas, targetPosition] = createAt;
			element.setPosition(targetPosition);
			canvas.renderingState.syncUpdate();
			moveElementCenterToPosition(element, targetPosition, canvas.renderingState);
		}
		if (placeCreatedEntity) await placeCreatedEntity(element, dropEvent);
		batch.store();
		model.setSelection([element]);
		getCommandBus(VisualAuthoringTopic).trigger("editEntity", { target: element });
	}
};
function constructTree(graph) {
	const items = /* @__PURE__ */ new Map();
	for (const model of graph.elementTypes) {
		const item = {
			id: model.id,
			label: model.label,
			count: model.count,
			children: []
		};
		items.set(item.id, item);
	}
	const childToParents = /* @__PURE__ */ new Map();
	for (const [childId, parentId] of graph.subtypeOf) multimapAdd(childToParents, childId, parentId);
	const edgesToDelete = [];
	const visiting = /* @__PURE__ */ new Set();
	const visited = /* @__PURE__ */ new Set();
	const visit = (childId) => {
		if (visited.has(childId)) return;
		visiting.add(childId);
		const parents = childToParents.get(childId);
		if (parents) for (const parentId of parents) if (visiting.has(parentId)) edgesToDelete.push([childId, parentId]);
		else visit(parentId);
		visiting.delete(childId);
	};
	for (const classId of items.keys()) visit(classId);
	for (const [childId, parentId] of edgesToDelete) {
		const parents = childToParents.get(childId);
		if (parents) parents.delete(parentId);
	}
	const roots = [];
	for (const item of items.values()) {
		const parents = childToParents.get(item.id);
		if (!parents || parents.size === 0) roots.push(item);
		else for (const parentId of parents) {
			const parentItem = items.get(parentId);
			if (parentItem) parentItem.children.push(item);
		}
	}
	return roots;
}
function createRoots(classTree, model, t) {
	const mapChildren = (children) => {
		const nodes = [];
		for (const item of children) nodes.push({
			iri: item.id,
			data: item,
			label: t.formatLabel(item.label, item.id, model.language),
			derived: mapChildren(item.children)
		});
		return nodes;
	};
	return mapChildren(classTree);
}
function getNewClassIris(existingClasses, classTree) {
	const classIris = /* @__PURE__ */ new Set();
	const visitClass = (model) => {
		if (!existingClasses.has(model.id)) classIris.add(model.id);
		model.children.forEach(visitClass);
	};
	classTree.forEach(visitClass);
	return classIris;
}
function normalizeSearchText(text) {
	return text.trim().toLowerCase();
}
function sortTree(roots) {
	function mapNodes(nodes) {
		if (nodes.length === 0) return nodes;
		const mapped = nodes.map(mapNode);
		mapped.sort(compareByLabel);
		return mapped;
	}
	function mapNode(node) {
		return TreeNode.setDerived(node, mapNodes(node.derived));
	}
	function compareByLabel(left, right) {
		return left.label.localeCompare(right.label);
	}
	return mapNodes(roots);
}
function applyFilters(state) {
	let filteredRoots = state.roots;
	if (state.appliedSearchText) filteredRoots = filterByKeyword(filteredRoots, state.appliedSearchText);
	if (state.showOnlyConstructible) filteredRoots = filterOnlyCreatable(filteredRoots, state.constructibleClasses);
	return {
		...state,
		filteredRoots
	};
}
function filterByKeyword(roots, searchText) {
	if (roots.length === 0) return roots;
	const searchFilter = makeCaseInsensitiveFilter(searchText);
	function collectByKeyword(acc, node) {
		const derived = node.derived.reduce(collectByKeyword, []);
		if (derived.length > 0 || searchFilter(node.label)) acc.push(TreeNode.setDerived(node, derived));
		return acc;
	}
	return roots.reduce(collectByKeyword, []);
}
function filterOnlyCreatable(roots, creatableClasses) {
	function collectOnlyCreatable(acc, node) {
		const derived = node.derived.reduce(collectOnlyCreatable, []);
		if (derived.length > 0 || creatableClasses.get(node.iri)) acc.push(TreeNode.setDerived(node, derived));
		return acc;
	}
	return roots.reduce(collectOnlyCreatable, []);
}
function getViewportCenterInPaperCoords(canvas) {
	const viewport = canvas.metrics.pane;
	return canvas.metrics.clientToPaperCoords(viewport.clientWidth / 2, viewport.clientHeight / 2);
}
function moveElementCenterToPosition(element, center, sizeProvider) {
	const size = sizeProvider.getElementSize(element) ?? {
		width: 0,
		height: 0
	};
	element.setPosition({
		x: center.x - size.width / 2,
		y: center.y - size.height / 2
	});
}
//#endregion
//#region src/paper/paper.tsx
var CLASS_NAME$25 = "reactodia-paper";
var Paper = class extends React.Component {
	rootRef = React.createRef();
	paneRef = React.createRef();
	resizeObserver;
	viewportAnimation;
	movingState;
	metrics;
	constructor(props) {
		super(props);
		this.state = {
			paneRef: this.paneRef,
			contentBounds: this.props.contentBounds,
			pageSize: this.props.pageSize,
			transform: emptyPane(this.props.pageSize),
			mounted: false
		};
		this.resizeObserver = new ResizeObserver(this.onResize);
		this.metrics = new class extends BasePaperMetrics {
			paper;
			constructor(paper) {
				super();
				this.paper = paper;
			}
			get pane() {
				return this.paper.pane ?? EMPTY_PANE;
			}
			get transform() {
				return this.paper.state.transform;
			}
			getClientRect() {
				return this.paper.pane?.getBoundingClientRect() ?? EMPTY_PANE_RECT;
			}
		}(this);
	}
	get root() {
		return this.rootRef.current;
	}
	get pane() {
		return this.paneRef.current;
	}
	render() {
		const { className, style, showScrollbars, panOnTouch = true, onContextMenu, onKeyDown, onKeyUp, renderLayers, paneProps, watermark, children } = this.props;
		const { transform, mounted } = this.state;
		const { width, height, scale, paddingX, paddingY } = transform;
		const scaledWidth = width * scale;
		const scaledHeight = height * scale;
		const transformStyle = {
			width: scaledWidth + paddingX,
			height: scaledHeight + paddingY,
			marginLeft: paddingX,
			marginTop: paddingY,
			paddingRight: paddingX,
			paddingBottom: paddingY
		};
		return /* @__PURE__ */ jsxs("div", {
			ref: this.rootRef,
			className: cx(CLASS_NAME$25, className, panOnTouch ? `${CLASS_NAME$25}--pan-on-touch` : void 0, showScrollbars ? void 0 : `${CLASS_NAME$25}--hide-scrollbars`),
			style,
			tabIndex: 0,
			onKeyDown,
			onKeyUp,
			children: [/* @__PURE__ */ jsxs("div", {
				...paneProps,
				className: `${CLASS_NAME$25}__pane`,
				ref: this.paneRef,
				onPointerDown: this.onAreaPointerDown,
				children: [/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$25}__layers`,
					style: transformStyle,
					onPointerDown: this.onPointerDown,
					onContextMenu,
					onScrollCapture: this.onScrollCapture,
					children: mounted ? renderLayers(transform) : null
				}), watermark]
			}), mounted ? children : void 0]
		});
	}
	componentDidMount() {
		const pane = this.pane;
		pane.addEventListener("dragover", this.onDragOver);
		pane.addEventListener("drop", this.onDragDrop);
		pane.addEventListener("scroll", this.onScrollPassive, { passive: true });
		pane.addEventListener("wheel", this.onWheel, { passive: false });
		this.resizeObserver.observe(pane);
		this.setState({ mounted: true });
	}
	static getDerivedStateFromProps(props, state) {
		const pane = state.paneRef.current;
		if (pane && !(Rect.equals(props.contentBounds, state.contentBounds) && props.pageSize.width === state.pageSize.width && props.pageSize.height === state.pageSize.height)) {
			const { clientWidth, clientHeight } = pane;
			const paneClientSize = {
				width: clientWidth,
				height: clientHeight
			};
			const adjusted = adjustPane(props.contentBounds, paneClientSize, props.pageSize, state.transform.scale);
			if (equalTransforms(state.transform, adjusted)) return null;
			return {
				...state,
				contentBounds: props.contentBounds,
				pageSize: props.pageSize,
				transform: adjusted
			};
		}
		return null;
	}
	getSnapshotBeforeUpdate(prevProps, prevState) {
		const { contentBounds, pageSize } = this.props;
		const { paneRef, transform } = this.state;
		const pane = paneRef.current;
		const sameContent = Rect.equals(contentBounds, prevProps.contentBounds) && pageSize.width === prevProps.pageSize.width && pageSize.height === prevProps.pageSize.height;
		let scroll;
		if (pane && !sameContent && !equalTransforms(transform, prevState.transform)) scroll = {
			left: pane.scrollLeft,
			top: pane.scrollTop
		};
		return { scroll };
	}
	componentDidUpdate(prevProps, prevState, snapshot) {
		const pane = this.state.paneRef.current;
		if (pane && snapshot.scroll) {
			const { scale, originX, originY, paddingX, paddingY } = this.state.transform;
			const prevTransform = prevState.transform;
			const scrollX = (originX - prevTransform.originX) * scale + (paddingX - prevTransform.paddingX);
			const scrollY = (originY - prevTransform.originY) * scale + (paddingY - prevTransform.paddingY);
			const scrollLeft = snapshot.scroll.left + scrollX;
			const scrollTop = snapshot.scroll.top + scrollY;
			pane.scrollLeft = scrollLeft;
			pane.scrollTop = scrollTop;
		}
		if (!equalTransforms(this.state.transform, prevState.transform)) this.props.onChangeTransform?.(prevState.transform);
	}
	componentWillUnmount() {
		const pane = this.pane;
		this.stopListeningToPointerMove();
		pane.removeEventListener("dragover", this.onDragOver);
		pane.removeEventListener("drop", this.onDragDrop);
		pane.removeEventListener("scroll", this.onScrollPassive);
		pane.removeEventListener("wheel", this.onWheel);
		this.resizeObserver.disconnect();
	}
	onAreaPointerDown = (e) => {
		if (e.target === this.pane) this.onPointerDown(e);
	};
	onPointerDown = (e) => {
		if (this.movingState) {
			this.handleMultiPointerDown(e);
			return;
		}
		const operation = this.props.onPointerOperation?.(e);
		if (!operation) return;
		if (operation.action === "move" || e.pointerType === "mouse") e.preventDefault();
		let panningOrigin;
		if (operation.action === "panning") {
			const { scrollLeft, scrollTop } = this.pane;
			panningOrigin = {
				scrollLeft,
				scrollTop
			};
			clearDocumentTextSelection();
		}
		const { pageX, pageY } = e;
		this.movingState = {
			operation,
			pointers: /* @__PURE__ */ new Map(),
			pointerMoved: false,
			originPointerId: e.pointerId,
			origin: {
				pageX,
				pageY
			},
			panningOrigin,
			pinchOrigin: void 0
		};
		this.handleMultiPointerDown(e);
		document.addEventListener("pointermove", this.onPointerMove);
		document.addEventListener("pointerup", this.onPointerUp);
		if (e.pointerType !== "mouse") document.addEventListener("pointercancel", this.onPointerCancel);
		operation.onPointerDown(e);
	};
	handleMultiPointerDown(e) {
		if (!this.movingState) return;
		const { pointers } = this.movingState;
		pointers.set(e.pointerId, {
			x: e.pageX,
			y: e.pageY
		});
		if (!this.movingState.pinchOrigin && pointers.size === 2) {
			e.preventDefault();
			this.movingState.pinchOrigin = {
				pointers: new Map(pointers),
				metrics: this.metrics.snapshot()
			};
		}
	}
	onPointerMove = (e) => {
		if (!this.movingState) return;
		const { origin, operation, panningOrigin } = this.movingState;
		const pageOffsetX = e.pageX - origin.pageX;
		const pageOffsetY = e.pageY - origin.pageY;
		if (e.isPrimary && Math.abs(pageOffsetX) >= 1 && Math.abs(pageOffsetY) >= 1) this.movingState.pointerMoved = true;
		if (this.handleMultiPointerMove(e)) {} else {
			e.preventDefault();
			if (panningOrigin) {
				const pane = this.pane;
				pane.classList.add(`${CLASS_NAME$25}--panning`);
				pane.scrollLeft = panningOrigin.scrollLeft - pageOffsetX;
				pane.scrollTop = panningOrigin.scrollTop - pageOffsetY;
			}
			operation.onPointerMove(e);
		}
	};
	handleMultiPointerMove(e) {
		if (!this.movingState) return false;
		const { pointers, pinchOrigin } = this.movingState;
		pointers.set(e.pointerId, {
			x: e.pageX,
			y: e.pageY
		});
		if (!pinchOrigin) return false;
		const [[pointerA, originA], [pointerB, originB]] = pinchOrigin.pointers;
		const lastA = pointers.get(pointerA);
		const lastB = pointers.get(pointerB);
		if (!(lastA && lastB)) return false;
		const last = Vector.scale(Vector.add(lastA, lastB), .5);
		const origin = Vector.scale(Vector.add(originA, originB), .5);
		const scaleMultiplier = Vector.length(Vector.subtract(lastB, lastA)) / Math.max(Vector.length(Vector.subtract(originB, originA)), 1);
		const originMetrics = pinchOrigin.metrics;
		const centerPaper = originMetrics.clientToPaperCoords(originMetrics.pane.clientWidth / 2, originMetrics.pane.clientHeight / 2);
		const lastPaper = originMetrics.pageToPaperCoords(last.x, last.y);
		const originPaper = originMetrics.pageToPaperCoords(origin.x, origin.y);
		const movedCenter = Vector.add(originPaper, Vector.scale(Vector.subtract(centerPaper, lastPaper), 1 / scaleMultiplier));
		const scale = originMetrics.transform.scale * scaleMultiplier;
		this.centerViewport(movedCenter, { scale });
		return true;
	}
	onPointerUp = (e) => {
		if (this.movingState) {
			const { originPointerId, pointers, pinchOrigin } = this.movingState;
			pointers.delete(e.pointerId);
			if (e.pointerId === originPointerId || pinchOrigin && pointers.size < 2) this.stopListeningToPointerMove(e);
		}
	};
	onPointerCancel = (e) => {
		this.stopListeningToPointerMove();
	};
	onScrollCapture = (e) => {
		if (this.movingState?.operation.hasSameTarget(e)) this.stopListeningToPointerMove();
	};
	stopListeningToPointerMove = (e) => {
		const movingState = this.movingState;
		this.movingState = void 0;
		if (movingState) {
			this.pane.classList.remove(`${CLASS_NAME$25}--panning`);
			document.removeEventListener("pointermove", this.onPointerMove);
			document.removeEventListener("pointerup", this.onPointerUp);
			document.removeEventListener("pointercancel", this.onPointerCancel);
			const { operation, pointerMoved, pinchOrigin } = movingState;
			if (e && !pinchOrigin) operation.onPointerUp(e, { triggerAsClick: !pointerMoved });
			else operation.onPointerCancel(e);
		}
	};
	onWheel = (e) => {
		const { wheelToScaleDelta } = this.props.scaleDefaults;
		const scaleDelta = wheelToScaleDelta(e);
		if (scaleDelta !== void 0) {
			e.preventDefault();
			const pivot = this.metrics.pageToPaperCoords(e.pageX, e.pageY);
			this.setScale(this.scale + scaleDelta, { pivot });
		}
	};
	onResize = () => {
		this.props.onResize?.();
	};
	centerViewport(paperPosition, options) {
		if (typeof options.scale === "number") {
			const { min, max } = this.props.scaleDefaults;
			let scale = options.scale;
			scale = Math.max(scale, min);
			scale = Math.min(scale, max);
			const viewportState = {
				center: paperPosition,
				scale: {
					x: scale,
					y: scale
				}
			};
			return this.setViewportState(viewportState, options);
		} else {
			const viewportState = { center: paperPosition };
			return this.setViewportState(viewportState, options);
		}
	}
	get scale() {
		return this.state.transform.scale;
	}
	setScale(value, options) {
		let scale = value;
		const { min, max } = this.props.scaleDefaults;
		scale = Math.max(scale, min);
		scale = Math.min(scale, max);
		let viewportState;
		if (options && options.pivot) {
			const { x, y } = options.pivot;
			const pane = this.pane;
			const paperCenter = this.metrics.clientToPaperCoords(pane.clientWidth / 2, pane.clientHeight / 2);
			const previousScale = this.state.transform.scale;
			const scaledBy = scale / previousScale;
			viewportState = {
				center: {
					x: x - (x - paperCenter.x) / scaledBy,
					y: y - (y - paperCenter.y) / scaledBy
				},
				scale: {
					x: scale,
					y: scale
				}
			};
		} else viewportState = { scale: {
			x: scale,
			y: scale
		} };
		return this.setViewportState(viewportState, options);
	}
	scaleToFitRect(paperRect, options = {}) {
		const { clientWidth, clientHeight } = this.pane;
		if (paperRect.width === 0) return Promise.resolve();
		const { min, maxFit } = this.props.scaleDefaults;
		const { width } = fitRectKeepingAspectRatio(paperRect, clientWidth, clientHeight);
		let scale = width / paperRect.width;
		scale = Math.max(scale, min);
		scale = Math.min(scale, maxFit);
		const viewPortState = {
			center: {
				x: paperRect.x + paperRect.width / 2,
				y: paperRect.y + paperRect.height / 2
			},
			scale: {
				x: scale,
				y: scale
			}
		};
		return this.setViewportState(viewPortState, options);
	}
	onDragOver = (e) => {
		const clientCoords = clientCoordsFor(this.pane, e);
		if (this.props.onDragOver?.(e, clientCoords)) e.preventDefault();
		else if (e.dataTransfer) e.dataTransfer.dropEffect = "none";
	};
	onDragDrop = (e) => {
		const clientCoords = clientCoordsFor(this.pane, e);
		this.props.onDragDrop?.(e, clientCoords);
	};
	onScrollPassive = (e) => {
		this.props.onScrollPassive?.(e);
	};
	get viewportState() {
		const { clientWidth, clientHeight, scrollLeft, scrollTop } = this.pane;
		const { originX, originY, paddingX, paddingY, scale } = this.state.transform;
		const scrollCenterX = scrollLeft + clientWidth / 2 - paddingX;
		const scrollCenterY = scrollTop + clientHeight / 2 - paddingY;
		return {
			center: {
				x: scrollCenterX / scale - originX,
				y: scrollCenterY / scale - originY
			},
			scale: {
				x: scale,
				y: scale
			}
		};
	}
	setViewportState(state, options) {
		if (this.viewportAnimation) this.viewportAnimation.cancellation.abort();
		const from = this.viewportState;
		const to = {
			...from,
			...state
		};
		if (options && (options.animate || options.duration && options.duration > 0)) {
			const viewportAnimation = {
				from,
				to,
				cancellation: new AbortController()
			};
			const awaitPromise = animateInterval(typeof options.duration === "number" ? options.duration : this.props.viewportDefaults.duration, (progress) => {
				const t = easeInOutBezier(progress);
				const computed = {
					center: {
						x: from.center.x + (to.center.x - from.center.x) * t,
						y: from.center.y + (to.center.y - from.center.y) * t
					},
					scale: {
						x: from.scale.x + (to.scale.x - from.scale.x) * t,
						y: from.scale.y + (to.scale.y - from.scale.y) * t
					}
				};
				this.applyViewportState(computed);
			}, viewportAnimation.cancellation.signal);
			this.viewportAnimation = viewportAnimation;
			return awaitPromise.then(() => {
				this.viewportAnimation = void 0;
			});
		} else {
			this.applyViewportState(to);
			return Promise.resolve();
		}
	}
	applyViewportState(targetState) {
		this.state.transform;
		const scale = targetState.scale.x;
		const paperCenter = targetState.center;
		this.setState((state) => {
			state.transform;
			return { transform: {
				...state.transform,
				scale
			} };
		}, () => {
			const pane = this.pane;
			const { originX, originY, paddingX, paddingY } = this.state.transform;
			const scrollCenterX = (paperCenter.x + originX) * scale;
			const scrollCenterY = (paperCenter.y + originY) * scale;
			const { clientWidth, clientHeight } = pane;
			pane.scrollLeft = scrollCenterX - clientWidth / 2 + paddingX;
			pane.scrollTop = scrollCenterY - clientHeight / 2 + paddingY;
		});
	}
};
var BasePaperMetrics = class {
	/** @deprecated */
	get area() {
		return this.pane;
	}
	snapshot() {
		const { clientWidth, clientHeight, offsetWidth, offsetHeight, scrollLeft, scrollTop } = this.pane;
		const { left, right, top, bottom } = this.getClientRect();
		return new SnapshotPaperMetrics({
			clientWidth,
			clientHeight,
			offsetWidth,
			offsetHeight,
			scrollLeft,
			scrollTop
		}, {
			left,
			right,
			top,
			bottom
		}, this.transform);
	}
	/** @deprecated */
	getTransform() {
		return this.transform;
	}
	getPaperSize() {
		const { width, height, scale } = this.transform;
		return {
			width: width / scale,
			height: height / scale
		};
	}
	getViewportPageRect() {
		const { left, right, top, bottom } = this.getClientRect();
		return {
			x: left + window.scrollX,
			y: top + window.scrollY,
			width: right - left,
			height: bottom - top
		};
	}
	pageToPaperCoords(pageX, pageY) {
		const { left, top } = this.getClientRect();
		return this.clientToPaperCoords(pageX - (left + window.scrollX), pageY - (top + window.scrollY));
	}
	paperToPageCoords(paperX, paperY) {
		const { x: paneX, y: paneY } = this.paperToScrollablePaneCoords(paperX, paperY);
		const { x: clientX, y: clientY } = this.scrollablePaneToClientCoords(paneX, paneY);
		const { left, top } = this.getClientRect();
		return {
			x: clientX + (left + window.scrollX),
			y: clientY + (top + window.scrollY)
		};
	}
	clientToPaperCoords(areaClientX, areaClientY) {
		const { x: paneX, y: paneY } = this.clientToScrollablePaneCoords(areaClientX, areaClientY);
		return this.scrollablePaneToPaperCoords(paneX, paneY);
	}
	clientToScrollablePaneCoords(areaClientX, areaClientY) {
		const { paddingX, paddingY } = this.transform;
		const { scrollLeft, scrollTop } = this.pane;
		return {
			x: areaClientX + scrollLeft - paddingX,
			y: areaClientY + scrollTop - paddingY
		};
	}
	scrollablePaneToClientCoords(paneX, paneY) {
		const { paddingX, paddingY } = this.transform;
		const { scrollLeft, scrollTop } = this.pane;
		return {
			x: paneX - scrollLeft + paddingX,
			y: paneY - scrollTop + paddingY
		};
	}
	scrollablePaneToPaperCoords(paneX, paneY) {
		return paperFromPaneCoords({
			x: paneX,
			y: paneY
		}, this.transform);
	}
	paperToScrollablePaneCoords(paperX, paperY) {
		return paneFromPaperCoords({
			x: paperX,
			y: paperY
		}, this.transform);
	}
};
var SnapshotPaperMetrics = class extends BasePaperMetrics {
	pane;
	clientRect;
	transform;
	constructor(pane, clientRect, transform) {
		super();
		this.pane = pane;
		this.clientRect = clientRect;
		this.transform = transform;
	}
	getClientRect() {
		return this.clientRect;
	}
	snapshot() {
		return this;
	}
};
var EMPTY_PANE = {
	clientWidth: 0,
	clientHeight: 0,
	offsetWidth: 0,
	offsetHeight: 0,
	scrollLeft: 0,
	scrollTop: 0
};
var EMPTY_PANE_RECT = {
	left: 0,
	right: 0,
	top: 0,
	bottom: 0
};
function clientCoordsFor(container, e) {
	const targetBox = (e.target instanceof SVGElement && e.target.ownerSVGElement !== null ? e.target.ownerSVGElement : e.target).getBoundingClientRect();
	const containerBox = container.getBoundingClientRect();
	return {
		x: e.offsetX + (targetBox.left - containerBox.left),
		y: e.offsetY + (targetBox.top - containerBox.top)
	};
}
/** Clears accidental text selection on the paper. */
function clearDocumentTextSelection() {
	if (document.getSelection) document.getSelection()?.removeAllRanges?.();
}
function wheelToScaleDeltaDefault(e, factor = .1) {
	return -1 * Math.max(-1, Math.min(1, e.deltaY || e.deltaX)) * factor;
}
//#endregion
//#region src/paper/toSvg.ts
var SVG_NAMESPACE = "http://www.w3.org/2000/svg";
var XML_ENCODING_HEADER = "<?xml version=\"1.0\" encoding=\"UTF-8\"?>";
/**
* @category Core
*/
function toSVG(options) {
	return exportSVG(options).then((svg) => {
		const svgText = new XMLSerializer().serializeToString(svg);
		return options.addXmlHeader ? XML_ENCODING_HEADER + svgText : svgText;
	});
}
async function exportSVG(options) {
	const { colorSchemeApi, contentBox: viewBox, watermarkSvg, preserveDimensions, convertImagesToDataUris, removeByCssSelectors = [], transformExported, embedFonts = true } = options;
	let clonedPaperSvg;
	let cssPropertyValues;
	colorSchemeApi.actInColorScheme("light", () => {
		clonedPaperSvg = composeExportedSvg(options.layers, viewBox);
		const knownProperties = /* @__PURE__ */ new Set();
		collectKnownPropertiesFromCssRules(knownProperties);
		collectKnownPropertiesFromInlineStyles(clonedPaperSvg.composedSvg, knownProperties);
		cssPropertyValues = captureCustomCssPropertyValues(options.styleRoot, knownProperties);
	});
	const { composedSvg, imageBounds } = clonedPaperSvg;
	for (const selector of removeByCssSelectors) for (const node of composedSvg.querySelectorAll(selector)) node.remove();
	if (transformExported) await transformExported(composedSvg);
	const appliedCssRules = collectAppliedCssFromDocument(composedSvg);
	const imageUrls = /* @__PURE__ */ new Map();
	if (convertImagesToDataUris) {
		collectImageUrlsFromElements(composedSvg, imageUrls);
		collectImageUrlsFromCssRules(appliedCssRules, imageUrls);
		collectImageUrlsFromInlineStyles(composedSvg, imageUrls);
	}
	if (preserveDimensions) {
		composedSvg.setAttribute("width", String(viewBox.width));
		composedSvg.setAttribute("height", String(viewBox.height));
	} else {
		composedSvg.setAttribute("width", "100%");
		composedSvg.setAttribute("height", "100%");
	}
	composedSvg.setAttribute("viewBox", `${viewBox.x} ${viewBox.y} ${viewBox.width} ${viewBox.height}`);
	if (watermarkSvg) addWatermark(composedSvg, viewBox, watermarkSvg);
	await fetchImages(imageUrls);
	for (const img of composedSvg.querySelectorAll("img")) {
		const exportKey = img.getAttribute("export-key");
		img.removeAttribute("export-key");
		if (exportKey) {
			const { width, height } = imageBounds[exportKey];
			img.setAttribute("width", width.toString());
			img.setAttribute("height", height.toString());
			const exportedUrl = imageUrls.get(img.src);
			if (exportedUrl) img.src = exportedUrl;
		}
	}
	embedImageUrlsToInlineStyles(composedSvg, imageUrls);
	const fontFaces = embedFonts ? await embedUsedFonts(composedSvg, appliedCssRules, cssPropertyValues, embedFonts === true ? {} : embedFonts) : [];
	const defs = document.createElementNS("http://www.w3.org/2000/svg", "defs");
	defs.innerHTML = `<style>${serializeCssPropertyValues(cssPropertyValues)}</style>\n<style>${exportCssRules(appliedCssRules, imageUrls)}</style>` + (fontFaces.length > 0 ? `\n<style>${fontFaces.join("\n")}</style>` : "");
	composedSvg.insertBefore(defs, composedSvg.firstChild);
	return composedSvg;
}
function addWatermark(svg, viewBox, watermarkSvg) {
	const WATERMARK_CLASS = "reactodia-exported-watermark";
	const WATERMARK_MAX_WIDTH = 120;
	const WATERMARK_PADDING = 20;
	const image = document.createElementNS(SVG_NAMESPACE, "image");
	image.setAttributeNS("http://www.w3.org/1999/xlink", "href", watermarkSvg);
	image.setAttribute("class", WATERMARK_CLASS);
	const width = Math.min(viewBox.width * .2, WATERMARK_MAX_WIDTH);
	const x = viewBox.x + viewBox.width - width - WATERMARK_PADDING;
	const y = viewBox.y + WATERMARK_PADDING;
	image.setAttribute("x", x.toString());
	image.setAttribute("y", y.toString());
	image.setAttribute("width", width.toString());
	image.setAttribute("opacity", "0.3");
	svg.insertBefore(image, svg.firstChild);
}
function collectAppliedCssFromDocument(targetSubtree) {
	const appliedRules = [];
	enumerateStylesheets((rule) => {
		if (!(rule instanceof CSSStyleRule)) return;
		if (toMatchableSelectors(rule.selectorText).some((selector) => {
			try {
				return Boolean(targetSubtree.querySelector(selector));
			} catch {
				return false;
			}
		})) appliedRules.push(rule);
	});
	return appliedRules;
}
/**
* Turns a CSS selector list into base selectors usable with `querySelector`:
* a pseudo-element is always the last part of a selector and cannot be matched,
* so its trailing `::x` is dropped and selectors left empty (as in a
* `*, ::before, ::after` reset) are removed.
*/
function toMatchableSelectors(selectorText) {
	return splitSelectorList(selectorText).map((selector) => selector.replace(/::[\w-]+(\([^)]*\))?\s*$/, "").trim()).filter((selector) => selector.length > 0);
}
/** Splits a selector list on top-level commas, ignoring `()`, `[]` and strings. */
function splitSelectorList(selectorText) {
	const selectors = [];
	let depth = 0;
	let inAttibute = false;
	let inQuote;
	let start = 0;
	for (let i = 0; i < selectorText.length; i++) {
		const ch = selectorText[i];
		if (inQuote !== void 0) {
			if (ch === inQuote && selectorText[i - 1] !== "\\") inQuote = void 0;
		} else if (ch === "\"" || ch === "'") inQuote = ch;
		else if (inAttibute) {
			if (ch === "]") inAttibute = false;
		} else if (ch === "[") inAttibute = true;
		else if (ch === "(") depth++;
		else if (ch === ")") depth--;
		else if (ch === "," && depth === 0) {
			selectors.push(selectorText.slice(start, i));
			start = i + 1;
		}
	}
	selectors.push(selectorText.slice(start));
	return selectors;
}
function collectImageUrlsFromElements(target, imageUrls) {
	for (const img of target.querySelectorAll("img")) if (img.src && !imageUrls.has(img.src)) imageUrls.set(img.src, null);
}
function collectImageUrlsFromCssRules(rules, imageUrls) {
	for (const rule of rules) {
		const maskUrl = parseCssImageUrl(rule.style.getPropertyValue("mask-image"));
		if (maskUrl && !imageUrls.has(maskUrl)) imageUrls.set(maskUrl, null);
		const backgroundUrl = parseCssImageUrl(rule.style.getPropertyValue("background-image"));
		if (backgroundUrl && !imageUrls.has(backgroundUrl)) imageUrls.set(backgroundUrl, null);
	}
}
function collectImageUrlsFromInlineStyles(target, imageUrls) {
	enumerateDescendants(target, (element) => {
		if (element instanceof HTMLElement) {
			const maskUrl = parseCssImageUrl(element.style.maskImage);
			if (maskUrl && !imageUrls.has(maskUrl)) imageUrls.set(maskUrl, null);
			const backgroundUrl = parseCssImageUrl(element.style.backgroundImage);
			if (backgroundUrl && !imageUrls.has(backgroundUrl)) imageUrls.set(backgroundUrl, null);
		}
	});
}
function embedImageUrlsToInlineStyles(target, imageUrls) {
	enumerateDescendants(target, (element) => {
		if (element instanceof HTMLElement) {
			const maskUrl = parseCssImageUrl(element.style.maskImage);
			const exportedMaskUrl = maskUrl ? imageUrls.get(maskUrl) : void 0;
			if (exportedMaskUrl) element.style.maskImage = serializeCssImageUrl(exportedMaskUrl);
			const backgroundUrl = parseCssImageUrl(element.style.backgroundImage);
			const exportedBackgroundUrl = backgroundUrl ? imageUrls.get(backgroundUrl) : void 0;
			if (exportedBackgroundUrl) element.style.backgroundImage = serializeCssImageUrl(exportedBackgroundUrl);
		}
	});
}
async function fetchImages(imageUrls) {
	await Promise.all(Array.from(imageUrls.keys(), async (sourceUrl) => {
		if (sourceUrl.startsWith("data:")) return;
		try {
			const dataUri = await exportImageAsDataUri(sourceUrl);
			if (dataUri && dataUri !== "data:image/svg+xml,") imageUrls.set(sourceUrl, dataUri);
		} catch (err) {
			console.warn("Reactodia: Failed to export image: " + sourceUrl, err);
		}
	}));
}
function exportCssRules(rules, imageUrls) {
	const cssTexts = rules.map((rule) => rule.cssText);
	const styleElement = document.createElement("style");
	document.body.appendChild(styleElement);
	const sheet = styleElement.sheet;
	try {
		for (let i = 0; i < rules.length; i++) {
			const rule = rules[i];
			const maskUrl = parseCssImageUrl(rule.style.getPropertyValue("mask-image"));
			const exportedMaskUrl = maskUrl ? imageUrls.get(maskUrl) : void 0;
			const backgroundUrl = parseCssImageUrl(rule.style.getPropertyValue("background-image"));
			const exportedBackgroundUrl = backgroundUrl ? imageUrls.get(backgroundUrl) : void 0;
			if (sheet && (exportedMaskUrl || exportedBackgroundUrl)) {
				sheet.insertRule(rule.cssText);
				const ruleCopy = sheet.cssRules[0];
				if (ruleCopy instanceof CSSStyleRule) {
					if (exportedMaskUrl) ruleCopy.style.setProperty("mask-image", serializeCssImageUrl(exportedMaskUrl));
					if (exportedBackgroundUrl) ruleCopy.style.setProperty("background-image", serializeCssImageUrl(exportedBackgroundUrl));
					cssTexts[i] = ruleCopy.cssText;
				}
				sheet.deleteRule(0);
			}
		}
	} finally {
		document.body.removeChild(styleElement);
	}
	return cssTexts.join("\n");
}
function parseCssImageUrl(imageValue) {
	if (imageValue) {
		const trimmedValue = imageValue.trim();
		let match = /^url\(\s*'(.*)'\s*\)$/i.exec(trimmedValue);
		if (match) return match[1];
		match = /^url\(\s*"(.*)"\s*\)$/i.exec(trimmedValue);
		if (match) return match[1];
	}
}
function serializeCssImageUrl(dataUri) {
	return `url("${dataUri}")`;
}
var DEFAULT_MAX_FONT_FILE_SIZE = 256 * 1024;
var DEFAULT_MAX_FONT_TOTAL_SIZE = 512 * 1024;
/**
* Inlines the web fonts used by the exported subtree as data URIs.
*
* Only `CSSStyleRule`s are exported into the result, so `@font-face` rules are
* left behind, and an exported image is not able to load an external font in
* any case: without this the text falls back to whatever the viewing system
* happens to provide, which changes the metrics as well as the typeface.
*/
async function embedUsedFonts(target, appliedRules, propertyValues, options) {
	const { maxFileSize = DEFAULT_MAX_FONT_FILE_SIZE, maxTotalSize = DEFAULT_MAX_FONT_TOTAL_SIZE } = options;
	const families = collectUsedFontFamilies(target, appliedRules, propertyValues);
	if (families.size === 0) return [];
	const fontFaces = [];
	let totalSize = 0;
	for (const rule of collectFontFaceRules()) {
		const family = primaryFontFamily(rule.style.getPropertyValue("font-family"), propertyValues);
		if (!(family && families.has(family))) continue;
		const source = findFontUrl(rule.style.getPropertyValue("src"));
		if (!source) continue;
		try {
			const response = await fetch(source);
			if (!response.ok) continue;
			if (Number(response.headers.get("content-length")) > maxFileSize) continue;
			const font = await response.blob();
			if (font.size > maxFileSize || totalSize + font.size > maxTotalSize) continue;
			totalSize += font.size;
			fontFaces.push(await serializeFontFace(rule, font));
		} catch (err) {
			console.warn("Reactodia: Failed to export font: " + source, err);
		}
	}
	return fontFaces;
}
/**
* Collects the font families the exported subtree asks for.
*
* Only the first family of each stack is taken: the rest are fallbacks, which
* are not used while the primary one is available.
*/
function collectUsedFontFamilies(target, appliedRules, propertyValues) {
	const families = /* @__PURE__ */ new Set();
	const addFrom = (value) => {
		const family = primaryFontFamily(value, propertyValues);
		if (family) families.add(family);
	};
	for (const rule of appliedRules) addFrom(rule.style.getPropertyValue("font-family"));
	enumerateDescendants(target, (element) => {
		if (element instanceof HTMLElement) addFrom(element.style.fontFamily);
	});
	return families;
}
/** Returns the first family of a font stack, normalized for comparison. */
function primaryFontFamily(value, propertyValues) {
	const family = substituteCssVariables(value, propertyValues).split(",")[0].trim().replace(/^["']|["']$/g, "").toLowerCase();
	return family.length > 0 ? family : void 0;
}
/** Substitutes `var()` references by the captured custom property values. */
function substituteCssVariables(value, propertyValues) {
	return value.replace(/var\(\s*(--[\w-]+)\s*(?:,([^)]*))?\)/g, (match, property, fallback) => propertyValues.get(property) ?? fallback ?? "");
}
function collectFontFaceRules() {
	const fontFaces = [];
	enumerateStylesheets((rule) => {
		if (rule instanceof CSSFontFaceRule) fontFaces.push(rule);
	});
	return fontFaces;
}
/** Returns the most compact source a font face offers, preferring WOFF2. */
function findFontUrl(src) {
	const urls = Array.from(src.matchAll(/url\(\s*["']?([^"')]+)["']?\s*\)/g), (match) => match[1]);
	const source = urls.find((url) => url.includes(".woff2")) ?? urls[0];
	if (!source || source.startsWith("data:")) return;
	try {
		return new URL(source, document.baseURI).href;
	} catch (err) {
		return;
	}
}
async function serializeFontFace(rule, font) {
	const dataUri = await new Promise((resolve, reject) => {
		const reader = new FileReader();
		reader.onload = () => resolve(String(reader.result));
		reader.onerror = () => reject(reader.error);
		reader.readAsDataURL(font);
	});
	return `@font-face{${[
		`font-family:${rule.style.getPropertyValue("font-family")}`,
		`font-style:${rule.style.getPropertyValue("font-style") || "normal"}`,
		`font-weight:${rule.style.getPropertyValue("font-weight") || "normal"}`,
		`src:url(${dataUri})`
	].join(";")}}`;
}
function composeExportedSvg(layers, viewBox) {
	const composedSvg = document.createElementNS(SVG_NAMESPACE, "svg");
	const composedViewport = document.createElementNS(SVG_NAMESPACE, "g");
	composedViewport.setAttribute("class", "reactodia-exported-canvas");
	composedSvg.appendChild(composedViewport);
	const imageBounds = Object.create(null);
	for (const layer of layers) if (layer instanceof SVGSVGElement) {
		const layerViewport = findSvgLayerViewport(layer.cloneNode(true));
		layerViewport.removeAttribute("transform");
		composedViewport.appendChild(layerViewport);
	} else if (layer instanceof HTMLElement) {
		const layerClone = layer.cloneNode(true);
		layerClone.classList.add("reactodia-exported-layer");
		layerClone.setAttribute("style", `transform: translate(${-viewBox.x}px,${-viewBox.y}px)`);
		const layerForeignObject = document.createElementNS(SVG_NAMESPACE, "foreignObject");
		layerForeignObject.appendChild(layerClone);
		layerForeignObject.setAttribute("transform", `translate(${viewBox.x},${viewBox.y})`);
		layerForeignObject.setAttribute("width", String(viewBox.width));
		layerForeignObject.setAttribute("height", String(viewBox.height));
		const layerRoot = document.createElementNS(SVG_NAMESPACE, "g");
		layerRoot.appendChild(layerForeignObject);
		composedViewport.appendChild(layerRoot);
		const clonedNodes = layerClone.querySelectorAll("img");
		let nextImageIndex = 0;
		for (const img of layer.querySelectorAll("img")) {
			const index = nextImageIndex;
			nextImageIndex++;
			const exportKey = `export-key-${index}`;
			clonedNodes[index].setAttribute("export-key", exportKey);
			imageBounds[exportKey] = {
				width: img.clientWidth,
				height: img.clientHeight
			};
		}
	}
	return {
		composedSvg,
		imageBounds
	};
}
function findSvgLayerViewport(layer) {
	let child = layer.firstChild;
	while (child) {
		if (child instanceof SVGGElement) return child;
		child = child.nextSibling;
	}
}
async function exportImageAsDataUri(url) {
	if (!url || url.startsWith("data:")) return url;
	const extensionMatch = url.match(/\.([a-zA-Z0-9]+)[^.a-zA-Z0-9]?[^.]*$/);
	const extension = extensionMatch ? extensionMatch[1].toLowerCase() : void 0;
	if (extension === "svg") try {
		const response = await fetch(url);
		if (response.ok) {
			const svgText = await response.text();
			if (svgText.length > 0) return "data:image/svg+xml," + encodeURIComponent(svgText.replace(/\r\n/g, "\n"));
		}
	} catch (err) {}
	const image = await loadCrossOriginImage(url);
	const canvas = document.createElement("canvas");
	canvas.width = image.width;
	canvas.height = image.height;
	canvas.getContext("2d").drawImage(image, 0, 0);
	try {
		const mimeType = "image/" + (extension === "jpg" ? "jpeg" : "png");
		return canvas.toDataURL(mimeType);
	} catch (e) {
		throw new Error(`Failed to convert image to data URI: ${url}`, { cause: e });
	}
}
function loadCrossOriginImage(src) {
	const image = new Image();
	image.crossOrigin = "anonymous";
	const promise = new Promise((resolve, reject) => {
		image.onload = () => resolve(image);
		image.onerror = (ev) => reject(ev);
	});
	image.src = src;
	return promise;
}
function collectKnownPropertiesFromCssRules(foundProperties) {
	enumerateStylesheets((rule) => {
		if (!(rule instanceof CSSStyleRule)) return;
		for (let i = 0; i < rule.style.length; i++) {
			const property = rule.style[i];
			if (property.startsWith("--") && !foundProperties.has(property)) foundProperties.add(property);
		}
	});
}
function collectKnownPropertiesFromInlineStyles(target, foundProperties) {
	const visit = (element) => {
		if (element instanceof HTMLElement) for (let i = 0; i < element.style.length; i++) {
			const property = element.style[i];
			if (property.startsWith("--") && !foundProperties.has(property)) foundProperties.add(property);
		}
	};
	enumerateDescendants(target, visit);
	let parent = target.parentElement;
	while (parent) {
		visit(parent);
		parent = parent.parentElement;
	}
}
function captureCustomCssPropertyValues(element, knownProperties) {
	const propertyValues = /* @__PURE__ */ new Map();
	const styles = getComputedStyle(element);
	for (const property of styles) if (property.startsWith("--")) {
		const value = styles.getPropertyValue(property);
		propertyValues.set(property, value);
	}
	for (const property of knownProperties) if (!propertyValues.has(property)) {
		const value = styles.getPropertyValue(property);
		if (value !== void 0) propertyValues.set(property, value);
	}
	return propertyValues;
}
function enumerateStylesheets(visit) {
	const visitedRules = /* @__PURE__ */ new WeakSet();
	const visitRule = (rule) => {
		if (visitedRules.has(rule)) return;
		visitedRules.add(rule);
		visit(rule);
		if (rule instanceof CSSLayerBlockRule) for (const subRule of rule.cssRules) visitRule(subRule);
	};
	for (let i = 0; i < document.styleSheets.length; i++) {
		let rules;
		try {
			const cssSheet = document.styleSheets[i];
			rules = cssSheet.cssRules || cssSheet.rules;
			if (!rules) continue;
		} catch (e) {
			continue;
		}
		for (const rule of rules) visitRule(rule);
	}
}
function enumerateDescendants(target, visit) {
	const visited = /* @__PURE__ */ new WeakSet();
	const visitElement = (element) => {
		if (visited.has(element)) return;
		visited.add(element);
		visit(element);
		for (const child of element.children) visitElement(child);
	};
	visitElement(target);
}
function serializeCssPropertyValues(propertyValues) {
	const parts = [":root {\n"];
	for (const [property, value] of propertyValues) parts.push("  ", property, ": ", value, ";\n");
	parts.push("}\n");
	return parts.join("");
}
var DEFAULT_MAX_FALLBACK_SIZE = {
	width: 8192,
	height: 8192
};
/**
* @category Core
*/
async function toDataURL(options) {
	const { mimeType = "image/png", maxFallbackSize = DEFAULT_MAX_FALLBACK_SIZE } = options;
	const svg = await exportSVG({
		...options,
		convertImagesToDataUris: true,
		mockImages: false,
		preserveDimensions: true
	});
	const svgBox = {
		width: Number(svg.getAttribute("width")),
		height: Number(svg.getAttribute("height"))
	};
	const { innerSize, outerSize, offset } = computeAutofit(svgBox, typeof options.width === "number" || typeof options.height === "number" ? {
		width: options.width,
		height: options.height
	} : fallbackContainerSize(svgBox, maxFallbackSize));
	svg.setAttribute("width", innerSize.width.toString());
	svg.setAttribute("height", innerSize.height.toString());
	const svgString = new XMLSerializer().serializeToString(svg);
	const { canvas, context } = createCanvas(outerSize.width, outerSize.height, options.backgroundColor);
	const image = await loadImage("data:image/svg+xml," + encodeURIComponent(svgString));
	context.drawImage(image, offset.x, offset.y, innerSize.width, innerSize.height);
	return canvas.toDataURL(mimeType, options.quality);
	function createCanvas(canvasWidth, canvasHeight, backgroundColor) {
		const cnv = document.createElement("canvas");
		cnv.width = canvasWidth;
		cnv.height = canvasHeight;
		const cnt = cnv.getContext("2d");
		if (backgroundColor) {
			cnt.fillStyle = backgroundColor;
			cnt.fillRect(0, 0, canvasWidth, canvasHeight);
		}
		return {
			canvas: cnv,
			context: cnt
		};
	}
}
function loadImage(source) {
	return new Promise((resolve, reject) => {
		const image = new Image();
		image.onload = function() {
			resolve(image);
		};
		image.onerror = function(ev) {
			reject(ev);
		};
		image.src = source;
	});
}
function computeAutofit(itemSize, containerSize) {
	const fit = fitRectKeepingAspectRatio(itemSize, containerSize.width, containerSize.height);
	const innerSize = {
		width: Math.floor(fit.width),
		height: Math.floor(fit.height)
	};
	const outerSize = {
		width: typeof containerSize.width === "number" ? containerSize.width : innerSize.width,
		height: typeof containerSize.height === "number" ? containerSize.height : innerSize.height
	};
	return {
		innerSize,
		outerSize,
		offset: {
			x: Math.round((outerSize.width - innerSize.width) / 2),
			y: Math.round((outerSize.height - innerSize.height) / 2)
		}
	};
}
function fallbackContainerSize(itemSize, maxCanvasSize) {
	const maxResolutionScale = Math.min(maxCanvasSize.width / itemSize.width, maxCanvasSize.height / itemSize.height);
	const resolutionScale = Math.min(2, maxResolutionScale);
	return {
		width: Math.floor(itemSize.width * resolutionScale),
		height: Math.floor(itemSize.height * resolutionScale)
	};
}
/**
* Creates and returns a blob from a data URL (either base64 encoded or not).
*
* @param {string} dataURL The data URL to convert.
* @return {Blob} A blob representing the array buffer data.
*/
function dataURLToBlob(dataURL) {
	const BASE64_MARKER = ";base64,";
	if (dataURL.indexOf(BASE64_MARKER) === -1) {
		const parts = dataURL.split(",");
		const contentType = parts[0].split(":")[1];
		const raw = decodeURIComponent(parts[1]);
		return new Blob([raw], { type: contentType });
	} else {
		const parts = dataURL.split(BASE64_MARKER);
		const contentType = parts[0].split(":")[1];
		const raw = window.atob(parts[1]);
		const rawLength = raw.length;
		const uInt8Array = new Uint8Array(rawLength);
		for (let i = 0; i < rawLength; ++i) uInt8Array[i] = raw.charCodeAt(i);
		return new Blob([uInt8Array], { type: contentType });
	}
}
//#endregion
//#region src/diagram/canvasArea.tsx
var CLASS_NAME$24 = "reactodia-canvas-area";
var DEFAULT_PAGE_SIZE = {
	width: 1500,
	height: 800
};
var VIEWPORT_DEFAULTS = { duration: 500 };
var LEFT_MOUSE_BUTTON = 0;
function CanvasArea(props) {
	const { model, renderingState, zoomOptions: partialZoomOptions, showScrollBars, watermarkSvg, watermarkUrl, children } = props;
	const colorSchemeApi = React.useContext(ColorSchemeApi);
	const paperRef = React.useRef(null);
	const linkLayerRef = React.useRef(null);
	const labelLayerRef = React.useRef(null);
	const elementLayerRef = React.useRef(null);
	const [placeLayerContext] = React.useState(() => createPlaceLayerContext());
	const zoomOptions = React.useMemo(() => zoomOptionsWithDefaults(partialZoomOptions), [partialZoomOptions]);
	const [contentBounds, setContentBounds] = React.useState({
		x: 0,
		y: 0,
		width: 0,
		height: 0
	});
	const [pointerMode, setPointerMode] = React.useState("panning");
	const [graphAnimations, setGraphAnimations] = React.useState({ count: 0 });
	const providedState = {
		colorSchemeApi,
		graphAnimations,
		watermarkSvg,
		zoomOptions
	};
	const [controller] = React.useState(() => new CanvasController(model, renderingState, providedState, {
		setContentBounds,
		setPointerMode,
		setGraphAnimations
	}, paperRef, linkLayerRef, labelLayerRef, elementLayerRef));
	React.useEffect(() => controller.setProvidedState(providedState));
	const canvasContext = React.useMemo(() => ({
		canvas: controller,
		model
	}), [model, controller]);
	const scaleDefaults = React.useMemo(() => ({
		min: zoomOptions.min,
		max: zoomOptions.max,
		maxFit: zoomOptions.maxFit,
		wheelToScaleDelta: controller.getWheelToScaleDelta
	}), [zoomOptions]);
	React.useLayoutEffect(() => {
		controller.centerTo();
		return () => controller.stopListening();
	}, []);
	const interactionBlocked = useObservedProperty(renderingState.events, "changeInteractionBlocked", () => renderingState.interactionBlocked);
	const style = { "--reactodia-canvas-animation-duration": graphAnimations.duration === void 0 ? void 0 : `${graphAnimations.duration}ms` };
	return /* @__PURE__ */ jsx(CanvasContext.Provider, {
		value: canvasContext,
		children: /* @__PURE__ */ jsx(Paper, {
			ref: paperRef,
			className: cx(CLASS_NAME$24, graphAnimations.count > 0 ? `${CLASS_NAME$24}--animated` : void 0),
			style,
			panOnTouch: pointerMode === "panning",
			showScrollbars: showScrollBars,
			scaleDefaults,
			viewportDefaults: VIEWPORT_DEFAULTS,
			onChangeTransform: controller.onChangeTransform,
			onContextMenu: controller.onContextMenu,
			onDragOver: controller.onDragOver,
			onDragDrop: controller.onDragDrop,
			onKeyDown: controller.onKeyDown,
			onKeyUp: controller.onKeyUp,
			onPointerOperation: controller.onPointerOperation,
			onResize: controller.onResize,
			onScrollPassive: controller.onScrollPassive,
			pageSize: DEFAULT_PAGE_SIZE,
			contentBounds,
			renderLayers: (paperTransform) => /* @__PURE__ */ jsxs(Fragment, { children: [
				/* @__PURE__ */ jsx(CanvasPlaceLayer, {
					layer: "underlay",
					context: placeLayerContext,
					className: `${CLASS_NAME$24}__widgets`
				}),
				/* @__PURE__ */ jsxs(SvgPaperLayer, {
					layerRef: linkLayerRef,
					className: `${CLASS_NAME$24}__linkGeometry`,
					style: { overflow: "visible" },
					paperTransform,
					role: "figure",
					children: [/* @__PURE__ */ jsx(LinkMarkers, {
						model,
						renderingState
					}), /* @__PURE__ */ jsx(LinkLayer, {
						model,
						renderingState
					})]
				}),
				/* @__PURE__ */ jsx(CanvasPlaceLayer, {
					layer: "overLinkGeometry",
					context: placeLayerContext,
					className: `${CLASS_NAME$24}__widgets`
				}),
				/* @__PURE__ */ jsx(LinkLabelLayer, {
					renderingState,
					paperTransform,
					layerRef: labelLayerRef
				}),
				/* @__PURE__ */ jsx(CanvasPlaceLayer, {
					layer: "overLinks",
					context: placeLayerContext,
					className: `${CLASS_NAME$24}__widgets`
				}),
				/* @__PURE__ */ jsx(ElementLayer, {
					layerRef: elementLayerRef,
					model,
					renderingState,
					paperTransform
				}),
				/* @__PURE__ */ jsx(CanvasPlaceLayer, {
					layer: "overElements",
					context: placeLayerContext,
					className: `${CLASS_NAME$24}__widgets`
				})
			] }),
			paneProps: { inert: interactionBlocked ? "" : void 0 },
			watermark: watermarkSvg ? /* @__PURE__ */ jsx("a", {
				href: watermarkUrl,
				target: "_blank",
				rel: "noreferrer",
				children: /* @__PURE__ */ jsx("img", {
					className: `${CLASS_NAME$24}__watermark`,
					src: watermarkSvg,
					draggable: false
				})
			}) : null,
			children: /* @__PURE__ */ jsx(CanvasPlaceLayerContext.Provider, {
				value: placeLayerContext,
				children
			})
		})
	});
}
var CanvasController = class {
	model;
	renderingState;
	state;
	setters;
	_paper;
	linkLayer;
	labelLayer;
	elementLayer;
	listener = new EventObserver();
	source = new EventSource();
	events = this.source;
	_contentBounds = {
		x: 0,
		y: 0,
		width: 0,
		height: 0
	};
	_pointerMode = "panning";
	constructor(model, renderingState, state, setters, _paper, linkLayer, labelLayer, elementLayer) {
		this.model = model;
		this.renderingState = renderingState;
		this.state = state;
		this.setters = setters;
		this._paper = _paper;
		this.linkLayer = linkLayer;
		this.labelLayer = labelLayer;
		this.elementLayer = elementLayer;
		const delayedAdjust = () => {
			renderingState.scheduleOnLayerUpdate(RenderingLayer.PaperArea, this.updateContentBounds);
		};
		this.listener.listen(model.events, "changeCells", delayedAdjust);
		this.listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changePosition) delayedAdjust();
		});
		this.listener.listen(model.events, "linkEvent", ({ data }) => {
			if (data.changeVertices) delayedAdjust();
		});
		this.listener.listen(renderingState.events, "changeElementSize", delayedAdjust);
		this.listener.listen(renderingState.shared.events, "findCanvas", (e) => {
			e.canvases.push(this);
		});
	}
	get paper() {
		const paper = this._paper.current;
		if (!paper) throw new Error("Reactodia: canvas paper is not initialized yet");
		return paper;
	}
	stopListening() {
		this.listener.stopListening();
	}
	setProvidedState(state) {
		this.state = state;
	}
	onChangeTransform = (previous) => {
		this.source.trigger("changeTransform", {
			previous,
			source: this
		});
		if (this.paper.metrics.transform.scale !== previous.scale) this.source.trigger("changeScale", {
			previous: previous.scale,
			source: this
		});
	};
	onContextMenu = (e) => {
		if (!this.isEventFromCellLayer(e)) return;
		const cell = findCell(e.target, e.currentTarget, this.model);
		this.source.trigger("contextMenu", {
			source: this,
			sourceEvent: e,
			target: cell
		});
	};
	onDragOver = (e, clientCoords) => {
		if (this.renderingState.shared.hasHandlerForNextDropOnPaper()) {
			if (e.dataTransfer) e.dataTransfer.dropEffect = "move";
			return true;
		} else {
			const { x, y } = clientCoords;
			const position = this.metrics.clientToPaperCoords(x, y);
			let allowDrop = false;
			const event = {
				source: this,
				sourceEvent: e,
				position,
				allowDrop: () => {
					allowDrop = true;
				}
			};
			this.source.trigger("dragover", event);
			return allowDrop;
		}
	};
	onDragDrop = (e, clientCoords) => {
		const { x, y } = clientCoords;
		const position = this.metrics.clientToPaperCoords(x, y);
		const event = {
			source: this,
			sourceEvent: e,
			position
		};
		if (this.renderingState.shared.tryHandleDropOnPaper(event)) {} else this.source.trigger("drop", event);
	};
	onKeyDown = (e) => {
		if (!this.isEventFromCellLayer(e)) return;
		this.source.trigger("keydown", {
			source: this,
			sourceEvent: e
		});
	};
	onKeyUp = (e) => {
		if (!this.isEventFromCellLayer(e)) return;
		this.source.trigger("keyup", {
			source: this,
			sourceEvent: e
		});
	};
	onPointerOperation = (event) => {
		if (event.button !== LEFT_MOUSE_BUTTON) return;
		let panning = false;
		let selection = false;
		let moveElement;
		let moveLink = false;
		const target = findCell(event.target, event.currentTarget, this.model);
		if (!target) {
			panning = this.shouldStartPanning(event);
			selection = !panning && this._pointerMode === "selection";
		} else if (target instanceof Element) {
			const { x: pointerX, y: pointerY } = this.metrics.pageToPaperCoords(event.pageX, event.pageY);
			const { x: elementX, y: elementY } = target.position;
			moveElement = {
				pointerX,
				pointerY,
				elementX,
				elementY
			};
		} else if (target instanceof Link || target instanceof LinkVertex) moveLink = true;
		const restoreGeometry = RestoreGeometry.capture(this.model);
		const batch = this.model.history.startBatch(restoreGeometry.title);
		const state = {
			target,
			restoreGeometry,
			batch,
			panning,
			moveElement
		};
		return {
			action: panning ? "panning" : selection || moveElement || moveLink ? "move" : void 0,
			hasSameTarget: (e) => {
				const cell = findCell(e.target, e.currentTarget, this.model);
				return cell !== void 0 && cell === target;
			},
			onPointerDown: (e) => {
				this.source.trigger("pointerDown", {
					source: this,
					sourceEvent: e,
					target,
					panning
				});
			},
			onPointerMove: (e) => this.onPointerMove(e, state),
			onPointerUp: (e, options) => this.onPointerUp(e, options, state),
			onPointerCancel: () => {
				const restore = restoreGeometry.filterOutUnchanged();
				if (restore.hasChanges()) batch.history.registerToUndo(restore);
				batch.discard({ revert: true });
			}
		};
	};
	shouldStartPanning(e) {
		const requireShift = this._pointerMode === "selection";
		return e.pointerType === "mouse" && e.shiftKey === requireShift && !(e.ctrlKey || e.altKey);
	}
	onPointerMove(e, state) {
		const { target, panning, moveElement } = state;
		if (!target) this.source.trigger("pointerMove", {
			source: this,
			sourceEvent: e,
			target,
			panning
		});
		else if (target instanceof Element) {
			if (moveElement) {
				const { x, y } = this.metrics.pageToPaperCoords(e.pageX, e.pageY);
				const { pointerX, pointerY, elementX, elementY } = moveElement;
				target.setPosition({
					x: elementX + x - pointerX,
					y: elementY + y - pointerY
				});
				this.source.trigger("pointerMove", {
					source: this,
					sourceEvent: e,
					target,
					panning
				});
			}
		} else if (target instanceof Link) {
			const location = this.metrics.pageToPaperCoords(e.pageX, e.pageY);
			const linkVertex = this.generateLinkVertex(target, location);
			linkVertex.createAt(location);
			state.target = linkVertex;
		} else if (target instanceof LinkVertex) {
			const location = this.metrics.pageToPaperCoords(e.pageX, e.pageY);
			target.moveTo(location);
			this.source.trigger("pointerMove", {
				source: this,
				sourceEvent: e,
				target,
				panning
			});
		}
	}
	generateLinkVertex(link, location) {
		const { model, renderingState } = this;
		const previous = link.vertices;
		const vertices = previous ? [...previous] : [];
		const source = model.getElement(link.sourceId);
		const target = model.getElement(link.targetId);
		return new LinkVertex(link, findNearestSegmentIndex(computePolyline(renderingState.getElementShape(source), renderingState.getElementShape(target), vertices), location));
	}
	onPointerUp(e, options, state) {
		const { triggerAsClick } = options;
		const { target, restoreGeometry, batch, panning } = state;
		this.source.trigger("pointerUp", {
			source: this,
			sourceEvent: e,
			target,
			panning,
			triggerAsClick
		});
		const restore = restoreGeometry.filterOutUnchanged();
		if (restore.hasChanges()) batch.history.registerToUndo(restore);
		batch.store();
	}
	onResize = () => {
		this.source.trigger("resize", { source: this });
	};
	onScrollPassive = (e) => {
		if (!this.isEventFromCellLayer(e)) return;
		this.source.trigger("scroll", {
			source: this,
			sourceEvent: e
		});
	};
	getWheelToScaleDelta = (e) => {
		switch (this.shouldZoom(e)) {
			case "zoom": return wheelToScaleDeltaDefault(e);
			case "prevent": return 0;
			default: return;
		}
	};
	shouldZoom(e) {
		const { requireCtrl } = this.zoomOptions;
		const target = e.target;
		if (requireCtrl) return e.ctrlKey ? "zoom" : "default";
		else if (e.ctrlKey) return "zoom";
		if (this.paper.root && target instanceof window.Element) {
			const layer = findParentWithin(target, this.paper.root, isCanvasPlaceLayer);
			if (layer) return hasScrollableParent(target, layer) ? "default" : findParentWithin(target, layer, doesPreventZoom) ? "prevent" : "zoom";
		}
		return this.isEventFromCellLayer(e) && target instanceof Node && (this.paper.root === target || this.paper.pane === target || this.paper.pane?.firstChild === target || !hasScrollableParent(target, this.linkLayer.current) && !hasScrollableParent(target, this.labelLayer.current) && !hasScrollableParent(target, this.elementLayer.current)) ? "zoom" : "default";
	}
	get metrics() {
		return this.paper.metrics;
	}
	get pointerMode() {
		return this._pointerMode;
	}
	setPointerMode(value) {
		const previous = this._pointerMode;
		if (previous === value) return;
		this._pointerMode = value;
		this.setters.setPointerMode(value);
		this.source.trigger("changePointerMode", {
			source: this,
			previous
		});
	}
	get zoomOptions() {
		return this.state.zoomOptions;
	}
	updateContentBounds = () => {
		const { elements, links } = this.model;
		this._contentBounds = getContentFittingBox(elements, links, this.renderingState);
		this.setters.setContentBounds(this._contentBounds);
	};
	isEventFromCellLayer(e) {
		const target = e.target;
		return target instanceof Node && Boolean(this.paper.root === target || this.paper.pane === target || this.paper.pane?.firstChild === target || this.linkLayer.current?.contains(target) || this.labelLayer.current?.contains(target) || this.elementLayer.current?.contains(target));
	}
	focus() {
		this.paper.root?.focus({ preventScroll: true });
	}
	async centerTo(paperPosition, options = {}) {
		await this.renderingState.updateLayersUpTo(RenderingLayer.PaperArea);
		const { width, height } = this.paper.metrics.transform;
		const paperCenter = paperPosition || {
			x: width / 2,
			y: height / 2
		};
		return this.paper.centerViewport(paperCenter, options);
	}
	async centerContent(options = {}) {
		await this.renderingState.updateLayersUpTo(RenderingLayer.PaperArea);
		const bbox = this._contentBounds;
		return this.centerTo({
			x: bbox.x + bbox.width / 2,
			y: bbox.y + bbox.height / 2
		}, options);
	}
	getScale() {
		return this.paper.scale;
	}
	setScale(value, options) {
		return this.paper.setScale(value, options);
	}
	zoomBy(value, options) {
		return this.setScale(this.getScale() + value, options);
	}
	zoomIn(scaleOptions) {
		return this.zoomBy(this.state.zoomOptions.step, scaleOptions);
	}
	zoomOut(scaleOptions) {
		return this.zoomBy(-this.state.zoomOptions.step, scaleOptions);
	}
	async zoomToFit(options = {}) {
		const { model, renderingState } = this;
		const { elements, links } = model;
		if (elements.length === 0) return this.centerTo();
		await this.renderingState.updateLayersUpTo(RenderingLayer.PaperArea);
		const bbox = getContentFittingBox(elements, links, renderingState);
		return this.zoomToFitRect(bbox, options);
	}
	zoomToFitRect(paperRect, options) {
		const { fitPadding } = this.state.zoomOptions;
		const paddedRect = {
			x: paperRect.x - fitPadding,
			y: paperRect.y - fitPadding,
			width: paperRect.width + fitPadding,
			height: paperRect.height + fitPadding
		};
		return this.paper.scaleToFitRect(paddedRect, options);
	}
	makeToSVGOptions(baseOptions) {
		const bounds = this._contentBounds;
		const { contentPadding = {
			x: 100,
			y: 100
		}, removeByCssSelectors = [], transformExported, embedFonts } = baseOptions;
		const linkLayer = this.linkLayer.current;
		const labelLayer = this.labelLayer.current;
		const elementLayer = this.elementLayer.current;
		if (!(linkLayer && labelLayer && elementLayer)) throw new Error("Cannot find element, link or label layers to export");
		return {
			colorSchemeApi: this.state.colorSchemeApi,
			styleRoot: linkLayer,
			contentBox: {
				x: bounds.x - contentPadding.x,
				y: bounds.y - contentPadding.y,
				width: bounds.width + contentPadding.x * 2,
				height: bounds.height + contentPadding.y * 2
			},
			layers: [
				linkLayer,
				labelLayer,
				elementLayer
			],
			preserveDimensions: true,
			convertImagesToDataUris: true,
			removeByCssSelectors: ["[data-reactodia-no-export]", ...removeByCssSelectors],
			transformExported,
			embedFonts,
			watermarkSvg: this.state.watermarkSvg
		};
	}
	exportSvg(options = {}) {
		return toSVG(this.makeToSVGOptions(options));
	}
	exportRaster(options = {}) {
		return toDataURL({
			...options,
			...this.makeToSVGOptions(options)
		});
	}
	isAnimatingGraph() {
		return this.state.graphAnimations.count > 0;
	}
	animateGraph(setupChanges, duration) {
		const timeout = typeof duration === "number" ? duration : VIEWPORT_DEFAULTS.duration;
		this.changeGraphAnimationCount(1, timeout);
		setupChanges();
		return delay(timeout).then(() => this.changeGraphAnimationCount(-1));
	}
	changeGraphAnimationCount(change, newDuration) {
		const beforeAnimating = this.isAnimatingGraph();
		flushSync(() => {
			this.setters.setGraphAnimations((previous) => ({
				count: previous.count + change,
				duration: newDuration ?? previous.duration
			}));
		});
		if (this.isAnimatingGraph() !== beforeAnimating) this.source.trigger("changeAnimatingGraph", {
			source: this,
			previous: beforeAnimating
		});
	}
};
function zoomOptionsWithDefaults(zoomOptions = {}) {
	return {
		min: zoomOptions.min ?? .2,
		max: zoomOptions.max ?? 2,
		maxFit: zoomOptions.maxFit ?? 1,
		step: zoomOptions.step ?? .1,
		fitPadding: zoomOptions.fitPadding ?? 20,
		requireCtrl: zoomOptions.requireCtrl ?? false
	};
}
function findCell(bottom, top, model) {
	if (!(bottom instanceof globalThis.Element)) return;
	let target = bottom;
	let vertexIndex;
	while (true) {
		if (target instanceof globalThis.Element) {
			if (target.hasAttribute("data-element-id")) return model.getElement(target.getAttribute("data-element-id"));
			else if (target.hasAttribute("data-link-id")) {
				const link = model.getLink(target.getAttribute("data-link-id"));
				if (!link) return;
				return typeof vertexIndex === "number" ? new LinkVertex(link, vertexIndex) : link;
			} else if (target.hasAttribute("data-vertex")) vertexIndex = Number(target.getAttribute("data-vertex"));
		}
		if (!target || target === top) break;
		target = target.parentNode;
	}
}
function hasScrollableParent(target, parent) {
	if (!(parent && parent.contains(target))) return false;
	let current = target;
	while (current && current !== parent) {
		if (current instanceof window.Element) {
			const style = getComputedStyle(current);
			if (style.overflowX === "scroll" || style.overflowY === "scroll") return true;
		}
		current = current.parentNode;
	}
	return false;
}
function doesPreventZoom(target) {
	return target.hasAttribute("data-reactodia-prevent-zoom");
}
//#endregion
//#region src/widgets/canvas.tsx
var CLASS_NAME$23 = "reactodia-canvas";
/**
* Component to display a canvas for the diagram with elements, links and additional widgets.
*
* @category Components
*/
function Canvas(props) {
	const { model, view, overlay } = useWorkspace();
	const { elementTemplateResolver, linkTemplateResolver, linkRouter, showScrollbars, zoomOptions, watermarkSvg, watermarkUrl, children } = props;
	const [renderingState] = React.useState(() => new MutableRenderingState({
		model,
		shared: view,
		elementTemplateResolver: elementTemplateResolver ? ((element) => {
			return elementTemplateResolver((element instanceof EntityElement ? element.data : void 0)?.types ?? [], element);
		}) : void 0,
		linkTemplateResolver: linkTemplateResolver ? ((link) => {
			return linkTemplateResolver(link instanceof RelationLink || link instanceof RelationGroup ? link.typeId : void 0, link);
		}) : void 0,
		linkRouter
	}));
	return /* @__PURE__ */ jsx("div", {
		className: CLASS_NAME$23,
		children: /* @__PURE__ */ jsxs(CanvasArea, {
			model,
			renderingState,
			zoomOptions,
			showScrollBars: showScrollbars,
			watermarkSvg,
			watermarkUrl,
			children: [
				children,
				/* @__PURE__ */ jsx(OverlaySupport, { overlay }),
				/* @__PURE__ */ jsx(RenameLinkSupport, {})
			]
		})
	});
}
//#endregion
//#region src/widgets/connectionsMenu/menuCommon.tsx
var CLASS_NAME$22 = "reactodia-connections-menu";
function LoadingSpinner(props) {
	return /* @__PURE__ */ jsx("div", {
		className: `${CLASS_NAME$22}__spinner`,
		children: /* @__PURE__ */ jsx(HtmlSpinner, {
			width: 30,
			height: 30,
			errorOccurred: Boolean(props.error)
		})
	});
}
//#endregion
//#region src/widgets/connectionsMenu/connectionList.tsx
function ConnectionList(props) {
	const { data, filterKey, sortMode, suggestions, allRelatedLink, onExpandLink, onMoveToFilter, scrolledListRef } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const isSmartMode = sortMode === "smart" && !filterKey;
	const textFilter = filterKey ? makeCaseInsensitiveFilter(filterKey) : void 0;
	const links = isSmartMode ? [] : (data.links || []).map((link) => model.getLinkType(link.id)?.data ?? link).filter((link) => {
		const text = t.formatLabel(link.label, link.id, model.language);
		return !textFilter || textFilter(text);
	}).sort(makeLinkTypeComparer(t, model.language));
	const probableLinks = (data.links ?? []).map((link) => model.getLinkType(link.id)?.data ?? link).filter((link) => {
		const score = suggestions.scores.get(link.id);
		return !links.includes(link) && score !== void 0 && (score.score > 0 || isSmartMode);
	}).sort(makeLinkTypeComparer(t, model.language, suggestions));
	const regularEntries = getConnectionLinks(links, {
		counts: data.counts,
		scores: suggestions.scores
	});
	const probableEntries = getConnectionLinks(probableLinks, {
		counts: data.counts,
		scores: suggestions.scores,
		probable: true
	});
	const entries = [];
	if (regularEntries.length > 1 || isSmartMode && probableEntries.length > 1) {
		const { inCount, outCount, inexact } = data.counts.get(allRelatedLink.id);
		const totalCount = inCount + outCount;
		entries.push({
			type: "link",
			key: allRelatedLink.id,
			linkType: allRelatedLink,
			count: inexact && totalCount > 0 ? "some" : totalCount
		});
		entries.push({ type: "separator" });
	}
	for (const entry of regularEntries) entries.push(entry);
	if (probableEntries.length > 0) {
		if (isSmartMode) {
			entries.push({ type: "probable-hint" });
			for (const entry of probableEntries) entries.push(entry);
		}
	}
	return /* @__PURE__ */ jsxs("div", {
		className: cx(`${CLASS_NAME$22}__links-list`, "reactodia-scrollable", entries.length === 0 ? `${CLASS_NAME$22}__links-list-empty` : void 0),
		ref: scrolledListRef,
		tabIndex: -1,
		children: [/* @__PURE__ */ jsx(FocusGroup, { children: ({ ref, controller }) => /* @__PURE__ */ jsx("ul", {
			ref,
			className: `${CLASS_NAME$22}__links-root`,
			role: "list",
			onClick: controller.defaultClick,
			onKeyDown: controller.defaultKeyDown,
			children: entries.map((item) => {
				if (item.type === "link") return /* @__PURE__ */ jsx(ConnectionLink, {
					link: item.linkType,
					direction: item.direction,
					count: item.count,
					filterKey: item.probable ? "" : filterKey,
					probability: item.probability,
					onExpandLink,
					onMoveToFilter
				}, getEntryKey(item));
				else if (item.type === "separator") return /* @__PURE__ */ jsx("hr", { className: `${CLASS_NAME$22}__links-list-hr` }, getEntryKey(item));
				else if (item.type === "probable-hint") return /* @__PURE__ */ jsx("li", {
					className: `${CLASS_NAME$22}__links-probably-label`,
					children: t.text("connections_menu.links.suggest_similar")
				}, getEntryKey(item));
				return null;
			})
		}) }), entries.length === 0 ? /* @__PURE__ */ jsx("label", {
			className: `${CLASS_NAME$22}__links-no-results`,
			children: t.text("connections_menu.links.no_results")
		}) : null]
	});
}
function getEntryKey(entry) {
	return entry.type === "link" ? entry.key : entry.type;
}
function getConnectionLinks(links, options) {
	const { counts, scores, probable } = options;
	const entries = [];
	const addView = (link, direction) => {
		const { inCount, outCount, inexact } = counts.get(link.id) ?? {
			inCount: 0,
			outCount: 0,
			inexact: false
		};
		const count = direction === "in" ? inCount : outCount;
		if (count === 0) return;
		const score = scores.get(link.id);
		entries.push({
			type: "link",
			key: `${direction}-${probable ? "probable" : ""}-${link.id}`,
			linkType: link,
			direction,
			count: inexact && count > 0 ? "some" : count,
			probable,
			probability: probable && score !== void 0 ? score.score : 0
		});
	};
	for (const link of links) {
		addView(link, "in");
		addView(link, "out");
	}
	return entries;
}
function makeLinkTypeComparer(t, language, suggestions) {
	return (a, b) => {
		if (suggestions) {
			const { scores } = suggestions;
			const aWeight = scores.has(a.id) ? scores.get(a.id).score : 0;
			const bWeight = scores.has(b.id) ? scores.get(b.id).score : 0;
			if (aWeight > bWeight) return -1;
			else if (aWeight < bWeight) return 1;
		}
		const aText = t.formatLabel(a.label, a.id, language);
		const bText = t.formatLabel(b.label, b.id, language);
		return aText.localeCompare(bText);
	};
}
function ConnectionLink(props) {
	const { link, filterKey, direction, count, onExpandLink, onMoveToFilter, probability = 0 } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const { ref, tabIndex } = useFocusGroupItem();
	const relation = t.formatLabel(link.label, link.id, model.language);
	const relationIri = model.locale.formatIri(link.id);
	const probabilityPercent = Math.round(probability * 100);
	const textLine = highlightSubstring(relation + (probabilityPercent > 0 ? ` (${probabilityPercent}%)` : ""), filterKey);
	const title = direction === "in" ? t.text("connections_menu.link.source_title", {
		relation,
		relationIri
	}) : direction === "out" ? t.text("connections_menu.link.target_title", {
		relation,
		relationIri
	}) : t.text("connections_menu.link.both_title", {
		relation,
		relationIri
	});
	const navigateTitle = direction === "in" ? t.text("connections_menu.link.source_navigate_title", {
		relation,
		relationIri
	}) : direction === "out" ? t.text("connections_menu.link.target_navigate_title", {
		relation,
		relationIri
	}) : t.text("connections_menu.link.both_navigate_title", {
		relation,
		relationIri
	});
	const onExpandLinkClick = () => {
		onExpandLink({
			chunkId: generate128BitID(),
			linkType: link,
			direction,
			expectedCount: count,
			pageCount: 1
		});
	};
	const onMoveToFilterClick = (e) => {
		e.stopPropagation();
		onMoveToFilter?.({
			chunkId: generate128BitID(),
			linkType: link,
			direction,
			expectedCount: count,
			pageCount: 1
		});
	};
	return /* @__PURE__ */ jsxs("li", {
		ref,
		className: `${CLASS_NAME$22}__link`,
		role: "listitem",
		"data-linktypeid": link.id,
		children: [/* @__PURE__ */ jsxs("button", {
			className: `${CLASS_NAME$22}__link-button`,
			title,
			onClick: onExpandLinkClick,
			tabIndex,
			children: [
				direction === "in" || direction === "out" ? /* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$22}__link-direction`,
					children: [direction === "in" && /* @__PURE__ */ jsx("div", { className: `reactodia-connections-menu__link-direction-in` }), direction === "out" && /* @__PURE__ */ jsx("div", { className: `reactodia-connections-menu__link-direction-out` })]
				}) : null,
				/* @__PURE__ */ jsx(WithFetchStatus, {
					type: "linkType",
					target: link.id,
					children: /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$22}__link-title`,
						children: textLine
					})
				}),
				count === "some" ? null : /* @__PURE__ */ jsx("span", {
					className: `reactodia-badge ${CLASS_NAME$22}__link-count`,
					children: count <= 100 ? count : `100+`
				}),
				onMoveToFilter ? /* @__PURE__ */ jsx("div", { className: `${CLASS_NAME$22}__link-filter-spacer` }) : null,
				/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$22}__link-navigate-button`,
					title: navigateTitle
				})
			]
		}), onMoveToFilter ? /* @__PURE__ */ jsx("button", {
			className: `${CLASS_NAME$22}__link-filter-button`,
			onClick: onMoveToFilterClick,
			title: t.text("connections_menu.link.move_to_filter.title"),
			tabIndex
		}) : null]
	});
}
//#endregion
//#region src/widgets/connectionsMenu/entityList.tsx
function EntityList(props) {
	const { data, isLoading, filterKey, onPressAddSelected, onMoveToFilter } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const language = useObservedProperty(model.events, "changeLanguage", () => model.language);
	const [selection, setSelection] = React.useState(() => /* @__PURE__ */ new Set());
	const objects = React.useMemo(() => {
		if (!filterKey) return data.elements;
		const textFilter = makeCaseInsensitiveFilter(filterKey);
		return data.elements.filter((element) => {
			return textFilter(model.locale.formatEntityLabel(element, language));
		});
	}, [
		data.elements,
		filterKey,
		language
	]);
	const presentEntities = getAllPresentEntities(model);
	const isAllSelected = objects.every((item) => presentEntities.has(item.id) || selection.has(item.id));
	const nonPresented = objects.filter((item) => !presentEntities.has(item.id));
	const active = nonPresented.filter((item) => selection.has(item.id));
	const selectedItems = active.length > 0 ? active : nonPresented;
	const countString = t.text("connections_menu.entities.counter_label", {
		count: active.length,
		total: data.elements.length
	});
	let extraCountInfo = null;
	if (data.chunk.expectedCount !== "some") {
		const extraCount = data.elements.length - Math.min(100, data.chunk.expectedCount);
		const extra = Math.abs(extraCount) > 100 ? `100+` : Math.abs(extraCount).toString();
		extraCountInfo = /* @__PURE__ */ jsx("span", {
			className: `${CLASS_NAME$22}__objects-extra`,
			title: extraCount === 0 ? void 0 : extraCount > 0 ? t.text("connections_menu.entities.extra_title", { value: extra }) : t.text("connections_menu.entities.missing_title", { value: extra }),
			children: extraCount === 0 ? null : extraCount > 0 ? t.text("connections_menu.entities.extra_label", { value: extra }) : t.text("connections_menu.entities.missing_label", { value: extra })
		});
	}
	const counter = /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$22}__objects-count`,
		children: [/* @__PURE__ */ jsx("span", { children: countString }), extraCountInfo]
	});
	return /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$22}__objects`,
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$22}__objects-select-all`,
				children: [
					/* @__PURE__ */ jsxs("label", { children: [/* @__PURE__ */ jsx("input", {
						type: "checkbox",
						name: "reactodia-connections-menu-select-all",
						checked: isAllSelected && nonPresented.length > 0,
						onChange: () => setSelection(new Set(isAllSelected ? [] : nonPresented.map((item) => item.id))),
						disabled: nonPresented.length === 0,
						title: t.text("connections_menu.select_all.title")
					}), /* @__PURE__ */ jsx("span", { children: t.text("connections_menu.select_all.label") })] }),
					/* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$22}__objects-spacer`,
						"aria-hidden": "true"
					}),
					counter
				]
			}),
			isLoading ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$22}__objects-loading`,
				children: /* @__PURE__ */ jsx(LoadingSpinner, {})
			}) : objects.length === 0 ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$22}__objects-no-results`,
				children: t.text("connections_menu.entities.no_results")
			}) : /* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$22}__objects-list`,
				tabIndex: -1,
				children: [/* @__PURE__ */ jsx(SearchResults, {
					items: objects,
					selection,
					onSelectionChanged: setSelection,
					highlightText: filterKey
				}), data.chunk.expectedCount !== "some" && data.chunk.expectedCount > 100 ? onMoveToFilter ? /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$22}__move-to-filter`,
					onClick: () => onMoveToFilter(data.chunk),
					children: t.text("connections_menu.entities.truncated_results_expand", { limit: 100 })
				}) : /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$22}__move-to-filter`,
					children: t.text("connections_menu.entities.truncated_results", { limit: 100 })
				}) : null]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$22}__objects-statusbar`,
				children: [/* @__PURE__ */ jsx("button", {
					className: cx(`${CLASS_NAME$22}__objects-add-button`, "reactodia-btn reactodia-btn-secondary"),
					disabled: isLoading || selectedItems.length <= 1,
					onClick: () => onPressAddSelected(selectedItems, "grouped"),
					children: t.text("connections_menu.entities.add_group")
				}), /* @__PURE__ */ jsx("button", {
					className: cx(`${CLASS_NAME$22}__objects-add-button`, "reactodia-btn reactodia-btn-primary"),
					disabled: isLoading || nonPresented.length === 0,
					onClick: () => onPressAddSelected(selectedItems, "separately"),
					children: active.length > 0 ? t.text("connections_menu.entities.add_selected") : t.text("connections_menu.entities.add_all")
				})]
			})
		]
	});
}
//#endregion
//#region src/widgets/connectionsMenu/connectionsMenu.tsx
/**
* Canvas widget component to explore and navigate the graph by adding
* connected entities to the diagram.
*
* @category Components
*/
function ConnectionsMenu(props) {
	const workspace = useWorkspace();
	const { canvas } = useCanvas();
	const t = useTranslation();
	const lastSortMode = React.useRef("alphabet");
	const commands = workspace.getCommandBus(ConnectionsMenuTopic);
	React.useEffect(() => {
		const listener = new EventObserver();
		listener.listen(commands, "findCapabilities", (e) => {
			e.capabilities.push({});
		});
		listener.listen(commands, "show", ({ targets }) => {
			if (targets.length === 0) return;
			const { model, overlay } = workspace;
			const virtualTarget = targets.length > 1 ? new VirtualTarget(targets, model, canvas) : void 0;
			const placeTarget = virtualTarget ?? targets[0];
			const targetIris = /* @__PURE__ */ new Set();
			for (const target of targets) for (const entity of iterateEntitiesOf(target)) targetIris.add(entity.id);
			overlay.showDialog({
				target: placeTarget,
				dialogType: BuiltinDialogType.connectionsMenu,
				style: {
					caption: t.text("connections_menu.dialog.caption"),
					defaultSize: {
						width: 310,
						height: 340
					},
					minSize: {
						width: 300,
						height: 250
					}
				},
				content: /* @__PURE__ */ jsx(ConnectionsMenuContent, {
					...props,
					placeTarget,
					targetIris: Array.from(targetIris),
					initialMode: lastSortMode.current,
					onChangeMode: (mode) => {
						lastSortMode.current = mode;
					},
					onCancel: () => overlay.hideDialog()
				}),
				onClose: () => virtualTarget?.removeFrom(model)
			});
		});
		return () => listener.stopListening();
	}, [commands]);
	return null;
}
var VirtualTarget = class extends VoidElement {
	listener = new EventObserver();
	constructor(elements, model, canvas) {
		super({});
		const elementSet = new Set(elements);
		const updateTargetPosition = () => {
			const { x, y, width, height } = getContentFittingBox(elements, [], canvas.renderingState);
			this.setPosition({
				x: x + width,
				y: y + height / 2
			});
		};
		updateTargetPosition();
		const batch = model.history.startBatch();
		model.addElement(this);
		batch.discard();
		this.listener.listen(model.events, "changeCells", (e) => {
			if (e.changedElement === this || e.updateAll) {
				if (model.getElement(this.id) !== this) this.listener.stopListening();
			}
		});
		this.listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changePosition && elementSet.has(data.changePosition.source)) updateTargetPosition();
		});
		this.listener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
			if (elementSet.has(e.source)) updateTargetPosition();
		});
	}
	removeFrom(model) {
		this.listener.stopListening();
		if (model.getElement(this.id) === this) {
			const batch = model.history.startBatch();
			model.removeElement(this.id);
			batch.discard();
		}
	}
};
function ConnectionsMenuContent(props) {
	const { canvas } = useCanvas();
	const workspace = useWorkspace();
	const t = useTranslation();
	const connectionSearch = useSearchInputStore({
		initialValue: "",
		submitTimeout: 200
	});
	const objectSearch = useSearchInputStore({ initialValue: "" });
	return /* @__PURE__ */ jsx(ConnectionsMenuInner, {
		...props,
		connectionSearch,
		objectSearch,
		canvas,
		workspace,
		translation: t
	});
}
var ConnectionsMenuInner = class extends React.Component {
	ALL_RELATED_ELEMENTS_LINK;
	handler = new EventObserver();
	linkTypesListener = new EventObserver();
	suggestionCancellation = new AbortController();
	linksScrolledListRef = React.createRef();
	linksScrollPosition;
	constructor(props) {
		super(props);
		const { targetIris, initialMode, suggestProperties, workspace: { model }, translation: t } = this.props;
		this.ALL_RELATED_ELEMENTS_LINK = {
			id: "urn:reactodia:allLinks",
			label: [model.factory.literal(t.text("connections_menu.all_link.label"))]
		};
		const allowSmartSort = Boolean(suggestProperties) && targetIris.length === 1;
		this.state = {
			loadingState: "none",
			panel: "connections",
			connectionSortMode: allowSmartSort ? initialMode : "alphabet",
			connectionSuggestions: {
				filterKey: null,
				scores: /* @__PURE__ */ new Map()
			}
		};
	}
	updateAll = () => this.forceUpdate();
	scheduleUpdateAll = () => {
		const { canvas } = this.props;
		canvas.renderingState.scheduleOnLayerUpdate(RenderingLayer.Overlay, this.updateAll);
	};
	componentDidMount() {
		const { connectionSearch, objectSearch, workspace: { model } } = this.props;
		this.handler.listen(model.events, "changeLanguage", this.scheduleUpdateAll);
		this.handler.listen(connectionSearch.events, "changeValue", this.updateAll);
		this.handler.listen(connectionSearch.events, "executeSearch", ({ value }) => {
			this.loadSuggestions(value);
		});
		this.handler.listen(objectSearch.events, "changeValue", this.updateAll);
		this.loadLinks();
	}
	getSnapshotBeforeUpdate(prevProps, prevState) {
		if (this.state.panel === "objects" && prevState.panel === "connections") {
			if (this.linksScrolledListRef.current) this.linksScrollPosition = this.linksScrolledListRef.current.scrollTop;
		}
		return 0;
	}
	componentDidUpdate(prevProps, prevState) {
		if (this.state.panel === "connections" && prevState.panel === "objects") {
			if (this.linksScrollPosition !== void 0 && this.linksScrolledListRef.current) this.linksScrolledListRef.current.scrollTop = this.linksScrollPosition;
		}
	}
	componentWillUnmount() {
		const { canvas } = this.props;
		this.handler.stopListening();
		this.linkTypesListener.stopListening();
		canvas.renderingState.cancelOnLayerUpdate(RenderingLayer.Overlay, this.updateAll);
		this.suggestionCancellation.abort();
	}
	async loadLinks() {
		const { targetIris, workspace: { model, triggerWorkspaceEvent } } = this.props;
		this.setState({
			loadingState: "loading",
			connections: {
				links: [],
				counts: /* @__PURE__ */ new Map()
			}
		});
		const requestInexact = targetIris.length > 1;
		const counts = /* @__PURE__ */ new Map();
		try {
			await Promise.all(targetIris.map((iri) => model.dataProvider.connectedLinkStats({
				elementId: iri,
				inexactCount: requestInexact
			}).then((linkTypes) => {
				for (const { id: linkTypeId, inCount, outCount, inexact } of linkTypes) {
					const previous = counts.get(linkTypeId) ?? {
						inCount: 0,
						outCount: 0,
						inexact: false
					};
					counts.set(linkTypeId, {
						inexact: Boolean(requestInexact || previous.inexact || inexact),
						inCount: previous.inCount + inCount,
						outCount: previous.outCount + outCount
					});
				}
			})));
		} catch (err) {
			console.error(err);
			this.setState({ loadingState: "error" });
			return;
		}
		const links = Array.from(counts.keys(), (linkTypeId) => model.createLinkType(linkTypeId)?.data ?? {
			id: linkTypeId,
			label: []
		});
		counts.set(this.ALL_RELATED_ELEMENTS_LINK.id, Array.from(counts.values()).reduce((a, b) => ({
			inexact: Boolean(a.inexact || b.inexact),
			inCount: a.inCount + b.inCount,
			outCount: a.outCount + b.outCount
		}), {
			inCount: 0,
			outCount: 0,
			inexact: false
		}));
		this.setState({
			loadingState: "completed",
			connections: {
				links,
				counts
			}
		}, () => {
			this.resubscribeOnLinkTypeEvents();
			triggerWorkspaceEvent(WorkspaceEventKey.connectionsLoadLinks);
			this.loadSuggestions("");
		});
	}
	resubscribeOnLinkTypeEvents() {
		const { workspace: { model } } = this.props;
		const { connections } = this.state;
		this.linkTypesListener.stopListening();
		if (connections) {
			for (const { id: linkTypeIri } of connections.links) {
				const linkType = model.createLinkType(linkTypeIri);
				this.linkTypesListener.listen(linkType.events, "changeData", this.scheduleUpdateAll);
			}
			const linkTypeIris = new Set(connections.links.map((link) => link.id));
			this.linkTypesListener.listen(model.events, "changeLinkVisibility", (e) => {
				if (linkTypeIris.has(e.source)) this.scheduleUpdateAll();
			});
		}
	}
	async loadObjects(chunk) {
		const { targetIris, workspace: { model, triggerWorkspaceEvent } } = this.props;
		const { linkType, direction, pageCount } = chunk;
		this.setState({
			loadingState: "loading",
			objects: {
				chunk,
				elements: []
			}
		});
		const loadedElements = /* @__PURE__ */ new Map();
		try {
			await Promise.all(targetIris.map((iri) => model.dataProvider.lookup({
				refElementId: iri,
				refElementLinkId: linkType.id === this.ALL_RELATED_ELEMENTS_LINK.id ? void 0 : linkType.id,
				linkDirection: direction,
				limit: pageCount * 100
			}).then((linkedElements) => {
				for (const { element } of linkedElements) loadedElements.set(element.id, element);
			})));
		} catch (err) {
			console.error(err);
			this.setState({ loadingState: "error" });
			return;
		}
		const elements = Array.from(loadedElements.values());
		this.setState({
			loadingState: "completed",
			objects: {
				chunk,
				elements
			}
		}, () => {
			triggerWorkspaceEvent(WorkspaceEventKey.connectionsLoadElements);
		});
	}
	async loadSuggestions(filterKey) {
		const { targetIris, suggestProperties, workspace: { model } } = this.props;
		const { loadingState, panel, connectionSortMode, connections, connectionSuggestions } = this.state;
		this.suggestionCancellation.abort();
		if (suggestProperties && targetIris.length === 1 && panel === "connections" && loadingState === "completed" && connections && (filterKey.length > 0 || connectionSortMode === "smart") && connectionSuggestions.filterKey !== filterKey) {
			const singleTargetIri = targetIris[0];
			const lang = model.language;
			const token = filterKey.trim();
			const properties = connections.links.map((link) => link.id);
			this.suggestionCancellation = new AbortController();
			const signal = this.suggestionCancellation.signal;
			const scoreList = await suggestProperties({
				elementId: singleTargetIri,
				token,
				properties,
				lang,
				signal
			});
			const scores = /* @__PURE__ */ new Map();
			for (const score of scoreList) scores.set(score.propertyIri, score);
			this.setState((state) => ({ connectionSuggestions: {
				...state.connectionSuggestions,
				filterKey,
				scores
			} }));
		}
	}
	render() {
		const { connectionSearch, objectSearch, translation: t } = this.props;
		const { panel } = this.state;
		return /* @__PURE__ */ jsxs("div", {
			className: CLASS_NAME$22,
			children: [
				this.getBreadCrumbs(),
				/* @__PURE__ */ jsx(SearchInput, {
					className: `${CLASS_NAME$22}__search`,
					store: panel === "connections" ? connectionSearch : objectSearch,
					inputProps: {
						name: "reactodia-connection-menu-filter",
						placeholder: t.textOptional("connections_menu.input.placeholder"),
						"data-reactodia-autofocus": true
					},
					children: this.renderSortSwitches()
				}),
				this.getBody()
			]
		});
	}
	getBreadCrumbs() {
		const { workspace: { model }, translation: t } = this.props;
		const { objects, panel } = this.state;
		if (objects && panel === "objects") {
			const { linkType, direction } = objects.chunk;
			const { label } = model.getLinkType(linkType.id)?.data ?? linkType;
			const localizedText = t.formatLabel(label, linkType.id, model.language);
			return /* @__PURE__ */ jsxs("span", {
				className: `${CLASS_NAME$22}__breadcrumbs`,
				children: [
					/* @__PURE__ */ jsx("button", {
						className: `${CLASS_NAME$22}__breadcrumbs-link`,
						onClick: this.onCollapseLink,
						children: t.text("connections_menu.breadcrumbs_root.label")
					}),
					"\xA0/\xA0",
					localizedText,
					" ",
					direction ? `(${direction})` : null
				]
			});
		} else return null;
	}
	onExpandLink = (chunk) => {
		const { objectSearch, workspace: { triggerWorkspaceEvent } } = this.props;
		const { objects } = this.state;
		this.setState({ panel: "objects" }, () => objectSearch.change({
			value: "",
			action: "clear"
		}));
		if (!(objects && objects.chunk.linkType.id === chunk.linkType.id && objects.chunk.direction === chunk.direction)) this.loadObjects(chunk);
		triggerWorkspaceEvent(WorkspaceEventKey.connectionsExpandLink);
	};
	onCollapseLink = () => {
		this.setState({
			panel: "connections",
			loadingState: "completed"
		});
	};
	getBody() {
		const { targetIris, connectionSearch, objectSearch, workspace: { getCommandBus } } = this.props;
		const { panel, connectionSortMode, loadingState, objects, connections, connectionSuggestions } = this.state;
		if (loadingState === "error") return /* @__PURE__ */ jsx(LoadingSpinner, { error: true });
		else if (objects && panel === "objects") return /* @__PURE__ */ jsx(EntityList, {
			data: objects,
			onMoveToFilter: this.onMoveToFilter,
			filterKey: objectSearch.value,
			isLoading: loadingState === "loading",
			onPressAddSelected: this.onAddSelectedElements
		}, objects.chunk.chunkId);
		else if (connections && panel === "connections") {
			if (loadingState === "loading" || connectionSortMode === "smart" && connectionSuggestions.filterKey === null) return /* @__PURE__ */ jsx(LoadingSpinner, {});
			const commands = getCommandBus(InstancesSearchTopic);
			const event = { capabilities: [] };
			commands.trigger("findCapabilities", event);
			return /* @__PURE__ */ jsx(ConnectionList, {
				data: connections,
				suggestions: connectionSuggestions,
				filterKey: connectionSearch.value,
				sortMode: connectionSortMode,
				allRelatedLink: this.ALL_RELATED_ELEMENTS_LINK,
				onExpandLink: this.onExpandLink,
				onMoveToFilter: event.capabilities.length > 0 && targetIris.length === 1 ? this.onMoveToFilter : void 0,
				scrolledListRef: this.linksScrolledListRef
			});
		} else return /* @__PURE__ */ jsx("div", {});
	}
	onAddSelectedElements = (selected, mode) => {
		const { onCancel } = this.props;
		const { objects } = this.state;
		const addedElementsIris = selected.map((item) => item.id);
		const linkTypeId = objects ? objects.chunk.linkType.id : void 0;
		const hasChosenLinkType = objects && linkTypeId !== this.ALL_RELATED_ELEMENTS_LINK.id;
		this.placeElements(addedElementsIris, hasChosenLinkType ? linkTypeId : void 0, mode);
		onCancel();
	};
	placeElements(elementIris, linkTypeId, mode) {
		const { placeTarget, canvas, workspace: { model, triggerWorkspaceEvent } } = this.props;
		const batch = model.history.startBatch(TranslatedText.text("connections_menu.place_elements.command"));
		let placedElements = [];
		switch (mode) {
			case "separately":
				placedElements = elementIris.map((iri) => model.createElement(iri));
				break;
			case "grouped": {
				const group = new EntityGroup({ items: elementIris.map((iri) => ({ data: EntityElement.placeholderData(iri) })) });
				model.addElement(group);
				placedElements = [group];
				break;
			}
		}
		canvas.renderingState.syncUpdate();
		batch.history.execute(placeElementsAroundTarget({
			target: placeTarget,
			elements: placedElements,
			graph: model,
			sizeProvider: canvas.renderingState
		}));
		if (linkTypeId && model.getLinkVisibility(linkTypeId) === "hidden") batch.history.execute(changeLinkTypeVisibility(model, linkTypeId, "visible"));
		batch.history.execute(requestElementData(model, elementIris));
		batch.history.execute(restoreLinksBetweenElements(model, { addedElements: elementIris }));
		batch.store();
		triggerWorkspaceEvent(WorkspaceEventKey.editorAddElements);
	}
	onMoveToFilter = (linkDataChunk) => {
		const { targetIris, workspace: { getCommandBus } } = this.props;
		const { linkType, direction } = linkDataChunk;
		const singleTargetIri = targetIris.length === 1 ? targetIris[0] : void 0;
		if (!singleTargetIri) return;
		const commands = getCommandBus(InstancesSearchTopic);
		if (linkType.id === this.ALL_RELATED_ELEMENTS_LINK.id) commands.trigger("setCriteria", { criteria: { refElement: singleTargetIri } });
		else commands.trigger("setCriteria", { criteria: {
			refElement: singleTargetIri,
			refElementLink: linkType.id,
			linkDirection: direction
		} });
	};
	renderSortSwitches() {
		const { targetIris, suggestProperties, translation: t } = this.props;
		const { panel } = this.state;
		if (!(panel === "connections" && suggestProperties && targetIris.length === 1)) return null;
		return /* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$22}__sort-switches`,
			children: [this.renderSortSwitch("alphabet", `${CLASS_NAME$22}__sort-label-alpha`, t.text("connections_menu.sort_alphabetically.title")), this.renderSortSwitch("smart", `${CLASS_NAME$22}__sort-label-smart`, t.text("connections_menu.sort_smart.title"))]
		});
	}
	renderSortSwitch(id, labelClass, title) {
		const { connectionSortMode } = this.state;
		return /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("input", {
			type: "radio",
			name: "sort",
			id,
			value: id,
			className: `${CLASS_NAME$22}__sort-switch`,
			onChange: this.onSortChange,
			checked: connectionSortMode === id
		}), /* @__PURE__ */ jsx("label", {
			htmlFor: id,
			title,
			className: cx(`${CLASS_NAME$22}__sort-switch-label`, labelClass)
		})] });
	}
	onSortChange = (e) => {
		const { onChangeMode, connectionSearch } = this.props;
		const { connectionSortMode } = this.state;
		const value = e.target.value;
		if (connectionSortMode !== value) this.setState({ connectionSortMode: value }, () => {
			onChangeMode(value);
			connectionSearch.change({
				value: connectionSearch.value,
				action: "submit"
			});
		});
	};
};
//#endregion
//#region src/widgets/dropOnCanvas.tsx
/**
* Canvas widget component to allow creating elements on the diagram
* by dragging then dropping an IRI (URI) to the canvas.
*
* @category Components
*/
function DropOnCanvas(props) {
	const { allowDrop, getDroppedItems = defaultGetDroppedOnCanvasItems } = props;
	const { canvas } = useCanvas();
	const { model, triggerWorkspaceEvent } = useWorkspace();
	React.useEffect(() => {
		const listener = new EventObserver();
		listener.listen(canvas.events, "dragover", (e) => {
			if (!allowDrop || allowDrop(e)) {
				if (e.sourceEvent.dataTransfer) e.sourceEvent.dataTransfer.dropEffect = "move";
				e.allowDrop();
			}
		});
		listener.listen(canvas.events, "drop", (e) => {
			e.sourceEvent.preventDefault();
			const items = getDroppedItems(e);
			if (items.length > 0) {
				const batch = model.history.startBatch(TranslatedText.text("drop_on_canvas.drop.command"));
				const presentOnDiagram = getAllPresentEntities(model);
				const addedIris = /* @__PURE__ */ new Set();
				const irisToLoad = [];
				const placedElements = [];
				for (const item of items) if (item.type === "element") {
					let addElement = false;
					let hasEntity = false;
					for (const entity of iterateEntitiesOf(item.element)) {
						hasEntity = true;
						if (!(presentOnDiagram.has(entity.id) || addedIris.has(entity.id))) {
							addElement = true;
							addedIris.add(entity.id);
							if (EntityElement.isPlaceholderData(entity)) irisToLoad.push(entity.id);
						}
					}
					if (!hasEntity || addElement) placedElements.push(item.element);
				}
				for (const element of placedElements) if (!model.getElement(element.id)) model.addElement(element);
				placeElements(placedElements, e.position, canvas);
				batch.history.execute(requestElementData(model, irisToLoad));
				batch.history.execute(restoreLinksBetweenElements(model, { addedElements: Array.from(addedIris) }));
				batch.store();
				if (placedElements.length > 0) placedElements[placedElements.length - 1].focus();
				model.setSelection(placedElements);
				canvas.renderingState.syncUpdate();
				triggerWorkspaceEvent(WorkspaceEventKey.editorAddElements);
			}
		});
		return () => listener.stopListening();
	}, [allowDrop, getDroppedItems]);
	return null;
}
/**
* Default handler to create {@link EntityElement entity elements} from a drop event.
*
* The handler tries to extract IRIs from drop event data in the following order:
*   1. Parse data with `application/x-reactodia-elements` format as JSON array of strings;
*   2. Decode a URI from a single string with `text/uri-list` format;
*   3. Use as-is string with `text` format.
*
* @see {@link DropOnCanvas}
* @see {@link DropOnCanvasProps.getDroppedItems}
*/
function defaultGetDroppedOnCanvasItems(e) {
	const tryGetIri = (type, decode = false) => {
		try {
			const iriString = e.sourceEvent.dataTransfer?.getData(type);
			if (!iriString) return;
			let iris = [];
			try {
				const parsed = JSON.parse(iriString);
				if (Array.isArray(parsed)) iris = parsed.filter((iri) => typeof iri === "string");
			} catch (e) {
				iris = [decode ? decodeURI(iriString) : iriString];
			}
			if (iris.length === 0) return;
			return iris.map((iri) => ({
				type: "element",
				element: new EntityElement({ data: EntityElement.placeholderData(iri) })
			}));
		} catch (e) {
			return;
		}
	};
	return tryGetIri("application/x-reactodia-elements") ?? tryGetIri("text/uri-list", true) ?? tryGetIri("text") ?? [];
}
function placeElements(elements, position, canvas) {
	for (const element of elements) element.setPosition(position);
	canvas.renderingState.syncUpdate();
	let { x, y } = position;
	let isFirst = true;
	for (const element of elements) {
		let { width, height } = boundsOf(element, canvas.renderingState);
		if (width === 0) width = 100;
		if (height === 0) height = 50;
		if (isFirst) {
			isFirst = false;
			x -= width / 2;
			y -= height / 2;
		}
		element.setPosition({
			x,
			y
		});
		y += height + 20;
	}
}
//#endregion
//#region src/widgets/editorForms/inputGroup.tsx
var FORM_CLASS$5 = "reactodia-form";
function InputGroup(props) {
	const { className, languages, propertyShapes, extraPropertyShape, propertyValues, onChangeData, resolveInput, readonly } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const language = useObservedProperty(model.events, "changeLanguage", () => model.language);
	const propertyIris = Array.from(propertyShapes.keys());
	if (extraPropertyShape) {
		for (const propertyIri in propertyValues) if (Object.prototype.hasOwnProperty.call(propertyValues, propertyIri) && !propertyShapes.has(propertyIri)) propertyIris.push(propertyIri);
	}
	useKeyedSyncStore(subscribePropertyTypes, propertyIris, model);
	if (propertyIris.length === 0) return null;
	const labelledProperties = [];
	for (const iri of propertyIris) {
		const shape = propertyShapes.get(iri) ?? extraPropertyShape;
		if (shape) {
			const property = model.getPropertyType(iri);
			const values = Object.prototype.hasOwnProperty.call(propertyValues, iri) ? propertyValues[iri] : void 0;
			labelledProperties.push({
				iri,
				label: t.formatLabel(property?.data?.label, iri, language),
				shape,
				values: values ?? []
			});
		}
	}
	labelledProperties.sort(compareProperties);
	return /* @__PURE__ */ jsx("div", {
		role: "list",
		className,
		children: labelledProperties.map(({ iri, label, shape, values }) => /* @__PURE__ */ jsx(Property, {
			iri,
			label,
			shape,
			languages,
			values,
			onChange: onChangeData,
			factory: model.factory,
			resolveInput,
			readonly: Boolean(readonly)
		}, iri))
	});
}
function compareProperties(a, b) {
	if (!(a.shape.order === void 0 && b.shape.order === void 0)) {
		const aOrder = a.shape.order ?? Infinity;
		const bOrder = b.shape.order ?? Infinity;
		if (aOrder !== bOrder) return aOrder - bOrder;
	}
	return a.label.localeCompare(b.label);
}
function Property(props) {
	const { iri, label, shape, languages, values, onChange, readonly, factory, resolveInput } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const input = resolveInput(iri, {
		shape,
		languages,
		values,
		updateValues: React.useCallback((updater) => {
			onChange(iri, updater);
		}, [iri, onChange]),
		factory,
		readonly,
		placeholder: t.text("visual_authoring.property.text_value.placeholder")
	});
	if (!input) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: `${FORM_CLASS$5}__row`,
		children: [/* @__PURE__ */ jsx("label", {
			title: t.text("visual_authoring.property.title", {
				property: label,
				propertyIri: model.locale.formatIri(iri)
			}),
			children: /* @__PURE__ */ jsx(WithFetchStatus, {
				type: "propertyType",
				target: iri,
				children: /* @__PURE__ */ jsx("span", { children: t.text("visual_authoring.property.label", {
					property: label,
					propertyIri: iri
				}) })
			})
		}), input]
	});
}
//#endregion
//#region src/widgets/editorForms/editEntityForm.tsx
var FORM_CLASS$4 = "reactodia-form";
var CLASS_NAME$21 = "reactodia-edit-entity-form";
/**
* Component which allows to build an entity editor by using provided state
* and callbacks to the nested components.
*
* `target` is an {@link EntityElement} on the canvas or one which
* was created but not placed on the canvas yet.
*
* @category Components
*/
function EntityEditor(props) {
	const { target, children } = props;
	const { editor } = useWorkspace();
	const initialEvent = editor.authoringState.elements.get(target.data.id);
	const [data, setData] = React.useState(computeChangedEntityData(target.data, initialEvent));
	React.useEffect(() => {
		const latestEvent = editor.authoringState.elements.get(target.data.id);
		setData(computeChangedEntityData(target.data, latestEvent));
	}, [target]);
	return children({
		data,
		updateData: setData,
		applyChanges: () => {
			let finalData = data;
			flushSync(() => {
				setData((previous) => {
					finalData = previous;
					return previous;
				});
			});
			editor.changeEntity(target.data, finalData);
		}
	});
}
function computeChangedEntityData(data, event) {
	if (event && event.type == "entityChange" && event.newIri) return {
		...data,
		id: event.newIri
	};
	return data;
}
function DefaultEditEntityForm(props) {
	const { target, resolveInput, onClose } = props;
	return /* @__PURE__ */ jsx(EntityEditor, {
		target,
		children: (providedProps) => /* @__PURE__ */ jsx(EditEntityProperties, {
			...providedProps,
			originalData: target.data,
			resolveInput,
			onClose
		})
	});
}
function EditEntityProperties(props) {
	const { originalData: entity, data, updateData, applyChanges, onClose, resolveInput } = props;
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const [metadata, metadataError] = useEntityMetadata(editor.metadataProvider, entity);
	const languages = React.useMemo(() => editor.metadataProvider?.getLiteralLanguages() ?? [], []);
	const iriValues = React.useMemo(() => [model.factory.namedNode(data.id)], [data.id]);
	const onChangeIri = React.useCallback((updater) => {
		updateData((previous) => {
			const nextIriValues = updater([model.factory.namedNode(previous.id)]);
			if (nextIriValues.length === 0) return previous;
			const iri = nextIriValues[0].value;
			return {
				...previous,
				id: iri
			};
		});
	}, []);
	const onChangeProperty = React.useCallback((property, updater) => {
		updateData((previous) => {
			const properties = previous.properties;
			const nextValues = updater((Object.prototype.hasOwnProperty.call(properties, property) ? properties[property] : void 0) ?? []);
			const nextProperties = {
				...properties,
				[property]: nextValues
			};
			if (nextValues.length === 0) delete nextProperties[property];
			return {
				...previous,
				properties: nextProperties
			};
		});
	}, []);
	if (!metadata) return /* @__PURE__ */ jsx("div", {
		className: cx(FORM_CLASS$4, CLASS_NAME$21, `${CLASS_NAME$21}--loading`),
		children: /* @__PURE__ */ jsx(HtmlSpinner, {
			width: 30,
			height: 30,
			errorOccurred: Boolean(metadataError)
		})
	});
	return /* @__PURE__ */ jsxs("div", {
		className: cx(FORM_CLASS$4, CLASS_NAME$21),
		children: [/* @__PURE__ */ jsxs("div", {
			className: `reactodia-scrollable ${FORM_CLASS$4}__body`,
			children: [/* @__PURE__ */ jsxs("div", {
				className: `${FORM_CLASS$4}__row`,
				children: [/* @__PURE__ */ jsx("label", { children: t.text("visual_authoring.edit_entity.iri.label") }), resolveInput("urn:reactodia:entityIri", {
					shape: IRI_SHAPE,
					languages,
					values: iriValues,
					updateValues: onChangeIri,
					factory: model.factory,
					readonly: !metadata.editable.canChangeIri,
					placeholder: t.text("visual_authoring.edit_entity.iri.placeholder")
				})]
			}), /* @__PURE__ */ jsx(InputGroup, {
				className: `${CLASS_NAME$21}__properties`,
				languages,
				extraPropertyShape: metadata.shape.extraProperty,
				propertyShapes: metadata.shape.properties,
				propertyValues: data.properties,
				onChangeData: onChangeProperty,
				readonly: !metadata.editable.canEdit,
				resolveInput
			})]
		}), /* @__PURE__ */ jsxs("div", {
			className: `${FORM_CLASS$4}__controls`,
			children: [/* @__PURE__ */ jsx("button", {
				type: "button",
				className: `reactodia-btn reactodia-btn-primary ${FORM_CLASS$4}__apply-button`,
				title: t.textOptional("visual_authoring.edit_entity.dialog.apply.title") ?? t.text("visual_authoring.dialog.apply.title"),
				onClick: () => {
					applyChanges();
					onClose();
				},
				children: t.textOptional("visual_authoring.edit_entity.dialog.apply.label") ?? t.text("visual_authoring.dialog.apply.label")
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				className: "reactodia-btn reactodia-btn-secondary",
				title: t.text("visual_authoring.dialog.cancel.title"),
				onClick: onClose,
				children: t.text("visual_authoring.dialog.cancel.label")
			})]
		})]
	});
}
var DEFAULT_ENTITY_SHAPE = { properties: /* @__PURE__ */ new Map() };
var IRI_SHAPE = {
	valueShape: { termType: "NamedNode" },
	minCount: 1,
	maxCount: 1
};
function useEntityMetadata(metadataProvider, entity) {
	const { data, status, error } = useAsync({
		input: [metadataProvider, entity],
		load: async ([provider, target], { signal }) => {
			if (provider) {
				const [editable, shape] = await Promise.all([provider.canModifyEntity(target, { signal }), provider.getEntityShape(target.types, { signal })]);
				return {
					editable,
					shape
				};
			} else return {
				editable: {},
				shape: DEFAULT_ENTITY_SHAPE
			};
		}
	});
	if (data && status === "completed") return [data];
	else return [void 0, error];
}
//#endregion
//#region src/widgets/editorForms/linkTypeSelector.tsx
var FORM_CLASS$3 = "reactodia-form";
function LinkTypeSelector(props) {
	const { link, onChange, disabled, error } = props;
	const { editor } = useWorkspace();
	const t = useTranslation();
	const fetchedTypes = useAsync({
		input: [
			editor.metadataProvider,
			link.source.id,
			link.target.id
		],
		load: ([provider], { signal }) => fetchPossibleLinkTypes({
			link,
			metadataProvider: provider,
			signal
		})
	});
	const linkTypes = fetchedTypes.status === "completed" ? fetchedTypes.data : void 0;
	const selectedType = (linkTypes ?? []).find(({ iri, direction }) => iri === link.base.linkTypeId && direction === link.direction);
	const onChangeType = (e) => {
		const key = Number(e.currentTarget.value);
		const nextType = linkTypes?.find((linkType) => linkType.key === key);
		if (nextType) onChange({
			...link,
			base: {
				...link.base,
				linkTypeId: nextType.iri
			},
			direction: nextType.direction
		});
	};
	return /* @__PURE__ */ jsxs("div", {
		className: `${FORM_CLASS$3}__control-row`,
		children: [
			/* @__PURE__ */ jsx("label", { children: t.text("visual_authoring.select_relation.type.label") }),
			linkTypes && fetchedTypes.status === "completed" ? /* @__PURE__ */ jsxs("select", {
				className: "reactodia-form-control",
				name: "reactodia-link-type-selector-select",
				value: selectedType?.key ?? -1,
				onChange: onChangeType,
				disabled,
				children: [/* @__PURE__ */ jsx("option", {
					value: -1,
					disabled: true,
					children: t.text("visual_authoring.select_relation.type.placeholder")
				}), /* @__PURE__ */ jsx(LinkTypeOptions, {
					link,
					linkTypes
				})]
			}) : /* @__PURE__ */ jsx("div", { children: /* @__PURE__ */ jsx(HtmlSpinner, {
				width: 20,
				height: 20,
				errorOccurred: fetchedTypes.status === "error"
			}) }),
			error ? /* @__PURE__ */ jsx("span", {
				className: `${FORM_CLASS$3}__control-error`,
				children: error
			}) : null
		]
	});
}
function LinkTypeOptions(props) {
	const { link, linkTypes } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const language = useObservedProperty(model.events, "changeLanguage", () => model.language);
	useKeyedSyncStore(subscribeLinkTypes, linkTypes.map((type) => type.iri), model);
	return /* @__PURE__ */ jsx(Fragment, { children: [...linkTypes].sort(makeLinkTypeComparatorByLabelAndDirection(model, t, language)).map(({ key, iri, direction }) => {
		const data = model.getLinkType(iri);
		const label = t.formatLabel(data?.data?.label, iri, model.language);
		let [sourceLabel, targetLabel] = [link.source, link.target].map((element) => model.locale.formatEntityLabel(element, model.language));
		if (direction === "in") [sourceLabel, targetLabel] = [targetLabel, sourceLabel];
		return /* @__PURE__ */ jsx("option", {
			value: key,
			children: t.text("visual_authoring.select_relation.relation_type.label", {
				relation: label,
				source: sourceLabel,
				target: targetLabel
			})
		}, key);
	}) });
}
function makeLinkTypeComparatorByLabelAndDirection(model, t, language) {
	return (a, b) => {
		const aData = model.getLinkType(a.iri);
		const bData = model.getLinkType(b.iri);
		const labelA = t.formatLabel(aData?.data?.label, a.iri, language);
		const labelB = t.formatLabel(bData?.data?.label, b.iri, language);
		const labelCompareResult = labelA.localeCompare(labelB);
		if (labelCompareResult !== 0) return labelCompareResult;
		if (a.direction === "out" && b.direction === "in") return -1;
		if (a.direction === "in" && b.direction === "out") return 1;
		return 0;
	};
}
async function fetchPossibleLinkTypes(params) {
	const { link, metadataProvider, signal } = params;
	if (!metadataProvider) return [];
	const connections = await mapAbortedToNull(metadataProvider.canConnect(link.source, link.target, void 0, { signal }), signal);
	if (connections === null) return [];
	const inLinkSet = /* @__PURE__ */ new Set();
	const outLinkSet = /* @__PURE__ */ new Set();
	for (const { inLinks, outLinks } of connections) {
		for (const linkType of inLinks) inLinkSet.add(linkType);
		for (const linkType of outLinks) outLinkSet.add(linkType);
	}
	let nextKey = 1;
	const dataLinkTypes = [];
	for (const linkType of outLinkSet) {
		dataLinkTypes.push({
			key: nextKey,
			iri: linkType,
			direction: "out"
		});
		nextKey++;
	}
	for (const linkType of inLinkSet) {
		dataLinkTypes.push({
			key: nextKey,
			iri: linkType,
			direction: "in"
		});
		nextKey++;
	}
	return dataLinkTypes;
}
function dataFromExtendedLink(link) {
	return {
		...link.base,
		sourceId: link.direction === "out" ? link.source.id : link.target.id,
		targetId: link.direction === "out" ? link.target.id : link.source.id
	};
}
function relationFromExtendedLink(link, source, target) {
	let [finalSource, finalTarget] = [source, target];
	if (link.direction === "in") [finalSource, finalTarget] = [finalTarget, finalSource];
	return new RelationLink({
		sourceId: finalSource.id,
		targetId: finalTarget.id,
		data: {
			...link.base,
			sourceId: finalSource.iri,
			targetId: finalTarget.iri
		}
	});
}
async function validateLinkType(currentLink, originalLink, workspace, t, signal) {
	const { model, editor } = workspace;
	if (currentLink.linkTypeId === "urn:reactodia:newLink") return {
		error: t.text("visual_authoring.select_relation.validation.error_required"),
		allowChange: true
	};
	if (equalLinks(currentLink, originalLink)) return {
		error: void 0,
		allowChange: true
	};
	if (isRelationOnDiagram(model, currentLink)) return {
		error: t.text("visual_authoring.select_relation.validation.error_duplicate"),
		allowChange: editor.temporaryState.links.has(currentLink)
	};
	if ((await model.dataProvider.links({
		primary: [currentLink.sourceId],
		secondary: [currentLink.targetId],
		linkTypeIds: [currentLink.linkTypeId],
		signal
	})).some((link) => equalLinks(link, currentLink))) return {
		error: t.text("visual_authoring.select_relation.validation.error_duplicate"),
		allowChange: false
	};
	return {
		error: void 0,
		allowChange: true
	};
}
function isRelationOnDiagram(model, target) {
	for (const link of model.links) for (const relation of iterateRelationsOf(link)) if (equalLinks(relation, target)) return true;
	return false;
}
//#endregion
//#region src/widgets/editorForms/editRelationForm.tsx
var FORM_CLASS$2 = "reactodia-form";
var CLASS_NAME$20 = "reactodia-edit-relation-form";
var RelationEditorContext = React.createContext(null);
/**
* Component which allows to build an relation editor by using provided state
* and callbacks to the nested components.
*
* `target` must be a {@link RelationLink} placed on the canvas.
*
* @category Components
*/
function RelationEditor(props) {
	const { relation, onChangeTarget, children } = props;
	const { model } = useWorkspace();
	const [version, setVersion] = React.useState(0);
	const relationSource = model.getElement(relation.sourceId);
	const relationTarget = model.getElement(relation.targetId);
	if (!(relationSource instanceof EntityElement && relationTarget instanceof EntityElement)) throw new Error("Reactodia: cannot find relation source and target on the canvas");
	React.useLayoutEffect(() => {
		setVersion((previous) => previous + 1);
	}, [relationSource, relationTarget]);
	return /* @__PURE__ */ jsx(RelationEditorInner, {
		original: relation,
		originalSource: relationSource,
		originalTarget: relationTarget,
		onChangeTarget,
		children
	}, version);
}
function RelationEditorInner(props) {
	const { original, originalSource, originalTarget, onChangeTarget, children } = props;
	const workspace = useWorkspace();
	const t = useTranslation();
	const { editor } = workspace;
	const [value, setValue] = React.useState(() => ({
		link: {
			base: original.data,
			source: originalSource.data,
			target: originalTarget.data,
			direction: "out"
		},
		validated: true,
		allowChange: true
	}));
	const [validating, setValidating] = React.useState(false);
	const lastValidated = React.useRef(void 0);
	React.useEffect(() => {
		const toValidate = dataFromExtendedLink(value.link);
		if (!lastValidated.current || !equalLinks(toValidate, lastValidated.current)) {
			const cancellation = new AbortController();
			setValidating(true);
			validateLinkType(toValidate, original.data, workspace, t, cancellation.signal).then((error) => {
				if (cancellation.signal.aborted) return;
				lastValidated.current = toValidate;
				setValue((previous) => ({
					...previous,
					...error,
					validated: true
				}));
				setValidating(false);
			});
			return () => cancellation.abort();
		}
	}, [value.link]);
	React.useEffect(() => {
		if (editor.temporaryState.links.has(original.data) && value.validated && value.allowChange) {
			const toApply = dataFromExtendedLink(value.link);
			if (!equalLinks(original.data, toApply)) {
				const linkBase = relationFromExtendedLink(value.link, originalSource, originalTarget);
				onChangeTarget(editor.createRelation(linkBase, { temporary: true }));
			}
		}
	}, [value]);
	const context = React.useMemo(() => ({
		value,
		setValue,
		validating
	}), [value, validating]);
	return /* @__PURE__ */ jsx(RelationEditorContext.Provider, {
		value: context,
		children: children({
			status: validating ? "validating" : value.error ? "invalid" : "ok",
			data: dataFromExtendedLink(value.link),
			linkSource: value.link.source,
			linkTarget: value.link.target,
			updateData: (update) => {
				setValue((previous) => {
					const nextData = update(dataFromExtendedLink(previous.link));
					const previousProperties = previous.link.base.properties;
					if (nextData.properties === previousProperties) return previous;
					return {
						...previous,
						link: {
							...previous.link,
							base: {
								...previous.link.base,
								properties: nextData.properties
							}
						}
					};
				});
			},
			applyChanges: () => {
				let finalValue = value;
				flushSync(() => {
					setValue((previous) => {
						finalValue = previous;
						return previous;
					});
				});
				const toApply = dataFromExtendedLink(finalValue.link);
				if (editor.temporaryState.links.has(original.data)) {
					editor.removeTemporaryCells([original]);
					const linkBase = relationFromExtendedLink(finalValue.link, originalSource, originalTarget);
					editor.createRelation(linkBase);
				} else if (!(equalLinks(original.data, toApply) && equalProperties(original.data.properties, toApply.properties))) editor.changeRelation(original.data, toApply);
			}
		})
	});
}
function DefaultEditRelationForm(props) {
	const { target, onChangeTarget, onClose, resolveInput } = props;
	return /* @__PURE__ */ jsx(RelationEditor, {
		relation: target,
		onChangeTarget,
		children: (providedProps) => /* @__PURE__ */ jsx(EditRelationProperties, {
			...providedProps,
			resolveInput,
			onClose
		})
	});
}
function EditRelationProperties(props) {
	const { status, data: linkData, linkSource, linkTarget, updateData, applyChanges, onClose, resolveInput } = props;
	const { editor } = useWorkspace();
	const t = useTranslation();
	const [metadata, metadataError] = useRelationMetadata(editor.metadataProvider, linkData, linkSource, linkTarget);
	const languages = React.useMemo(() => editor.metadataProvider?.getLiteralLanguages() ?? [], []);
	const onChangeProperty = (property, updater) => {
		updateData((previous) => {
			const { properties } = previous;
			const nextValues = updater((Object.prototype.hasOwnProperty.call(properties, property) ? properties[property] : void 0) ?? []);
			const nextProperties = {
				...properties,
				[property]: nextValues
			};
			if (nextValues.length === 0) delete nextProperties[property];
			return {
				...previous,
				properties: nextProperties
			};
		});
	};
	return /* @__PURE__ */ jsxs("div", {
		className: FORM_CLASS$2,
		children: [/* @__PURE__ */ jsxs("div", {
			className: `reactodia-scrollable ${FORM_CLASS$2}__body`,
			children: [/* @__PURE__ */ jsx(RelationTypeSelector, {}), !metadata ? /* @__PURE__ */ jsx("div", {
				className: cx(FORM_CLASS$2, CLASS_NAME$20, `${CLASS_NAME$20}--loading`),
				children: /* @__PURE__ */ jsx(HtmlSpinner, {
					width: 30,
					height: 30,
					errorOccurred: Boolean(metadataError)
				})
			}) : metadata.shape ? /* @__PURE__ */ jsx(InputGroup, {
				className: `${CLASS_NAME$20}__properties`,
				languages,
				extraPropertyShape: metadata.shape.extraProperty,
				propertyShapes: metadata.shape.properties,
				propertyValues: linkData.properties,
				onChangeData: onChangeProperty,
				resolveInput
			}) : null]
		}), /* @__PURE__ */ jsxs("div", {
			className: `${FORM_CLASS$2}__controls`,
			children: [/* @__PURE__ */ jsx("button", {
				className: `reactodia-btn reactodia-btn-primary ${FORM_CLASS$2}__apply-button`,
				onClick: () => {
					applyChanges();
					onClose();
				},
				disabled: status !== "ok",
				title: t.textOptional("visual_authoring.edit_relation.dialog.apply.title") ?? t.text("visual_authoring.dialog.apply.title"),
				children: t.textOptional("visual_authoring.edit_relation.dialog.apply.label") ?? t.text("visual_authoring.dialog.apply.label")
			}), /* @__PURE__ */ jsx("button", {
				className: "reactodia-btn reactodia-btn-secondary",
				onClick: onClose,
				title: t.text("visual_authoring.dialog.cancel.title"),
				children: t.text("visual_authoring.dialog.cancel.label")
			})]
		})]
	});
}
/**
* Component to change relation type and/or its direction from a custom property editor
* for the {@link VisualAuthoring}.
*
* @category Components
*/
function RelationTypeSelector() {
	const t = useTranslation();
	const context = React.useContext(RelationEditorContext);
	if (!context) throw new Error("Reactodia: cannot use <RelationTypeSelector> outside <RelationEditor>");
	const { value, setValue, validating } = context;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(LinkTypeSelector, {
		link: value.link,
		error: value.error,
		onChange: (link) => setValue({
			link,
			error: void 0,
			validated: false,
			allowChange: false
		})
	}), validating ? /* @__PURE__ */ jsx("div", {
		className: `${FORM_CLASS$2}__progress`,
		children: /* @__PURE__ */ jsx(ProgressBar, {
			state: "loading",
			title: t.text("visual_authoring.edit_relation.validation_progress.title"),
			height: 10
		})
	}) : null] });
}
function useRelationMetadata(metadataProvider, link, linkSource, linkTarget) {
	const { data, status, error } = useAsync({
		input: [
			metadataProvider,
			link.linkTypeId,
			linkSource,
			linkTarget
		],
		load: ([provider, _typeId, source, target], { signal }) => {
			if (provider) return Promise.all([provider.canModifyRelation(link, source, target, { signal }), provider.getRelationShape(link.linkTypeId, linkSource, linkTarget, { signal })]);
			else return;
		}
	});
	if (data && status === "completed") {
		const [loadedStatus, loadedShape] = data;
		return [{ shape: loadedStatus.canEdit ? loadedShape : null }];
	} else return [void 0, error];
}
//#endregion
//#region src/widgets/editorForms/defaultPropertyEditor.tsx
/**
* Default editor for entity or relation properties at {@link VisualAuthoring}.
*
* @category Components
* @see {@link PropertyEditor}
*/
function DefaultPropertyEditor(props) {
	const { options, resolveInput } = props;
	switch (options.type) {
		case "entity": return /* @__PURE__ */ jsx(DefaultEditEntityForm, {
			...options,
			resolveInput
		});
		case "relation": return /* @__PURE__ */ jsx(DefaultEditRelationForm, {
			...options,
			resolveInput
		});
		default: return null;
	}
}
//#endregion
//#region src/widgets/utility/resizableBox.tsx
var CLASS_NAME$19 = "reactodia-resizable-box";
function ResizableBox(props) {
	const { className, mapCoordsFromPage, startResize, minWidth = 0, minHeight = 0 } = props;
	const [operation, setOperation] = React.useState();
	const onBeginDragHandle = (e) => {
		e.stopPropagation();
		setOperation(startResize());
	};
	const onEndDragHandle = () => {
		operation?.end();
		setOperation(void 0);
	};
	const resize = (dx, dy, dw, dh) => {
		if (!operation) return;
		const { initialBounds } = operation;
		const origin = mapCoordsFromPage(0, 0);
		const { x: mappedDx, y: mappedDy } = Vector.subtract(mapCoordsFromPage(dx, dy), origin);
		const { x: mappedDw, y: mappedDh } = Vector.subtract(mapCoordsFromPage(dw, dh), origin);
		const nextBounds = {
			x: Math.min(initialBounds.x + mappedDx, initialBounds.x + initialBounds.width - minWidth),
			y: Math.min(initialBounds.y + mappedDy, initialBounds.y + initialBounds.height - minHeight),
			width: Math.max(initialBounds.width + mappedDw, minWidth),
			height: Math.max(initialBounds.height + mappedDh, minHeight)
		};
		operation.onResize(nextBounds);
	};
	React.useEffect(() => {
		return () => operation?.end();
	}, []);
	const handleCorner = /* @__PURE__ */ jsx("svg", {
		className: `${CLASS_NAME$19}__corner`,
		viewBox: "-2 -2 14 14",
		children: /* @__PURE__ */ jsx("circle", {
			cx: 5,
			cy: 5,
			r: 5
		})
	});
	const handleVertical = /* @__PURE__ */ jsx("svg", {
		className: `${CLASS_NAME$19}__vertical`,
		viewBox: "-2 -2 34 14",
		children: /* @__PURE__ */ jsx("rect", {
			x: 0,
			y: 0,
			rx: 2,
			ry: 2,
			width: 30,
			height: 10
		})
	});
	const handleHorizontal = /* @__PURE__ */ jsx("svg", {
		className: `${CLASS_NAME$19}__horizontal`,
		viewBox: "-2 -2 14 34",
		children: /* @__PURE__ */ jsx("rect", {
			x: 0,
			y: 0,
			rx: 2,
			ry: 2,
			width: 10,
			height: 30
		})
	});
	const HANDLE_CLASS = `${CLASS_NAME$19}__handle`;
	return /* @__PURE__ */ jsxs("div", {
		className: cx(CLASS_NAME$19, className),
		children: [
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "n",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(0, dy, 0, -dy),
				children: handleVertical
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "s",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(0, 0, 0, dy),
				children: handleVertical
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "w",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(dx, 0, -dx, 0),
				children: handleHorizontal
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "e",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(0, 0, dx, 0),
				children: handleHorizontal
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "nw",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(dx, dy, -dx, -dy),
				children: handleCorner
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "ne",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(0, dy, dx, -dy),
				children: handleCorner
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "sw",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(dx, 0, -dx, dy),
				children: handleCorner
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				className: HANDLE_CLASS,
				dock: "se",
				onBeginDragHandle,
				onEndDragHandle,
				onDragHandle: (e, dx, dy) => resize(0, 0, dx, dy),
				children: handleCorner
			})
		]
	});
}
//#endregion
//#region src/widgets/selectionAction.tsx
var CLASS_NAME$18 = "reactodia-selection-action";
/**
* Base component to display an action on the selected diagram elements
* from {@link Halo} or {@link Selection}.
*
* @category Components
*/
function SelectionAction(props) {
	const { dock, dockRow, dockColumn, className, title, hotkey, disabled, onSelect, onMouseDown, onPointerDown, children } = props;
	const actionKey = useCanvasHotkey(hotkey, disabled ? void 0 : onSelect);
	const titleWithHotkey = title && actionKey ? `${title} (${actionKey.text})` : title;
	return /* @__PURE__ */ jsx("button", {
		type: "button",
		className: cx(CLASS_NAME$18, getDockClass(dock), className),
		style: getDockStyle(dockRow, dockColumn),
		title: titleWithHotkey,
		"aria-label": title,
		"aria-keyshortcuts": actionKey?.text,
		disabled,
		onClick: onSelect,
		onMouseDown,
		onPointerDown,
		children
	});
}
function getDockClass(dock) {
	switch (dock) {
		case "nw": return `${CLASS_NAME$18}--dock-nw`;
		case "n": return `${CLASS_NAME$18}--dock-n`;
		case "ne": return `${CLASS_NAME$18}--dock-ne`;
		case "e": return `${CLASS_NAME$18}--dock-e`;
		case "se": return `${CLASS_NAME$18}--dock-se`;
		case "s": return `${CLASS_NAME$18}--dock-s`;
		case "sw": return `${CLASS_NAME$18}--dock-sw`;
		case "w": return `${CLASS_NAME$18}--dock-w`;
		default: return;
	}
}
function getDockStyle(dockRow, dockColumn) {
	return {
		"--reactodia-dock-x": dockColumn ?? 0,
		"--reactodia-dock-y": dockRow ?? 0
	};
}
function useSingleSelectedElement(model) {
	return useObservedProperty(model.events, "changeSelection", () => {
		const target = model.selection.length === 1 ? model.selection[0] : void 0;
		return target instanceof Element ? target : void 0;
	});
}
/**
* Selection action component to display a loading spinner.
*
* @category Components
*/
function SelectionActionSpinner(props) {
	const { dock, dockRow, dockColumn, className, title } = props;
	return /* @__PURE__ */ jsx("div", {
		role: "button",
		className: cx(className, getDockClass(dock), `${CLASS_NAME$18}__spinner`),
		style: getDockStyle(dockRow, dockColumn),
		title,
		children: /* @__PURE__ */ jsx(HtmlSpinner, {
			width: 20,
			height: 20
		})
	});
}
/**
* Selection action component to remove an element from the diagram.
*
* Removing the elements adds a command to the command history.
*
* @category Components
*/
function SelectionActionRemove(props) {
	const { className, title, hotkey, ...otherProps } = props;
	const { canvas } = useCanvas();
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	let newEntities = 0;
	let totalEntities = 0;
	for (const element of elements) if (element instanceof EntityElement) {
		totalEntities++;
		if (AuthoringState.isAddedEntity(editor.authoringState, element.iri)) newEntities++;
	}
	const singleNewEntity = newEntities === 1 && totalEntities === 1;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, singleNewEntity ? `${CLASS_NAME$18}__delete` : `${CLASS_NAME$18}__remove`),
		title: title ? title : singleNewEntity ? t.text("selection_action.remove.title_new") : elements.length === 1 ? t.text("selection_action.remove.title_single") : t.text("selection_action.remove.title"),
		hotkey: hotkey === void 0 ? "None+Delete" : hotkey,
		onSelect: () => {
			editor.removeSelectedElements();
			canvas.focus();
		}
	});
}
/**
* Selection action component to zoom-in or zoom-out the viewport
* to fit all selected diagram elements.
*
* @category Components
*/
function SelectionActionZoomToFit(props) {
	const { className, title, ...otherProps } = props;
	const { model, canvas } = useCanvas();
	const t = useTranslation();
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	if (elements.length <= 1) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__zoomToFit`),
		title: title ?? t.text("selection_action.zoom_to_fit.title"),
		onSelect: () => {
			const links = /* @__PURE__ */ new Set();
			for (const element of elements) for (const link of model.getElementLinks(element)) links.add(link);
			const fittingBox = getContentFittingBox(elements, links, canvas.renderingState);
			canvas.zoomToFitRect(fittingBox, { animate: true });
		}
	});
}
/**
* Selection action component to perform graph layout algorithm on the sub-graph
* formed from the selected elements.
*
* Applying the layout adds a command to the command history.
*
* @category Components
*/
function SelectionActionLayout(props) {
	const { className, title, ...otherProps } = props;
	const { model, canvas } = useCanvas();
	const { performLayout } = useWorkspace();
	const t = useTranslation();
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	if (elements.length <= 1) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__layout`),
		title: title ?? t.text("selection_action.layout.title"),
		onSelect: () => {
			performLayout({
				canvas,
				selectedElements: new Set(elements),
				animate: true
			});
		}
	});
}
/**
* Selection action component to toggle expanded state for the selected elements.
*
* Elements are collapsed if all of them are expanded, otherwise only collapsed
* ones are expanded instead.
*
* This action is visible only when at least one of the selected elements
* have {@link TemplateProperties.Expanded} property in {@link ElementTemplate.supports}.
*
* Expanding or collapsing the elements adds a command to the command history.
*
* @category Components
*/
function SelectionActionExpand(props) {
	const { className, title, ...otherProps } = props;
	const { canvas } = useCanvas();
	const { model } = useWorkspace();
	const t = useTranslation();
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	const elementExpandedStore = useElementExpandedStore(model, elements);
	const canExpand = (element) => {
		const template = canvas.renderingState.getElementTemplate(element);
		return Boolean(template.supports?.[TemplateProperties.Expanded]);
	};
	const allExpanded = useSyncStore(useLayerDebouncedStore(elementExpandedStore, canvas.renderingState), () => elements.every((element) => !canExpand(element) || element.isExpanded));
	if (!elements.some(canExpand)) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, allExpanded ? `${CLASS_NAME$18}__collapse` : `${CLASS_NAME$18}__expand`),
		title: title ?? (elements.length === 1 ? t.text("selection_action.expand.title_single") : t.text("selection_action.expand.title")),
		onSelect: () => {
			if (elements.length === 1) {
				const target = elements[0];
				model.history.execute(setElementExpanded(target, !target.isExpanded));
			} else {
				const batch = model.history.startBatch(allExpanded ? TranslatedText.text("selection_action.expand.collapse_command") : TranslatedText.text("selection_action.expand.expand_command"));
				for (const element of elements) if (canExpand(element)) batch.history.execute(setElementExpanded(element, !allExpanded));
				batch.store();
			}
		}
	});
}
function useElementExpandedStore(model, elements) {
	return React.useCallback((onChange) => {
		if (elements.length === 0) return () => {};
		const elementSet = new Set(elements);
		const listener = new EventObserver();
		listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changeElementState && elementSet.has(data.changeElementState.source)) {
				const previousExpanded = Boolean(data.changeElementState.previous.get(TemplateProperties.Expanded));
				if (data.changeElementState.source.isExpanded !== previousExpanded) onChange();
			}
		});
		return () => listener.stopListening();
	}, [model.events, elements]);
}
/**
* Selection action component to display a link to the entity IRI.
*
* This action is visible only if the selected element is an {@link EntityElement}.
*
* @category Components
*/
function SelectionActionAnchor(props) {
	const { dock, dockRow, dockColumn, className, title, anchorProps, onSelect } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const target = useSingleSelectedElement(model);
	if (!(target instanceof EntityElement)) return null;
	const preparedAnchor = model.locale.prepareAnchor(target.iri);
	return /* @__PURE__ */ jsx("a", {
		...preparedAnchor,
		role: "button",
		...anchorProps,
		className: cx(CLASS_NAME$18, getDockClass(dock), className, `${CLASS_NAME$18}__link`),
		style: getDockStyle(dockRow, dockColumn),
		title: title ?? t.text("selection_action.anchor.title"),
		onClick: (e) => {
			if (onSelect) onSelect(target, e);
			else preparedAnchor.onClick?.(e);
		}
	});
}
/**
* Selection action component to open a {@link ConnectionsMenu} for the selected entities.
*
* This action is visible if at least one {@link EntityElement} or {@link EntityGroup}
* is selected.
*
* @category Components
*/
function SelectionActionConnections(props) {
	const { className, title, ...otherProps } = props;
	const { model, overlay, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const menuOpened = useObservedProperty(overlay.events, "changeOpenedDialog", () => overlay.openedDialog?.knownType === BuiltinDialogType.connectionsMenu);
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	let entityCount = 0;
	for (const element of elements) for (const _entity of iterateEntitiesOf(element)) entityCount++;
	const commands = getCommandBus(ConnectionsMenuTopic);
	const event = { capabilities: [] };
	commands.trigger("findCapabilities", event);
	if (!(event.capabilities.length > 0 && entityCount > 0)) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, menuOpened ? `${CLASS_NAME$18}__navigate-close` : `${CLASS_NAME$18}__navigate-open`),
		title: title ?? t.text("selection_action.connections.title"),
		onSelect: () => {
			if (menuOpened) overlay.hideDialog();
			else commands.trigger("show", { targets: elements });
		}
	});
}
/**
* Selection action component to add the selected entity to the {@link InstancesSearch} filter.
*
* This action is visible only if the selected element is an {@link EntityElement}.
*
* @category Components
*/
function SelectionActionAddToFilter(props) {
	const { className, title, ...otherProps } = props;
	const { model, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const target = useSingleSelectedElement(model);
	const commands = getCommandBus(InstancesSearchTopic);
	const event = { capabilities: [] };
	commands.trigger("findCapabilities", event);
	if (!(target instanceof EntityElement && event.capabilities.length > 0)) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__add-to-filter`),
		title: title ?? t.text("selection_action.add_to_filter.title"),
		onSelect: () => {
			commands.trigger("setCriteria", { criteria: { refElement: target.iri } });
		}
	});
}
/**
* Selection action component to group or ungroup selected elements.
*
* Selected elements can be grouped if only {@link EntityElement entity elements}
* are selected, the elements can be ungrouped if only {@link EntityGroup entity groups}
* are selected.
*
* Grouping or un-grouping the elements adds a command to the command history.
*
* @category Components
*/
function SelectionActionGroup(props) {
	const { className, title, hotkey, ...otherProps } = props;
	const { canvas } = useCanvas();
	const workspace = useWorkspace();
	const t = useTranslation();
	const { model } = workspace;
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	const canGroup = elements.length > 0 && elements.every((element) => element instanceof EntityElement);
	const canUngroup = elements.length > 0 && elements.every((element) => element instanceof EntityGroup);
	const onSelect = async () => {
		if (canGroup) {
			const group = await groupEntities(workspace, {
				elements,
				canvas
			});
			model.setSelection([group]);
			group.focus();
		} else if (canUngroup) {
			const ungrouped = await ungroupAllEntities(workspace, {
				groups: elements,
				canvas
			});
			model.setSelection(ungrouped);
			canvas.focus();
		}
	};
	if (elements.length === 0 || elements.length === 1 && canGroup || elements.every((element) => element instanceof AnnotationElement)) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, canUngroup ? `${CLASS_NAME$18}__ungroup` : `${CLASS_NAME$18}__group`),
		disabled: !(canGroup || canUngroup),
		title: title ?? (canUngroup ? t.text("selection_action.group.title_ungroup") : t.text("selection_action.group.title")),
		hotkey: hotkey === void 0 ? "None+G" : hotkey,
		onSelect
	});
}
/**
* Selection action component to start creating a link to an existing or a new element.
*
* This action is visible either if selected element is an {@link AnnotationElement}
* it is an {@link EntityElement} and {@link EditorController.inAuthoringMode graph authoring mode}
* is active.
*
* Creating a link adds a command to the command history.
*
* @category Components
*/
function SelectionActionEstablishLink(props) {
	const { model } = useCanvas();
	const target = useSingleSelectedElement(model);
	if (target instanceof EntityElement) return /* @__PURE__ */ jsx(SelectionActionEstablishRelation, {
		...props,
		target
	});
	else if (target instanceof AnnotationElement) return /* @__PURE__ */ jsx(SelectionActionEstablishAnnotationLink, {
		...props,
		target
	});
	else return null;
}
function SelectionActionEstablishRelation(props) {
	const { target, className, title, linkType, ...otherProps } = props;
	const { canvas } = useCanvas();
	const { editor, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const inAuthoringMode = useObservedProperty(editor.events, "changeMode", () => editor.inAuthoringMode);
	const canLink = useCanEstablishLink(editor, inAuthoringMode ? target : void 0, linkType);
	if (!inAuthoringMode) return null;
	else if (canLink === void 0) {
		const { dock, dockRow, dockColumn } = props;
		return /* @__PURE__ */ jsx(SelectionActionSpinner, {
			dock,
			dockRow,
			dockColumn
		});
	}
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__establish-link`),
		disabled: !canLink,
		title: title ?? (canLink ? t.text("selection_action.establish_relation.title") : t.text("selection_action.establish_relation.title_disabled")),
		onPointerDown: (e) => {
			e.preventDefault();
			const point = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			getCommandBus(VisualAuthoringTopic).trigger("startDragEdit", { operation: {
				mode: "connect",
				source: target,
				linkType,
				point
			} });
		}
	});
}
function useCanEstablishLink(editor, target, linkType) {
	const { canvas } = useCanvas();
	const entityTarget = target instanceof EntityElement ? target : void 0;
	const targetData = useSyncStore(useEventStore(entityTarget?.events, "changeData"), () => entityTarget?.data);
	const authoringState = useSyncStore(useLayerDebouncedStore(useEventStore(editor.events, "changeAuthoringState"), canvas.renderingState), () => editor.authoringState);
	const authoringEvent = target instanceof EntityElement ? authoringState.elements.get(target.iri) : void 0;
	const { data: canLink, status } = useAsync({
		input: [
			editor.metadataProvider,
			targetData,
			authoringEvent,
			linkType
		],
		load: async ([provider, targetData, authoringEvent, linkType], { signal }) => {
			if (provider && targetData && !(authoringEvent && authoringEvent.type === "entityDelete")) return (await provider.canConnect(targetData, void 0, linkType, { signal })).length > 0;
			return false;
		}
	});
	return status === "completed" ? canLink : void 0;
}
function SelectionActionEstablishAnnotationLink(props) {
	const { target, className, title, linkType, ...otherProps } = props;
	const { canvas } = useCanvas();
	const { getCommandBus } = useWorkspace();
	const t = useTranslation();
	const commands = getCommandBus(AnnotationTopic);
	const event = { capabilities: [] };
	commands.trigger("findCapabilities", event);
	if (event.capabilities.length === 0) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__establish-link`),
		title: title ?? t.text("selection_action.establish_relation.title"),
		onPointerDown: (e) => {
			e.preventDefault();
			const point = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			getCommandBus(AnnotationTopic).trigger("startDragOperation", { operation: {
				mode: "connect",
				source: target,
				point
			} });
		}
	});
}
/**
* Selection action component to create a new annotaion element
* connected to the selected elements.
*
* @category Components
*/
function SelectionActionAnnotate(props) {
	const { className, title, initialContent, ...otherProps } = props;
	const { model, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const elements = useObservedProperty(model.events, "changeSelection", () => model.selection).filter((item) => item instanceof Element);
	const commands = getCommandBus(AnnotationTopic);
	const event = { capabilities: [] };
	commands.trigger("findCapabilities", event);
	if (elements.length === 0 || event.capabilities.length === 0) return null;
	return /* @__PURE__ */ jsx(SelectionAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$18}__annotate`),
		title: title ?? t.text("selection_action.annotate.title"),
		onSelect: () => {
			let content;
			if (typeof initialContent === "function") content = initialContent(elements);
			else if (initialContent === void 0) content = {
				type: "plaintext",
				text: t.text("selection_action.annotate.defaultContent")
			};
			else if (initialContent !== null) content = initialContent;
			getCommandBus(AnnotationTopic).trigger("createAnnotation", {
				targets: elements,
				content
			});
		}
	});
}
//#endregion
//#region src/widgets/halo.tsx
/**
* Canvas widget component to display actions for the single selected diagram element.
*
* @category Components
*/
function Halo(props) {
	const { model, canvas } = useCanvas();
	const singleTarget = useSingleSelectedElement(model);
	if (singleTarget) return /* @__PURE__ */ jsx(CanvasPlaceAt, {
		layer: "overElements",
		children: /* @__PURE__ */ jsx(HaloInner, {
			...props,
			target: singleTarget,
			canvas,
			model
		})
	});
	return null;
}
var CLASS_NAME$17 = "reactodia-halo";
var HaloInner = class extends React.Component {
	listener = new EventObserver();
	targetListener = new EventObserver();
	constructor(props) {
		super(props);
		this.state = {};
	}
	componentDidMount() {
		const { canvas, target } = this.props;
		this.listener.listen(canvas.events, "changeTransform", () => this.forceUpdate());
		this.listenToElement(target);
	}
	componentDidUpdate(prevProps) {
		if (prevProps.target !== this.props.target) this.listenToElement(this.props.target);
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.listenToElement(void 0);
	}
	listenToElement(element) {
		const { canvas } = this.props;
		this.targetListener.stopListening();
		if (element) {
			this.targetListener.listenAny(element.events, this.onElementEvent);
			this.targetListener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
				if (e.source === element) flushSync(() => {
					this.forceUpdate();
				});
			});
		}
	}
	onElementEvent = (data) => {
		if (data.changePosition) flushSync(() => {
			this.forceUpdate();
		});
	};
	render() {
		const { target, canvas, model, margin, children } = this.props;
		const template = canvas.renderingState.getElementTemplate(target);
		const resizable = Boolean(template.supports?.[TemplateProperties.ElementSize]);
		const bbox = boundsOf(target, canvas.renderingState);
		const { x: x0, y: y0 } = canvas.metrics.paperToScrollablePaneCoords(bbox.x, bbox.y);
		const { x: x1, y: y1 } = canvas.metrics.paperToScrollablePaneCoords(bbox.x + bbox.width, bbox.y + bbox.height);
		const style = {
			"--reactodia-selection-single-box-margin": margin,
			"--reactodia-halo-left": `${x0}px`,
			"--reactodia-halo-top": `${y0}px`,
			"--reactodia-halo-width": `${x1 - x0}px`,
			"--reactodia-halo-height": `${y1 - y0}px`
		};
		return /* @__PURE__ */ jsxs("div", {
			className: cx(CLASS_NAME$17, resizable ? `${CLASS_NAME$17}--resizable` : void 0),
			style,
			children: [resizable && /* @__PURE__ */ jsx(ResizableBox, {
				className: `${CLASS_NAME$17}__resizer`,
				mapCoordsFromPage: (x, y) => canvas.metrics.pageToPaperCoords(x, y),
				startResize: () => {
					model.history.registerToUndo(Command.compound(TranslatedText.text("commands.transform_elements.title"), [RestoreGeometry.capturePartial([target], [])]));
					return new ElementResizeOperation(target, bbox);
				},
				minWidth: 40,
				minHeight: 40
			}), children ?? /* @__PURE__ */ jsxs(Fragment, { children: [
				/* @__PURE__ */ jsx(SelectionActionGroup, {
					dock: "nw",
					dockColumn: 1
				}),
				/* @__PURE__ */ jsx(SelectionActionRemove, { dock: "ne" }),
				/* @__PURE__ */ jsx(SelectionActionExpand, { dock: "s" }),
				/* @__PURE__ */ jsx(SelectionActionAnchor, { dock: "w" }),
				/* @__PURE__ */ jsx(SelectionActionConnections, { dock: "e" }),
				/* @__PURE__ */ jsx(SelectionActionAddToFilter, { dock: "se" }),
				/* @__PURE__ */ jsx(SelectionActionAnnotate, {
					dock: "se",
					dockColumn: 1
				}),
				/* @__PURE__ */ jsx(SelectionActionEstablishLink, { dock: "sw" })
			] })]
		});
	}
};
var ElementResizeOperation = class {
	target;
	initialBounds;
	constructor(target, initialBounds) {
		this.target = target;
		this.initialBounds = initialBounds;
	}
	onResize({ x, y, width, height }) {
		this.target.setPosition({
			x,
			y
		});
		this.target.setElementState(this.target.elementState.set(TemplateProperties.ElementSize, {
			width,
			height
		}));
	}
	end() {}
};
//#endregion
//#region src/widgets/linkAction.tsx
var LinkActionProvidedContext = React.createContext(null);
/**
* React hook to get rendering context for the link action.
*
* Throws an error if called outside {@link HaloLink} component.
*
* @category Hooks
*/
function useLinkActionContext() {
	const context = React.useContext(LinkActionProvidedContext);
	if (!context) throw new Error("Missing context for LinkAction");
	return context;
}
var CLASS_NAME$16 = "reactodia-link-action";
/**
* Base component to display an action on the selected link from {@link HaloLink}.
*
* @category Components
*/
function LinkAction(props) {
	const { dockSide, dockIndex, disabled, className, title, hotkey, onSelect, onMouseDown, onPointerDown, children } = props;
	const { getPosition } = useLinkActionContext();
	const actionKey = useCanvasHotkey(hotkey, disabled ? void 0 : onSelect);
	const titleWithHotkey = title && actionKey ? `${title} (${actionKey.text})` : title;
	return /* @__PURE__ */ jsx("button", {
		role: "button",
		className: cx(className, CLASS_NAME$16),
		style: getPosition(dockSide, dockIndex),
		disabled,
		title: titleWithHotkey,
		"aria-label": title,
		"aria-keyshortcuts": actionKey?.text,
		onClick: onSelect,
		onMouseDown,
		onPointerDown,
		children
	});
}
/**
* Link action component to display a loading spinner.
*
* @category Components
*/
function LinkActionSpinner(props) {
	const { dockSide, dockIndex, className, title } = props;
	const { buttonSize, getPosition } = useLinkActionContext();
	return /* @__PURE__ */ jsx("div", {
		className: cx(className, `${CLASS_NAME$16}__spinner`),
		style: getPosition(dockSide, dockIndex),
		title,
		children: /* @__PURE__ */ jsx(HtmlSpinner, {
			width: buttonSize,
			height: buttonSize
		})
	});
}
/**
* Link action component to start editing the link.
*
* This action is visible only when {@link EditorController.inAuthoringMode graph authoring mode}
* is active.
*
* @category Components
*/
function LinkActionEdit(props) {
	const { className, title, ...otherProps } = props;
	const { link } = useLinkActionContext();
	const { model, editor, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const inAuthoringMode = useObservedProperty(editor.events, "changeMode", () => editor.inAuthoringMode);
	const fetchedCanModify = useCanModifyLink(inAuthoringMode ? link : void 0, model, editor);
	const canModify = fetchedCanModify.status === "completed" ? fetchedCanModify.data : void 0;
	const linkIsDeleted = useObservedProperty(editor.events, "changeAuthoringState", () => link instanceof RelationLink && AuthoringState.isDeletedRelation(editor.authoringState, link.data));
	if (!(inAuthoringMode && link instanceof RelationLink) || linkIsDeleted) return null;
	else if (canModify === void 0) {
		const { dockSide, dockIndex } = props;
		return /* @__PURE__ */ jsx(LinkActionSpinner, {
			dockSide,
			dockIndex
		});
	}
	return /* @__PURE__ */ jsx(LinkAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$16}__button`, `${CLASS_NAME$16}__edit`),
		title: title ?? (canModify.canChangeType ? t.text("link_action.edit_relation.title") : t.text("link_action.edit_relation.title_disabled")),
		disabled: !canModify.canChangeType,
		onSelect: () => getCommandBus(VisualAuthoringTopic).trigger("editRelation", { target: link })
	});
}
/**
* Link action component to delete the link.
* 
* This action is visible either if the link is an {@link AnnotationLink} or
* it is an {@link RelationLink} and {@link EditorController.inAuthoringMode graph authoring mode}
* is active.
*
* Deleting a link adds a command to the command history.
*
* @category Components
*/
function LinkActionDelete(props) {
	const { link } = useLinkActionContext();
	if (link instanceof RelationLink) return /* @__PURE__ */ jsx(LinkActionDeleteRelation, {
		...props,
		link
	});
	else if (link instanceof AnnotationLink) return /* @__PURE__ */ jsx(LinkActionDeleteAnnotation, {
		...props,
		link
	});
	else return null;
}
function LinkActionDeleteRelation(props) {
	const { link, className, title, hotkey, ...otherProps } = props;
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const fetchedCanModify = useCanModifyLink(link, model, editor);
	const canModify = fetchedCanModify.status === "completed" ? fetchedCanModify.data : void 0;
	const linkIsDeleted = useObservedProperty(editor.events, "changeAuthoringState", () => link instanceof RelationLink && AuthoringState.isDeletedRelation(editor.authoringState, link.data));
	if (!(editor.inAuthoringMode && link instanceof RelationLink) || linkIsDeleted) return null;
	else if (canModify === void 0) {
		const { dockSide, dockIndex } = props;
		return /* @__PURE__ */ jsx(LinkActionSpinner, {
			dockSide,
			dockIndex
		});
	}
	return /* @__PURE__ */ jsx(LinkAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$16}__button`, `${CLASS_NAME$16}__delete`),
		title: title ?? (canModify.canDelete ? t.text("link_action.delete_relation.title") : t.text("link_action.delete_relation.title_disabled")),
		hotkey: hotkey === void 0 ? "None+Delete" : hotkey,
		disabled: !canModify.canDelete,
		onSelect: () => editor.deleteRelation(link.data)
	});
}
function useCanModifyLink(link, graph, editor) {
	const { canvas } = useCanvas();
	const authoringState = useSyncStore(useLayerDebouncedStore(useEventStore(editor.events, "changeAuthoringState"), canvas.renderingState), () => editor.authoringState);
	return useAsync({
		input: [
			editor.metadataProvider,
			link,
			authoringState
		],
		load: async ([provider, relation, state], { signal }) => {
			if (provider && relation instanceof RelationLink && !isSourceOrTargetDeleted(state, relation)) {
				const source = graph.getElement(relation.sourceId);
				const target = graph.getElement(relation.targetId);
				return provider.canModifyRelation(relation.data, source.data, target.data, { signal });
			}
			return {};
		}
	});
}
function isSourceOrTargetDeleted(state, link) {
	return AuthoringState.isDeletedEntity(state, link.data.sourceId) || AuthoringState.isDeletedEntity(state, link.data.targetId);
}
function LinkActionDeleteAnnotation(props) {
	const { link, className, title, hotkey, ...otherProps } = props;
	const { editor } = useWorkspace();
	const t = useTranslation();
	return /* @__PURE__ */ jsx(LinkAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$16}__button`, `${CLASS_NAME$16}__delete`),
		title: title ?? t.text("link_action.delete_annotation.title"),
		hotkey: hotkey === void 0 ? "None+Delete" : hotkey,
		onSelect: () => editor.removeItems([link])
	});
}
/**
* Link action component to change the link by moving its endpoint to another element.
*
* The changed endpoint is specified via {@link LinkActionStyleProps.dockSide dockSide} prop.
*
* This action is visible either if the link is an {@link AnnotationLink} or
* it is an {@link RelationLink} and {@link EditorController.inAuthoringMode graph authoring mode}
* is active.
*
* Changing a link adds a command to the command history.
*
* @category Components
*/
function LinkActionMoveEndpoint(props) {
	const { link } = useLinkActionContext();
	if (link instanceof RelationLink) return /* @__PURE__ */ jsx(LinkActionMoveRelationEndpoint, {
		...props,
		link
	});
	else if (link instanceof AnnotationLink) return /* @__PURE__ */ jsx(LinkActionMoveAnnotationEndpoint, {
		...props,
		link
	});
	else return null;
}
function LinkActionMoveRelationEndpoint(props) {
	const { link, dockSide, className, title, ...otherProps } = props;
	const { buttonSize, getAngleInDegrees } = useLinkActionContext();
	const { canvas } = useCanvas();
	const { editor, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const inAuthoringMode = useObservedProperty(editor.events, "changeMode", () => editor.inAuthoringMode);
	const linkIsDeleted = useObservedProperty(editor.events, "changeAuthoringState", () => AuthoringState.isDeletedRelation(editor.authoringState, link.data));
	if (!inAuthoringMode) return null;
	return /* @__PURE__ */ jsx(LinkAction, {
		...otherProps,
		dockSide,
		dockIndex: 0,
		className: cx(className, `${CLASS_NAME$16}__endpoint`),
		title: title ?? (dockSide === "source" ? t.text("link_action.move_relation.move_source_title") : t.text("link_action.move_relation.move_target_title")),
		disabled: linkIsDeleted,
		onPointerDown: (e) => {
			e.preventDefault();
			const point = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			getCommandBus(VisualAuthoringTopic).trigger("startDragEdit", { operation: {
				mode: dockSide === "source" ? "moveSource" : "moveTarget",
				link,
				point
			} });
		},
		children: /* @__PURE__ */ jsx(LinkEndpointHandle, {
			dockSide,
			buttonSize,
			angle: getAngleInDegrees(dockSide)
		})
	});
}
function LinkActionMoveAnnotationEndpoint(props) {
	const { link, dockSide, className, title, ...otherProps } = props;
	const { buttonSize, getAngleInDegrees } = useLinkActionContext();
	const { canvas } = useCanvas();
	const { getCommandBus } = useWorkspace();
	const t = useTranslation();
	const commands = getCommandBus(AnnotationTopic);
	const event = { capabilities: [] };
	commands.trigger("findCapabilities", event);
	if (event.capabilities.length === 0) return null;
	return /* @__PURE__ */ jsx(LinkAction, {
		...otherProps,
		dockSide,
		dockIndex: 0,
		className: cx(className, `${CLASS_NAME$16}__endpoint`),
		title: title ?? (dockSide === "source" ? t.text("link_action.move_relation.move_source_title") : t.text("link_action.move_relation.move_target_title")),
		onPointerDown: (e) => {
			e.preventDefault();
			const point = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			getCommandBus(AnnotationTopic).trigger("startDragOperation", { operation: {
				mode: dockSide === "source" ? "moveSource" : "moveTarget",
				link,
				point
			} });
		},
		children: /* @__PURE__ */ jsx(LinkEndpointHandle, {
			dockSide,
			buttonSize,
			angle: getAngleInDegrees(dockSide)
		})
	});
}
function LinkEndpointHandle(props) {
	const { dockSide, buttonSize, angle } = props;
	return /* @__PURE__ */ jsx("svg", {
		width: buttonSize,
		height: buttonSize,
		style: { transform: `rotate(${angle}deg)` },
		children: /* @__PURE__ */ jsx("g", {
			transform: `scale(${buttonSize})`,
			children: dockSide === "source" ? /* @__PURE__ */ jsx("circle", {
				r: .5,
				cx: .5,
				cy: .5,
				fill: "currentColor"
			}) : /* @__PURE__ */ jsx("polygon", {
				points: "0,0.5 1,1 1,0",
				fill: "currentColor"
			})
		})
	});
}
/**
* Link action component to rename a link (change its label).
*
* This action is visible only when {@link RenameLinkProvider.canRename} returns `true`
* for the selected link.
*
* @category Components
*/
function LinkActionRename(props) {
	const { className, title } = props;
	const { link } = useLinkActionContext();
	const { canvas } = useCanvas();
	const { view: { renameLinkProvider }, getCommandBus } = useWorkspace();
	const t = useTranslation();
	const labelBounds = useSyncStore(useEventStore(canvas.renderingState.events, "changeLinkLabelBounds"), () => canvas.renderingState.getLinkLabelBounds(link));
	if (!(renameLinkProvider?.canRename(link) && labelBounds)) return null;
	const { x, y, width, height } = labelBounds;
	const { x: left, y: top } = canvas.metrics.paperToScrollablePaneCoords(x + width, y + height / 2);
	const size = {
		width: 15,
		height: 17
	};
	const style = {
		width: size.width,
		height: size.height,
		left,
		top: top - size.height / 2
	};
	return /* @__PURE__ */ jsx("button", {
		className: cx(className, `${CLASS_NAME$16}__rename`),
		style,
		title: title ?? t.text("link_action.rename_link.title"),
		onClick: () => getCommandBus(AnnotationTopic).trigger("renameLink", { target: link })
	});
}
//#endregion
//#region src/widgets/haloLink.tsx
/**
* Canvas widget component to display actions for the single selected diagram link.
*
* @category Components
*/
function HaloLink(props) {
	const { canvas, model } = useCanvas();
	const singleTarget = useSyncStore(useEventStore(model.events, "changeSelection"), () => {
		const target = model.selection.length === 1 ? model.selection[0] : void 0;
		return target instanceof Link ? target : void 0;
	});
	if (singleTarget) return /* @__PURE__ */ jsx(HaloLinkInner, {
		...props,
		target: singleTarget,
		model,
		canvas
	});
	return null;
}
var CLASS_NAME$15 = "reactodia-halo-link";
var DEFAULT_HIGHLIGHT_MARGIN = 0;
var DEFAULT_BUTTON_SIZE = 20;
var DEFAULT_BUTTON_MARGIN = 5;
var HaloLinkInner = class HaloLinkInner extends React.Component {
	listener = new EventObserver();
	targetListener = new EventObserver();
	constructor(props) {
		super(props);
		this.state = { actionContext: HaloLinkInner.makeActionContext(this.props) };
	}
	static makeActionContext(props) {
		const { target, model, canvas, buttonSize = DEFAULT_BUTTON_SIZE, buttonMargin = DEFAULT_BUTTON_MARGIN } = props;
		const spline = computeLinkSpline(target, model, canvas.renderingState);
		if (!spline) return null;
		const polylineLength = computePolylineLength(spline.geometry.points);
		const getPosition = (side, index) => {
			const shift = (buttonSize + buttonMargin) * index;
			const point = getPointAlongPolyline(spline.geometry.points, side === "source" ? shift : polylineLength - shift);
			const { x, y } = canvas.metrics.paperToScrollablePaneCoords(point.x, point.y);
			return {
				top: y - buttonSize / 2,
				left: x - buttonSize / 2
			};
		};
		const getAngleInDegrees = (side) => {
			const { points } = spline.geometry;
			const start = points[side === "source" ? 1 : points.length - 1];
			const end = points[side === "source" ? 0 : points.length - 2];
			const unit = Vector.normalize(Vector.subtract(end, start));
			return Math.atan2(unit.y, unit.x) * (180 / Math.PI);
		};
		return {
			link: target,
			spline,
			buttonSize,
			getPosition,
			getAngleInDegrees
		};
	}
	componentDidMount() {
		const { canvas, target } = this.props;
		this.listener.listen(canvas.events, "changeTransform", () => this.forceUpdate());
		this.listenToTarget(target);
	}
	componentDidUpdate(prevProps) {
		if (this.props.target !== prevProps.target) this.listenToTarget(this.props.target);
		if (!(this.props.target === prevProps.target && this.props.buttonSize === prevProps.buttonSize && this.props.buttonMargin === prevProps.buttonMargin)) this.updateActionContext();
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.listenToTarget(void 0);
	}
	listenToTarget(link) {
		const { model, canvas } = this.props;
		this.targetListener.stopListening();
		if (link) {
			const source = model.getElement(link.sourceId);
			const target = model.getElement(link.targetId);
			this.targetListener.listen(source.events, "changePosition", this.updateActionContext);
			this.targetListener.listen(target.events, "changePosition", this.updateActionContext);
			this.targetListener.listen(link.events, "changeVertices", this.updateActionContext);
			this.targetListener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
				if (e.source === source || e.source === target) this.updateActionContext();
			});
			this.targetListener.listen(canvas.renderingState.events, "changeRoutings", (e) => {
				if (e.previous.get(link.id) !== e.source.getRouting(link.id)) this.updateActionContext();
			});
		}
	}
	updateActionContext = () => {
		this.setState((state, props) => ({ actionContext: HaloLinkInner.makeActionContext(props) }));
	};
	render() {
		const { canvas, highlightMargin = DEFAULT_HIGHLIGHT_MARGIN, buttonSize = DEFAULT_BUTTON_SIZE, buttonMargin = DEFAULT_BUTTON_MARGIN, children } = this.props;
		const { actionContext } = this.state;
		if (!actionContext) return null;
		const style = {
			"--reactodia-link-button-size": `${buttonSize}px`,
			"--reactodia-link-button-margin": `${buttonMargin}px`
		};
		return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(LinkHighlight, {
			actionContext,
			style,
			margin: highlightMargin,
			canvas
		}), /* @__PURE__ */ jsx(CanvasPlaceAt, {
			layer: "overElements",
			children: /* @__PURE__ */ jsx("div", {
				className: CLASS_NAME$15,
				style,
				children: /* @__PURE__ */ jsx(LinkActionProvidedContext.Provider, {
					value: actionContext,
					children: children ?? /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx(LinkActionMoveEndpoint, { dockSide: "target" }),
						/* @__PURE__ */ jsx(LinkActionMoveEndpoint, { dockSide: "source" }),
						/* @__PURE__ */ jsx(LinkActionEdit, {
							dockSide: "target",
							dockIndex: 1
						}),
						/* @__PURE__ */ jsx(LinkActionDelete, {
							dockSide: "target",
							dockIndex: 2
						}),
						/* @__PURE__ */ jsx(LinkActionRename, {})
					] })
				})
			})
		})] });
	}
};
function computeLinkSpline(link, graph, renderingState) {
	const source = graph.getElement(link.sourceId);
	const target = graph.getElement(link.targetId);
	if (!(source && target)) return;
	const template = renderingState.getLinkTemplate(link);
	const route = renderingState.getRouting(link.id);
	const verticesDefinedByUser = link.vertices || [];
	const vertices = route ? route.vertices : verticesDefinedByUser;
	const sourceShape = renderingState.getElementShape(source);
	const targetShape = renderingState.getElementShape(target);
	const points = computePolyline(sourceShape, targetShape, vertices);
	return Spline.create({
		type: template?.spline ?? Spline.defaultType,
		points,
		source: Rect.center(sourceShape.bounds),
		target: Rect.center(targetShape.bounds)
	});
}
function LinkHighlight(props) {
	const { actionContext: { link, spline }, style, margin, canvas } = props;
	const labelBounds = useSyncStore(useEventStore(canvas.renderingState.events, "changeLinkLabelBounds"), () => canvas.renderingState.getLinkLabelBounds(link));
	let labelHighlightStyle;
	if (labelBounds) {
		const { x: x0, y: y0 } = canvas.metrics.paperToScrollablePaneCoords(labelBounds.x, labelBounds.y);
		const { x: x1, y: y1 } = canvas.metrics.paperToScrollablePaneCoords(labelBounds.x + labelBounds.width, labelBounds.y + labelBounds.height);
		labelHighlightStyle = {
			left: x0 - margin,
			top: y0 - margin,
			width: x1 - x0 + margin * 2,
			height: y1 - y0 + margin * 2
		};
	}
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(CanvasPlaceAt, {
		layer: "overLinkGeometry",
		children: /* @__PURE__ */ jsx(SvgPaperLayer, {
			paperTransform: canvas.metrics.transform,
			className: CLASS_NAME$15,
			style: {
				...style,
				overflow: "visible",
				pointerEvents: "none"
			},
			children: /* @__PURE__ */ jsx("path", {
				className: `${CLASS_NAME$15}__path-highlight`,
				d: spline.toPath()
			})
		})
	}), labelHighlightStyle && /* @__PURE__ */ jsx(CanvasPlaceAt, {
		layer: "overLinks",
		children: /* @__PURE__ */ jsx("div", {
			className: CLASS_NAME$15,
			style,
			children: /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$15}__label-highlight`,
				style: labelHighlightStyle
			})
		})
	})] });
}
//#endregion
//#region src/widgets/utility/inlineEntity.tsx
var CLASS_NAME$14 = "reactodia-inline-entity";
function InlineEntity(props) {
	const { target } = props;
	const { model, getElementTypeStyle } = useWorkspace();
	const t = useTranslation();
	const label = model.locale.formatEntityLabel(target, model.language);
	const { color } = getElementTypeStyle(target.types);
	return /* @__PURE__ */ jsx("span", {
		className: CLASS_NAME$14,
		style: { "--reactodia-inline-entity-color": color },
		title: formatEntityTitle(target, model, t),
		children: /* @__PURE__ */ jsx("span", {
			className: `${CLASS_NAME$14}__label`,
			children: label
		})
	});
}
//#endregion
//#region src/widgets/instancesSearch.tsx
/**
* Component to search for entities by various filter criteria
* to add them as elements to the diagram.
*
* @category Components
*/
function InstancesSearch(props) {
	const { searchStore, searchTimeout = "explicit", minSearchTermLength = 3 } = props;
	const uncontrolledSearch = useSearchInputStore({
		initialValue: "",
		submitTimeout: searchTimeout,
		allowSubmit: (term) => term.length >= minSearchTermLength
	});
	const effectiveSearchStore = searchStore ?? uncontrolledSearch;
	const workspace = useWorkspace();
	const t = useTranslation();
	return /* @__PURE__ */ jsx(InstancesSearchInner, {
		...props,
		isControlled: Boolean(searchStore),
		searchStore: effectiveSearchStore,
		minSearchTermLength,
		workspace,
		translation: t
	});
}
var CLASS_NAME$13 = "reactodia-instances-search";
var ITEMS_PER_PAGE = 100;
var InstancesSearchInner = class extends React.Component {
	listener = new EventObserver();
	criteriaListener = new EventObserver();
	searchListener = new EventObserver();
	delayedUpdateAll = new Debouncer();
	requestCancellation = new AbortController();
	currentRequest;
	constructor(props) {
		super(props);
		this.state = {
			criteria: {},
			resultId: 0,
			selection: /* @__PURE__ */ new Set()
		};
	}
	componentDidMount() {
		const { workspace } = this.props;
		const { model } = workspace;
		this.listener.listen(model.events, "changeLanguage", () => this.forceUpdate());
		this.listener.listen(model.events, "loadingStart", () => {
			this.setState({ criteria: {} }, () => this.props.searchStore.change({
				value: "",
				action: "clear"
			}));
		});
		const commands = workspace.getCommandBus(InstancesSearchTopic);
		this.listener.listen(commands, "findCapabilities", (e) => {
			e.capabilities.push({});
		});
		this.listener.listen(commands, "setCriteria", ({ criteria }) => {
			const { searchStore, onChangeCriteria, workspace: { triggerWorkspaceEvent } } = this.props;
			triggerWorkspaceEvent(WorkspaceEventKey.searchUpdateCriteria);
			this.setState({ criteria }, () => {
				searchStore.change({
					value: criteria.text ?? "",
					action: "clear"
				});
				onChangeCriteria?.(this.state.criteria);
			});
		});
		this.listenSearch();
		this.resubscribeToCriteria();
		this.queryItems(false);
	}
	componentDidUpdate(prevProps, prevState) {
		if (this.props.searchStore.events !== prevProps.searchStore.events) this.listenSearch();
		if (this.state.criteria !== prevState.criteria) {
			this.resubscribeToCriteria();
			this.queryItems(false);
		}
	}
	listenSearch() {
		const { searchStore } = this.props;
		this.searchListener.stopListening();
		this.searchListener.listen(searchStore.events, "executeSearch", ({ value }) => {
			this.submitCriteriaUpdate(value, { triggerChange: false });
		});
		this.searchListener.listen(searchStore.events, "clearSearch", () => {
			this.submitCriteriaUpdate("", { triggerChange: false });
		});
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.criteriaListener.stopListening();
		this.searchListener.stopListening();
		this.requestCancellation.abort();
		this.currentRequest = void 0;
	}
	resubscribeToCriteria() {
		const { workspace: { model } } = this.props;
		const { criteria } = this.state;
		this.criteriaListener.stopListening();
		if (criteria.elementType) {
			const elementType = model.createElementType(criteria.elementType);
			if (elementType) this.criteriaListener.listen(elementType.events, "changeData", this.scheduleUpdateAll);
		}
		if (criteria.refElement) {
			const element = model.elements.find((element) => element instanceof EntityElement && element.iri === criteria.refElement);
			if (element) this.criteriaListener.listen(element.events, "changeData", this.scheduleUpdateAll);
		}
		if (criteria.refElementLink) {
			const linkType = model.createLinkType(criteria.refElementLink);
			if (linkType) this.criteriaListener.listen(linkType.events, "changeData", this.scheduleUpdateAll);
		}
	}
	scheduleUpdateAll = () => {
		this.delayedUpdateAll.call(this.updateAll);
	};
	updateAll = () => this.forceUpdate();
	render() {
		const { className, isControlled, searchStore, minSearchTermLength, translation: t } = this.props;
		const progressState = this.state.querying ? "loading" : this.state.error ? "error" : this.state.items ? "completed" : "none";
		const resultItems = this.state.items ?? [];
		const actionsAreHidden = this.state.querying || this.state.selection.size === 0;
		return /* @__PURE__ */ jsxs("div", {
			className: cx(CLASS_NAME$13, isControlled ? `${CLASS_NAME$13}--controlled` : void 0, className),
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$13}__criteria`,
					children: [this.renderCriteria(), isControlled ? null : /* @__PURE__ */ jsx(SearchInput, {
						store: searchStore,
						className: `${CLASS_NAME$13}__text-criteria`,
						inputProps: {
							name: "reactodia-instances-search-text",
							placeholder: t.textOptional("search_entities.input.placeholder")
						}
					})]
				}),
				/* @__PURE__ */ jsx(ProgressBar, {
					state: progressState,
					title: t.text("search_entities.query_progress.title")
				}),
				/* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$13}__rest reactodia-scrollable`,
					tabIndex: -1,
					children: [/* @__PURE__ */ jsx(SearchResults, {
						items: resultItems,
						highlightText: this.state.criteria.text,
						selection: this.state.selection,
						onSelectionChanged: this.onSelectionChanged,
						footer: resultItems.length === 0 ? /* @__PURE__ */ jsx(NoSearchResults, {
							hasQuery: this.state.items !== void 0,
							minSearchTermLength
						}) : null
					}), /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$13}__rest-end`,
						children: /* @__PURE__ */ jsx("button", {
							type: "button",
							className: `${CLASS_NAME$13}__load-more reactodia-btn reactodia-btn-primary`,
							disabled: this.state.querying,
							style: { display: this.state.moreItemsAvailable ? void 0 : "none" },
							title: t.text("search_entities.show_more_results.title"),
							onClick: () => this.queryItems(true),
							children: t.text("search_entities.show_more_results.label")
						})
					})]
				}, this.state.resultId),
				/* @__PURE__ */ jsxs("div", {
					className: cx(`${CLASS_NAME$13}__actions`, actionsAreHidden ? `${CLASS_NAME$13}__actions-hidden` : void 0),
					"aria-hidden": actionsAreHidden ? "true" : void 0,
					children: [/* @__PURE__ */ jsx("button", {
						type: "button",
						className: `${CLASS_NAME$13}__action reactodia-btn reactodia-btn-secondary`,
						disabled: this.state.querying || this.state.selection.size <= 1,
						title: t.text("search_entities.add_group.title"),
						onClick: () => this.placeSelectedItems("group"),
						children: t.text("search_entities.add_group.label")
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						className: `${CLASS_NAME$13}__action reactodia-btn reactodia-btn-primary`,
						disabled: this.state.querying || this.state.selection.size === 0,
						title: t.text("search_entities.add_selected.title"),
						onClick: () => this.placeSelectedItems("separately"),
						children: t.text("search_entities.add_selected.label")
					})]
				})
			]
		});
	}
	onSelectionChanged = (newSelection) => {
		this.setState({ selection: newSelection });
	};
	renderCriteria() {
		const { workspace: { model }, translation: t } = this.props;
		const { criteria } = this.state;
		const criterions = [];
		if (criteria.elementType) {
			const elementTypeInfo = model.getElementType(criteria.elementType);
			const elementTypeLabel = t.formatLabel(elementTypeInfo?.data?.label, criteria.elementType, model.language);
			criterions.push(/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$13}__criterion`,
				children: [this.renderRemoveCriterionButtons(() => this.setState({ criteria: {
					...criteria,
					elementType: void 0
				} }, () => this.props.onChangeCriteria?.(this.state.criteria))), t.template("search_entities.criteria_has_type", { entityType: /* @__PURE__ */ jsx("span", {
					className: `${CLASS_NAME$13}__criterion-class`,
					title: criteria.elementType,
					children: elementTypeLabel
				}) })]
			}, "hasType"));
		} else if (criteria.refElement) {
			const refElementData = findEntityData(model, criteria.refElement) ?? EntityElement.placeholderData(criteria.refElement);
			let linkTypeLabel;
			if (criteria.refElementLink) {
				const linkTypeData = model.getLinkType(criteria.refElementLink);
				linkTypeLabel = t.formatLabel(linkTypeData?.data?.label, criteria.refElementLink, model.language);
			}
			const entity = /* @__PURE__ */ jsx(InlineEntity, { target: refElementData });
			const relationType = criteria.refElementLink ? /* @__PURE__ */ jsx("span", {
				className: `${CLASS_NAME$13}__criterion-link-type`,
				title: criteria.refElementLink,
				children: linkTypeLabel
			}) : void 0;
			const sourceIcon = /* @__PURE__ */ jsx("span", { className: `${CLASS_NAME$13}__link-direction-in` });
			const targetIcon = /* @__PURE__ */ jsx("span", { className: `${CLASS_NAME$13}__link-direction-out` });
			criterions.push(/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$13}__criterion`,
				children: [this.renderRemoveCriterionButtons(() => this.setState({ criteria: {
					...criteria,
					refElement: void 0,
					refElementLink: void 0
				} }, () => this.props.onChangeCriteria?.(this.state.criteria))), !criteria.refElementLink ? t.template("search_entities.criteria_connected", {
					entity,
					relationType,
					sourceIcon,
					targetIcon
				}) : criteria.linkDirection === "in" ? t.template("search_entities.criteria_connected_to_source", {
					entity,
					relationType,
					sourceIcon,
					targetIcon
				}) : criteria.linkDirection == "out" ? t.template("search_entities.criteria_connected_to_target", {
					entity,
					relationType,
					sourceIcon,
					targetIcon
				}) : t.template("search_entities.criteria_connected_via", {
					entity,
					relationType,
					sourceIcon,
					targetIcon
				})]
			}, "hasLinkedElement"));
		}
		return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$13}__criterions`,
			children: criterions
		});
	}
	renderRemoveCriterionButtons(onClick) {
		return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$13}__criterion-remove reactodia-btn-group reactodia-btn-group-xs`,
			children: /* @__PURE__ */ jsx("button", {
				type: "button",
				title: "Remove criteria",
				className: cx(`${CLASS_NAME$13}__criterion-remove-button`, "reactodia-btn reactodia-btn-default"),
				onClick
			})
		});
	}
	submitCriteriaUpdate(term, options) {
		const { onChangeCriteria } = this.props;
		this.setState((state) => {
			const text = term === "" ? void 0 : term;
			return state.criteria.text === text ? null : { criteria: {
				...state.criteria,
				text
			} };
		}, options.triggerChange ? () => onChangeCriteria?.(this.state.criteria) : void 0);
	}
	queryItems(loadMoreItems) {
		const { workspace: { model, triggerWorkspaceEvent } } = this.props;
		this.requestCancellation.abort();
		let request;
		if (loadMoreItems) {
			if (!this.currentRequest) throw new Error("Cannot request more items without initial request.");
			const { limit } = this.currentRequest;
			request = {
				...this.currentRequest,
				limit: typeof limit === "number" ? limit + ITEMS_PER_PAGE : limit
			};
		} else request = createRequest(this.state.criteria);
		if (!(request.text || request.elementTypeId || request.refElementId || request.refElementLinkId)) {
			this.setState({
				querying: false,
				error: void 0,
				items: void 0,
				selection: /* @__PURE__ */ new Set(),
				moreItemsAvailable: false
			});
			return;
		}
		this.requestCancellation = new AbortController();
		const signal = this.requestCancellation.signal;
		request = {
			...request,
			signal
		};
		this.currentRequest = request;
		this.setState({
			querying: true,
			error: void 0,
			moreItemsAvailable: false
		});
		model.dataProvider.lookup(request).then((elements) => {
			if (signal.aborted) return;
			this.processFilterData(elements);
			triggerWorkspaceEvent(WorkspaceEventKey.searchQueryItem);
		}).catch((error) => {
			if (signal.aborted) return;
			console.error(error);
			this.setState({
				querying: false,
				error
			});
		});
	}
	processFilterData(elements) {
		const requestedAdditionalItems = typeof this.currentRequest.limit === "number" && this.currentRequest.limit > ITEMS_PER_PAGE;
		const existingIris = /* @__PURE__ */ new Set();
		if (requestedAdditionalItems) for (const item of this.state.items) existingIris.add(item.id);
		const items = requestedAdditionalItems ? [...this.state.items] : [];
		for (const { element } of elements) {
			if (existingIris.has(element.id)) continue;
			items.push(element);
		}
		const moreItemsAvailable = typeof this.currentRequest.limit === "number" && elements.length >= this.currentRequest.limit;
		if (requestedAdditionalItems) this.setState({
			querying: false,
			items,
			error: void 0,
			moreItemsAvailable
		});
		else this.setState({
			querying: false,
			resultId: this.state.resultId + 1,
			items,
			selection: /* @__PURE__ */ new Set(),
			error: void 0,
			moreItemsAvailable
		});
	}
	placeSelectedItems(mode) {
		const { onAddElements, workspace: { model, view } } = this.props;
		const canvas = view.findAnyCanvas();
		const { items, selection } = this.state;
		if (!canvas || selection.size === 0) return;
		const batch = model.history.startBatch(TranslatedText.text("search_entities.place_elements.command"));
		const selectedEntities = items ? items.filter((item) => selection.has(item.id)) : Array.from(selection, EntityElement.placeholderData);
		let elements;
		if (mode === "separately") {
			const target = new VoidElement({ position: getViewportPlacementPosition(canvas, .3, .5) });
			elements = selectedEntities.map((entity) => model.createElement(entity));
			canvas.renderingState.syncUpdate();
			batch.history.execute(placeElementsAroundTarget({
				target,
				elements,
				graph: model,
				sizeProvider: canvas.renderingState,
				distance: 150
			}));
		} else {
			const group = new EntityGroup({
				items: selectedEntities.map((data) => ({ data })),
				position: getViewportPlacementPosition(canvas, .5, .5)
			});
			elements = [group];
			model.addElement(group);
			canvas.renderingState.syncUpdate();
			const { x, y, width, height } = boundsOf(group, canvas.renderingState);
			group.setPosition({
				x: x - width / 2,
				y: y - height / 2
			});
		}
		const addedElements = Array.from(selection);
		batch.history.execute(requestElementData(model, addedElements));
		batch.history.execute(restoreLinksBetweenElements(model, { addedElements }));
		batch.store();
		onAddElements?.(elements);
	}
};
function findEntityData(graph, iri) {
	for (const element of graph.elements) for (const entity of iterateEntitiesOf(element)) if (entity.id === iri) return entity;
}
function createRequest(criteria) {
	const { text, elementType, refElement, refElementLink, linkDirection } = criteria;
	return {
		text,
		elementTypeId: elementType,
		refElementId: refElement,
		refElementLinkId: refElementLink,
		linkDirection,
		limit: ITEMS_PER_PAGE
	};
}
function getViewportPlacementPosition(canvas, fractionX, fractionY) {
	const viewport = canvas.metrics.pane;
	return canvas.metrics.clientToPaperCoords(viewport.clientWidth * fractionX, viewport.clientHeight * fractionY);
}
//#endregion
//#region src/widgets/linksToolbox.tsx
/**
* Component to display incoming and outgoing link types from selected elements,
* toggle their visibility and initiate the lookup for connected entities.
*
* @category Components
*/
function LinkTypesToolbox(props) {
	const { searchStore, searchTimeout = 200, minSearchTermLength = 1 } = props;
	const uncontrolledSearch = useSearchInputStore({
		initialValue: "",
		submitTimeout: searchTimeout,
		allowSubmit: (term) => term.length >= minSearchTermLength
	});
	const effectiveSearchStore = searchStore ?? uncontrolledSearch;
	const workspace = useWorkspace();
	const t = useTranslation();
	return /* @__PURE__ */ jsx(LinkTypesToolboxInner, {
		...props,
		isControlled: Boolean(searchStore),
		searchStore: effectiveSearchStore,
		minSearchTermLength,
		workspace,
		translation: t
	});
}
var CLASS_NAME$12 = "reactodia-links-toolbox";
var DEFAULT_TRACK_SELECTED = true;
var LinkTypesToolboxInner = class extends React.Component {
	listener = new EventObserver();
	selectionListener = new EventObserver();
	searchListener = new EventObserver();
	linkListener = new EventObserver();
	debounceSelection = new Debouncer(50);
	delayedUpdateAll = new Debouncer();
	constructor(props) {
		super(props);
		this.state = { filteredLinks: {
			term: "",
			diagramHasLink: false,
			selection: [],
			selectionLinks: /* @__PURE__ */ new Set(),
			links: []
		} };
	}
	componentDidMount() {
		const { workspace: { model } } = this.props;
		this.listener.listen(model.events, "loadingSuccess", this.updateOnCurrentSelection);
		this.listener.listen(model.events, "changeLanguage", this.updateOnCurrentSelection);
		this.subscribeToSelectionChanges();
		this.listenSearch();
		this.updateOnCurrentSelection();
	}
	componentDidUpdate(prevProps, prevState) {
		const { trackSelected = DEFAULT_TRACK_SELECTED, searchStore } = this.props;
		if (trackSelected !== prevProps.trackSelected) this.subscribeToSelectionChanges();
		if (searchStore.events !== prevProps.searchStore.events) this.listenSearch();
		if (this.state.filteredLinks !== prevState.filteredLinks) this.subscribeOnFilteredLinksEvents();
	}
	listenSearch() {
		const { searchStore } = this.props;
		this.searchListener.stopListening();
		this.searchListener.listen(searchStore.events, "executeSearch", ({ value }) => {
			this.setState((state, props) => applyFilter(state, value, props));
		});
		this.searchListener.listen(searchStore.events, "clearSearch", () => {
			this.setState((state, props) => applyFilter(state, "", props));
		});
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.selectionListener.stopListening();
		this.searchListener.stopListening();
		this.linkListener.stopListening();
		this.debounceSelection.dispose();
		this.delayedUpdateAll.dispose();
	}
	subscribeToSelectionChanges() {
		const { trackSelected = DEFAULT_TRACK_SELECTED, workspace: { model } } = this.props;
		this.selectionListener.stopListening();
		if (trackSelected) this.selectionListener.listen(model.events, "changeSelection", () => {
			this.debounceSelection.call(this.updateOnCurrentSelection);
		});
	}
	updateOnCurrentSelection = () => {
		this.setState((state, props) => applyFilter(state, props.searchStore.value, props));
	};
	subscribeOnFilteredLinksEvents() {
		const { workspace: { model } } = this.props;
		const { filteredLinks } = this.state;
		this.linkListener.stopListening();
		const linkTypeIris = /* @__PURE__ */ new Set();
		for (const linkType of filteredLinks.links) {
			linkTypeIris.add(linkType.iri);
			this.linkListener.listen(linkType.type.events, "changeData", this.onLinkChanged);
		}
		this.linkListener.listen(model.events, "changeLinkVisibility", (e) => {
			if (linkTypeIris.has(e.source)) this.onLinkChanged();
		});
	}
	onLinkChanged = () => {
		this.delayedUpdateAll.call(() => this.forceUpdate());
	};
	render() {
		const { className, isControlled, searchStore, minSearchTermLength, workspace, translation: t } = this.props;
		const { filteredLinks } = this.state;
		const connectedLinks = filteredLinks.links.filter((link) => filteredLinks.selectionLinks.has(link.iri));
		const otherLinks = filteredLinks.links.filter((link) => !filteredLinks.selectionLinks.has(link.iri));
		return /* @__PURE__ */ jsxs("div", {
			className: cx(CLASS_NAME$12, isControlled ? `${CLASS_NAME$12}--controlled` : void 0, className),
			children: [/* @__PURE__ */ jsxs("div", {
				className: `${CLASS_NAME$12}__heading`,
				children: [isControlled ? null : /* @__PURE__ */ jsx(SearchInput, {
					store: searchStore,
					className: `${CLASS_NAME$12}__filter`,
					inputProps: {
						name: "reactodia-link-types-filter",
						placeholder: t.textOptional("search_link_types.input.placeholder")
					}
				}), /* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$12}__switch-all`,
					children: [/* @__PURE__ */ jsx(VisibilityControl, {
						onSetVisibility: (mode) => changeLinkTypeState(filteredLinks.links, mode, workspace),
						disabled: filteredLinks.links.length === 0
					}), /* @__PURE__ */ jsxs("span", { children: ["\xA0", t.text("search_link_types.switch_all.label")] })]
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$12}__rest`,
				children: /* @__PURE__ */ jsxs("div", {
					className: "reactodia-scrollable",
					children: [
						connectedLinks.length > 0 ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("div", {
							role: "heading",
							className: `${CLASS_NAME$12}__links-heading`,
							children: filteredLinks.selection.length === 1 ? t.template("search_link_types.heading_connected_single", { entity: /* @__PURE__ */ jsx(InlineEntity, { target: filteredLinks.selection[0] }) }) : t.template("search_link_types.heading_connected", { count: filteredLinks.selection.length })
						}), this.renderLinks(connectedLinks)] }) : null,
						connectedLinks.length > 0 && otherLinks.length > 0 ? /* @__PURE__ */ jsx("div", {
							role: "heading",
							className: `${CLASS_NAME$12}__links-heading`,
							children: t.template("search_link_types.heading_other", { count: otherLinks.length })
						}) : null,
						this.renderLinks(otherLinks),
						filteredLinks.links.length === 0 ? /* @__PURE__ */ jsx(NoSearchResults, {
							className: `${CLASS_NAME$12}__no-results`,
							hasQuery: filteredLinks.term.length > 0,
							minSearchTermLength,
							message: filteredLinks.diagramHasLink ? void 0 : t.text("search_link_types.no_results")
						}) : null
					]
				})
			})]
		});
	}
	renderLinks(links) {
		const { workspace: { getCommandBus } } = this.props;
		const { filteredLinks } = this.state;
		const commands = getCommandBus(InstancesSearchTopic);
		const event = { capabilities: [] };
		commands.trigger("findCapabilities", event);
		return /* @__PURE__ */ jsx("ul", {
			className: `${CLASS_NAME$12}__links`,
			children: links.map((link) => /* @__PURE__ */ jsx(LinkInToolBox, {
				link,
				onAddToFilter: event.capabilities.length > 0 && filteredLinks.selectionLinks.has(link.iri) ? this.onAddToFilter : void 0,
				filterKey: filteredLinks.term
			}, link.iri))
		});
	}
	onAddToFilter = (linkType) => {
		const { workspace: { getCommandBus } } = this.props;
		const { filteredLinks } = this.state;
		if (filteredLinks.selection.length === 1) getCommandBus(InstancesSearchTopic).trigger("setCriteria", { criteria: {
			refElement: filteredLinks.selection[0].id,
			refElementLink: linkType
		} });
	};
};
function applyFilter(state, term, props) {
	const { trackSelected = DEFAULT_TRACK_SELECTED, workspace: { model }, translation: t } = props;
	const allLinkTypeIris = /* @__PURE__ */ new Set();
	for (const link of model.links) if (link instanceof RelationLink || link instanceof RelationGroup) allLinkTypeIris.add(link.typeId);
	const termFilter = makeCaseInsensitiveFilter(term);
	const allLinkTypes = Array.from(allLinkTypeIris, (iri) => model.createLinkType(iri)).map((link) => ({
		iri: link.id,
		type: link,
		label: t.formatLabel(link.data?.label, link.id, model.language)
	})).filter((link) => termFilter(link.label)).sort((a, b) => {
		return a.label.localeCompare(b.label);
	});
	const entities = /* @__PURE__ */ new Map();
	const selectionLinks = /* @__PURE__ */ new Set();
	if (trackSelected) {
		for (const item of model.selection) if (item instanceof Element) {
			for (const entity of iterateEntitiesOf(item)) entities.set(entity.id, entity);
			for (const link of model.getElementLinks(item)) selectionLinks.add(link.typeId);
		}
	}
	const filteredLinks = {
		term,
		diagramHasLink: allLinkTypeIris.size > 0,
		selection: Array.from(entities.values()),
		selectionLinks,
		links: allLinkTypes
	};
	return {
		...state,
		filteredLinks
	};
}
function LinkInToolBox(props) {
	const { link, filterKey, onAddToFilter } = props;
	const workspace = useWorkspace();
	const t = useTranslation();
	const { model } = workspace;
	return /* @__PURE__ */ jsxs("li", {
		"data-linktypeid": link.iri,
		className: `${CLASS_NAME$12}__link-item`,
		children: [
			/* @__PURE__ */ jsx(VisibilityControl, {
				className: `${CLASS_NAME$12}__link-buttons`,
				visibility: model.getLinkVisibility(link.iri),
				onSetVisibility: (mode) => changeLinkTypeState([link], mode, workspace)
			}),
			/* @__PURE__ */ jsx(WithFetchStatus, {
				type: "linkType",
				target: link.iri,
				children: /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$12}__link-title`,
					children: highlightSubstring(link.label, filterKey)
				})
			}),
			onAddToFilter ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$12}__filter-button`,
				title: t.text("search_link_types.add_to_filter.title"),
				onClick: () => onAddToFilter(link.iri)
			}) : null
		]
	});
}
function VisibilityControl(props) {
	const { className, visibility, onSetVisibility, disabled } = props;
	const t = useTranslation();
	return /* @__PURE__ */ jsxs("div", {
		className: cx(className, "reactodia-btn-group reactodia-btn-group-xs"),
		children: [
			/* @__PURE__ */ jsx("button", {
				title: t.text("search_link_types.switch_hidden.title"),
				className: cx(`${CLASS_NAME$12}__toggle-invisible`, "reactodia-btn reactodia-btn-default", visibility === "hidden" ? "active" : void 0),
				disabled,
				onClick: () => onSetVisibility("hidden")
			}),
			/* @__PURE__ */ jsx("button", {
				title: t.text("search_link_types.switch_without_label.title"),
				className: cx(`${CLASS_NAME$12}__toggle-lines-only`, "reactodia-btn reactodia-btn-default", visibility === "withoutLabel" ? "active" : void 0),
				disabled,
				onClick: () => onSetVisibility("withoutLabel")
			}),
			/* @__PURE__ */ jsx("button", {
				title: t.text("search_link_types.switch_visible.title"),
				className: cx(`${CLASS_NAME$12}__toggle-visible`, "reactodia-btn reactodia-btn-default", visibility === "visible" ? "active" : void 0),
				disabled,
				onClick: () => onSetVisibility("visible")
			})
		]
	});
}
function changeLinkTypeState(linkTypes, state, workspace) {
	const { model } = workspace;
	const batch = model.history.startBatch(TranslatedText.text("search_link_types.switch.command"));
	for (const linkType of linkTypes) model.history.execute(changeLinkTypeVisibility(model, linkType.iri, state));
	batch.store();
}
//#endregion
//#region src/widgets/navigator.tsx
/**
* Canvas widget component to display a minimap of the diagram contents.
*
* @category Components
*/
function Navigator(props) {
	const { canvas } = useCanvas();
	const workspace = useWorkspace();
	const t = useTranslation();
	const colorScheme = useColorScheme();
	return /* @__PURE__ */ jsx(NavigatorInner, {
		...props,
		canvas,
		workspace,
		translation: t,
		colorScheme
	});
}
var CLASS_NAME$11 = "reactodia-navigator";
var MIN_SCALE = .25;
var MAX_SIZE_FRACTION = .9;
var DEFAULT_EXPANDED = "auto";
var DEFAULT_AUTO_COLLAPSE_FRACTION = .4;
var DEFAULT_WIDTH = 300;
var DEFAULT_HEIGHT = 160;
var DEFAULT_SCALE_PADDING = .2;
var NavigatorInner = class extends React.Component {
	delayedRedraw = new Debouncer();
	delayedSizeCheck = new Debouncer();
	listener = new EventObserver();
	canvas;
	transform;
	isDraggingViewport = false;
	constructor(props) {
		super(props);
		const { expanded = DEFAULT_EXPANDED } = this.props;
		this.state = {
			initialized: false,
			expanded: Boolean(expanded),
			autoToggle: expanded === "auto",
			allowExpand: true
		};
	}
	componentDidMount() {
		const { canvas, workspace: { model } } = this.props;
		const { renderingState } = canvas;
		this.listener.listen(model.events, "changeCells", this.scheduleRedraw);
		this.listener.listen(model.events, "elementEvent", this.scheduleRedraw);
		this.listener.listen(canvas.events, "pointerMove", this.scheduleRedraw);
		this.listener.listen(canvas.events, "scroll", this.scheduleRedraw);
		this.listener.listen(canvas.events, "resize", () => {
			this.delayedSizeCheck.call(this.onCheckSize);
		});
		this.listener.listen(renderingState.shared.events, "changeHighlight", this.scheduleRedraw);
		this.listener.listen(renderingState.events, "changeElementSize", this.scheduleRedraw);
		this.onCheckSize();
	}
	shouldComponentUpdate(nextProps, nextState) {
		return !(nextProps.colorScheme === this.props.colorScheme && nextProps.dock === this.props.dock && nextProps.dockOffsetX === this.props.dockOffsetX && nextProps.dockOffsetY === this.props.dockOffsetY && nextProps.width === this.props.width && nextProps.height === this.props.height && nextState === this.state);
	}
	componentDidUpdate(prevProps) {
		if (this.props.colorScheme !== prevProps.colorScheme) this.scheduleRedraw();
	}
	componentWillUnmount() {
		this.delayedRedraw.dispose();
		this.delayedSizeCheck.dispose();
		this.listener.stopListening();
		this.stopDragViewport();
	}
	scheduleRedraw = () => {
		if (this.state.expanded) this.delayedRedraw.call(this.draw);
	};
	draw = () => {
		const { canvas, workspace: { model }, width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT } = this.props;
		const pt = canvas.metrics.transform;
		this.calculateTransform(pt);
		const style = computeDrawStyle(this.props, this.canvas);
		const ctx = this.canvas.getContext("2d");
		ctx.fillStyle = style.backgroundFill;
		ctx.clearRect(0, 0, width, height);
		ctx.fillRect(0, 0, width, height);
		if (model.elements.length === 0) return;
		const paneStart = paneTopLeft(pt);
		const paneEnd = Vector.add(paneStart, totalPaneSize(pt));
		const start = canvasFromPaneCoords(paneStart, pt, this.transform);
		const end = canvasFromPaneCoords(paneEnd, pt, this.transform);
		ctx.fillStyle = style.scrollablePaneFill;
		ctx.fillRect(start.x, start.y, end.x - start.x, end.y - start.y);
		ctx.save();
		const { clientWidth, clientHeight } = canvas.metrics.pane;
		const viewportStart = canvas.metrics.clientToScrollablePaneCoords(0, 0);
		const viewportEnd = canvas.metrics.clientToScrollablePaneCoords(clientWidth, clientHeight);
		const { x: x0, y: y0 } = canvasFromPaneCoords(viewportStart, pt, this.transform);
		const { x: x1, y: y1 } = canvasFromPaneCoords(viewportEnd, pt, this.transform);
		const viewportRect = {
			x: x0,
			y: y0,
			width: x1 - x0,
			height: y1 - y0
		};
		this.fillViewport(ctx, viewportRect, style);
		this.drawElements(ctx, pt, style);
		this.strokeViewport(ctx, viewportRect, style);
		ctx.restore();
	};
	drawElements(ctx, pt, style) {
		const { canvas, workspace: { model } } = this.props;
		for (const element of model.elements) {
			const { type, bounds: { x, y, width, height } } = canvas.renderingState.getElementShape(element);
			ctx.beginPath();
			ctx.fillStyle = this.chooseElementColor(element, style);
			const { x: x1, y: y1 } = canvasFromPaperCoords({
				x,
				y
			}, pt, this.transform);
			const { x: x2, y: y2 } = canvasFromPaperCoords({
				x: x + width,
				y: y + height
			}, pt, this.transform);
			switch (type) {
				case "ellipse":
					ctx.ellipse((x1 + x2) / 2, (y1 + y2) / 2, (x2 - x1) / 2, (y2 - y1) / 2, 0, Math.PI * 2, 0);
					break;
				default:
					ctx.rect(x1, y1, x2 - x1, y2 - y1);
					break;
			}
			ctx.fill();
		}
	}
	chooseElementColor(element, style) {
		const { canvas, workspace: { getElementStyle } } = this.props;
		const { highlighter } = canvas.renderingState.shared;
		if (highlighter && !highlighter(element)) return "lightgray";
		const { color } = getElementStyle(element);
		return color;
	}
	fillViewport(ctx, viewportRect, style) {
		ctx.fillStyle = style.viewportFill;
		ctx.fillRect(viewportRect.x, viewportRect.y, viewportRect.width, viewportRect.height);
	}
	strokeViewport(ctx, viewportRect, style) {
		const { width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT } = this.props;
		const { x: x1, y: y1 } = viewportRect;
		const x2 = x1 + viewportRect.width;
		const y2 = y1 + viewportRect.height;
		setCanvasStroke(ctx, style.viewportStroke);
		ctx.strokeRect(x1, y1, x2 - x1, y2 - y1);
		ctx.beginPath();
		if (x1 < 0) {
			const startX = ctx.lineWidth;
			ctx.moveTo(startX, y1);
			ctx.lineTo(startX, y2);
		}
		if (y1 < 0) {
			const startY = ctx.lineWidth;
			ctx.moveTo(x1, startY);
			ctx.lineTo(x2, startY);
		}
		if (x2 > width) {
			const endX = width - ctx.lineWidth;
			ctx.moveTo(endX, y1);
			ctx.lineTo(endX, y2);
		}
		if (y2 > height) {
			const endY = height - ctx.lineWidth;
			ctx.moveTo(x1, endY);
			ctx.lineTo(x2, endY);
		}
		setCanvasStroke(ctx, style.overflowStroke);
		ctx.stroke();
	}
	calculateTransform(pt) {
		const { canvas, workspace: { model }, width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT, scalePadding = DEFAULT_SCALE_PADDING } = this.props;
		const box = getContentFittingBox(model.elements, model.links, canvas.renderingState);
		const displayPadding = {
			x: Math.max(box.width, width / MIN_SCALE) * scalePadding,
			y: Math.max(box.height, height / MIN_SCALE) * scalePadding
		};
		const displayStart = paneFromPaperCoords({
			x: box.x - displayPadding.x,
			y: box.y - displayPadding.y
		}, pt);
		const displayEnd = paneFromPaperCoords({
			x: box.x + box.width + displayPadding.x,
			y: box.y + box.height + displayPadding.y
		}, pt);
		const displaySize = {
			x: displayEnd.x - displayStart.x,
			y: displayEnd.y - displayStart.y
		};
		const scale = Math.min(width / displaySize.x, height / displaySize.y);
		const canvasOffset = {
			x: (width - displaySize.x * scale) / 2,
			y: (height - displaySize.y * scale) / 2
		};
		this.transform = {
			scale,
			canvasOffset,
			paneOffset: displayStart
		};
	}
	canvasFromPageCoords(pageX, pageY) {
		const { top, left } = this.canvas.getBoundingClientRect();
		return {
			x: pageX - left - window.scrollX,
			y: pageY - top - window.scrollY
		};
	}
	onCheckSize = () => {
		const { canvas, autoCollapseFraction = DEFAULT_AUTO_COLLAPSE_FRACTION, width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT } = this.props;
		const { expanded, autoToggle, allowExpand } = this.state;
		const { clientWidth, clientHeight } = canvas.metrics.pane;
		const strictExpanded = width < clientWidth * MAX_SIZE_FRACTION && height < clientHeight * MAX_SIZE_FRACTION;
		const autoExpanded = strictExpanded && width < clientWidth * autoCollapseFraction && height < clientHeight * autoCollapseFraction;
		if (autoToggle && expanded !== autoExpanded) this.setState({
			expanded: autoExpanded,
			allowExpand: strictExpanded
		}, () => {
			this.scheduleRedraw();
			this.setInitialized();
		});
		else if (strictExpanded !== allowExpand) this.setState({ allowExpand: strictExpanded }, () => {
			this.setInitialized();
		});
		else this.setInitialized();
	};
	async setInitialized() {
		await Promise.resolve();
		this.setState({ initialized: true });
	}
	render() {
		const { dock = "se", dockOffsetX, dockOffsetY, width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT, translation: t } = this.props;
		const { initialized, expanded, allowExpand } = this.state;
		const expandedWhenAllowed = expanded && allowExpand;
		return /* @__PURE__ */ jsx(ViewportDock, {
			dock,
			dockOffsetX,
			dockOffsetY,
			children: /* @__PURE__ */ jsxs("div", {
				className: cx(CLASS_NAME$11, initialized ? `${CLASS_NAME$11}--animated` : void 0, `${CLASS_NAME$11}--${expandedWhenAllowed ? "expanded" : "collapsed"}`),
				style: expandedWhenAllowed ? {
					width,
					height
				} : void 0,
				children: [/* @__PURE__ */ jsx("canvas", {
					ref: this.onCanvasMount,
					width,
					height,
					onMouseDown: (e) => {
						this.startDragViewport();
						this.onDragViewport(e);
					},
					onWheel: this.onWheel
				}), /* @__PURE__ */ jsx("button", {
					type: "button",
					className: `${CLASS_NAME$11}__toggle`,
					title: expanded ? t.text("navigator.toggle_collapse.title") : t.text("navigator.toggle_expand.title"),
					disabled: !allowExpand,
					onClick: this.onToggleClick,
					children: /* @__PURE__ */ jsx("div", { className: `${CLASS_NAME$11}__toggle-icon` })
				})]
			})
		});
	}
	onCanvasMount = (canvas) => {
		this.canvas = canvas;
	};
	startDragViewport() {
		if (!this.isDraggingViewport) {
			this.isDraggingViewport = true;
			document.addEventListener("mousemove", this.onDragViewport);
			document.addEventListener("mouseup", this.onMouseUp);
		}
	}
	stopDragViewport() {
		if (this.isDraggingViewport) {
			this.isDraggingViewport = false;
			document.removeEventListener("mousemove", this.onDragViewport);
			document.removeEventListener("mouseup", this.onMouseUp);
		}
	}
	onDragViewport = (e) => {
		e.preventDefault();
		if (this.isDraggingViewport) {
			const { canvas } = this.props;
			const canvasCoords = this.canvasFromPageCoords(e.pageX, e.pageY);
			const paperTransform = canvas.metrics.transform;
			const paperCoords = paperFromCanvasCoords(canvasCoords, paperTransform, this.transform);
			canvas.centerTo(paperCoords);
		}
	};
	onMouseUp = () => {
		this.stopDragViewport();
	};
	onWheel = (e) => {
		e.preventDefault();
		const { canvas } = this.props;
		const delta = Math.max(-1, Math.min(1, e.deltaY || e.deltaX));
		canvas.zoomBy(-delta * .1);
	};
	onToggleClick = () => {
		this.setState((state) => ({
			expanded: !state.expanded,
			autoToggle: false
		}), this.scheduleRedraw);
	};
};
function computeDrawStyle(props, styleSource) {
	const { backgroundFill, scrollablePaneFill, viewportFill, viewportStroke, overflowStroke } = props;
	const computedStyle = getComputedStyle(styleSource);
	return {
		backgroundFill: backgroundFill ?? computedStyle.getPropertyValue("--reactodia-navigator-background-fill"),
		scrollablePaneFill: scrollablePaneFill ?? computedStyle.getPropertyValue("--reactodia-navigator-scrollable-pane-fill"),
		viewportFill: viewportFill ?? computedStyle.getPropertyValue("--reactodia-navigator-viewport-fill"),
		viewportStroke: viewportStroke ?? parseStrokeFromPropertyValues(computedStyle.getPropertyValue("--reactodia-navigator-viewport-stroke-color"), computedStyle.getPropertyValue("--reactodia-navigator-viewport-stroke-width"), computedStyle.getPropertyValue("--reactodia-navigator-viewport-stroke-dash")),
		overflowStroke: overflowStroke ?? parseStrokeFromPropertyValues(computedStyle.getPropertyValue("--reactodia-navigator-overflow-stroke-color"), computedStyle.getPropertyValue("--reactodia-navigator-overflow-stroke-width"), computedStyle.getPropertyValue("--reactodia-navigator-overflow-stroke-dash"))
	};
}
function parseStrokeFromPropertyValues(color, width, dash) {
	const widthNumber = Number(width);
	const dashArray = dash.split(" ").map((v) => Number(v));
	return {
		color: color ? color : void 0,
		width: Number.isFinite(widthNumber) ? widthNumber : void 0,
		dash: dashArray.every((v) => Number.isFinite(v)) ? dashArray : void 0
	};
}
function setCanvasStroke(canvas, stroke) {
	const { color = "transparent", width = 1, dash = [] } = stroke;
	canvas.strokeStyle = color;
	canvas.lineWidth = width;
	canvas.setLineDash(dash);
}
function canvasFromPaneCoords(pane, pt, nt) {
	return {
		x: nt.canvasOffset.x + (pane.x - nt.paneOffset.x) * nt.scale,
		y: nt.canvasOffset.y + (pane.y - nt.paneOffset.y) * nt.scale
	};
}
function canvasFromPaperCoords(paper, pt, nt) {
	return canvasFromPaneCoords(paneFromPaperCoords(paper, pt), pt, nt);
}
function paperFromCanvasCoords(canvas, pt, nt) {
	return paperFromPaneCoords({
		x: nt.paneOffset.x + (canvas.x - nt.canvasOffset.x) / nt.scale,
		y: nt.paneOffset.y + (canvas.y - nt.canvasOffset.y) / nt.scale
	}, pt);
}
//#endregion
//#region src/widgets/selection.tsx
var CLASS_NAME$10 = "reactodia-selection";
/**
* Canvas widget component for rectangular element selection on the diagram.
*
* @category Components
*/
function Selection(props) {
	const { hotkeySelectAll } = props;
	const { model, canvas } = useCanvas();
	const selectedElements = useSyncStoreWithComparator(useEventStore(model.events, "changeSelection"), () => model.selection.filter((cell) => cell instanceof Element), shallowArrayEqual);
	const [highlightedBox, setHighlightedBox] = React.useState();
	React.useEffect(() => {
		let origin;
		const listener = new EventObserver();
		listener.listen(canvas.events, "pointerDown", (e) => {
			const requireShift = e.source.pointerMode !== "selection";
			const allowSelection = e.sourceEvent.shiftKey === requireShift;
			if (!e.target && !e.panning && allowSelection) {
				e.sourceEvent.preventDefault();
				const { pageX, pageY } = e.sourceEvent;
				origin = {
					pageX,
					pageY
				};
				setHighlightedBox(makePaperBox(origin, origin, canvas.metrics));
				listenMove();
			}
		});
		let moveListener;
		const listenMove = () => {
			moveListener?.stopListening();
			moveListener = new EventObserver();
			moveListener.listen(canvas.events, "pointerMove", (e) => {
				const { pageX, pageY } = e.sourceEvent;
				if (origin) setHighlightedBox(makePaperBox(origin, {
					pageX,
					pageY
				}, canvas.metrics));
			});
		};
		listener.listen(canvas.events, "pointerUp", (e) => {
			const { pageX, pageY } = e.sourceEvent;
			moveListener?.stopListening();
			setHighlightedBox(void 0);
			if (e.triggerAsClick) {
				const requireShift = e.source.pointerMode !== "selection";
				if ((e.sourceEvent.shiftKey === requireShift || model.selection.length > 1) && e.target instanceof Element) {
					e.sourceEvent.preventDefault();
					toggleSelected(e.target, model);
				}
			} else if (origin) applySelection(makePaperBox(origin, {
				pageX,
				pageY
			}, canvas.metrics), model, canvas);
			origin = void 0;
		});
		return () => {
			listener.stopListening();
			moveListener?.stopListening();
		};
	}, []);
	useCanvasHotkey(hotkeySelectAll === void 0 ? "Mod+A" : hotkeySelectAll, () => {
		model.setSelection(model.elements);
	});
	if (highlightedBox || selectedElements.length > 1) return /* @__PURE__ */ jsx(CanvasPlaceAt, {
		layer: "overElements",
		children: /* @__PURE__ */ jsx(SelectionBox, {
			...props,
			model,
			canvas,
			selectedElements,
			highlightedBox
		})
	});
	else return null;
}
function makePaperBox(start, end, metrics) {
	const { x: x0, y: y0 } = metrics.pageToPaperCoords(start.pageX, start.pageY);
	const { x: x1, y: y1 } = metrics.pageToPaperCoords(end.pageX, end.pageY);
	return {
		x: Math.min(x0, x1),
		y: Math.min(y0, y1),
		width: Math.abs(x1 - x0),
		height: Math.abs(y1 - y0)
	};
}
function* findUnselectedElements(elements, selectionBox, alreadySelected, sizeProvider) {
	for (const element of elements) {
		const elementRect = boundsOf(element, sizeProvider);
		if (!alreadySelected.has(element) && Rect.intersects(selectionBox, elementRect)) yield element;
	}
}
function toggleSelected(element, model) {
	const { selection } = model;
	const nextSelection = selection.includes(element) ? selection.filter((item) => item != element) : [...selection, element];
	model.setSelection(nextSelection);
}
function applySelection(selectionBox, model, canvas) {
	const selection = new Set(model.selection);
	const newlySelected = Array.from(findUnselectedElements(model.elements, selectionBox, selection, canvas.renderingState));
	if (newlySelected.length > 0) model.setSelection([...model.selection, ...newlySelected]);
}
function SelectionBox(props) {
	const { model, canvas, selectedElements, highlightedBox, boxMargin = 5, itemMargin = 2, children } = props;
	useSyncStore(useEventStore(canvas.events, "changeTransform"), () => canvas.metrics.transform);
	const selectedBoxStyle = positionStyleForPaperRect(useSyncStoreWithComparator(useLayerDebouncedStore(useElementBoundsStore(model, canvas, selectedElements), canvas.renderingState), () => getContentFittingBox(selectedElements, [], canvas.renderingState), Rect.equals), boxMargin, canvas.metrics);
	const highlightedElements = highlightedBox ? Array.from(findUnselectedElements(model.elements, highlightedBox, new Set(selectedElements), canvas.renderingState)) : [];
	const moveControllerRef = React.useRef(void 0);
	if (!moveControllerRef.current) moveControllerRef.current = new StatefulMoveController(canvas, model);
	React.useEffect(() => {
		return () => {
			moveControllerRef.current?.onPointerUp();
		};
	}, []);
	const moveController = moveControllerRef.current;
	moveController.setElements(selectedElements);
	return /* @__PURE__ */ jsxs("div", {
		className: CLASS_NAME$10,
		children: [
			selectedElements.length > 1 ? /* @__PURE__ */ jsxs(Fragment, { children: [
				selectedElements.map((element) => /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$10}__selectedItem`,
					style: positionStyleForPaperRect(boundsOf(element, canvas.renderingState), itemMargin, canvas.metrics)
				}, element.id)),
				/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$10}__selectedActions`,
					style: selectedBoxStyle,
					children: children ?? /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx(SelectionActionZoomToFit, {
							dock: "nw",
							dockColumn: 1
						}),
						/* @__PURE__ */ jsx(SelectionActionLayout, {
							dock: "nw",
							dockColumn: 2
						}),
						/* @__PURE__ */ jsx(SelectionActionGroup, {
							dock: "nw",
							dockColumn: 3
						}),
						/* @__PURE__ */ jsx(SelectionActionRemove, { dock: "ne" }),
						/* @__PURE__ */ jsx(SelectionActionConnections, { dock: "e" }),
						/* @__PURE__ */ jsx(SelectionActionExpand, { dock: "s" }),
						/* @__PURE__ */ jsx(SelectionActionAnnotate, {
							dock: "se",
							dockColumn: 1
						})
					] })
				}),
				/* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$10}__selectedBox`,
					style: selectedBoxStyle,
					onClick: moveController.onClick,
					onPointerDown: moveController.onPointerDown,
					onPointerMove: moveController.onPointerMove,
					onPointerUp: moveController.onPointerUp,
					onPointerCancel: moveController.onPointerUp
				})
			] }) : null,
			highlightedBox ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$10}__highlightedBox`,
				style: positionStyleForPaperRect(highlightedBox, 0, canvas.metrics)
			}) : null,
			highlightedElements.map((element) => /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$10}__highlightedItem`,
				style: positionStyleForPaperRect(boundsOf(element, canvas.renderingState), itemMargin, canvas.metrics)
			}, element.id))
		]
	});
}
function useElementBoundsStore(model, canvas, elements) {
	return React.useCallback((onChange) => {
		if (elements.length === 0) return () => {};
		const elementSet = new Set(elements);
		const listener = new EventObserver();
		listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changePosition && elementSet.has(data.changePosition.source)) onChange();
		});
		listener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
			if (elementSet.has(e.source)) onChange();
		});
		return () => listener.stopListening();
	}, [
		model.events,
		canvas.renderingState.events,
		elements
	]);
}
function positionStyleForPaperRect(paperRect, margin, metrics) {
	const { x: x0, y: y0 } = metrics.paperToScrollablePaneCoords(paperRect.x, paperRect.y);
	const { x: x1, y: y1 } = metrics.paperToScrollablePaneCoords(paperRect.x + paperRect.width, paperRect.y + paperRect.height);
	return {
		left: x0 - margin,
		top: y0 - margin,
		width: x1 - x0 + margin * 2,
		height: y1 - y0 + margin * 2
	};
}
var StatefulMoveController = class {
	canvas;
	model;
	elements = [];
	moveState;
	constructor(canvas, model) {
		this.canvas = canvas;
		this.model = model;
	}
	setElements(elements) {
		this.elements = elements;
	}
	onClick = (e) => {
		if (this.isToggleSelectionEvent(e)) {
			e.preventDefault();
			const point = this.canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			const element = findElementAtPoint(this.model.elements, point, this.canvas.renderingState);
			if (element) toggleSelected(element, this.model);
		}
	};
	onPointerDown = (e) => {
		const { canvas, model, elements } = this;
		if (this.isToggleSelectionEvent(e)) return;
		e.preventDefault();
		e.currentTarget.setPointerCapture(e.pointerId);
		const origin = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
		const elementIds = /* @__PURE__ */ new Set();
		for (const element of elements) elementIds.add(element.id);
		const positions = /* @__PURE__ */ new Map();
		const links = /* @__PURE__ */ new Map();
		for (const element of elements) {
			positions.set(element, element.position);
			for (const link of model.getElementLinks(element)) if (link.vertices.length > 0 && link.sourceId === element.id && elementIds.has(link.targetId)) links.set(link, link.vertices);
		}
		this.moveState = {
			origin,
			positions,
			links,
			command: RestoreGeometry.capturePartial(elements, Array.from(links.keys()))
		};
		canvas.focus();
	};
	isToggleSelectionEvent(e) {
		return e.shiftKey;
	}
	onPointerMove = (e) => {
		const { canvas, moveState } = this;
		if (moveState) {
			e.preventDefault();
			const { x, y } = canvas.metrics.pageToPaperCoords(e.pageX, e.pageY);
			const dx = x - moveState.origin.x;
			const dy = y - moveState.origin.y;
			for (const [element, position] of moveState.positions) element.setPosition({
				x: position.x + dx,
				y: position.y + dy
			});
			for (const [link, vertices] of moveState.links) link.setVertices(vertices.map((p) => ({
				x: p.x + dx,
				y: p.y + dy
			})));
			canvas.renderingState.syncUpdate();
		}
	};
	onPointerUp = (e) => {
		if (this.moveState) {
			e?.preventDefault();
			if (this.moveState.command.hasChanges()) this.model.history.registerToUndo(this.moveState.command);
			this.moveState = void 0;
		}
	};
};
//#endregion
//#region src/widgets/toolbar.tsx
var CLASS_NAME$9 = "reactodia-toolbar";
/**
* Canvas widget component to display a simple toolbar with a dropdown menu.
*
* @category Components
*/
function Toolbar(props) {
	const { dock, dockOffsetX, dockOffsetY, menu, children } = props;
	const t = useTranslation();
	const dropUp = dock === "s" || dock === "sw" || dock === "se";
	return /* @__PURE__ */ jsx(ViewportDock, {
		dock,
		dockOffsetX,
		dockOffsetY,
		children: /* @__PURE__ */ jsxs("div", {
			className: cx(CLASS_NAME$9, dropUp ? `${CLASS_NAME$9}--drop-up` : void 0),
			role: "toolbar",
			children: [menu ? /* @__PURE__ */ jsx(DropdownMenu, {
				className: `${CLASS_NAME$9}__menu`,
				direction: dropUp ? "up" : "down",
				title: t.text("toolbar.menu_toggle.title"),
				children: menu
			}) : null, children ? /* @__PURE__ */ jsx("div", {
				className: cx(`${CLASS_NAME$9}__quick-access-group`, "reactodia-btn-group", "reactodia-btn-group-sm"),
				children
			}) : null]
		})
	});
}
//#endregion
//#region src/widgets/toolbarAction.tsx
var CLASS_NAME$8 = "reactodia-toolbar-action";
/**
* Base component to display an action on the {@link Toolbar} itself or
* in the dropdown menu in a generic way.
*
* @category Components
*/
function ToolbarAction(props) {
	const { className, title, disabled, hotkey, onSelect, children } = props;
	const insideDropdown = useInsideDropdown();
	const actionKey = useCanvasHotkey(hotkey, onSelect);
	const titleWithHotkey = title && actionKey ? `${title} (${actionKey.text})` : title;
	return insideDropdown ? /* @__PURE__ */ jsxs(DropdownMenuItem, {
		className,
		title: titleWithHotkey,
		"aria-label": title,
		"aria-keyshortcuts": actionKey?.text,
		disabled,
		onSelect,
		children: [children, actionKey ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("span", { className: `${CLASS_NAME$8}__spacer` }), /* @__PURE__ */ jsx("span", {
			className: `${CLASS_NAME$8}__hotkey`,
			children: actionKey.text
		})] }) : null]
	}) : /* @__PURE__ */ jsx("button", {
		type: "button",
		className: cx(className, "reactodia-btn reactodia-btn-default"),
		title: titleWithHotkey,
		"aria-label": title,
		"aria-keyshortcuts": actionKey?.text,
		disabled,
		onClick: onSelect,
		children
	});
}
/**
* Toolbar action component to open a file selection dialog.
*
* @category Components
*/
function ToolbarActionOpen(props) {
	const { className, fileAccept, onSelect, children, ...otherProps } = props;
	const inputRef = React.useRef(null);
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__open`),
		onSelect: () => {
			inputRef.current?.click();
		},
		children
	}), /* @__PURE__ */ jsx("input", {
		ref: inputRef,
		role: "none",
		type: "file",
		className: `${CLASS_NAME$8}__open-input`,
		accept: fileAccept,
		onChange: (e) => {
			if (e.currentTarget.files) {
				const file = e.currentTarget.files[0];
				onSelect(file);
			}
		}
	})] });
}
/**
* Toolbar action component to save diagram layout state or apply authored changed.
*
* @category Components
*/
function ToolbarActionSave(props) {
	const { className, title, mode = "any", onSelect, children, ...otherProps } = props;
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const hasLayoutChanges = useObservedProperty(model.history.events, "historyChanged", () => model.history.undoStack.length > 0);
	const canPersistChanges = useObservedProperty(editor.events, "changeAuthoringState", () => !AuthoringState.isEmpty(editor.authoringState));
	let enabled = true;
	let defaultTitle;
	if (mode === "layout") {
		enabled = hasLayoutChanges && !canPersistChanges;
		defaultTitle = t.text("toolbar_action.save_layout.title");
	} else if (mode === "authoring") {
		enabled = canPersistChanges;
		defaultTitle = t.text("toolbar_action.save_authoring.title");
	} else enabled = hasLayoutChanges || canPersistChanges;
	return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__save`),
		disabled: !enabled,
		onSelect,
		title: title ?? defaultTitle,
		children
	});
}
/**
* Toolbar action component to clear diagram content.
*
* Clearing the diagram adds a command to the command history.
*
* @category Components
*/
function ToolbarActionClearAll(props) {
	const { className, title, ...otherProps } = props;
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__clear-all`),
		title: title ?? t.text("toolbar_action.clear_all.title"),
		onSelect: () => {
			const batch = model.history.startBatch(TranslatedText.text("toolbar_action.clear_all.command"));
			editor.removeItems([...model.elements]);
			batch.store();
		},
		children: t.text("toolbar_action.clear_all.label")
	});
}
/**
* Toolbar action component to export the diagram into a file, or print it.
*
* @category Components
*/
function ToolbarActionExport(props) {
	const { className, title, kind, fileName = "diagram", rasterOptions, children, ...otherProps } = props;
	const { canvas } = useCanvas();
	const t = useTranslation();
	if (kind === "exportRaster") return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__export-image`),
		title: title ?? t.text("toolbar_action.export_raster.title"),
		onSelect: async () => {
			const exportOptions = rasterOptions ?? { backgroundColor: "white" };
			saveAs(dataURLToBlob(await canvas.exportRaster(exportOptions)), `${fileName}.png`);
		},
		children: children ?? t.text("toolbar_action.export_raster.label")
	});
	else if (kind === "exportSvg") return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__export-image`),
		title: title ?? t.text("toolbar_action.export_svg.title"),
		onSelect: async () => {
			const svg = await canvas.exportSvg({ addXmlHeader: true });
			saveAs(new Blob([svg], { type: "image/svg+xml" }), `${fileName}.svg`);
		},
		children: children ?? t.text("toolbar_action.export_svg.label")
	});
	else if (kind === "print") return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__print`),
		title: title ?? t.text("toolbar_action.export_print.title"),
		onSelect: async () => {
			const printWindow = window.open("", void 0, "width=1280,height=720");
			if (!printWindow) {
				console.error("Reactodia: failed to open print preview window");
				return;
			}
			const svg = await canvas.exportSvg();
			printWindow.document.write(svg);
			printWindow.document.close();
			printWindow.print();
		},
		children: children ?? t.text("toolbar_action.export_print.label")
	});
	else return null;
}
/**
* Toolbar action component to undo a command from the command history.
*
* @category Components
*/
function ToolbarActionUndo(props) {
	const { className, title, hotkey, ...otherProps } = props;
	const { model: { history } } = useCanvas();
	const t = useTranslation();
	const insideDropdown = useInsideDropdown();
	const undoCommand = useObservedProperty(history.events, "historyChanged", () => {
		const { undoStack } = history;
		return undoStack.length === 0 ? void 0 : undoStack[undoStack.length - 1];
	});
	const commandTitle = !title && undoCommand ? resolveCommandTitle(undoCommand, t) : void 0;
	return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__undo`),
		disabled: !undoCommand,
		title: title ?? (commandTitle === void 0 ? t.text("toolbar_action.undo.title") : t.text("toolbar_action.undo.title_named", { command: commandTitle })),
		hotkey: hotkey === void 0 ? "Mod+Z" : hotkey,
		onSelect: () => history.undo(),
		children: insideDropdown ? t.text("toolbar_action.undo.label") : null
	});
}
/**
* Toolbar action component to redo a command from the command history.
*
* @category Components
*/
function ToolbarActionRedo(props) {
	const { className, title, hotkey, ...otherProps } = props;
	const { model: { history } } = useCanvas();
	const t = useTranslation();
	const insideDropdown = useInsideDropdown();
	const redoCommand = useObservedProperty(history.events, "historyChanged", () => {
		const { redoStack } = history;
		return history.redoStack.length === 0 ? void 0 : redoStack[redoStack.length - 1];
	});
	const commandTitle = !title && redoCommand ? resolveCommandTitle(redoCommand, t) : void 0;
	return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__redo`),
		disabled: !redoCommand,
		title: title ?? (commandTitle === void 0 ? t.text("toolbar_action.redo.title") : t.text("toolbar_action.redo.title_named", { command: commandTitle })),
		hotkey: hotkey === void 0 ? "Mod+Shift+Z" : hotkey,
		onSelect: () => history.redo(),
		children: insideDropdown ? t.text("toolbar_action.redo.label") : null
	});
}
function resolveCommandTitle(command, t) {
	if (typeof command.title === "string" || typeof command.title === "undefined") return command.title;
	else return command.title.resolve(t);
}
/**
* Toolbar action component to perform graph layout algorithm on the diagram content.
*
* Applying the layout adds a command to the command history.
*
* @category Components
*/
function ToolbarActionLayout(props) {
	const { className, title, ...otherProps } = props;
	const { model, canvas } = useCanvas();
	const { performLayout } = useWorkspace();
	const t = useTranslation();
	const diagramIsEmpty = useObservedProperty(model.events, "changeCells", () => model.elements.length === 0);
	return /* @__PURE__ */ jsx(ToolbarAction, {
		...otherProps,
		className: cx(className, `${CLASS_NAME$8}__layout`),
		title: title ?? t.text("toolbar_action.layout.title"),
		disabled: diagramIsEmpty,
		onSelect: async () => {
			await performLayout({
				canvas,
				animate: true
			});
		},
		children: t.text("toolbar_action.layout.label")
	});
}
/**
* Toolbar component to select a data language for the workspace.
*
* @category Components
*/
function ToolbarLanguageSelector(props) {
	const { className, title, languages } = props;
	const { model } = useCanvas();
	const t = useTranslation();
	const currentLanguage = useObservedProperty(model.events, "changeLanguage", () => model.language);
	return languages.length === 0 ? null : /* @__PURE__ */ jsxs("div", {
		className: cx(className, `${CLASS_NAME$8}__language-selector`),
		title: title ?? t.text("toolbar_action.language_selector.title"),
		children: [/* @__PURE__ */ jsx("label", { htmlFor: "reactodia-language-selector" }), /* @__PURE__ */ jsx("select", {
			id: "reactodia-language-selector",
			value: currentLanguage,
			onChange: (e) => model.setLanguage(e.currentTarget.value),
			children: languages.map(({ code, label }) => /* @__PURE__ */ jsx("option", {
				value: code,
				children: label
			}, code))
		})]
	});
}
//#endregion
//#region src/widgets/unifiedSearch/searchSection.tsx
var UnifiedSearchSectionContext = React.createContext(null);
/**
* React hook to get provided search context for the current section.
*
* Throws an error if called from a non-mounted or non-section component.
* 
* @category Hooks
*/
function useUnifiedSearchSection(props = {}) {
	const context = React.useContext(UnifiedSearchSectionContext);
	if (!context) throw new Error("Missing Reactodia unified search section context");
	const { searchStore, isSectionActive, setSectionActive } = context;
	const [shouldRender, setShouldRender] = React.useState(isSectionActive);
	const { searchTimeout = 0, allowSubmit } = props;
	const derivedSearchState = useSearchInputStore({
		initialValue: searchStore.value,
		submitTimeout: searchTimeout,
		allowSubmit
	});
	React.useLayoutEffect(() => {
		if (isSectionActive) {
			setShouldRender(true);
			const listener = new EventObserver();
			listener.listen(searchStore.events, "changeValue", ({ source, action }) => {
				derivedSearchState.change({
					value: source.value,
					action
				});
			});
			derivedSearchState.change({
				value: searchStore.value,
				action: "submit"
			});
			return () => listener.stopListening();
		}
	}, [isSectionActive]);
	React.useEffect(() => {
		if (shouldRender) derivedSearchState.change({
			value: searchStore.value,
			action: "submit"
		});
	}, [shouldRender]);
	return {
		shouldRender,
		isSectionActive,
		setSectionActive,
		searchStore: derivedSearchState
	};
}
//#endregion
//#region src/widgets/unifiedSearch/builtinSearchSections.tsx
var SECTION_ELEMENT_TYPES_CLASS = "reactodia-search-section-element-types";
/**
* Search section (provider) to lookup element types via tree.
*
* @category Components
*/
function SearchSectionElementTypes(props) {
	const { searchTimeout = 200, minSearchTermLength = 2, draggableItems, placeCreatedEntity } = props;
	const { searchStore, shouldRender } = useUnifiedSearchSection({
		searchTimeout,
		allowSubmit: (term) => term.length >= minSearchTermLength
	});
	return shouldRender ? /* @__PURE__ */ jsx(ClassTree, {
		className: SECTION_ELEMENT_TYPES_CLASS,
		searchStore,
		draggableItems,
		placeCreatedEntity
	}) : null;
}
var SECTION_ENTITIES_CLASS = "reactodia-search-section-entities";
/**
* Search section (provider) to lookup entities.
*
* @category Components
*/
function SearchSectionEntities(props) {
	const { searchTimeout = 600, minSearchTermLength = 3 } = props;
	const { getCommandBus } = useWorkspace();
	const { shouldRender, setSectionActive, searchStore } = useUnifiedSearchSection({
		searchTimeout,
		allowSubmit: (term) => term.length >= minSearchTermLength
	});
	const commands = getCommandBus(InstancesSearchTopic);
	React.useEffect(() => {
		const listener = new EventObserver();
		listener.listen(commands, "setCriteria", ({ criteria }) => {
			setSectionActive(true, criteriaAsSearchExtra(criteria));
		});
		return () => listener.stopListening();
	}, [shouldRender, commands]);
	return /* @__PURE__ */ jsx(InstancesSearch, {
		className: SECTION_ENTITIES_CLASS,
		searchStore,
		onChangeCriteria: (criteria) => {
			if (shouldRender) setSectionActive(true, criteriaAsSearchExtra(criteria));
		},
		onAddElements: () => {
			setSectionActive(false);
		}
	});
}
function criteriaAsSearchExtra(criteria) {
	if (criteria.text || criteria.elementType || criteria.refElement) return criteria;
	else return;
}
var SECTION_LINK_TYPES_CLASS = "reactodia-search-section-link-types";
/**
* Search section (provider) to lookup link types.
*
* @category Components
*/
function SearchSectionLinkTypes(props) {
	const { searchTimeout = 200, minSearchTermLength = 1 } = props;
	const { shouldRender, isSectionActive, searchStore } = useUnifiedSearchSection({
		searchTimeout,
		allowSubmit: (term) => term.length >= minSearchTermLength
	});
	return shouldRender ? /* @__PURE__ */ jsx(LinkTypesToolbox, {
		className: SECTION_LINK_TYPES_CLASS,
		trackSelected: isSectionActive,
		searchStore
	}) : null;
}
//#endregion
//#region src/widgets/unifiedSearch/unifiedSearch.tsx
var CLASS_NAME$7 = "reactodia-unified-search";
/**
* Component to display a search input with a dropdown for results.
*
* One or many available search sections (providers) can be specified via
* {@link UnifiedSearchProps.sections sections} prop.
*
* @category Components
*/
function UnifiedSearch(props) {
	const { sections, direction = "down", defaultWidth = 300, defaultHeight = 350, minWidth = 200, minHeight = 150, offsetWithMaxWidth = 20, offsetWithMaxHeight = 20, hotkeyFocus } = props;
	const [expanded, setExpanded] = React.useState(false);
	const [activeSection, setActiveSection] = React.useState(ActiveSection.empty);
	const findDefaultSectionKey = () => {
		if (sections.some((section) => section.key === activeSection.previousActive)) return activeSection.previousActive;
		else if (sections.length > 0) return sections[0].key;
		else return;
	};
	const searchStore = useSearchInputStore({ initialValue: "" });
	const searchTerm = useObservedProperty(searchStore.events, "changeValue", () => searchStore.value);
	React.useEffect(() => {
		const onClearSearch = () => {
			setActiveSection((previous) => previous.deactivate());
			setExpanded(false);
		};
		searchStore.events.on("clearSearch", onClearSearch);
		return () => searchStore.events.off("clearSearch", onClearSearch);
	}, [
		searchStore,
		setActiveSection,
		setExpanded
	]);
	const [panelSize, setPanelSize] = React.useState({
		width: defaultWidth,
		height: defaultHeight
	});
	const maxSize = React.useRef({
		width: Infinity,
		height: Infinity
	});
	const resizePanel = (size) => {
		const nextSize = {
			width: Math.max(size.width, minWidth),
			height: Math.max(size.height, minHeight)
		};
		if (!(panelSize.width === nextSize.width && panelSize.height === nextSize.height)) setPanelSize(nextSize);
	};
	const updateMaxSize = (nextMaxSize) => {
		maxSize.current = nextMaxSize;
		if (panelSize.width > nextMaxSize.width || panelSize.height > nextMaxSize.height) setPanelSize({ ...panelSize });
	};
	const effectiveSize = {
		width: Math.min(panelSize.width, maxSize.current.width),
		height: Math.min(panelSize.height, maxSize.current.height)
	};
	const sectionsWithContext = React.useMemo(() => sections.map((section) => ({
		...section,
		context: {
			searchStore,
			isSectionActive: section.key === activeSection?.key,
			setSectionActive: (active, searchExtra) => {
				if (active) {
					setActiveSection((previous) => previous.activate(section.key, searchExtra));
					setExpanded(true);
					toggleInputRef.current?.focus();
				} else {
					setActiveSection((previous) => previous.deactivate());
					setExpanded(false);
				}
			}
		}
	})), [
		sections,
		searchStore,
		activeSection,
		setActiveSection,
		setExpanded
	]);
	const { getCommandBus } = useWorkspace();
	const toggleInputRef = React.useRef(null);
	const onFocus = React.useCallback(({ sectionKey }) => {
		if (sectionKey) if (sections.some((section) => section.key === sectionKey)) setActiveSection((previous) => previous.activate(sectionKey));
		else console.warn(`Unified search: cannot activate section that does not exists: ${sectionKey}`);
		toggleInputRef.current?.focus();
	}, [sections, setActiveSection]);
	const commands = getCommandBus(UnifiedSearchTopic);
	React.useEffect(() => {
		commands.on("focus", onFocus);
		return () => commands.off("focus", onFocus);
	}, [commands, onFocus]);
	const actionFocus = useCanvasHotkey(hotkeyFocus, () => onFocus({}));
	const hasSearchQuery = searchTerm.length > 0 || activeSection.getSearchExtra() !== void 0;
	const onClickOutside = React.useCallback(() => {
		if (!hasSearchQuery) setExpanded(false);
	}, [hasSearchQuery, setExpanded]);
	return /* @__PURE__ */ jsx(Dropdown, {
		className: CLASS_NAME$7,
		direction,
		expanded,
		"aria-keyshortcuts": actionFocus?.text,
		onClickOutside,
		toggle: /* @__PURE__ */ jsx(SearchToggle, {
			inputRef: toggleInputRef,
			searchTerm,
			setSearchTerm: (value) => searchStore.change({
				value,
				action: "input"
			}),
			onSubmit: () => {},
			onClear: hasSearchQuery ? () => searchStore.change({
				value: "",
				action: "clear"
			}) : void 0,
			expanded,
			setExpanded: (nextExpanded) => {
				setActiveSection((previous) => {
					if (nextExpanded) {
						if (previous.key === void 0) {
							const defaultSectionKey = findDefaultSectionKey();
							if (defaultSectionKey) return previous.activate(defaultSectionKey);
						}
					} else return previous.deactivate();
					return previous;
				});
				setExpanded(nextExpanded);
			},
			minWidth,
			panelSize: effectiveSize
		}),
		children: /* @__PURE__ */ jsx(SearchContent, {
			visible: expanded,
			sections: sectionsWithContext,
			activeSectionKey: activeSection.key,
			onActivateSection: (key) => setActiveSection((previous) => previous.switch(key)),
			dropUp: direction === "up",
			size: effectiveSize,
			minSize: {
				width: minWidth,
				height: minHeight
			},
			offsetForMaxSize: {
				x: offsetWithMaxWidth,
				y: offsetWithMaxHeight
			},
			onResize: resizePanel,
			setMaxSize: updateMaxSize
		})
	});
}
var ActiveSection = class ActiveSection {
	key;
	previousActive;
	searchExtras;
	static empty = new ActiveSection(void 0, void 0, {});
	constructor(key, previousActive, searchExtras) {
		this.key = key;
		this.previousActive = previousActive;
		this.searchExtras = searchExtras;
	}
	getSearchExtra() {
		const { key, searchExtras } = this;
		if (key !== void 0 && Object.prototype.hasOwnProperty.call(searchExtras, key)) return searchExtras[key];
	}
	activate(key, extra) {
		if (this.key === key && this.getSearchExtra() === extra) return this;
		return new ActiveSection(key, this.key === key ? this.previousActive : this.key, {
			...this.searchExtras,
			[key]: extra
		});
	}
	deactivate() {
		return new ActiveSection(void 0, this.key, this.searchExtras);
	}
	switch(key) {
		if (this.key === key) return this;
		return new ActiveSection(key, this.key, this.searchExtras);
	}
};
function SearchToggle(props) {
	const { inputRef, searchTerm, setSearchTerm, onSubmit, onClear, expanded, setExpanded, minWidth, panelSize } = props;
	const t = useTranslation();
	return /* @__PURE__ */ jsxs("div", {
		className: cx(`${CLASS_NAME$7}__toggle`),
		style: { width: panelSize?.width },
		role: "search",
		children: [/* @__PURE__ */ jsx("input", {
			ref: inputRef,
			role: "searchbox",
			type: "text",
			className: `${CLASS_NAME$7}__search-input`,
			placeholder: t.textOptional("unified_search.input.placeholder") ?? t.text("search_defaults.input.placeholder"),
			name: "reactodia-search",
			value: searchTerm,
			onClick: () => setExpanded(true),
			onFocus: () => setExpanded(true),
			onChange: (e) => setSearchTerm(e.currentTarget.value),
			onKeyUp: (e) => {
				switch (e.key) {
					case "Enter":
						if (searchTerm.length > 0) setExpanded(true);
						onSubmit();
						break;
					case "Escape":
						if (onClear) onClear();
						else setExpanded(false);
						break;
					default:
						setExpanded(true);
						break;
				}
			}
		}), onClear ? /* @__PURE__ */ jsx("button", {
			type: "button",
			title: t.text("unified_search.input_clear.title"),
			className: `${CLASS_NAME$7}__clear-button`,
			onClick: onClear
		}) : expanded ? /* @__PURE__ */ jsx("button", {
			type: "button",
			title: t.text("unified_search.input_collapse.title"),
			className: `${CLASS_NAME$7}__collapse-button`,
			onClick: () => setExpanded(false)
		}) : /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$7}__search-icon`,
			onClick: () => inputRef.current?.focus()
		})]
	});
}
function SearchContent(props) {
	const { visible, sections, activeSectionKey, onActivateSection, dropUp, size, minSize, offsetForMaxSize, onResize, setMaxSize } = props;
	const { canvas } = useCanvas();
	const panelRef = React.useRef(null);
	const panelBounds = React.useRef(void 0);
	const updatePanelBounds = () => {
		if (!panelRef.current) return;
		const { x: rawX, y: rawY, width, height } = panelRef.current.getBoundingClientRect();
		const x = rawX + window.scrollX;
		const y = rawY + window.scrollY;
		const [alignmentX] = getParentDockAlignment(panelRef.current);
		const viewport = canvas.metrics.getViewportPageRect();
		panelBounds.current = {
			bounds: {
				x,
				y,
				width,
				height
			},
			centeredByX: alignmentX === "center",
			centerOffsetX: Math.max(x * 2 + width - viewport.x * 2 - viewport.width, 0)
		};
	};
	useViewportResizeHandler(() => {
		if (!panelBounds.current) updatePanelBounds();
		if (panelBounds.current) {
			const { bounds, centeredByX, centerOffsetX } = panelBounds.current;
			const viewport = canvas.metrics.getViewportPageRect();
			const viewportRight = viewport.x + viewport.width;
			const viewportBottom = viewport.y + viewport.height;
			const proposedWidth = centeredByX ? viewport.width - centerOffsetX - offsetForMaxSize.x : viewportRight - bounds.x - offsetForMaxSize.x;
			const proposedHeight = dropUp ? bounds.y - viewport.y - offsetForMaxSize.y : viewportBottom - bounds.y - offsetForMaxSize.y;
			setMaxSize({
				width: Math.max(proposedWidth, minSize.width),
				height: Math.max(proposedHeight, minSize.height)
			});
		}
	});
	const onDrag = (e, dx, dy) => {
		if (panelBounds.current) {
			const { bounds, centeredByX } = panelBounds.current;
			onResize({
				width: bounds.width + dx * (centeredByX ? 2 : 1),
				height: bounds.height + (dropUp ? -dy : dy)
			});
		}
	};
	const sectionPanelId = (section) => `reactodia-unified-search-panel-${section.key}`;
	return /* @__PURE__ */ jsxs("div", {
		inert: visible ? void 0 : "",
		ref: panelRef,
		className: `${CLASS_NAME$7}__panel`,
		style: {
			width: size.width,
			height: size.height
		},
		children: [
			/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$7}__section-tabs`,
				role: "tablist",
				children: sections.map((section) => /* @__PURE__ */ jsx("button", {
					className: cx(`${CLASS_NAME$7}__section-tab`, "reactodia-btn", "reactodia-btn-default", section.key === activeSectionKey ? "active" : void 0),
					role: "tab",
					"aria-selected": section.key === activeSectionKey,
					"aria-controls": sectionPanelId(section),
					"aria-label": section.title,
					title: section.title,
					onClick: () => onActivateSection(section.key),
					children: section.label
				}, section.key))
			}),
			sections.map((section) => /* @__PURE__ */ jsx(UnifiedSearchSectionContext.Provider, {
				value: section.context,
				children: /* @__PURE__ */ jsx("div", {
					id: sectionPanelId(section),
					role: "tabpanel",
					className: cx(`${CLASS_NAME$7}__section`, section.context.isSectionActive ? `${CLASS_NAME$7}__section--active` : void 0),
					children: section.component
				})
			}, section.key)),
			/* @__PURE__ */ jsx(DraggableHandle, {
				dock: dropUp ? "n" : "s",
				axis: "y",
				onBeginDragHandle: updatePanelBounds,
				onDragHandle: onDrag
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				dock: "e",
				axis: "x",
				className: `${CLASS_NAME$7}__right-handle`,
				onBeginDragHandle: updatePanelBounds,
				onDragHandle: onDrag
			}),
			/* @__PURE__ */ jsx(DraggableHandle, {
				dock: dropUp ? "ne" : "se",
				axis: "all",
				className: `${CLASS_NAME$7}__corner-handle`,
				onBeginDragHandle: updatePanelBounds,
				onDragHandle: onDrag
			})
		]
	});
}
function useViewportResizeHandler(handler) {
	const { canvas } = useCanvas();
	const sizeCheckerRef = React.useRef(void 0);
	React.useLayoutEffect(() => {
		sizeCheckerRef.current = handler;
	});
	React.useLayoutEffect(() => {
		const debouncer = new Debouncer();
		const onViewportResize = () => {
			const sizeChecker = sizeCheckerRef.current;
			if (sizeChecker) debouncer.call(sizeChecker);
		};
		canvas.events.on("resize", onViewportResize);
		sizeCheckerRef.current?.();
		return () => {
			debouncer.dispose();
			canvas.events.off("resize", onViewportResize);
		};
	}, []);
}
//#endregion
//#region src/widgets/editorForms/elementTypeSelector.tsx
function ElementTypeSelector(props) {
	const searchStore = useSearchInputStore({
		initialValue: "",
		submitTimeout: 200
	});
	const workspace = useWorkspace();
	const translation = useTranslation();
	return /* @__PURE__ */ jsx(ElementTypeSelectorInner, {
		...props,
		searchStore,
		workspace,
		translation
	});
}
var CLASS_NAME$6 = "reactodia-element-selector";
var FORM_CLASS$1 = "reactodia-form";
var ElementTypeSelectorInner = class extends React.Component {
	listener = new EventObserver();
	fetchTypesCancellation = new AbortController();
	filterCancellation = new AbortController();
	loadingItemCancellation = new AbortController();
	constructor(props) {
		super(props);
		this.state = {
			elementTypesState: "none",
			existingElements: [],
			existingElementsState: "none"
		};
	}
	componentDidMount() {
		const { searchStore, workspace: { model } } = this.props;
		this.listener.listen(searchStore.events, "changeValue", ({ source }) => {
			if (source.value.length === 0) this.searchExistingElements("");
			else this.forceUpdate();
		});
		this.listener.listen(searchStore.events, "executeSearch", ({ value }) => {
			this.searchExistingElements(value);
		});
		this.fetchPossibleElementTypes();
	}
	componentWillUnmount() {
		this.listener.stopListening();
		this.fetchTypesCancellation.abort();
		this.filterCancellation.abort();
		this.loadingItemCancellation.abort();
	}
	async fetchPossibleElementTypes() {
		const { source, workspace: { editor: { metadataProvider } }, translation: t } = this.props;
		if (!metadataProvider) return;
		this.fetchTypesCancellation.abort();
		this.fetchTypesCancellation = new AbortController();
		const signal = this.fetchTypesCancellation.signal;
		const connections = await mapAbortedToNull(metadataProvider.canConnect(source, void 0, void 0, { signal }), signal);
		if (connections === null) return;
		const elementTypeSet = /* @__PURE__ */ new Set();
		for (const { targetTypes } of connections) for (const type of targetTypes) elementTypeSet.add(type);
		this.setState({ elementTypes: Array.from(elementTypeSet) });
	}
	async searchExistingElements(searchString) {
		const { workspace: { model } } = this.props;
		this.filterCancellation.abort();
		this.setState({
			existingElements: [],
			existingElementsState: "none"
		});
		if (searchString.length > 0) {
			this.setState({ existingElementsState: "loading" });
			this.filterCancellation = new AbortController();
			const signal = this.filterCancellation.signal;
			const request = createRequest({ text: searchString });
			let lookupItems;
			try {
				lookupItems = await mapAbortedToNull(model.dataProvider.lookup(request), signal);
				if (lookupItems === null) return;
			} catch (err) {
				console.error("Error looking up existing entities", err);
				this.setState({ existingElementsState: "error" });
				return;
			}
			const existingElements = lookupItems.map((linked) => linked.element);
			this.setState({
				existingElements,
				existingElementsState: "none"
			});
		}
	}
	onElementTypeChange = async (e) => {
		this.setState({ elementTypesState: "loading" });
		this.loadingItemCancellation.abort();
		this.loadingItemCancellation = new AbortController();
		const signal = this.loadingItemCancellation.signal;
		const { onChange, workspace: { model, editor: { metadataProvider } }, translation: t } = this.props;
		const elementTypeIri = e.target.value;
		let createdEntity;
		try {
			createdEntity = await mapAbortedToNull(metadataProvider.createEntity(elementTypeIri, {
				translation: t,
				language: model.language,
				signal
			}), signal);
			if (createdEntity === null) return;
		} catch (err) {
			console.error("Error calling MetadataProvider.createEntity()", err);
			this.setState({ elementTypesState: "error" });
			return;
		}
		this.setState({ elementTypesState: "none" });
		onChange({
			value: createdEntity,
			isNew: true,
			loading: false
		});
	};
	renderElementTypeSelector() {
		const { elementValue, translation: t } = this.props;
		const { elementTypes, elementTypesState } = this.state;
		const value = elementValue.value.data.types.length ? elementValue.value.data.types[0] : "";
		if (elementTypesState !== "none") return /* @__PURE__ */ jsx(HtmlSpinner, {
			width: 20,
			height: 20,
			errorOccurred: elementTypesState === "error"
		});
		return /* @__PURE__ */ jsxs("div", {
			className: `${FORM_CLASS$1}__control-row`,
			children: [
				/* @__PURE__ */ jsx("label", { children: t.text("visual_authoring.select_entity.type.label") }),
				elementTypes ? /* @__PURE__ */ jsxs("select", {
					className: "reactodia-form-control",
					name: "reactodia-element-type-selector-select",
					value,
					onChange: (e) => void this.onElementTypeChange(e),
					children: [/* @__PURE__ */ jsx("option", {
						value: PlaceholderEntityType,
						disabled: true,
						children: t.text("visual_authoring.select_entity.type.placeholder")
					}), /* @__PURE__ */ jsx(ElementTypeOptions, { elementTypes })]
				}) : /* @__PURE__ */ jsx("div", { children: /* @__PURE__ */ jsx(HtmlSpinner, {
					width: 20,
					height: 20
				}) }),
				elementValue.error ? /* @__PURE__ */ jsx("span", {
					className: `${FORM_CLASS$1}__control-error`,
					children: elementValue.error
				}) : ""
			]
		});
	}
	renderExistingElementsList() {
		const { searchStore, elementValue } = this.props;
		const { elementTypes, existingElements, existingElementsState } = this.state;
		if (existingElementsState !== "none") return /* @__PURE__ */ jsx("div", {
			className: `${CLASS_NAME$6}__results-spinner`,
			children: /* @__PURE__ */ jsx(HtmlSpinner, {
				width: 30,
				height: 30,
				errorOccurred: existingElementsState === "error"
			})
		});
		return /* @__PURE__ */ jsx(ExistingElementList, {
			items: existingElements,
			requiredElementTypes: elementTypes,
			selected: elementValue.isNew ? void 0 : elementValue.value.data,
			onSelect: (item) => void this.onSelectExistingItem(item),
			highlightText: searchStore.value
		});
	}
	async onSelectExistingItem(data) {
		const { onChange, workspace: { model } } = this.props;
		this.loadingItemCancellation.abort();
		this.loadingItemCancellation = new AbortController();
		const signal = this.loadingItemCancellation.signal;
		onChange({
			value: { data },
			isNew: false,
			loading: true
		});
		const result = await model.dataProvider.elements({ elementIds: [data.id] });
		if (signal.aborted) return;
		onChange({
			value: { data: result.get(data.id) },
			isNew: false,
			loading: false
		});
	}
	render() {
		const { searchStore, translation: t } = this.props;
		return /* @__PURE__ */ jsxs("div", {
			className: cx(`${FORM_CLASS$1}__row`, CLASS_NAME$6, searchStore.value.length > 0 ? `${CLASS_NAME$6}--search-existing` : void 0),
			children: [/* @__PURE__ */ jsx(SearchInput, {
				store: searchStore,
				className: `${CLASS_NAME$6}__search`,
				inputProps: {
					className: `${CLASS_NAME$6}__search-input`,
					name: "reactodia-element-type-selector-search",
					placeholder: t.textOptional("visual_authoring.select_entity.input.placeholder"),
					autoFocus: true
				},
				children: /* @__PURE__ */ jsx("span", { className: `${CLASS_NAME$6}__search-icon` })
			}), searchStore.value.length > 0 ? /* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$6}__existing-elements-list`,
				role: "listbox",
				"aria-label": t.text("visual_authoring.select_entity.results.aria_label"),
				tabIndex: -1,
				children: this.renderExistingElementsList()
			}) : /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
				className: `${CLASS_NAME$6}__separator`,
				children: /* @__PURE__ */ jsx("i", {
					className: `${CLASS_NAME$6}__separator-text`,
					children: t.text("visual_authoring.select_entity.separator.label")
				})
			}), this.renderElementTypeSelector()] })]
		});
	}
};
function ExistingElementList(props) {
	const { items, requiredElementTypes, selected, onSelect, highlightText } = props;
	const { model, editor } = useWorkspace();
	const selectedIri = selected?.id;
	const selection = React.useMemo(() => new Set(selectedIri === void 0 ? void 0 : [selectedIri]), [selectedIri]);
	const cellsVersion = useSyncStore(useFrameDebouncedStore(useEventStore(model.events, "changeCells")), () => model.cellsVersion);
	const isEntityOnDiagram = React.useMemo(() => {
		const presentEntities = getAllPresentEntities(model);
		return (item) => presentEntities.has(item.id);
	}, [cellsVersion]);
	const temporaryState = useObservedProperty(editor.events, "changeTemporaryState", () => editor.temporaryState);
	return /* @__PURE__ */ jsx(SearchResults, {
		items,
		selection,
		onSelectionChanged: (nextSelection) => {
			if (nextSelection.size === 1) {
				const [nextIri] = nextSelection;
				const nextItem = items.find((item) => item.id === nextIri);
				if (nextItem) onSelect(nextItem);
			}
		},
		isItemDisabled: React.useCallback((item) => {
			const hasAppropriateType = requiredElementTypes && requiredElementTypes.some((type) => item.types.includes(type));
			return isEntityOnDiagram(item) && !temporaryState.elements.has(item.id) || !hasAppropriateType;
		}, [
			isEntityOnDiagram,
			temporaryState,
			requiredElementTypes
		]),
		highlightText,
		useDragAndDrop: false,
		multiSelection: false,
		footer: items.length === 0 ? /* @__PURE__ */ jsx(NoSearchResults, { hasQuery: true }) : void 0
	});
}
function ElementTypeOptions(props) {
	const { elementTypes } = props;
	const { model } = useWorkspace();
	const t = useTranslation();
	const language = useObservedProperty(model.events, "changeLanguage", () => model.language);
	useKeyedSyncStore(subscribeElementTypes, elementTypes, model);
	return /* @__PURE__ */ jsx(Fragment, { children: [...elementTypes].sort(makeElementTypeComparatorByLabel(model, t, language)).map((elementType) => {
		const type = model.getElementType(elementType);
		const label = t.formatLabel(type?.data?.label, elementType, language);
		return /* @__PURE__ */ jsx("option", {
			value: elementType,
			children: t.text("visual_authoring.select_entity.entity_type.label", {
				type: label,
				typeIri: elementType
			})
		}, elementType);
	}) });
}
function makeElementTypeComparatorByLabel(model, t, language) {
	return (a, b) => {
		const typeA = model.getElementType(a);
		const typeB = model.getElementType(b);
		const labelA = t.formatLabel(typeA?.data?.label, a, language);
		const labelB = t.formatLabel(typeB?.data?.label, b, language);
		return labelA.localeCompare(labelB);
	};
}
function validateElementType(element, t) {
	const error = element.types.indexOf("urn:reactodia:newElement") < 0 ? void 0 : t.text("visual_authoring.select_entity.validation.error_required");
	return Promise.resolve({
		error,
		allowChange: true
	});
}
//#endregion
//#region src/widgets/editorForms/findOrCreateEntityForm.tsx
var FORM_CLASS = "reactodia-form";
var FindOrCreateEntityForm = class extends React.Component {
	static contextType = WorkspaceContext;
	link;
	validationCancellation = new AbortController();
	constructor(props) {
		super(props);
		const { source, target, initialTargetIsNew, originalLink } = this.props;
		this.link = originalLink;
		this.state = {
			elementValue: {
				value: { data: target.data },
				isNew: initialTargetIsNew,
				loading: false,
				validated: true,
				allowChange: true
			},
			linkValue: {
				link: {
					base: originalLink.data,
					source: source.data,
					target: target.data,
					direction: "out"
				},
				validated: true,
				allowChange: true
			}
		};
	}
	componentDidMount() {
		this.validate();
	}
	componentWillUnmount() {
		this.validationCancellation.abort();
	}
	validate() {
		const { originalLink, translation: t } = this.props;
		const { elementValue, linkValue } = this.state;
		this.setState({ isValidating: true });
		this.validationCancellation.abort();
		this.validationCancellation = new AbortController();
		const signal = this.validationCancellation.signal;
		const validateElement = validateElementType(elementValue.value.data, t);
		const validateLink = validateLinkType(dataFromExtendedLink(linkValue.link), originalLink.data, this.context, t, signal);
		Promise.all([validateElement, validateLink]).then(([elementError, linkError]) => {
			if (signal.aborted) return;
			this.setState({ isValidating: false });
			this.setElementOrLink({
				elementValue: {
					...elementValue,
					...elementError,
					validated: true
				},
				linkValue: {
					...linkValue,
					...linkError,
					validated: true
				}
			});
		});
	}
	render() {
		const { source, originalLink, translation: t } = this.props;
		const { elementValue, linkValue, isValidating } = this.state;
		const isValid = !elementValue.error && !linkValue.error;
		return /* @__PURE__ */ jsxs("div", {
			className: FORM_CLASS,
			children: [/* @__PURE__ */ jsxs("div", {
				className: `${FORM_CLASS}__body`,
				children: [
					/* @__PURE__ */ jsx(ElementTypeSelector, {
						source: source.data,
						elementValue,
						onChange: (newState) => {
							this.setElementOrLink({
								elementValue: {
									value: newState.value,
									isNew: newState.isNew,
									loading: newState.loading,
									error: void 0,
									validated: false,
									allowChange: false
								},
								linkValue: {
									link: {
										base: originalLink.data,
										source: source.data,
										target: newState.value.data,
										direction: "out"
									},
									validated: false,
									allowChange: false
								}
							});
						}
					}),
					elementValue.loading ? /* @__PURE__ */ jsxs("div", {
						style: { display: "flex" },
						children: [
							/* @__PURE__ */ jsx(HtmlSpinner, {
								width: 20,
								height: 20
							}),
							"\xA0",
							t.text("visual_authoring.find_or_create.loading.label")
						]
					}) : /* @__PURE__ */ jsx(LinkTypeSelector, {
						link: linkValue.link,
						error: linkValue.error,
						onChange: (link) => this.setElementOrLink({ linkValue: {
							link,
							error: void 0,
							validated: false,
							allowChange: false
						} }),
						disabled: elementValue.error !== void 0
					}),
					isValidating ? /* @__PURE__ */ jsx("div", {
						className: `${FORM_CLASS}__progress`,
						children: /* @__PURE__ */ jsx(ProgressBar, {
							state: "loading",
							title: t.text("visual_authoring.find_or_create.validation_progress.title"),
							height: 10
						})
					}) : null
				]
			}), /* @__PURE__ */ jsxs("div", {
				className: `${FORM_CLASS}__controls`,
				children: [/* @__PURE__ */ jsx("button", {
					className: `reactodia-btn reactodia-btn-primary ${FORM_CLASS}__apply-button`,
					onClick: this.onApply,
					disabled: elementValue.loading || !isValid || isValidating,
					title: t.textOptional("visual_authoring.find_or_create.dialog.apply.title") ?? t.text("visual_authoring.dialog.apply.title"),
					children: t.textOptional("visual_authoring.find_or_create.dialog.apply.label") ?? t.text("visual_authoring.dialog.apply.label")
				}), /* @__PURE__ */ jsx("button", {
					className: "reactodia-btn reactodia-btn-secondary",
					onClick: this.props.onCancel,
					title: t.text("visual_authoring.dialog.cancel.title"),
					children: t.text("visual_authoring.dialog.cancel.label")
				})]
			})]
		});
	}
	setElementOrLink({ elementValue, linkValue }) {
		const { model, editor } = this.context;
		this.setState((state) => ({
			elementValue: elementValue || state.elementValue,
			linkValue: linkValue || state.linkValue
		}), () => {
			const { source, target } = this.props;
			const { elementValue, linkValue } = this.state;
			if (elementValue && !elementValue.validated || linkValue && !linkValue.validated) this.validate();
			if (elementValue && elementValue.validated && elementValue.allowChange) {
				const previous = target.data;
				let temporaryState = editor.temporaryState;
				temporaryState = TemporaryState.removeEntity(temporaryState, previous);
				temporaryState = TemporaryState.removeRelation(temporaryState, this.link.data);
				editor.setTemporaryState(temporaryState);
				const batch = model.history.startBatch();
				batch.history.execute(changeEntityData(model, target.iri, elementValue.value.data));
				batch.discard();
				temporaryState = TemporaryState.addEntity(temporaryState, target.data);
				temporaryState = TemporaryState.addRelation(temporaryState, this.link.data);
				editor.setTemporaryState(temporaryState);
			}
			if (linkValue && linkValue.validated && linkValue.allowChange) {
				editor.removeTemporaryCells([this.link]);
				const linkBase = relationFromExtendedLink(linkValue.link, source, target);
				this.link = editor.createRelation(linkBase, { temporary: true });
			}
		});
	}
	onApply = () => {
		const { model, editor } = this.context;
		const { source, target, onAfterApply } = this.props;
		const { elementValue, linkValue } = this.state;
		const link = this.link;
		if (!elementValue.isNew) editor.setTemporaryState(TemporaryState.removeEntity(editor.temporaryState, elementValue.value.data));
		editor.removeTemporaryCells([target, link]);
		const batch = model.history.startBatch(elementValue.isNew ? TranslatedText.text("visual_authoring.find_or_create.create_command") : TranslatedText.text("visual_authoring.find_or_create.connect_command"));
		if (elementValue.isNew) {
			model.addElement(target);
			if (elementValue.value.elementState) target.setElementState(elementValue.value.elementState);
			editor.setAuthoringState(AuthoringState.addEntity(editor.authoringState, target.data));
		} else model.requestLinks({ addedElements: [elementValue.value.data.id] });
		const linkBase = relationFromExtendedLink(linkValue.link, source, target);
		editor.createRelation(linkBase);
		batch.store();
		onAfterApply();
	};
};
//#endregion
//#region src/widgets/visualAuthoring/authoredEntityDecorator.tsx
var CLASS_NAME$5 = "reactodia-authoring-state";
function AuthoredEntityDecorator(props) {
	const { target, inlineActions } = props;
	const { canvas } = useCanvas();
	const { model, editor } = useWorkspace();
	const t = useTranslation();
	const onlyTargetSelected = useObservedProperty(model.events, "changeSelection", () => model.selection.length === 1 && model.selection[0] === target);
	const data = useObservedProperty(target.events, "changeData", () => target.data);
	const dependencies = [data];
	const state = useSyncStore(useEventStore(editor.events, "changeAuthoringState", dependencies), () => editor.authoringState.elements.get(data.id));
	const validation = useSyncStore(useEventStore(editor.events, "changeValidationState", dependencies), () => editor.validationState.elements.get(data.id));
	const isTemporary = useSyncStore(useEventStore(editor.events, "changeTemporaryState", dependencies), () => editor.temporaryState.elements.has(data.id));
	const [shape, setShape] = React.useState(() => canvas.renderingState.getElementShape(target));
	React.useEffect(() => {
		const listener = new EventObserver();
		listener.listen(canvas.renderingState.events, "changeElementSize", (e) => {
			if (e.source === target) setShape(canvas.renderingState.getElementShape(target));
		});
		return () => listener.stopListening();
	}, [target]);
	let elementOutlines = null;
	if (isTemporary) elementOutlines = /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(ElementSvgShape, {
		shape,
		className: `${CLASS_NAME$5}__outline-overlay`
	}), /* @__PURE__ */ jsx(ElementSvgShape, {
		shape,
		fill: "url(#stripe-pattern)"
	})] });
	else if (state && state.type === "entityDelete") {
		const cx = shape.bounds.width / 2;
		const cy = shape.bounds.height / 2;
		const rx = shape.type === "rect" ? cx : cx * Math.SQRT1_2;
		const ry = shape.type === "rect" ? cy : cy * Math.SQRT1_2;
		elementOutlines = /* @__PURE__ */ jsxs("g", { children: [
			/* @__PURE__ */ jsx(ElementSvgShape, {
				shape,
				className: `${CLASS_NAME$5}__outline-overlay`
			}),
			/* @__PURE__ */ jsx("line", {
				className: `${CLASS_NAME$5}__outline-cross-line`,
				x1: cx - rx,
				y1: cy - ry,
				x2: cx + rx,
				y2: cy + ry
			}),
			/* @__PURE__ */ jsx("line", {
				className: `${CLASS_NAME$5}__outline-cross-line`,
				x1: cx - rx,
				y1: cy + ry,
				x2: cx + rx,
				y2: cy - ry
			})
		] }, target.id);
	}
	let elementValidations = null;
	if (validation) {
		const severity = getMaxSeverity(validation.items);
		const title = validation.items.map((item) => {
			if (item.propertyType) {
				const propertyType = model.getPropertyType(item.propertyType);
				return `${t.formatLabel(propertyType?.data?.label, item.propertyType, model.language)}: ${item.message}`;
			} else return item.message;
		}).join("\n");
		elementValidations = /* @__PURE__ */ jsxs("div", {
			className: cx(`${CLASS_NAME$5}__item-validation`, getSeverityClass$1(severity)),
			title,
			children: [validation.loading ? /* @__PURE__ */ jsx(HtmlSpinner, {
				width: 15,
				height: 17
			}) : /* @__PURE__ */ jsx("div", { className: `${CLASS_NAME$5}__item-validation-icon` }), !validation.loading && validation.items.length > 0 ? validation.items.length : void 0]
		});
	}
	const elementState = /* @__PURE__ */ jsx("div", {
		className: `${CLASS_NAME$5}__state-indicator`,
		children: /* @__PURE__ */ jsxs("div", {
			className: `${CLASS_NAME$5}__state-indicator-body`,
			children: [
				state ? /* @__PURE__ */ jsx("span", {
					className: `${CLASS_NAME$5}__state-label`,
					children: state.type === "entityAdd" ? t.text("authoring_state.entity_add.label") : state.type === "entityChange" ? t.text("authoring_state.entity_change.label") : state.type === "entityDelete" ? t.text("authoring_state.entity_delete.label") : null
				}) : null,
				isTemporary ? null : /* @__PURE__ */ jsx(InlineActions, {
					target,
					onlySelected: onlyTargetSelected,
					state,
					allActions: Boolean(inlineActions)
				}),
				elementValidations
			]
		})
	}, target.id);
	return /* @__PURE__ */ jsx(ElementDecoration, {
		target,
		children: /* @__PURE__ */ jsxs("div", {
			className: cx(`${CLASS_NAME$5}__decorator`, !state && !validation ? `${CLASS_NAME$5}__decorator--optional` : void 0, onlyTargetSelected ? `${CLASS_NAME$5}__decorator--selected` : void 0),
			"data-reactodia-no-export": "true",
			children: [elementOutlines ? /* @__PURE__ */ jsxs("svg", {
				className: `${CLASS_NAME$5}__element-outlines`,
				width: shape.bounds.width,
				height: shape.bounds.height,
				children: [/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsx("pattern", {
					id: "stripe-pattern",
					patternUnits: "userSpaceOnUse",
					width: 13,
					height: 13,
					patternTransform: "rotate(45)",
					children: /* @__PURE__ */ jsx("line", {
						className: `${CLASS_NAME$5}__outline-stripe-line`,
						x1: 0,
						y: 0,
						x2: 0,
						y2: 13
					})
				}) }), elementOutlines]
			}) : null, elementState]
		})
	});
}
function getSeverityClass$1(severity) {
	switch (severity) {
		case "info": return `${CLASS_NAME$5}--severity-info`;
		case "warning": return `${CLASS_NAME$5}--severity-warning`;
		case "error": return `${CLASS_NAME$5}--severity-error`;
		default: return;
	}
}
function InlineActions(props) {
	const { target, onlySelected, state, allActions } = props;
	const { editor } = useWorkspace();
	const t = useTranslation();
	const authored = useAuthoredEntity(target.data, allActions);
	const tabIndex = onlySelected ? 0 : -1;
	return /* @__PURE__ */ jsxs("div", {
		className: `${CLASS_NAME$5}__actions`,
		children: [
			allActions && (!state || state.type === "entityAdd" || state.type === "entityChange") ? /* @__PURE__ */ jsx("button", {
				className: `${CLASS_NAME$5}__action ${CLASS_NAME$5}__action-edit`,
				disabled: !authored.canEdit,
				onClick: () => authored.onEdit(target),
				title: authored.canEdit ? t.text("authoring_state.entity_action_edit.title") : t.text("authoring_state.entity_action_edit.title_disabled"),
				tabIndex,
				children: t.text("authoring_state.entity_action_edit.label")
			}) : null,
			allActions && !state || state?.type === "entityAdd" ? /* @__PURE__ */ jsx("button", {
				className: `${CLASS_NAME$5}__action ${CLASS_NAME$5}__action-delete`,
				disabled: !authored.canDelete,
				onClick: () => authored.onDelete(),
				title: authored.canEdit ? t.text("authoring_state.entity_action_delete.title") : t.text("authoring_state.entity_action_delete.title_disabled"),
				tabIndex,
				children: t.text("authoring_state.entity_action_delete.label")
			}) : null,
			state && state.type !== "entityAdd" ? /* @__PURE__ */ jsx("button", {
				className: `${CLASS_NAME$5}__action ${CLASS_NAME$5}__action-discard`,
				onClick: () => editor.discardChange(state),
				title: t.text("authoring_state.entity_action_discard.title"),
				tabIndex,
				children: t.text("authoring_state.entity_action_discard.label")
			}) : null
		]
	});
}
function ElementSvgShape(props) {
	const { shape, ...otherProps } = props;
	switch (shape.type) {
		case "rect": return /* @__PURE__ */ jsx("rect", {
			x: 0,
			y: 0,
			width: shape.bounds.width,
			height: shape.bounds.height,
			...otherProps
		});
		case "ellipse": {
			const rx = shape.bounds.width / 2;
			const ry = shape.bounds.height / 2;
			return /* @__PURE__ */ jsx("ellipse", {
				cx: rx,
				cy: ry,
				rx,
				ry,
				...otherProps
			});
		}
		default: return null;
	}
}
//#endregion
//#region src/widgets/visualAuthoring/authoredRelationOverlay.tsx
function AuthoredRelationOverlay(props) {
	const { canvas } = useCanvas();
	const workspace = useWorkspace();
	const t = useTranslation();
	const { editor } = workspace;
	if (!useObservedProperty(editor.events, "changeMode", () => editor.inAuthoringMode)) return null;
	return /* @__PURE__ */ jsx(LinkStateWidgetInner, {
		...props,
		workspace,
		canvas,
		translation: t
	});
}
var CLASS_NAME$4 = "reactodia-authoring-state";
var DEFAULT_LINK_LABEL_MARGIN = 5;
var LinkStateWidgetInner = class extends React.Component {
	listener = new EventObserver();
	componentDidMount() {
		this.listenEvents();
	}
	componentWillUnmount() {
		const { canvas } = this.props;
		this.listener.stopListening();
		canvas.renderingState.cancelOnLayerUpdate(RenderingLayer.Overlay, this.performUpdate);
	}
	listenEvents() {
		const { workspace: { model, editor }, canvas } = this.props;
		this.listener.listen(model.events, "elementEvent", ({ data }) => {
			if (data.changePosition) this.scheduleUpdate();
		});
		this.listener.listen(model.events, "linkEvent", ({ data }) => {
			if (data.changeVertices) this.scheduleUpdate();
		});
		this.listener.listen(model.events, "changeCells", this.scheduleUpdate);
		this.listener.listen(editor.events, "changeAuthoringState", this.scheduleUpdate);
		this.listener.listen(editor.events, "changeTemporaryState", this.scheduleUpdate);
		this.listener.listen(editor.events, "changeValidationState", this.scheduleUpdate);
		this.listener.listen(canvas.events, "changeTransform", this.performUpdate);
		this.listener.listen(canvas.renderingState.events, "changeElementSize", this.scheduleUpdate);
		this.listener.listen(canvas.renderingState.events, "changeLinkLabelBounds", this.scheduleUpdate);
	}
	scheduleUpdate = () => {
		const { canvas } = this.props;
		canvas.renderingState.scheduleOnLayerUpdate(RenderingLayer.Overlay, this.performUpdate);
	};
	performUpdate = () => {
		this.forceUpdate();
	};
	calculateLinkPath(link) {
		return this.calculateSpline(link).toPath();
	}
	calculateSpline(link) {
		const { workspace: { model }, canvas } = this.props;
		const source = model.getElement(link.sourceId);
		const target = model.getElement(link.targetId);
		const template = canvas.renderingState.getLinkTemplate(link);
		const route = canvas.renderingState.getRouting(link.id);
		const verticesDefinedByUser = link.vertices || [];
		const vertices = route ? route.vertices : verticesDefinedByUser;
		const sourceShape = canvas.renderingState.getElementShape(source);
		const targetShape = canvas.renderingState.getElementShape(target);
		const points = computePolyline(sourceShape, targetShape, vertices);
		return Spline.create({
			type: template?.spline ?? Spline.defaultType,
			points,
			source: Rect.center(sourceShape.bounds),
			target: Rect.center(targetShape.bounds)
		});
	}
	renderLinkStateLabels() {
		const { workspace: { model, editor } } = this.props;
		const rendered = [];
		for (const link of model.links) {
			if (!(link instanceof RelationLink)) continue;
			const state = editor.authoringState.links.get(link.data);
			const renderedState = this.renderLinkStatus(state);
			const renderedValidations = this.renderLinkValidations(link.data);
			if (renderedState || renderedValidations) {
				const labelPosition = this.getLinkStateLabelPosition(link);
				if (labelPosition) {
					const style = {
						left: labelPosition.x,
						top: labelPosition.y
					};
					rendered.push(/* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$4}__state-indicator`,
						style,
						children: /* @__PURE__ */ jsxs("div", {
							className: `${CLASS_NAME$4}__state-indicator-body`,
							children: [renderedState, renderedValidations]
						})
					}, link.id));
				}
			}
		}
		return rendered;
	}
	renderLinkStatus(state) {
		const { workspace: { editor }, translation: t } = this.props;
		if (!state) return null;
		return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("span", {
			className: `${CLASS_NAME$4}__state-label`,
			children: state.type === "relationAdd" ? t.text("authoring_state.relation_add.label") : state.type === "relationChange" ? t.text("authoring_state.relation_change.label") : state.type === "relationDelete" ? t.text("authoring_state.relation_delete.label") : null
		}), /* @__PURE__ */ jsx("span", {
			className: `${CLASS_NAME$4}__action ${CLASS_NAME$4}__action-discard`,
			onClick: () => editor.discardChange(state),
			title: t.text("authoring_state.relation_action_discard.title"),
			children: t.text("authoring_state.relation_action_discard.label")
		})] });
	}
	renderLinkStateHighlighting() {
		const { workspace: { model, editor } } = this.props;
		const rendered = [];
		for (const link of model.links) {
			if (!(link instanceof RelationLink)) continue;
			if (editor.temporaryState.links.has(link.data)) {
				const path = this.calculateLinkPath(link);
				rendered.push(/* @__PURE__ */ jsx("path", {
					className: `${CLASS_NAME$4}__link-temporary`,
					d: path
				}, link.id));
			} else {
				const event = editor.authoringState.links.get(link.data);
				const isDeletedLink = AuthoringState.isDeletedRelation(editor.authoringState, link.data);
				const hasUncertainEndpoints = AuthoringState.hasEntityChangedIri(editor.authoringState, link.data.sourceId) || AuthoringState.hasEntityChangedIri(editor.authoringState, link.data.targetId);
				if (event || isDeletedLink || hasUncertainEndpoints) {
					const path = this.calculateLinkPath(link);
					const className = isDeletedLink ? `${CLASS_NAME$4}__link-deleted` : event?.type === "relationAdd" ? `${CLASS_NAME$4}__link-added` : event?.type === "relationChange" ? `${CLASS_NAME$4}__link-changed` : hasUncertainEndpoints ? `${CLASS_NAME$4}__link-uncertain` : `${CLASS_NAME$4}__link-changed`;
					rendered.push(/* @__PURE__ */ jsx("path", {
						className,
						d: path
					}, link.id));
				}
			}
		}
		return rendered;
	}
	getLinkStateLabelPosition(link) {
		const { canvas, linkLabelMargin = DEFAULT_LINK_LABEL_MARGIN } = this.props;
		const labelBounds = canvas.renderingState.getLinkLabelBounds(link);
		if (labelBounds) {
			const { x, y } = labelBounds;
			return {
				x,
				y: y - linkLabelMargin / 2
			};
		} else {
			const spline = this.calculateSpline(link);
			const polylineLength = computePolylineLength(spline.geometry.points);
			return getPointAlongPolyline(spline.geometry.points, polylineLength / 2);
		}
	}
	renderLinkValidations(key) {
		const { workspace: { model, editor }, translation: t } = this.props;
		const { validationState } = editor;
		const validation = validationState.links.get(key);
		if (!validation) return null;
		const title = validation.items.map((item) => {
			if (item.propertyType) {
				const propertyType = model.getPropertyType(item.propertyType);
				return `${t.formatLabel(propertyType?.data?.label, item.propertyType, model.language)}: ${item.message}`;
			} else return item.message;
		}).join("\n");
		const severity = getMaxSeverity(validation.items);
		return /* @__PURE__ */ jsxs("div", {
			className: cx(`${CLASS_NAME$4}__item-validation`, getSeverityClass(severity)),
			title,
			children: [validation.loading ? /* @__PURE__ */ jsx(HtmlSpinner, {
				width: 15,
				height: 17
			}) : /* @__PURE__ */ jsx("div", { className: `${CLASS_NAME$4}__item-validation-icon` }), !validation.loading && validation.items.length > 0 ? validation.items.length : void 0]
		});
	}
	render() {
		const { canvas } = this.props;
		const transform = canvas.metrics.transform;
		const { scale, originX, originY } = transform;
		const htmlTransformStyle = {
			position: "absolute",
			left: 0,
			top: 0,
			transform: `scale(${scale},${scale})translate(${originX}px,${originY}px)`
		};
		return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(CanvasPlaceAt, {
			layer: "overLinkGeometry",
			children: /* @__PURE__ */ jsx("div", {
				className: CLASS_NAME$4,
				children: /* @__PURE__ */ jsx(SvgPaperLayer, {
					paperTransform: transform,
					style: {
						overflow: "visible",
						pointerEvents: "none"
					},
					children: this.renderLinkStateHighlighting()
				})
			})
		}), /* @__PURE__ */ jsx(CanvasPlaceAt, {
			layer: "overElements",
			children: /* @__PURE__ */ jsx("div", {
				className: CLASS_NAME$4,
				children: /* @__PURE__ */ jsx("div", {
					className: `${CLASS_NAME$4}__validation-layer`,
					style: htmlTransformStyle,
					children: this.renderLinkStateLabels()
				})
			})
		})] });
	}
};
function getSeverityClass(severity) {
	switch (severity) {
		case "info": return `${CLASS_NAME$4}--severity-info`;
		case "warning": return `${CLASS_NAME$4}--severity-warning`;
		case "error": return `${CLASS_NAME$4}--severity-error`;
		default: return;
	}
}
//#endregion
//#region src/widgets/visualAuthoring/dragEditLayer.tsx
function DragEditLayer(props) {
	const { operation, onFinishEditing } = props;
	const workspace = useWorkspace();
	const t = useTranslation();
	const { model, editor } = workspace;
	const { metadataProvider } = editor;
	return /* @__PURE__ */ jsx(DragLinkMover, {
		operation,
		createLink: React.useCallback((source, target, original) => {
			if (original && !(original instanceof RelationLink)) throw new Error("DragEditLayer: cannot clone a non-relation link");
			const linkTemplate = new RelationLink({
				sourceId: source.id,
				targetId: target.id,
				data: {
					sourceId: source instanceof EntityElement ? source.iri : "",
					targetId: target instanceof EntityElement ? target.iri : "",
					linkTypeId: original?.data.linkTypeId ?? "urn:reactodia:newLink",
					properties: original?.data.properties ?? {}
				},
				vertices: original?.vertices,
				linkState: original?.linkState
			});
			const link = editor.createRelation(linkTemplate, { temporary: true });
			if (!original) model.setLinkVisibility(PlaceholderRelationType, "withoutLabel");
			return link;
		}, [operation]),
		canConnect: React.useCallback(async (source, link, target, signal) => {
			const targetEvent = target instanceof EntityElement ? editor.authoringState.elements.get(target.iri) : void 0;
			if (!(metadataProvider && link instanceof RelationLink && source instanceof EntityElement && (!target || target instanceof EntityElement) && (!targetEvent || targetEvent.type !== "entityDelete"))) return new EntityDragConnection([], workspace, t);
			const linkTypeId = link.data.linkTypeId === "urn:reactodia:newLink" ? void 0 : link.data.linkTypeId;
			if (target && linkTypeId && model.findLink(linkTypeId, source.id, target.id)) return new EntityDragConnection([], workspace, t);
			return new EntityDragConnection(await metadataProvider.canConnect(source.data, target?.data, linkTypeId, { signal }), workspace, t);
		}, [metadataProvider]),
		cleanupLink: React.useCallback((link) => {
			if (link instanceof RelationLink) editor.setTemporaryState(TemporaryState.removeRelation(editor.temporaryState, link.data));
		}, []),
		onFinish: onFinishEditing
	});
}
var EntityDragConnection = class {
	connections;
	workspace;
	translation;
	constructor(connections, workspace, translation) {
		this.connections = connections;
		this.workspace = workspace;
		this.translation = translation;
	}
	get allowed() {
		return this.connections.length > 0;
	}
	async connect(source, target, targetPosition, canvas, signal) {
		if (!(source instanceof EntityElement && (!target || target instanceof EntityElement))) return;
		let createdTarget = target;
		if (!createdTarget) {
			createdTarget = await this.createNewElement(signal);
			createdTarget.setPosition(targetPosition);
			canvas.renderingState.syncUpdate();
			setElementCenterAtPoint(createdTarget, targetPosition, canvas.renderingState);
		}
		const modifiedLink = await this.createNewLink(source, createdTarget, signal);
		this.afterCommit(target, createdTarget, modifiedLink);
	}
	async moveSource(link, newSource, canvas, signal) {
		const { editor } = this.workspace;
		if (!(link instanceof RelationLink && newSource instanceof EntityElement)) return;
		const modifiedLink = editor.moveRelationSource({
			link,
			newSource
		});
		this.afterCommit(newSource, void 0, modifiedLink);
	}
	async moveTarget(link, newTarget, canvas, signal) {
		const { editor } = this.workspace;
		if (!(link instanceof RelationLink && newTarget instanceof EntityElement)) return;
		const modifiedLink = editor.moveRelationTarget({
			link,
			newTarget
		});
		this.afterCommit(newTarget, void 0, modifiedLink);
	}
	async createNewElement(signal) {
		const { model, editor } = this.workspace;
		if (!editor.metadataProvider) throw new Error("Cannot create new entity without metadata provider");
		const elementTypes = /* @__PURE__ */ new Set();
		for (const { targetTypes } of this.connections) for (const typeIri of targetTypes) elementTypes.add(typeIri);
		const selectedType = elementTypes.size === 1 ? Array.from(elementTypes)[0] : PlaceholderEntityType;
		const createdEntity = await editor.metadataProvider.createEntity(selectedType, {
			translation: this.translation,
			language: model.language,
			signal
		});
		const element = editor.createEntity(createdEntity.data, { temporary: true });
		if (createdEntity.elementState) element.setElementState(createdEntity.elementState);
		return element;
	}
	async createNewLink(source, target, signal) {
		const { model, editor } = this.workspace;
		if (!editor.metadataProvider) throw new Error("Cannot create new relation without metadata provider");
		const inLinkSet = /* @__PURE__ */ new Set();
		const outLinkSet = /* @__PURE__ */ new Set();
		for (const { targetTypes, inLinks, outLinks } of this.connections) if (target.data.types.some((type) => targetTypes.has(type))) {
			for (const linkType of inLinks) inLinkSet.add(linkType);
			for (const linkType of outLinks) outLinkSet.add(linkType);
		}
		const singleInLink = inLinkSet.size === 1 ? [...inLinkSet][0] : void 0;
		const singleOutLink = outLinkSet.size === 1 ? [...outLinkSet][0] : void 0;
		let linkTypeIri;
		let direction;
		if (inLinkSet.size === 0 && singleOutLink) {
			linkTypeIri = singleOutLink;
			direction = "out";
		} else if (singleInLink && outLinkSet.size === 0) {
			linkTypeIri = singleInLink;
			direction = "in";
		} else if (singleInLink && singleOutLink && singleInLink === singleOutLink) {
			linkTypeIri = singleOutLink;
			direction = model.findLink(linkTypeIri, source.id, target.id) ? "in" : "out";
		} else {
			linkTypeIri = PlaceholderRelationType;
			direction = "out";
		}
		let [effectiveSource, effectiveTarget] = [source, target];
		if (direction === "in") [effectiveSource, effectiveTarget] = [effectiveTarget, effectiveSource];
		const createdRelation = await editor.metadataProvider.createRelation(effectiveSource.data, effectiveTarget.data, linkTypeIri, {
			translation: this.translation,
			language: model.language,
			signal
		});
		const link = new RelationLink({
			sourceId: effectiveSource.id,
			targetId: effectiveTarget.id,
			data: createdRelation.data,
			linkState: createdRelation.linkState
		});
		const existingLink = model.findLink(link.typeId, link.sourceId, link.targetId);
		return existingLink instanceof RelationLink ? existingLink : editor.createRelation(link, { temporary: true });
	}
	afterCommit(selectedTarget, createdTarget, modifiedLink) {
		const { model, getCommandBus } = this.workspace;
		if (selectedTarget) {
			model.setSelection([modifiedLink]);
			getCommandBus(VisualAuthoringTopic).trigger("editRelation", { target: modifiedLink });
		} else if (createdTarget && modifiedLink) {
			model.setSelection([createdTarget]);
			getCommandBus(VisualAuthoringTopic).trigger("findOrCreateEntity", {
				link: modifiedLink,
				target: createdTarget
			});
		}
	}
};
function setElementCenterAtPoint(element, point, sizeProvider) {
	const { width, height } = sizeProvider.getElementSize(element) ?? {
		width: 0,
		height: 0
	};
	element.setPosition({
		x: point.x - width / 2,
		y: point.y - height / 2
	});
}
//#endregion
//#region src/widgets/visualAuthoring/visualAuthoring.tsx
/**
* Canvas widget component to provide UI for the visual graph authoring.
*
* @category Components
*/
function VisualAuthoring(props) {
	const { propertyEditor, inlineEntityActions = true } = props;
	const { model, editor, overlay, getCommandBus } = useWorkspace();
	const t = useTranslation();
	React.useLayoutEffect(() => {
		const listener = new EventObserver();
		listener.listen(overlay.events, "changeOpenedDialog", ({ previous }) => {
			if (previous && previous.target) editor.removeTemporaryCells([previous.target]);
		});
		return () => {
			listener.stopListening();
		};
	}, []);
	React.useLayoutEffect(() => {
		const commands = getCommandBus(VisualAuthoringTopic);
		const listener = new EventObserver();
		listener.listen(commands, "editEntity", ({ target }) => {
			let editorTarget;
			if (target instanceof EntityElement) editorTarget = target;
			else editorTarget = new EntityElement({ data: target });
			const content = propertyEditor({
				type: "entity",
				target: editorTarget,
				onClose: () => overlay.hideDialog()
			});
			overlay.showDialog({
				target: target instanceof EntityElement ? target : void 0,
				dialogType: BuiltinDialogType.editEntity,
				style: {
					caption: t.text("visual_authoring.edit_entity.dialog.caption"),
					defaultSize: {
						width: 340,
						height: 400
					}
				},
				content,
				holdSelection: true
			});
		});
		listener.listen(commands, "findOrCreateEntity", (e) => {
			const { link, target } = e;
			const content = /* @__PURE__ */ jsx(FindOrCreateEntityForm, {
				source: model.getElement(link.sourceId),
				target,
				initialTargetIsNew: editor.temporaryState.elements.has(target.iri),
				originalLink: link,
				onAfterApply: () => {
					overlay.hideDialog();
					if (AuthoringState.isAddedEntity(editor.authoringState, target.iri)) commands.trigger("editEntity", { target });
				},
				onCancel: () => overlay.hideDialog(),
				translation: t
			});
			overlay.showDialog({
				target,
				dialogType: BuiltinDialogType.findOrCreateEntity,
				style: {
					caption: t.text("visual_authoring.find_or_create.dialog.caption"),
					minSize: {
						width: 250,
						height: 320
					}
				},
				content,
				onClose: () => editor.removeAllTemporaryCells()
			});
		});
		listener.listen(commands, "editRelation", ({ target: link }) => {
			const caption = editor.temporaryState.links.has(link.data) ? t.text("visual_authoring.edit_relation.dialog.caption_new") : t.text("visual_authoring.edit_relation.dialog.caption");
			const content = propertyEditor({
				type: "relation",
				target: link,
				onChangeTarget: (newTarget) => {
					overlay.hideDialog();
					commands.trigger("editRelation", { target: newTarget });
				},
				onClose: () => overlay.hideDialog()
			});
			overlay.showDialog({
				target: link,
				dialogType: BuiltinDialogType.editRelation,
				style: {
					defaultSize: {
						width: 340,
						height: 300
					},
					caption
				},
				content,
				onClose: () => editor.removeTemporaryCells([link])
			});
		});
		return () => listener.stopListening();
	}, []);
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(AuthoredRelationOverlay, {}),
		/* @__PURE__ */ jsx(EntityDecorators, { inlineActions: inlineEntityActions }),
		/* @__PURE__ */ jsx(DragEditState, {})
	] });
}
function EntityDecoratorsInner(props) {
	const { inlineActions } = props;
	const { canvas } = useCanvas();
	const { model, editor } = useWorkspace();
	const inAuthoringMode = useObservedProperty(editor.events, "changeMode", () => editor.inAuthoringMode);
	const cellsVersion = useSyncStore(useLayerDebouncedStore(useEventStore(model.events, "changeCells"), canvas.renderingState), () => model.cellsVersion);
	const cachedDecorators = React.useMemo(() => /* @__PURE__ */ new WeakMap(), [inlineActions, cellsVersion]);
	const decorators = [];
	if (inAuthoringMode) {
		for (const element of model.elements) if (element instanceof EntityElement) {
			let decorator = cachedDecorators.get(element);
			if (!decorator) {
				decorator = /* @__PURE__ */ jsx(AuthoredEntityDecorator, {
					target: element,
					inlineActions
				}, element.id);
				cachedDecorators.set(element, decorator);
			}
			decorators.push(decorator);
		}
	}
	return decorators;
}
var EntityDecorators = React.memo(EntityDecoratorsInner);
function DragEditState() {
	const [layer, setLayer] = React.useState(null);
	const { getCommandBus } = useWorkspace();
	React.useLayoutEffect(() => {
		const commands = getCommandBus(VisualAuthoringTopic);
		const listener = new EventObserver();
		listener.listen(commands, "startDragEdit", ({ operation }) => {
			setLayer(/* @__PURE__ */ jsx(DragEditLayer, {
				operation,
				onFinishEditing: () => setLayer(null)
			}));
		});
		return () => listener.stopListening();
	}, []);
	return layer;
}
//#endregion
//#region src/widgets/zoomControl.tsx
var CLASS_NAME$3 = "reactodia-zoom-control";
/**
* Canvas widget component to display zoom controls (zoom-in, zoom-out, zoom-to-fit).
*
* @category Components
*/
function ZoomControl(props) {
	const { dock, dockOffsetX, dockOffsetY, showPointerModeToggle = true } = props;
	const { canvas } = useCanvas();
	const t = useTranslation();
	const pointerMode = useObservedProperty(canvas.events, "changePointerMode", () => canvas.pointerMode);
	return /* @__PURE__ */ jsx(ViewportDock, {
		dock,
		dockOffsetX,
		dockOffsetY,
		children: /* @__PURE__ */ jsxs("div", {
			className: CLASS_NAME$3,
			role: "toolbar",
			children: [
				/* @__PURE__ */ jsx("button", {
					type: "button",
					className: cx(`${CLASS_NAME$3}__zoom-in-button`, "reactodia-btn reactodia-btn-default"),
					title: t.text("zoom_control.zoom_in.title"),
					onClick: () => canvas.zoomIn()
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					className: cx(`${CLASS_NAME$3}__zoom-out-button`, "reactodia-btn reactodia-btn-default"),
					title: t.text("zoom_control.zoom_out.title"),
					onClick: () => canvas.zoomOut()
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					className: cx(`${CLASS_NAME$3}__zoom-fit-button`, "reactodia-btn reactodia-btn-default"),
					title: t.text("zoom_control.zoom_to_fit.title"),
					onClick: () => canvas.zoomToFit({ animate: true })
				}),
				showPointerModeToggle ? /* @__PURE__ */ jsx("button", {
					type: "button",
					className: cx(`${CLASS_NAME$3}__selection-mode-button`, "reactodia-btn reactodia-btn-default", pointerMode === "selection" ? "active" : void 0),
					title: t.text("zoom_control.pointer_mode.title"),
					onClick: () => canvas.setPointerMode(pointerMode === "panning" ? "selection" : "panning")
				}) : null
			]
		})
	});
}
//#endregion
//#region src/workspace/accordion.tsx
var CLASS_NAME$2 = "reactodia-accordion";
var Accordion = class extends React.Component {
	element;
	items = [];
	dragOrigin;
	defaultProps;
	constructor(props) {
		super(props);
		const childCount = React.Children.count(this.props.children);
		this.state = {
			sizes: [],
			collapsed: [],
			resizing: true,
			percents: React.Children.map(this.props.children, () => `${100 / childCount}%`)
		};
	}
	componentDidMount() {
		this.updateSizes();
	}
	componentDidUpdate(prevProps, prevState) {
		const { direction, children, onResize, animationDuration } = this.props;
		if (React.Children.count(children) !== React.Children.count(prevProps.children)) this.updateSizes();
		const { sizes, resizing } = this.state;
		if (sizes !== prevState.sizes && onResize) {
			const duration = animationDuration ?? getAnimationDuration(this.element);
			if (resizing || !duration) onResize(direction);
			else setTimeout(() => onResize(direction), duration);
		}
	}
	get isVertical() {
		return this.props.direction === "vertical";
	}
	clientSize(element) {
		const { clientWidth, clientHeight } = element;
		return this.isVertical ? clientHeight : clientWidth;
	}
	offsetSize(element) {
		const { offsetWidth, offsetHeight } = element;
		return this.isVertical ? offsetHeight : offsetWidth;
	}
	updateSizes() {
		const { children } = this.props;
		const defaultProps = /* @__PURE__ */ new Map();
		React.Children.forEach(children, (child, index) => {
			if (typeof child !== "object") return;
			if (child) {
				const { defaultSize, defaultCollapsed, collapsedSize, minSize } = child.props;
				defaultProps.set(index, {
					defaultSize,
					defaultCollapsed,
					collapsedSize,
					minSize
				});
			} else defaultProps.set(index, {});
		});
		this.defaultProps = defaultProps;
		this.setState(({ collapsed }) => {
			const totalSize = this.clientSize(this.element.parentElement);
			let leftSize = totalSize;
			let leftChildCount = React.Children.count(children);
			const newCollapsed = [];
			this.defaultProps.forEach(({ defaultSize, defaultCollapsed = false }, index) => {
				let itemCollapsed = collapsed[index] === void 0 ? defaultCollapsed : collapsed[index];
				const isLastItem = index === this.defaultProps.size - 1;
				const noExpandedItems = newCollapsed.findIndex((c) => !c) < 0;
				if (isLastItem && noExpandedItems) itemCollapsed = false;
				const size = itemCollapsed ? this.sizeWhenCollapsed(index) : defaultSize;
				if (size) {
					leftSize = leftSize - size;
					leftChildCount = leftChildCount - 1;
				}
				newCollapsed.push(itemCollapsed);
			});
			const newSizes = [];
			const newPercents = [];
			this.defaultProps.forEach(({ defaultSize, minSize }, index) => {
				let size = leftSize / leftChildCount;
				const collapsedSize = this.sizeWhenCollapsed(index);
				if (newCollapsed[index]) size = collapsedSize;
				else if (defaultSize !== void 0) size = defaultSize;
				else if (minSize !== void 0 && size < minSize) size = collapsedSize + minSize;
				if (size < collapsedSize) {
					size = collapsedSize;
					newCollapsed[index] = true;
				}
				const percent = `${100 * size / totalSize}%`;
				newSizes.push(size);
				newPercents.push(percent);
			});
			return {
				sizes: newSizes,
				percents: newPercents,
				collapsed: newCollapsed
			};
		}, () => {
			this.setState({ resizing: false });
		});
	}
	render() {
		const { className, style, direction } = this.props;
		const { resizing } = this.state;
		return /* @__PURE__ */ jsx("div", {
			ref: this.onElementMount,
			className: cx(CLASS_NAME$2, `${CLASS_NAME$2}--${direction}`, resizing ? `${CLASS_NAME$2}--resizing` : void 0, className),
			style,
			children: this.renderItems()
		});
	}
	onElementMount = (element) => {
		this.element = element;
	};
	renderItems() {
		const { sizes, percents, collapsed } = this.state;
		const { children, direction } = this.props;
		return React.Children.map(children, (child, index) => {
			if (typeof child !== "object") throw new Error("Reactodia: Accordion should have only AccordionItem elements as children");
			else if (child === null) return child;
			const lastChild = index === React.Children.count(children) - 1;
			const size = collapsed[index] ? sizes[index] : percents[index];
			const additionalProps = {
				ref: (element) => {
					this.items[index] = element;
				},
				collapsed: collapsed[index],
				size,
				direction,
				onChangeCollapsed: (itemCollapsed) => this.onItemChangeCollapsed({
					itemIndex: index,
					itemCollapsed
				}),
				onBeginDragHandle: lastChild ? void 0 : () => this.onBeginDragHandle(index),
				onDragHandle: lastChild ? void 0 : (dx, dy) => this.onDragHandle(index, dx, dy),
				onEndDragHandle: this.onEndDragHandle
			};
			return React.cloneElement(child, additionalProps);
		});
	}
	onBeginDragHandle(itemIndex) {
		this.dragOrigin = {
			totalSize: this.clientSize(this.element),
			sizes: this.computeEffectiveItemSizes(),
			collapsed: [...this.state.collapsed]
		};
		this.setState({ resizing: true }, () => {
			const { direction, onStartResize } = this.props;
			if (onStartResize) onStartResize(direction);
		});
	}
	onEndDragHandle = () => {
		this.setState({ resizing: false }, () => this.tryCollapseTooSmallItem());
	};
	tryCollapseTooSmallItem() {
		const collapsedIndex = this.state.sizes.findIndex((size, index) => {
			const defaults = this.defaultProps.get(index);
			return defaults && defaults.minSize && size < defaults.minSize;
		});
		if (collapsedIndex >= 0) this.onItemChangeCollapsed({
			itemIndex: collapsedIndex,
			itemCollapsed: true
		});
	}
	computeEffectiveItemSizes() {
		const sizes = [];
		this.items.forEach((item, index) => {
			if (!item) return;
			if (this.state.collapsed[index]) {
				const collapsedSize = this.sizeWhenCollapsed(index);
				sizes.push(collapsedSize);
			} else sizes.push(this.offsetSize(item.element));
		});
		return sizes;
	}
	sizeWhenCollapsed = (index) => {
		const item = this.items[index];
		if (!item) return 0;
		const { collapsedSize } = this.defaultProps.get(index);
		if (collapsedSize !== void 0) return collapsedSize;
		return (item.header ? this.clientSize(item.header) : 0) + (this.offsetSize(item.element) - this.clientSize(item.element));
	};
	onDragHandle(itemIndex, dx, dy) {
		const sizes = [...this.dragOrigin.sizes];
		const collapsed = [...this.dragOrigin.collapsed];
		const originTotalSize = this.dragOrigin.totalSize;
		new SizeDistributor(sizes, collapsed, originTotalSize, this.sizeWhenCollapsed).distribute(itemIndex + 1, this.isVertical ? dy : dx);
		const percents = sizes.map((size) => `${100 * size / originTotalSize}%`);
		this.setState({
			sizes,
			percents,
			collapsed
		});
	}
	onItemChangeCollapsed({ itemIndex, itemCollapsed }) {
		const totalSize = this.clientSize(this.element);
		const sizes = this.computeEffectiveItemSizes();
		if (sizes.length === 1) return;
		const collapsed = [...this.state.collapsed];
		const effectiveSize = sizes[itemIndex];
		const collapsedSize = this.sizeWhenCollapsed(itemIndex);
		const distributor = new SizeDistributor(sizes, collapsed, totalSize, this.sizeWhenCollapsed);
		if (itemCollapsed) {
			const splitShift = Math.max(effectiveSize - collapsedSize, 0);
			sizes[itemIndex] = collapsedSize;
			if (itemIndex === sizes.length - 1) distributor.expand(splitShift, itemIndex - 1);
			else distributor.expand(splitShift, itemIndex + 1);
		} else {
			const { defaultSize, minSize } = this.defaultProps.get(itemIndex);
			const shift = (defaultSize || totalSize / sizes.length) - collapsedSize;
			let freeSize = 0;
			if (itemIndex === sizes.length - 1) freeSize = distributor.collapseForward({
				shift,
				from: itemIndex - 1,
				to: itemIndex
			});
			else freeSize = distributor.collapseForward({
				shift,
				from: itemIndex + 1,
				to: sizes.length
			});
			freeSize = Math.max(freeSize, distributor.leftoverSize());
			if (freeSize < shift) freeSize += distributor.collapseForward({
				shift: shift - freeSize,
				from: 0,
				to: itemIndex
			});
			if (minSize !== void 0 && freeSize < minSize) freeSize = minSize;
			sizes[itemIndex] = Math.round(collapsedSize + freeSize);
		}
		collapsed[itemIndex] = itemCollapsed;
		const percents = sizes.map((size) => `${100 * size / totalSize}%`);
		this.setState({
			sizes,
			percents,
			collapsed
		});
	}
};
var SizeDistributor = class {
	sizes;
	collapsed;
	totalSize;
	sizeWhenCollapsed;
	constructor(sizes, collapsed, totalSize, sizeWhenCollapsed) {
		this.sizes = sizes;
		this.collapsed = collapsed;
		this.totalSize = totalSize;
		this.sizeWhenCollapsed = sizeWhenCollapsed;
	}
	distribute(splitIndex, splitShift) {
		if (splitShift > 0) {
			let freeSize = this.collapseForward({
				shift: splitShift,
				from: splitIndex,
				to: this.sizes.length
			});
			freeSize = Math.max(freeSize, this.leftoverSize());
			this.expand(freeSize, splitIndex - 1);
		} else {
			let freeSize = this.collapseBackward({
				shift: -splitShift,
				from: 0,
				to: splitIndex
			});
			freeSize = Math.max(freeSize, this.leftoverSize());
			this.expand(freeSize, splitIndex);
		}
	}
	collapseForward({ shift, from, to }) {
		if (shift <= 0) return 0;
		let shiftLeft = shift;
		for (let i = from; i < to; i++) shiftLeft = this.collapse(shiftLeft, i);
		return shift - shiftLeft;
	}
	collapseBackward({ shift, from, to }) {
		if (shift <= 0) return 0;
		let shiftLeft = shift;
		for (let i = to - 1; i >= from; i--) shiftLeft = this.collapse(shiftLeft, i);
		return shift - shiftLeft;
	}
	collapse(shift, index) {
		if (this.collapsed[index]) return shift;
		const size = this.sizes[index];
		const collapsedSize = this.sizeWhenCollapsed(index);
		const newSize = Math.round(Math.max(size - shift, collapsedSize));
		this.sizes[index] = newSize;
		this.collapsed[index] = newSize <= collapsedSize;
		return shift - (size - newSize);
	}
	expand(shift, index) {
		if (shift <= 0) return 0;
		const oldSize = this.sizes[index];
		const collapsedSize = this.sizeWhenCollapsed(index);
		const newSize = Math.round(oldSize + shift);
		this.sizes[index] = newSize;
		this.collapsed[index] = newSize <= collapsedSize;
		return newSize - oldSize;
	}
	leftoverSize() {
		const sizeSum = this.sizes.reduce((sum, size) => sum + size, 0);
		return Math.max(this.totalSize - sizeSum, 0);
	}
};
function getAnimationDuration(element) {
	if (element) {
		const value = getComputedStyle(element).getPropertyValue("--reactodia-accordion-transition-duration");
		const match = /^([0-9.]+)(s|ms)$/.exec(value);
		if (match) {
			const duration = Number(match[1]);
			const unit = match[2] === "s" ? 1e3 : 1;
			return Number.isFinite(duration) ? duration * unit : void 0;
		}
	}
}
//#endregion
//#region src/workspace/accordionItem.tsx
var DockSide = /* @__PURE__ */ function(DockSide) {
	DockSide[DockSide["Left"] = 1] = "Left";
	DockSide[DockSide["Right"] = 2] = "Right";
	return DockSide;
}({});
var CLASS_NAME$1 = "reactodia-accordion-item";
var AccordionItem = class AccordionItem extends React.Component {
	static ARROW_SHIFT_STEP = 50;
	_element = React.createRef();
	_header = React.createRef();
	constructor(props) {
		super(props);
		this.state = { resizing: false };
	}
	get element() {
		return this._element.current;
	}
	get header() {
		return this._header.current;
	}
	get isVertical() {
		return this.props.direction === "vertical";
	}
	render() {
		const { ariaLabel, heading, className, style, bodyClassName, children, bodyRef, collapsed, size, direction, dockSide, onBeginDragHandle, onDragHandle, onEndDragHandle } = this.props;
		const { resizing } = this.state;
		const shouldRenderHandle = onBeginDragHandle && onDragHandle && onEndDragHandle;
		const providedStyle = this.isVertical ? { height: size } : {
			width: size,
			"--reactodia-accordion-item-size": typeof size === "number" ? `${size}px` : void 0
		};
		const isMounted = !(collapsed && dockSide);
		return /* @__PURE__ */ jsxs("section", {
			ref: this._element,
			className: cx(CLASS_NAME$1, collapsed ? `${CLASS_NAME$1}--collapsed` : `${CLASS_NAME$1}--expanded`, `${CLASS_NAME$1}--${direction}`, resizing ? `${CLASS_NAME$1}--resizing` : void 0, className),
			style: {
				...style,
				...providedStyle
			},
			"aria-label": ariaLabel ?? (typeof heading === "string" ? heading : void 0),
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: `${CLASS_NAME$1}__inner`,
					children: [heading ? /* @__PURE__ */ jsx("button", {
						className: `${CLASS_NAME$1}__header`,
						ref: this._header,
						onClick: () => this.props.onChangeCollapsed(!collapsed),
						children: heading
					}) : null, /* @__PURE__ */ jsx("div", {
						className: `${CLASS_NAME$1}__body`,
						children: children && isMounted ? children : /* @__PURE__ */ jsx("div", {
							ref: bodyRef,
							className: bodyClassName
						})
					})]
				}),
				shouldRenderHandle ? /* @__PURE__ */ jsx(DraggableHandle, {
					role: "separator",
					className: cx(`${CLASS_NAME$1}__handle`, `${CLASS_NAME$1}__handle-${direction}`),
					tabIndex: 0,
					"aria-valuenow": typeof size === "string" && size.endsWith("%") ? parseFloat(size) : 0,
					onBeginDragHandle: (e) => {
						this.setState({ resizing: true });
						onBeginDragHandle();
					},
					onDragHandle: (e, x, y) => onDragHandle(x, y),
					onEndDragHandle: (e) => {
						this.setState({ resizing: false });
						onEndDragHandle();
					},
					onKeyDown: (e) => {
						const step = AccordionItem.ARROW_SHIFT_STEP;
						let shift = 0;
						if (this.isVertical) {
							if (e.key === "ArrowUp") shift -= step;
							else if (e.key === "ArrowDown") shift += step;
						} else if (e.key === "ArrowLeft") shift -= step;
						else if (e.key === "ArrowRight") shift += step;
						if (shift !== 0) {
							e.preventDefault();
							onBeginDragHandle();
							onDragHandle(shift, shift);
							onEndDragHandle();
						}
					}
				}) : null,
				this.renderToggleButton()
			]
		});
	}
	renderToggleButton() {
		const { collapsed, dockSide, titleDockExpand, titleDockCollapse, onChangeCollapsed } = this.props;
		if (!dockSide) return null;
		const side = dockSide === 1 ? "left" : "right";
		const label = collapsed ? titleDockExpand : titleDockCollapse;
		return /* @__PURE__ */ jsx("button", {
			className: cx(`${CLASS_NAME$1}__handle-btn`, `${CLASS_NAME$1}__handle-btn-${side}`),
			onClick: () => onChangeCollapsed(!collapsed),
			"aria-label": label,
			title: label
		});
	}
};
//#endregion
//#region src/workspace/workspaceLayout.tsx
var DEFAULT_HORIZONTAL_COLLAPSED_SIZE = 28;
/**
* Layout component to display a row of resizable sub-components.
*
* This component accepts only layout rows, columns or items as children.
*
* @category Components
* @see {@link WorkspaceLayoutColumn}
* @see {@link WorkspaceLayoutItem}
*/
function WorkspaceLayoutRow(props) {
	return /* @__PURE__ */ jsx(WorkspaceContainer, {
		...props,
		type: "row"
	});
}
/**
* Layout component to display a column of resizable sub-components.
*
* This component accepts only layout rows, columns or items as children.
*
* @category Components
* @see {@link WorkspaceLayoutRow}
* @see {@link WorkspaceLayoutItem}
*/
function WorkspaceLayoutColumn(props) {
	return /* @__PURE__ */ jsx(WorkspaceContainer, {
		...props,
		type: "column"
	});
}
/**
* Layout component to display an item withing a layout row or columns.
*
* The item may be collapsed, in that case the children will not be rendered.
*
* @category Components
* @see {@link WorkspaceLayoutRow}
* @see {@link WorkspaceLayoutColumn}
*/
function WorkspaceLayoutItem(props) {
	return props.children;
}
/**
* React context with handlers for layout component events.
*/
var WorkspaceLayoutResizeContext = React.createContext(null);
function WorkspaceContainer(props) {
	const { type, className, style, animationDuration, children } = props;
	const resizeContext = React.useContext(WorkspaceLayoutResizeContext);
	const t = React.useContext(TranslationContext);
	const childCount = React.Children.count(children);
	const items = React.Children.map(children, (child, index) => {
		if (child === null) return child;
		const childId = child.props.id ?? `${type}-child-${index}`;
		let dockSide;
		if (type === "row" && !child.props.undocked) {
			if (index === 0) dockSide = DockSide.Left;
			else if (index === childCount - 1) dockSide = DockSide.Right;
		}
		let collapsedSize = child.props.collapsedSize;
		if (collapsedSize === void 0 && type === "row") collapsedSize = DEFAULT_HORIZONTAL_COLLAPSED_SIZE;
		return /* @__PURE__ */ jsx(AccordionItem, {
			id: childId,
			className: child.type === WorkspaceLayoutItem ? child.props.className : void 0,
			style: child.type === WorkspaceLayoutItem ? child.props.style : void 0,
			ariaLabel: child.type === WorkspaceLayoutItem ? child.props["aria-label"] : void 0,
			heading: child.type === WorkspaceLayoutItem ? child.props.heading : void 0,
			dockSide,
			titleDockExpand: t?.textOptional("workspace_layout.toggle_expand.title"),
			titleDockCollapse: t?.textOptional("workspace_layout.toggle_collapse.title"),
			defaultSize: child.props.defaultSize,
			defaultCollapsed: child.props.defaultCollapsed,
			collapsedSize,
			minSize: child.props.minSize,
			children: child
		}, childId);
	});
	return /* @__PURE__ */ jsx(Accordion, {
		className,
		style,
		direction: type === "row" ? "horizontal" : "vertical",
		onStartResize: resizeContext?.onStartResize,
		onResize: resizeContext?.onResize ?? (() => void 0),
		animationDuration,
		children: items
	});
}
//#endregion
//#region src/workspace/workspaceRoot.tsx
var CLASS_NAME = "reactodia-workspace";
/**
* Component to establish inheritable style defaults for the workspace.
*
* @category Components
*/
function WorkspaceRoot(props) {
	const { colorScheme = "auto" } = props;
	const outerApi = React.useContext(ColorSchemeApi);
	const outerColorScheme = React.useContext(ColorSchemeContext);
	const preferredColorScheme = usePreferredColorScheme(!outerColorScheme && colorScheme === "auto");
	let effectiveColorScheme = colorScheme;
	if (effectiveColorScheme === "auto") effectiveColorScheme = outerColorScheme ?? (preferredColorScheme === "dark" ? "dark" : "light");
	const [actedColorScheme, setActedColorScheme] = React.useState();
	if (actedColorScheme !== void 0) effectiveColorScheme = actedColorScheme;
	const colorSchemeApi = React.useMemo(() => outerApi.defined ? outerApi : {
		defined: true,
		actInColorScheme: (scheme, action) => {
			ReactDOM.flushSync(() => setActedColorScheme(scheme));
			action();
			ReactDOM.flushSync(() => setActedColorScheme(void 0));
		}
	}, [outerApi, setActedColorScheme]);
	return /* @__PURE__ */ jsx(ColorSchemeApi.Provider, {
		value: colorSchemeApi,
		children: /* @__PURE__ */ jsx(ColorSchemeContext.Provider, {
			value: effectiveColorScheme,
			children: /* @__PURE__ */ jsx("div", {
				className: cx(CLASS_NAME, props.className, effectiveColorScheme === "dark" ? "reactodia-workspace-dark" : "reactodia-workspace-light"),
				style: props.style,
				"data-theme": effectiveColorScheme,
				children: props.children
			})
		})
	});
}
function usePreferredColorScheme(track) {
	const html = document.documentElement;
	const [theme, setTheme] = React.useState(() => html.getAttribute("data-theme") ?? (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : ""));
	React.useLayoutEffect(() => {
		if (track) {
			const preferDarkTheme = window.matchMedia("(prefers-color-scheme: dark)");
			const updateTheme = () => {
				setTheme(html.getAttribute("data-theme") ?? (preferDarkTheme.matches ? "dark" : ""));
			};
			preferDarkTheme.addEventListener("change", updateTheme);
			const observer = new MutationObserver(updateTheme);
			if (html) observer.observe(html, {
				attributes: true,
				attributeFilter: ["data-theme"]
			});
			return () => {
				preferDarkTheme.removeEventListener("change", updateTheme);
				observer.disconnect();
			};
		}
	}, [html, track]);
	return theme;
}
//#endregion
//#region src/workspace/classicWorkspace.tsx
/**
* Component with default ready-to-use workspace with a canvas and
* all components and widgets pre-configured (classic layout).
*
* @category Components
*/
function ClassicWorkspace(props) {
	const { className, style, colorScheme, children, leftColumn, rightColumn, canvas, canvasWidgets, annotations, connectionsMenu, dropOnCanvas, halo, haloLink, selection, navigator, zoomControl, visualAuthoring, toolbar, classTree, instancesSearch, linkToolbox } = props;
	const t = useTranslation();
	return /* @__PURE__ */ jsx(WorkspaceRoot, {
		className,
		style,
		colorScheme,
		children: /* @__PURE__ */ jsxs(WorkspaceLayoutRow, { children: [
			leftColumn === null ? null : /* @__PURE__ */ jsxs(WorkspaceLayoutColumn, {
				defaultSize: 275,
				...leftColumn,
				children: [/* @__PURE__ */ jsx(WorkspaceLayoutItem, {
					id: "classes",
					heading: t.text("classic_workspace.class_tree.heading"),
					children: /* @__PURE__ */ jsx(ClassTree, { ...classTree })
				}), /* @__PURE__ */ jsx(WorkspaceLayoutItem, {
					id: "instances",
					heading: t.text("classic_workspace.instances.heading"),
					children: /* @__PURE__ */ jsx(InstancesSearch, { ...instancesSearch })
				})]
			}),
			/* @__PURE__ */ jsx(WorkspaceLayoutItem, {
				id: "canvas",
				"aria-label": t.text("classic_workspace.canvas.label"),
				children: /* @__PURE__ */ jsxs(Canvas, {
					...canvas,
					children: [
						visualAuthoring ? /* @__PURE__ */ jsx(VisualAuthoring, { ...visualAuthoring }) : null,
						annotations === null ? null : /* @__PURE__ */ jsx(AnnotationSupport, { ...annotations }),
						connectionsMenu === null ? null : /* @__PURE__ */ jsx(ConnectionsMenu, { ...connectionsMenu }),
						dropOnCanvas === null ? null : /* @__PURE__ */ jsx(DropOnCanvas, { ...dropOnCanvas }),
						halo === null ? null : /* @__PURE__ */ jsx(Halo, { ...halo }),
						haloLink === null ? null : /* @__PURE__ */ jsx(HaloLink, { ...haloLink }),
						selection === null ? null : /* @__PURE__ */ jsx(Selection, { ...selection }),
						toolbar === null ? null : /* @__PURE__ */ jsx(ClassicToolbar, {
							dock: "nw",
							...toolbar
						}),
						zoomControl === null ? null : /* @__PURE__ */ jsx(ZoomControl, {
							dock: "sw",
							...zoomControl
						}),
						navigator === null ? null : /* @__PURE__ */ jsx(Navigator, {
							dock: "se",
							...navigator
						}),
						canvasWidgets,
						children
					]
				})
			}),
			rightColumn === null ? null : /* @__PURE__ */ jsx(WorkspaceLayoutColumn, {
				defaultSize: 275,
				defaultCollapsed: true,
				...rightColumn,
				children: /* @__PURE__ */ jsx(WorkspaceLayoutItem, {
					id: "connections",
					heading: t.text("classic_workspace.connections.heading"),
					children: /* @__PURE__ */ jsx(LinkTypesToolbox, { ...linkToolbox })
				})
			})
		] })
	});
}
/**
* Canvas widget component to display a toolbar with defaults
* for the classic workspace layout.
*
* @category Components
* @see {@link ClassicWorkspace}
*/
function ClassicToolbar(props) {
	const { dock, dockOffsetX, dockOffsetY, menu, children, languages = [] } = props;
	return /* @__PURE__ */ jsx(Toolbar, {
		dock,
		dockOffsetX,
		dockOffsetY,
		menu: menu === null ? null : menu ?? /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsx(ToolbarActionClearAll, {}),
			/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "exportRaster" }),
			/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "exportSvg" }),
			/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "print" })
		] }),
		children: children === null ? null : children ?? /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsx(ToolbarActionUndo, {}),
			/* @__PURE__ */ jsx(ToolbarActionRedo, {}),
			/* @__PURE__ */ jsx(ToolbarActionLayout, {}),
			/* @__PURE__ */ jsx(ToolbarLanguageSelector, { languages })
		] })
	});
}
//#endregion
//#region src/workspace/defaultWorkspace.tsx
/**
* Component with default ready-to-use workspace with a canvas and
* all components and widgets pre-configured.
*
* @category Components
*/
function DefaultWorkspace(props) {
	const { className, style, colorScheme, children, canvas, canvasWidgets, annotations, connectionsMenu, dropOnCanvas, halo, haloLink, selection, navigator, visualAuthoring, zoomControl, menu, search, actions, mainToolbar, actionsToolbar, languages = [] } = props;
	const t = useTranslation();
	const menuContent = menu === null ? null : menu ?? /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(ToolbarActionClearAll, {}),
		/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "exportRaster" }),
		/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "exportSvg" }),
		/* @__PURE__ */ jsx(ToolbarActionExport, { kind: "print" })
	] });
	const actionsContent = actions === null ? null : actions ?? /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(ToolbarActionUndo, {}),
		/* @__PURE__ */ jsx(ToolbarActionRedo, {}),
		/* @__PURE__ */ jsx(ToolbarActionLayout, {}),
		/* @__PURE__ */ jsx(ToolbarLanguageSelector, { languages })
	] });
	const defaultSections = React.useMemo(() => [
		{
			key: "elementTypes",
			label: t.text("default_workspace.search_section_entity_types.label"),
			title: t.text("default_workspace.search_section_entity_types.title"),
			component: /* @__PURE__ */ jsx(SearchSectionElementTypes, {})
		},
		{
			key: "entities",
			label: t.text("default_workspace.search_section_entities.label"),
			title: t.text("default_workspace.search_section_entities.title"),
			component: /* @__PURE__ */ jsx(SearchSectionEntities, {})
		},
		{
			key: "linkTypes",
			label: t.text("default_workspace.search_section_link_types.label"),
			title: t.text("default_workspace.search_section_link_types.title"),
			component: /* @__PURE__ */ jsx(SearchSectionLinkTypes, {})
		}
	], []);
	const menuDock = mainToolbar?.dock ?? "nw";
	const menuDropUp = menuDock === "s" || menuDock === "sw" || menuDock === "se";
	return /* @__PURE__ */ jsx(WorkspaceRoot, {
		className,
		style,
		colorScheme,
		children: /* @__PURE__ */ jsxs(Canvas, {
			...canvas,
			children: [
				visualAuthoring ? /* @__PURE__ */ jsx(VisualAuthoring, { ...visualAuthoring }) : null,
				annotations === null ? null : /* @__PURE__ */ jsx(AnnotationSupport, { ...annotations }),
				connectionsMenu === null ? null : /* @__PURE__ */ jsx(ConnectionsMenu, { ...connectionsMenu }),
				dropOnCanvas === null ? null : /* @__PURE__ */ jsx(DropOnCanvas, { ...dropOnCanvas }),
				halo === null ? null : /* @__PURE__ */ jsx(Halo, { ...halo }),
				haloLink === null ? null : /* @__PURE__ */ jsx(HaloLink, { ...haloLink }),
				selection === null ? null : /* @__PURE__ */ jsx(Selection, { ...selection }),
				/* @__PURE__ */ jsx(Toolbar, {
					...mainToolbar,
					dock: menuDock,
					menu: menuContent,
					children: search === null ? null : /* @__PURE__ */ jsx(UnifiedSearch, {
						...search,
						direction: search?.direction ?? (menuDropUp ? "up" : "down"),
						sections: search?.sections ?? defaultSections
					})
				}),
				zoomControl === null ? null : /* @__PURE__ */ jsx(ZoomControl, {
					dock: "w",
					...zoomControl
				}),
				actionsContent === null ? null : /* @__PURE__ */ jsx(Toolbar, {
					...actionsToolbar,
					dock: actionsToolbar?.dock ?? "sw",
					menu: null,
					children: actionsContent
				}),
				navigator === null ? null : /* @__PURE__ */ jsx(Navigator, {
					dock: "se",
					...navigator
				}),
				canvasWidgets,
				children
			]
		})
	});
}
//#endregion
//#region src/workspace/workspaceProvider.tsx
var DEFAULT_LANGUAGE = "en";
var DEFAULT_TYPE_STYLE_RESOLVER = (types) => void 0;
var TYPE_STYLE_COLOR_SEED = 195935983;
/**
* Creates standalone workspace context which stores graph data and provides
* means to display and interact with the diagram.
*
* @see {@link WorkspaceProvider}
*/
function createWorkspace(params) {
	return new RefCountedWorkspaceContext(params);
}
/**
* Top-level component to mount and provide specified workspace context
* to the child UI components.
*
* @category Components
* @see {@link createWorkspace}
*/
function WorkspaceProvider(props) {
	const { workspace, onMount, children } = props;
	React.useEffect(() => {
		return workspace.mount();
	}, [workspace]);
	React.useEffect(() => {
		if (onMount) {
			onMount({ getContext: () => workspace });
			return () => onMount(null);
		}
	}, [onMount]);
	return /* @__PURE__ */ jsx(TranslationProvider, {
		translation: workspace.translation,
		children: /* @__PURE__ */ jsx(WorkspaceContext.Provider, {
			value: workspace,
			children
		})
	});
}
var RefCountedWorkspaceContext = class {
	refCount = 0;
	cancellation = new AbortController();
	extensionCommands = /* @__PURE__ */ new WeakMap();
	resolveTypeStyle;
	cachedTypeStyles;
	cachedGroupStyles;
	annotationStyles = new Map([
		["default", { color: "#bec3c9" }],
		["primary", { color: "#337ab7" }],
		["success", { color: "#5cb85c" }],
		["info", { color: "#54c7ec" }],
		["warning", { color: "#ffba00" }],
		["danger", { color: "#c9302c" }]
	]);
	layoutTypeProvider;
	model;
	view;
	editor;
	overlay;
	translation;
	triggerWorkspaceEvent;
	constructor(params) {
		const { translation = new DefaultTranslation({ bundles: [DefaultTranslationBundle] }), history = new InMemoryHistory(), dialogSettingsProvider = new DefaultDialogSettingsProvider(), metadataProvider, validationProvider, renameLinkProvider, typeStyleResolver, defaultLanguage = DEFAULT_LANGUAGE, defaultLayout, onWorkspaceEvent = () => {} } = params;
		this.translation = translation;
		this.resolveTypeStyle = typeStyleResolver ?? DEFAULT_TYPE_STYLE_RESOLVER;
		this.cachedTypeStyles = /* @__PURE__ */ new WeakMap();
		this.cachedGroupStyles = /* @__PURE__ */ new WeakMap();
		this.model = new DataDiagramModel({
			history,
			translation: this.translation
		});
		this.model.setLanguage(defaultLanguage);
		this.view = new SharedCanvasState({
			defaultElementResolver: (element) => {
				if (element instanceof AnnotationElement) return NoteTemplate;
				return StandardTemplate;
			},
			defaultLinkResolver: (link) => {
				if (link instanceof AnnotationLink) return NoteLinkTemplate;
				return StandardLinkTemplate;
			},
			defaultLayout,
			renameLinkProvider: renameLinkProvider === null ? void 0 : renameLinkProvider ?? new DefaultRenameLinkProvider()
		});
		this.editor = new EditorController({
			model: this.model,
			translation: this.translation,
			getDisposeSignal: () => this.disposeSignal,
			metadataProvider,
			validationProvider
		});
		this.overlay = new OverlayController({
			model: this.model,
			view: this.view,
			translation: this.translation,
			dialogSettingsProvider
		});
		this.triggerWorkspaceEvent = onWorkspaceEvent;
		this.layoutTypeProvider = { getElementTypes: (element) => {
			if (element instanceof EntityElement) return element.data.types;
			return [];
		} };
		const listener = new EventObserver();
		listener.listen(this.model.events, "loadingSuccess", () => {
			for (const canvas of this.view.findAllCanvases()) {
				canvas.renderingState.syncUpdate();
				canvas.zoomToFit();
			}
		});
		if (onWorkspaceEvent) {
			listener.listen(this.model.events, "changeSelection", () => onWorkspaceEvent(WorkspaceEventKey.editorChangeSelection));
			listener.listen(this.overlay.events, "changeOpenedDialog", () => onWorkspaceEvent(WorkspaceEventKey.editorToggleDialog));
		}
	}
	mount() {
		this.refCount++;
		let mounted = true;
		return () => {
			if (mounted) {
				mounted = false;
				this.refCount--;
				if (this.refCount === 0) {
					this.view.dispose();
					this.cancellation.abort();
					this.cancellation = new AbortController();
				}
			}
		};
	}
	get disposeSignal() {
		return this.cancellation.signal;
	}
	getCommandBus = (extension) => {
		let commands = this.extensionCommands.get(extension);
		if (!commands) {
			commands = new EventSource();
			this.extensionCommands.set(extension, commands);
		}
		return commands;
	};
	getElementStyle = (element) => {
		if (element instanceof EntityElement) return this.getElementTypeStyle(element.data.types);
		else if (element instanceof EntityGroup) return this.getGroupTypeStyle(element);
		else return this.tryGetColorVariantStyle(element) ?? this.getElementTypeStyle([]);
	};
	getElementTypeStyle = (types) => {
		let processedStyle = this.cachedTypeStyles.get(types);
		if (!processedStyle) {
			const customStyle = this.resolveTypeStyle(types);
			let color;
			if (customStyle && customStyle.color) color = hcl(customStyle.color).toString();
			else color = hcl(getHueFromClasses(types, TYPE_STYLE_COLOR_SEED), 40, 75).toString();
			processedStyle = {
				...customStyle,
				color
			};
			this.cachedTypeStyles.set(types, processedStyle);
		}
		return processedStyle;
	};
	getGroupTypeStyle(group) {
		const { items } = group;
		let processedStyle = this.cachedGroupStyles.get(items);
		if (!processedStyle) {
			const countPerTypes = new HashMap(hashTypeIris, shallowArrayEqual);
			for (const item of items) countPerTypes.set(item.data.types, (countPerTypes.get(item.data.types) ?? 0) + 1);
			let maxCount = 0;
			let typesAtMax = [];
			for (const [types, count] of countPerTypes) if (count > maxCount) {
				maxCount = count;
				typesAtMax = types;
			}
			processedStyle = this.getElementTypeStyle(typesAtMax);
			this.cachedGroupStyles.set(items, processedStyle);
		}
		return processedStyle;
	}
	tryGetColorVariantStyle(element) {
		const { elementState } = element;
		const colorVariant = elementState.get(TemplateProperties.ColorVariant);
		if (colorVariant || element instanceof AnnotationElement) return this.annotationStyles.get(colorVariant ?? "default");
	}
	performLayout = async (params) => {
		const { canvas: targetCanvas, layoutFunction, selectedElements, fixedElements, animate, signal, zoomToFit = true } = params;
		const { model, view, overlay, disposeSignal } = this;
		const t = this.translation;
		const canvas = targetCanvas ?? view.findAnyCanvas();
		if (!canvas) throw new Error("Failed to find any canvas to perform layout");
		canvas.renderingState.syncUpdate();
		const task = overlay.startTask({
			title: t.text("workspace.perform_layout.task"),
			delay: 200
		});
		let calculatedLayout;
		try {
			calculatedLayout = await calculateLayout({
				layoutFunction: layoutFunction ?? view.defaultLayout,
				model,
				selectedElements,
				fixedElements,
				sizeProvider: canvas.renderingState,
				typeProvider: this.layoutTypeProvider,
				signal: signal ?? disposeSignal
			});
		} catch (err) {
			task.setError(err);
			console.error("Failed to compute graph layout", err);
			return;
		} finally {
			task.end();
		}
		const batch = model.history.startBatch(TranslatedText.text("workspace.perform_layout.command"));
		batch.history.registerToUndo(RestoreGeometry.capture(model));
		for (const link of model.links) link.setVertices([]);
		batch.history.registerToUndo(restoreViewport(canvas));
		if (animate) await Promise.all([canvas.animateGraph(() => {
			applyLayout(calculatedLayout, model);
			batch.store();
		}), zoomToFit ? canvas.zoomToFit({ animate: true }) : null]);
		else {
			applyLayout(calculatedLayout, model);
			batch.store();
			if (zoomToFit) await canvas.zoomToFit();
		}
	};
	group = (params) => {
		return groupEntities(this, params);
	};
	ungroupAll = (params) => {
		return ungroupAllEntities(this, params);
	};
	ungroupSome = (params) => {
		return ungroupSomeEntities(this, params);
	};
};
function getHueFromClasses(types, seed) {
	const hash = hashTypeIris(types, seed);
	return 360 * ((hash === void 0 ? 0 : hash) / 2147483647);
}
function hashTypeIris(types, seed = 0) {
	let hash = seed | 0;
	for (const name of types) hash = chainHash(hash, hashString(name, hash));
	return hash | 0;
}
/**
* React hook to perform asynchronous initialization of the workspace.
*
* This function could be used to setup data provider, fetch initial data
* or import existing diagram layout.
*
* The command history is automatically reset when the initialization is done.
*
* **Example**:
* ```ts
* const workspace = React.useState(() => Reactodia.createWorkspace({...}));
* const {onMount} = Reactodia.useLoadedWorkspace(async ({context, signal}) => {
*     // ...
* });
* return (
*     <Reactodia.WorkspaceProvider
*         workspace={workspace}
*         onMount={onMount}>
*         ...
*     </Reactodia.Workspace>
* );
* ```
* 
* @category Hooks
*/
function useLoadedWorkspace(onLoad, deps) {
	const [context, setContext] = React.useState();
	const stateRef = React.useRef(void 0);
	if (stateRef.current) stateRef.current.latestOnLoad = onLoad;
	else {
		const state = {
			latestOnLoad: onLoad,
			context: void 0,
			loadedWorkspace: {
				getContext: () => {
					if (!state.context) throw new Error("Cannot get Reactodia Workspace context: it is not mounted yet");
					return state.context;
				},
				onMount: (workspace) => {
					const context = workspace?.getContext();
					state.context = context;
					setContext(context);
				}
			}
		};
		stateRef.current = state;
	}
	React.useEffect(() => {
		if (context) {
			const latestOnLoad = stateRef.current.latestOnLoad;
			const controller = new AbortController();
			(async () => {
				const task = context.overlay.startTask();
				try {
					await Promise.resolve();
					await latestOnLoad({
						context,
						signal: controller.signal
					});
				} catch (err) {
					if (!controller.signal.aborted) {
						task.setError(err);
						console.error("Reactodia: failed to load a workspace", err);
					}
				} finally {
					task.end();
				}
				if (!controller.signal.aborted) context.model.history.reset();
			})();
			return () => {
				controller.abort();
				context.model.discardLayout();
			};
		}
	}, [context, ...deps]);
	return stateRef.current.loadedWorkspace;
}
/**
* Default {@link RenameLinkProvider} implementation for the {@link Workspace workspace}.
*
* Unless overridden, it allows to rename {@link AnnotationLink} graph links
* and stores the changed label in the {@link Link.linkState link template state}.
*
* @see {@link WorkspaceProps.renameLinkProvider}
* @see {@link RenameLinkToLinkStateProvider}
*/
var DefaultRenameLinkProvider = class extends RenameLinkToLinkStateProvider {
	canRename(link) {
		return link instanceof AnnotationLink;
	}
};
//#endregion
//#region src/workspace/workspaceWrapper.tsx
/**
* Top-level component which establishes workspace context, which stores
* graph data and provides means to display and interact with the diagram.
*
* For more control over workspace lifecycle {@link WorkspaceProvider}
* with {@link createWorkspace} can be used instead.
*
* @category Components
*/
var Workspace = class extends React.Component {
	_workspace;
	/** @hidden */
	static contextType = TranslationContext;
	/** @hidden */
	constructor(props, context) {
		super(props, context);
		const { history, dialogSettingsProvider, metadataProvider, validationProvider, renameLinkProvider, typeStyleResolver, selectLabelLanguage, translations = [], useDefaultTranslation = true, defaultLanguage, defaultLayout, onWorkspaceEvent } = this.props;
		let bundles = translations;
		if (useDefaultTranslation) bundles = [...translations, DefaultTranslationBundle];
		this._workspace = createWorkspace({
			translation: this.context ?? new DefaultTranslation({
				bundles,
				selectLabel: selectLabelLanguage
			}),
			history,
			dialogSettingsProvider,
			metadataProvider,
			validationProvider,
			renameLinkProvider,
			typeStyleResolver,
			defaultLanguage,
			defaultLayout,
			onWorkspaceEvent
		});
	}
	/**
	* Returns top-level workspace context.
	*/
	getContext() {
		return this._workspace;
	}
	/** @hidden */
	render() {
		const { children } = this.props;
		return /* @__PURE__ */ jsx(WorkspaceProvider, {
			workspace: this._workspace,
			children
		});
	}
};
//#endregion
//#region src/workspace.ts
/*!
* Reactodia Workspace library https://reactodia.github.io/
*
* Copyright (C) 2026 Alexey Morozov (https://github.com/AlexeyMz)
* Copyright (C) 2019 metaphacts GmbH.
* License: LGPL-2.1 or later.
*/
//#endregion
export { AnnotationElement, AnnotationLink, AnnotationSupport, AnnotationTopic, AuthoringState, BaseMetadataProvider, BasicLink, BuiltinDialogType, Canvas, CanvasPlaceAt, ClassTree, ClassicEntity, ClassicTemplate, ClassicToolbar, ClassicWorkspace, Command, CommandBusTopic, CompositeDataProvider, ConnectionsMenu, ConnectionsMenuTopic, DBPediaSettings, Debouncer, DecoratedDataProvider, DefaultDataLocaleProvider, DefaultDialogSettingsProvider, DefaultLink, DefaultLinkRouter, DefaultLinkTemplate, DefaultPropertyEditor, DefaultRenameLinkProvider, DefaultTranslation, DefaultTranslationBundle, DefaultWorkspace, DiagramContextV1, DraggableHandle, DropOnCanvas, DropdownMenu, DropdownMenuItem, EditorController, Element, ElementDecoration, ElementType, EmptyDataProvider, EntityEditor, EntityElement, EntityGroup, EventObserver, EventSource, FocusGroup, GroupPaginator, Halo, HaloLink, HtmlPaperLayer, HtmlSpinner, InMemoryHistory, IndexQuadBy, IndexedDbCachedProvider, InstancesSearch, InstancesSearchTopic, KeyedObserver, Link, LinkAction, LinkActionDelete, LinkActionEdit, LinkActionMoveEndpoint, LinkActionRename, LinkActionSpinner, LinkLabel, LinkMarkerArrowhead, LinkMarkerCircle, LinkMarkerDiamond, LinkPath, LinkType, LinkTypesToolbox, LinkVertex, LinkVertices, ListElementView, Navigator, NoteAnnotation, NoteEntity, NoteLink, NoteLinkTemplate, NoteTemplate, OverlayController, OwlRdfsSettings, OwlStatsSettings, PlaceholderDataProperty, PlaceholderEntityType, PlaceholderRelationType, ProgressBar, PropertyType, rdfModel_exports as Rdf, RdfDataProvider, RdfSettings, Rect, RelationEditor, RelationGroup, RelationLink, RelationTypeSelector, RenameLinkToLinkStateProvider, RenderingLayer, RestoreGeometry, RoundEntity, RoundTemplate, SearchResults, SearchSectionElementTypes, SearchSectionEntities, SearchSectionLinkTypes, Selection, SelectionAction, SelectionActionAddToFilter, SelectionActionAnchor, SelectionActionAnnotate, SelectionActionConnections, SelectionActionEstablishLink, SelectionActionExpand, SelectionActionGroup, SelectionActionLayout, SelectionActionRemove, SelectionActionSpinner, SelectionActionZoomToFit, SparqlDataProvider, Spinner, StandardEntity, StandardEntityGroup, StandardLinkTemplate, StandardRelation, StandardTemplate, SvgPaperLayer, TemplateProperties, TemplateState, TemporaryState, Toolbar, ToolbarAction, ToolbarActionClearAll, ToolbarActionExport, ToolbarActionLayout, ToolbarActionOpen, ToolbarActionRedo, ToolbarActionSave, ToolbarActionUndo, ToolbarLanguageSelector, TranslatedText, TranslationProvider, UnifiedSearch, UnifiedSearchTopic, ValidationState, Vector, ViewportDock, VisualAuthoring, VisualAuthoringTopic, VoidElement, WikidataSettings, WithFetchStatus, Workspace, WorkspaceContext, WorkspaceEventKey, WorkspaceLayoutColumn, WorkspaceLayoutItem, WorkspaceLayoutResizeContext, WorkspaceLayoutRow, WorkspaceProvider, WorkspaceRoot, ZoomControl, animateInterval, applyLayout, boundsOf, calculateAveragePosition, calculateLayout, changeEntityData, changeLinkTypeVisibility, changeRelationData, computePolyline, computePolylineLength, createWorkspace, defaultGetDroppedOnCanvasItems, defineLayoutWorker, delay, delayProviderDecorator, equalElements, equalLinks, equalProperties, equalSubtypeEdges, findElementAtPoint, findNearestSegmentIndex, getContentFittingBox, getPointAlongPolyline, groupEntities, hashLink, hashSubtypeEdge, highlightSubstring, indexedDataset, isEncodedBlank, isPolylineEqual, iterateEntitiesOf, iterateRelationsOf, mapAbortedToNull, moveComparator, owl, paneTopLeft, pathFromPolyline, placeElementsAroundTarget, rdf, rdfs, requestElementData, restoreCapturedLinkGeometry, restoreLinksBetweenElements, restoreViewport, schema, setElementExpanded, setElementState, setEntityElementData, setEntityGroupItems, setLinkState, setRelationGroupItems, setRelationLinkData, shallowArrayEqual, startDragElements, subscribeElementTypes, subscribeLinkTypes, subscribePropertyTypes, totalPaneSize, translateToPositiveQuadrant, ungroupAllEntities, ungroupSomeEntities, uniformGrid, useAsync, useAuthoredEntity, useCanvas, useCanvasHotkey, useColorScheme, useEventStore, useFocusGroup, useFocusGroupItem, useFrameDebouncedStore, useKeyedSyncStore, useLayerDebouncedStore, useLinkActionContext, useLoadedWorkspace, useObservedProperty, useProvidedEntities, useResolvedAssetUrl, useSearchInputStore, useSyncStore, useSyncStoreWithComparator, useTranslation, useUnifiedSearchSection, useWorker, useWorkspace, xsd };

//# sourceMappingURL=workspace.js.map